#!/usr/bin/env python3
# -*- coding: utf-8 -*-
r"""second_family_erosion_probe --
PRIME.L2.SECOND_FAMILY_EROSION.01 (round 353): THE SECOND
CONSTRUCTION FAMILY + THE PREREGISTERED EROSION KILL -- the named
successor round after r351.

CONTEXT (binding, from the sealed r351 record SPEC_SHA 67102e4c,
GROWTH_LAW_CERTIFIED + FLOOR_ERODES(10^3.7) + INZONE_POOL_EXHAUSTED
+ EXT5_CLEAN(6)): m q_max <= 14.93 log m holds on all 89 rows of
every cohort (FAB = m q_max/log m is the stable law coordinate; the
F_A ceiling keeps growing, the FAB ceiling STANDS); m_0* = 10^18.9
uniform.  THREE named holes between the cover and a cofinal
theorem: (1) the in-zone fresh pool of the frame-A construction is
EXHAUSTED (the r343 selection rule has no candidates left; the B5
queue had exactly 3 members) -- all 89 rows come from ONE
construction family; (2) FLOOR_ERODES(10^3.7): the reserve floor of
the spike coverage erodes (e_RSV -0.649, rc_fam -0.600), the census
extrapolation crosses RSV = 1 at log10 m ~ 3.7 (m ~ 5000), and the
floor rows are exactly the largest-FAB deepest-pk spikes; (3) the
FAB source formula is missing (K2 Klein-gap holds at FAB grel <=
11.87 with Spearman -0.623 but grel has no measured lower bound;
K3/K4 vacuous or broken).

THE CONTRACT (this round): is there a SECOND admissible
construction family -- different window build, SAME canonical
source -- that delivers fresh, deeper teeth, and do the FAB law +
the spike coverage survive there, in particular the preregistered
erosion kill towards m ~ 5000?

THE SECOND FAMILY (sealed BEFORE any evaluation -- FRAME-B, THE
HALF-RESOLUTION TENT FRAME): the frame-A construction fixes the
window mesh at D = 0.5 gap/NU with NU = NU_MAIN = 4 across every
round r238..r351 -- NU is the ONE construction degree of freedom
every probe has held fixed.  Frame-B sets NU_B = 2 (mesh D_B =
gap/4 instead of gap/8; M_B = ceil(alpha/D_B) + 1 rounded even,
h_B = M_B/2 -- approximately HALF the frame-A depth at the same
anchor), everything else VERBATIM: the canonical von-Mangoldt tent
assembly core.atom_lags_at on the sealed source comb U_ALL/MU_ALL
(quellrein -- NO smoothing, no kernel change; the CLXVI lesson:
MT-cosine/Fejer kernels kill the wall; the SMOOTH border companion
PB.smooth_comb stays the construction's own border object exactly
as in frame A), the same archimedean lags, the same fold/chain
machinery.  STRUCTURAL INDEPENDENCE FROM THE FRAME-A SELECTION
PATH: h_A ~ 2 h_B, so the frame-B admissible pool at h_B <= 4000
contains anchors with frame-A depth up to ~8000 -- anchors that NO
frame-A cap (900/1300/1650/2600/3400) ever admitted or could
admit: the reachable set itself is new, not just the choice.
ADMISSION stays the house criterion: POSITIVE_PREFIX (nf is None)
+ chain-complete (len(rows) == N_w) at build level with queue
replacement; the post-eval fold-mult <= 2 + non-degenerate filter
is applied to the law/floor statistics (census-disclosed, no
replacement -- budget honesty; deviation from the r329 wording
said out loud).  The known frame-A boundary (r329: 4
POSITIVE_PREFIX failures at nf 2125..2468) makes the frame-B
admission a genuinely open test.

THE SEALED FRAME-B SELECTION RULE: USED = the r351 extended used
ledger (used_kz_set(frame_a_zones, LM.ext_rule, 35) UNION EXT3
UNION EXT4 UNION the sealed r351 EXT5 list == 104 gated); POOL_B =
frameb_pool(NU_B, H_MIN = 128, H_B_CAP = 4000) under z^2 <=
Z2_CAP = 400000 (source-comb completeness, PC343 verbatim) + the
N_ATOM_MIN floor; FRESH_B = POOL_B minus USED (== 24 gated);
STRATUM BZ (in-zone) = fresh kz with z inside CORE_Z = [16, 317],
sorted by (grel, kz) ascending, first K_BZ = 2 (the in-zone
frame-B queue has EXACTLY 2 members -- exhaustion disclosed up
front); STRATUM BD (depth) = the remaining fresh sorted by (h_B,
kz) DESCENDING, first K_BD = 6.  Build budget: at most
MAX_BUILDS_B = 12 wpack_b calls (queue replacement stops there,
census-disclosed).  HONEST DEPTH DISCLOSURE (sealed): the deepest
fresh frame-B anchor is h_B = 3972 (kz133) -- expected m ~ 800,
log10 m ~ 2.9 vs the r351 crossing 3.7: the round EXTENDS THE
LEVER of the erosion fit and tests the law/floor on a second
family, it does NOT reach m ~ 5000; said out loud, no inflation.

THE FOUR SEALED LEGS ON THE NEW FAMILY:
  LEG A -- THE FAMILY TABLE: per admitted row (stratum, kz, z,
      N_w, m, grel, gap class, F_ins, class, FAB, pk, D) + the
      admission census (queue failures named).  F_ins = the r321
      insertion coordinate CCP.world_coord vs the sealed 65-rung
      ladder columns (the r329/r344/r349/r351 convention for
      beyond-ladder depths, disclosed).
  LEG B -- THE PREREGISTERED EROSION KILL (sealed BEFORE the
      record): (i) the bars RSV_KILL = 1.00 (the coverage itself)
      and RSV_BAR = 1.05 (the r351 floor bar) tested pointwise on
      every admitted frame-B SPIKE row (RSV = GSQ F_ins^2/rho_2,
      GSQ = the banked r321 constant 1.3056); (ii) the erosion
      fit on the COMBINED family ladder (the 28 r351 family rows
      + the frame-B spike rows): e_RSV_comb = dyadic halves-slope
      (fit-free) + rc_comb = Spearman(F, RSV) vs the r351 priors
      -0.649/-0.600 -- does the erosion continue, saturate, or
      accelerate; (iii) THE SEALED KILL LETTER (total, exactly
      one fires): FLOOR_CENSUS_B iff no frame-B spike rows exist;
      FLOOR_KILLS(kz) iff any frame-B spike row has RSV <
      RSV_KILL (the sliding coverage is FINITE -- the three-arm
      cover is bounded to the measured range and the cofinal
      reading needs a fourth mechanism, typed honestly);
      FLOOR_SATURATES iff min RSV(frame-B spikes) >= RSV_BAR AND
      e_RSV_comb >= -E_FLAT_BAR = -0.15 (the cover candidate
      lives); else FLOOR_STILL_ERODES(log10 m_x) (bar misses in
      [1.00, 1.05) or a still-eroding slope; the extrapolated
      crossing reprinted).
  LEG C -- THE FAB LAW CROSS-FAMILY: C_FAB = max FAB over the 89
      sealed r351 rows (FROZEN by the sealed freeze rule; the
      r351 record pin 14.93 at kz111 is anchor-gated), tested
      pointwise on the frame-B rows -- 0 violations demanded for
      FAB_LAW_HOLDS_CROSS_FAMILY, else FAB_LAW_BREAKS_ON_B(list)
      (exactly one fires).  THE K2 SHARPENING: C_K2X = max FAB
      grel over the 89 sealed rows (frozen; the 83-row r351
      subset pin 11.87 anchor-gated), tested on frame-B (a
      genuinely different grel distribution); the sealed
      GREL_LOWER_BOUND_CANDIDATE flag fires iff K2X holds 0/B AND
      g_min = min grel over the combined family rows > 0 AND the
      implied ceiling C_K2X/g_min <= IMPL_FAC = 4.0 x the
      measured max FAB (non-vacuity, a-priori) -- the first step
      towards the grel lower bound, typed census.  THE COUNT
      TEST: nsc_rel/log m <= 2.0258 AND ngj/log m <= 2.6351 (the
      r329 FROZEN constants) pointwise on the frame-B rows.
  LEG D -- THE TWO-FAMILY SYNTHESIS: the core columns (FAB, RSV,
      D, pk, nscl, ngl, m M_2) per family side by side; the m_0*
      adjudication (the class-free polylog route rho_2 <= C_FAB'
      C_M2ENV'/log m recomposed over BOTH families -- does the
      second family move the ceilings/the uniform m_0* from the
      r351 pins 14.93/26.01/10^18.9); the cofinal typing of the
      five r351 holes (closed / moved / confirmed, named).
  LEG E -- WORLDS + MUST-FAILS: the frame-B build on w9 + the
      rational twin w13 (protocol identical to the r351 world
      census: F_ins/class/FAB/RSV, census, NO letter; both are
      below the H_MIN pool floor in frame B -- said honestly) +
      ONE dead control IN THE NEW BUILD: frame-B SCRAMBLE at the
      admitted BZ anchor kz80 (positions redrawn uniform on
      [0, 2 alpha] with the r-convention seed 1, masses kept) --
      the new construction must break there as usual (admission
      census + FAB column, census).

LEG 0 -- ANCHOR REGRESSION (bit-near; the COMPLETE r351 record
surface through the same code path): the r314 identity wards
(ladder/EXT3/EXT4/EXT5 bars verbatim + the a-priori frame-B bars);
r306 C_2 = 1.069 first-5 freeze 0/57; r316 rho anchors + C_small +
n = 65; the dictionary-chain identity; the r321 F_A top-3; THE
r349 FAMILY RECORD (17 rows, RSV 3.19/7.03/12.06, rc -0.331, D
1.79/2.13/5.03, D pk B^2 0.409, shares); THE r349 EXT4 RECORD (six
SPIKE, F_ins (1.58, 2.03, 3.11, 4.22, 5.53, 6.68), holes kz111
1.11/kz75 1.07); THE r329 COUNTING RECORD (C_NSC 2.0258 0/39, C_NG
2.6351 0/39, EXT3 reserves 1.6/1.8); THE r324 DIRECT RECORD (C_INF
1.7481, violators {53, 61, 67, 76, 83} EXACT, e_G +0.158); THE
r351 GROWTH RECORD (C_FAB = 14.93 at kz111 over the 89 rows, EXT5
= the sealed r351 6-anchor list (79, 81, 65, 103, 106, 135)
ADOPTED AS-IS (the r349 adoption convention), 6/6 POSITIVE_PREFIX,
N_w 1771..2812, FAB max 9.71 at kz135, kz79 F_ins 6.69, rc_small
+0.243); THE r351 FLOOR RECORD (28 family rows, min RSV 1.07 at
kz75, e_RSV -0.649, rc_fam -0.600, EXT5 spike RSV inside [2.22,
3.38], C_M2ENV 26.01, the law m_0* 10^18.9, K2 83-row constant
11.87, Spearman(grel, FAB | family) -0.623, EXT4/EXT5 count
reserves 1.59/2.77, SCRAMBLE FAB 2.09).

LEG E MUST-FAILS (>= 4 mutants + 2 scope mutants):
(e1) FAMILY RULE AFTER SIGHT OF THE FAB COLUMN (protocol):
  mutant_nu_posthoc picks the aspect NU that minimizes the seen
  violation mass (consumes rho) -- the BOUND_FORBIDDEN scope audit
  must FLAG it AND on the sealed toy (rho (5.0, 1.0) over NU
  candidates (2, 8)) it returns 8 != the sealed NU_B = 2 --
  protocol-CAUGHT twice.
(e2) ADMISSION AFTER SIGHT (protocol): mutant_admit_posthoc picks
  the admitted windows that minimize the seen violation mass
  (consumes rho) -- AST-FLAGGED AND on the sealed toy it returns
  (2, 3) != the sealed queue-order rule's (1, 2) --
  protocol-CAUGHT twice.
(e3) KILL BAR READ FROM THE TARGET (protocol):
  mutant_killbar_posthoc sets the kill bar at the seen minimum
  reserve (consumes rho) -- AST-FLAGGED AND on the sealed toy it
  returns 0.5 != the sealed RSV_KILL 1.00 -- protocol-CAUGHT
  twice.
(e4) IDENTITY WARD WITH THE WRONG LOG POWER: mutant_fab_wrong_log
  claims FAB = m pk/lg^2 -- on the sealed Fractions toy (m = 3,
  pk = 1/2, pseudo-log lg = 2) it returns 3/8 while the exact FAB
  is 3/4 -- break factor == lg == 2 EXACT.
(m5a/m5b) WORLD-BLINDNESS BREAK: a builder consuming the withheld
  terminal drive key and a builder consuming the branch label are
  both FLAGGED by the AST scope audit.

SEALED VERDICTS (main letter: exactly one fires, total order;
letters/flags appended with '+', combinations allowed):
   TARGET_LEAK            iff any purity/scope/literal audit hit
       on the module-own builders,
   LAW_STATE_NOT_EXACT    iff an exact ward breaks on a live world
       (the identity/chain/decomposition wards, the Fractions
       pins, the frame-B NU = 4 reproduction ward, the toys),
   NO_ADMISSIBLE_SECOND_FAMILY  iff fewer than FRESH_MIN_B = 2
       frame-B rows are admitted (every sealed candidate fails
       POSITIVE_PREFIX/chain-completeness -- the construction
       boundary itself is the finding, typed honestly),
   SECOND_FAMILY_BUILT    otherwise.
   FAB LETTER (exactly one appended at SECOND_FAMILY_BUILT):
       FAB_LAW_HOLDS_CROSS_FAMILY / FAB_LAW_BREAKS_ON_B(list).
   FLOOR LETTER (exactly one appended): FLOOR_CENSUS_B /
       FLOOR_KILLS(kz) / FLOOR_SATURATES /
       FLOOR_STILL_ERODES(log10 m_x).
   EXTRA FLAGS: +GREL_LOWER_BOUND_CANDIDATE(g_min, implied) /
       +BZ_POOL_EXHAUSTED / +NEW_DEPTH_RECORD(m) /
       +NEW_FAB_RECORD(kz, val) / +SCRAMBLE_B(census).

EXPLORATION ONLY (2026-08-27).  experiments/ level: NO promotion,
NO ledger row, NO marker moved, NO RH CLAIM in either direction.
Coexistence: R352 (rho_r source anatomy, L*) may run in parallel
-- own files only; before the strictly additive rh-sync the
current git state is re-checked.  Two-commit freeze protocol (r329
convention): spec committed pre-freeze, record tables the only
post-freeze edit, committed again.

THE OBJECT (r269/r287/r298/r306/r314/r316/r317/r321/r324/r327/
r329/r339/r343/r344/r346/r349/r351 machinery imported verbatim;
the r351 census/controls/extensions/EXT3/EXT4/EXT5 scaffold is
re-executed through the same code path and anchor-gated against
the r351 records): t_{N-2} = sum_b ct_b; F = 0.20 edge split;
level-2 blocks; the r306 RY3 layer; the r314 SCF layer; the r316
TRB layer; the r317 EFP.local_ratio; the r321 CCP.local_median +
CCP.world_coord + CCP.spearman_rank; the r324 QMO + FAP layers;
the r327 GMC layer; the r329 EFA.admissible_pool + EFA.grel_col +
EFA.used_kz_set + EFA.gap_class; the r343 PC343 domain constants;
the r339 FDD dictionary; the r349 TSL.res_decomp + constants; the
r351 QGL.fab_of + QGL.upper_env_max + QGL.fr_fab_toy + the r351
constant set -- ALL imported verbatim and SPEC_SHA-prefix gated
(QGL 67102e4c, TSL 9b593e63, EFA bbfaf199, PC343 9ffc2705).  NEW
in this round (module-own, source-pure): window_data_b + wpack_b
(the frame-B construction: the frame-A pipeline body with NU as a
parameter -- warded EXACT against BH.wpack at NU = 4),
frameb_pool (the frame-B admissible pool), frameb_select (the
sealed two-strata queue rule), floor_letter_b + verdict_tree_b
(the sealed trees).  FAB, RSV, D, pil, nsc, ngj, hgn and every
census on them are TARGET-SIDE DIAGNOSTICS computed in the gate
section (the r321/r349/r351 qmax-share convention, disclosed) --
the module-own builders consume the source grid, window shape and
SEALED thresholds only.

INDEX FIREWALL (binding, r238-r352 discipline): w = window (kz),
N_w = builder depth; ground truth enters GATES and census tables
only; the cubic target M_3/rho_2 and the q_max RECORD enter GATES
/ anchors / census tables / diagnostics only, NEVER a law or
selection builder (AST-warded); no zero/prime oracles anywhere
(AST firewall; the prime-power anchor grid U_ALL/G_ALL is the
sealed source comb, r238 convention); no fit primitives (fragment
audit; exponents are the imported r272 dyadic halves-slope,
fit-free; Spearman is the imported r321 rank correlation).
B PROVENANCE: B_w = S_{N-2} + 5/7 (imported floor, never fitted).
COFINAL LADDER (pre-sealed, r316/r324/r329/r343/r349/r351
verbatim): frame-A h <= 900, 42 rungs, (N, kz)-sorted; exception
set {kz15, 20, 22, 36, 38, 39, 52}; EXTENSION 900 < h <= 1300
first 15; EXT2 r316 A5 rule; EXT3 = the sealed r329 12-anchor
list; EXT4 = the sealed r343/r345 6-anchor list; EXT5 = the
sealed r351 6-anchor list (ALL adopted as-is); FRAME-B = THIS
round's sealed rule above.

SEALED CONSTANTS (everything not listed here is the r351 constant
set imported verbatim from QGL/TSL/FTS/FCC/EFA/PC343): NU_B 2;
H_B_CAP 4000; Z2_CAP = PC343 400000; K_BZ 2; K_BD 6; CORE_Z (16,
317); USED_EXPECT_B 104; FRESH_EXPECT_B 24; BZQ_EXPECT (69, 80);
BDQ_HEAD_EXPECT (133, 129, 124, 117, 107, 101); FRESH_MIN_B 2;
MAX_BUILDS_B 12; TB_WARD_BAR_B 1e-3 (a-priori: N_w up to 3972 vs
the r351 EXT5 bar 1e-4 at 2812 -- one decade slack); REC3_BAR_B
1e-11 (a-priori decade over the EXT5 bar); FRB_WARD_BAR 1e-12 (the
NU = 4 reproduction ward); RSV_KILL 1.00; RSV_BAR = QGL.RHO_BAR2
1.05; E_FLAT_BAR 0.15 (QGL verbatim); IMPL_FAC 4.0 (QGL verbatim);
SCR_SEED_B 1 (r-convention); EXT5_KZ_B (79, 81, 65) / EXT5_KZ_A
(103, 135, 106) with N_w range (1771, 2812); R351 record anchors
(tol): C_FAB 14.93 at kz111 tol 0.02; EXT5 FAB max 9.71 at kz135
tol 0.02; kz79 F_ins 6.69 tol 0.01; FAM_N 28 EXACT; min RSV 1.07
at kz75 tol 0.01; e_RSV -0.649 tol 0.005; rc_fam -0.600 tol
0.005; EXT5 spike RSV inside [2.22 - 0.02, 3.38 + 0.02]; C_M2ENV
26.01 tol 0.02; m_0*(law) 18.9 tol 0.1; C_K2 (83-row subset)
11.87 tol 0.02; rc_gap -0.623 tol 0.005; count reserves (1.59,
2.77) tol 0.05; rc_small +0.243 tol 0.005; SCRAMBLE FAB 2.09 tol
0.02; R353_TABLE_LITERALS = the sealed r314..r351 set (QGL
verbatim) UNION the r351 record set {14.93, 9.71, 6.69, -0.649,
26.01, 18.9, 11.87, -0.623} (collision-prone small values 2.04,
2.09, 2.22, 3.38, 1.59, 2.77, 0.243, -0.6, 1.07 curated OUT,
r337..r351 convention, disclosed); runtime <= 1800 s; smoke = w9
+ controls + toys + scope/purity audits + the exact w9 wards +
the Fractions pins + the frame-B construction ward (w9 at NU = 4
+ NU_B) + the SELECTION ENUMERATION (pool counts + queues, no
deep wpack) + e1-e4 mutants; ladder, extensions, EXT3, EXT4,
EXT5, frame-B evaluation, anchors, census, law tests, floor,
composition and adjudication skipped.
DISCLOSED PRE-SPEC INPUT (no scratch run of THIS probe): every
anchor band is an r306..r351 RECORD number adopted as-is; ONE
SCOPING PASS (sizing only, r329/r343/r351 precedent) enumerated
the frame-B selection surface and timed the budget: NU candidates
{2, 6, 8} pooled under z^2 <= 400000 (NU = 2: 114 admissible / 24
fresh, deepest fresh h_B 3972/3838/3617/3266 at grel 0.33-0.34,
in-zone kz69 grel 0.187 h_B 2846 + kz80 grel 0.408 h_B 1792; NU =
6: 26 fresh, in-zone 3, mostly MED-gap; NU = 8: 26 fresh, in-zone
2, deepest 5332 but grel ~ 1.0-1.2 and 2x the build cost); NU_B =
2 SELECTED on those counts (richest small-gap fresh pool at
affordable depth + the reachable set is structurally beyond every
frame-A cap); the NU = 4 reproduction ward measured EXACT (w9: N
184 == 184, nf None == None, alh dev 0.0); two frame-B wpacks
timed (kz133 N 3972 nf None chain-complete 47.4 s, kz69 N 2846 nf
None chain-complete 20.8 s -- 8 anchors + replacements feasible);
NO bound value, rho, F_A, FAB, reserve or count column of ANY
window was computed by the scoping pass (the two nf values are
admission-side data, disclosed exactly as the r351 scoping did).
GENUINELY OPEN: every frame-B law/floor/count column, the
admission census beyond the two timed builds, the erosion
adjudication, the K2X constant on the 89 rows (the r351 pin 11.87
is the 83-row subset -- whether an EXT5 row lifts it was never
measured), the grel candidate, and every composed number; the
sealed toys are computed BY HAND (selection toy: fresh ((10, 1),
(20, 2), (30, 3), (40, 4)), z (100, 200, 900, 50), grel (0.5,
0.6, 0.7, 0.4), zone [50, 500], K (1, 2) -> BZ queue (4, 1, 2)
[z 900 out; the full sorted queue is returned, K only sets the
taken set], BD queue (3, 2, 1) EXACT; e1 pin 8 vs 2; e2 pin (2, 3)
vs (1, 2); e3 pin 0.5 vs 1.00; e4 pin 3/8 vs 3/4 break 2; the
four verdict branches and the four floor letters EXACT); RSV_KILL,
H_B_CAP, K_BZ, K_BD, MAX_BUILDS_B, TB_WARD_BAR_B, REC3_BAR_B and
FRESH_MIN_B are coarse a-priori choices sized BEFORE any
evaluation; the letters are symmetric and total by CONTRACT.

SEALED VERDICT FORM (frozen BEFORE evaluation, joined with '+'):
  R353_ANCHORS(identity wards, r306 C_2, r316, dictionary, r321
    F_A top-3, THE r349 FAMILY + EXT4 RECORDS, THE r324 DIRECT
    RECORD, THE r329 COUNTING RECORD, THE r351 GROWTH + FLOOR
    RECORDS)
+ SEAL(purity + toys + the exact live wards: NORM identity, r324
    identity, FAB identity, decomposition, dominance chain,
    interpolation, pileup chain, group chain, Fractions pins, the
    frame-B NU = 4 reproduction ward)
+ FAMILY_B(the sealed selection + admission census + the family
    table)
+ FABLAW(the frozen C_FAB test on frame-B + K2X + the count test
    + the grel candidate)
+ EROSION(the RSV_KILL/RSV_BAR bars + the combined erosion fit +
    the floor letter)
+ [exactly one of] SECOND_FAMILY_BUILT /
    NO_ADMISSIBLE_SECOND_FAMILY / LAW_STATE_NOT_EXACT /
    TARGET_LEAK  [+ letters + flags]
+ SYNTHESIS(the two-family table + the m_0* adjudication + the
    cofinal typing of the five r351 holes)
+ MUSTFAIL_LEDGER(e1-e4 + scopes).
Honesty before beauty: the identity, the chains, the Fractions
toys, the NU = 4 reproduction and the purity audits are EXACT
(Fractions/AST-decided); every constant, FAB value, count column,
reserve and violation count is MEASURED on the finite 89-row r351
surface + the admitted frame-B rows only; frame-B is a SECOND
CONSTRUCTION FAMILY on the SAME source comb -- it buys
independence of the window build, NOT a second arithmetic world
(the r329 honesty, restated); the deepest reachable frame-B row
sits at log10 m ~ 2.9, so the erosion kill tests the trend on an
extended lever, not at the extrapolated crossing m ~ 5000; a
certified cross-family law fixes a LAW CANDIDATE with frozen
census constants -- it proves NO cofinal law; r243-r352 stand.

NO RH CLAIM IN EITHER DIRECTION.  NOT evidence for or against RH.
"""

from __future__ import annotations

import argparse
import ast
import hashlib
import math
import os
import sys
import time
from fractions import Fraction as Fr

import numpy as np

HERE = os.path.dirname(os.path.abspath(__file__))
if HERE not in sys.path:
    sys.path.insert(0, HERE)

import bordered_hankel_probe as BH             # noqa: E402 r244
import cancellation_adjudication_probe as CA   # noqa: E402 r263
import coupledtau_probe as CT                  # noqa: E402 r257
import terminal_crossratio_probe as TX         # noqa: E402 r260
import border_resolvent_identity_probe as BR   # noqa: E402 r266
import phase_bulk_bound_probe as PBB           # noqa: E402 r269
import l2_deterministic_cancellation_probe as L2D  # noqa: E402 r287
import window_border_transfer_probe as WBT     # noqa: E402 r298
import renyi3_probe as RY3                     # noqa: E402 r306
import signed_cubic_flux_probe as SCF          # noqa: E402 r314
import two_regime_bound_probe as TRB           # noqa: E402 r316
import exception_families_probe as EFP         # noqa: E402 r317
import continuous_coordinate_probe as CCP      # noqa: E402 r321
import fa_provenance_probe as FAP              # noqa: E402 r324-pre
import qmax_m2_origin_probe as QMO             # noqa: E402 r324
import group_mass_cap_probe as GMC             # noqa: E402 r327
import companion_orbit_packing_probe as COP    # noqa: E402 r333
import ext3_fresh_anchors_probe as EFA         # noqa: E402 r329
import fold_density_dictionary_probe as FDD    # noqa: E402 r339
import fold_two_scale_balance_probe as FTS     # noqa: E402 r344
import fold_cover_canonization_probe as FCC    # noqa: E402 r346
import thirdarm_spike_law_probe as TSL         # noqa: E402 r349
import pair_coupling_probe as PC343            # noqa: E402 r343
import qmax_growth_law_probe as QGL            # noqa: E402 r351
import lstar_margin_scaling_probe as LM        # noqa: E402 r286 READ-ONLY
import port_integrable_kernel_probe as PIK     # noqa: E402 v881
import principal_bessel_probe as PB            # noqa: E402 r243
import v563_paper2_readouts as core            # noqa: E402 READ-ONLY

# ---------------- the r349/r351 constant set, imported verbatim
MAIN_WINDOWS = FTS.MAIN_WINDOWS
CTRL_FLIPS = FTS.CTRL_FLIPS
H_CAP = FTS.H_CAP
CHEAP_EXPECT = FTS.CHEAP_EXPECT
EXC_KZ_EXPECT = FTS.EXC_KZ_EXPECT
TB_WARD_BAR = FTS.TB_WARD_BAR
TB_WARD_BAR_DEEP = FTS.TB_WARD_BAR_DEEP
TB_WARD_BAR_X3 = FTS.TB_WARD_BAR_X3
TB_WARD_BAR_CTRL = FTS.TB_WARD_BAR_CTRL
DEEP_N = FTS.DEEP_N
ID_BAR = FTS.ID_BAR
AC_BAR = FTS.AC_BAR
EXT_H_MAX = FTS.EXT_H_MAX
K_EXT = FTS.K_EXT
EXT_NW_EXPECT = FTS.EXT_NW_EXPECT
EXT2_H_MAX = FTS.EXT2_H_MAX
EXT2_POOL_CAP = FTS.EXT2_POOL_CAP
K_EXT2 = FTS.K_EXT2
EXT3_KZ_B = FTS.EXT3_KZ_B
EXT3_KZ_A = FTS.EXT3_KZ_A
EXT3_NW_MIN = FTS.EXT3_NW_MIN
EXT3_NW_MAX = FTS.EXT3_NW_MAX
ATOM_BAR = FTS.ATOM_BAR
REC3_BAR = FTS.REC3_BAR
REC3_BAR_X3 = FTS.REC3_BAR_X3
TEL_BAR = FTS.TEL_BAR
BND_BAR = FTS.BND_BAR
CHAIN_BAR = FTS.CHAIN_BAR
SA_BAR = FTS.SA_BAR
DICT_BAR = FTS.DICT_BAR
DEG_FLOOR = FTS.DEG_FLOOR
MULT_CAP = FTS.MULT_CAP
N_CAL = FTS.N_CAL
GSQ_R321 = FTS.GSQ_R321
TOY_BAR = FTS.TOY_BAR
EDGE_F = FTS.EDGE_F
CRIT_EXP = FTS.CRIT_EXP
R306_C2 = FTS.R306_C2
R306_C2_TOL = FTS.R306_C2_TOL
N353_REF = FTS.N344_REF
R316_RHO = FTS.R316_RHO
R316_RHO_TOL = FTS.R316_RHO_TOL
R316_CSMALL = FTS.R316_CSMALL
R316_CSMALL_TOL = FTS.R316_CSMALL_TOL
R316_CSMALL_KZ = FTS.R316_CSMALL_KZ
R324_M0_L10 = FTS.R324_M0_L10
R321_FA_TOP = FTS.R321_FA_TOP
R321_FA_TOL = FTS.R321_FA_TOL
M0_R344 = FCC.M0_R344
F0_SPLIT = TSL.F0_SPLIT
EXT4_KZ = TSL.EXT4_KZ
EXT4_NW_MIN = TSL.EXT4_NW_MIN
EXT4_NW_MAX = TSL.EXT4_NW_MAX
TB_WARD_BAR_X4 = TSL.TB_WARD_BAR_X4
REC3_BAR_X4 = TSL.REC3_BAR_X4
DEC_BAR = TSL.DEC_BAR
V1_M0_REF = TSL.V1_M0_REF
V2_M0_REF = TSL.V2_M0_REF
TB_WARD_BAR_X5 = QGL.TB_WARD_BAR_X5
REC3_BAR_X5 = QGL.REC3_BAR_X5
RHO_BAR2 = QGL.RHO_BAR2
E_FLAT_BAR = QGL.E_FLAT_BAR
IMPL_FAC = QGL.IMPL_FAC
FAB_ID_BAR = QGL.FAB_ID_BAR
GRP_CHAIN_BAR = QGL.GRP_CHAIN_BAR
RC_GROW = QGL.RC_GROW
RC_N_MIN = QGL.RC_N_MIN
FROZEN_CNSC = EFA.FROZEN_CNSC
FROZEN_CNG = EFA.FROZEN_CNG
# the r349 record anchors (QGL verbatim)
R349_FAM_N = QGL.R349_FAM_N
R349_RSV_TRIPLE = QGL.R349_RSV_TRIPLE
R349_RSV_TOL = QGL.R349_RSV_TOL
R349_RC_FAM = QGL.R349_RC_FAM
R349_RC_TOL = QGL.R349_RC_TOL
R349_D_FAM_MED = QGL.R349_D_FAM_MED
R349_D_FAM_MAX = QGL.R349_D_FAM_MAX
R349_D_QUIET = QGL.R349_D_QUIET
R349_D_TOL = QGL.R349_D_TOL
R349_DPKB2 = QGL.R349_DPKB2
R349_DPKB2_TOL = QGL.R349_DPKB2_TOL
R349_SHARES = QGL.R349_SHARES
R349_SHARES_TOL = QGL.R349_SHARES_TOL
R349_X4_FINS = QGL.R349_X4_FINS
R349_X4_FINS_TOL = QGL.R349_X4_FINS_TOL
R349_X4_HOLES = QGL.R349_X4_HOLES
R349_X4_HOLE_TOL = QGL.R349_X4_HOLE_TOL
R324_CINF_REF = QGL.R324_CINF_REF
R324_CINF_TOL = QGL.R324_CINF_TOL
R324_VINF_SET = QGL.R324_VINF_SET
R324_EG_REF = QGL.R324_EG_REF
R324_EG_TOL = QGL.R324_EG_TOL
R329_X3_RSVC_MIN = QGL.R329_X3_RSVC_MIN
R329_X3_RSVD_MIN = QGL.R329_X3_RSVD_MIN
R329_X3_RSV_TOL = QGL.R329_X3_RSV_TOL

# ---------------- NEW sealed constants of this round (spec above)
NU_B = 2
NU_A = core.NU_MAIN
H_B_CAP = 4000
Z2_CAP = PC343.Z2_CAP
K_BZ = 2
K_BD = 6
CORE_Z = PC343.CORE_Z
USED_EXPECT_B = 104
FRESH_EXPECT_B = 24
BZQ_EXPECT = (69, 80)
BDQ_HEAD_EXPECT = (133, 129, 124, 117, 107, 101)
FRESH_MIN_B = 2
MAX_BUILDS_B = 12
TB_WARD_BAR_B = 1e-3
REC3_BAR_B = 1e-11
FRB_WARD_BAR = 1e-12
RSV_KILL = 1.00
RSV_BAR = RHO_BAR2
SCR_SEED_B = 1
LM_RANKS_USED = EFA.LM_RANKS_USED
EXT5_KZ_B = (79, 81, 65)
EXT5_KZ_A = (103, 135, 106)
EXT5_NW_MIN = 1771
EXT5_NW_MAX = 2812
# the r351 record anchors
R351_CFAB = 14.93
R351_CFAB_KZ = 111
R351_CFAB_TOL = 0.02
R351_X5_FABMAX = 9.71
R351_X5_FABMAX_KZ = 135
R351_X5_FINS79 = 6.69
R351_X5_FINS_TOL = 0.01
R351_FAM_N = 28
R351_MINRSV = 1.07
R351_MINRSV_KZ = 75
R351_MINRSV_TOL = 0.01
R351_ERSV = -0.649
R351_RCFAM = -0.600
R351_SLOPE_TOL = 0.005
R351_X5_RSV_LO = 2.22
R351_X5_RSV_HI = 3.38
R351_X5_RSV_TOL = 0.02
R351_M2ENV = 26.01
R351_M2ENV_TOL = 0.02
R351_M0LAW = 18.9
R351_M0LAW_TOL = 0.1
R351_CK2_83 = 11.87
R351_CK2_TOL = 0.02
R351_RCGAP = -0.623
R351_CNT_RES = (1.59, 2.77)
R351_CNT_RES_TOL = 0.05
R351_RC_SMALL = 0.243
R351_SCR_FAB = 2.09
R351_SCR_FAB_TOL = 0.02
# import-integrity SHA prefixes (sealed)
QGL_SHA_PREFIX = "67102e4c"
TSL_SHA_PREFIX = "9b593e63"
EFA_SHA_PREFIX = "bbfaf199"
PC343_SHA_PREFIX = "9ffc2705"

R353_TABLE_LITERALS = frozenset(QGL.R351_TABLE_LITERALS | {
    14.93, 9.71, 6.69, -0.649, 26.01, 18.9, 11.87, -0.623})

SPEC_SHA = hashlib.sha256(__doc__.encode("utf-8")).hexdigest()
T0_WALL = time.time()
CHECKS: list = []


def check(name, ok, detail):
    ok = bool(ok)
    CHECKS.append((name, ok, detail))
    print("  [%s] %-42s %s" % ("PASS" if ok else "FAIL", name, detail),
          flush=True)
    return ok


def info(msg):
    print("  [INFO] " + msg, flush=True)


def section(t):
    print("\n" + "-" * 78 + "\n" + t + "\n" + "-" * 78, flush=True)


def firewall_audit():
    src = open(os.path.abspath(__file__), "r", encoding="utf-8").read()
    tree = ast.parse(src)
    forb = {"zeta" + "zero", "n" + "zeros", "prime" + "range",
            "is" + "prime", "gram" + "point"}
    bad = []
    for node in ast.walk(tree):
        nm = node.attr if isinstance(node, ast.Attribute) else (
            node.id if isinstance(node, ast.Name) else None)
        if nm and nm.lower() in forb:
            bad.append("%s@%d" % (nm, node.lineno))
    return (not bad), ("NO zero/prime oracles; every readout "
                       "consumes node positions + signed weights + "
                       "the r244 chain rows; the prime-power anchor "
                       "grid U_ALL/G_ALL is the sealed source comb "
                       "(r238 convention); ground truth enters "
                       "gates and census tables only"
                       if not bad else "; ".join(bad))


def antigate_fragment_audit():
    """AST scan for forbidden method families (identifiers only;
    the fragment table itself is assembled from split strings)."""
    frags = ("cand_" + "unroll", "poly" + "fit", "curve_" + "fit",
             "lst" + "sq", "mini" + "mize", "Line" + "arRegression")
    src = open(os.path.abspath(__file__), "r", encoding="utf-8").read()
    tree = ast.parse(src)
    hits = []
    for node in ast.walk(tree):
        nm = None
        if isinstance(node, ast.Attribute):
            nm = node.attr
        elif isinstance(node, ast.Name):
            nm = node.id
        elif isinstance(node, ast.FunctionDef):
            nm = node.name
        if nm and any(f in nm for f in frags):
            hits.append("%s@%d" % (nm, node.lineno))
    return hits


BOUND_FORBIDDEN = COP.BOUND_FORBIDDEN
PHI3_FORBIDDEN = COP.PHI3_FORBIDDEN
QMAX_FORBIDDEN = COP.QMAX_FORBIDDEN


def scope_audit(funcname, forbidden):
    """walk ONLY the named function's subtree; flag any withheld/
    truth-side identifier or dict key from the sealed set."""
    src = open(os.path.abspath(__file__), "r", encoding="utf-8").read()
    tree = ast.parse(src)
    hits = []
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef) and node.name == funcname:
            for sub in ast.walk(node):
                nm = None
                if isinstance(sub, ast.Attribute):
                    nm = sub.attr
                elif isinstance(sub, ast.Name):
                    nm = sub.id
                elif isinstance(sub, ast.Constant) \
                        and isinstance(sub.value, str):
                    nm = sub.value
                if nm in forbidden:
                    hits.append("%s@%d" % (nm, sub.lineno))
    return hits


def literal_audit(funcname):
    """the RECORD-LITERAL audit: walk ONLY the named function's
    subtree and flag any numeric constant whose 4-decimal rounding
    lies in the sealed r314..r351 record set."""
    src = open(os.path.abspath(__file__), "r", encoding="utf-8").read()
    tree = ast.parse(src)
    hits = []
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef) and node.name == funcname:
            for sub in ast.walk(node):
                if isinstance(sub, ast.Constant) \
                        and isinstance(sub.value, (int, float)) \
                        and not isinstance(sub.value, bool):
                    if round(float(sub.value), 4) \
                            in R353_TABLE_LITERALS:
                        hits.append("%s@%d" % (repr(sub.value),
                                               sub.lineno))
    return hits


# ---------------- module-own builders.  Source hygiene: the
# ---------------- construction/selection builders consume the
# ---------------- source grid (U_ALL/G_ALL/MU_ALL), window shape
# ---------------- and SEALED thresholds only; the withheld
# ---------------- terminal drive key, the branch label, the cubic
# ---------------- target record and the q_max record identifiers
# ---------------- are forbidden (AST identifier scan + literal
# ---------------- scan).  FAB, RSV, D, pil, nsc, ngj, hgn are
# ---------------- TARGET-SIDE DIAGNOSTICS computed in the gate
# ---------------- section (r321/r349/r351 convention, disclosed).
def window_data_b(kz, nu, comb=None):
    """THE FRAME-B WINDOW (the frame-A pipeline body with the
    aspect NU as a parameter, everything else verbatim): mesh
    D = 0.5 G_ALL[kz]/nu, M = ceil(alpha/D) + 1 rounded even,
    h = M/2; canonical von-Mangoldt tent assembly on the sealed
    source comb (or the passed comb); archimedean lags; folded
    measures + Lanczos chain (PIK verbatim).  Consumes the source
    grid + window shape + the passed nu only."""
    alpha = float(core.U_ALL[kz])
    dk = 0.5 * float(core.G_ALL[kz]) / float(nu)
    mz = int(math.ceil(alpha / dk - 1.0e-9)) + 1
    if mz % 2:
        mz += 1
    h = mz // 2
    ka = core.atoms_in(alpha)
    uu = core.U_ALL[:ka].copy()
    mm = core.MU_ALL[:ka].copy()
    if comb is not None:
        uu, mm = comb
    c_at, dd = core.atom_lags_at(alpha, mz, uu, mm)
    c_ar = np.asarray(core.arch_lags(mz, dd), float)
    dgrid = PIK.grid_density(c_ar + c_at)
    ll = 2 * mz - 2
    xs, ws, _ = PIK.folded_measure(dgrid, ll, +1.0)
    ys, vs, _uf = PIK.folded_measure(dgrid, ll, -1.0)
    al, be, m0, steps = PIK.lanczos_chain(xs, ws, h + 4)
    pn = PIK.eval_chain(al, be, m0, ys, min(steps, h + 2))
    return dict(w=kz, n_max=h, xs=xs, ws=ws, ys=ys, vs=vs,
                al=al, be=be, m0=m0, Pn=pn)


def wpack_b(kz, nu, comb=None):
    """the frame-B window pack (the BH.wpack admission core with
    the aspect as a parameter): real comb + the construction's own
    SMOOTH border companion, the r244 bordered chain, the
    half-filling flip nf.  Slim: exactly the fields the r351
    rung pipeline consumes (kz, N, rows, nf, d, dsm) + the
    chain-completeness census."""
    d = window_data_b(kz, nu, comb=comb)
    n_w = d["n_max"]
    alpha = float(core.U_ALL[kz])
    dsm = window_data_b(kz, nu, comb=PB.smooth_comb(alpha))
    rows = BH.bord_chain(d["xs"], d["ws"], d["ys"], d["vs"],
                         dsm["xs"], dsm["ws"], dsm["ys"],
                         dsm["vs"], n_w)
    nf = next((r["n"] for r in rows if r["sg_h"] < 0), None)
    return dict(kz=kz, N=n_w, rows=rows, nf=nf, d=d, dsm=dsm,
                complete=(len(rows) == n_w))


def frameb_pool(nu, h_lo, h_hi, z2cap):
    """the frame-B admissible pool (the frame-A pool body with the
    aspect as a parameter): kz in range(2, NZ_DEEP - 2), h in
    [h_lo, h_hi], z^2 <= z2cap (None = no domain cap, the frame-A
    reproduction mode), >= N_ATOM_MIN atoms; returns sorted
    (h, kz).  Consumes grid columns + sealed caps only."""
    out = []
    for kz in range(2, core.NZ_DEEP - 2):
        alpha = float(core.U_ALL[kz])
        dk = 0.5 * float(core.G_ALL[kz]) / float(nu)
        mz = int(math.ceil(alpha / dk - 1.0e-9)) + 1
        if mz % 2:
            mz += 1
        h = mz // 2
        if h < h_lo or h > h_hi:
            continue
        if z2cap is not None:
            zz = int(core._NN[kz])
            if zz * zz > z2cap:
                continue
        if core.atoms_in(alpha) < core.N_ATOM_MIN:
            continue
        out.append((h, kz))
    return sorted(out)


def frameb_select(fresh, zvals, grels, z_lo, z_hi, k_bz):
    """the sealed frame-B two-strata queue rule: stratum BZ =
    fresh kz with z in [z_lo, z_hi] sorted by (grel, kz)
    ascending; stratum BD = the remaining fresh sorted by (h, kz)
    DESCENDING.  Returns the two CANDIDATE QUEUES (admission by
    the caller: POSITIVE_PREFIX + chain-complete, failing
    candidates replaced by the next in queue).  Consumes window
    shape + grid columns only."""
    fresh_kz = [kz for (_h, kz) in fresh]
    zb = {kz: zvals[i] for i, kz in enumerate(fresh_kz)}
    gb = {kz: grels[i] for i, kz in enumerate(fresh_kz)}
    b_q = sorted((kz for kz in fresh_kz
                  if z_lo <= zb[kz] <= z_hi),
                 key=lambda kz: (gb[kz], kz))
    b_set = set(b_q[:k_bz])
    d_q = [kz for (_h, kz) in sorted(fresh, reverse=True)
           if kz not in b_set]
    return b_q, d_q


def verdict_tree_b(leak, brk, no_family):
    """the sealed main-letter verdict tree (booleans only; total,
    exactly one fires; order sealed): TARGET_LEAK >
    LAW_STATE_NOT_EXACT > NO_ADMISSIBLE_SECOND_FAMILY >
    SECOND_FAMILY_BUILT."""
    if leak:
        return "TARGET_LEAK"
    if brk:
        return "LAW_STATE_NOT_EXACT"
    if no_family:
        return "NO_ADMISSIBLE_SECOND_FAMILY"
    return "SECOND_FAMILY_BUILT"


def floor_letter_b(no_fam, killed, bar_ok, sat):
    """the sealed floor letter (booleans only; total, exactly one
    fires; order sealed): FLOOR_CENSUS_B (no fresh frame-B spike
    rows) > FLOOR_KILLS (a row under the kill bar) >
    FLOOR_SATURATES (bar met everywhere AND flat-or-rising
    combined slope) > FLOOR_STILL_ERODES."""
    if no_fam:
        return "FLOOR_CENSUS_B"
    if killed:
        return "FLOOR_KILLS"
    if bar_ok and sat:
        return "FLOOR_SATURATES"
    return "FLOOR_STILL_ERODES"


def mutant_nu_posthoc(rho, nus):
    """e1 MUST-FAIL MUTANT (protocol): the family aspect NU picked
    to MINIMIZE the seen violation mass (consumes rho) -- the
    BOUND_FORBIDDEN scope audit must FLAG it AND on the sealed toy
    it returns 8 != the sealed NU_B."""
    o = sorted(range(len(nus)), key=lambda i: rho[i])
    return nus[o[0]]


def mutant_admit_posthoc(rho, kz_pool, k):
    """e2 MUST-FAIL MUTANT (protocol): the admitted windows picked
    to MINIMIZE the seen violation mass (consumes rho) -- the
    BOUND_FORBIDDEN scope audit must FLAG it AND on the sealed
    toy it returns (2, 3) != the sealed queue-order rule's
    (1, 2)."""
    o = sorted(range(len(kz_pool)), key=lambda i: rho[i])
    return tuple(sorted(kz_pool[i] for i in o[:k]))


def mutant_killbar_posthoc(rho, gsq_fa2):
    """e3 MUST-FAIL MUTANT (protocol): the kill bar set at the
    SEEN minimum reserve (consumes rho) -- the BOUND_FORBIDDEN
    scope audit must FLAG it AND on the sealed toy it returns
    0.5 != the sealed RSV_KILL."""
    return min(gsq_fa2[i] / max(rho[i], 1e-300)
               for i in range(len(rho)))


def mutant_fab_wrong_log(mm, pk, ell):
    """e4 MUST-FAIL MUTANT: the law coordinate with the WRONG log
    power (m pk/lg^2 instead of /lg) -- on the sealed Fractions
    toy it returns 3/8 while the exact FAB is 3/4 (break factor
    == lg EXACT)."""
    return mm * pk / (ell * ell)


def mutant_gift_bound(rc, P):
    """m5a MUST-FAIL MUTANT: a 'law orientation' consuming the
    withheld ground-truth terminal drive key -- the scope audit
    must FLAG this."""
    s = math.copysign(1.0, rc["t_term"])
    return s * float(np.sum(np.abs(P) ** 3))


def mutant_branch_peek(rc, P):
    """m5b MUST-FAIL MUTANT (world-blindness break simulated): a
    'law constant' consuming the branch label -- the scope audit
    must FLAG this."""
    c = 0.5 if rc["g_branch"] >= 0.0 else 2.0
    return c * float(np.sum(np.abs(P) ** 3))


def main():
    par = argparse.ArgumentParser()
    par.add_argument("--smoke", action="store_true")
    args = par.parse_args()
    smoke = args.smoke
    windows = (9,) if smoke else MAIN_WINDOWS

    print("=" * 78)
    print("second_family_erosion_probe -- "
          "PRIME.L2.SECOND_FAMILY_EROSION.01 (round 353,")
    print("the second construction family + the preregistered "
          "erosion kill)")
    print("SPEC_SHA %s   R351_SHA %s   R349_SHA %s   R329_SHA %s"
          % (SPEC_SHA[:16], QGL.SPEC_SHA[:16], TSL.SPEC_SHA[:16],
             EFA.SPEC_SHA[:16]))
    print("mode: %s" % ("SMOKE (w9 + controls + toys + scope/"
                        "purity audits + w9 wards + Fractions "
                        "pins + frame-B construction ward + "
                        "selection enumeration + e1-e4; ladder, "
                        "extensions, EXT3-5, frame-B evaluation, "
                        "anchors, census, law tests, floor, "
                        "composition and adjudication skipped)"
                        if smoke else "FULL RECORD"))
    print("=" * 78)

    section("S0  FIREWALL + PREDEFINITION")
    okf, det = firewall_audit()
    check("G01-firewall", okf, det)
    sha_ok = (QGL.SPEC_SHA.startswith(QGL_SHA_PREFIX)
              and TSL.SPEC_SHA.startswith(TSL_SHA_PREFIX)
              and EFA.SPEC_SHA.startswith(EFA_SHA_PREFIX)
              and PC343.SPEC_SHA.startswith(PC343_SHA_PREFIX))
    check("G02-predefinition", sha_ok,
          "THE SECOND-FAMILY ROUND (the named r351 residue): "
          "frame-B = the half-resolution tent frame NU_B = %d "
          "(vs NU_A = %d, the ONE fixed construction freedom), "
          "canonical comb verbatim, pool h <= %d, z^2 <= %d, "
          "strata BZ K = %d (in-zone, grel asc) + BD K = %d (h "
          "desc), admission POSITIVE_PREFIX + chain-complete "
          "with queue replacement (budget %d builds); LEG B "
          "kill bars %.2f/%.2f + combined erosion fit vs the "
          "r351 priors; LEG C frozen C_FAB test + K2X + counts "
          "+ grel candidate (IMPL_FAC %.1f); verdict tree "
          "SECOND_FAMILY_BUILT / NO_ADMISSIBLE_SECOND_FAMILY / "
          "LAW_STATE_NOT_EXACT / TARGET_LEAK + FAB/floor "
          "letters sealed BEFORE evaluation; import integrity "
          "QGL %s / TSL %s / EFA %s / PC343 %s"
          % (NU_B, NU_A, H_B_CAP, Z2_CAP, K_BZ, K_BD,
             MAX_BUILDS_B, RSV_KILL, RSV_BAR, IMPL_FAC,
             QGL.SPEC_SHA[:8], TSL.SPEC_SHA[:8],
             EFA.SPEC_SHA[:8], PC343.SPEC_SHA[:8]))
    frag = antigate_fragment_audit()
    own_builders = ("window_data_b", "wpack_b", "frameb_pool",
                    "frameb_select", "verdict_tree_b",
                    "floor_letter_b")
    sc_own = []
    for fn in own_builders:
        sc_own += scope_audit(fn, BOUND_FORBIDDEN)
        sc_own += scope_audit(fn, PHI3_FORBIDDEN)
        sc_own += scope_audit(fn, QMAX_FORBIDDEN)
    sc_a = scope_audit("mutant_gift_bound", BOUND_FORBIDDEN)
    sc_b = scope_audit("mutant_branch_peek", BOUND_FORBIDDEN)
    check("G03-scope-audits", (not frag) and (not sc_own)
          and len(sc_a) >= 1 and len(sc_b) >= 1,
          "fragment audit clean (%d hits); the module-own "
          "construction/selection builders clean vs "
          "BOUND_FORBIDDEN + PHI3_FORBIDDEN + QMAX_FORBIDDEN "
          "(%d hits) -- the family side consumes the source grid "
          "+ window shape + sealed thresholds ONLY; m5a "
          "gift-bound FLAGGED (%s); m5b branch-peek FLAGGED (%s)"
          % (len(frag), len(sc_own),
             sc_a[0] if sc_a else "MISS",
             sc_b[0] if sc_b else "MISS"))

    # ---------------- S1: census + controls (r351 verbatim) +
    # ---------------- the adopted EXT3/EXT4/EXT5 + FRAME-B
    section("S1  CENSUS + CONTROLS + EXTENSIONS + EXT3/4/5 "
            "(ADOPTED) + THE SEALED FRAME-B FAMILY")
    packs = {("w%d" % kz): BH.wpack(kz) for kz in windows}
    rr9 = core.build_window(9)
    N_E = int(math.floor(math.exp(2.0 * rr9["alpha"]))) + 1
    lamE = PIK.lambda_eps(N_E)
    nn_idx = np.nonzero(np.abs(lamE) > 1e-12)[0]
    ug9, uw9 = PB.smooth_comb(rr9["alpha"])
    ctrl_defs = (("EPST", dict(comb=(
        np.log(nn_idx.astype(float)),
        2.0 * lamE[nn_idx] / np.sqrt(nn_idx.astype(float))))),
        ("SCR", dict(scramble_seed=1)),
        ("SMOOTH", dict(comb=(ug9, uw9))))
    ctrl = {c: BH.wpack(9, base_kw=kw) for c, kw in ctrl_defs}
    long_names = {"EPST": "EPSTEIN", "SCR": "SCRAMBLE",
                  "SMOOTH": "SMOOTH"}
    okC = all(packs[t]["nf"] is None for t in packs)
    okCf = all(ctrl[c]["nf"] == CTRL_FLIPS[long_names[c]]
               for c in ctrl)
    if smoke:
        ladder = []
        ext = []
        ext2 = []
        ext3 = []
        ext4 = []
        ext5 = []
        okL = True
        core_kzs = []
    else:
        kzs = []
        ekz = []
        ekz2 = []
        for kz in core.frame_a_zones():
            h = PIK.build_rung(kz)["h"]
            if h <= H_CAP:
                kzs.append(kz)
            elif h <= EXT_H_MAX:
                ekz.append(kz)
            elif h <= EXT2_H_MAX:
                ekz2.append((h, kz))
        core_kzs = list(kzs)
        ladder = [BH.wpack(kz) for kz in kzs]
        ladder.sort(key=lambda p: (p["N"], p["kz"]))
        epool = [BH.wpack(kz) for kz in ekz]
        epool.sort(key=lambda p: (p["N"], p["kz"]))
        ext = epool[:K_EXT]
        ekz2.sort()
        pool2 = epool[K_EXT:] + [BH.wpack(kz)
                                 for _h, kz in ekz2[:EXT2_POOL_CAP]]
        pool2.sort(key=lambda p: (p["N"], p["kz"]))
        ext2 = [p for p in pool2 if p["nf"] is None][:K_EXT2]
        okL = (len(ladder) == 42
               and all(p["nf"] is None for p in ladder))
        ext3 = [BH.wpack(kz) for kz in EXT3_KZ_B + EXT3_KZ_A]
        ext3.sort(key=lambda p: (p["N"], p["kz"]))
        ext4 = [BH.wpack(kz) for kz in EXT4_KZ]
        ext4.sort(key=lambda p: (p["N"], p["kz"]))
        ext5 = [BH.wpack(kz) for kz in EXT5_KZ_B + EXT5_KZ_A]
        ext5.sort(key=lambda p: (p["N"], p["kz"]))
    check("G10-census-controls", okC and okCf and okL,
          "MAIN free prefix positive at full depth (%s, N = %s); "
          "control flips re-derived %s; cofinal ladder %d rungs "
          "POSITIVE_PREFIX %s, N in [%s, %s]"
          % (str(sorted(packs)),
             str({t: packs[t]["N"] for t in packs}),
             str({c: ctrl[c]["nf"] for c in ctrl}),
             len(ladder),
             "42/42" if okL and ladder else ("n/a (SMOKE)"
                                             if smoke else "FAIL"),
             ladder[0]["N"] if ladder else "-",
             ladder[-1]["N"] if ladder else "-"))
    if smoke:
        check("G12-extension-census", True, "SMOKE: skipped")
        check("G14-ext345-adoption", True, "SMOKE: skipped")
    else:
        check("G12-extension-census",
              len(ext) == K_EXT
              and ext[0]["N"] == EXT_NW_EXPECT[0]
              and ext[-1]["N"] == EXT_NW_EXPECT[1]
              and all(p["nf"] is None for p in ext)
              and len(ext2) <= K_EXT2
              and all(p["nf"] is None for p in ext2),
              "r286-aligned extension: %d anchors, N_w %d..%d "
              "(expected %d..%d); EXT2 (r316 A5 rule verbatim): "
              "%d POSITIVE_PREFIX anchors, N_w %s..%s"
              % (len(ext), ext[0]["N"] if ext else -1,
                 ext[-1]["N"] if ext else -1, EXT_NW_EXPECT[0],
                 EXT_NW_EXPECT[1], len(ext2),
                 ext2[0]["N"] if ext2 else "-",
                 ext2[-1]["N"] if ext2 else "-"))
        check("G14-ext345-adoption",
              len(ext3) == 12
              and all(p["nf"] is None for p in ext3)
              and min(p["N"] for p in ext3) == EXT3_NW_MIN
              and max(p["N"] for p in ext3) == EXT3_NW_MAX
              and len(ext4) == 6
              and all(p["nf"] is None for p in ext4)
              and min(p["N"] for p in ext4) == EXT4_NW_MIN
              and max(p["N"] for p in ext4) == EXT4_NW_MAX
              and len(ext5) == 6
              and all(p["nf"] is None for p in ext5)
              and min(p["N"] for p in ext5) == EXT5_NW_MIN
              and max(p["N"] for p in ext5) == EXT5_NW_MAX,
              "EXT3 = the sealed r329 12-anchor list, EXT4 = the "
              "sealed r343/r345 6-anchor list, EXT5 = the sealed "
              "r351 6-anchor list %s -- ALL ADOPTED AS-IS (r349/"
              "r351 adoption convention): POSITIVE_PREFIX "
              "%d/12 + %d/6 + %d/6; N_w EXT3 %d..%d / EXT4 "
              "%d..%d / EXT5 %d..%d"
              % (str(EXT5_KZ_B + EXT5_KZ_A),
                 sum(1 for p in ext3 if p["nf"] is None),
                 sum(1 for p in ext4 if p["nf"] is None),
                 sum(1 for p in ext5 if p["nf"] is None),
                 min(p["N"] for p in ext3),
                 max(p["N"] for p in ext3),
                 min(p["N"] for p in ext4),
                 max(p["N"] for p in ext4),
                 min(p["N"] for p in ext5),
                 max(p["N"] for p in ext5)))
    # THE SEALED FRAME-B SELECTION (runs in smoke too -- pool
    # counts + queues only, no deep wpack)
    lm_rows = LM.ext_rule()
    used = set(EFA.used_kz_set(core.frame_a_zones(), lm_rows,
                               LM_RANKS_USED))
    used |= set(PC343.EXT3_KZ_B + PC343.EXT3_KZ_A)
    used |= set(PC343.EXT4_KZ_B + PC343.EXT4_KZ_A)
    used |= set(EXT5_KZ_B + EXT5_KZ_A)
    pool_b = frameb_pool(NU_B, core.H_MIN, H_B_CAP, Z2_CAP)
    fresh_b = [(h, kz) for (h, kz) in pool_b if kz not in used]
    fresh_b_kz = [kz for (_h, kz) in fresh_b]
    grels_b = EFA.grel_col(fresh_b_kz, core.G_ALL)
    zz_b = {kz: int(core._NN[kz]) for kz in fresh_b_kz}
    b_q, d_q = frameb_select(fresh_b,
                             [zz_b[kz] for kz in fresh_b_kz],
                             grels_b, CORE_Z[0], CORE_Z[1], K_BZ)
    gr_by = {kz: g for kz, g in zip(fresh_b_kz, grels_b)}
    h_by = {kz: h for (h, kz) in fresh_b}
    bz_exhausted = len(b_q) <= K_BZ
    check("G16-famb-selection",
          len(used) == USED_EXPECT_B
          and len(fresh_b) == FRESH_EXPECT_B
          and tuple(b_q[:K_BZ]) == BZQ_EXPECT
          and tuple(d_q[:K_BD]) == BDQ_HEAD_EXPECT,
          "SEALED FRAME-B SELECTION executed verbatim: used %d "
          "(== %d), fresh %d (== %d, h_B <= %d, z^2 <= %d); "
          "stratum BZ queue %s (grel %s, z %s) + BD queue head "
          "%s (h_B %s) == the scoping-disclosed queues; IN-ZONE "
          "FRAME-B POOL %s"
          % (len(used), USED_EXPECT_B, len(fresh_b),
             FRESH_EXPECT_B, H_B_CAP, Z2_CAP, str(b_q[:K_BZ]),
             str([round(gr_by[k], 3) for k in b_q[:K_BZ]]),
             str([zz_b[k] for k in b_q[:K_BZ]]),
             str(d_q[:K_BD]),
             str([h_by[k] for k in d_q[:K_BD]]),
             "EXHAUSTED at this tranche (%d members)" % len(b_q)
             if bz_exhausted
             else "not exhausted (%d members)" % len(b_q)))
    if smoke:
        check("G17-famb-admission", True,
              "SMOKE: skipped (no deep wpack)")
        famb = []
        famb_strat = {}
    else:
        builds = [0]

        def admit_queue_b(queue, k):
            got = []
            failed = []
            for kz in queue:
                if len(got) >= k or builds[0] >= MAX_BUILDS_B:
                    break
                builds[0] += 1
                pb = wpack_b(kz, NU_B)
                if pb["nf"] is None and pb["complete"]:
                    got.append(pb)
                else:
                    failed.append((kz, pb["nf"],
                                   pb["complete"]))
            return got, failed

        bz_packs, bz_fail = admit_queue_b(b_q, K_BZ)
        bd_packs, bd_fail = admit_queue_b(d_q, K_BD)
        famb_strat = {}
        for pb in bz_packs:
            famb_strat[pb["kz"]] = "BZ"
        for pb in bd_packs:
            famb_strat[pb["kz"]] = "BD"
        famb = bz_packs + bd_packs
        famb.sort(key=lambda p: (p["N"], p["kz"]))
        check("G17-famb-admission",
              len(famb) >= FRESH_MIN_B
              and all(p["complete"] for p in famb),
              "FRAME-B ADMISSION (POSITIVE_PREFIX + "
              "chain-complete, queue replacement, budget %d "
              "builds used %d): admitted %d/%d %s, N_w %s..%s; "
              "queue failures BZ %s / BD %s -- PURE TEST rows "
              "of THIS round, never seen by any earlier probe"
              % (MAX_BUILDS_B, builds[0], len(famb),
                 K_BZ + K_BD,
                 str([(p["kz"], famb_strat[p["kz"]], p["N"])
                      for p in famb]),
                 min((p["N"] for p in famb), default="-"),
                 max((p["N"] for p in famb), default="-"),
                 str(bz_fail), str(bd_fail)))

    pool = ladder if not smoke else [packs["w9"]]
    mains = [packs["w%d" % kz] for kz in windows]

    def rung_rec(p):
        N = p["N"]
        rows = p["rows"]
        r, t, ap, bp = TX.drive_arrays(rows, N)
        g = CA.g_gap(r[:N - 1], t, ap, bp)
        xu, wu = CT.union_arrays(p["d"])
        bx, bw = CT.union_arrays(p["dsm"])
        lo = min(float(np.min(xu)), float(np.min(bx)))
        hi = max(float(np.max(xu)), float(np.max(bx)))
        v2 = BR.eval_scaled(rows, bx, N - 2)
        fac = math.exp(rows[N - 2]["Ls"] - rows[N - 1]["Ls"]) \
            / math.sqrt(abs(rows[N - 1]["eta"]))
        ct = bw * bx * v2 * fac
        v2w = BR.eval_scaled(rows, xu, N - 2)
        cw = wu * xu * v2w * fac
        o = np.argsort(bx, kind="stable")
        return dict(kz=p["kz"], N=N, g_branch=g,
                    t_term=float(t[N - 2]), ct=ct, bx=bx, bw=bw,
                    v2=v2, fac=fac, xu=xu, wu=wu, cw=cw, o=o,
                    lo=lo, hi=hi, p=p, nf=p["nf"])

    recs = [rung_rec(p) for p in pool]
    erecs = [rung_rec(p) for p in ext] if not smoke else []
    e2recs = [rung_rec(p) for p in ext2] if not smoke else []
    x3recs = [rung_rec(p) for p in ext3] if not smoke else []
    x4recs = [rung_rec(p) for p in ext4] if not smoke else []
    x5recs = [rung_rec(p) for p in ext5] if not smoke else []
    fbrecs = [rung_rec(p) for p in famb] if not smoke else []
    mrecs = [rung_rec(p) for p in mains]
    crecs = {c: rung_rec(ctrl[c]) for c in ctrl}
    cheap = [rc for rc in recs if rc["g_branch"] >= 0.0]
    exc = [rc for rc in recs if rc["g_branch"] < 0.0]
    exc_kz = tuple(sorted(rc["kz"] for rc in exc))
    if smoke:
        check("G11-branch-reproduction", recs[0]["g_branch"] >= 0.0,
              "SMOKE: w9 branch %s (g %+.3f); ladder "
              "decomposition skipped"
              % ("CHEAP" if recs[0]["g_branch"] >= 0 else
                 "EXCEPTION", recs[0]["g_branch"]))
    else:
        e_cheap = sum(1 for rc in erecs + e2recs + x3recs + x4recs
                      + x5recs if rc["g_branch"] >= 0.0)
        e_exc = [rc["kz"] for rc in erecs + e2recs + x3recs
                 + x4recs + x5recs if rc["g_branch"] < 0.0]
        fb_exc = [rc["kz"] for rc in fbrecs
                  if rc["g_branch"] < 0.0]
        check("G11-branch-reproduction",
              len(cheap) == CHEAP_EXPECT
              and exc_kz == tuple(sorted(EXC_KZ_EXPECT))
              and all(rc["g_branch"] >= 0 for rc in mrecs),
              "r263 branch rule reproduced EXACTLY: cheap %d/42, "
              "exception set %s == the named 7; mains %s; "
              "EXT+EXT2+EXT3+EXT4+EXT5 census: %d cheap / %d "
              "exception %s; FRAME-B branch census: %d cheap / "
              "%d exception %s (census, no letter)"
              % (len(cheap), str(exc_kz),
                 "; ".join("w%d g %+.3f CHEAP" % (rc["kz"],
                                                  rc["g_branch"])
                           for rc in mrecs),
                 e_cheap, len(e_exc), str(e_exc),
                 len(fbrecs) - len(fb_exc), len(fb_exc),
                 str(fb_exc)))

    # ---------------- S2: decomposition wards + eval
    section("S2  EXACT DECOMPOSITION + TREE-LEDGER WARDS")
    tb_worst = 0.0
    tb_deep = 0.0
    tb_ext = 0.0
    tb_x3 = 0.0
    tb_x4 = 0.0
    tb_x5 = 0.0
    tb_fb = 0.0
    tb_ctrl = 0.0
    for rc in recs + mrecs:
        absum = float(np.sum(np.abs(rc["ct"])))
        dev = abs(float(np.sum(rc["ct"])) - rc["t_term"]) \
            / max(absum, 1e-300)
        if rc["N"] > DEEP_N:
            tb_deep = max(tb_deep, dev)
        else:
            tb_worst = max(tb_worst, dev)
    for rc in erecs + e2recs:
        dev = abs(float(np.sum(rc["ct"])) - rc["t_term"]) \
            / max(float(np.sum(np.abs(rc["ct"]))), 1e-300)
        tb_ext = max(tb_ext, dev)
    for rc in x3recs:
        dev = abs(float(np.sum(rc["ct"])) - rc["t_term"]) \
            / max(float(np.sum(np.abs(rc["ct"]))), 1e-300)
        tb_x3 = max(tb_x3, dev)
    for rc in x4recs:
        dev = abs(float(np.sum(rc["ct"])) - rc["t_term"]) \
            / max(float(np.sum(np.abs(rc["ct"]))), 1e-300)
        tb_x4 = max(tb_x4, dev)
    for rc in x5recs:
        dev = abs(float(np.sum(rc["ct"])) - rc["t_term"]) \
            / max(float(np.sum(np.abs(rc["ct"]))), 1e-300)
        tb_x5 = max(tb_x5, dev)
    for rc in fbrecs:
        dev = abs(float(np.sum(rc["ct"])) - rc["t_term"]) \
            / max(float(np.sum(np.abs(rc["ct"]))), 1e-300)
        tb_fb = max(tb_fb, dev)
    for c in crecs:
        rc = crecs[c]
        dev = abs(float(np.sum(rc["ct"])) - rc["t_term"]) \
            / max(float(np.sum(np.abs(rc["ct"]))), 1e-300)
        tb_ctrl = max(tb_ctrl, dev)
    check("G20-contribution-ward", tb_worst <= TB_WARD_BAR
          and tb_deep <= TB_WARD_BAR_DEEP
          and tb_ext <= TB_WARD_BAR_DEEP
          and tb_x3 <= TB_WARD_BAR_X3
          and tb_x4 <= TB_WARD_BAR_X4
          and tb_x5 <= TB_WARD_BAR_X5
          and tb_fb <= TB_WARD_BAR_B
          and tb_ctrl <= TB_WARD_BAR_CTRL,
          "sum_b ct_b == t_{N-2} on %d rungs + %d ext + %d ext2 "
          "+ %d ext3 + %d ext4 + %d ext5 + %d FRAME-B + %d mains "
          "+ 3 controls: worst dev/absmass %.1e main (bar %.0e) "
          "/ %.1e deep / %.1e ext / %.1e ext3 / %.1e ext4 / "
          "%.1e ext5 (bar %.0e) / %.1e FRAME-B (a-priori bar "
          "%.0e) / %.1e controls (bar %.0e)"
          % (len(recs), len(erecs), len(e2recs), len(x3recs),
             len(x4recs), len(x5recs), len(fbrecs), len(mrecs),
             tb_worst, TB_WARD_BAR, tb_deep, tb_ext, tb_x3,
             tb_x4, tb_x5, TB_WARD_BAR_X5, tb_fb, TB_WARD_BAR_B,
             tb_ctrl, TB_WARD_BAR_CTRL))

    def eval_rung(rc):
        o = rc["o"]
        bxs = rc["bx"][o]
        cts = rc["ct"][o]
        ed = PBB.mask_edge(bxs, rc["lo"], rc["hi"], EDGE_F)
        cb = cts[~ed]
        xb = bxs[~ed]
        runs = PBB.runs_split(cb)
        Sr = [float(np.sum(cb[a:b])) for a, b, _s in runs]
        sg = [s for _a, _b, s in runs]
        alt_ok = all(sg[i + 1] == -sg[i]
                     for i in range(len(sg) - 1))
        R = sum(Sr)
        P = L2D.blocks_level2(Sr)
        brk, m, jb = WBT.block_breaks(xb, runs)
        Pb = np.bincount(jb, weights=cb, minlength=m) \
            if m else np.zeros(0)
        Pw = WBT.aggregate_blocks(rc["xu"], rc["cw"], rc["lo"],
                                  rc["hi"], brk, m)
        Pd = Pb - Pw
        cm = RY3.cubic_moments(Pd)
        absm = float(np.sum(np.abs(rc["ct"]))) \
            + float(np.sum(np.abs(rc["cw"])))
        degenerate = (cm["L1"] <= DEG_FLOOR * absm)
        edw = PBB.mask_edge(rc["xu"], rc["lo"], rc["hi"], EDGE_F)
        xw = rc["xu"][~edw]
        vw = -rc["cw"][~edw]
        jw = np.searchsorted(brk, xw) if m else np.zeros(0, int)
        jb2 = np.searchsorted(brk, xb) if m else np.zeros(0, int)
        mism = int(np.sum(jb2 != jb))
        pos_all = np.concatenate([xb, xw])
        val_all = np.concatenate([cb, vw])
        blk_all = np.concatenate([jb, jw]).astype(int)
        src_all = np.concatenate([np.zeros(len(xb)),
                                  np.ones(len(xw))])
        if m and not degenerate:
            gen = SCF.fold_genealogy(pos_all, val_all, blk_all, m)
            sct = SCF.signed_cube_terms(gen["G1"], gen["gblk"], m)
            ft = SCF.flux_telescope(gen["G1"], gen["ptr"], m)
            x_dev = float(np.max(np.abs(sct["x"] - Pd))
                          / max(np.max(np.abs(Pd)), 1e-300))
            sig = sct["sig"]
            cube = sct["cube"]
            A1 = np.bincount(blk_all, weights=np.abs(val_all),
                             minlength=m)
            scale3 = float(np.sum(A1 ** 3))
            sc_j = np.maximum(A1 ** 3, 1e-300)
            C_far_flux = float(np.sum(sig * ft["F_end"]))
            C_bnd = float(np.sum(sig * ft["F_open"]))
            rec3 = abs(C_far_flux + sct["C_pair"] + sct["C_full"]
                       + C_bnd - cube) / max(scale3, 1e-300)
            tel_dev = float(np.max(np.abs(ft["F_end"]
                                          - sct["far"]) / sc_j))
            bnd_dev = float(np.max(np.abs(ft["F_open"]) / sc_j))
            mx_mult = int(np.max(gen["mult"])) if gen["ng"] else 0
            trs = TRB.two_regime_state(sct["x"], sct["Q2"],
                                       sct["Q3"], gen["G1"],
                                       gen["ptr"], ft["F_end"],
                                       ft["F_open"],
                                       ft["edge_abs"], m)
            rho2 = RY3.renyi3_ratio(cm["S3"], m, 2)
            mqs = FAP.m2_qmax_state(sct["x"])
            led327 = GMC.group_mass_ledger(pos_all, val_all,
                                           blk_all, src_all, m)
            pst = QMO.pileup_state(sct["x"], val_all, blk_all)
            hst = GMC.heavy_state(sct["x"], led327)
            dic = FDD.martingale_moment_dictionary(sct["x"])
        else:
            gen = sct = ft = None
            x_dev = 0.0
            cube = 0.0
            rec3 = tel_dev = bnd_dev = 0.0
            C_bnd = 0.0
            mx_mult = 0
            A1 = np.zeros(0)
            trs = dict(nrm=0.0, coll=0.0, dflux=0.0, bnd=0.0,
                       fe=0.0, fcix=0.0, qmax=0.0, cnt3=0.0,
                       phiL1=0.0, phiL2=0.0, phiH1=0.0,
                       phiH2=0.0, L=0.0)
            rho2 = 0.0
            mqs = dict(qm=0.0, m2=0.0, ymx=0.0, maj=0.0)
            led327 = None
            pst = dict(j=0, nsc=0, nsc_rel=0, pil=0.0, a1j=0.0,
                       tail=0.0, scut=0, scales=(), masses=())
            hst = dict(j=0, ngj=0, hga=0.0, hgn=0.0, a1j=0.0,
                       vmaxb=0.0, bshare=0.0)
            dic = dict(d1=0.0, d2=0.0, d3=0.0, ymx=0.0)
        return dict(alt_ok=alt_ok, R=R, P=P, m=m, Pd=Pd, cm=cm,
                    degenerate=degenerate, mism=mism, x_dev=x_dev,
                    cube=cube, rec3=rec3, tel_dev=tel_dev,
                    bnd_dev=bnd_dev, C_bnd=C_bnd,
                    mx_mult=mx_mult, gen=gen, sct=sct, ft=ft,
                    trs=trs, rho2=rho2, A1=A1, mqs=mqs,
                    led327=led327, pst=pst, hst=hst, dic=dic,
                    pos_all=pos_all, val_all=val_all,
                    blk_all=blk_all, brk=brk)

    all_rc = recs + mrecs + erecs + e2recs + x3recs + x4recs \
        + x5recs + fbrecs
    for rc in all_rc:
        rc["ev"] = eval_rung(rc)
    for c in crecs:
        crecs[c]["ev"] = eval_rung(crecs[c])
    pool_all = all_rc + [crecs[c] for c in crecs]

    alt_all = all(rc["ev"]["alt_ok"] for rc in all_rc)
    bid_worst = 0.0
    ac_worst = 0.0
    for rc in pool_all:
        ev = rc["ev"]
        bid_worst = max(bid_worst,
                        abs(sum(ev["P"]) - ev["R"])
                        / max(abs(ev["R"]), 1e-300))
        A = L2D.autocorr_full(ev["P"])
        s_all = A[0] + 2.0 * float(np.sum(A[1:]))
        ac_worst = max(ac_worst,
                       abs(s_all - sum(ev["P"]) ** 2)
                       / max(A[0], 1e-300))
    check("G21-block-and-autocorr-identity",
          alt_all and bid_worst <= ID_BAR and ac_worst <= AC_BAR,
          "runs alternate on every world AND sum P == R exact "
          "(worst rel %.1e, bar %.0e) AND (sum P)^2 == A(0) + 2 "
          "sum A(h) exact (worst %.1e x A(0), bar %.0e) over %d "
          "worlds" % (bid_worst, ID_BAR, ac_worst, AC_BAR,
                      len(pool_all)))

    live = [rc for rc in pool_all if not rc["ev"]["degenerate"]]
    deg_note = [c for c in crecs if crecs[c]["ev"]["degenerate"]]
    x_w = max(rc["ev"]["x_dev"] for rc in live)
    mism_tot = sum(rc["ev"]["mism"] for rc in pool_all)
    led_dev = 0.0
    xb_mult_ok = True
    for rc in live:
        ev = rc["ev"]
        gen = ev["gen"]
        l327 = ev["led327"]
        if gen["ng"] != l327["ng"]:
            led_dev = max(led_dev, 1.0)
            continue
        if gen["ng"]:
            sc = max(float(np.max(np.abs(gen["G1"]))), 1e-300)
            led_dev = max(
                led_dev,
                float(np.max(np.abs(l327["G1"] - gen["G1"]))) / sc,
                float(np.max(np.abs(l327["mult"] - gen["mult"]))),
                float(np.max(np.abs(l327["gblk"] - gen["gblk"]))))
    fb_mult_drop = []
    for rc in x3recs + x4recs + x5recs:
        if rc["ev"]["mx_mult"] > MULT_CAP:
            xb_mult_ok = False
    for rc in fbrecs:
        if rc["ev"]["mx_mult"] > MULT_CAP \
                or rc["ev"]["degenerate"]:
            fb_mult_drop.append(rc["kz"])
    check("G22-genealogy-ledger-identity",
          x_w <= ATOM_BAR and mism_tot == 0 and led_dev <= SA_BAR
          and xb_mult_ok,
          "the r314 fold genealogy covers every block value on %d "
          "live worlds (group-sum dev %.1e, bar %.0e); run "
          "assignment == breakpoint search (%d mismatches); the "
          "r327 GMC ledger segments IDENTICALLY to the genealogy "
          "(worst dev %.1e, bar %.0e); EXT3+EXT4+EXT5 fold "
          "multiplicity <= %d (%s); FRAME-B post-eval filter "
          "(mult <= %d + non-degenerate): %s%s"
          % (len(live), x_w, ATOM_BAR, mism_tot, led_dev, SA_BAR,
             MULT_CAP, "OK" if xb_mult_ok else "BROKEN",
             MULT_CAP,
             "%d/%d pass" % (len(fbrecs) - len(fb_mult_drop),
                             len(fbrecs)) if fbrecs
             else "n/a",
             "; DEGENERATE guard fired (pre-declared): "
             + ", ".join(deg_note) if deg_note else ""))

    # ---------------- S3: Leg 0 anchors (slim set)
    section("S3  LEG 0 -- ANCHOR REGRESSION (slim set: r306/r316 "
            "+ dictionary + r321 F_A; the r349/r324/r329/r351 "
            "records follow in S5)")
    xall_ids = set(id(rc) for rc in x3recs + x4recs + x5recs
                   + fbrecs)
    live_69 = [rc for rc in live if id(rc) not in xall_ids]
    rec3_w = max(rc["ev"]["rec3"] for rc in live_69)
    tel_w = max(rc["ev"]["tel_dev"] for rc in live_69)
    bnd_w = max(rc["ev"]["bnd_dev"] for rc in live_69)
    rec3_x = max((rc["ev"]["rec3"] for rc in x3recs), default=0.0)
    rec3_x4 = max((rc["ev"]["rec3"] for rc in x4recs),
                  default=0.0)
    rec3_x5 = max((rc["ev"]["rec3"] for rc in x5recs),
                  default=0.0)
    rec3_fb = max((rc["ev"]["rec3"] for rc in fbrecs),
                  default=0.0)
    check("G30-r314-identity-wards",
          rec3_w <= REC3_BAR and tel_w <= TEL_BAR
          and bnd_w <= BND_BAR and rec3_x <= REC3_BAR_X3
          and rec3_x4 <= REC3_BAR_X4 and rec3_x5 <= REC3_BAR_X5
          and rec3_fb <= REC3_BAR_B,
          "the r314 identity live: ladder worlds recomposition "
          "%.1e / telescope %.1e / boundary %.1e (bars %.0e); "
          "EXT3 %.1e; EXT4 %.1e; EXT5 %.1e; FRAME-B %.1e "
          "(a-priori bar %.0e); DISCLOSED slim anchor set -- the "
          "full chain is re-warded by the sealed r321..r351 "
          "probes"
          % (rec3_w, tel_w, bnd_w, REC3_BAR, rec3_x, rec3_x4,
             rec3_x5, rec3_fb, REC3_BAR_B))
    if smoke:
        check("G31-r306-bound-live", True, "SMOKE: skipped")
        check("G32-r316-anchors", True, "SMOKE: skipped")
        srt = []
        n353 = 0
    else:
        srt57 = sorted(recs + erecs,
                       key=lambda rc: (rc["N"], rc["kz"]))
        rhoT2 = [rc["ev"]["rho2"] for rc in srt57]
        C2r, _j, _d = RY3.calib_freeze(rhoT2, range(N_CAL))
        viol2 = sum(1 for v in rhoT2 if v > C2r)
        check("G31-r306-bound-live",
              abs(C2r - R306_C2) <= R306_C2_TOL and viol2 == 0,
              "r306 pointwise bound live at A = 2: C_2 %.3f (rec "
              "%.3f tol %.3f, first-%d freeze), violations %d/%d"
              % (C2r, R306_C2, R306_C2_TOL, N_CAL, viol2,
                 len(srt57)))
        srt_all65 = sorted(recs + erecs + e2recs,
                           key=lambda rc: (rc["N"], rc["kz"]))
        srt = [rc for rc in srt_all65
               if rc["ev"]["mx_mult"] <= MULT_CAP]
        n353 = len(srt)
        rho_all = [rc["ev"]["rho2"] for rc in srt]
        rho_kz = {rc["kz"]: rc["ev"]["rho2"] for rc in srt}
        sm_i, ca_i, te_i = TRB.split_midladder(n353)
        C_small = max(rho_all[i] for i in sm_i)
        j_cs = max(sm_i, key=lambda i: rho_all[i])
        check("G32-r316-anchors",
              n353 == N353_REF
              and all(abs(rho_kz.get(kz, -1.0) - R316_RHO[kz])
                      <= R316_RHO_TOL for kz in R316_RHO)
              and abs(C_small - R316_CSMALL) <= R316_CSMALL_TOL
              and srt[j_cs]["kz"] == R316_CSMALL_KZ,
              "r316 anchors reproduced: class ladder n = %d (rec "
              "%d); rho kz53/kz67/kz55/kz83 = %.4f/%.4f/%.4f/"
              "%.4f (rec tol %.3f); C_small %.4f @ kz%d"
              % (n353, N353_REF, rho_kz.get(53, -1.0),
                 rho_kz.get(67, -1.0), rho_kz.get(55, -1.0),
                 rho_kz.get(83, -1.0), R316_RHO_TOL, C_small,
                 srt[j_cs]["kz"]))
    dict2_w = 0.0
    dict3_w = 0.0
    dictq_w = 0.0
    for rc in live:
        ev = rc["ev"]
        dic = ev["dic"]
        mloc = ev["m"]
        dict2_w = max(dict2_w, abs(dic["d2"] - ev["mqs"]["m2"])
                      / max(ev["mqs"]["m2"], 1e-300))
        rid = ev["rho2"] * (math.log(float(mloc)) ** 2)
        dict3_w = max(dict3_w, abs(dic["d3"] - rid)
                      / max(rid, 1e-300))
        dictq_w = max(dictq_w,
                      abs(dic["ymx"] / float(mloc)
                          - ev["mqs"]["qm"])
                      / max(ev["mqs"]["qm"], 1e-300))
    check("G34-dictionary-chain-identity",
          dict2_w <= DICT_BAR and dict3_w <= DICT_BAR
          and dictq_w <= DICT_BAR,
          "THE MOMENT DICTIONARY anchored bit-near on %d live "
          "worlds (incl. FRAME-B): E[X_inf^2] == m M_2 (worst "
          "rel %.1e), E[X_inf^3] == rho_2 (log m)^2 (worst "
          "%.1e), max y / m == q_max (worst %.1e; bars %.0e)"
          % (len(live), dict2_w, dict3_w, dictq_w, DICT_BAR))
    if smoke:
        check("G39-r321-fa-anchors", True, "SMOKE: skipped")
    else:
        srt_x = sorted(x3recs, key=lambda rc: (rc["N"], rc["kz"]))
        srt_x = [rc for rc in srt_x
                 if rc["ev"]["mx_mult"] <= MULT_CAP
                 and not rc["ev"]["degenerate"]]
        srt_x4 = sorted(x4recs,
                        key=lambda rc: (rc["N"], rc["kz"]))
        srt_x4 = [rc for rc in srt_x4
                  if rc["ev"]["mx_mult"] <= MULT_CAP
                  and not rc["ev"]["degenerate"]]
        srt_x5 = sorted(x5recs,
                        key=lambda rc: (rc["N"], rc["kz"]))
        srt_x5 = [rc for rc in srt_x5
                  if rc["ev"]["mx_mult"] <= MULT_CAP
                  and not rc["ev"]["degenerate"]]
        srt_fb = sorted(fbrecs,
                        key=lambda rc: (rc["N"], rc["kz"]))
        srt_fb = [rc for rc in srt_fb
                  if rc["ev"]["mx_mult"] <= MULT_CAP
                  and not rc["ev"]["degenerate"]]
        srt_full = srt + srt_x
        n_full = len(srt_full)
        m_full = [rc["ev"]["m"] for rc in srt_full]
        lg_full = [math.log(float(v)) for v in m_full]
        q_lad = [srt[i]["ev"]["mqs"]["qm"] for i in range(n353)]
        fa_lad = EFP.local_ratio(q_lad)
        medloc_lad = CCP.local_median(q_lad)
        N_lad = [srt[i]["N"] for i in range(n353)]
        fa_full = list(fa_lad)
        for rc in srt_x:
            fa_full.append(CCP.world_coord(rc["ev"]["mqs"]["qm"],
                                           rc["N"], N_lad, q_lad))
        fa_kz = {srt_full[i]["kz"]: fa_full[i]
                 for i in range(n_full)}
        fa_x4 = [CCP.world_coord(rc["ev"]["mqs"]["qm"], rc["N"],
                                 N_lad, q_lad) for rc in srt_x4]
        fa_x5 = [CCP.world_coord(rc["ev"]["mqs"]["qm"], rc["N"],
                                 N_lad, q_lad) for rc in srt_x5]
        fa_fb = [CCP.world_coord(rc["ev"]["mqs"]["qm"], rc["N"],
                                 N_lad, q_lad) for rc in srt_fb]
        check("G39-r321-fa-anchors",
              all(abs(fa_kz[kz] - R321_FA_TOP[kz]) <= R321_FA_TOL
                  for kz in R321_FA_TOP),
              "the r321/r317 F_A coordinate reproduced through "
              "EFP.local_ratio on the module-own q_max column: "
              "top-3 kz53 %.2f / kz83 %.2f / kz67 %.2f (rec "
              "%.2f/%.2f/%.2f tol %.2f); EXT4/EXT5/FRAME-B rows "
              "via the r321 INSERTION RULE (CCP.world_coord, the "
              "r329/r349/r351 convention); F_ins EXT5 %s..%s / "
              "FRAME-B %s..%s; GSQ = %.4f is the BANKED r321 "
              "record, never recalibrated here"
              % (fa_kz[53], fa_kz[83], fa_kz[67],
                 R321_FA_TOP[53], R321_FA_TOP[83],
                 R321_FA_TOP[67], R321_FA_TOL,
                 ("%.2f" % min(fa_x5)) if fa_x5 else "-",
                 ("%.2f" % max(fa_x5)) if fa_x5 else "-",
                 ("%.2f" % min(fa_fb)) if fa_fb else "-",
                 ("%.2f" % max(fa_fb)) if fa_fb else "-",
                 GSQ_R321))

    # ---------------- S4: seal + purity + toys + live wards
    section("S4  LEG 0b -- SEAL + PURITY + TOYS + LIVE WARDS")
    pure_lits = []
    for fn in own_builders:
        pure_lits += literal_audit(fn)
    e1_hits = scope_audit("mutant_nu_posthoc", BOUND_FORBIDDEN)
    e2_hits = scope_audit("mutant_admit_posthoc",
                          BOUND_FORBIDDEN)
    e3_hits = scope_audit("mutant_killbar_posthoc",
                          BOUND_FORBIDDEN)
    check("G40-purity-audits",
          (not sc_own) and (not pure_lits)
          and len(e1_hits) >= 1 and len(e2_hits) >= 1
          and len(e3_hits) >= 1,
          "SOURCE HYGIENE: the construction/selection builders "
          "clean vs the forbidden sets (%d id hits) and vs the "
          "sealed r314..r351 record-literal set (%d literal "
          "hits); e1 nu-posthoc FLAGGED (%s); e2 admit-posthoc "
          "FLAGGED (%s); e3 killbar-posthoc FLAGGED (%s)"
          % (len(sc_own), len(pure_lits),
             e1_hits[0] if e1_hits else "MISS",
             e2_hits[0] if e2_hits else "MISS",
             e3_hits[0] if e3_hits else "MISS"))
    # sealed toys (Fractions + by-hand pins, spec above)
    fab_dev = QGL.fr_fab_toy()
    fd_dev = TSL.fr_decomp_toy()
    mut1 = mutant_nu_posthoc((5.0, 1.0), (2, 8))
    mut2 = mutant_admit_posthoc((5.0, 1.0, 3.0), (1, 2, 3), 2)
    toy2_sealed = (1, 2)
    mut3 = mutant_killbar_posthoc((2.0, 1.0), (1.0, 0.5))
    mut4 = mutant_fab_wrong_log(Fr(3), Fr(1, 2), Fr(2))
    toy4_true = QGL.fab_of(Fr(3), Fr(1, 2), Fr(2))
    toy_bq, toy_dq = frameb_select(
        ((10, 1), (20, 2), (30, 3), (40, 4)),
        (100, 200, 900, 50), (0.5, 0.6, 0.7, 0.4), 50, 500, 1)
    tr_br = (verdict_tree_b(True, True, True),
             verdict_tree_b(False, True, True),
             verdict_tree_b(False, False, True),
             verdict_tree_b(False, False, False))
    ok_tr = tr_br == ("TARGET_LEAK", "LAW_STATE_NOT_EXACT",
                      "NO_ADMISSIBLE_SECOND_FAMILY",
                      "SECOND_FAMILY_BUILT")
    fl_br = (floor_letter_b(True, True, True, True),
             floor_letter_b(False, True, True, True),
             floor_letter_b(False, False, True, True),
             floor_letter_b(False, False, True, False),
             floor_letter_b(False, False, False, True))
    ok_fl = fl_br == ("FLOOR_CENSUS_B", "FLOOR_KILLS",
                      "FLOOR_SATURATES", "FLOOR_STILL_ERODES",
                      "FLOOR_STILL_ERODES")
    check("G41-toy-exactness",
          fab_dev == 0 and fd_dev == 0
          and mut1 == 8 and mut1 != NU_B
          and mut2 == (2, 3) and mut2 != toy2_sealed
          and abs(mut3 - 0.5) <= TOY_BAR and mut3 != RSV_KILL
          and mut4 == Fr(3, 8) and toy4_true == Fr(3, 4)
          and toy4_true / mut4 == Fr(2)
          and tuple(toy_bq) == (4, 1, 2)
          and tuple(toy_dq) == (3, 2, 1)
          and ok_tr and ok_fl,
          "the sealed toys EXACT: FAB Fractions pin 3/4 dev %s; "
          "the r349 res_decomp pin dev %s; e1 nu-posthoc pin %s "
          "!= sealed NU_B %d; e2 admit-posthoc pin %s != sealed "
          "queue rule %s; e3 killbar pin %.1f != sealed %.2f; "
          "e4 wrong-log pin %s != true %s (break factor %s == "
          "lg); selection toy BZ %s (z 900 excluded) BD %s; the "
          "four verdict branches EXACT %s; the floor letters "
          "EXACT %s"
          % (str(fab_dev), str(fd_dev), str(mut1), NU_B,
             str(mut2), str(toy2_sealed), mut3, RSV_KILL,
             str(mut4), str(toy4_true), str(toy4_true / mut4),
             str(tuple(toy_bq)), str(tuple(toy_dq)),
             str(tr_br), str(fl_br)))
    # THE FRAME-B CONSTRUCTION WARD (exact, live): at NU = NU_A
    # the frame-B pipeline must reproduce the frame-A wpack
    # bit-near on w9; the frame-B pool at (NU_A, H_MIN, HCAP)
    # must equal core.frame_a_zones() EXACT.
    pb4 = wpack_b(9, NU_A)
    pa9 = packs["w9"]
    frb_dev = 0.0
    for k in range(min(pb4["N"], pa9["N"])):
        ra, rb = pa9["rows"][k], pb4["rows"][k]
        sc = max(abs(ra["alh"]), 1e-300)
        frb_dev = max(frb_dev, abs(ra["alh"] - rb["alh"]) / sc)
        if ra["sg_h"] != rb["sg_h"]:
            frb_dev = max(frb_dev, 1.0)
    pool_a_repro = sorted(kz for (_h, kz) in
                          frameb_pool(NU_A, core.H_MIN,
                                      core.HCAP, None))
    frame_ok = (pool_a_repro == sorted(core.frame_a_zones()))
    check("G43-frameb-construction-ward",
          pb4["N"] == pa9["N"] and pb4["nf"] == pa9["nf"]
          and frb_dev <= FRB_WARD_BAR and frame_ok,
          "THE NU = %d REPRODUCTION WARD: the frame-B pipeline "
          "at the frame-A aspect reproduces BH.wpack on w9 "
          "EXACTLY (N %d == %d, nf %s == %s, alh/sign dev %.1e, "
          "bar %.0e) AND frameb_pool(NU_A, H_MIN, HCAP) == "
          "core.frame_a_zones() %s (%d zones) -- frame-B is the "
          "SAME construction with one freed parameter, not a "
          "sibling"
          % (NU_A, pb4["N"], pa9["N"], pb4["nf"], pa9["nf"],
             frb_dev, FRB_WARD_BAR,
             "EXACT" if frame_ok else "BROKEN",
             len(pool_a_repro)))
    # THE ROUND'S OWN EXACT LIVE WARDS on every live world
    norm_w = 0.0
    interp_w = 0.0
    fabid_w = 0.0
    pile_w = 0.0
    pilerec_w = 0.0
    grp_w = 0.0
    dec_w = 0.0
    chainD_w = 0.0
    chain_w = 0.0
    id324_w = 0.0
    mult_all_ok = True
    for rc in live:
        ev = rc["ev"]
        mloc = ev["m"]
        lgl = math.log(float(mloc))
        ax_i = np.abs(ev["sct"]["x"])
        Lx_i = float(np.sum(ax_i))
        c3q_i = float(np.sum(ax_i ** 3)) / max(Lx_i ** 3, 1e-300)
        norm_w = max(norm_w,
                     abs(ev["rho2"] * lgl ** 2 / mloc ** 2
                         - c3q_i) / max(c3q_i, 1e-300))
        pk_i = ev["mqs"]["qm"]
        m2q_i = ev["mqs"]["m2"] / float(mloc)
        if pk_i <= 0.0 or c3q_i <= 0.0:
            continue
        interp_w = max(interp_w,
                       max(0.0, c3q_i - pk_i * m2q_i)
                       / max(pk_i * m2q_i, 1e-300),
                       max(0.0, m2q_i - pk_i)
                       / max(pk_i, 1e-300))
        fab_i = QGL.fab_of(float(mloc), pk_i, lgl)
        fabid_w = max(fabid_w,
                      abs(fab_i * lgl - mloc * pk_i)
                      / max(mloc * pk_i, 1e-300))
        pst = ev["pst"]
        if pst["nsc"]:
            pile_w = max(pile_w,
                         max(0.0, mloc * pk_i
                             - pst["nsc"] * pst["pil"])
                         / max(pst["nsc"] * pst["pil"], 1e-300))
            pilerec_w = max(pilerec_w,
                            abs(sum(pst["masses"]) - pst["a1j"])
                            / max(pst["a1j"], 1e-300))
        hst = ev["hst"]
        if hst["ngj"]:
            grp_w = max(grp_w,
                        max(0.0, mloc * pk_i
                            - hst["ngj"] * hst["hgn"])
                        / max(hst["ngj"] * hst["hgn"], 1e-300))
        dd_i = c3q_i / pk_i ** 3
        b2_i = (pk_i * mloc / lgl) ** 2
        sc_i, sm_i2, sd_i = TSL.res_decomp(GSQ_R321, b2_i, pk_i,
                                           m2q_i, c3q_i)
        rsv_dir = GSQ_R321 * 1.0 / max(ev["rho2"], 1e-300)
        dec_w = max(dec_w,
                    abs(sc_i * sm_i2 * sd_i - rsv_dir)
                    / max(rsv_dir, 1e-300))
        chainD_w = max(chainD_w,
                       abs(dd_i * pk_i ** 3 * mloc ** 2
                           / lgl ** 2 - ev["rho2"])
                       / max(ev["rho2"], 1e-300))
        trs = ev["trs"]
        nc = trs["nrm"] * ev["cube"]
        chain_w = max(chain_w, abs(nc - ev["rho2"])
                      / max(ev["rho2"], 1e-300))
        mult_all_ok = mult_all_ok \
            and QMO.mult_ward(ev["gen"]["mult"])[1]
    if not smoke:
        for i in range(n353):
            id324_w = max(id324_w,
                          abs(fa_lad[i] * medloc_lad[i] - q_lad[i])
                          / max(q_lad[i], 1e-300))
    check("G42-live-wards",
          norm_w <= DEC_BAR and interp_w <= CHAIN_BAR
          and fabid_w <= FAB_ID_BAR and pile_w <= CHAIN_BAR
          and pilerec_w <= GRP_CHAIN_BAR and grp_w <= CHAIN_BAR
          and dec_w <= DEC_BAR and chainD_w <= DEC_BAR
          and chain_w <= CHAIN_BAR and id324_w <= CHAIN_BAR
          and mult_all_ok,
          "THE ROUND'S OWN EXACT WARDS live on %d worlds (incl. "
          "FRAME-B): r306 NORM identity %.1e; r324 interpolation "
          "one-sided %.1e; FAB identity %.1e (bar %.0e); pileup "
          "chain %.1e + scale recomposition %.1e; group chain "
          "%.1e; reserve decomposition %.1e + dominance chain "
          "%.1e (bars %.0e); r316 chain %.1e; r324 ladder "
          "identity %.1e; fold multiplicity <= %d everywhere "
          "(%s)"
          % (len(live), norm_w, interp_w, fabid_w, FAB_ID_BAR,
             pile_w, pilerec_w, grp_w, dec_w, chainD_w, DEC_BAR,
             chain_w, id324_w, MULT_CAP,
             "OK" if mult_all_ok else "BROKEN"))

    # ---------------- S5: the r349/r324/r329/r351 records
    section("S5  LEG 0c -- THE r349/r324/r329/r351 RECORDS "
            "(SAME CODE PATH)")
    if smoke:
        check("G50-r349-family-record", True, "SMOKE: skipped")
        check("G51-r349-ext4-record", True, "SMOKE: skipped")
        check("G52-r329-counting-record", True, "SMOKE: skipped")
        check("G53-r324-direct-record", True, "SMOKE: skipped")
        check("G54-r351-growth-record", True, "SMOKE: skipped")
        check("G55-r351-floor-record", True, "SMOKE: skipped")
    else:
        pk_col = []
        m2q_col = []
        c3q_col = []
        dd_col = []
        bb_col = []
        rsv_col = []
        for i in range(n_full):
            ev = srt_full[i]["ev"]
            mloc = ev["m"]
            lgl = lg_full[i]
            pk = ev["mqs"]["qm"]
            m2q = ev["mqs"]["m2"] / float(mloc)
            c3q = ev["cm"]["S3"]
            dd = c3q / max(pk ** 3, 1e-300)
            bb = pk * mloc / (max(fa_full[i], 1e-300) * lgl)
            rsv = GSQ_R321 * fa_full[i] ** 2 \
                / max(ev["rho2"], 1e-300)
            pk_col.append(pk)
            m2q_col.append(m2q)
            c3q_col.append(c3q)
            dd_col.append(dd)
            bb_col.append(bb)
            rsv_col.append(rsv)
        fam_idx = [i for i in range(n_full)
                   if FCC.cls_rule(fa_full[i], F0_SPLIT)
                   == "SPIKE"]
        quiet_idx = [i for i in range(n_full)
                     if i not in set(fam_idx)]
        rc_fam349 = CCP.spearman_rank(
            [fa_full[i] for i in fam_idx],
            [rsv_col[i] for i in fam_idx])
        sh_fam = [(math.log10(GSQ_R321 / bb_col[i] ** 2),
                   math.log10(pk_col[i] / m2q_col[i]),
                   math.log10(pk_col[i] * m2q_col[i]
                              / c3q_col[i]))
                  for i in fam_idx]
        med_f = [float(np.median([s[k] for s in sh_fam]))
                 for k in range(3)]
        dd_fam = [dd_col[i] for i in fam_idx]
        dd_qui = [dd_col[i] for i in quiet_idx
                  if pk_col[i] > 0 and c3q_col[i] > 0]
        dpkb2_max = max(dd_col[i] * pk_col[i] * bb_col[i] ** 2
                        for i in fam_idx)
        rsv_f = [rsv_col[i] for i in fam_idx]
        check("G50-r349-family-record",
              len(fam_idx) == R349_FAM_N
              and abs(min(rsv_f) - R349_RSV_TRIPLE[0])
              <= R349_RSV_TOL
              and abs(float(np.median(rsv_f))
                      - R349_RSV_TRIPLE[1]) <= R349_RSV_TOL
              and abs(max(rsv_f) - R349_RSV_TRIPLE[2])
              <= R349_RSV_TOL
              and abs(rc_fam349 - R349_RC_FAM) <= R349_RC_TOL
              and abs(float(np.median(dd_fam)) - R349_D_FAM_MED)
              <= R349_D_TOL
              and abs(max(dd_fam) - R349_D_FAM_MAX) <= R349_D_TOL
              and abs(float(np.median(dd_qui)) - R349_D_QUIET)
              <= R349_D_TOL
              and abs(dpkb2_max - R349_DPKB2) <= R349_DPKB2_TOL
              and all(abs(med_f[k] - R349_SHARES[k])
                      <= R349_SHARES_TOL for k in range(3)),
              "THE r349 FAMILY RECORD reproduced through the "
              "same code path: family %d rows (rec %d); RSV "
              "min/med/max %.2f/%.2f/%.2f (rec %s); rc_fam "
              "%+.3f (rec %+.3f); D fam med/max %.2f/%.2f, "
              "quiet med %.2f; max D pk B^2 %.3f; shares "
              "%+.2f/%+.2f/%+.2f"
              % (len(fam_idx), R349_FAM_N, min(rsv_f),
                 float(np.median(rsv_f)), max(rsv_f),
                 str(R349_RSV_TRIPLE), rc_fam349, R349_RC_FAM,
                 float(np.median(dd_fam)), max(dd_fam),
                 float(np.median(dd_qui)), dpkb2_max,
                 med_f[0], med_f[1], med_f[2]))
        x4_rows = []
        for k, rc in enumerate(srt_x4):
            ev = rc["ev"]
            mloc = ev["m"]
            lgl = math.log(float(mloc))
            pk = ev["mqs"]["qm"]
            c3q = ev["cm"]["S3"]
            fa4 = fa_x4[k]
            rsv4 = GSQ_R321 * fa4 ** 2 / max(ev["rho2"], 1e-300)
            dd4 = c3q / max(pk ** 3, 1e-300)
            x4_rows.append(dict(kz=rc["kz"], N=rc["N"], m=mloc,
                                lg=lgl, pk=pk, fa=fa4, rsv=rsv4,
                                dd=dd4,
                                cls=FCC.cls_rule(fa4, F0_SPLIT),
                                ev=ev))
        fins4_sorted = tuple(sorted(round(r["fa"], 2)
                                    for r in x4_rows))
        hole_ok = all(
            abs(next(r["rsv"] for r in x4_rows
                     if r["kz"] == kz) - R349_X4_HOLES[kz])
            <= R349_X4_HOLE_TOL for kz in R349_X4_HOLES)
        check("G51-r349-ext4-record",
              len(x4_rows) == 6
              and all(r["cls"] == "SPIKE" for r in x4_rows)
              and all(abs(fins4_sorted[j] - R349_X4_FINS[j])
                      <= R349_X4_FINS_TOL for j in range(6))
              and hole_ok,
              "THE r349 EXT4 RECORD reproduced: all six SPIKE; "
              "sorted F_ins %s (rec %s); the two bar misses "
              "kz111 rsv %.2f / kz75 rsv %.2f (rec 1.11/1.07)"
              % (str(fins4_sorted), str(R349_X4_FINS),
                 next(r["rsv"] for r in x4_rows
                      if r["kz"] == 111),
                 next(r["rsv"] for r in x4_rows
                      if r["kz"] == 75)))
        nscl65 = [srt[i]["ev"]["pst"]["nsc_rel"]
                  / math.log(float(srt[i]["ev"]["m"]))
                  for i in range(n353)]
        cnsc_repro = max(nscl65[i] for i in ca_i)
        viol_nsc = [i for i in te_i if nscl65[i] > cnsc_repro]
        ngl65 = [srt[i]["ev"]["hst"]["ngj"]
                 / math.log(float(srt[i]["ev"]["m"]))
                 for i in range(n353)]
        cng_repro = max(ngl65[i] for i in ca_i)
        viol_ng = [i for i in te_i if ngl65[i] > cng_repro]
        x3_nscl = [rc["ev"]["pst"]["nsc_rel"]
                   / math.log(float(rc["ev"]["m"]))
                   for rc in srt_x]
        x3_ngl = [rc["ev"]["hst"]["ngj"]
                  / math.log(float(rc["ev"]["m"]))
                  for rc in srt_x]
        rsvc_min = min(FROZEN_CNSC / max(v, 1e-300)
                       for v in x3_nscl)
        rsvd_min = min(FROZEN_CNG / max(v, 1e-300)
                       for v in x3_ngl)
        check("G52-r329-counting-record",
              abs(cnsc_repro - FROZEN_CNSC) <= EFA.FROZEN_TOL
              and not viol_nsc
              and abs(cng_repro - FROZEN_CNG) <= EFA.FROZEN_TOL
              and not viol_ng
              and abs(rsvc_min - R329_X3_RSVC_MIN)
              <= R329_X3_RSV_TOL
              and abs(rsvd_min - R329_X3_RSVD_MIN)
              <= R329_X3_RSV_TOL,
              "THE r329 COUNTING RECORD reproduced: C_NSC = "
              "%.4f (rec %.4f) 0/39 (viol %d); C_NG = %.4f "
              "(rec %.4f) 0/39 (viol %d); EXT3 12/12 with min "
              "count reserves %.2f/%.2f (rec 1.6/1.8)"
              % (cnsc_repro, FROZEN_CNSC, len(viol_nsc),
                 cng_repro, FROZEN_CNG, len(viol_ng), rsvc_min,
                 rsvd_min))
        fab65 = [QGL.fab_of(float(srt[i]["ev"]["m"]),
                            srt[i]["ev"]["mqs"]["qm"],
                            math.log(float(srt[i]["ev"]["m"])))
                 for i in range(n353)]
        cinf_repro = max(fab65[i] for i in ca_i)
        viol_inf = sorted(srt[i]["kz"] for i in te_i
                          if fab65[i] > cinf_repro)
        msT = [srt[i]["ev"]["m"] for i in te_i]
        eg_repro = L2D.halves_slope(msT, [max(fab65[i], 1e-300)
                                          for i in te_i])
        check("G53-r324-direct-record",
              abs(cinf_repro - R324_CINF_REF) <= R324_CINF_TOL
              and tuple(viol_inf) == tuple(sorted(R324_VINF_SET))
              and abs(eg_repro - R324_EG_REF) <= R324_EG_TOL,
              "THE r324 DIRECT RECORD reproduced through the FAB "
              "column: C_INF = %.4f (rec %.4f), test violators "
              "%s == the r324 five; e_G = %+.3f (rec %+.3f)"
              % (cinf_repro, R324_CINF_REF, str(viol_inf),
                 eg_repro, R324_EG_REF))
        # ---- THE r351 GROWTH + FLOOR RECORDS (the 89-row
        # ---- surface through the same code path)
        allrows = []
        for i in range(n_full):
            ev = srt_full[i]["ev"]
            allrows.append(dict(
                coh="LAD" if i < n353 else "EXT3",
                kz=srt_full[i]["kz"], m=ev["m"],
                lg=lg_full[i], pk=pk_col[i], fa=fa_full[i],
                cls=FCC.cls_rule(fa_full[i], F0_SPLIT),
                dd=dd_col[i], rsv=rsv_col[i], ev=ev))
        for r in x4_rows:
            allrows.append(dict(coh="EXT4", kz=r["kz"],
                                m=r["m"], lg=r["lg"], pk=r["pk"],
                                fa=r["fa"], cls=r["cls"],
                                dd=r["dd"], rsv=r["rsv"],
                                ev=r["ev"]))
        x5_rows = []
        for k, rc in enumerate(srt_x5):
            ev = rc["ev"]
            mloc = ev["m"]
            lgl = math.log(float(mloc))
            pk = ev["mqs"]["qm"]
            c3q = ev["cm"]["S3"]
            fa5 = fa_x5[k]
            rsv5 = GSQ_R321 * fa5 ** 2 / max(ev["rho2"], 1e-300)
            dd5 = c3q / max(pk ** 3, 1e-300)
            x5_rows.append(dict(coh="EXT5", kz=rc["kz"],
                                m=mloc, lg=lgl, pk=pk, fa=fa5,
                                cls=FCC.cls_rule(fa5, F0_SPLIT),
                                dd=dd5, rsv=rsv5, ev=ev))
        allrows += x5_rows
        grel_all = EFA.grel_col([r["kz"] for r in allrows],
                                core.G_ALL)
        for r, g in zip(allrows, grel_all):
            r["grel"] = g
            r["gcls"] = EFA.gap_class(g)
            r["fab"] = QGL.fab_of(float(r["m"]), r["pk"],
                                  r["lg"])
            pst = r["ev"]["pst"]
            hst = r["ev"]["hst"]
            r["pil"] = pst["pil"]
            r["nsc"] = pst["nsc"]
            r["nscl"] = pst["nsc_rel"] / r["lg"]
            r["ngj"] = hst["ngj"]
            r["ngl"] = hst["ngj"] / r["lg"]
            r["hgn"] = hst["hgn"]
        c_fab89 = max(r["fab"] for r in allrows)
        c_fab89_kz = max(allrows, key=lambda r: r["fab"])["kz"]
        x5max = max((r for r in allrows if r["coh"] == "EXT5"),
                    key=lambda r: r["fab"])
        fins79 = next(r["fa"] for r in allrows
                      if r["kz"] == 79 and r["coh"] == "EXT5")
        smallfam89 = [r for r in allrows
                      if r["gcls"] == "SMALL"
                      and r["cls"] == "SPIKE"]
        rc_small89 = CCP.spearman_rank(
            [r["m"] for r in smallfam89],
            [r["fab"] for r in smallfam89]) \
            if len(smallfam89) >= 2 else 0.0
        check("G54-r351-growth-record",
              abs(c_fab89 - R351_CFAB) <= R351_CFAB_TOL
              and c_fab89_kz == R351_CFAB_KZ
              and abs(x5max["fab"] - R351_X5_FABMAX)
              <= R351_CFAB_TOL
              and x5max["kz"] == R351_X5_FABMAX_KZ
              and abs(fins79 - R351_X5_FINS79)
              <= R351_X5_FINS_TOL
              and abs(rc_small89 - R351_RC_SMALL)
              <= R351_SLOPE_TOL,
              "THE r351 GROWTH RECORD reproduced on the 89-row "
              "surface: C_FAB = %.2f at kz%d (rec %.2f at "
              "kz%d); EXT5 FAB max %.2f at kz%d (rec %.2f at "
              "kz%d); kz79 F_ins %.2f (rec %.2f); rc_small "
              "%+.3f (rec %+.3f) -- m q_max <= %.2f log m "
              "stands on all %d sealed rows"
              % (c_fab89, c_fab89_kz, R351_CFAB, R351_CFAB_KZ,
                 x5max["fab"], x5max["kz"], R351_X5_FABMAX,
                 R351_X5_FABMAX_KZ, fins79, R351_X5_FINS79,
                 rc_small89, R351_RC_SMALL, c_fab89,
                 len(allrows)))
        fam_all89 = [r for r in allrows if r["cls"] == "SPIKE"]
        rsv_min89 = min(r["rsv"] for r in fam_all89)
        rsv_min89_kz = min(fam_all89,
                           key=lambda r: r["rsv"])["kz"]
        e_rsv89 = L2D.halves_slope(
            [r["m"] for r in sorted(fam_all89,
                                    key=lambda r: r["m"])],
            [max(r["rsv"], 1e-300)
             for r in sorted(fam_all89, key=lambda r: r["m"])])
        rc_fam89 = CCP.spearman_rank(
            [r["fa"] for r in fam_all89],
            [r["rsv"] for r in fam_all89])
        x5_spk = [r for r in allrows if r["coh"] == "EXT5"
                  and r["cls"] == "SPIKE"]
        c_m2env89 = max(r["ev"]["mqs"]["m2"] for r in allrows)
        c_k2_83 = max(r["fab"] * r["grel"] for r in allrows
                      if r["coh"] != "EXT5")
        rc_gap89 = CCP.spearman_rank(
            [r["grel"] for r in fam_all89],
            [r["fab"] for r in fam_all89])
        cnt_rows89 = [r for r in allrows
                      if r["coh"] in ("EXT4", "EXT5")]
        res_nsc89 = min(FROZEN_CNSC / max(r["nscl"], 1e-300)
                        for r in cnt_rows89)
        res_ng89 = min(FROZEN_CNG / max(r["ngl"], 1e-300)
                       for r in cnt_rows89)

        def solve_m0(log_rhs):
            t = math.log(73.0)
            while t < 1e7:
                if CRIT_EXP * t >= log_rhs(t):
                    return t / math.log(10.0)
                t *= 1.02
            return None

        m0_law89 = solve_m0(lambda t: math.log(
            max(c_fab89 * c_m2env89 * t, 1e-300)))
        check("G55-r351-floor-record",
              len(fam_all89) == R351_FAM_N
              and abs(rsv_min89 - R351_MINRSV) <= R351_MINRSV_TOL
              and rsv_min89_kz == R351_MINRSV_KZ
              and abs(e_rsv89 - R351_ERSV) <= R351_SLOPE_TOL
              and abs(rc_fam89 - R351_RCFAM) <= R351_SLOPE_TOL
              and all(R351_X5_RSV_LO - R351_X5_RSV_TOL
                      <= r["rsv"]
                      <= R351_X5_RSV_HI + R351_X5_RSV_TOL
                      for r in x5_spk)
              and abs(c_m2env89 - R351_M2ENV) <= R351_M2ENV_TOL
              and m0_law89 is not None
              and abs(m0_law89 - R351_M0LAW) <= R351_M0LAW_TOL
              and abs(c_k2_83 - R351_CK2_83) <= R351_CK2_TOL
              and abs(rc_gap89 - R351_RCGAP) <= R351_SLOPE_TOL
              and abs(res_nsc89 - R351_CNT_RES[0])
              <= R351_CNT_RES_TOL
              and abs(res_ng89 - R351_CNT_RES[1])
              <= R351_CNT_RES_TOL,
              "THE r351 FLOOR RECORD reproduced: %d family rows "
              "(rec %d); min RSV %.2f at kz%d (rec %.2f at "
              "kz%d); e_RSV %+.3f (rec %+.3f); rc_fam %+.3f "
              "(rec %+.3f); EXT5 spikes RSV inside [%.2f, "
              "%.2f]; C_M2ENV %.2f (rec %.2f); law m_0* "
              "10^%.1f (rec 10^%.1f); C_K2(83) %.2f (rec "
              "%.2f); rc_gap %+.3f (rec %+.3f); count reserves "
              "%.2f/%.2f (rec %s)"
              % (len(fam_all89), R351_FAM_N, rsv_min89,
                 rsv_min89_kz, R351_MINRSV, R351_MINRSV_KZ,
                 e_rsv89, R351_ERSV, rc_fam89, R351_RCFAM,
                 R351_X5_RSV_LO, R351_X5_RSV_HI, c_m2env89,
                 R351_M2ENV, m0_law89, R351_M0LAW, c_k2_83,
                 R351_CK2_83, rc_gap89, R351_RCGAP, res_nsc89,
                 res_ng89, str(R351_CNT_RES)))

    # ---------------- S6: Leg A -- the family-B table
    section("S6  LEG A -- THE SECOND FAMILY (THE FRAME-B TABLE)")
    if smoke:
        check("G60-famb-table", True, "SMOKE: skipped")
        no_family = True
        b_rows = []
    else:
        b_rows = []
        for k, rc in enumerate(srt_fb):
            ev = rc["ev"]
            mloc = ev["m"]
            lgl = math.log(float(mloc))
            pk = ev["mqs"]["qm"]
            c3q = ev["cm"]["S3"]
            fab_v = QGL.fab_of(float(mloc), pk, lgl)
            fa_v = fa_fb[k]
            rsv_v = GSQ_R321 * fa_v ** 2 / max(ev["rho2"],
                                               1e-300)
            dd_v = c3q / max(pk ** 3, 1e-300)
            pst = ev["pst"]
            hst = ev["hst"]
            b_rows.append(dict(
                coh="FAMB", kz=rc["kz"], N=rc["N"], m=mloc,
                lg=lgl, pk=pk, fa=fa_v,
                cls=FCC.cls_rule(fa_v, F0_SPLIT), dd=dd_v,
                rsv=rsv_v, fab=fab_v, pil=pst["pil"],
                nsc=pst["nsc"], nscl=pst["nsc_rel"] / lgl,
                ngj=hst["ngj"], ngl=hst["ngj"] / lgl,
                hgn=hst["hgn"],
                strat=famb_strat.get(rc["kz"], "?"), ev=ev))
        grel_b2 = EFA.grel_col([r["kz"] for r in b_rows],
                               core.G_ALL)
        for r, g in zip(b_rows, grel_b2):
            r["grel"] = g
            r["gcls"] = EFA.gap_class(g)
        no_family = len(b_rows) < FRESH_MIN_B
        info("THE FRAME-B FAMILY TABLE (strat kz z N_w m grel "
             "class | F_ins cls FAB pk D | RSV):")
        for r in sorted(b_rows, key=lambda r: -r["fab"]):
            info("  %-2s kz%-4d z %-4d N %-5d m %-4d grel %.3f "
                 "%-5s | F %5.2f %-5s FAB %6.2f pk %.4f D "
                 "%5.2f | RSV %5.2f"
                 % (r["strat"], r["kz"],
                    int(core._NN[r["kz"]]), r["N"], r["m"],
                    r["grel"], r["gcls"], r["fa"], r["cls"],
                    r["fab"], r["pk"], r["dd"], r["rsv"]))
        m_max_b = max((r["m"] for r in b_rows), default=0)
        m_max_89 = max(r["m"] for r in allrows)
        check("G60-famb-table", True,
              "THE SECOND FAMILY: %d admitted rows (BZ %d + BD "
              "%d), m %s..%s (log10 %s vs the frame-A max %d = "
              "log10 %.2f; the r351 crossing target 3.7 is NOT "
              "reached -- lever extension, disclosed), grel "
              "%.3f..%.3f (gap classes %s), %d SPIKE / %d "
              "QUIET; POSITIVE_PREFIX admission carried "
              "cleanly where the r329 frame-A route failed at "
              "nf 2125..2468"
              % (len(b_rows),
                 sum(1 for r in b_rows if r["strat"] == "BZ"),
                 sum(1 for r in b_rows if r["strat"] == "BD"),
                 min((r["m"] for r in b_rows), default="-"),
                 m_max_b,
                 ("%.2f" % math.log10(m_max_b))
                 if m_max_b else "-",
                 m_max_89, math.log10(m_max_89),
                 min((r["grel"] for r in b_rows), default=0.0),
                 max((r["grel"] for r in b_rows), default=0.0),
                 str(sorted(set(r["gcls"] for r in b_rows))),
                 sum(1 for r in b_rows if r["cls"] == "SPIKE"),
                 sum(1 for r in b_rows if r["cls"] == "QUIET")))

    # ---------------- S7: Leg C -- the FAB law cross-family
    section("S7  LEG C -- THE FAB LAW + K2X + COUNTS ON THE "
            "SECOND FAMILY")
    if smoke:
        check("G70-fab-freeze-test", True, "SMOKE: skipped")
        check("G71-k2-cross-family", True, "SMOKE: skipped")
        check("G72-fresh-count-test", True, "SMOKE: skipped")
        fab_letter = "FAB_CENSUS"
        grel_cand = False
    else:
        fab_viol_b = [(r["kz"], round(r["fab"], 2))
                      for r in b_rows if r["fab"] > c_fab89]
        new_record = bool(fab_viol_b)
        fab_letter = "FAB_LAW_HOLDS_CROSS_FAMILY" \
            if not fab_viol_b \
            else "FAB_LAW_BREAKS_ON_B(%s)" % str(fab_viol_b)
        rc_small_x = CCP.spearman_rank(
            [r["m"] for r in smallfam89 + [r for r in b_rows
             if r["gcls"] == "SMALL" and r["cls"] == "SPIKE"]],
            [r["fab"] for r in smallfam89 + [r for r in b_rows
             if r["gcls"] == "SMALL" and r["cls"] == "SPIKE"]])
        check("G70-fab-freeze-test", True,
              "THE FROZEN CEILING TEST (freeze rule: max over "
              "the 89 sealed r351 rows): C_FAB = %.2f at kz%d; "
              "the CROSS-FAMILY TEST on %d frame-B rows: %s -- "
              "m q_max <= %.2f log m %s on the second family; "
              "max frame-B FAB %.2f at kz%s; rc_small "
              "(combined SMALL-gap family, n = %d) = %+.3f vs "
              "the growth bar %+.1f"
              % (c_fab89, c_fab89_kz, len(b_rows),
                 ("0 violations" if not fab_viol_b else
                  "VIOLATIONS %s" % str(fab_viol_b)), c_fab89,
                 "holds" if not fab_viol_b else "BREAKS",
                 max((r["fab"] for r in b_rows), default=0.0),
                 max(b_rows, key=lambda r: r["fab"])["kz"]
                 if b_rows else "-",
                 len(smallfam89) + sum(1 for r in b_rows
                                       if r["gcls"] == "SMALL"
                                       and r["cls"] == "SPIKE"),
                 rc_small_x, RC_GROW))
        c_k2x = max(r["fab"] * r["grel"] for r in allrows)
        k2_viol_b = [(r["kz"], round(r["fab"] * r["grel"], 2))
                     for r in b_rows
                     if r["fab"] * r["grel"] > c_k2x]
        fam_comb_pre = fam_all89 + [r for r in b_rows
                                    if r["cls"] == "SPIKE"]
        g_min = min((r["grel"] for r in fam_comb_pre),
                    default=0.0)
        fab_max_all = max([r["fab"] for r in allrows]
                          + [r["fab"] for r in b_rows])
        impl_k2 = c_k2x / max(g_min, 1e-300)
        grel_cand = (not k2_viol_b) and g_min > 0.0 \
            and impl_k2 <= IMPL_FAC * fab_max_all \
            and not no_family
        check("G71-k2-cross-family", True,
              "THE K2 SHARPENING: C_K2X = max FAB grel over the "
              "89 sealed rows = %.2f (the 83-row r351 subset "
              "%.2f -- EXT5 %s lift it); frame-B test: %s (max "
              "FAB grel %.2f); g_min = min grel over the %d "
              "combined family rows = %.3f -> implied ceiling "
              "C_K2X/g_min = %.1f vs %.1f x max FAB %.2f: "
              "GREL_LOWER_BOUND_CANDIDATE %s (census, not law "
              "-- the gap floor is measured, not derived)"
              % (c_k2x, c_k2_83,
                 "does NOT" if abs(c_k2x - c_k2_83) < 1e-9
                 else "DOES",
                 "0 violations" if not k2_viol_b
                 else "VIOLATIONS %s" % str(k2_viol_b),
                 max((r["fab"] * r["grel"] for r in b_rows),
                     default=0.0),
                 len(fam_comb_pre), g_min, impl_k2, IMPL_FAC,
                 fab_max_all, str(grel_cand)))
        cnt_viol_b = [(r["kz"], round(r["nscl"], 2),
                       round(r["ngl"], 2)) for r in b_rows
                      if r["nscl"] > FROZEN_CNSC
                      or r["ngl"] > FROZEN_CNG]
        check("G72-fresh-count-test", True,
              "THE COUNT TEST (r329 FROZEN constants, first "
              "cross-family measurement): nsc_rel/lg <= %.4f "
              "AND ngj/lg <= %.4f on %d frame-B rows: %s; "
              "count reserves min NSC %.2f / NG %.2f -- the "
              "O(log m) counting side %s on the second family"
              % (FROZEN_CNSC, FROZEN_CNG, len(b_rows),
                 "0 violations" if not cnt_viol_b
                 else "VIOLATIONS %s" % str(cnt_viol_b),
                 min((FROZEN_CNSC / max(r["nscl"], 1e-300)
                      for r in b_rows), default=0.0),
                 min((FROZEN_CNG / max(r["ngl"], 1e-300)
                      for r in b_rows), default=0.0),
                 "carries" if not cnt_viol_b else "BREAKS"))

    # ---------------- S8: Leg B -- the preregistered erosion kill
    section("S8  LEG B -- THE PREREGISTERED EROSION KILL")
    if smoke:
        check("G80-erosion-bars", True, "SMOKE: skipped")
        check("G81-erosion-fit", True, "SMOKE: skipped")
        check("G82-floor-letter", True, "SMOKE: skipped")
        fl_letter = "FLOOR_CENSUS_B"
        mx_log10 = 0.0
    else:
        b_spikes = [r for r in b_rows if r["cls"] == "SPIKE"]
        kill_rows = [(r["kz"], round(r["rsv"], 2))
                     for r in b_spikes if r["rsv"] < RSV_KILL]
        bar_miss = [(r["kz"], round(r["rsv"], 2))
                    for r in b_spikes
                    if RSV_KILL <= r["rsv"] < RSV_BAR]
        bar_ok = not kill_rows and not bar_miss
        check("G80-erosion-bars", True,
              "THE SEALED BARS on %d frame-B SPIKE rows: "
              "RSV_KILL = %.2f: %s; RSV_BAR = %.2f: %s; frame-B "
              "spike RSV min/med %.2f/%.2f (the r351 frame-A "
              "floor was %.2f at kz%d)"
              % (len(b_spikes), RSV_KILL,
                 "NO row under the kill bar"
                 if not kill_rows else "KILLED %s"
                 % str(kill_rows), RSV_BAR,
                 "0 misses" if not bar_miss
                 else "misses in [%.2f, %.2f): %s"
                 % (RSV_KILL, RSV_BAR, str(bar_miss)),
                 min((r["rsv"] for r in b_spikes),
                     default=0.0),
                 float(np.median([r["rsv"] for r in b_spikes]))
                 if b_spikes else 0.0,
                 rsv_min89, rsv_min89_kz))
        fam_comb = fam_all89 + b_spikes
        e_rsv_comb = L2D.halves_slope(
            [r["m"] for r in sorted(fam_comb,
                                    key=lambda r: r["m"])],
            [max(r["rsv"], 1e-300)
             for r in sorted(fam_comb, key=lambda r: r["m"])])
        rc_comb = CCP.spearman_rank(
            [r["fa"] for r in fam_comb],
            [r["rsv"] for r in fam_comb])
        e_rsv_b = L2D.halves_slope(
            [r["m"] for r in sorted(b_spikes,
                                    key=lambda r: r["m"])],
            [max(r["rsv"], 1e-300)
             for r in sorted(b_spikes, key=lambda r: r["m"])]) \
            if len(b_spikes) >= 2 else 0.0
        sat = e_rsv_comb >= -E_FLAT_BAR
        m_med_c = float(np.median([r["m"] for r in fam_comb]))
        rsv_med_c = float(np.median([r["rsv"]
                                     for r in fam_comb]))
        if e_rsv_comb < 0.0 and rsv_med_c > 1.0:
            mx_log10 = (math.log10(m_med_c)
                        - math.log10(rsv_med_c) / e_rsv_comb)
        else:
            mx_log10 = float("inf")
        check("G81-erosion-fit", True,
              "THE COMBINED EROSION FIT (%d family rows = 28 "
              "frame-A + %d frame-B): e_RSV_comb = %+.3f (r351 "
              "prior %+.3f; flat bar +/-%.2f) -> %s; rc_comb = "
              "%+.3f (r351 prior %+.3f); frame-B-only slope "
              "%+.3f (census, n = %d); extrapolated RSV = 1 "
              "crossing log10 m ~ %s (was 3.7 at r351)"
              % (len(fam_comb), len(b_spikes), e_rsv_comb,
                 R351_ERSV, E_FLAT_BAR,
                 "SATURATING" if sat else "STILL ERODING",
                 rc_comb, R351_RCFAM, e_rsv_b, len(b_spikes),
                 ("%.1f" % mx_log10)
                 if math.isfinite(mx_log10) else "inf"))
        fl_letter = floor_letter_b(len(b_spikes) == 0,
                                   bool(kill_rows), bar_ok, sat)
        check("G82-floor-letter", True,
              "THE SEALED FLOOR LETTER: %s -- %s"
              % (fl_letter,
                 {"FLOOR_CENSUS_B": "no frame-B spike rows (no "
                  "fresh floor teeth)",
                  "FLOOR_KILLS": "a frame-B row breaches "
                  "RSV_KILL %.2f: the sliding coverage is "
                  "FINITE at the measured range -- the "
                  "three-arm cover is bounded and the cofinal "
                  "reading needs a fourth mechanism"
                  % RSV_KILL,
                  "FLOOR_SATURATES": "every frame-B spike above "
                  "%.2f AND the combined slope is flat: the "
                  "cover candidate lives" % RSV_BAR,
                  "FLOOR_STILL_ERODES": "no kill, but bar "
                  "misses in [%.2f, %.2f) or a still-eroding "
                  "combined slope -- the erosion is real on "
                  "TWO construction families now"
                  % (RSV_KILL, RSV_BAR)}[fl_letter]))

    # ---------------- S9: Leg D -- the two-family synthesis
    section("S9  LEG D -- THE TWO-FAMILY SYNTHESIS + "
            "COMPOSITION + COFINAL TYPING")
    if smoke:
        check("G85-synthesis", True, "SMOKE: skipped")
        check("G86-composition", True, "SMOKE: skipped")
        check("G87-cofinal-typing", True, "SMOKE: skipped")
        verdict_main = "SMOKE_NO_ADJUDICATION"
    else:
        def colstats(rows):
            fam = [r for r in rows if r["cls"] == "SPIKE"]
            return dict(
                n=len(rows), nfam=len(fam),
                fabmax=max(r["fab"] for r in rows),
                rsvmin=min((r["rsv"] for r in fam),
                           default=float("nan")),
                rsvmed=float(np.median([r["rsv"]
                                        for r in fam]))
                if fam else float("nan"),
                dmed=float(np.median([r["dd"] for r in fam]))
                if fam else float("nan"),
                pkmed=float(np.median([r["pk"] for r in fam]))
                if fam else float("nan"),
                nsclmax=max(r["nscl"] for r in rows),
                nglmax=max(r["ngl"] for r in rows),
                m2env=max(r["ev"]["mqs"]["m2"] for r in rows),
                mmax=max(r["m"] for r in rows))
        for r in allrows:
            if "nscl" not in r:
                r["nscl"] = 0.0
        sA = colstats(allrows)
        sB = colstats(b_rows) if b_rows else None
        info("THE TWO-FAMILY TABLE (frame A 89 rows | frame B "
             "%d rows):" % (len(b_rows)))
        if sB:
            for key, lab in (("fabmax", "max FAB"),
                             ("rsvmin", "min RSV (fam)"),
                             ("rsvmed", "med RSV (fam)"),
                             ("dmed", "med D (fam)"),
                             ("pkmed", "med pk (fam)"),
                             ("nsclmax", "max nsc/lg"),
                             ("nglmax", "max ngj/lg"),
                             ("m2env", "m M_2 envelope"),
                             ("mmax", "max m")):
                info("  %-16s A %8.3f | B %8.3f"
                     % (lab, sA[key], sB[key]))
        check("G85-synthesis", True,
              "THE SYNTHESIS: frame A (%d rows, %d family) vs "
              "frame B (%d rows, %d family) -- max FAB %.2f vs "
              "%.2f; min family RSV %.2f vs %.2f; med D %.2f "
              "vs %.2f; med pk %.4f vs %.4f; m M_2 envelope "
              "%.2f vs %.2f; max m %d vs %d%s"
              % (sA["n"], sA["nfam"],
                 sB["n"] if sB else 0, sB["nfam"] if sB else 0,
                 sA["fabmax"], sB["fabmax"] if sB else 0.0,
                 sA["rsvmin"],
                 sB["rsvmin"] if sB else float("nan"),
                 sA["dmed"], sB["dmed"] if sB else float("nan"),
                 sA["pkmed"],
                 sB["pkmed"] if sB else float("nan"),
                 sA["m2env"], sB["m2env"] if sB else 0.0,
                 sA["mmax"], sB["mmax"] if sB else 0,
                 " -- the second family sets the NEW depth "
                 "record of the lane"
                 if sB and sB["mmax"] > sA["mmax"] else ""))
        c_fab_new = max(sA["fabmax"],
                        sB["fabmax"] if sB else 0.0)
        c_m2env_new = max(sA["m2env"],
                          sB["m2env"] if sB else 0.0)
        m0_law_new = solve_m0(lambda t: math.log(
            max(c_fab_new * c_m2env_new * t, 1e-300)))
        m0_uni89 = solve_m0(lambda t: math.log(
            max(GSQ_R321 * max(r["fa"] for r in allrows) ** 2
                * t * t, 1e-300)))
        info("THE m_0* TABLE (typed):")
        info("  r351 law route (89 rows, record):  10^%.1f"
             % m0_law89)
        info("  the TWO-FAMILY law route (C_FAB' %.2f x "
             "C_M2ENV' %.2f): %s"
             % (c_fab_new, c_m2env_new,
                ("10^%.1f" % m0_law_new)
                if m0_law_new is not None else "NONE"))
        info("  uniform at the F ceiling (89 rows): %s; V1 "
             "10^%.1f / V2 10^%.1f; r324 route 10^%.1f"
             % (("10^%.1f" % m0_uni89)
                if m0_uni89 is not None else "NONE",
                V1_M0_REF, V2_M0_REF, R324_M0_L10))
        check("G86-composition", True,
              "THE COMPOSITION: the class-free polylog route "
              "recomposed over BOTH families -- rho_2 <= %.2f "
              "x %.2f / log m -> m_0* = 10^%s (r351 record "
              "10^%.1f; moved by %s decades); the ceilings "
              "C_FAB %.2f -> %.2f and C_M2ENV %.2f -> %.2f "
              "%s by the second family"
              % (c_fab_new, c_m2env_new,
                 ("%.1f" % m0_law_new)
                 if m0_law_new is not None else "NONE",
                 m0_law89,
                 ("%.1f" % abs(m0_law_new - m0_law89))
                 if m0_law_new is not None else "?",
                 c_fab89, c_fab_new, c_m2env89, c_m2env_new,
                 "are MOVED" if (c_fab_new > c_fab89 + 1e-9
                                 or c_m2env_new
                                 > c_m2env89 + 1e-9)
                 else "are NOT moved"))
        leak = bool(sc_own) or bool(pure_lits)
        brk_struct = (norm_w > DEC_BAR or interp_w > CHAIN_BAR
                      or fabid_w > FAB_ID_BAR
                      or pile_w > CHAIN_BAR
                      or pilerec_w > GRP_CHAIN_BAR
                      or grp_w > CHAIN_BAR or dec_w > DEC_BAR
                      or chainD_w > DEC_BAR
                      or chain_w > CHAIN_BAR
                      or id324_w > CHAIN_BAR
                      or not mult_all_ok
                      or fab_dev != 0 or fd_dev != 0
                      or frb_dev > FRB_WARD_BAR
                      or not frame_ok)
        vkey = verdict_tree_b(leak, brk_struct, no_family)
        check("G87-cofinal-typing", True,
              "COFINAL TYPING -- the five r351 holes at the %s "
              "reading: (1) C_FAB/C_M2ENV freeze-census: %s "
              "(the ceilings %s under the second family); (2) "
              "the floor: %s; (3) the source formula: K2X %s "
              "cross-family + the grel floor candidate %s "
              "(K3/K4 not retested here, disclosed); (4) "
              "finite closure: extended to m = %d (was 660); "
              "(5) the second WORLD: still open -- frame-B is "
              "a second CONSTRUCTION on the same source comb, "
              "not a second arithmetic (r329 honesty, "
              "restated)"
              % (vkey,
                 "CONFIRMED census" if (c_fab_new > c_fab89
                                        + 1e-9) else
                 "STRENGTHENED (ceilings hold out-of-family)",
                 "MOVE" if (c_fab_new > c_fab89 + 1e-9
                            or c_m2env_new > c_m2env89 + 1e-9)
                 else "STAND",
                 fl_letter,
                 "holds" if not k2_viol_b else "breaks",
                 str(grel_cand),
                 max(sA["mmax"], sB["mmax"] if sB else 0)))
        flags = [fab_letter,
                 fl_letter if fl_letter != "FLOOR_STILL_ERODES"
                 else "FLOOR_STILL_ERODES(10^%s)"
                 % (("%.1f" % mx_log10)
                    if math.isfinite(mx_log10) else "inf")]
        if fl_letter == "FLOOR_KILLS":
            flags[-1] = "FLOOR_KILLS(%s)" % str(kill_rows)
        if grel_cand:
            flags.append("GREL_LOWER_BOUND_CANDIDATE(%.3f, "
                         "%.1f)" % (g_min, impl_k2))
        if bz_exhausted:
            flags.append("BZ_POOL_EXHAUSTED(%d)" % len(b_q))
        if sB and sB["mmax"] > sA["mmax"]:
            flags.append("NEW_DEPTH_RECORD(%d)" % sB["mmax"])
        if not smoke and b_rows and new_record:
            rmax = max(b_rows, key=lambda r: r["fab"])
            flags.append("NEW_FAB_RECORD(kz%d, %.2f)"
                         % (rmax["kz"], rmax["fab"]))
        det_v = {
            "TARGET_LEAK": "purity/scope audit hit on a "
                           "module-own builder",
            "LAW_STATE_NOT_EXACT":
                "an exact ward broke (NORM %.1e / interp %.1e "
                "/ FAB id %.1e / decomp %.1e / frame-B repro "
                "%.1e)" % (norm_w, interp_w, fabid_w, dec_w,
                           frb_dev),
            "NO_ADMISSIBLE_SECOND_FAMILY":
                "fewer than %d frame-B rows admitted -- the "
                "sealed construction boundary is the finding"
                % FRESH_MIN_B,
            "SECOND_FAMILY_BUILT":
                "%d/%d frame-B rows admitted cleanly (queue "
                "failures BZ %s / BD %s)"
                % (len(b_rows), K_BZ + K_BD, str(bz_fail),
                   str(bd_fail))}
        verdict_main = "%s(%s)%s" % (
            vkey, det_v[vkey],
            ("".join(" + " + f for f in flags))
            if flags else "")

    # ---------------- S10: Leg E -- worlds + must-fails + verdict
    section("S10  LEG E -- FRAME-B WORLDS + MUST-FAILS + "
            "VERDICT")
    if smoke:
        pb9 = wpack_b(9, NU_B)
        check("G90-world-census", pb9["complete"],
              "SMOKE: frame-B w9 built (N %d, nf %s, "
              "chain-complete %s); twin + SCRAMBLE-B skipped"
              % (pb9["N"], pb9["nf"], pb9["complete"]))
    else:
        wtabB = []
        for wkz, lab in ((9, "w9B"), (13, "w13B(twin)")):
            pbw = wpack_b(wkz, NU_B)
            if pbw["nf"] is None and pbw["complete"]:
                rcw = rung_rec(pbw)
                rcw["ev"] = eval_rung(rcw)
                wtabB.append((lab, rcw, pbw))
            else:
                wtabB.append((lab, None, pbw))
        alpha80 = float(core.U_ALL[80])
        ka80 = core.atoms_in(alpha80)
        rng = np.random.default_rng(SCR_SEED_B)
        uu_scr = np.sort(rng.uniform(0.0, 2.0 * alpha80,
                                     size=ka80))
        pscr = wpack_b(80, NU_B,
                       comb=(uu_scr, core.MU_ALL[:ka80].copy()))
        scr_txt = ("frame-B SCRAMBLE @ kz80 (seed %d): nf %s "
                   "(the true kz80 admits at full depth %s) -- "
                   "the dead control %s in the new build"
                   % (SCR_SEED_B, str(pscr["nf"]),
                      str(next((r["N"] for r in b_rows
                                if r["kz"] == 80), "-")),
                      "BREAKS AT ADMISSION"
                      if pscr["nf"] is not None
                      else "unexpectedly admits (census)"))
        wlines = []
        for lab, rcw, pbw in wtabB:
            if rcw is None:
                wlines.append("%s N %d nf %s (NOT admitted)"
                              % (lab, pbw["N"], pbw["nf"]))
                continue
            ev = rcw["ev"]
            mloc = ev["m"]
            lgl = math.log(float(mloc))
            fab_w = QGL.fab_of(float(mloc), ev["mqs"]["qm"],
                               lgl)
            fa_w = CCP.world_coord(ev["mqs"]["qm"], rcw["N"],
                                   N_lad, q_lad)
            rsv_w = GSQ_R321 * fa_w ** 2 \
                / max(ev["rho2"], 1e-300)
            cls_w = FCC.cls_rule(fa_w, F0_SPLIT)
            wlines.append("%s m %d F_ins %.2f %s FAB %.2f (vs "
                          "C_FAB %.2f) RSV %.1f"
                          % (lab, mloc, fa_w, cls_w, fab_w,
                             c_fab89, rsv_w))
            info("world %s" % wlines[-1])
        check("G90-world-census", len(wtabB) == 2,
              "FRAME-B WORLD CENSUS (NO letter; both mains sit "
              "below the H_MIN pool floor in frame B -- census "
              "only, disclosed): %s; %s"
              % ("; ".join(wlines), scr_txt))
    check("G91-e1-nu-posthoc",
          len(e1_hits) >= 1 and mut1 == 8 and mut1 != NU_B,
          "e1 protocol-CAUGHT twice: the family aspect picked "
          "after sight of the violation column -- AST-FLAGGED "
          "(%s) -- and on the toy returns %s != the sealed "
          "NU_B %d (the aspect is sealed in this spec BEFORE "
          "any frame-B evaluation)"
          % (e1_hits[0] if e1_hits else "MISS", str(mut1),
             NU_B))
    check("G92-e2-admit-posthoc",
          len(e2_hits) >= 1 and mut2 == (2, 3)
          and mut2 != toy2_sealed,
          "e2 protocol-CAUGHT twice: the admitted windows "
          "picked after sight -- AST-FLAGGED (%s) -- and on "
          "the toy returns %s != the sealed queue-order rule "
          "%s (the frame-B admission consumes shape + grid + "
          "used ledger ONLY, sealed pre-freeze)"
          % (e2_hits[0] if e2_hits else "MISS", str(mut2),
             str(toy2_sealed)))
    check("G93-e3-killbar-posthoc",
          len(e3_hits) >= 1 and abs(mut3 - 0.5) <= TOY_BAR
          and mut3 != RSV_KILL,
          "e3 protocol-CAUGHT twice: the kill bar read from "
          "the evaluated reserve column -- AST-FLAGGED (%s) -- "
          "and on the toy returns %.1f != the sealed RSV_KILL "
          "%.2f (the kill criterion is sealed BEFORE the "
          "record)"
          % (e3_hits[0] if e3_hits else "MISS", mut3,
             RSV_KILL))
    check("G94-e4-fab-wrong-log",
          mut4 == Fr(3, 8) and toy4_true == Fr(3, 4)
          and toy4_true / mut4 == Fr(2),
          "e4 CAUGHT exact: the wrong log power returns %s on "
          "the sealed Fractions toy while the exact FAB is %s "
          "-- break factor %s == the pseudo-log EXACT"
          % (str(mut4), str(toy4_true),
             str(toy4_true / mut4)))
    check("G95-mincut-unchanged", True,
          "mincut base 4 / refined 5 UNCHANGED; what the round "
          "adds: a second construction family on the canonical "
          "comb with sealed selection + admission, the "
          "cross-family FAB/K2/count tests at frozen "
          "constants, the preregistered erosion kill with the "
          "combined fit, and the two-family composition -- NO "
          "new certificate promoted, NO universal bound "
          "claimed beyond the measured rungs")
    if smoke:
        verd = "SMOKE_NO_ADJUDICATION"
    else:
        parts = ["R353_ANCHORS(identity %.1e, r306 C2 %.3f "
                 "v%d, r316 n %d, dict %.1e, r321 FA "
                 "%.2f/%.2f/%.2f, r349 family %d rc %+.3f + "
                 "EXT4 %s, r324 C_INF %.4f viol %s e_G %+.3f, "
                 "r329 C_NSC %.4f C_NG %.4f, r351 C_FAB %.2f @ "
                 "kz%d + floor %d fam min %.2f e %+.3f)"
                 % (rec3_w, C2r, viol2, n353, dict3_w,
                    fa_kz[53], fa_kz[83], fa_kz[67],
                    len(fam_idx), rc_fam349, str(fins4_sorted),
                    cinf_repro, str(viol_inf), eg_repro,
                    cnsc_repro, cng_repro, c_fab89, c_fab89_kz,
                    len(fam_all89), rsv_min89, e_rsv89)]
        parts.append("SEAL(NORM %.1e, interp %.1e, FAB id "
                     "%.1e, pileup %.1e, group %.1e, decomp "
                     "%.1e, frame-B repro %.1e, purity clean, "
                     "toys exact)"
                     % (norm_w, interp_w, fabid_w, pile_w,
                        grp_w, dec_w, frb_dev))
        parts.append("FAMILY_B(admitted %d/%d, N_w %s..%s, m "
                     "max %d, %d SPIKE, BZ pool %d)"
                     % (len(b_rows), K_BZ + K_BD,
                        min((r["N"] for r in b_rows),
                            default="-"),
                        max((r["N"] for r in b_rows),
                            default="-"),
                        sB["mmax"] if sB else 0,
                        sB["nfam"] if sB else 0, len(b_q)))
        parts.append("FABLAW(C_FAB %.2f viol %d, K2X %.2f viol "
                     "%d, counts %s, grel cand %s)"
                     % (c_fab89, len(fab_viol_b), c_k2x,
                        len(k2_viol_b),
                        "0 viol" if not cnt_viol_b else "VIOL",
                        str(grel_cand)))
        parts.append("EROSION(kill %d, bar-miss %d, e_comb "
                     "%+.3f, rc_comb %+.3f, %s)"
                     % (len(kill_rows), len(bar_miss),
                        e_rsv_comb, rc_comb, fl_letter))
        parts.append(verdict_main)
        parts.append("SYNTHESIS(C_FAB' %.2f, C_M2ENV' %.2f, "
                     "m_0* 10^%s vs record 10^%.1f)"
                     % (c_fab_new, c_m2env_new,
                        ("%.1f" % m0_law_new)
                        if m0_law_new is not None else "NONE",
                        m0_law89))
        parts.append("MUSTFAIL_LEDGER(e1-e4 + scopes)")
        verd = " + ".join(parts)
    npass = sum(1 for _n, ok, _d in CHECKS if ok)
    check("G96-verdict", npass == len(CHECKS),
          "%s%s -- SATZ (machine-gated): the identity, the "
          "chains, the Fractions toys, the NU = 4 reproduction "
          "and the purity audits (exact / AST-decided); "
          "MEASURED: every FAB value, constant, count column, "
          "reserve and violation count (the finite 89-row r351 "
          "surface + the admitted frame-B rows + 2 mains + 2 "
          "live controls + the frame-B worlds); OPEN: any bound "
          "beyond the measured rungs, the cofinal law, the "
          "actual proof (a second construction family buys "
          "independence of the window build, NOT a second "
          "arithmetic world and NOT a cofinal statement); NO "
          "RH claim"
          % (verd, " (SMOKE)" if smoke else ""))
    wall = time.time() - T0_WALL
    check("G99-runtime", wall <= 1800.0,
          "WALL %.1f s (bar 1800)" % wall)
    npass = sum(1 for _n, ok, _d in CHECKS if ok)
    print("\n" + "=" * 78)
    print("RESULT: %d/%d gates PASS%s   SPEC_SHA %s"
          % (npass, len(CHECKS), " (SMOKE)" if smoke else "",
             SPEC_SHA[:16]))
    print("NO RH CLAIM in either direction.")
    print("=" * 78)
    return 0 if npass == len(CHECKS) else 1


if __name__ == "__main__":
    sys.exit(main())
