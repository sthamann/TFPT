#!/usr/bin/env python3
# -*- coding: utf-8 -*-
r"""roadmap_probe -- PRIME.ROADMAP.01: the staged research roadmap,
compiled and machine-grounded.

EXPLORATION ONLY (2026-08-15).  This probe proves nothing about RH and
claims no progress on RH in any direction.  It compiles the corpus's
complete machine-checked map into ONE artifact the program never
produced: a staged, milestone-gated research roadmap from today's
frozen state to a complete RH proof, in which EVERY stage is
falsifiable against the corpus's own instruments.  The probe asserts
every machine-groundable claim of that roadmap: the acceptance-gate
values are re-computed from cited constants or grep-warded verbatim in
their owning frozen artifacts, the stage-dependency DAG is checked
acyclic with exactly the named open stages, and the downstream
extraction chain's artifacts are checked present with their verdicts
grep-verified.  A roadmap is NOT progress on RH; no stage is claimed
started; the mathematics of Stages 1-2 does not yet exist.  NO RH
CLAIM.

===========================================================================
STAGE 0 (DONE -- the frozen state this roadmap starts from)
===========================================================================
  S0.1  The candidate atlas is complete and adjudicated: 24 candidate
        sign sources (untested_sign_sources_probe, tally 19 -> 24,
        0 PASS) extended to 25 by the positive-proportion class round
        (levinson_class_probe, tally 24 -> 25, 0 PASS) -- every named
        unconditional classical theorem type about zero positions is
        formally priced against the wall budget.
  S0.2  The RH implication chain has exactly ONE open edge, E4
        (kill_atlas_dag_probe: predefined mesh-cofinal ladder
        positivity), with four named classical citations carried.
  S0.3  The one open input holds THREE equivalent lemma forms:
        uniform Weyl-disk contraction of one explicit canonical system
        (SUZKREIN-CARRIER-OPEN), the sign positions of one explicit
        ground eigenvector (SIGNPOS-PREDICTOR-EXACT / Parity Lemma),
        and Hausdorff cell positivity of one explicit Pascal field
        (HAUSDORFF-EQUIVALENCE-SOUND).
  S0.4  Two certified falsification instruments, both channels open
        and EMPTY: the Euler-Pick ladder certified through N = 4
        (floors below) and the Hausdorff safe-point field at certified
        depth 86 (gamma* reach 189/563).
  S0.5  The largest proven positivity region of the campaign
        (moonshot_sol_probe, round 103; external cited inputs
        Platt--Trudgian 2021 + Rosser 1941 + the classical gamma_1
        isolation): C_{n,k}(256) > 0 for EVERY n >= 0 and every
        0 <= k <= 10^19.  Re-derived here from the cited constants.
  S0.6  The rigidity law: screw positivity to depth r pins the measure
        onto the prime comb exponentially tightly, ln eta*(r) =
        -1.073 r - 7.08, conditioning-priced (RIGIDITY-TIGHTENING).
  S0.7  The cohomological specification sheet: seven properties, six
        machine-grounded, one citation-typed (COHOMSPEC-COMPILED).
  S0.8  27 kernel-checked Lean theorems around the reduction chain
        (EulerPick / DeltaOneNoGo / ParityLemma / SVSkeleton +
        CofinalWeil / CofinalPredefinition), axioms exactly
        [propext, Classical.choice, Quot.sound].

===========================================================================
STAGE 1 (OPEN -- the substrate; mathematics that DOES NOT YET EXIST)
===========================================================================
The missing mathematics is structural positivity of Hodge-index type
for Spec Z.  Verified literature state (cohomspec_probe round 94/95,
P7): Deninger's dynamical Lefschetz trace formula is PROVED for smooth
foliated flows (Alvarez Lopez--Kordyukov, arXiv:2402.06671) but NO
arithmetic dynamical system realizing Spec Z has been constructed --
the trace formula has no arithmetic host; Connes--Consani have
Riemann--Roch on the periodic orbits C_p of the scaling site and for
the ring Z (Selecta Math. 2017 and sequels) but NOT on the square of
the scaling site -- exactly the surface substitute whose Hodge-index
inequality would be the structural positivity.  Two alternative
milestones (either suffices):
  M1a   An arithmetic dynamical system hosting the proven trace
        formula, whose periodic orbits are log p with weights
        log p / p^{m/2}.  ACCEPTANCE GATE (minutes-scale, machine):
        its trace formula, evaluated on the exponential test class
        f_c(v) = 1_{v>=0} sum_j c_j e^{-sigma_j v}, must reproduce the
        (AC)/Euler--Pick interface c^T P_N c = <-g'', f_c * f_c~> at
        the 16 frozen sigma values of the SVPIN spec (owning
        artifacts: vbk_invariant_probe V0c;
        krein_screw_realization_probe SIGMA GRID).
  M1b   OR Riemann--Roch on the square of the scaling site.
        ACCEPTANCE GATE: the resulting intersection pairing evaluated
        on exponential autocorrelations must reproduce the P_N ladder
        -- the certified floors below (N <= 4) are the acceptance
        values; a mismatch at any certified N kills the candidate in
        minutes.
Frozen acceptance values (outward-rounded interval floors, owning
artifacts eulerpick_ladder_frozen.log / sieve4_run1.log):
  N=1: [4.5917135e-2, 4.5917136e-2]     N=2: [9.0288701e-6, 9.0289075e-6]
  N=3: [2.3643695e-10, 1.1497752e-9]    N=4: [8.278338e-15, 1.3840906e-14]

===========================================================================
STAGE 2 (OPEN -- the index inequality; mathematics that DOES NOT YET
EXIST)
===========================================================================
The Hodge-index/Castelnuovo analog on the Stage-1 substrate: the
structural positivity statement itself.  ACCEPTANCE GATES from the
corpus (each a measured consumption requirement):
  M2a   It must FAIL on the surrogate worlds exactly at the measured
        depths: the prime-free smooth world at window depth r = 0.264
        (support error below log 2) and the weight-scrambled world at
        r = 0.744 (exact log p / p^{m/2} weights necessary) -- a
        positivity surviving either control is structurally incapable
        of being the missing input (vbk_invariant_probe, DEPTHS).
  M2b   It must be signed and alignment-carrying (the v913 class:
        v913_signed_alignment_localization) -- no magnitude, density
        or existence statement can substitute (Littlewood empties the
        magnitude class; density is the emptied E2 class; existence
        closes at border membership).
  M2c   It must survive the tau-screen (frozen bands: PASS |slope| <=
        0.30, RELOCATION >= 0.70) -- a positivity whose margin tracks
        the wall scalar at slope ~1 is a coordinate system, not a
        theorem.
  M2d   Its finite sections must reproduce the certified Hausdorff
        cells (all cells n+k <= 86 positive, 5290 cells) and extend
        the k <= 10^19 theorem's reach -- i.e. strictly enlarge the
        certified region, not rename it.

===========================================================================
STAGE 3 (CONDITIONAL-COMPLETE -- the descent; already kernel-checked
or theorem-grade modulo named classical citations)
===========================================================================
From the Stage-2 index inequality to localized Weil positivity in ANY
ONE of the three lemma forms, then the machine-held extraction to RH.
The corpus already carries every downstream step:
  D1    v848_extraction_chain (PRIME.EXTRACTION.CHAIN.01,
        EXTRACTION-CHAIN-COMPLETE): cofinal finite positivity ==> Weil
        positivity ==> RH, quantifier-reduced, hypothesis (H) isolated.
  D2    v912_form_convergence_theorem: the per-element form
        convergence UNCONDITIONAL with explicit rate O(D^2 log(1/D)).
  D3    The Lean kernel tier: CofinalWeil / CofinalPredefinition
        (extraction + predefinition hardening), EulerPick
        (pickMatrix_posSemidef, forward PSD for every N), SVSkeleton
        (sv_implies_rh + the honesty lock skeleton_not_unconditional),
        ParityLemma, DeltaOneNoGo.
  D4    The SVPIN skeleton (SVPIN-ROUTE-OPEN): Herglotz/Vitali/
        identity-theorem chain sound gate by gate, burden in (SV).
  D5    The Hausdorff equivalence (HAUSDORFF-EQUIVALENCE-SOUND): RH
        <=> all-cell positivity at the safe point, skeleton audited.
  D6    The CCM interface (CCM-SAME-CURRENCY): the one external
        published reduction reproduces the Euler-Pick matrices
        entrywise -- any external candidate lands in the same currency.
No step of Stage 3 is new mathematics; what Stage 3 consumes is
exactly the Stage-2 output, in any one lemma form.

===========================================================================
THE FALSIFICATION SPINE (parallel, permanent)
===========================================================================
The two certified instruments keep running as standing disproof
channels (a single certified negative would be an RH-disproof
candidate): Euler-Pick N <= 4 (open and empty), Hausdorff certified
depth 86 (open and empty).  The named extension walls: the Euler-Pick
N = 5 rung is KNOWLEDGE-walled at the psi-envelope height T = 1e19
(the beyond-1e19 floor exceeds lambda_5 by 4.7e5 at any sieve cap --
no compute helps; sieve4_run1.log G21); the Form-A n = 0 mechanism
exhausts at k ~ 1.405e19 (re-derived here: the elementary bound
crosses the tail at 1.40e19 < k* < 1.41e19) -- extending either wall
is new knowledge, not new hardware.

===========================================================================
THE STAGE DAG AND THE HONEST PRICING
===========================================================================
  S0 -> {M1a | M1b} -> S2 -> S3 -> RH,  with the falsification spine
  parallel to every stage.
Open stages: EXACTLY {S1a, S1b, S2} (S1 closes if either milestone
lands).  S3 is conditional-complete (kernel-checked or theorem-grade
modulo the four named classical citations).  Stage 1 needs NEW
mathematics (an arithmetic host or the square-site Riemann--Roch --
verified as not existing in the literature today); Stage 2 needs NEW
mathematics (the structural positivity itself -- the corpus proves its
interface and its obstructions, not its existence); Stage 3 is
assembly, already machine-held.  NO timeline is promised, NO RH claim
is made, and the roadmap's value is exactly this: every step is now
FALSIFIABLE and INTERFACE-DEFINED, so any candidate, from anyone, can
be adjudicated in minutes against frozen gates.

VERDICT ENUM (frozen): ROADMAP-COMPILED(stages, gates) iff all gates
pass; ROADMAP-INSTRUMENT-EDGE otherwise.  Writes nothing but stdout.
No verification/, no ledger, no manifest.  NO RH CLAIM.
"""

from __future__ import annotations

import hashlib
import os
import time

import mpmath as mp

HERE = os.path.dirname(os.path.abspath(__file__))
REPO = os.path.abspath(os.path.join(HERE, "..", ".."))
LEAN = os.path.join(REPO, "experiments", "lean4-carrier-rigidity",
                    "TfptCarrier")
VERIF = os.path.join(REPO, "verification")

FROZEN_SPEC = __doc__
SPEC_SHA = hashlib.sha256(FROZEN_SPEC.encode("utf-8")).hexdigest()
START = time.time()
N_CHECKS_EXPECTED = 29

mp.mp.dps = 50

CHECKS: list[tuple[str, bool]] = []


def check(name: str, condition: bool, detail: str) -> None:
    result = bool(condition)
    CHECKS.append((name, result))
    print("  [%s] %-34s %s" % ("PASS" if result else "FAIL", name, detail))


def section(title: str) -> None:
    print("\n" + "=" * 74)
    print(title)
    print("=" * 74)


def blob(*relpath: str) -> str:
    path = os.path.join(*relpath)
    with open(path, "r", encoding="utf-8", errors="replace") as handle:
        return handle.read()


def ward(text: str, *tokens: str) -> bool:
    return all(token in text for token in tokens)


# ---------------------------------------------------------------- Form A
# re-derivation from cited constants only (PT height, Rosser constants,
# gamma_1 isolation) -- no zero table, no zeta evaluator.
A_SAFE = mp.mpf(256)
A_SHIFT = A_SAFE - mp.mpf("0.25")
T_PT = mp.mpf("3000175332800")
GAMMA1_LO = mp.mpf("14.1347")
GAMMA1_HI = mp.mpf("14.1348")
R_INT = mp.mpf(3) / 2
K_RECT = mp.mpf(10) ** 19


def rosser_error(t: mp.mpf) -> mp.mpf:
    return (mp.mpf("0.137") * mp.log(t)
            + mp.mpf("0.443") * mp.log(mp.log(t)) + mp.mpf("1.588"))


def tail_bound(n: int) -> mp.mpf:
    log_scale = mp.log(T_PT / (2 * mp.pi))
    if n == 0:
        return A_SAFE / mp.pi * (log_scale + 1) / T_PT
    ratio = A_SAFE / (T_PT ** 2 + A_SHIFT)
    return (A_SAFE * (n + 1) / (2 * mp.pi * n) * log_scale / T_PT
            * ratio ** n)


def count_half_lower(theta: mp.mpf) -> mp.mpf:
    gstar = mp.sqrt(A_SAFE * theta)
    return (gstar * (R_INT - 1 / R_INT) / (2 * mp.pi)
            * mp.log(gstar / (2 * mp.pi * R_INT)) / 2)


def beta_endpoint_constant() -> mp.mpf:
    return mp.mpf("0.5") * min(R_INT ** 2 * mp.exp(-R_INT ** 2),
                               R_INT ** -2 * mp.exp(-R_INT ** -2))


def main() -> int:
    print("roadmap_probe -- PRIME.ROADMAP.01 (the staged roadmap, compiled)")
    print("SPEC_SHA %s" % SPEC_SHA[:16])
    print("NO RH CLAIM. EXPLORATION ONLY. A roadmap is not progress on RH.")

    disc = HERE

    section("S0  STAGE 0 -- THE FROZEN STATE (grep-warded + re-derived)")

    uss = blob(disc, "untested_sign_sources_probe.py")
    lev = blob(disc, "levinson_class_probe.py")
    lev_log = blob(disc, "levinson_class_probe.run.log")
    check("G01 atlas tally 19->24->25, 0 PASS",
          ward(uss, "gate tally 19 -> 24", "-> 24 candidates")
          and ward(lev, "24 -> 25, 0 PASS")
          and ward(lev_log, "tally 24 -> 25, 0 PASS",
                   "LEVINSON-ADJUDICATED"),
          "untested_sign_sources + levinson_class own the tally chain")

    atlas = blob(disc, "kill_atlas_dag_probe.py")
    check("G02 exactly one open edge E4",
          ward(atlas, "THE FIRST OPEN EDGE IS E4", "E4  MPOS_FIN"),
          "kill_atlas_dag_probe: E4 = predefined mesh-cofinal positivity")

    krein = blob(disc, "krein_screw_realization_probe.py")
    signpos = blob(disc, "signpos_probe.py")
    hs_log = blob(disc, "hausdorff_safepoint_probe.frozen.log")
    check("G03 three equivalent lemma forms",
          ward(krein, "SUZKREIN-CARRIER-OPEN(lemma: uniform Weyl-disk")
          and ward(signpos, "SIGNPOS-PREDICTOR-EXACT")
          and ward(hs_log, "HAUSDORFF-EQUIVALENCE-SOUND"),
          "Weyl-disk contraction / sign positions / Hausdorff cells")

    ep_log = blob(disc, "eulerpick_ladder_frozen.log")
    floors_123 = (
        ("N=1", "4.5917135e-2", "4.5917136e-2"),
        ("N=2", "9.0288701e-6", "9.0289075e-6"),
        ("N=3", "2.3643695e-10", "1.1497752e-9"),
    )
    ok_ladder = all(
        ward(ep_log, "%s  lo=%s" % (tag, lo), "hi=%s" % hi,
             "CERTIFIED POSITIVE")
        and 0 < float(lo) <= float(hi)
        for tag, lo, hi in floors_123)
    check("G04 Euler-Pick floors N<=3 certified", ok_ladder,
          "outward-rounded intervals, all lo > 0 (frozen log verbatim)")

    s4_log = blob(disc, "sieve4_run1.log")
    n4_lo, n4_hi = 8.278338e-15, 1.3840906e-14
    check("G05 Euler-Pick N=4 certificate at 1e13",
          ward(s4_log, "N=4  lo=8.278338e-15", "hi=1.3840906e-14",
               "346065536839 (published 346065536839)")
          and 0 < n4_lo < n4_hi,
          "lambda_min(P_4) in [8.278e-15, 1.384e-14]; pi(1e13) pinned")

    check("G06 Hausdorff certified depth 86",
          ward(hs_log, "d_max = 86", "5290 cells certified",
               "gamma* reach (main, a=256) = 189.3",
               "gamma* reach (arm, a=1024) = 563.4"),
          "5290 cells, gamma* 189/563 -- the cheapest certified instrument")

    rig_log = blob(disc, "rigidity_inverse_frozen.log")
    check("G07 rigidity law frozen",
          ward(rig_log, "ln eta*(r) = -1.0730 r -7.0783",
               "RIGIDITY-TIGHTENING", "c_mod=1.073"),
          "pinning law = conditioning price up to O(1)")

    cohomspec = blob(disc, "cohomspec_probe.py")
    check("G08 specification sheet compiled",
          ward(cohomspec, "COHOMSPEC-COMPILED(7 properties: "
                          "6 machine-grounded"),
          "seven properties, six machine-grounded, one citation-typed")

    lean_files = ["EulerPick.lean", "DeltaOneNoGo.lean", "ParityLemma.lean",
                  "SVSkeleton.lean", "CofinalWeil.lean",
                  "CofinalPredefinition.lean"]
    lean_ok = all(os.path.isfile(os.path.join(LEAN, name))
                  for name in lean_files)
    sv_lean = blob(LEAN, "SVSkeleton.lean")
    ep_lean = blob(LEAN, "EulerPick.lean")
    check("G09 Lean kernel tier present",
          lean_ok and ward(sv_lean, "skeleton_not_unconditional",
                           "sv_implies_rh")
          and ward(ep_lean, "pickMatrix_posSemidef"),
          "6 modules incl. the honesty lock and the forward PSD theorem")

    # -- the 1e19 theorem, re-derived from cited constants ----------------
    b0 = tail_bound(0)
    b1 = tail_bound(1)
    d_const = beta_endpoint_constant()
    y_lo = A_SAFE / (A_SAFE + GAMMA1_HI ** 2)
    q_lo = GAMMA1_LO ** 2 / (A_SAFE + GAMMA1_LO ** 2)
    bridge_24 = y_lo * q_lo ** 24 / b0
    hcount_25 = (2 * count_half_lower(mp.mpf(25))
                 / (4 * rosser_error(R_INT * mp.sqrt(A_SAFE * 25))))
    row0_ratio = count_half_lower(K_RECT) * d_const / K_RECT / b0
    low_ratio = y_lo ** 2 * q_lo ** 80 / b1
    theta_max = K_RECT / 2
    high_ratio = (count_half_lower(theta_max)
                  * (d_const / theta_max) ** 2) / b1
    top_ok = R_INT * mp.sqrt(A_SAFE * theta_max) < T_PT
    moonshot = blob(disc, "moonshot_sol_probe.py")
    check("G10 the k<=1e19 theorem re-derived",
          bridge_24 > 1 and hcount_25 > 1 and row0_ratio > 1
          and low_ratio > 335 and high_ratio > 1416 and top_ok
          and ward(moonshot, "0<=k<=10^19"),
          "row0 %.6f, bridge %.3f, low %.1f, high %.1f (all > bars)"
          % (float(row0_ratio), float(bridge_24), float(low_ratio),
             float(high_ratio)))

    ratio_140 = (count_half_lower(mp.mpf("1.40e19")) * d_const
                 / mp.mpf("1.40e19")) / b0
    ratio_141 = (count_half_lower(mp.mpf("1.41e19")) * d_const
                 / mp.mpf("1.41e19")) / b0
    check("G11 the n=0 mechanism wall located",
          ratio_140 > 1 > ratio_141,
          "elementary bound crosses the tail between k = 1.40e19 and "
          "1.41e19 (ratios %.6f / %.6f)"
          % (float(ratio_140), float(ratio_141)))

    section("S1  STAGE 1 -- SUBSTRATE MILESTONES (interfaces + literature)")

    vbk = blob(disc, "vbk_invariant_probe.py")
    svpin = blob(disc, "stieltjes_vitali_pin_probe.py")
    check("G12 M1a acceptance interface frozen",
          ward(vbk, "c^T Pick_N c = <-g'', f_c*f_c~>.")
          and ward(krein, "SIGMA GRID (16 values",
                   "SIGMAS = (0.6, 0.75, 0.9,")
          and ward(svpin, "SIGMAS = (0.6, 0.75, 0.9,"),
          "the (AC) identity + the 16 frozen sigma of the SVPIN spec")

    check("G13 M1a literature state verified",
          ward(cohomspec, "arXiv:2402.06671", "math/0505354")
          and "no arithmetic system" in cohomspec,
          "trace formula proved for smooth flows; NO arithmetic host")

    check("G14 M1b literature state verified",
          ward(cohomspec, "square of the scaling site",
               "square, which is exactly the missing positivity substrate"),
          "Riemann-Roch on orbits/Z but NOT on the square (cited)")

    parsed = [(float(lo), float(hi)) for _t, lo, hi in floors_123]
    parsed.append((n4_lo, n4_hi))
    monotone = all(parsed[i][0] > parsed[i + 1][1]
                   for i in range(len(parsed) - 1))
    check("G15 M1b acceptance ladder numeric",
          monotone and all(lo > 0 for lo, _h in parsed),
          "P_N floors N=1..4 positive and strictly ordered -- any "
          "candidate pairing is checked against these values in minutes")

    section("S2  STAGE 2 -- INDEX-INEQUALITY GATES (consumption reqs)")

    check("G16 M2a control-death depths",
          ward(vbk, "SMOOTH_EXIT = 0.264",
               "DEPTHS = (0.264, 0.744",
               "r=0.264 and r=0.744"),
          "must fail smooth at r = 0.264 (support) and scramble at "
          "r = 0.744 (weights)")

    v913 = blob(VERIF, "v913_signed_alignment_localization.py")
    check("G17 M2b signed/alignment class",
          ward(v913, "signed", "alignment") and ward(atlas, "v913"),
          "the v913 isolate class, tied to E4 in the kill atlas")

    ranktrace = blob(disc, "anthropic_ranktrace_core_probe.py")
    check("G18 M2c tau-screen bands frozen",
          ward(atlas, "PASS iff |s| <= 0.30", "RELOCATION iff s >= 0.70")
          and ward(ranktrace, "SLOPE_PASS = 0.30", "SLOPE_RELOC = 0.70"),
          "PASS |slope| <= 0.30 / RELOCATION >= 0.70")

    check("G19 M2d finite-section reproduction targets",
          ward(hs_log, "all cells n+k <= d certified positive")
          and row0_ratio > 1,
          "Hausdorff cells to depth 86 + the k <= 1e19 region as the "
          "floor any Stage-2 candidate must strictly enlarge")

    section("S3  STAGE 3 -- THE DESCENT (already machine-held)")

    v848 = blob(VERIF, "v848_extraction_chain.py")
    v912 = blob(VERIF, "v912_form_convergence_theorem.py")
    check("G20 extraction chain + rate theorem",
          ward(v848, "PRIME.EXTRACTION.CHAIN.01",
               "EXTRACTION-CHAIN-COMPLETE")
          and ward(v912, "O(D^2 log(1/D))"),
          "v848 (chain, hypothesis (H) isolated) + v912 (rate theorem)")

    check("G21 SVPIN skeleton", "SVPIN-ROUTE-OPEN" in svpin,
          "Herglotz/Vitali/identity-theorem chain, burden in (SV)")

    check("G22 Hausdorff equivalence",
          "HAUSDORFF-EQUIVALENCE-SOUND(" in hs_log,
          "RH <=> all-cell positivity at the safe point, audited")

    ccm = blob(disc, "ccm_pn_adjudication_probe.py")
    check("G23 CCM same-currency interface",
          "CCM-SAME-CURRENCY" in ccm,
          "the external P(n) reduction lands entrywise in Euler-Pick")

    check("G24 Lean descent composition",
          ward(sv_lean, "theorem sv_implies_rh",
               "theorem skeleton_not_unconditional"),
          "sv_implies_rh + the honesty lock (packaging alone yields "
          "nothing)")

    section("SP  THE FALSIFICATION SPINE (open-and-empty + named walls)")

    check("G25 both channels open and empty",
          "CERTIFIED NEGATIVE" not in ep_log
          and "CERTIFIED NEGATIVE" not in s4_log
          and ward(ep_log, "CERTIFIED POSITIVE")
          and ward(hs_log, "d_max = 86"),
          "no certified negative anywhere; a single one would be an "
          "RH-disproof candidate")

    check("G26 the extension walls named",
          ward(s4_log, "KNOWLEDGE-walled at T=1e19",
               "floor/lambda_5=4.7e+05")
          and ward(moonshot, "does not touch the k->infinity quantifier"),
          "Euler-Pick N=5 = psi-knowledge wall; Form-A n=0 mechanism "
          "exhausts at ~1.405e19 (G11)")

    check("G27 the classical census closed",
          ward(lev_log,
               "kappa*_ledger = ['0.995169', '0.998287', '0.998816']"),
          "positive-proportion class priced final: useless below "
          "kappa* ~ 0.995-0.999 -- every named classical type now "
          "adjudicated")

    section("DAG  THE STAGE GRAPH")

    edges = {
        "S0": ["S1a", "S1b"],
        "S1a": ["S2"],
        "S1b": ["S2"],
        "S2": ["S3"],
        "S3": ["RH"],
        "RH": [],
    }
    status = {"S0": "DONE", "S1a": "OPEN", "S1b": "OPEN", "S2": "OPEN",
              "S3": "CONDITIONAL-KERNEL-CHECKED", "RH": "TARGET"}
    # Kahn topological sort -> acyclicity
    indeg = {node: 0 for node in edges}
    for node in edges:
        for succ in edges[node]:
            indeg[succ] += 1
    queue = [node for node in edges if indeg[node] == 0]
    seen = 0
    while queue:
        node = queue.pop()
        seen += 1
        for succ in edges[node]:
            indeg[succ] -= 1
            if indeg[succ] == 0:
                queue.append(succ)
    acyclic = seen == len(edges)
    open_stages = sorted(node for node, st in status.items()
                         if st == "OPEN")
    # every path S0 -> RH passes through S2 and S3, and through S1a or S1b
    def paths(node: str) -> list[list[str]]:
        if node == "RH":
            return [["RH"]]
        return [[node] + tail for succ in edges[node]
                for tail in paths(succ)]

    all_paths = paths("S0")
    gated = all(("S2" in p and "S3" in p and ("S1a" in p or "S1b" in p))
                for p in all_paths)
    check("G28 stage DAG acyclic, open = {S1a,S1b,S2}",
          acyclic and open_stages == ["S1a", "S1b", "S2"] and gated
          and status["S3"] == "CONDITIONAL-KERNEL-CHECKED",
          "%d paths S0->RH, all through S1->S2->S3; no other route"
          % len(all_paths))

    section("V  PATTERN GATE + COMPOSITE VERDICT")
    prior_count = len(CHECKS)
    prior_pass = sum(ok for _n, ok in CHECKS)
    pattern_ok = (prior_count == N_CHECKS_EXPECTED - 1
                  and prior_pass == prior_count)
    check("G29 pattern gate",
          pattern_ok,
          "expected %d prior checks with zero fails (got %d, %d fails)"
          % (N_CHECKS_EXPECTED - 1, prior_count,
             prior_count - prior_pass))

    elapsed = time.time() - START
    total_pass = sum(ok for _n, ok in CHECKS)
    print("\n  checks %d/%d PASS; runtime %.2f s; SPEC_SHA %s"
          % (total_pass, len(CHECKS), elapsed, SPEC_SHA[:16]))
    if total_pass == len(CHECKS):
        print("\nVERDICT: ROADMAP-COMPILED(stages: S0 DONE; S1a|S1b OPEN "
              "(new mathematics required); S2 OPEN (new mathematics "
              "required); S3 CONDITIONAL-KERNEL-CHECKED; gates %d/%d)"
              % (total_pass, len(CHECKS)))
    else:
        print("\nVERDICT: ROADMAP-INSTRUMENT-EDGE(%d/%d)"
              % (total_pass, len(CHECKS)))
    print("NO RH CLAIM. A roadmap is not progress on RH; no stage is "
          "claimed started; the mathematics of Stages 1-2 does not yet "
          "exist.")
    return 0 if total_pass == len(CHECKS) else 1


if __name__ == "__main__":
    raise SystemExit(main())
