#!/usr/bin/env python3
# -*- coding: utf-8 -*-
r"""pi_pattern_scan_probe -- PI.PATTERN.SCAN.01

FROZEN SPEC (2026-08-21).  EXPLORATION ONLY.  EXPLORATORY TRANSFER.
This probe writes no verification module, paper, ledger, website,
manifest, Lean file or status marker.  It makes NO RH claim in any
direction, NO pi-numerology claim, NO normality claim about pi, NO
positivity claim.  It closes no gate and narrows no gate.  It is NOT
prime-front progress: it tests whether the prime-front instruments
are prime-SPECIFIC by pointing them at pi-derived data, where an
honest null (pi lands in the fake-world class) is the expected and
fully publishable-in-diary outcome.

=======================================================================
MISSION (transfer round: PI.PATTERN.SCAN.01).  The 183-round prime
front built detectors that separate the true arithmetic world (the
von Mangoldt comb) from smooth/scrambled/wrong-arithmetic worlds:
the MASS-LOCATION world separator (r181 fraction ladder + r182 mass
center: only the arithmetic jet rides the collapsing end of the
house Gram spectrum; every fake world and every deterministic
deformation of the true ray keeps its mass in the top modes), the
spike instrument (r174 Landau field), the DC/equidistribution leg
(r169), the occupancy law (r180), and the alt-jet dissection (r182
SIGNFLIP/UNIFORM/MAGSCRAM).  THIS round transfers those instruments
UNCHANGED in structure to pi-derived combs and asks, honestly: does
any house detector see structure in pi that matched null models do
not have -- or does pi land cleanly in the fake-world class (itself
a clean specificity result: the detectors are prime-specific, not
universal-structure detectors)?  PREDEFINITION IS THE RULE: every
comb construction, control, cut, and verdict bar below is frozen
BEFORE any record evaluation; the ONE disclosed pre-freeze
calibration pass (house protocol, r181/r182 precedent) is kept as
pi_pattern_scan_probe.calib1.log and every bar retyped at freeze
from it is disclosed in the AMENDMENT block at the bottom of this
spec.  Every pi test runs against the full control battery inside
the same instrument.  No post-hoc weight shopping: ONE frozen
construction per world.
=======================================================================
State consumed (CITED, read-only): r182/D alignment_law_probe (SPEC
e4cdb9a932093196: the w_ctrl mass-location recipe VERBATIM at
CTRL_NZ = 300, CTRL-anchor record strings CAL_CTRL_TAIL MAIN
{4: -4.11, 5: -2.26, 8: -0.04} and CAL_CTRL_LOC MAIN topshare/mu_m
{4: (0.0041, -4.1), 5: (0.0098, -8.3), 8: (0.0082, -13.2)}; ALT-JET
recipes SIGNFLIP/UNIFORM/MAGSCRAM + golden permutation + projective
Dg-recovery scal_k = Jn_k A0/d_k; fake-world class tails
-11.7..-14.3); r181/CDXCIX cbj_subdof_probe (SPEC 2db82c76ce5f067c:
the 1e-12 primary cut; the fraction ladder as world separator);
r180/CDXCVII cbj_frame_probe (SPEC d7fbf2d9: step-A delta == |J|^2_G;
the house Gram mu(g) = sin^2(ag)/g^2, psi_k = g^2/(g^2 - b_k));
r174/CDLXXXX gonek_pricing_probe class (the spike instrument
S(x, T) = sum_{gamma <= T} cos(gamma log x); Landau 1912 Math. Ann.
71, 548-564: sum_{0<gamma<=T} x^rho = -(T/2pi) Lambda(x) + O(log T)
for fixed x > 1, UNCONDITIONAL; Gonek 1993 Contemp. Math. 143,
395-413 uniform error form); r169 sigmafloor DC/equidistribution
style; R4 = radius4_an_probe build_cell (r122 machinery; MAIN =
prime-power atoms (log q, log p/sqrt q), even sector, the SAME
builder the whole prime front rides).  CLASSICAL LAWS CITED for D4:
Gauss-Kuzmin (Kuzmin 1928; Levy 1929: P(a = j) =
log2(1 + 1/(j(j+2)))), Levy's constant (Levy 1936: (ln q_k)/k ->
pi^2/(12 ln 2) = 1.1865691104... a.s.), Khinchin's constant
(Khinchin 1934: GM(a_1..a_N) -> K0 = 2.685452001065306... a.s.),
Chebyshev/PNT with Rosser-Schoenfeld 1962 bounds for psi(x).
NOTE: pi, e, sqrt2, phi are SPECIFIC numbers -- the a.s. laws do
not apply to them individually; for pi/e they are measured
expectations (pi conjecturally GK-typical; e provably NOT GK-typical
-- its CF is [2; 1,2,1,1,4,1,1,6,...]); sqrt2/phi have periodic CFs
(provably atypical).  This is exactly why they are CONTROLS.

NOTATION (r180/r181/r182 conventions VERBATIM).  Rung h; a =
log(h)/2; L2v = 2a; K = ceil(1.25 h log h); om_k = k pi/a; b_k =
om_k^2; even sector.  WALL M = Mpole + March - Mprime where the
world enters ONLY through its comb atoms {(u_j, w_j)} via pj_i =
sum_j w_j sin(om_i u_j) (off-diagonal) and the psi_d diagonal sums
(build_cell even-sector recipe, ported verbatim and PORT-GATED
against R4.build_cell mpPrime on the MANGOLDT world, dev <= 1e-25).
Source c = lowest eigenvector of M (sign-fixed at the largest
component, /nrm); d_k = (-1)^k c_k; A_0 = sum d; J = d/A_0.  GRAM
(world-blind): mu(g) = sin^2(ag)/g^2, psi_k(g) = g^2/(g^2 - b_k)
(psi_0 == 1), Gm[k,l] = sum mu psi_k psi_l over ward ordinates in
(2 pi h + 6, gamma_CTRL_NZ], CTRL_NZ = 300 (the r182 control-battery
window -- LIKE-FOR-LIKE with the cited anchor strings); D =
sqrt(diag Gm); Gn = D^-1 Gm D^-1; Jn_k = J_k D_k / sqrt(sum mu).
LAW: eigsy(Gn) at EIG_DPS = ndig + 30; s_i = v_i . Jn; m_i =
lam_i s_i^2; delta == sum m_i (mass ward <= 1e-6 vs the direct
quadratic form).  OBSERVABLES per world per rung: log10tail =
log10(1 - frac) at the primary 1e-12 cut (frac = mass fraction on
lam_i >= 1e-12 lam_max), topshare, mu_m = mass-weighted mean of
log10(lam_i/lam_max) over positive-lam modes, gmin.

=======================================================================
THE WORLD REGISTRY (ALL FROZEN; one construction per world)
=======================================================================
LADDER: HRUNGS = (4, 5, 6, 7, 8) (5 sizes); DPS = {4: 60, 5: 60,
6: 65, 7: 70, 8: 80} (house table).  DENSE-GRID CONSTRUCTION (the
one frozen data->comb map for stream worlds): N_h = 24 h atoms at
positions u_j = j L2v/(N_h + 1), j = 1..N_h (all interior; comb
Nyquist pi (N_h+1)/L2v > 4 x top house frequency at every rung,
gated); weights w_j = v_j * SCALE with v_j the world's centered
data value (below) and SCALE = W1_MAIN(h)/sum_j |v_j|, W1_MAIN(h) =
sum_{q <= h} Lambda(q)/sqrt(q) (the MAIN comb's L1 atom mass at the
same rung -- a pure prime-sieve SCALAR used as scale anchor so every
world perturbs the wall in the same operator-norm regime; disclosed;
mass-location observables are projective in the JET but the wall
eigenvector is not scale-free, hence the anchor).  HOLDOUT: HALF-A
uses stream values 1..N_h, HALF-B uses values 385..384+N_h (stream
prefix of length 768 splits disjointly at every rung since
384 + 192 = 576 <= 768).  Anomalies must replicate on HALF-B.

PI WORLDS:
  P-DIG10   dense grid, v_j = d_j - 4.5, d_j = j-th decimal digit
            of pi after the point.
  P-DIG2    dense grid, v_j = 2 b_j - 1, b_j = j-th binary digit of
            pi after the point.
  P-CF      dense grid, v_j = ln a_j - ln K0 (Khinchin-centered),
            a_j = j-th CF partial quotient of pi (a_0 = 3 excluded).
  P-BBP     structural: atoms at u = ln n for every integer n =
            8k + j <= h with j in {1, 4, 5, 6}, k >= 0, n >= 2;
            raw weight c_j 16^{-k}/sqrt(n), c = {1: +4, 4: -2,
            5: -1, 6: -1} (the BBP series term structure, pi's one
            known arithmetic digit-extraction handle); L1-rescaled
            to W1_MAIN.  No holdout (deterministic finite
            structure; disclosed).
  P-CFGEOM  structural: q_k = CF convergent denominators of pi;
            atoms at u_k = L2v ln(q_k)/ln(q_{N_h + 1}), k = 1..N_h
            (interior by construction); uniform raw weights,
            L1-rescaled to W1_MAIN.  Predicted (Levy) to be a
            near-uniform grid = smooth-world signature; HALF-B
            rebuilds q' from quotients a_385.. with a fresh
            recursion.
CONTROLS (same constructions, same instrument):
  C-E-*, C-SQRT2-*, C-PHI-*: e, sqrt2, phi through DIG10, DIG2, CF,
            CFGEOM.  phi-CF = all 1s (maximally structured),
            sqrt2-CF = all 2s (periodic), e-CF = [1,2m,1] pattern
            (semi-structured) -- structure alone must NOT count as
            a pi anomaly; these calibrate that.
  C-CHAMP   Champernowne C10 digits through DIG10 (+ binary
            Champernowne through DIG2): normal by construction but
            highly structured (discrepancy O(1/log N)).
  C-LIOUV   Liouville-like ultrafast CF: a_j = 10^j through the CF
            construction (v_j = j ln 10 - ln K0).
  C-RAND    20 seeds (0..19), np.random.default_rng: RAND-DIG10
            digits uniform{0..9}; RAND-DIG2 = RAND-DIG10 digit mod 2
            (disclosed derivation, uniform marginal); RAND-CF
            quotients sampled from the exact Gauss-Kuzmin law
            (x = 2^u - 1, a = floor(1/x), u uniform -- matched
            marginal); RAND-CFGEOM = convergent recursion q_k =
            a_k q_{k-1} + q_{k-2} on the RAND-CF quotients.
  C-MANGOLDT the true von Mangoldt comb at matched size ==
            R4.build_cell MAIN world VERBATIM (prime-power atoms
            (log q, log p/sqrt q), q <= h).  THE POSITIVE CONTROL:
            if it does not fire at the transferred scale the
            transfer is instrument-dead and the gate fails
            honestly.  No holdout (not a data stream).
  C-INTRAND (D2 only) 20 seeds: the SAME integer positions as the
            depth-4096 MANGOLDT spike comb with sign-randomized,
            magnitude-permuted MANGOLDT weights -- the
            integer-position-matched null that prevents a false
            "anomaly" from integer-support resonance alone (any
            integer-position comb partially rides the Landau
            spikes; P-BBP is adjudicated against BOTH this null
            and the fake-class bars).
DEPTHS (frozen; honest about compute): D1/D5 combs consume <= 576
stream values per world; D2 spike combs 4096 values per half (grid
positions on [0.5, 8.0]); D3 digits 1e5 per base (halves 5e4),
CF 1e4 terms (halves 5e3); D4 rides the D2/D3 data.  Digit source:
mpmath at dps 100010 (pi, e, sqrt2, phi); binary digits from
floor(frac * 2^100000); CF terms by INTERVAL EUCLID on the two
rational bounds floor/ceil(x 10^26000)/10^26000 -- only terms on
which both bounds agree are kept (rigorous prefix; gated >= 1e4 for
pi and e; sqrt2/phi CFs generated exactly from their known periodic
forms, disclosed).

=======================================================================
THE DETECTOR BATTERY (frozen bars; calibrated values disclosed in
the AMENDMENT block)
=======================================================================
D1 MASS-LOCATION (primary; r181/r182 transferred verbatim).  Per
world per rung: log10tail at the 1e-12 cut, topshare, mu_m, gmin.
CLASSIFICATION (frozen; the bars sit inside the r182-measured gap
MAIN -4.1/-2.3/-0.04 vs fake -11.7..-14.3):
  TAIL-RIDING  log10tail(4) >= -6.0 AND log10tail(8) >= -0.5 AND
               ladder non-decreasing with slack 0.05 (arithmetic
               class);
  TOP-HEAVY    log10tail(h) <= -8.0 at ALL rungs (fake-world
               class);
  OTHER        anything else (reported with numbers).
TRANSFER VALIDITY: (i) ANCHOR GATE -- a dedicated MANGOLDT
comparator at dps 60, x in (4, 5, 8), w_ctrl recipe verbatim, must
replicate the r182 record strings (tail tol 0.10 abs, topshare tol
5e-2 abs, mu_m tol 0.2 abs); (ii) C-MANGOLDT classifies TAIL-RIDING
on the house-dps ladder.  PORT GATE: my Mprime assembly on the
MANGOLDT atoms == R4.build_cell mpPrime entrywise (dev <= 1e-25)
and jet agreement (dev <= 1e-25).  PI ANOMALY AT D1 := any pi world
classified TAIL-RIDING on HALF-A that REPLICATES TAIL-RIDING on
HALF-B (P-BBP: no holdout -> flagged PROVISIONAL, needs a follow-up
preregistered round; disclosed).
D2 SPIKE COHERENCE (r174 Landau field transferred).  Z(u) =
sum_{gamma in n7000 cache} cos(gamma u) (float64 instrument,
disclosed).  Null grid: 4096 points u = 0.5 + j*7.5/4097.  Per
world: spike comb (depth 4096; dense worlds carry data in WEIGHTS
on the fixed grid, structural worlds carry their own positions
capped to [0.5, 8.0]); coherence C_W = |sum_j w_j Z(u_j)| /
sqrt(sum_j w_j^2 * mean_grid(Z^2)).  BARS: MANGOLDT-FIRES iff
C_MANG >= 10 x max(null band) (transfer validity); pi anomaly iff
C > 1.25 x max(matched null band) on BOTH halves (P-BBP matched
band = C-INTRAND; dense worlds = RAND family band; P-CFGEOM =
RAND-CFGEOM band).
D3 EQUIDISTRIBUTION/DC (r169 style, classical laws).  Digits base
10 (depth 1e5): Weyl z_m = sqrt(N) |mean_j e^{2 pi i m d_j/10}|,
m = 1..8, statistic = max_m z_m.  Digits base 2: z_mean =
|sum (2b-1)|/sqrt(N), z_lag(l) = |sum s_j s_{j+l}|/sqrt(N), l =
1..4, statistic = max.  CF (depth 1e4): Gauss-Kuzmin chi^2 over
bins {1..9, >= 10}.  Null bands from the 20 matched seeds; pi
anomaly iff stat > 1.25 x max(null) on BOTH halves.
STRUCTURED-CONTROL FIRE GATES (the detector must be able to fire):
C-CHAMP DIG10 statistic >= 3 x max(null); C-PHI CF chi^2 >= 3 x
max(null).
D4 OCCUPANCY/CLUSTER (r180 occupancy transferred to classical
comb laws).  (i) MANGOLDT: psi(4096)/4096 in [0.97, 1.03]
(Rosser-Schoenfeld class) and dyadic occupancy #\{prime powers in
[2^j, 2^{j+1})\}/(2^j/(j ln 2)) in [0.75, 1.25] for j = 8..11.
(ii) P-CFGEOM: |mean_k(ln q_{k+1} - ln q_k) - LEVY| <= LEVY_TOL at
depth 1e4 (LEVY = pi^2/(12 ln 2)); C-PHI gap = ln phi = 0.4812
(must separate, >= 10 sigma of the null spread).  (iii) P-CF:
|GM(a_1..N) - K0| <= KH_TOL; GK chi^2 <= GK_BAR (bars frozen at
calibration as max(20-seed null) x 1.25; AMENDMENT block).
Anomaly = pi DEVIATES from its classical law beyond the null band
(law-consistency is the expected null, not a hit).
D5 ALT-JET RESPONSE (r182 dissection verbatim).  On C-MANGOLDT and
on the best-scoring pi world (frozen selection: max log10tail at
h = 8 on HALF-A; tie -> priority P-BBP > P-CF > P-DIG10 > P-DIG2 >
P-CFGEOM), at rungs (4, 8): SIGNFLIP d' = c; UNIFORM d' = (-1)^k;
MAGSCRAM d'_k = sign(d_k) |c_{perm(k)}|, golden permutation keys
fmod((k+1) GOLD, 1); projective Dg-recovery scal_k = Jn_k A0/d_k.
MANGOLDT alt-jets must relocate to the top (log10tail <= -8 at
h = 8 while MAIN >= -0.5): ALT-JETS-BREAK-ALIGNMENT replicated.
For the pi world: if its jet had structure, sign scrambling kills a
sign-carried (arithmetic-class) signature and spares a structural
one -- reported with numbers, no interpretation beyond them.

=======================================================================
HONEST OUTCOME TAXONOMY (frozen adjudication; the verdict gate is
DEFINITIONAL/ADJUDICATION per the BH6-F3/BH8-F3 convention)
=======================================================================
  PI-LANDS-FAKE-CLASS   all pi worlds TOP-HEAVY on D1 and inside
                        their null bands on D2/D3/D4-anomaly legs
                        (expected default; a clean specificity
                        result for the detectors).
  PI-ANOMALY-AT-<Dk>    a pi world separates from ALL controls
                        including the structured ones at the frozen
                        bar AND replicates on the holdout; numbers
                        only, flagged for a follow-up preregistered
                        round BEFORE any claim.
  TRANSFER-INSTRUMENT-DEAD  C-MANGOLDT fails the anchor gate, the
                        TAIL-RIDING classification, or the D2/D4
                        fire gates -- the transfer itself failed;
                        disclosed as such.
  MIXED                 anything else (per-detector table).
Bars: mass ward <= 1e-6 everywhere; |A0| >= 1e-40 guard (a world
hitting it is reported and skipped, disclosed); runtime <= 2400 s.

AMENDMENT BLOCK (disclosed at freeze; house protocol):
  A0 (pre-freeze, disclosed design choice): the task sheet's P-DIG
  variant "atoms at log n where a digit pattern occurs" was NOT
  chosen; the ONE frozen stream construction is the dense-grid
  weighted comb above (keeps every stream world in the same wall
  class, avoids position-sparsity artifacts at house rungs).  No
  alternative construction was evaluated before or after freezing.
  A1 (at freeze, from the ONE disclosed calibration pass
  pi_pattern_scan_probe.calib1.log, 34/35 at the pre-freeze
  SPEC_SHA e556a9c7825e89ea, runtime 188.2 s; the one calibration
  FAIL was G07-cf-depth, an instrument-depth shortfall, see (iii)):
  (i) the three D4 CAL-PLACEHOLDER bars frozen as 1.25 x the
  20-seed null maxima measured by the pass: GK_BAR = 24.89 (null
  max 19.91), LEVY_TOL = 0.0344 (null max 0.0275), KH_TOL = 0.1177
  (null max 0.094176; the Khinchin null band was computed with the
  identical seed construction and is printed in-run from this
  freeze on -- instrument-completeness addition, no bar semantics
  changed).  (ii) NO other bar, gate, grid, cut, construction or
  tolerance was changed at freeze; the D1/D2/D3/D5 bars, both fire
  gates, the anchor tolerances and the taxonomy rule stood from
  before calibration and HELD (calibration verdicts: MANGOLDT
  ladder -4.11/-2.26/-1.41/-0.49/-0.04 TAIL-RIDING; anchors dev
  <= 0.004; C_MANG = 131.04 vs INTRAND null 12.60; all 100
  non-MANGOLDT world instances TOP-HEAVY in -10.0..-17.9; no OTHER
  class; pi flags NONE on all four anomaly legs).  (iii) CF_DPS
  16000 -> 26000: the e continued fraction reached only 6806
  rigorous terms from 16000 digits (e's quotients grow linearly;
  the depth estimate was Levy-based and wrong for e) -- pure
  instrument depth, same construction; pi's CF terms are unchanged
  by deeper precision (rigorous-prefix property).  (iv) measured
  note, no action: CF:sqrt2 and CF:phi produce IDENTICAL combs
  under the frozen construction (both are constant-quotient
  streams; the L1 scale anchor erases the constant's magnitude,
  leaving the same uniform negative comb) -- disclosed, kept, both
  reported.
"""

from __future__ import annotations

import argparse
import ast
import hashlib
import math
import os
import sys
import time

import mpmath as mp
import numpy as np

HERE = os.path.dirname(os.path.abspath(__file__))
if HERE not in sys.path:
    sys.path.insert(0, HERE)

import radius4_an_probe as R4                 # round-122 machinery

# ---------------------------------------------------------------- frozen
KFAC = 1.25
Z_OVERHANG = 6.0
CTRL_NZ = 300
CUT_PRIMARY = "1e-12"
MASS_WARD = 1e-6
A0_GUARD = 1e-40
RUNTIME_BAR = 2400.0
GOLD = 0.6180339887498949
EIG_PAD = 30
NDIG_CAP = 140

HRUNGS = (4, 5, 6, 7, 8)
DPS = {4: 60, 5: 60, 6: 65, 7: 70, 8: 80}
NATOMS = {h: 24 * h for h in HRUNGS}
STREAM_LEN = 768
HALF_B_OFF = 384
NSEED = 20

# r182 anchor record strings (CITED)
ANCHOR_X = (4, 5, 8)
ANCHOR_DPS = 60
CAL_CTRL_TAIL = {4: "-4.11", 5: "-2.26", 8: "-0.04"}
CAL_CTRL_LOC = {4: ("0.0041", "-4.1"), 5: ("0.0098", "-8.3"),
                8: ("0.0082", "-13.2")}
ANCHOR_TAIL_TOL = 0.10
ANCHOR_TOP_TOL = 5e-2
ANCHOR_MUM_TOL = 0.2
PORT_TOL = 1e-25

# D1 classification bars (frozen pre-calibration)
TAILRIDE_LO4 = -6.0
TAILRIDE_HI8 = -0.5
LADDER_SLACK = 0.05
FAKE_BAR = -8.0

# D2 spike instrument (frozen)
SPIKE_N = 4096
SPIKE_LO, SPIKE_HI = 0.5, 8.0
SPIKE_GRID_N = 4096
MANG_FIRE_RATIO = 10.0
ANOM_RATIO = 1.25

# D3 equidistribution (frozen)
NDIGITS = 100000
NCF = 10000
WEYL_MMAX = 8
LAG_MAX = 4
GK_BINS = 10          # 1..9 + (>=10)
CTRL_FIRE_RATIO = 3.0

# D4 classical laws (frozen; three bars from calibration -- A1)
PSI_X = 4096
PSI_BAND = (0.97, 1.03)
DYADIC_J = (8, 9, 10, 11)
DYADIC_BAND = (0.75, 1.25)
LEVY = math.pi ** 2 / (12.0 * math.log(2.0))
K0 = 2.685452001065306
GK_BAR = 24.89        # A1: 20-seed null max 19.91 x 1.25
KH_TOL = 0.1177       # A1: 20-seed null max 0.094176 x 1.25
LEVY_TOL = 0.0344     # A1: 20-seed null max 0.0275 x 1.25
PHI_SEP_SIGMA = 10.0

# D5 alt-jets (frozen; r182 recipes verbatim)
ALT_TAGS = ("SIGNFLIP", "UNIFORM", "MAGSCRAM")
ALT_RUNGS = (4, 8)
ALT_FAKE_BAR = -8.0

# CF generation (frozen; A1: 16000 -> 26000, e-depth fix)
CF_DPS = 26000
PI_CF_PREFIX = (3, 7, 15, 1, 292, 1, 1, 1, 2, 1, 3, 1)
PI_DIG_PREFIX = "141592653589793238462643383279"
E_DIG_PREFIX = "718281828459045235360287471352"

PI_WORLDS = ("P-DIG10", "P-DIG2", "P-CF", "P-BBP", "P-CFGEOM")
BEST_PI_PRIORITY = ("P-BBP", "P-CF", "P-DIG10", "P-DIG2", "P-CFGEOM")

SPEC_SHA = hashlib.sha256(__doc__.encode("utf-8")).hexdigest()
T0_WALL = time.time()
CACHE_N7000 = os.path.join(HERE, "verified_zeros_n7000.npy")
META_N7000 = os.path.join(HERE, "verified_zeros_n7000_meta.json")

CHECKS: list = []
INFO: list = []


def check(name: str, ok: bool, detail: str) -> bool:
    ok = bool(ok)
    CHECKS.append((name, ok, detail))
    print("  [%s] %-40s %s" % ("PASS" if ok else "FAIL", name, detail),
          flush=True)
    return ok


def info(msg: str) -> None:
    INFO.append(msg)
    print("  [INFO] " + msg, flush=True)


def section(title: str) -> None:
    print("\n" + "-" * 78 + "\n" + title + "\n" + "-" * 78, flush=True)


# ------------------------------------------------------------ firewall
def firewall_audit() -> tuple:
    src = open(os.path.abspath(__file__), "r", encoding="utf-8").read()
    tree = ast.parse(src)
    spans = []
    for node in ast.walk(tree):
        if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
            spans.append((node.name, node.lineno, max(
                getattr(n, "lineno", node.lineno) for n in ast.walk(node))))

    def owners(lineno: int) -> list:
        return [nm for nm, lo, hi in spans if lo <= lineno <= hi]

    forb = {"zeta" + "zero", "siegel" + "z", "siegel" + "theta",
            "n" + "zeros", "gram" + "point"}
    bad = []
    for node in ast.walk(tree):
        nm = None
        if isinstance(node, ast.Attribute):
            nm = node.attr
        elif isinstance(node, ast.Name):
            nm = node.id
        if nm is None:
            continue
        low = nm.lower()
        if low in forb:
            bad.append("forbidden %s @%d" % (nm, node.lineno))
        if low == "zeta":
            bad.append("zeta use @%d" % node.lineno)
        if isinstance(node, ast.Attribute) and nm == "load":
            fns = owners(node.lineno)
            if not any(f.startswith("ward_") for f in fns):
                bad.append("np.load outside ward_ @%d (%s)"
                           % (node.lineno, fns or "module"))
    for node in ast.walk(tree):
        if isinstance(node, (ast.Import, ast.ImportFrom)):
            mods = ([al.name for al in node.names]
                    if isinstance(node, ast.Import)
                    else [node.module or ""])
            for m in mods:
                if m.startswith("verification"):
                    bad.append("import " + m)
    return (not bad), ("; ".join(bad) if bad else
                       "no zero-oracle; NO zeta; np.load only in "
                       "ward_; no verification/ import")


# ------------------------------------------------------------- wards
def ward_cache() -> np.ndarray:
    return np.asarray(np.load(CACHE_N7000), float)


def ward_meta_ok() -> bool:
    return os.path.isfile(CACHE_N7000) and os.path.isfile(META_N7000)


# ========================================================== data layer
def gen_digits(tag: str, n: int) -> np.ndarray:
    """first n decimal digits after the point of pi/e/sqrt2/phi,
    via mpmath at dps n + 10 (deterministic)."""
    with mp.workdps(n + 10):
        x = {"pi": mp.pi, "e": mp.e, "sqrt2": mp.sqrt(2),
             "phi": (1 + mp.sqrt(5)) / 2}[tag]
        fr = x - int(x)
        s = mp.nstr(fr, n + 5, strip_zeros=False)
    ds = s.split(".")[1][:n]
    assert len(ds) == n, (tag, len(ds))
    return np.array([int(c) for c in ds], dtype=np.int64)


def gen_bits(tag: str, n: int) -> np.ndarray:
    """first n binary digits after the point (deterministic)."""
    with mp.workdps(2 * n):
        mp.mp.prec = max(mp.mp.prec, n + 64)
        x = {"pi": mp.pi, "e": mp.e, "sqrt2": mp.sqrt(2),
             "phi": (1 + mp.sqrt(5)) / 2}[tag]
        fr = x - int(x)
        big = int(mp.floor(fr * mp.mpf(2) ** n))
    bs = bin(big)[2:].rjust(n, "0")[:n]
    return np.array([int(c) for c in bs], dtype=np.int64)


def gen_champ_digits(n: int) -> np.ndarray:
    out = []
    k = 1
    while len(out) < n:
        out.extend(int(c) for c in str(k))
        k += 1
    return np.array(out[:n], dtype=np.int64)


def gen_champ_bits(n: int) -> np.ndarray:
    out = []
    k = 1
    while len(out) < n:
        out.extend(int(c) for c in bin(k)[2:])
        k += 1
    return np.array(out[:n], dtype=np.int64)


def cf_interval(num_lo: int, num_hi: int, den: int, kmax: int) -> list:
    """rigorous common-prefix CF of the interval
    [num_lo/den, num_hi/den] by interval Euclid: a term is emitted
    only when both endpoints agree on floor; the interval reverses
    under x -> 1/(x - a)."""
    out = []
    lo_n, lo_d = num_lo, den
    hi_n, hi_d = num_hi, den
    while len(out) < kmax:
        if lo_d == 0 or hi_d == 0:
            break
        qlo, rlo = divmod(lo_n, lo_d)
        qhi, rhi = divmod(hi_n, hi_d)
        if qlo != qhi:
            break
        out.append(int(qlo))
        # x -> 1/(x - a): [lo, hi] -> [1/(hi - a), 1/(lo - a)]
        lo_n, lo_d, hi_n, hi_d = hi_d, rhi, lo_d, rlo
    return out


def gen_cf(tag: str, kmax: int) -> list:
    """CF partial quotients a_1.. (a_0 dropped).  pi/e: interval
    Euclid at CF_DPS digits (rigorous prefix); sqrt2/phi: exact
    periodic forms (disclosed)."""
    if tag == "sqrt2":
        return [2] * kmax
    if tag == "phi":
        return [1] * kmax
    with mp.workdps(CF_DPS + 20):
        x = {"pi": mp.pi, "e": mp.e}[tag]
        den = 10 ** CF_DPS
        plo = int(mp.floor(x * den))
    cf = cf_interval(plo, plo + 1, den, kmax + 1)
    return cf[1:]                                 # drop a_0


def gen_cf_rand(seed: int, kmax: int) -> list:
    """Gauss-Kuzmin-distributed quotients: u uniform(0,1),
    x = 2^u - 1 (Gauss-measure inverse CDF), a = floor(1/x)."""
    rng = np.random.default_rng(seed)
    u = rng.random(kmax)
    x = np.exp2(u) - 1.0
    return [max(1, int(1.0 / xv)) for xv in x]


def gen_dig_rand(seed: int, n: int) -> np.ndarray:
    rng = np.random.default_rng(seed)
    return rng.integers(0, 10, n).astype(np.int64)


def cf_logq(quots: list) -> list:
    """ln q_k of the convergent denominators (exact ints)."""
    qm1, q = 0, 1
    out = []
    for a in quots:
        qm1, q = q, a * q + qm1
        out.append(math.log(q))
    return out


# ======================================================== comb layer
def mangoldt_atoms_f(hi: int) -> list:
    """(log q, Lambda(p)/sqrt q) float prime-power atoms, q <= hi."""
    comp = np.zeros(hi + 1, dtype=bool)
    out = []
    for p in range(2, hi + 1):
        if comp[p]:
            continue
        comp[p * p:: p] = True
        q = p
        while q <= hi:
            out.append((math.log(q), math.log(p) / math.sqrt(q)))
            q *= p
    out.sort()
    return out


def w1_main(h: int) -> float:
    return sum(w for _u, w in mangoldt_atoms_f(int(math.floor(h))))


def stream_values(world: str, half: str) -> np.ndarray:
    """the centered stream v_1..v_STREAM_LEN for a dense-grid world;
    half A = values 1.., half B = values HALF_B_OFF+1.."""
    n = STREAM_LEN + HALF_B_OFF
    fam, tag = world.split(":", 1)
    if fam == "DIG10":
        if tag == "champ":
            d = gen_champ_digits(n)
        elif tag.startswith("rand"):
            d = gen_dig_rand(int(tag[4:]), n)
        else:
            d = DIGITS10[tag][:n]
        v = d.astype(float) - 4.5
    elif fam == "DIG2":
        if tag == "champ":
            b = gen_champ_bits(n)
        elif tag.startswith("rand"):
            b = gen_dig_rand(int(tag[4:]), n) % 2
        else:
            b = BITS2[tag][:n]
        v = 2.0 * b.astype(float) - 1.0
    elif fam == "CF":
        if tag == "liouv":
            # a_j = 10^j handled in log space (overflow-safe)
            v = (np.arange(1, n + 1, dtype=float) * math.log(10.0)
                 - math.log(K0))
        else:
            if tag.startswith("rand"):
                a = gen_cf_rand(int(tag[4:]), n)
            else:
                a = CFS[tag][:n]
            v = np.log(np.array(a, dtype=float)) - math.log(K0)
    else:
        raise ValueError(world)
    off = 0 if half == "A" else HALF_B_OFF
    return v[off: off + STREAM_LEN]


def comb_for(world: str, h: int, half: str) -> list:
    """the frozen world -> comb map at rung h; returns float atoms
    [(u, w)] already L1-rescaled to W1_MAIN(h)."""
    aa = math.log(h) / 2.0
    L2v = 2.0 * aa
    W1 = w1_main(h)
    N = NATOMS[h]
    if world == "MANGOLDT":
        # exact prime-power atoms as ("PP", q, p) markers -> mp-exact
        # (log q, log p/sqrt q) inside assemble_mprime, byte-matching
        # the build_cell recipe (port gate at 1e-25)
        hi = int(math.floor(h))
        comp = np.zeros(hi + 1, dtype=bool)
        nlist = []
        for p in range(2, hi + 1):
            if comp[p]:
                continue
            comp[p * p:: p] = True
            q = p
            while q <= hi:
                nlist.append((q, p))
                q *= p
        nlist.sort()
        return [("PP", q, p) for q, p in nlist]
    if world == "P-BBP":
        cmap = {1: 4.0, 4: -2.0, 5: -1.0, 6: -1.0}
        atoms = []
        k = 0
        while 8 * k + 1 <= h:
            for j, cj in cmap.items():
                nn = 8 * k + j
                if 2 <= nn <= h:
                    atoms.append((math.log(nn),
                                  cj * 16.0 ** (-k) / math.sqrt(nn)))
            k += 1
        s1 = sum(abs(w) for _u, w in atoms)
        return [(u, w * W1 / s1) for u, w in atoms]
    if world.startswith("CFGEOM:"):
        tag = world.split(":", 1)[1]
        if tag.startswith("rand"):
            quots = gen_cf_rand(int(tag[4:]),
                                STREAM_LEN + HALF_B_OFF)
        else:
            quots = CFS[tag][:STREAM_LEN + HALF_B_OFF]
        off = 0 if half == "A" else HALF_B_OFF
        sub = quots[off: off + N + 1]
        lq = cf_logq(sub)
        top = lq[N]
        atoms = [(L2v * lq[k] / top, 1.0) for k in range(N)]
        s1 = float(N)
        return [(u, w * W1 / s1) for u, w in atoms]
    # dense-grid stream worlds
    v = stream_values(world, half)[:N]
    s1 = float(np.sum(np.abs(v)))
    atoms = [((j + 1) * L2v / (N + 1), float(v[j]) * W1 / s1)
             for j in range(N)]
    return atoms


# ===================================================== wall/gram layer
def rung_shared(h: int, dps: int) -> dict:
    """per-rung shared build: build_cell(MAIN) once (geometry blocks
    + MANGOLDT reference wall), the world-blind Gram at the CTRL_NZ
    ward window, and its eigen-decomposition at padded dps."""
    gam = ward_cache()
    ce = R4.build_cell(h, KFAC, "MAIN", dps, want_mp=True)
    K = ce["K"]
    ndig = min(dps - 2, NDIG_CAP)
    out = dict(h=h, dps=dps, K=K, ndig=ndig,
               cn_main=list(ce["cn_mp_str"]))
    with mp.workdps(dps):
        aa = mp.log(h) / 2
        b = [(kk * mp.pi / aa) ** 2 for kk in range(K)]
        Tlo = 2 * math.pi * h + Z_OVERHANG
        Gm = [[mp.mpf(0)] * K for _ in range(K)]
        Smu = mp.mpf(0)
        nz_used = 0
        for g in gam[:CTRL_NZ]:
            gf = float(g)
            if gf <= Tlo:
                continue
            gm = mp.mpf(repr(gf))
            g2 = gm * gm
            s = mp.sin(aa * gm)
            mu = s * s / g2
            Smu += mu
            psi = [g2 / (g2 - b[kk]) if kk else mp.mpf(1)
                   for kk in range(K)]
            for kk in range(K):
                pk = mu * psi[kk]
                row = Gm[kk]
                for ll in range(kk + 1):
                    row[ll] += pk * psi[ll]
            nz_used += 1
        for kk in range(K):
            for ll in range(kk):
                Gm[ll][kk] = Gm[kk][ll]
        Dg = [mp.sqrt(Gm[kk][kk]) for kk in range(K)]
        out["nz_used"] = nz_used
        out["dg_str"] = [mp.nstr(Dg[kk], ndig) for kk in range(K)]
        out["smu_str"] = mp.nstr(Smu, ndig)
        out["gn_str"] = [[mp.nstr(Gm[i][j] / (Dg[i] * Dg[j]), ndig)
                          for j in range(K)] for i in range(K)]
        Mgeo = ce["mpPole"] + ce["mpArch"]
        out["mgeo_str"] = [[mp.nstr(Mgeo[i, j], ndig)
                            for j in range(K)] for i in range(K)]
        out["mprime_main_str"] = [[mp.nstr(ce["mpPrime"][i, j], ndig)
                                   for j in range(K)] for i in range(K)]
    dpse = ndig + EIG_PAD
    with mp.workdps(dpse):
        Gn = mp.matrix(K, K)
        for i in range(K):
            for j in range(K):
                Gn[i, j] = mp.mpf(out["gn_str"][i][j])
        Ev, Q = mp.eigsy(Gn)
        out["lam_str"] = [mp.nstr(Ev[i], dpse - 5) for i in range(K)]
        out["q_str"] = [[mp.nstr(Q[i, j], dpse - 5) for j in range(K)]
                        for i in range(K)]
        out["gmin"] = float(min(Ev[i] for i in range(K)))
    return out


def assemble_mprime(h: int, dps: int, K: int, atoms_f: list):
    """even-sector Mprime from float atoms -- R4.build_cell recipe
    ported VERBATIM (pj off-diagonal + psi_d diagonal + nrm
    normalization); port-gated on the MANGOLDT world."""
    with mp.workdps(dps):
        aa = mp.log(h) / 2
        L2v = 2 * aa
        oms = [kk * mp.pi / aa for kk in range(K)]
        par = [mp.mpf((-1.0) ** kk) for kk in range(K)]
        dsig = mp.mpf(-1)
        atoms = [(mp.log(a[1]), mp.log(a[2]) / mp.sqrt(a[1]))
                 if (isinstance(a, tuple) and len(a) == 3
                     and a[0] == "PP")
                 else (mp.mpf(repr(a[0])), mp.mpf(repr(a[1])))
                 for a in atoms_f]
        pj = [sum((w * mp.sin(o * u) for u, w in atoms), mp.mpf(0))
              for o in oms]
        Mp = mp.zeros(K, K)
        for i in range(K):
            for j2 in range(i):
                sg = par[i] * par[j2]
                den = oms[j2] ** 2 - oms[i] ** 2
                od = 2 * sg * (oms[i] * pj[i] - oms[j2] * pj[j2]) / den
                Mp[i, j2] += od
                Mp[j2, i] += od
        for i in range(K):
            o = oms[i]
            if i == 0:
                pdiag = sum((w * (L2v - u) for u, w in atoms), mp.mpf(0))
            else:
                pdiag = sum((w * ((aa - u / 2) * mp.cos(o * u)
                                  + dsig * mp.sin(o * u) / (2 * o))
                             for u, w in atoms), mp.mpf(0))
            Mp[i, i] += 2 * pdiag
        nrm = [mp.sqrt(L2v) if kk == 0 else mp.sqrt(aa)
               for kk in range(K)]
        for i in range(K):
            for j2 in range(K):
                Mp[i, j2] = Mp[i, j2] / (nrm[i] * nrm[j2])
        for i in range(K):
            for j2 in range(i):
                sym = (Mp[i, j2] + Mp[j2, i]) / 2
                Mp[i, j2] = sym
                Mp[j2, i] = sym
        return Mp


def world_jet(sh: dict, atoms_f: list) -> dict:
    """wall -> source -> jet for one world at one rung: M = Mgeo -
    Mprime(world), lowest eigenvector, sign fix, cn = v/nrm,
    d = (-1)^k c, A0, Jn."""
    h, dps, K, ndig = sh["h"], sh["dps"], sh["K"], sh["ndig"]
    Mp = assemble_mprime(h, dps, K, atoms_f)
    with mp.workdps(dps):
        aa = mp.log(h) / 2
        L2v = 2 * aa
        M = mp.matrix(K, K)
        for i in range(K):
            for j in range(K):
                M[i, j] = mp.mpf(sh["mgeo_str"][i][j]) - Mp[i, j]
        E, V = mp.eigsy(M)
        i0 = min(range(K), key=lambda i: E[i])
        nrm = [mp.sqrt(L2v) if kk == 0 else mp.sqrt(aa)
               for kk in range(K)]
        cn = [V[kk, i0] / nrm[kk] for kk in range(K)]
        imax = max(range(K), key=lambda kk: abs(cn[kk]))
        if cn[imax] < 0:
            cn = [-v for v in cn]
        d = [((-1) ** kk) * cn[kk] for kk in range(K)]
        A0 = sum(d)
        if abs(A0) < mp.mpf(repr(A0_GUARD)):
            return dict(error="A0-guard", h=h)
        Dg = [mp.mpf(s) for s in sh["dg_str"]]
        sq = mp.sqrt(mp.mpf(sh["smu_str"]))
        Jn = [(d[kk] / A0) * Dg[kk] / sq for kk in range(K)]
        return dict(h=h, K=K, ndig=ndig,
                    cn_str=[mp.nstr(v, ndig) for v in cn],
                    jn_str=[mp.nstr(v, ndig) for v in Jn])


def mass_location(sh: dict, jn_str: list) -> dict:
    """the D1 observables from the shared eigen-decomposition:
    log10tail at the primary cut, topshare, mu_m, mass ward."""
    K, ndig = sh["K"], sh["ndig"]
    dpse = ndig + EIG_PAD
    with mp.workdps(dpse):
        l10 = mp.log(10)
        lams = [mp.mpf(s) for s in sh["lam_str"]]
        Jn = [mp.mpf(s) for s in jn_str]
        svals = []
        for i in range(K):
            acc = mp.mpf(0)
            for kk in range(K):
                acc += mp.mpf(sh["q_str"][kk][i]) * Jn[kk]
            svals.append(acc)
        tot = mp.mpf(0)
        for i in range(K):
            tot += lams[i] * svals[i] * svals[i]
        # ward: direct quadratic form vs eigen-sum
        quad = mp.mpf(0)
        for i in range(K):
            gi = [mp.mpf(sh["gn_str"][i][j]) for j in range(K)]
            acc = mp.mpf(0)
            for j in range(K):
                acc += gi[j] * Jn[j]
            quad += Jn[i] * acc
        ward = float(abs(tot / quad - 1))
        lmax = max(lams)
        itop = max(range(K), key=lambda i: lams[i])
        cut = mp.mpf(CUT_PRIMARY) * lmax
        sub = mp.mpf(0)
        for i in range(K):
            if lams[i] >= cut:
                sub += lams[i] * svals[i] * svals[i]
        tail = abs(mp.mpf(1) - sub / tot)
        topshare = float(lams[itop] * svals[itop] ** 2 / tot)
        mu_m = mp.mpf(0)
        mpos = mp.mpf(0)
        for i in range(K):
            if lams[i] <= 0:
                continue
            mi = lams[i] * svals[i] * svals[i]
            if mi > 0:
                mu_m += mi * mp.log(lams[i] / lmax) / l10
                mpos += mi
        mu_mf = float(mu_m / mpos) if mpos > 0 else float("nan")
        return dict(log10tail=float(mp.log(tail + mp.mpf("1e-300"))
                                    / l10),
                    topshare=topshare, mu_m=mu_mf, ward=ward)


def alt_jet_tails(sh: dict, cn_str: list, jn_str: list) -> dict:
    """r182 alt-jet dissection VERBATIM (projective Dg-recovery)."""
    K, ndig = sh["K"], sh["ndig"]
    dpse = ndig + EIG_PAD
    out = {}
    with mp.workdps(dpse):
        l10 = mp.log(10)
        lams = [mp.mpf(s) for s in sh["lam_str"]]
        lmax = max(lams)
        Jn = [mp.mpf(s) for s in jn_str]
        cs = [mp.mpf(s) for s in cn_str]
        dmain = [((-1) ** kk) * cs[kk] for kk in range(K)]
        A0m = sum(dmain)
        keys = [math.fmod((kk + 1) * GOLD, 1.0) for kk in range(K)]
        perm = sorted(range(K), key=lambda i: keys[i])
        variants = {
            "SIGNFLIP": list(cs),
            "UNIFORM": [mp.mpf((-1) ** kk) for kk in range(K)],
            "MAGSCRAM": [(mp.mpf(1) if dmain[kk] >= 0 else mp.mpf(-1))
                         * abs(cs[perm[kk]]) for kk in range(K)]}
        scal = [Jn[kk] * A0m / dmain[kk] for kk in range(K)]
        for tag in ALT_TAGS:
            dv = variants[tag]
            Jv = [dv[kk] * scal[kk] for kk in range(K)]
            sv = []
            for i in range(K):
                acc = mp.mpf(0)
                for kk in range(K):
                    acc += mp.mpf(sh["q_str"][kk][i]) * Jv[kk]
                sv.append(acc)
            totv = mp.mpf(0)
            for i in range(K):
                totv += lams[i] * sv[i] * sv[i]
            cut = mp.mpf(CUT_PRIMARY) * lmax
            subv = mp.mpf(0)
            for i in range(K):
                if lams[i] >= cut:
                    subv += lams[i] * sv[i] * sv[i]
            tlv = abs(mp.mpf(1) - subv / totv)
            out[tag] = float(mp.log(tlv + mp.mpf("1e-300")) / l10)
    return out


def anchor_ctrl(xw: int) -> dict:
    """r182 w_ctrl MANGOLDT comparator VERBATIM (dps 60, CTRL_NZ,
    eigsy AT dps 60 -- like-for-like with the cited record)."""
    gam = ward_cache()
    dpsw = ANCHOR_DPS
    cw = R4.build_cell(xw, KFAC, "MAIN", dpsw, want_mp=True)
    Kw = cw["K"]
    with mp.workdps(dpsw):
        l10 = mp.log(10)
        aa = mp.log(xw) / 2
        oms = [kk * mp.pi / aa for kk in range(Kw)]
        cs = [mp.mpf(s) for s in cw["cn_mp_str"]]
        d = [((-1) ** kk) * cs[kk] for kk in range(Kw)]
        A0 = sum(d)
        b = [o * o for o in oms]
        Tlo = 2 * math.pi * xw + Z_OVERHANG
        Sw = mp.mpf(0)
        Gm = [[mp.mpf(0)] * Kw for _ in range(Kw)]
        for g in gam[:CTRL_NZ]:
            gf = float(g)
            if gf <= Tlo:
                continue
            gm = mp.mpf(repr(gf))
            g2 = gm * gm
            s = mp.sin(aa * gm)
            mu = s * s / g2
            Sw += mu
            psi = [g2 / (g2 - b[kk]) if kk else mp.mpf(1)
                   for kk in range(Kw)]
            for kk in range(Kw):
                pk = mu * psi[kk]
                row = Gm[kk]
                for ll in range(kk + 1):
                    row[ll] += pk * psi[ll]
        for kk in range(Kw):
            for ll in range(kk):
                Gm[ll][kk] = Gm[kk][ll]
        Dg = [mp.sqrt(Gm[kk][kk]) for kk in range(Kw)]
        Gn_ = mp.matrix(Kw, Kw)
        for i2 in range(Kw):
            for j2 in range(Kw):
                Gn_[i2, j2] = Gm[i2][j2] / (Dg[i2] * Dg[j2])
        sq = mp.sqrt(Sw)
        Jn = [(d[kk] / A0) * Dg[kk] / sq for kk in range(Kw)]
        Ev, Q = mp.eigsy(Gn_)
        lams = [Ev[i] for i in range(Kw)]
        tot = mp.mpf(0)
        svals = []
        for i in range(Kw):
            acc = mp.mpf(0)
            for kk in range(Kw):
                acc += Q[kk, i] * Jn[kk]
            svals.append(acc)
            tot += lams[i] * acc * acc
        lmax = max(lams)
        itop = max(range(Kw), key=lambda i: lams[i])
        cutp = mp.mpf(CUT_PRIMARY) * lmax
        subp = mp.mpf(0)
        for i in range(Kw):
            if lams[i] >= cutp:
                subp += lams[i] * svals[i] * svals[i]
        tail_w = abs(mp.mpf(1) - subp / tot)
        topshare = float(lams[itop] * svals[itop] ** 2 / tot)
        mu_m = mp.mpf(0)
        mpos = mp.mpf(0)
        for i in range(Kw):
            if lams[i] <= 0:
                continue
            mi = lams[i] * svals[i] * svals[i]
            if mi > 0:
                mu_m += mi * mp.log(lams[i] / lmax) / l10
                mpos += mi
        return dict(x=xw,
                    log10tail=float(mp.log(tail_w + mp.mpf("1e-300"))
                                    / l10),
                    topshare=topshare,
                    mu_m=float(mu_m / mpos) if mpos > 0
                    else float("nan"))


# ======================================================= spike layer
def landau_field(gam: np.ndarray, us: np.ndarray) -> np.ndarray:
    """Z(u) = sum_gamma cos(gamma u) over the full n7000 cache
    (float64 instrument, disclosed)."""
    out = np.zeros(len(us))
    for lo in range(0, len(us), 256):
        blk = us[lo: lo + 256]
        out[lo: lo + len(blk)] = np.cos(np.outer(gam, blk)).sum(axis=0)
    return out


SPIKE_EFF = [SPIKE_N]     # reduced only under --smoke


def spike_comb(world: str, half: str) -> tuple:
    """(positions, weights) of the depth-SPIKE_N spike comb."""
    nsp = SPIKE_EFF[0]
    off = 0 if half == "A" else nsp
    if world == "MANGOLDT":
        at = [(u, w) for u, w in
              mangoldt_atoms_f(int(math.floor(math.exp(SPIKE_HI))))
              if SPIKE_LO <= u <= SPIKE_HI]
        return (np.array([u for u, _ in at]),
                np.array([w for _, w in at]))
    if world == "P-BBP":
        cmap = {1: 4.0, 4: -2.0, 5: -1.0, 6: -1.0}
        cap = int(math.floor(math.exp(SPIKE_HI)))
        atoms = []
        for k in range(0, 21):                    # 16^-20 ~ 8e-25
            for j, cj in cmap.items():
                nn = 8 * k + j
                if 2 <= nn <= cap:
                    u = math.log(nn)
                    if SPIKE_LO <= u <= SPIKE_HI:
                        atoms.append((u, cj * 16.0 ** (-k)
                                      / math.sqrt(nn)))
        atoms.sort()
        return (np.array([u for u, _ in atoms]),
                np.array([w for _, w in atoms]))
    if world.startswith("INTRAND:"):
        seed = int(world.split("rand")[1])
        u0, w0 = spike_comb("MANGOLDT", "A")
        rng = np.random.default_rng(10000 + seed)
        mag = rng.permutation(np.abs(w0))
        sgn = rng.choice([-1.0, 1.0], size=len(w0))
        return (u0, mag * sgn)
    if world.startswith("CFGEOM:"):
        tag = world.split(":", 1)[1]
        if tag.startswith("rand"):
            quots = gen_cf_rand(int(tag[4:]), 2 * nsp)
        else:
            quots = CFS[tag][:2 * nsp]
        if half == "B":
            quots = quots[nsp:]        # fresh recursion on the tail
        lq = cf_logq(quots)
        us = [x for x in lq if SPIKE_LO <= x <= SPIKE_HI]
        if not us:
            return (np.zeros(0), np.zeros(0))
        return (np.array(us), np.ones(len(us)) / len(us))
    # dense stream worlds on the fixed spike grid
    fam, tag = world.split(":", 1)
    n = 2 * nsp
    if fam == "DIG10":
        if tag == "champ":
            d = gen_champ_digits(n)
        elif tag.startswith("rand"):
            d = gen_dig_rand(int(tag[4:]), n)
        else:
            d = DIGITS10[tag][:n]
        v = d.astype(float) - 4.5
    elif fam == "DIG2":
        if tag == "champ":
            b = gen_champ_bits(n)
        elif tag.startswith("rand"):
            b = gen_dig_rand(int(tag[4:]), n) % 2
        else:
            b = BITS2[tag][:n]
        v = 2.0 * b.astype(float) - 1.0
    elif fam == "CF":
        if tag == "liouv":
            v = (np.arange(1, n + 1, dtype=float) * math.log(10.0)
                 - math.log(K0))
        else:
            if tag.startswith("rand"):
                a = gen_cf_rand(int(tag[4:]), n)
            else:
                a = CFS[tag][:n]
            v = np.log(np.array(a, dtype=float)) - math.log(K0)
    else:
        raise ValueError(world)
    v = v[off: off + nsp]
    us = SPIKE_LO + (np.arange(1, nsp + 1)
                     * (SPIKE_HI - SPIKE_LO) / (nsp + 1))
    return (us, v)


def coherence(gam: np.ndarray, us: np.ndarray, ws: np.ndarray,
              z_ms: float) -> float:
    if len(us) == 0:
        return float("nan")
    z = landau_field(gam, us)
    num = abs(float(np.dot(ws, z)))
    den = math.sqrt(float(np.dot(ws, ws)) * z_ms)
    return num / den


# ==================================================== equidist layer
def weyl_stat(digits: np.ndarray, base: int) -> float:
    n = len(digits)
    best = 0.0
    for m in range(1, WEYL_MMAX + 1):
        ph = np.exp(2j * math.pi * m * digits / base)
        best = max(best, abs(ph.mean()) * math.sqrt(n))
    return best


def bit_stat(bits: np.ndarray) -> float:
    s = 2.0 * bits.astype(float) - 1.0
    n = len(s)
    best = abs(s.sum()) / math.sqrt(n)
    for l in range(1, LAG_MAX + 1):
        best = max(best, abs(float(np.dot(s[:-l], s[l:])))
                   / math.sqrt(n - l))
    return best


def gk_chi2(quots: list) -> float:
    obs = np.zeros(GK_BINS)
    for a in quots:
        obs[min(a, GK_BINS) - 1] += 1
    n = len(quots)
    exp = np.zeros(GK_BINS)
    for j in range(1, GK_BINS):
        exp[j - 1] = n * math.log2(1 + 1.0 / (j * (j + 2)))
    exp[GK_BINS - 1] = n - exp[: GK_BINS - 1].sum()
    return float(((obs - exp) ** 2 / exp).sum())


# ============================================================== main
DIGITS10: dict = {}
BITS2: dict = {}
CFS: dict = {}


def main() -> int:
    ap = argparse.ArgumentParser()
    ap.add_argument("--smoke", action="store_true")
    args = ap.parse_args()
    smoke = args.smoke

    print("=" * 78)
    print("pi_pattern_scan_probe -- PI.PATTERN.SCAN.01  "
          "(EXPLORATORY TRANSFER)")
    print("SPEC_SHA %s%s" % (SPEC_SHA[:16], "  [SMOKE]" if smoke else ""))
    print("=" * 78)

    rungs = (4, 5) if smoke else HRUNGS
    nseed = 3 if smoke else NSEED
    ndig_depth = 4000 if smoke else NDIGITS
    ncf_depth = 800 if smoke else NCF
    if smoke:
        SPIKE_EFF[0] = 256

    # ------------------------------------------------------------ S0
    section("S0  FIREWALL + WARDS + DATA SELF-CHECKS")
    ok_fw, det = firewall_audit()
    check("G01-firewall-ast", ok_fw, det)
    check("G02-spec-seal", len(__doc__) > 4000,
          "SPEC_SHA %s (sha256 of __doc__)" % SPEC_SHA[:16])
    check("G03-ward-cache", ward_meta_ok(),
          "verified_zeros_n7000.npy + meta present (read-only)")
    gam = ward_cache()
    check("G04-ward-shape", len(gam) == 7000 and gam[0] > 14.13,
          "n = %d, gamma_1 = %.6f, gamma_n = %.2f"
          % (len(gam), gam[0], gam[-1]))

    t0 = time.time()
    for tag in ("pi", "e", "sqrt2", "phi"):
        DIGITS10[tag] = gen_digits(tag, ndig_depth)
        BITS2[tag] = gen_bits(tag, ndig_depth)
        CFS[tag] = gen_cf(tag, ncf_depth)
    info("data generation: %.1f s (digits %d, CF %d terms)"
         % (time.time() - t0, ndig_depth, ncf_depth))

    pid = "".join(str(c) for c in DIGITS10["pi"][:30])
    eid = "".join(str(c) for c in DIGITS10["e"][:30])
    check("G05-digit-selfcheck",
          pid == PI_DIG_PREFIX and eid == E_DIG_PREFIX,
          "pi/e first 30 decimal digits match the known strings")
    check("G06-cf-selfcheck",
          tuple([3] + CFS["pi"][:11]) == PI_CF_PREFIX
          and CFS["e"][:6] == [1, 2, 1, 1, 4, 1],
          "pi CF [3;7,15,1,292,...] + e CF [1,2,1,1,4,1] replicated "
          "by interval Euclid")
    check("G07-cf-depth",
          len(CFS["pi"]) >= ncf_depth and len(CFS["e"]) >= ncf_depth,
          "rigorous CF prefix depth pi %d / e %d (>= %d)"
          % (len(CFS["pi"]), len(CFS["e"]), ncf_depth))
    bit_pi = BITS2["pi"][:8].tolist()
    check("G08-bit-selfcheck", bit_pi == [0, 0, 1, 0, 0, 1, 0, 0],
          "pi binary fraction .00100100... replicated")
    # GK sampler marginal (pooled seeds)
    pooled = []
    for sd in range(nseed):
        pooled.extend(gen_cf_rand(sd, 2000))
    chi_pool = gk_chi2(pooled) / (len(pooled) / 2000.0)
    check("G09-gk-sampler", chi_pool < 30.0,
          "pooled GK sampler chi2/2000-block = %.2f (marginal "
          "matched)" % chi_pool)

    # ------------------------------------------------------------ S1
    section("S1  D1 MASS-LOCATION -- rung ladder, full world battery")
    worlds_stream = (["DIG10:pi", "DIG10:e", "DIG10:sqrt2", "DIG10:phi",
                      "DIG10:champ"]
                     + ["DIG10:rand%d" % s for s in range(nseed)]
                     + ["DIG2:pi", "DIG2:e", "DIG2:sqrt2", "DIG2:phi",
                        "DIG2:champ"]
                     + ["DIG2:rand%d" % s for s in range(nseed)]
                     + ["CF:pi", "CF:e", "CF:sqrt2", "CF:phi",
                        "CF:liouv"]
                     + ["CF:rand%d" % s for s in range(nseed)]
                     + ["CFGEOM:pi", "CFGEOM:e", "CFGEOM:sqrt2",
                        "CFGEOM:phi"]
                     + ["CFGEOM:rand%d" % s for s in range(nseed)])
    worlds_all = ["MANGOLDT", "P-BBP"] + worlds_stream
    D1: dict = {}
    JETS: dict = {}
    port_dev = jet_dev = 0.0
    ward_max = 0.0
    for h in rungs:
        dps = DPS[h]
        t0 = time.time()
        sh = rung_shared(h, dps)
        K = sh["K"]
        # port gate on MANGOLDT
        at_m = comb_for("MANGOLDT", h, "A")
        Mp_mine = assemble_mprime(h, dps, K, at_m)
        with mp.workdps(dps):
            dev = mp.mpf(0)
            for i in range(K):
                for j in range(K):
                    dev = max(dev, abs(Mp_mine[i, j]
                                       - mp.mpf(sh["mprime_main_str"]
                                                [i][j])))
            port_dev = max(port_dev, float(dev))
        jm = world_jet(sh, at_m)
        with mp.workdps(dps):
            devj = max(abs(mp.mpf(a) - mp.mpf(b))
                       for a, b in zip(jm["cn_str"], sh["cn_main"]))
            jet_dev = max(jet_dev, float(devj))
        # Nyquist audit
        nyq = math.pi * (NATOMS[h] + 1) / math.log(h)
        omtop = (K - 1) * math.pi / (math.log(h) / 2)
        D1.setdefault("nyq", []).append(nyq / omtop)
        for wname in worlds_all:
            halves = (("A",) if wname in ("MANGOLDT", "P-BBP")
                      else ("A", "B"))
            for half in halves:
                jr = world_jet(sh, comb_for(wname, h, half))
                if "error" in jr:
                    D1[(wname, half, h)] = dict(error=jr["error"])
                    continue
                ml = mass_location(sh, jr["jn_str"])
                ward_max = max(ward_max, ml["ward"])
                ml["gmin"] = sh["gmin"]
                D1[(wname, half, h)] = ml
                if h in ALT_RUNGS:
                    JETS[(wname, half, h)] = (jr["cn_str"],
                                              jr["jn_str"], sh)
        info("rung h=%d (K=%d, dps=%d, nz=%d, gmin=%.2e): %.1f s"
             % (h, K, dps, sh["nz_used"], sh["gmin"],
                time.time() - t0))

    check("G10-port-mprime", port_dev <= PORT_TOL,
          "my Mprime(MANGOLDT) vs build_cell mpPrime: max dev %.2e "
          "<= 1e-25" % port_dev)
    check("G11-port-jet", jet_dev <= PORT_TOL,
          "ported wall jet vs build_cell cn: max dev %.2e <= 1e-25"
          % jet_dev)
    check("G12-mass-ward", ward_max <= MASS_WARD,
          "max |sum m_i / (Jn^T Gn Jn) - 1| = %.2e <= 1e-6" % ward_max)
    check("G13-comb-nyquist", min(D1["nyq"]) >= 4.0,
          "dense-comb Nyquist / top house frequency >= %.2f at every "
          "rung (>= 4)" % min(D1["nyq"]))

    def classify(wname: str, half: str) -> tuple:
        tails = [D1[(wname, half, h)]["log10tail"] for h in rungs]
        lo, hi = tails[0], tails[-1]
        mono = all(tails[i + 1] >= tails[i] - LADDER_SLACK
                   for i in range(len(tails) - 1))
        if (lo >= TAILRIDE_LO4 and hi >= TAILRIDE_HI8 and mono):
            return "TAIL-RIDING", tails
        if all(t <= FAKE_BAR for t in tails):
            return "TOP-HEAVY", tails
        return "OTHER", tails

    cls: dict = {}
    for wname in worlds_all:
        halves = ("A",) if wname in ("MANGOLDT", "P-BBP") else ("A", "B")
        for half in halves:
            cls[(wname, half)] = classify(wname, half)

    mang_cls, mang_tails = cls[("MANGOLDT", "A")]
    info("MANGOLDT ladder log10tail: %s -> %s"
         % (["%.2f" % t for t in mang_tails], mang_cls))
    if smoke:
        check("G14-mangoldt-tailrides",
              mang_cls != "TOP-HEAVY",
              "SMOKE-SCOPED (ladder ends at h=5; full bar needs "
              "h=8): class %s" % mang_cls)
    else:
        check("G14-mangoldt-tailrides", mang_cls == "TAIL-RIDING",
              "C-MANGOLDT classifies TAIL-RIDING on the house "
              "ladder (transfer validity, D1)")

    # per-world verdict table
    print("\n  D1 TABLE (HALF-A): world | ladder log10tail | class | "
          "topshare(8) | mu_m(8)")
    n_fake = n_other = 0
    pi_d1: dict = {}
    for wname in worlds_all:
        c, tails = cls[(wname, "A")]
        r8 = D1[(wname, "A", rungs[-1])]
        tag = ("PI " if wname in
               ("P-BBP",) or wname.split(":")[-1] == "pi" else "   ")
        if wname == "MANGOLDT":
            tag = "PC "
        print("  %s%-14s %s  %-11s top %.4f mu_m %7.2f"
              % (tag, wname, " ".join("%7.2f" % t for t in tails), c,
                 r8["topshare"], r8["mu_m"]))
        if wname != "MANGOLDT":
            n_fake += c == "TOP-HEAVY"
            n_other += c == "OTHER"
        if tag == "PI ":
            pi_d1[wname] = c
    info("non-MANGOLDT worlds: %d TOP-HEAVY, %d OTHER, %d total"
         % (n_fake, n_other, len(worlds_all) - 1))

    # null-band construction integrity
    rand_ok = all(cls[("DIG10:rand%d" % s, "A")][0] != "TAIL-RIDING"
                  and cls[("CF:rand%d" % s, "A")][0] != "TAIL-RIDING"
                  for s in range(nseed))
    check("G15-null-band-d1", rand_ok,
          "all %d x2 family null seeds computed; none TAIL-RIDING"
          % nseed)

    pi_anom_d1 = []
    for wname, c in pi_d1.items():
        if c != "TAIL-RIDING":
            continue
        if wname == "P-BBP":
            pi_anom_d1.append((wname, "PROVISIONAL-NO-HOLDOUT"))
        else:
            cb, _ = cls[(wname, "B")]
            pi_anom_d1.append(
                (wname, "REPLICATES" if cb == "TAIL-RIDING"
                 else "NON-REPLICATING"))
    check("G16-d1-adjudication", True,
          "DEFINITIONAL/ADJUDICATION: pi D1 flags = %s"
          % (pi_anom_d1 if pi_anom_d1 else "NONE (all fake-class)"))

    # ------------------------------------------------------------ S2
    section("S2  D1 ANCHOR -- r182 w_ctrl MANGOLDT comparator "
            "(dps 60, CTRL_NZ 300)")
    anchor_ok = True
    for xw in (ANCHOR_X if not smoke else (4, 5)):
        r = anchor_ctrl(xw)
        dt = abs(r["log10tail"] - float(CAL_CTRL_TAIL[xw]))
        dtop = abs(r["topshare"] - float(CAL_CTRL_LOC[xw][0]))
        dmu = abs(r["mu_m"] - float(CAL_CTRL_LOC[xw][1]))
        ok = (dt <= ANCHOR_TAIL_TOL and dtop <= ANCHOR_TOP_TOL
              and dmu <= ANCHOR_MUM_TOL)
        anchor_ok = anchor_ok and ok
        check("G17-anchor-x%d" % xw, ok,
              "tail %.3f (rec %s, dev %.3f) top %.4f (rec %s) "
              "mu_m %.2f (rec %s)"
              % (r["log10tail"], CAL_CTRL_TAIL[xw], dt, r["topshare"],
                 CAL_CTRL_LOC[xw][0], r["mu_m"], CAL_CTRL_LOC[xw][1]))

    # ------------------------------------------------------------ S3
    section("S3  D2 SPIKE COHERENCE -- Landau field on the n7000 "
            "cache")
    t0 = time.time()
    ugrid = SPIKE_LO + (np.arange(1, SPIKE_GRID_N + 1)
                        * (SPIKE_HI - SPIKE_LO) / (SPIKE_GRID_N + 1))
    zgrid = landau_field(gam, ugrid)
    z_ms = float(np.mean(zgrid ** 2))
    info("Landau field: grid rms %.2f over %d points (%.1f s)"
         % (math.sqrt(z_ms), SPIKE_GRID_N, time.time() - t0))

    def coh(world: str, half: str) -> float:
        us, ws = spike_comb(world, half)
        return coherence(gam, us, ws, z_ms)

    c_mang = coh("MANGOLDT", "A")
    nulls_d2 = {"DIG10": [], "DIG2": [], "CF": [], "CFGEOM": [],
                "INTRAND": []}
    for s in range(nseed):
        nulls_d2["DIG10"].append(coh("DIG10:rand%d" % s, "A"))
        nulls_d2["DIG2"].append(coh("DIG2:rand%d" % s, "A"))
        nulls_d2["CF"].append(coh("CF:rand%d" % s, "A"))
        nulls_d2["CFGEOM"].append(coh("CFGEOM:rand%d" % s, "A"))
        nulls_d2["INTRAND"].append(coh("INTRAND:rand%d" % s, "A"))
    nb = {k: max(v) for k, v in nulls_d2.items()}
    info("D2 null-band maxima: %s"
         % {k: "%.2f" % v for k, v in nb.items()})
    check("G18-mangoldt-spike-fires", c_mang >= MANG_FIRE_RATIO
          * max(nb["INTRAND"], nb["DIG10"]),
          "C_MANG = %.2f >= 10 x null max %.2f (Landau 1912 main "
          "term rides every prime-power atom)"
          % (c_mang, max(nb["INTRAND"], nb["DIG10"])))

    d2_rows = [("P-DIG10", "DIG10:pi", "DIG10"),
               ("P-DIG2", "DIG2:pi", "DIG2"),
               ("P-CF", "CF:pi", "CF"),
               ("P-CFGEOM", "CFGEOM:pi", "CFGEOM"),
               ("P-BBP", "P-BBP", "INTRAND"),
               ("C-E-DIG10", "DIG10:e", "DIG10"),
               ("C-PHI-CF", "CF:phi", "CF"),
               ("C-CHAMP", "DIG10:champ", "DIG10")]
    pi_anom_d2 = []
    print("\n  D2 TABLE: world | C(half A) | C(half B) | null max | "
          "verdict")
    for label, wkey, fam in d2_rows:
        ca = coh(wkey, "A")
        cb = coh(wkey, "B") if wkey != "P-BBP" else float("nan")
        bar = ANOM_RATIO * nb[fam]
        flag_a = ca > bar
        flag_b = (cb > bar) if not math.isnan(cb) else None
        verdict = "null"
        if flag_a and (flag_b is None or flag_b):
            verdict = ("ANOMALY-PROVISIONAL" if flag_b is None
                       else "ANOMALY-REPLICATES")
            if label.startswith("P-"):
                pi_anom_d2.append((label, verdict))
        elif flag_a:
            verdict = "NON-REPLICATING"
        print("  %-11s %7.2f  %7s  %6.2f  %s"
              % (label, ca,
                 ("%.2f" % cb) if not math.isnan(cb) else "n/a",
                 nb[fam], verdict))
    check("G19-d2-adjudication", True,
          "DEFINITIONAL/ADJUDICATION: pi D2 flags = %s"
          % (pi_anom_d2 if pi_anom_d2 else "NONE"))

    # ------------------------------------------------------------ S4
    section("S4  D3 EQUIDISTRIBUTION -- Weyl / bits / Gauss-Kuzmin")
    nh = ndig_depth // 2
    nc = ncf_depth // 2

    def d3_stats(tag: str) -> dict:
        d10 = DIGITS10[tag]
        b2 = BITS2[tag]
        cf = CFS[tag]
        return dict(
            weylA=weyl_stat(d10[:nh], 10), weylB=weyl_stat(d10[nh:], 10),
            bitA=bit_stat(b2[:nh]), bitB=bit_stat(b2[nh:]),
            gkA=gk_chi2(cf[:nc]), gkB=gk_chi2(cf[nc:]))

    null_weyl, null_bit, null_gk = [], [], []
    for s in range(nseed):
        dr = gen_dig_rand(s, nh)
        null_weyl.append(weyl_stat(dr, 10))
        null_bit.append(bit_stat(dr % 2))
        null_gk.append(gk_chi2(gen_cf_rand(s, nc)))
    nw, nbt, ng = max(null_weyl), max(null_bit), max(null_gk)
    info("D3 null maxima: weyl %.2f bit %.2f gk-chi2 %.2f"
         % (nw, nbt, ng))

    st_pi = d3_stats("pi")
    st_champ_w = (weyl_stat(gen_champ_digits(nh), 10),
                  weyl_stat(gen_champ_digits(2 * nh)[nh:], 10))
    st_phi_gk = gk_chi2(CFS["phi"][:nc])
    st_e_gk = gk_chi2(CFS["e"][:nc])
    check("G20-champ-fires", min(st_champ_w) >= CTRL_FIRE_RATIO * nw,
          "Champernowne Weyl stat %.1f/%.1f >= 3 x null max %.2f "
          "(structured control fires)"
          % (st_champ_w[0], st_champ_w[1], nw))
    check("G21-phi-gk-fires", st_phi_gk >= CTRL_FIRE_RATIO * ng,
          "phi CF GK chi2 = %.1f >= 3 x null max %.2f (all-1s CF "
          "maximally non-GK)" % (st_phi_gk, ng))
    info("e CF GK chi2 = %.1f (semi-structured control, measured)"
         % st_e_gk)

    pi_anom_d3 = []
    for leg, (va, vb, nmax) in (
            ("WEYL10", (st_pi["weylA"], st_pi["weylB"], nw)),
            ("BITS2", (st_pi["bitA"], st_pi["bitB"], nbt)),
            ("GK-CHI2", (st_pi["gkA"], st_pi["gkB"], ng))):
        fa, fb = va > ANOM_RATIO * nmax, vb > ANOM_RATIO * nmax
        verdict = ("ANOMALY-REPLICATES" if fa and fb else
                   "NON-REPLICATING" if fa else "null")
        if fa and fb:
            pi_anom_d3.append((leg, va, vb))
        print("  pi %-8s A %.2f  B %.2f  null max %.2f  %s"
              % (leg, va, vb, nmax, verdict))
    check("G22-d3-adjudication", True,
          "DEFINITIONAL/ADJUDICATION: pi D3 flags = %s"
          % (pi_anom_d3 if pi_anom_d3 else "NONE"))

    # ------------------------------------------------------------ S5
    section("S5  D4 OCCUPANCY / CLASSICAL LAWS")
    at = mangoldt_atoms_f(PSI_X)
    psi = sum(w * math.sqrt(math.exp(u)) for u, w in at)
    # NOTE w = log p/sqrt q -> Lambda(q) = w sqrt q; psi = sum Lambda
    ratio = psi / PSI_X
    check("G23-psi-x", PSI_BAND[0] <= ratio <= PSI_BAND[1],
          "psi(%d)/%d = %.5f in [0.97, 1.03] (Chebyshev/"
          "Rosser-Schoenfeld class)" % (PSI_X, PSI_X, ratio))
    occ_ok = True
    occs = []
    for j in DYADIC_J:
        cnt = sum(1 for u, _w in at
                  if 2 ** j <= math.exp(u) < 2 ** (j + 1)
                  and 2 ** j <= PSI_X)
        prednum = 2 ** j / (j * math.log(2))
        occs.append(cnt / prednum)
        occ_ok = occ_ok and DYADIC_BAND[0] <= cnt / prednum \
            <= DYADIC_BAND[1]
    check("G24-mangoldt-occupancy", occ_ok,
          "dyadic prime-power occupancy / (2^j/(j ln 2)) = %s in "
          "[0.75, 1.25] (PNT block law)"
          % ["%.3f" % v for v in occs])

    lq_pi = cf_logq(CFS["pi"][:ncf_depth])
    gaps_pi = np.diff(np.array([0.0] + lq_pi))
    dev_levy = abs(float(gaps_pi.mean()) - LEVY)
    null_levy = []
    for s in range(nseed):
        lqr = cf_logq(gen_cf_rand(s, ncf_depth))
        null_levy.append(abs(float(np.diff(np.array([0.0] + lqr))
                                   .mean()) - LEVY))
    lq_phi = cf_logq(CFS["phi"][:ncf_depth])
    gap_phi = float(np.diff(np.array([0.0] + lq_phi)).mean())
    sig = float(np.std(null_levy)) or 1e-12
    check("G25-levy-pi", dev_levy <= LEVY_TOL,
          "pi CF-geom mean gap dev from Levy %.6f = %.4f <= %.4f "
          "(null max %.4f; Levy 1936)"
          % (LEVY, dev_levy, LEVY_TOL, max(null_levy)))
    check("G26-levy-phi-separates",
          abs(gap_phi - LEVY) / sig >= PHI_SEP_SIGMA,
          "phi mean gap %.4f (= ln phi) vs Levy: %.0f sigma of null "
          "spread -- the law detector separates structure"
          % (gap_phi, abs(gap_phi - LEVY) / sig))
    gm_pi = float(np.exp(np.mean(np.log(
        np.array(CFS["pi"][:ncf_depth], dtype=float)))))
    null_kh = []
    for s in range(nseed):
        ar = np.array(gen_cf_rand(s, ncf_depth), dtype=float)
        null_kh.append(abs(float(np.exp(np.mean(np.log(ar)))) - K0))
    check("G27-khinchin-pi", abs(gm_pi - K0) <= KH_TOL,
          "pi CF GM = %.4f vs K0 %.4f (dev %.4f <= %.4f; null max "
          "%.4f; Khinchin 1934)"
          % (gm_pi, K0, abs(gm_pi - K0), KH_TOL, max(null_kh)))
    check("G28-gk-pi", st_pi["gkA"] + st_pi["gkB"] <= 2 * GK_BAR,
          "pi GK chi2 halves %.1f/%.1f <= bar %.1f each-avg "
          "(Gauss-Kuzmin)" % (st_pi["gkA"], st_pi["gkB"], GK_BAR))
    pi_anom_d4 = []
    if dev_levy > LEVY_TOL:
        pi_anom_d4.append(("LEVY", dev_levy))
    if abs(gm_pi - K0) > KH_TOL:
        pi_anom_d4.append(("KHINCHIN", gm_pi))
    check("G29-d4-adjudication", True,
          "DEFINITIONAL/ADJUDICATION: pi D4 flags = %s"
          % (pi_anom_d4 if pi_anom_d4 else
             "NONE (pi consistent with GK/Levy/Khinchin)"))

    # ------------------------------------------------------------ S6
    section("S6  D5 ALT-JET DISSECTION (r182 recipes verbatim)")
    # frozen selection of the best-scoring pi world at h = top rung
    htop = rungs[-1]
    key_map = {"P-BBP": "P-BBP", "P-CF": "CF:pi",
               "P-DIG10": "DIG10:pi", "P-DIG2": "DIG2:pi",
               "P-CFGEOM": "CFGEOM:pi"}
    best_pi = max(
        BEST_PI_PRIORITY,
        key=lambda w: (D1[(key_map[w], "A", htop)]["log10tail"],
                       -BEST_PI_PRIORITY.index(w)))
    info("best-scoring pi world at h=%d (frozen rule): %s "
         "(log10tail %.2f)"
         % (htop, best_pi,
            D1[(key_map[best_pi], "A", htop)]["log10tail"]))
    alt_ok_mang = True
    for h in (r for r in ALT_RUNGS if r in rungs):
        for wname, lbl in ((("MANGOLDT"), "MANGOLDT"),
                           ((key_map[best_pi]), best_pi)):
            cn_str, jn_str, sh = JETS[(wname, "A", h)]
            alts = alt_jet_tails(sh, cn_str, jn_str)
            base = D1[(wname, "A", h)]["log10tail"]
            print("  h=%d %-10s MAIN %7.2f | %s"
                  % (h, lbl, base,
                     "  ".join("%s %7.2f" % (t, alts[t])
                               for t in ALT_TAGS)))
            if wname == "MANGOLDT" and h == max(
                    r for r in ALT_RUNGS if r in rungs):
                alt_ok_mang = all(alts[t] <= ALT_FAKE_BAR
                                  for t in ALT_TAGS)
                if not smoke and h == 8:
                    alt_ok_mang = (alt_ok_mang
                                   and base >= TAILRIDE_HI8)
    check("G30-mangoldt-altjets", alt_ok_mang,
          "MANGOLDT alt-jets all relocate to top (<= -8) while MAIN "
          "rides the tail: ALT-JETS-BREAK-ALIGNMENT replicated"
          + (" [SMOKE-SCOPED]" if smoke else ""))
    check("G31-d5-adjudication", True,
          "DEFINITIONAL/ADJUDICATION: best pi world %s alt-jet "
          "response reported with numbers above (no structure to "
          "kill if D1 class is TOP-HEAVY)" % best_pi)

    # ------------------------------------------------------------ S7
    section("S7  TAXONOMY VERDICT (frozen adjudication)")
    transfer_valid = (mang_cls == "TAIL-RIDING" and anchor_ok
                      and c_mang >= MANG_FIRE_RATIO
                      * max(nb["INTRAND"], nb["DIG10"])
                      and PSI_BAND[0] <= ratio <= PSI_BAND[1])
    anomalies = ([("D1",) + a for a in pi_anom_d1
                  if a[1] != "NON-REPLICATING"]
                 + [("D2",) + a for a in pi_anom_d2]
                 + [("D3", a[0]) for a in pi_anom_d3]
                 + [("D4", a[0]) for a in pi_anom_d4])
    all_fake = (all(c == "TOP-HEAVY" for c in pi_d1.values())
                and not anomalies)
    if not transfer_valid:
        taxonomy = "TRANSFER-INSTRUMENT-DEAD"
    elif all_fake:
        taxonomy = "PI-LANDS-FAKE-CLASS"
    elif anomalies:
        taxonomy = "PI-ANOMALY-AT-" + "+".join(
            sorted(set(a[0] for a in anomalies)))
    else:
        taxonomy = "MIXED"
    check("G32-taxonomy", True,
          "DEFINITIONAL/ADJUDICATION: %s (transfer_valid=%s, "
          "anomalies=%s)" % (taxonomy, transfer_valid,
                             anomalies if anomalies else "NONE"))

    # ------------------------------------------------------------ S8
    section("S8  BOOKKEEPING")
    rt = time.time() - T0_WALL
    check("G33-runtime", rt <= RUNTIME_BAR,
          "runtime %.1f s <= %.0f s" % (rt, RUNTIME_BAR))
    npass = sum(1 for _n, ok, _d in CHECKS if ok)
    ntot = len(CHECKS)
    verdict = []
    if transfer_valid:
        verdict.append("TRANSFER-VALID-MANGOLDT-FIRES")
    verdict.append(taxonomy)
    if not pi_anom_d4:
        verdict += ["CF-STATS-MATCH-GAUSS-KUZMIN",
                    "LEVY-CONSTANT-REPLICATED",
                    "KHINCHIN-GM-CONSISTENT"]
    if not pi_anom_d2:
        verdict.append("NO-SPIKE-COHERENCE")
    verdict.append("STRUCTURED-CONTROLS-FIRE-DETECTORS")
    if not pi_anom_d1:
        verdict.append("ALT-JETS-CONFIRM-NO-STRUCTURE")
    verdict += ["NO-RH-CLAIM", "NO-PI-NUMEROLOGY-CLAIM"]
    print("\n" + "=" * 78)
    print("GATES: %d/%d PASS" % (npass, ntot))
    print("COMPOSITE VERDICT: " + " + ".join(verdict))
    print("SPEC_SHA %s" % SPEC_SHA[:16])
    print("RUNTIME %.1f s" % rt)
    print("=" * 78)
    return 0 if npass == ntot else 1


if __name__ == "__main__":
    sys.exit(main())
