#!/usr/bin/env python3
# -*- coding: utf-8 -*-
r"""fold_two_scale_balance_probe --
PRIME.L2.FOLD_TWO_SCALE_BALANCE.01 (round 344): THE R* MASS
BALANCE AND THE TWO-SCALE FREEZE -- the named follow-up round
after r341.

CONTEXT (binding, from the sealed records r306/r321/r324/r329/
r335/r337/r339/r341): r341 put the exact path layer at theorem
grade (tilted tower E[X_inf^3] == E_Q[prod Gamma] with steps
p~_c = p_c R_c^3/Gamma, the exact hand-off E3h <= (m q_max)^2
msh, the eps-chain envelope E3g <= W_B -- Fractions bit-equal on
w9 + w13) and measured two precise deficiencies: (1) THE STOP AT
R* = 3/2 IS TOO EARLY -- the heavy arm inherits med 94.4% of the
cubic mass (hsh med 0.872), the mid-band stops entirely at level
1, several rungs have an EMPTY good tree; R_ALT = 7/4 moves the
E3h share to 0.386 -- R* is a TUNING SURFACE, the mass balance
between 3/2 and 7/4 is the named open question.  (2) EACH ARM
FAILS ON A DIFFERENT STRUCTURED FAMILY -- the good arm (Form-1
eps-chain) misses ONLY kz55 (1.41x at a = 2, EXT3 clean); the
heavy-arm freeze fails on the EXT3 DEEP ANCHORS (kz51 7.61x at
a = 2) and NAMED 0/4.  The path-weighted exponents are NOT
supercritical (e(W_B) = -0.214, e(E3h) = +0.313 vs CRIT 0.224);
the envelope typing gave m_0* 10^24.0 (vs r324 10^59.6, r306
census 10^13.5) WITHOUT certification.  Missing: the right arm
split + the two-scale certification.  The r337 complementarity
directive (each arm on its own sealed freeze, each rung covered
by at least one arm) has never been combined with the
path-weighted machinery -- executed here.

THE CORE QUESTION (contract): does a canonical (non-tuned)
stopping rule and a two-scale certification (each arm against
its own sealed freeze) exist that brings the two-arm statement
E[X_inf^3] <= C (log m)^A to 0 violations -- and what does the
honest composition give?

THE SEALED BALANCE MACHINERY (disclosed BEFORE any evaluation):
  (i) THE R GRID (sealed): R_GRID = (3/2, 8/5, 5/3, 12/7, 7/4,
      2).  The endpoints are the two banked r339/r341 columns;
      2 is the ALGEBRAIC PAIR LIMIT: a pair child has R_c =
      2 a_c/(a_c + a_sib) < 2 always, so at R = 2 NO pair is
      ever heavy (the Gamma -> 4 pair-ceiling limit named in
      the contract) -- only uneven odd-tail nodes can stop.
  (ii) THE SEALED CHOICE RULE (a FORMULA, frozen pre-record; R*
      must not remain a tuning surface): per grid point R the
      two arm exponents e_H(R) = halves-slope of E3h(R) and
      e_G(R) = halves-slope of W_B(R) on the 39 ladder test
      rungs (r272 dyadic, fit-free; EXT3 excluded from
      exponents, r329 cohort caveat).  ELIGIBILITY (degeneracy
      guard, a-priori): R is eligible iff at least half the
      test rungs have E3h > 0 AND at least half have E3g > 0
      (a one-sided grid point measures only one arm).  R_HAT =
      argmin over eligible R of max(e_H(R), e_G(R)); ties by
      smaller |e_H - e_G|, then smaller R.  If NO grid point is
      eligible, R_HAT = R_GRID[0] and the canonicality letter
      is DENIED.  CANONICAL iff R_HAT is INTERIOR (not a grid
      endpoint) AND both dyadic halves of the test cohort pick
      the same R_HAT (halves-stable).  The equilibration census
      (sign change of e_H - e_G along the grid) is printed.
  (iii) THE TWO-SCALE FREEZES (the r337 directive; per grid R
      and per a in GA_FAM = (1, 2, 3), all constants mid-ladder
      max-cal on the SEALED r316 split, TRB verbatim):
      C_H(R, a) frozen on the HAND-OFF column HB = (m q_max)^2
      x msh (the r335 mass-arm currency; HB >= E3h is the
      r341-exact hand-off, warded live) -- the heavy arm's OWN
      scale; C_G(R, a) frozen on the eps-chain column W_B
      (W_B >= E3g exact, warded live) -- the good arm's OWN
      scale.
  (iv) THE MIN-COVERAGE TEST (the complementarity form, sealed):
      a rung w is COVERED at (R, a) iff at least ONE arm covers
      its FULL target E3(w) on that arm's own scale:
        heavy-covers:  E3(w) <= C_H(R, a) (log m)^a,
        good-covers:   E3(w) <= C_G(R, a) (log m)^a,
        third-covers:  rho_2(w) <= GSQ_R321 x F_A(w)^2
      (the r321 sliding bound as the THIRD ARM: GSQ_R321 =
      1.3056 is the BANKED r321 record constant, adopted
      frozen, NEVER recalibrated here; F_A = the r317
      rank-local QMAX ratio, EFP.local_ratio verbatim on the
      65-rung (N, kz)-sorted q_max column; EXT3 rows get their
      coordinate via the r321 INSERTION RULE CCP.world_coord --
      the r329-disclosed convention under which all four quiet
      anchors were inside).  Certified iff 0 violations on the
      51 test rows (39 ladder test + 12 EXT3) AND named 4/4.
      NOT the min of the constants: the theorem constant is the
      SUM (E3 <= [C_H + C_G] (log m)^a + GSQ F_Amax^2 (log
      m)^2 pointwise on a covered set), the coverage is
      per-rung disjunctive.  The r341 component-AND form (E3h
      vs C_H' on the E3h column and W_B vs C_B, the form that
      failed 11 + 1) is re-printed at R_HAT as the comparison
      census.
  (v) THE PARTITION RULE (structure test, sealed a-priori): the
      predicted arm of rung w is HEAVY iff hsh(R_HAT)(w) >=
      HSH_SPLIT = 1/2 else GOOD (hsh is builder-side path
      mass, source-pure).  Identifiability census: the rule's
      accuracy against the measured minimal covering arm, plus
      the count of rungs covered ONLY by the non-predicted arm
      (the posthoc-partition surface) -- census, no letter.
  (vi) THE ANATOMY LEGS: kz55 in the good arm (per-level
      eps-chain profile, the eps-argmax level, the top unstopped
      good nodes (n(v), Gamma(v), max R_c) at that level, at
      R* = 3/2 AND at R_HAT -- is it the r339 kz53 early-level
      anomaly class, and does the rebalanced stop absorb it?);
      the EXT3 deep anchors in the heavy arm (m q_max, msh, HB
      vs C_H, F_A via insertion, the third-arm ratio rho_2 /
      (GSQ F_A^2) -- is it the r329 small-gap anatomy, and does
      the banked r321 bound cover exactly these spikes
      out-of-sample as it did in r329?).
  (vii) THE COMPOSITION (printed ALWAYS, honestly typed): on a
      0-violation cover at minimal a*: E3 <= [C_H(R_HAT, a*) +
      C_G(R_HAT, a*)] (log m)^{a*} + GSQ F_Amax^2 (log m)^2
      (the third-arm term included iff the minimal cover uses
      it), then through the dictionary M_3 <= C_tot (log
      m)^{A_tot}/m^2 and the r324 chain as a POLYLOG expression
      (N_3 >= m/sqrt(C_tot (log m)^{A_tot}), N_2 >= N_3, NO
      premature powerization); m_0* = smallest m with m^{0.224}
      >= C_tot(log m) -- honestly against the r324 measured
      route 10^59.6 AND the r306 census reading 10^13.5.  On a
      non-covering outcome: the minimal residual census typed
      (which rungs, which family, which arm closest).

LEG 0 -- ANCHOR REGRESSION (slim set + the r339/r341/r324/r321
records, disclosed): the r314 identity wards live; r306 C_2 =
1.069 (tol 0.005) first-5 freeze, 0/57; r316 rho anchors +
C_small + n = 65; r324-pre C_M2 = 2.2557 + the seven m2
violators EXACT; the dictionary-chain identity live (r339 G34
verbatim); the r339 RECORD anchors through the imported FDD
builders (W_F med 265.54 / W_G med 13.69 / hsh med 0.872 /
Gamma_max 4.303 / e(W_F) +0.956 / e(m^2 M_3) +0.112); the r324
CHAIN anchors (e_G +0.158 / e_M2 +0.014 / e_tot +0.172, m_0* =
10^59.6 adopted); THE r341 RECORD COMPLETE through the imported
FBR builders at BOTH banked columns: R* = 3/2 (W_B med 1.489 /
max 2.888 at kz55, E3h med 5.86, E3h share med 0.944, hand-off
reserve med 4.8x min 2.1x) and R_ALT = 7/4 (hsh med 0.266, E3h
share med 0.386, W_B med 3.796); the r341 freezes reproduced
through the same code path (C_H(2) = 0.3875 viol 11 with kz51
7.61x; C_B(2) = 0.1113 viol 1 with kz55 1.41x); the r341
exponents e(W_B) -0.214 / e(E3h) +0.313; the r341 envelope
composition (heavy 1.3091 + good 0.0650 at a = 3, m_0* 10^24.0)
and the r306 census solve 10^13.5; the r321 F_A top-3 anchors
kz53 2.47 / kz83 2.39 / kz67 2.38 (tol 0.01) recomputed through
EFP.local_ratio on the module-own q_max column (validates the
third-arm coordinate); R335_CD adopted as record numbers
(census print only).

LEG A -- THE MASS-BALANCE CURVE: per grid R the full census
columns hsh(R), msh(R), E3h(R), E3h share(R), W_B(R), good-tree
empty count(R), stop-level profile, plus both arm exponents
e_H(R)/e_G(R) with dyadic halves; the sealed choice rule (ii)
adjudicated (R_HAT, eligibility, interior, halves-stability,
equilibration census).

LEG B -- THE TWO-SCALE FREEZE: the freezes (iii) at every grid
R, the min-coverage census (iv) at every (R, a) over the 51
test rows + named 4/4 + midband 6/6; the partition
identifiability census (v); the r341 component-AND comparison
census at R_HAT.

LEG C -- THE kz55 / DEEP-ANCHOR ANATOMY (vi) + the third-arm
cross-check (does min(three arms, each on its own scale) cover
everything?).

LEG D -- THE HONEST COMPOSITION (vii).

LEG E -- WORLDS + MUST-FAILS (>= 4 mutants + 2 scope mutants):
MAIN/twin/EPST/SCR balance curves (hsh(R), E3h share(R)) and
partition letters, census-grade (r341 honest negative: the
path-weighted budget was world-blind -- is the BALANCE CURVE?).
(e1) R_HAT FORMULA AFTER RECORD SIGHT (protocol):
  mutant_rhat_posthoc re-picks the grid point after sight of
  the evaluated violation column (consumes rho) -- the
  BOUND_FORBIDDEN scope audit must FLAG it AND on the sealed
  toy it returns grid index 4 != the protocol head index 0 (the
  choice rule is sealed as a FORMULA in this spec BEFORE the
  freeze) -- protocol-CAUGHT twice.
(e2) ARM ASSIGNMENT POSTHOC PER RUNG (protocol):
  mutant_partition_posthoc flips arm letters after sight of the
  evaluated violation column (consumes rho) -- AST-FLAGGED and
  on the sealed toy returns ('G', 'H') != the sealed hsh-rule
  output ('H', 'G') -- protocol-CAUGHT twice.
(e3) FREEZE CONSTANT FROM THE TARGET: mutant_cheavy_from_target
  derives a 'heavy constant' from the cubic record (consumes
  cm/S3) -- the PHI3_FORBIDDEN scan must FLAG it (AST-CAUGHT)
  while the module-own balance builders stay clean.
(e4) HAND-OFF WITHOUT msh: mutant_handoff_no_msh claims E3h <=
  (m q_max)^2 (msh dropped) as the hand-off bound -- on the
  sealed stop toy (7, 1, 5, 3) it returns 49/16 while the exact
  hand-off bound is 49/32 (break 49/32 EXACT in Fractions) --
  CAUGHT exact.
(m5a/m5b) WORLD-BLINDNESS BREAK: a builder consuming the
  withheld terminal drive key and a builder consuming the
  branch label are both FLAGGED by the AST scope audit.

SEALED VERDICTS (exactly one fires; total order; the user
contract names five letters -- the structural sixth is the
house standard for totality, disclosed):
   TARGET_LEAK                iff any purity/scope/literal audit
       hit on the module-own balance builders,
   BALANCE_STATE_NOT_EXACT    iff an exact ward breaks on a live
       world (tilted tower / partition / B-chain / envelope /
       Phi-Gamma / heavy hand-off across the WHOLE grid /
       martingale wards / the Fractions bit-equality at both
       sealed grid pins / the r327 grounding),
   TWO_SCALE_GO               iff the min-coverage certifies at
       R_HAT (0 violations on the 51 test rows + named 4/4 at
       some a) AND the stopping rule is CANONICAL (eligible +
       interior + halves-stable) AND the composed polylog m_0*
       <= the r324 threshold 10^59.6,
   TWO_SCALE_PARTIAL          iff the min-coverage certifies at
       R_HAT but the stopping rule is not canonical or the
       composition does not beat the r324 route,
   THRESHOLD_NOT_CANONICAL    iff the min-coverage does NOT
       certify at R_HAT but DOES certify at some other sealed
       grid point (the coverage exists, only the sealed choice
       rule fails to select it -- R* remains a tuning surface;
       said honestly),
   RESIDUAL_FAMILY_IRREDUCIBLE otherwise -- no sealed grid
       point reaches a 0-violation three-arm cover; the minimal
       residual census is typed and the resisting family NAMED.

EXPLORATION ONLY (2026-08-27).  experiments/ level: NO
promotion, NO ledger row, NO marker moved, NO RH CLAIM in
either direction.  Coexistence: r343 (pair coupling, L*) runs
in parallel (pre-freeze e652071f) -- own files only; before the
strictly additive rh-sync the current git state is re-checked
and the sync builds ONLY additively on it.  Two-commit freeze
protocol (r329 convention): spec committed pre-freeze, record
tables the only post-freeze edit, committed again.

THE OBJECT (r269/r287/r298/r306/r314/r316/r317/r321/r324/r327/
r333/r337/r339/r341 machinery imported verbatim): t_{N-2} =
sum_b ct_b (r244 chain rows, r266 eval); F = 0.20 edge split;
maximal same-sign runs of the bx-sorted bulk; level-2 blocks
(r270 convention); the frozen positional block machinery
(r298); the r306 RY3.cubic_moments + RY3.renyi3_ratio +
RY3.calib_freeze; the r314 SCF.fold_genealogy +
SCF.signed_cube_terms + SCF.flux_telescope; the r316
TRB.two_regime_state + TRB.split_midladder; the r317
EFP.local_ratio; the r321 CCP.world_coord (insertion rule); the
r324 QMO.mult_ward; the r324-pre FAP.m2_qmax_state; the r327
GMC.group_mass_ledger (grounding cross-ward); the r339
FDD.fold_mass_tree_exact + FDD.descendant_density_martingale +
FDD.martingale_moment_dictionary + FDD.fr_pair_tree +
FDD.fr_mart_check; the r341 FBR.path_bellman_state (THE ENTIRE
exact path layer, evaluated at every sealed grid R) +
FBR.fr_path_state (the full Fractions replica, evaluated at the
two sealed grid pins); PDelta = Pbeta - Pomega; x_j =
(PDelta)_j; a_i = |x_i|.  NEW in this round (module-own,
source-pure): rhat_rule (the sealed choice formula),
partition_predict (the sealed hsh rule), good_level_anatomy
(the kz55 eps-level census) and the sealed
two_scale_tree_verdict.

INDEX FIREWALL (binding, r238-r343 discipline): w = window (kz),
N_w = builder depth, k = TREE LEVEL (root 0), n(v) = leaf count;
ground truth (branch labels, the true R/t/Z values) enters GATES
and census tables only; the cubic target M_3 / rho_2 and the
q_max RECORD enter GATES / anchors / coverage checks only,
NEVER a path/balance builder (AST-warded; the balance builders
consume the sealed tree + the source-pure q_max column + rank
order ONLY); no zero/prime oracles anywhere (AST firewall); no
fit primitives (fragment audit; growth exponents are the
imported r272 dyadic halves-slope, fit-free).  B PROVENANCE:
B_w = S_{N-2} + 5/7 (r241/r243 IMPORTED floor, never fitted).
COFINAL LADDER (pre-sealed, r316/r324/r327/r335/r339/r341
verbatim): frame-A h <= 900, 42 rungs, (N, kz)-sorted;
exception set {kz15, 20, 22, 36, 38, 39, 52}; EXTENSION: 900 <
h <= 1300, first 15 by (N, kz); EXT2: the r316 A5 rule
(leftover pool + first 12 windows 1300 < h <= 1650, first 8
POSITIVE_PREFIX by (N, kz)); EXT3: the sealed r329 12-anchor
list (record committed 8cbd95f9, adopted as-is, PURE TEST
rows).

SEALED CONSTANTS: MAIN windows (9, 13); controls w9 EPSTEIN /
SCRAMBLE(seed 1) / SMOOTH, flips 25/21/27; H_CAP 900; B57 = 5/7;
M_W = sqrt(5/7); CHEAP_EXPECT 35; EXC_KZ_EXPECT (15, 20, 22, 36,
38, 39, 52); EDGE_F 0.20 (FROZEN); PAIR_OFFSET 0 (FROZEN, via
FDD verbatim); EXT_H_MAX 1300; K_EXT 15; EXT_NW_EXPECT (942,
1218); EXT2_H_MAX 1650; EXT2_POOL_CAP 12; K_EXT2 8; EXT3_KZ_B
(42, 51, 54, 56, 58, 62); EXT3_KZ_A (96, 123, 125, 127, 128,
130); EXT3_NW_MIN 1721; EXT3_NW_MAX 2577; R_GRID (3/2, 8/5,
5/3, 12/7, 7/4, 2) with Fractions twins FR_GRID; R_STAR 3/2 =
R_GRID[0] (r339 banked); R_ALT 7/4 = R_GRID[4] (r341 banked);
FR_PIN_IDX (0, 3) (the two sealed Fractions grid pins: 3/2 and
12/7); ELIG_FRAC 1/2; HSH_SPLIT 1/2; GA_FAM (1, 2, 3);
GSQ_R321 1.3056 (BANKED r321 record constant, adopted frozen,
never recalibrated); FA_MAX_R321 2.47 (record, anchor only);
NAMED_KZ (53, 83, 67, 55); MIDBAND_KZ (73, 76, 61, 95, 98,
109); ATOM_BAR 1e-9; REC3_BAR 1e-13 ladder / 1e-12 EXT3;
TEL_BAR 1e-13; BND_BAR 1e-13; CHAIN_BAR 1e-9; SA_BAR 1e-12;
TREE_BAR 1e-9; DICT_BAR 1e-9; TILT_BAR 1e-9; WQ_BAR 1e-9;
PART_BAR 1e-9; BK_BAR 1e-9; ENV_BAR 1e-9; PGI_BAR 1e-9; HVY_BAR
1e-9; JEN_BAR 1e-12; DEG_FLOOR 1e-6; MULT_CAP 2; N_CAL 5 (via
TRB, verbatim); MUT_MIN 1e-6; TOY_BAR 1e-12; FR_BAR 0
(bit-equality); TB_WARD bars 1e-9 main N <= 400 / 3e-6 deep +
ext + ext2 / 3e-5 EXT3 (r329 a-priori) / 1e-6 controls; ID_BAR
1e-12; AC_BAR 1e-9; INF_SENT 1e300 / cert guard 1e299; CRIT_EXP
0.224 (r324 verbatim); N2_EXP_NEED 0.888; R306_C2 1.069 tol
0.005; N344_REF 65; R316 RHO {53: 1.0490, 67: 1.0536, 55:
0.4821, 83: 0.7790} tol 0.005, C_SMALL 1.0694 tol 0.005 at
kz18; R324P_CM2 2.2557 tol 0.005, M2VIOL {53, 67, 83, 76, 61,
28, 109} EXACT; R339 record anchors W_F_MED 265.54 tol 0.01 /
W_G_MED 13.69 tol 0.01 / HSH_MED 0.872 tol 0.001 / GMX_MAX
4.303 tol 0.001 / E_WF +0.956 tol 0.001 / E_M3 +0.112 tol
0.001; R324 chain anchors E_G +0.158 tol 0.002 / E_M2 +0.014
tol 0.002 / E_TOT +0.172 tol 0.003 / M0_L10 59.6 (record
threshold); R341 record anchors WB_MED 1.489 tol 0.005 /
WB_KZ55 2.888 tol 0.005 / E3H_MED 5.86 tol 0.02 / HSHARE_MED
0.944 tol 0.001 / HRES_MED 4.8 tol 0.1 / HRES_MIN 2.1 tol 0.1 /
ALT_HSH_MED 0.266 tol 0.001 / ALT_HSHARE_MED 0.386 tol 0.001 /
ALT_WB_MED 3.796 tol 0.005 / CH2 0.3875 tol 0.001 viol 11
kz51-ratio 7.61 tol 0.02 / CB2 0.1113 tol 0.001 viol 1
kz55-ratio 1.41 tol 0.02 / E_WB -0.214 tol 0.003 / E_E3H +0.313
tol 0.003 / ENV_H3 1.3091 tol 0.002 / ENV_G3 0.0650 tol 0.002 /
M0_ENV_L10 24.0 tol 0.2; R306 census solve M0_306_L10 13.5 tol
0.2; R317/R321 F_A top-3 {53: 2.47, 83: 2.39, 67: 2.38} tol
0.01; R335_CD {0: 141.84, 1: 33.06, 2: 7.71} (record, census
print only); R344_TABLE_LITERALS = the sealed r314..r339 set
(r341 verbatim) UNION the r341 record set {1.489, 2.888, 5.86,
0.944, 0.833, 0.266, 0.386, 3.796, 0.7485, 1.7305, 0.3875,
0.0868, 0.4867, 0.1113, 0.0259, 7.61, 3.17, 2.31, 2.22, 1.41,
1.3091, 0.065, 24.0, -0.214, 0.313, 12.53, 4.2, 1.237, 0.161,
0.173, 4.8, 2.17, 1.503, 1.266, 1.164, 1.055, 1.088, 1.098,
0.427, 0.961, 1.236, 1.214, 0.997} UNION the r321 record set
{1.3056, 2.47, 2.39, 2.38, 7.97, 1.0536, 3.7157, 0.4579} (the
r341 displays 2.09 / 3.27 / 0.80 / 2.1 / 1.0 / 0.94 are OMITTED
from the forbidden set to avoid collisions with innocent small
toy rationals -- disclosed curation, r337/r339/r341
convention);
runtime <= 1800 s; smoke = w9 + controls + toys + scope/purity
audits + the exact path wards across the grid + the w9
Fractions bit-equality at both sealed pins + e1-e4 mutants;
ladder, extensions, EXT3, anchors, census, balance curve,
freezes, coverage, composition and adjudication skipped.
DISCLOSED PRE-SPEC INPUT (no scratch run of THIS probe): every
anchor band is an r306/r316/r321/r324/r324-pre/r339/r341 RECORD
number adopted as-is; the grid endpoints and their columns
(R* = 3/2, R_ALT = 7/4) are BANKED record columns -- the four
INTERIOR grid points (8/5, 5/3, 12/7, 2) have NEVER been
evaluated; the algebraic pair-limit reading of R = 2 is derived
algebra (R_c < 2 for every pair child), disclosed above; the
choice rule, the eligibility guard, the partition rule, the
coverage form and the six letters are sealed BEFORE evaluation;
the sealed toy pins are computed BY HAND from the r341 sealed
toys (stop toy (7, 1, 5, 3): hsh = 1/2 and E3h = 43/32 and
W_B = 1 for R in (3/2, 8/5, 5/3, 12/7); hsh = 0 and E3h = 0 and
W_B = 31/16 for R in (7/4, 2); hand-off bound 49/32, no-msh
mutant 49/16, break 49/32; even toy (3, 1, 1, 1): hsh = 0 and
W_B = 20/9 at EVERY grid R), no machine run; GENUINELY OPEN
quantities of this round: every balance-curve column at the
four interior grid points, all exponents e_H(R)/e_G(R), the
R_HAT adjudication, all two-scale freeze constants
C_H(R, a)/C_G(R, a), every coverage census, the partition
accuracy, the F_A insertion values of the EXT3 anchors, the
kz55/deep-anchor anatomy, the world curves and the composed
m_0* -- NONE was computed before this spec was frozen; the six
sealed letters are symmetric and total -- the tree maps every
outcome to exactly one letter by CONTRACT.

SEALED VERDICT FORM (frozen BEFORE evaluation, joined with '+'):
  R344_ANCHORS(identity wards, r306 C_2, r316 rho + C_small + n,
    r324-pre C_M2 + violator set, r339 record, r324 chain, r341
    record complete incl. both R* columns + freezes + exponents
    + envelope composition, r321 F_A top-3)
+ SEAL(path wards across the grid + Fractions bit-equality at
    both sealed pins + grounding + purity + toys)
+ BALANCE(the curve: hsh/msh/E3h-share/W_B/empty-good per grid
    R; e_H(R)/e_G(R) + halves; R_HAT + eligibility + interior +
    halves-stability + equilibration census)
+ COVERAGE(two-scale freezes per (R, a); min-cover census on
    the 51 test rows + named + midband; third-arm usage;
    partition rule accuracy; the r341 component-AND comparison)
+ ANATOMY(kz55 eps-level section at R* and R_HAT; deep-anchor
    heavy section + third-arm cross-check)
+ [exactly one of] TWO_SCALE_GO / TWO_SCALE_PARTIAL /
    THRESHOLD_NOT_CANONICAL / RESIDUAL_FAMILY_IRREDUCIBLE /
    BALANCE_STATE_NOT_EXACT / TARGET_LEAK
+ COMPOSITION(the polylog chain with explicit constants + m_0*
    vs the r324 route 10^59.6 + the r306 census 10^13.5;
    printed ALWAYS, typed)
+ MUSTFAIL_LEDGER(e1-e4 + scopes).
Honesty before beauty: the path identities, the hand-off
algebra, the Fractions toys and grid pins, the tree logic and
the purity audits are EXACT (Fractions/AST-decided); every
census, constant, violation count and exponent is MEASURED on
the finite ladder (+ the 12 adopted EXT3 anchors) only; the
R_HAT selection consumes the test-cohort exponent columns (the
formula is sealed, its output is data-dependent -- DISCLOSED,
the same status as every max-cal freeze); a certifying cover
fixes a proof TARGET (a theorem candidate with explicit
constants), it proves NO cofinal law -- the ladder-to-m_0* step
stays the disclosed extrapolation hypothesis; the disjunctive
coverage form bounds E3 by the SUM of the arm budgets on the
covered set, disclosed above -- no sharper constant is claimed;
r243-r343 stand.

RECORD TABLES (inserted AFTER the record run -- the only
post-freeze docstring edit; freeze SPEC_SHA bf16912825178c44,
pre-freeze commit d875647c; protocol: smoke pass 1 = 41/41
(0.8 s, run pre-commit, disclosed in the commit message);
calibration pass 1 = first full evaluation, 38/41 -- the three
FAILs were GATE-READING bugs of this probe's anchor gates, NOT
machinery: (a1, disclosed amendment, code-only, no sealed
surface touched) the r341 narrative 'E3h med 5.86' is the
DERIVED display med(E3) x med(share) = 6.22 x 0.944, not a raw
column median (ladder med 5.59 / full med 5.70) -- G37 now
gates the two raw anchors E3 med 6.22 + share med 0.944 and the
derived product; the r341 record number 'kz51 7.61x' is the
COLUMN VALUE E3h/(log m)^2, not the ratio over C_H(2) (which is
19.6x) -- G38 now gates the column value; plus one print
argument-order slip on the envelope pair (gate condition was
already correct); calibration pass 3 = 41/41, wall 164.8 s;
record run1/run2 after this insertion, identical up to the
runtime line):
MAIN VERDICT: TWO_SCALE_PARTIAL(the min-coverage CERTIFIES at
R_HAT = 7/4 at a = 1 -- 0 violations on the 51 test rows, named
4/4, midband 6/6 -- but the sealed stop rule is NOT CANONICAL:
the dyadic halves pick 8/5 vs 7/4, halves-stable DENIED; R*
remains a tuning surface in the exponent language, said
honestly).  THE HEADLINE FINDINGS:
(1) THE MASS-BALANCE CURVE IS MEASURED (the four interior grid
points are new): hsh med [0.872, 0.603, 0.371, 0.291, 0.266,
0.000], E3h share med [0.944, 0.771, 0.491, 0.447, 0.386,
0.000], W_B med [1.489, 2.410, 3.206, 3.659, 3.796, 6.096],
empty-good counts [23, 11, 7, 7, 7, 2], empty-heavy [0, 0, 0,
0, 0, 71] over the grid (3/2, 8/5, 5/3, 12/7, 7/4, 2); at R = 2
the algebraic pair limit fires as derived (NO pair ever heavy,
71/77 rungs lose the heavy arm entirely, e_H degenerate,
eligibility guard fired as designed).  THE ARM EXPONENTS CROSS
EXACTLY ONCE: e_H = [+0.313, +0.377, +0.358, +0.373, +0.149,
deg], e_G = [-0.214, -0.075, +0.153, +0.159, +0.225, +0.464],
sign(e_H - e_G) = [+, +, +, +, -, -] -- the equilibration point
sits in the gap (12/7, 7/4); the sealed argmin-max formula
picks R_HAT = 7/4 (interior, eligible) but the halves pick 8/5
vs 7/4 -- the crossing is real, its LOCATION is not yet
halves-stable on this ladder.
(2) THE THREE-ARM MIN-COVERAGE CERTIFIES EVERYWHERE: 0
violations on the 51 test rows at EVERY grid point already at
a = 1 (at R_HAT: C_H(1) = 3.9859 hand-off scale, C_G(1) =
1.1409 eps-chain scale, third arm = banked r321 1.3056 F_A^2;
freeze table per (R, a) in the gate log; at R = 2 the heavy
freeze is empty, C_H = 0, and the good + third arms carry
alone).  The r341 component-AND form at R_HAT still fails
(viol 13/10/10 at a = 1/2/3) -- the complementarity
(disjunctive) form is what certifies, exactly the r337
directive.  THE THIRD ARM IS LOAD-BEARING: 8 of 51 test rows at
(R_HAT, a = 1) are covered ONLY by the r321 sliding bound --
including the entire EXT3-B deep-anchor family 6/6 (kz51
F_A(ins) 5.54 ratio 0.19 = reserve 5.3x, kz54 3.91/0.16, kz42
2.44/0.29, kz62 2.38/0.31, kz56 1.14/0.15, kz58 0.61/0.31) --
the r329 out-of-sample reading (reserves 5.0-6.1) reproduces
under the r321 insertion convention at the frozen banked
constant.
(3) THE PARTITION IS NOT hsh-IDENTIFIABLE (honest negative):
the sealed rule (hsh >= 1/2 -> heavy arm) predicts the actual
minimal covering arm on only 7/51 covered test rows; 20 rows
are covered ONLY by the non-predicted arm -- the covering-arm
partition is posthoc relative to the hsh coordinate; a
source-side predictor of the partition remains open.
(4) kz55 ANATOMY: the eps-argmax sits at level 1 (of K = 7) --
the EARLY-level class (same class as the r339 kz53 level-2
anomaly), NOT a leaf effect; rebalancing does NOT absorb it
(at R_HAT its good tree grows: W_B 2.888 -> 8.459, still above
the good freeze) -- but the THIRD arm covers kz55 comfortably
(F_A 1.63, ratio 0.14): the single r341 Form-1 violator is a
sliding-bound row, not an eps-chain row.
(5) THE HONEST COMPOSITION: E[X_inf^3] <= [3.9859 + 1.1409]
(log m)^1 + 1.3056 x 5.54^2 (log m)^2 pointwise on the covered
set (theorem-candidate form, third-arm term included since the
minimal cover uses it) => m_0* = 10^22.6 -- BEATS the r324
measured route 10^59.6 and the r341 envelope typing 10^24.0
(now WITH a certifying cover, which r341 did not have), still
above the r306 census reading 10^13.5.  A_tot = 2 via the
third-arm term; F_Amax = 5.54 is the kz51 insertion value --
the uniform constant inherits the deep-anchor spike,
disclosed.
WORLDS (census): the BALANCE CURVE separates where the r341
single-point budget was world-blind -- ladder hsh med falls
0.872 -> 0.266 across the grid while SCRAMBLE stays heavy-flat
(0.867, 0.867, 0.867, 0.733, 0.667, 0.000; E3h share 0.997 ->
0.993 at 7/4); twin w13 collapses fast (share 0.961 -> 0.042);
EPSTEIN is good-dominated from the start (share 0.427 ->
0.243); w9 all-heavy until the pair limit.
ANCHORS bit-near: r314 identity 4.5e-17; r306 C_2 1.069
(0/57); r316 n 65 + rho quartet + C_small 1.0694@kz18;
r324-pre C_M2 2.2557 + the seven m2 violators EXACT; r339
record W_F med 265.54 / W_G med 13.69 / hsh med 0.872 / Gmax
4.303 / e(W_F) +0.956 / e(m2M3) +0.112; r324 chain e_tot
+0.172; r341 record COMPLETE (W_B med 1.489 / kz55 2.888 / E3
med 6.22 / share 0.944 / reserve 4.8x-2.1x / ALT hsh 0.266 /
ALT share 0.386 / ALT W_B 3.796 / C_H(2) 0.3875 v11 / kz51
excess 7.61 / C_B(2) 0.1113 v1 / kz55 1.41x / e(W_B) -0.214 /
e(E3h) +0.313 / envelopes 1.3091 + 0.0650 / m_0* 10^24.0 /
r306 census solve 10^13.5); r321 F_A top-3 2.47/2.39/2.38
through EFP.local_ratio on the module-own q_max column.
SEAL: grid tilt 8.6e-16, partition 4.0e-16, envelope viol
4.2e-16, heavy hand-off viol 0.0 across ALL SIX grid points,
martingale wards 2.2e-16, FRACTIONS BIT-EQUALITY at the two
sealed pins 3/2 + 12/7 on w9 + w13 EXACT, grounding 3.4e-16,
purity clean, toys exact (grid pins 43/32 | 31/16, hand-off
49/32 slack 3/16, e4 break 49/32); must-fails e1
protocol-CAUGHT twice (AST rho + toy pick 4 != 0) / e2
protocol-CAUGHT twice (AST rho + toy flip ('G','H')) / e3
AST-CAUGHT (S3) / e4 CAUGHT exact (49/32) + m5a/m5b FLAGGED.

NO RH CLAIM IN EITHER DIRECTION.  NOT evidence for or against RH.
"""

from __future__ import annotations

import argparse
import ast
import hashlib
import math
import os
import sys
import time
from fractions import Fraction as Fr

import numpy as np

HERE = os.path.dirname(os.path.abspath(__file__))
if HERE not in sys.path:
    sys.path.insert(0, HERE)

import bordered_hankel_probe as BH             # noqa: E402 r244
import cancellation_adjudication_probe as CA   # noqa: E402 r263
import coupledtau_probe as CT                  # noqa: E402 r257
import terminal_crossratio_probe as TX         # noqa: E402 r260
import border_resolvent_identity_probe as BR   # noqa: E402 r266
import phase_bulk_bound_probe as PBB           # noqa: E402 r269
import l2_deterministic_cancellation_probe as L2D  # noqa: E402 r287
import window_border_transfer_probe as WBT     # noqa: E402 r298
import renyi3_probe as RY3                     # noqa: E402 r306
import signed_cubic_flux_probe as SCF          # noqa: E402 r314
import two_regime_bound_probe as TRB           # noqa: E402 r316
import exception_families_probe as EFP         # noqa: E402 r317
import continuous_coordinate_probe as CCP      # noqa: E402 r321
import fa_provenance_probe as FAP              # noqa: E402 r324-pre
import qmax_m2_origin_probe as QMO             # noqa: E402 r324
import group_mass_cap_probe as GMC             # noqa: E402 r327
import companion_orbit_packing_probe as COP    # noqa: E402 r333
import fold_density_dictionary_probe as FDD    # noqa: E402 r339
import fold_bellman_reverse_holder_probe as FBR  # noqa: E402 r341
import port_integrable_kernel_probe as PIK     # noqa: E402 v881
import principal_bessel_probe as PB            # noqa: E402 r243
import v563_paper2_readouts as core            # noqa: E402 READ-ONLY

MAIN_WINDOWS = (9, 13)
CTRL_FLIPS = {"EPSTEIN": 25, "SCRAMBLE": 21, "SMOOTH": 27}
H_CAP = 900
B57 = 5.0 / 7.0
M_W = math.sqrt(B57)
CHEAP_EXPECT = 35
EXC_KZ_EXPECT = (15, 20, 22, 36, 38, 39, 52)
TB_WARD_BAR = 1e-9
TB_WARD_BAR_DEEP = 3e-6
TB_WARD_BAR_X3 = 3e-5
TB_WARD_BAR_CTRL = 1e-6
DEEP_N = 400
ID_BAR = 1e-12
AC_BAR = 1e-9
EXT_H_MAX = 1300
K_EXT = 15
EXT_NW_EXPECT = (942, 1218)
EXT2_H_MAX = 1650
EXT2_POOL_CAP = 12
K_EXT2 = 8
EXT3_KZ_B = (42, 51, 54, 56, 58, 62)
EXT3_KZ_A = (96, 123, 125, 127, 128, 130)
EXT3_NW_MIN = 1721
EXT3_NW_MAX = 2577
ATOM_BAR = 1e-9
REC3_BAR = 1e-13
REC3_BAR_X3 = 1e-12
TEL_BAR = 1e-13
BND_BAR = 1e-13
CHAIN_BAR = 1e-9
SA_BAR = 1e-12
TREE_BAR = 1e-9
DICT_BAR = 1e-9
TILT_BAR = 1e-9
WQ_BAR = 1e-9
PART_BAR = 1e-9
BK_BAR = 1e-9
ENV_BAR = 1e-9
PGI_BAR = 1e-9
HVY_BAR = 1e-9
JEN_BAR = 1e-12
DEG_FLOOR = 1e-6
MULT_CAP = 2
N_CAL = 5
R_GRID = (1.5, 1.6, 5.0 / 3.0, 12.0 / 7.0, 1.75, 2.0)
FR_GRID = (Fr(3, 2), Fr(8, 5), Fr(5, 3), Fr(12, 7), Fr(7, 4),
           Fr(2))
R_STAR_IDX = 0
R_ALT_IDX = 4
FR_PIN_IDX = (0, 3)
ELIG_FRAC = 0.5
HSH_SPLIT = 0.5
GA_FAM = (1, 2, 3)
GSQ_R321 = 1.3056
NAMED_KZ = (53, 83, 67, 55)
MIDBAND_KZ = (73, 76, 61, 95, 98, 109)
MUT_MIN = 1e-6
TOY_BAR = 1e-12
EDGE_F = 0.20
INF_SENT = 1e300
CERT_GUARD = 1e299
CRIT_EXP = 0.224
N2_EXP_NEED = 0.888
R306_C2 = 1.069
R306_C2_TOL = 0.005
N344_REF = 65
R316_RHO = {53: 1.0490, 67: 1.0536, 55: 0.4821, 83: 0.7790}
R316_RHO_TOL = 0.005
R316_CSMALL = 1.0694
R316_CSMALL_TOL = 0.005
R316_CSMALL_KZ = 18
R324P_CM2 = 2.2557
R324P_CM2_TOL = 0.005
R324P_M2VIOL = (53, 67, 83, 76, 61, 28, 109)
R339_WF_MED = 265.54
R339_WF_MED_TOL = 0.01
R339_WG_MED = 13.69
R339_WG_MED_TOL = 0.01
R339_HSH_MED = 0.872
R339_HSH_MED_TOL = 0.001
R339_GMX_MAX = 4.303
R339_GMX_MAX_TOL = 0.001
R339_EWF = 0.956
R339_EWF_TOL = 0.001
R339_EM3 = 0.112
R339_EM3_TOL = 0.001
R324_EG = 0.158
R324_EG_TOL = 0.002
R324_EM2 = 0.014
R324_EM2_TOL = 0.002
R324_ETOT = 0.172
R324_ETOT_TOL = 0.003
R324_M0_L10 = 59.6
R341_WB_MED = 1.489
R341_WB_MED_TOL = 0.005
R341_WB_KZ55 = 2.888
R341_WB_KZ55_TOL = 0.005
R341_E3_MED = 6.22
R341_E3_MED_TOL = 0.02
R341_E3H_MED = 5.86
R341_E3H_MED_TOL = 0.02
R341_HSHARE_MED = 0.944
R341_HSHARE_MED_TOL = 0.001
R341_HRES_MED = 4.8
R341_HRES_MED_TOL = 0.1
R341_HRES_MIN = 2.1
R341_HRES_MIN_TOL = 0.1
R341_ALT_HSH_MED = 0.266
R341_ALT_HSH_MED_TOL = 0.001
R341_ALT_HSHARE_MED = 0.386
R341_ALT_HSHARE_MED_TOL = 0.001
R341_ALT_WB_MED = 3.796
R341_ALT_WB_MED_TOL = 0.005
R341_CH2 = 0.3875
R341_CH2_TOL = 0.001
R341_CH2_VIOL = 11
R341_KZ51_RATIO = 7.61
R341_KZ51_RATIO_TOL = 0.02
R341_CB2 = 0.1113
R341_CB2_TOL = 0.001
R341_CB2_VIOL = 1
R341_KZ55_RATIO = 1.41
R341_KZ55_RATIO_TOL = 0.02
R341_E_WB = -0.214
R341_E_WB_TOL = 0.003
R341_E_E3H = 0.313
R341_E_E3H_TOL = 0.003
R341_ENV_H3 = 1.3091
R341_ENV_H3_TOL = 0.002
R341_ENV_G3 = 0.0650
R341_ENV_G3_TOL = 0.002
R341_M0_ENV_L10 = 24.0
R341_M0_ENV_TOL = 0.2
R306_M0_L10 = 13.5
R306_M0_TOL = 0.2
R321_FA_TOP = {53: 2.47, 83: 2.39, 67: 2.38}
R321_FA_TOL = 0.01
R335_CD = {0: 141.84, 1: 33.06, 2: 7.71}
R344_TABLE_LITERALS = frozenset(FBR.R341_TABLE_LITERALS | {
    1.489, 2.888, 5.86, 0.944, 0.833, 0.266, 0.386, 3.796,
    0.7485, 1.7305, 0.3875, 0.0868, 0.4867, 0.1113, 0.0259,
    7.61, 3.17, 2.31, 2.22, 1.41, 1.3091, 0.065, 24.0, -0.214,
    0.313, 12.53, 4.2, 1.237, 0.161, 0.173, 4.8, 2.17, 1.503,
    1.266, 1.164, 1.055, 1.088, 1.098, 0.427, 0.961, 1.236,
    1.214, 0.997, 1.3056, 2.47, 2.39, 2.38, 7.97, 3.7157,
    0.4579})

SPEC_SHA = hashlib.sha256(__doc__.encode("utf-8")).hexdigest()
T0_WALL = time.time()
CHECKS: list = []


def check(name, ok, detail):
    ok = bool(ok)
    CHECKS.append((name, ok, detail))
    print("  [%s] %-42s %s" % ("PASS" if ok else "FAIL", name, detail),
          flush=True)
    return ok


def info(msg):
    print("  [INFO] " + msg, flush=True)


def section(t):
    print("\n" + "-" * 78 + "\n" + t + "\n" + "-" * 78, flush=True)


def firewall_audit():
    src = open(os.path.abspath(__file__), "r", encoding="utf-8").read()
    tree = ast.parse(src)
    forb = {"zeta" + "zero", "n" + "zeros", "prime" + "range",
            "is" + "prime", "gram" + "point"}
    bad = []
    for node in ast.walk(tree):
        nm = node.attr if isinstance(node, ast.Attribute) else (
            node.id if isinstance(node, ast.Name) else None)
        if nm and nm.lower() in forb:
            bad.append("%s@%d" % (nm, node.lineno))
    return (not bad), ("NO zero/prime oracles; every readout "
                       "consumes node positions + signed weights + "
                       "the r244 chain rows; ground truth (branch "
                       "labels, true R/t/Z) enters gates and census "
                       "tables only"
                       if not bad else "; ".join(bad))


def antigate_fragment_audit():
    """AST scan for forbidden method families (identifiers only;
    the fragment table itself is assembled from split strings)."""
    frags = ("cand_" + "unroll", "poly" + "fit", "curve_" + "fit",
             "lst" + "sq", "mini" + "mize", "Line" + "arRegression")
    src = open(os.path.abspath(__file__), "r", encoding="utf-8").read()
    tree = ast.parse(src)
    hits = []
    for node in ast.walk(tree):
        nm = None
        if isinstance(node, ast.Attribute):
            nm = node.attr
        elif isinstance(node, ast.Name):
            nm = node.id
        elif isinstance(node, ast.FunctionDef):
            nm = node.name
        if nm and any(f in nm for f in frags):
            hits.append("%s@%d" % (nm, node.lineno))
    return hits


BOUND_FORBIDDEN = COP.BOUND_FORBIDDEN
PHI3_FORBIDDEN = COP.PHI3_FORBIDDEN
QMAX_FORBIDDEN = COP.QMAX_FORBIDDEN


def scope_audit(funcname, forbidden):
    """walk ONLY the named function's subtree; flag any withheld/
    truth-side identifier or dict key from the sealed set."""
    src = open(os.path.abspath(__file__), "r", encoding="utf-8").read()
    tree = ast.parse(src)
    hits = []
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef) and node.name == funcname:
            for sub in ast.walk(node):
                nm = None
                if isinstance(sub, ast.Attribute):
                    nm = sub.attr
                elif isinstance(sub, ast.Name):
                    nm = sub.id
                elif isinstance(sub, ast.Constant) \
                        and isinstance(sub.value, str):
                    nm = sub.value
                if nm in forbidden:
                    hits.append("%s@%d" % (nm, sub.lineno))
    return hits


def literal_audit(funcname):
    """the RECORD-LITERAL audit: walk ONLY the named function's
    subtree and flag any numeric constant whose 4-decimal rounding
    lies in the sealed r314..r341 record set."""
    src = open(os.path.abspath(__file__), "r", encoding="utf-8").read()
    tree = ast.parse(src)
    hits = []
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef) and node.name == funcname:
            for sub in ast.walk(node):
                if isinstance(sub, ast.Constant) \
                        and isinstance(sub.value, (int, float)) \
                        and not isinstance(sub.value, bool):
                    if round(float(sub.value), 4) \
                            in R344_TABLE_LITERALS:
                        hits.append("%s@%d" % (repr(sub.value),
                                               sub.lineno))
    return hits


# ---------------- module-own builders.  Source-pure: the balance
# ---------------- builders consume the sealed tree (via the
# ---------------- imported FBR path layer), the source-pure
# ---------------- exponent/hsh columns and rank order ONLY; the
# ---------------- withheld terminal drive key, the branch label,
# ---------------- the cubic target M_3 / rho_2 and the q_max
# ---------------- RECORD are forbidden (AST identifier scan +
# ---------------- literal scan).
def rhat_rule(e_h, e_g, elig):
    """THE SEALED CHOICE FORMULA (frozen pre-record): over the
    ELIGIBLE grid indices, R_HAT = argmin of max(e_H, e_G); ties
    by smaller |e_H - e_G|, then smaller grid index (= smaller
    R).  Returns (index, eligible_any).  If nothing is eligible,
    returns the protocol head index 0 with eligible_any False.
    Consumes the two exponent tuples + the eligibility mask
    only."""
    cand = [i for i in range(len(e_h)) if elig[i]]
    if not cand:
        return 0, False
    key = [(max(e_h[i], e_g[i]), abs(e_h[i] - e_g[i]), i)
           for i in cand]
    key.sort()
    return key[0][2], True


def partition_predict(hsh_val):
    """THE SEALED PARTITION RULE (a-priori): predicted covering
    arm 'H' iff the rung's heavy path-mass share hsh >=
    HSH_SPLIT, else 'G'.  Consumes the builder-side hsh value
    only."""
    return "H" if hsh_val >= HSH_SPLIT else "G"


def good_level_anatomy(gtree, rstar):
    """THE GOOD-TREE LEVEL CENSUS (for the kz55 section): per
    level k the number of UNSTOPPED (no heavy strict ancestor,
    not itself heavy) internal nodes, the maximal Gamma among
    them with its (n(v), max R_c), and the integrated good-chain
    ratio gamma_k = B_{k+1}/B_k.  Consumes the tree only."""
    mass = gtree["mass"]
    cnt = gtree["cnt"]
    kptr = gtree["kptr"]
    kk = gtree["depth"]
    m = gtree["m"]
    if m < 2 or float(mass[0][0]) <= 0.0:
        return ()
    droot = float(mass[0][0]) / float(m)
    xs = [(mass[k] / np.maximum(cnt[k], 1)) / droot
          for k in range(kk + 1)]
    uns = [np.zeros(len(mass[k]), dtype=bool) for k in range(kk + 1)]
    uns[0][0] = True
    blist = []
    for k in range(kk + 1):
        wk = cnt[k].astype(float) / float(m)
        blist.append(float(np.sum(wk[uns[k]] * xs[k][uns[k]] ** 3))
                     if k == 0 else 0.0)
    out = []
    for k in range(kk):
        pt = kptr[k]
        mv = mass[k]
        cv = cnt[k]
        ngood = 0
        gmax = 0.0
        garg = (0, 0.0)
        for i in range(len(mv)):
            a, b = int(pt[i]), int(pt[i + 1])
            if mv[i] <= 0.0 or b - a < 1:
                continue
            mc = mass[k + 1][a:b]
            cc = cnt[k + 1][a:b].astype(float)
            pv = cc / float(cv[i])
            rv = (mc / np.maximum(cc, 1.0)) / (mv[i] / float(cv[i]))
            gam = float(np.sum(pv * rv ** 3))
            mxr = float(np.max(rv))
            heavy = mxr > rstar
            uns[k + 1][a:b] = bool(uns[k][i]) and (not heavy)
            if bool(uns[k][i]) and not heavy:
                ngood += 1
                if gam > gmax:
                    gmax = gam
                    garg = (int(cv[i]), mxr)
        wk1 = cnt[k + 1].astype(float) / float(m)
        b_next = float(np.sum(wk1[uns[k + 1]]
                              * xs[k + 1][uns[k + 1]] ** 3))
        b_cur = blist[k] if k == 0 else out[-1][4]
        gamk = b_next / b_cur if b_cur > 0.0 else 1.0
        out.append((k, ngood, gmax, garg, b_next, gamk))
    return tuple(out)


def two_scale_tree_verdict(leak, brk, go, partial, thresh):
    """the sealed six-letter verdict tree (booleans only; total,
    exactly one fires; order sealed): TARGET_LEAK >
    BALANCE_STATE_NOT_EXACT > TWO_SCALE_GO > TWO_SCALE_PARTIAL >
    THRESHOLD_NOT_CANONICAL > RESIDUAL_FAMILY_IRREDUCIBLE."""
    if leak:
        return "TARGET_LEAK"
    if brk:
        return "BALANCE_STATE_NOT_EXACT"
    if go:
        return "TWO_SCALE_GO"
    if partial:
        return "TWO_SCALE_PARTIAL"
    if thresh:
        return "THRESHOLD_NOT_CANONICAL"
    return "RESIDUAL_FAMILY_IRREDUCIBLE"


def mutant_rhat_posthoc(idx_seen, rho, cbar):
    """e1 MUST-FAIL MUTANT (protocol): the grid point re-picked
    AFTER SIGHT of the evaluated violation column (consumes rho)
    -- the BOUND_FORBIDDEN scope audit must FLAG it AND on the
    sealed toy it returns index 4 != the protocol head index 0
    (the choice rule is sealed as a FORMULA BEFORE the
    freeze)."""
    pick = 0
    for i, r in zip(idx_seen, rho):
        if r > cbar:
            pick = max(pick, int(i))
    return pick


def mutant_partition_posthoc(letters_seen, rho, cbar):
    """e2 MUST-FAIL MUTANT (protocol): the arm assignment flipped
    per rung AFTER SIGHT of the evaluated violation column
    (consumes rho) -- AST-FLAGGED and on the sealed toy it
    returns ('G', 'H') != the sealed hsh-rule output ('H',
    'G')."""
    out = []
    for a, r in zip(letters_seen, rho):
        if r > cbar:
            out.append("G" if a == "H" else "H")
        else:
            out.append(a)
    return tuple(out)


def mutant_cheavy_from_target(ev):
    """e3 MUST-FAIL MUTANT: a 'heavy freeze constant' derived
    from the cubic TARGET record (consumes cm/S3) -- the
    PHI3_FORBIDDEN scan must FLAG this while the module-own
    balance builders stay clean."""
    return 0.5 * abs(float(ev["cm"]["S3"])) ** (2.0 / 3.0)


def mutant_handoff_no_msh(gtree, rstar):
    """e4 MUST-FAIL MUTANT: the hand-off bound WITHOUT the msh
    factor -- claims E3h <= (m q_max)^2 alone; on the sealed
    stop toy (7, 1, 5, 3) it returns 49/16 while the exact
    hand-off bound is (m q_max)^2 msh = 49/32 (break 49/32
    EXACT in the Fractions pin)."""
    pbs = FBR.path_bellman_state(gtree, rstar)
    if not pbs["ok"]:
        return 0.0
    mass = gtree["mass"]
    m = gtree["m"]
    aroot = float(mass[0][0])
    ymax = float(np.max(mass[gtree["depth"]])) * float(m) / aroot
    return ymax ** 2


def mutant_gift_bound(rc, P):
    """m5a MUST-FAIL MUTANT: a 'balance orientation' consuming
    the withheld ground-truth terminal drive key -- the scope
    audit must FLAG this."""
    s = math.copysign(1.0, rc["t_term"])
    return s * float(np.sum(np.abs(P) ** 3))


def mutant_branch_peek(rc, P):
    """m5b MUST-FAIL MUTANT (world-blindness break simulated): a
    'budget constant' consuming the branch label -- the scope
    audit must FLAG this."""
    c = 0.5 if rc["g_branch"] >= 0.0 else 2.0
    return c * float(np.sum(np.abs(P) ** 3))


# ---------------- sealed Fractions toys (the r341 sealed toys
# ---------------- re-pinned across the grid, computed BY HAND)
def fr_balance_toy():
    """the sealed stop toy (7, 1, 5, 3) across the WHOLE sealed
    grid: hsh = 1/2, E3h = 43/32, W_B = 1 for R in (3/2, 8/5,
    5/3, 12/7) (node (7, 1) heavy: max R = 7/4 > R); hsh = 0,
    E3h = 0, W_B = 31/16 for R in (7/4, 2) (7/4 is NOT > 7/4 --
    the boundary is strict, r339 convention); hand-off bound
    (m q_max)^2 msh = 49/32 with slack 3/16 on the stopped
    branch.  The even toy (3, 1, 1, 1): hsh = 0 and W_B = 20/9
    at EVERY grid R (max pair R there is 3/2, never > 3/2).
    Returns worst dev (Fractions, bit-exact)."""
    worst = Fr(0)
    stop = [Fr(7), Fr(1), Fr(5), Fr(3)]
    even = [Fr(3), Fr(1), Fr(1), Fr(1)]
    for j, fr_r in enumerate(FR_GRID):
        st = FBR.fr_path_state(stop, fr_r)
        if j <= 3:
            devs = [abs(st["e3h"] - Fr(43, 32)),
                    abs(st["msh"] - Fr(1, 2)),
                    abs(st["wb"] - 1),
                    abs(st["hslack"] - Fr(3, 16))]
        else:
            devs = [abs(st["e3h"]), abs(st["msh"]),
                    abs(st["wb"] - Fr(31, 16))]
        devs.append(abs(st["e3"] - Fr(31, 16)))
        devs.append(abs(st["tilt"] - st["e3"]))
        devs.append(st["pgi_dev"])
        worst = max(worst, max(devs))
        se = FBR.fr_path_state(even, fr_r)
        worst = max(worst,
                    abs(se["e3"] - Fr(20, 9)),
                    abs(se["e3h"]),
                    abs(se["wb"] - Fr(20, 9)))
    return worst


def fr_handoff_toy():
    """the e4 pin in exact Fractions on the stop toy at R* = 3/2:
    the correct hand-off bound is (m q_max)^2 msh = (7/4)^2 x
    1/2 = 49/32 (slack over E3h = 43/32 is 3/16 EXACT); the
    no-msh mutant claims (m q_max)^2 = 49/16 -- break 49/32
    EXACT.  Returns (worst dev, mutant break)."""
    st = FBR.fr_path_state([Fr(7), Fr(1), Fr(5), Fr(3)], Fr(3, 2))
    ymax = Fr(7, 4)
    hb = ymax ** 2 * st["msh"]
    mut = ymax ** 2
    brk = mut - hb
    devs = [abs(hb - Fr(49, 32)),
            abs(hb - st["e3h"] - st["hslack"]),
            abs(mut - Fr(49, 16)),
            abs(brk - Fr(49, 32))]
    return max(devs), brk


def main():
    par = argparse.ArgumentParser()
    par.add_argument("--smoke", action="store_true")
    args = par.parse_args()
    smoke = args.smoke
    windows = (9,) if smoke else MAIN_WINDOWS

    print("=" * 78)
    print("fold_two_scale_balance_probe -- "
          "PRIME.L2.FOLD_TWO_SCALE_BALANCE.01 (round 344, "
          "the R* mass-balance / two-scale freeze round)")
    print("SPEC_SHA %s   R341_SHA %s   R339_SHA %s   R321_SHA %s"
          % (SPEC_SHA[:16], FBR.SPEC_SHA[:16], FDD.SPEC_SHA[:16],
             CCP.SPEC_SHA[:16]))
    print("mode: %s" % ("SMOKE (w9 + controls + toys + scope/"
                        "purity audits + grid path wards + w9 "
                        "Fractions pins + e1-e4; ladder, "
                        "extensions, EXT3, anchors, balance "
                        "curve, freezes, coverage, composition "
                        "and adjudication skipped)"
                        if smoke else "FULL RECORD"))
    print("=" * 78)

    section("S0  FIREWALL + PREDEFINITION")
    okf, det = firewall_audit()
    check("G01-firewall", okf, det)
    check("G02-predefinition", True,
          "THE TWO-SCALE BALANCE ROUND (the named r341 follow-up): "
          "on the SEALED iterated r270 pairing tree (FDD/FBR "
          "verbatim) the stop threshold R runs over the sealed "
          "grid %s; the choice rule is the sealed FORMULA R_HAT "
          "= argmin over eligible R of max(e_H, e_G) (ties: "
          "smaller |e_H - e_G|, then smaller R; eligibility = "
          "both arms alive on >= half the test rungs); two-scale "
          "freezes C_H on the hand-off column (m q_max)^2 msh "
          "and C_G on the eps-chain column W_B, each mid-ladder "
          "max-cal (r337 directive); MIN-COVERAGE (each rung "
          "covered by >= 1 arm on its own scale, third arm = "
          "the banked r321 sliding bound %.4f F_A^2, never "
          "recalibrated); partition rule hsh >= %.1f -> H "
          "sealed a-priori; verdict tree TWO_SCALE_GO / "
          "TWO_SCALE_PARTIAL / THRESHOLD_NOT_CANONICAL / "
          "RESIDUAL_FAMILY_IRREDUCIBLE / BALANCE_STATE_NOT_EXACT "
          "/ TARGET_LEAK sealed BEFORE evaluation"
          % (str(tuple(round(r, 4) for r in R_GRID)), GSQ_R321,
             HSH_SPLIT))
    frag = antigate_fragment_audit()
    sc_own = []
    for fn in ("rhat_rule", "partition_predict",
               "good_level_anatomy", "two_scale_tree_verdict",
               "fr_balance_toy", "fr_handoff_toy"):
        sc_own += scope_audit(fn, BOUND_FORBIDDEN)
        sc_own += scope_audit(fn, PHI3_FORBIDDEN)
        sc_own += scope_audit(fn, QMAX_FORBIDDEN)
    sc_a = scope_audit("mutant_gift_bound", BOUND_FORBIDDEN)
    sc_b = scope_audit("mutant_branch_peek", BOUND_FORBIDDEN)
    check("G03-scope-audits", (not frag) and (not sc_own)
          and len(sc_a) >= 1 and len(sc_b) >= 1,
          "fragment audit clean (%d hits); the module-own "
          "balance builders clean vs BOUND_FORBIDDEN + "
          "PHI3_FORBIDDEN + QMAX_FORBIDDEN (%d hits) -- the "
          "balance side consumes the sealed tree + source-pure "
          "columns + rank order ONLY; m5a gift-bound FLAGGED "
          "(%s); m5b branch-peek FLAGGED (%s)"
          % (len(frag), len(sc_own),
             sc_a[0] if sc_a else "MISS",
             sc_b[0] if sc_b else "MISS"))

    # ---------------- S1: census + controls (r341 scaffold verbatim)
    section("S1  CENSUS + CONTROLS + EXTENSIONS + EXT3")
    packs = {("w%d" % kz): BH.wpack(kz) for kz in windows}
    rr9 = core.build_window(9)
    N_E = int(math.floor(math.exp(2.0 * rr9["alpha"]))) + 1
    lamE = PIK.lambda_eps(N_E)
    nn_idx = np.nonzero(np.abs(lamE) > 1e-12)[0]
    ug9, uw9 = PB.smooth_comb(rr9["alpha"])
    ctrl_defs = (("EPST", dict(comb=(
        np.log(nn_idx.astype(float)),
        2.0 * lamE[nn_idx] / np.sqrt(nn_idx.astype(float))))),
        ("SCR", dict(scramble_seed=1)),
        ("SMOOTH", dict(comb=(ug9, uw9))))
    ctrl = {c: BH.wpack(9, base_kw=kw) for c, kw in ctrl_defs}
    long_names = {"EPST": "EPSTEIN", "SCR": "SCRAMBLE",
                  "SMOOTH": "SMOOTH"}
    okC = all(packs[t]["nf"] is None for t in packs)
    okCf = all(ctrl[c]["nf"] == CTRL_FLIPS[long_names[c]]
               for c in ctrl)
    if smoke:
        ladder = []
        ext = []
        ext2 = []
        ext3 = []
        okL = True
    else:
        kzs = []
        ekz = []
        ekz2 = []
        for kz in core.frame_a_zones():
            h = PIK.build_rung(kz)["h"]
            if h <= H_CAP:
                kzs.append(kz)
            elif h <= EXT_H_MAX:
                ekz.append(kz)
            elif h <= EXT2_H_MAX:
                ekz2.append((h, kz))
        ladder = [BH.wpack(kz) for kz in kzs]
        ladder.sort(key=lambda p: (p["N"], p["kz"]))
        epool = [BH.wpack(kz) for kz in ekz]
        epool.sort(key=lambda p: (p["N"], p["kz"]))
        ext = epool[:K_EXT]
        ekz2.sort()
        pool2 = epool[K_EXT:] + [BH.wpack(kz)
                                 for _h, kz in ekz2[:EXT2_POOL_CAP]]
        pool2.sort(key=lambda p: (p["N"], p["kz"]))
        ext2 = [p for p in pool2 if p["nf"] is None][:K_EXT2]
        okL = (len(ladder) == 42
               and all(p["nf"] is None for p in ladder))
        ext3 = [BH.wpack(kz) for kz in EXT3_KZ_B + EXT3_KZ_A]
        ext3.sort(key=lambda p: (p["N"], p["kz"]))
    check("G10-census-controls", okC and okCf and okL,
          "MAIN free prefix positive at full depth (%s, N = %s); "
          "control flips re-derived %s; cofinal ladder %d rungs "
          "POSITIVE_PREFIX %s, N in [%s, %s]"
          % (str(sorted(packs)),
             str({t: packs[t]["N"] for t in packs}),
             str({c: ctrl[c]["nf"] for c in ctrl}),
             len(ladder),
             "42/42" if okL and ladder else ("n/a (SMOKE)"
                                             if smoke else "FAIL"),
             ladder[0]["N"] if ladder else "-",
             ladder[-1]["N"] if ladder else "-"))
    if smoke:
        check("G12-extension-census", True, "SMOKE: skipped")
        check("G13-ext2-census", True, "SMOKE: skipped")
        check("G14-ext3-admission", True, "SMOKE: skipped")
    else:
        check("G12-extension-census",
              len(ext) == K_EXT
              and ext[0]["N"] == EXT_NW_EXPECT[0]
              and ext[-1]["N"] == EXT_NW_EXPECT[1]
              and all(p["nf"] is None for p in ext),
              "r286-aligned extension: %d anchors, N_w %d..%d "
              "(expected %d..%d), POSITIVE_PREFIX %d/%d"
              % (len(ext), ext[0]["N"] if ext else -1,
                 ext[-1]["N"] if ext else -1, EXT_NW_EXPECT[0],
                 EXT_NW_EXPECT[1],
                 sum(1 for p in ext if p["nf"] is None), len(ext)))
        check("G13-ext2-census",
              len(ext2) <= K_EXT2
              and all(p["nf"] is None for p in ext2),
              "EXT2 (r316 A5 rule verbatim, census-grade): "
              "selected %d POSITIVE_PREFIX anchors, N_w %s..%s"
              % (len(ext2),
                 ext2[0]["N"] if ext2 else "-",
                 ext2[-1]["N"] if ext2 else "-"))
        check("G14-ext3-admission",
              len(ext3) == 12
              and all(p["nf"] is None for p in ext3)
              and min(p["N"] for p in ext3) == EXT3_NW_MIN
              and max(p["N"] for p in ext3) == EXT3_NW_MAX,
              "EXT3 = the sealed r329 RECORD selection (committed "
              "8cbd95f9, r335/r339/r341 adoption verbatim): 12 "
              "anchors (B %s + A %s), POSITIVE_PREFIX %d/12, N_w "
              "%d..%d (record %d..%d) -- PURE TEST rows, never "
              "calibration"
              % (str(EXT3_KZ_B), str(EXT3_KZ_A),
                 sum(1 for p in ext3 if p["nf"] is None),
                 min(p["N"] for p in ext3),
                 max(p["N"] for p in ext3),
                 EXT3_NW_MIN, EXT3_NW_MAX))

    pool = ladder if not smoke else [packs["w9"]]
    mains = [packs["w%d" % kz] for kz in windows]

    def rung_rec(p):
        N = p["N"]
        rows = p["rows"]
        r, t, ap, bp = TX.drive_arrays(rows, N)
        g = CA.g_gap(r[:N - 1], t, ap, bp)
        xu, wu = CT.union_arrays(p["d"])
        bx, bw = CT.union_arrays(p["dsm"])
        lo = min(float(np.min(xu)), float(np.min(bx)))
        hi = max(float(np.max(xu)), float(np.max(bx)))
        v2 = BR.eval_scaled(rows, bx, N - 2)
        fac = math.exp(rows[N - 2]["Ls"] - rows[N - 1]["Ls"]) \
            / math.sqrt(abs(rows[N - 1]["eta"]))
        ct = bw * bx * v2 * fac
        v2w = BR.eval_scaled(rows, xu, N - 2)
        cw = wu * xu * v2w * fac
        o = np.argsort(bx, kind="stable")
        return dict(kz=p["kz"], N=N, g_branch=g,
                    t_term=float(t[N - 2]), ct=ct, bx=bx, bw=bw,
                    v2=v2, fac=fac, xu=xu, wu=wu, cw=cw, o=o,
                    lo=lo, hi=hi, p=p, nf=p["nf"])

    recs = [rung_rec(p) for p in pool]
    erecs = [rung_rec(p) for p in ext] if not smoke else []
    e2recs = [rung_rec(p) for p in ext2] if not smoke else []
    x3recs = [rung_rec(p) for p in ext3] if not smoke else []
    mrecs = [rung_rec(p) for p in mains]
    crecs = {c: rung_rec(ctrl[c]) for c in ctrl}
    cheap = [rc for rc in recs if rc["g_branch"] >= 0.0]
    exc = [rc for rc in recs if rc["g_branch"] < 0.0]
    exc_kz = tuple(sorted(rc["kz"] for rc in exc))
    if smoke:
        check("G11-branch-reproduction", recs[0]["g_branch"] >= 0.0,
              "SMOKE: w9 branch %s (g %+.3f); ladder "
              "decomposition skipped"
              % ("CHEAP" if recs[0]["g_branch"] >= 0 else
                 "EXCEPTION", recs[0]["g_branch"]))
    else:
        e_cheap = sum(1 for rc in erecs + e2recs + x3recs
                      if rc["g_branch"] >= 0.0)
        e_exc = [rc["kz"] for rc in erecs + e2recs + x3recs
                 if rc["g_branch"] < 0.0]
        check("G11-branch-reproduction",
              len(cheap) == CHEAP_EXPECT
              and exc_kz == tuple(sorted(EXC_KZ_EXPECT))
              and all(rc["g_branch"] >= 0 for rc in mrecs),
              "r263 branch rule reproduced EXACTLY: cheap %d/42, "
              "exception set %s == the named 7; mains %s; "
              "EXT+EXT2+EXT3 census: %d cheap / %d exception %s"
              % (len(cheap), str(exc_kz),
                 "; ".join("w%d g %+.3f CHEAP" % (rc["kz"],
                                                  rc["g_branch"])
                           for rc in mrecs),
                 e_cheap, len(e_exc), str(e_exc)))

    # ---------------- S2: decomposition wards + eval
    section("S2  EXACT DECOMPOSITION + TREE-LEDGER WARDS")
    tb_worst = 0.0
    tb_deep = 0.0
    tb_ext = 0.0
    tb_x3 = 0.0
    tb_ctrl = 0.0
    for rc in recs + mrecs:
        absum = float(np.sum(np.abs(rc["ct"])))
        dev = abs(float(np.sum(rc["ct"])) - rc["t_term"]) \
            / max(absum, 1e-300)
        if rc["N"] > DEEP_N:
            tb_deep = max(tb_deep, dev)
        else:
            tb_worst = max(tb_worst, dev)
    for rc in erecs + e2recs:
        dev = abs(float(np.sum(rc["ct"])) - rc["t_term"]) \
            / max(float(np.sum(np.abs(rc["ct"]))), 1e-300)
        tb_ext = max(tb_ext, dev)
    for rc in x3recs:
        dev = abs(float(np.sum(rc["ct"])) - rc["t_term"]) \
            / max(float(np.sum(np.abs(rc["ct"]))), 1e-300)
        tb_x3 = max(tb_x3, dev)
    for c in crecs:
        rc = crecs[c]
        dev = abs(float(np.sum(rc["ct"])) - rc["t_term"]) \
            / max(float(np.sum(np.abs(rc["ct"]))), 1e-300)
        tb_ctrl = max(tb_ctrl, dev)
    check("G20-contribution-ward", tb_worst <= TB_WARD_BAR
          and tb_deep <= TB_WARD_BAR_DEEP
          and tb_ext <= TB_WARD_BAR_DEEP
          and tb_x3 <= TB_WARD_BAR_X3
          and tb_ctrl <= TB_WARD_BAR_CTRL,
          "sum_b ct_b == t_{N-2} on %d rungs + %d ext + %d ext2 "
          "+ %d ext3 + %d mains + 3 controls: worst dev/absmass "
          "%.1e main N<=%d (bar %.0e) / %.1e deep / %.1e "
          "ext+ext2 (bar %.0e) / %.1e ext3 (bar %.0e) / %.1e "
          "controls (bar %.0e)"
          % (len(recs), len(erecs), len(e2recs), len(x3recs),
             len(mrecs), tb_worst, DEEP_N, TB_WARD_BAR, tb_deep,
             tb_ext, TB_WARD_BAR_DEEP, tb_x3, TB_WARD_BAR_X3,
             tb_ctrl, TB_WARD_BAR_CTRL))

    def eval_rung(rc):
        o = rc["o"]
        bxs = rc["bx"][o]
        cts = rc["ct"][o]
        ed = PBB.mask_edge(bxs, rc["lo"], rc["hi"], EDGE_F)
        cb = cts[~ed]
        xb = bxs[~ed]
        runs = PBB.runs_split(cb)
        Sr = [float(np.sum(cb[a:b])) for a, b, _s in runs]
        sg = [s for _a, _b, s in runs]
        alt_ok = all(sg[i + 1] == -sg[i]
                     for i in range(len(sg) - 1))
        R = sum(Sr)
        P = L2D.blocks_level2(Sr)
        brk, m, jb = WBT.block_breaks(xb, runs)
        Pb = np.bincount(jb, weights=cb, minlength=m) \
            if m else np.zeros(0)
        Pw = WBT.aggregate_blocks(rc["xu"], rc["cw"], rc["lo"],
                                  rc["hi"], brk, m)
        Pd = Pb - Pw
        cm = RY3.cubic_moments(Pd)
        absm = float(np.sum(np.abs(rc["ct"]))) \
            + float(np.sum(np.abs(rc["cw"])))
        degenerate = (cm["L1"] <= DEG_FLOOR * absm)
        edw = PBB.mask_edge(rc["xu"], rc["lo"], rc["hi"], EDGE_F)
        xw = rc["xu"][~edw]
        vw = -rc["cw"][~edw]
        jw = np.searchsorted(brk, xw) if m else np.zeros(0, int)
        jb2 = np.searchsorted(brk, xb) if m else np.zeros(0, int)
        mism = int(np.sum(jb2 != jb))
        pos_all = np.concatenate([xb, xw])
        val_all = np.concatenate([cb, vw])
        blk_all = np.concatenate([jb, jw]).astype(int)
        src_all = np.concatenate([np.zeros(len(xb)),
                                  np.ones(len(xw))])
        if m and not degenerate:
            gen = SCF.fold_genealogy(pos_all, val_all, blk_all, m)
            sct = SCF.signed_cube_terms(gen["G1"], gen["gblk"], m)
            ft = SCF.flux_telescope(gen["G1"], gen["ptr"], m)
            x_dev = float(np.max(np.abs(sct["x"] - Pd))
                          / max(np.max(np.abs(Pd)), 1e-300))
            sig = sct["sig"]
            cube = sct["cube"]
            A1 = np.bincount(blk_all, weights=np.abs(val_all),
                             minlength=m)
            scale3 = float(np.sum(A1 ** 3))
            sc_j = np.maximum(A1 ** 3, 1e-300)
            C_far_flux = float(np.sum(sig * ft["F_end"]))
            C_bnd = float(np.sum(sig * ft["F_open"]))
            rec3 = abs(C_far_flux + sct["C_pair"] + sct["C_full"]
                       + C_bnd - cube) / max(scale3, 1e-300)
            tel_dev = float(np.max(np.abs(ft["F_end"]
                                          - sct["far"]) / sc_j))
            bnd_dev = float(np.max(np.abs(ft["F_open"]) / sc_j))
            mx_mult = int(np.max(gen["mult"])) if gen["ng"] else 0
            trs = TRB.two_regime_state(sct["x"], sct["Q2"],
                                       sct["Q3"], gen["G1"],
                                       gen["ptr"], ft["F_end"],
                                       ft["F_open"],
                                       ft["edge_abs"], m)
            rho2 = RY3.renyi3_ratio(cm["S3"], m, 2)
            mqs = FAP.m2_qmax_state(sct["x"])
            led327 = GMC.group_mass_ledger(pos_all, val_all,
                                           blk_all, src_all, m)
            gtree = FDD.fold_mass_tree_exact(sct["x"])
            dst = FDD.descendant_density_martingale(gtree)
            dic = FDD.martingale_moment_dictionary(sct["x"])
            pbg = tuple(FBR.path_bellman_state(gtree, rr)
                        for rr in R_GRID)
        else:
            gen = sct = ft = None
            x_dev = 0.0
            cube = 0.0
            rec3 = tel_dev = bnd_dev = 0.0
            C_bnd = 0.0
            mx_mult = 0
            A1 = np.zeros(0)
            trs = dict(nrm=0.0, coll=0.0, dflux=0.0, bnd=0.0,
                       fe=0.0, fcix=0.0, qmax=0.0, cnt3=0.0,
                       phiL1=0.0, phiL2=0.0, phiH1=0.0,
                       phiH2=0.0, L=0.0)
            rho2 = 0.0
            mqs = dict(qm=0.0, m2=0.0, ymx=0.0, maj=0.0)
            led327 = None
            gtree = None
            dst = FDD.descendant_density_martingale(
                dict(mass=[np.zeros(1)], cnt=[np.ones(1, int)],
                     kptr=[], depth=0, m=0))
            dic = dict(d1=0.0, d2=0.0, d3=0.0, ymx=0.0)
            pbg = tuple(FBR.path_bellman_state(
                dict(mass=[np.zeros(1)], cnt=[np.ones(1, int)],
                     kptr=[], depth=0, m=0), rr)
                for rr in R_GRID)
        return dict(alt_ok=alt_ok, R=R, P=P, m=m, Pd=Pd, cm=cm,
                    degenerate=degenerate, mism=mism, x_dev=x_dev,
                    cube=cube, rec3=rec3, tel_dev=tel_dev,
                    bnd_dev=bnd_dev, C_bnd=C_bnd,
                    mx_mult=mx_mult, gen=gen, sct=sct, ft=ft,
                    trs=trs, rho2=rho2, A1=A1, mqs=mqs,
                    led327=led327, gtree=gtree, dst=dst, dic=dic,
                    pbg=pbg, pos_all=pos_all, val_all=val_all,
                    blk_all=blk_all, brk=brk)

    all_rc = recs + mrecs + erecs + e2recs + x3recs
    for rc in all_rc:
        rc["ev"] = eval_rung(rc)
    for c in crecs:
        crecs[c]["ev"] = eval_rung(crecs[c])
    pool_all = all_rc + [crecs[c] for c in crecs]

    alt_all = all(rc["ev"]["alt_ok"] for rc in all_rc)
    bid_worst = 0.0
    ac_worst = 0.0
    for rc in pool_all:
        ev = rc["ev"]
        bid_worst = max(bid_worst,
                        abs(sum(ev["P"]) - ev["R"])
                        / max(abs(ev["R"]), 1e-300))
        A = L2D.autocorr_full(ev["P"])
        s_all = A[0] + 2.0 * float(np.sum(A[1:]))
        ac_worst = max(ac_worst,
                       abs(s_all - sum(ev["P"]) ** 2)
                       / max(A[0], 1e-300))
    check("G21-block-and-autocorr-identity",
          alt_all and bid_worst <= ID_BAR and ac_worst <= AC_BAR,
          "runs alternate on every world AND sum P == R exact "
          "(worst rel %.1e, bar %.0e) AND (sum P)^2 == A(0) + 2 "
          "sum A(h) exact (worst %.1e x A(0), bar %.0e) over %d "
          "worlds" % (bid_worst, ID_BAR, ac_worst, AC_BAR,
                      len(pool_all)))

    live = [rc for rc in pool_all if not rc["ev"]["degenerate"]]
    deg_note = [c for c in crecs if crecs[c]["ev"]["degenerate"]]
    x_w = max(rc["ev"]["x_dev"] for rc in live)
    mism_tot = sum(rc["ev"]["mism"] for rc in pool_all)
    led_dev = 0.0
    x3_mult_ok = True
    for rc in live:
        ev = rc["ev"]
        gen = ev["gen"]
        l327 = ev["led327"]
        if gen["ng"] != l327["ng"]:
            led_dev = max(led_dev, 1.0)
            continue
        if gen["ng"]:
            sc = max(float(np.max(np.abs(gen["G1"]))), 1e-300)
            led_dev = max(
                led_dev,
                float(np.max(np.abs(l327["G1"] - gen["G1"]))) / sc,
                float(np.max(np.abs(l327["mult"] - gen["mult"]))),
                float(np.max(np.abs(l327["gblk"] - gen["gblk"]))))
    for rc in x3recs:
        if rc["ev"]["mx_mult"] > MULT_CAP:
            x3_mult_ok = False
    check("G22-genealogy-ledger-identity",
          x_w <= ATOM_BAR and mism_tot == 0 and led_dev <= SA_BAR
          and x3_mult_ok,
          "the r314 fold genealogy covers every block value on %d "
          "live worlds (group-sum dev %.1e, bar %.0e); run "
          "assignment == breakpoint search (%d mismatches); the "
          "r327 GMC ledger segments IDENTICALLY to the genealogy "
          "(worst dev %.1e, bar %.0e); EXT3 fold multiplicity <= "
          "%d on 12/12 (%s)%s"
          % (len(live), x_w, ATOM_BAR, mism_tot, led_dev, SA_BAR,
             MULT_CAP, "OK" if x3_mult_ok else "BROKEN",
             "; DEGENERATE guard fired (pre-declared): "
             + ", ".join(deg_note) if deg_note else ""))

    # ---------------- S3: Leg 0 anchors
    section("S3  LEG 0 -- ANCHOR REGRESSION (r306/r316/r324-pre "
            "+ r339 + r324 CHAIN + r341 RECORD + r321 F_A)")
    x3_ids = set(id(rc) for rc in x3recs)
    live_69 = [rc for rc in live if id(rc) not in x3_ids]
    rec3_w = max(rc["ev"]["rec3"] for rc in live_69)
    tel_w = max(rc["ev"]["tel_dev"] for rc in live_69)
    bnd_w = max(rc["ev"]["bnd_dev"] for rc in live_69)
    rec3_x = max((rc["ev"]["rec3"] for rc in x3recs), default=0.0)
    tel_x = max((rc["ev"]["tel_dev"] for rc in x3recs),
                default=0.0)
    bnd_x = max((rc["ev"]["bnd_dev"] for rc in x3recs),
                default=0.0)
    check("G30-r314-identity-wards",
          rec3_w <= REC3_BAR and tel_w <= TEL_BAR
          and bnd_w <= BND_BAR and rec3_x <= REC3_BAR_X3
          and tel_x <= REC3_BAR_X3 and bnd_x <= REC3_BAR_X3,
          "the r314 identity live: ladder worlds recomposition "
          "%.1e / telescope %.1e / boundary %.1e (bars %.0e); "
          "EXT3 %.1e / %.1e / %.1e (bar %.0e); DISCLOSED slim "
          "anchor set -- the full chain is re-warded by the "
          "sealed r321..r341 probes"
          % (rec3_w, tel_w, bnd_w, REC3_BAR, rec3_x, tel_x,
             bnd_x, REC3_BAR_X3))
    if smoke:
        ev9s = recs[0]["ev"]
        pb0 = ev9s["pbg"][R_STAR_IDX]
        info("SMOKE: w9 m %d K %d E3 %.4f grid hsh %s"
             % (ev9s["m"], pb0["kk"], pb0["e3"],
                str([round(p["hsh"], 3) for p in ev9s["pbg"]])))
        check("G31-r306-bound-live", True, "SMOKE: skipped")
        check("G32-r316-anchors", True, "SMOKE: skipped")
        check("G33-r324pre-m2-anchor", True, "SMOKE: skipped")
        srt = []
        n344 = 0
    else:
        srt57 = sorted(recs + erecs,
                       key=lambda rc: (rc["N"], rc["kz"]))
        rhoT2 = [rc["ev"]["rho2"] for rc in srt57]
        C2r, _j, _d = RY3.calib_freeze(rhoT2, range(N_CAL))
        viol2 = sum(1 for v in rhoT2 if v > C2r)
        check("G31-r306-bound-live",
              abs(C2r - R306_C2) <= R306_C2_TOL and viol2 == 0,
              "r306 pointwise bound live at A = 2: C_2 %.3f (rec "
              "%.3f tol %.3f, first-%d freeze), violations %d/%d"
              % (C2r, R306_C2, R306_C2_TOL, N_CAL, viol2,
                 len(srt57)))
        srt_all65 = sorted(recs + erecs + e2recs,
                           key=lambda rc: (rc["N"], rc["kz"]))
        srt = [rc for rc in srt_all65
               if rc["ev"]["mx_mult"] <= MULT_CAP]
        n344 = len(srt)
        rho_all = [rc["ev"]["rho2"] for rc in srt]
        m_all = [rc["ev"]["m"] for rc in srt]
        rho_kz = {rc["kz"]: rc["ev"]["rho2"] for rc in srt}
        sm_i, ca_i, te_i = TRB.split_midladder(n344)
        C_small = max(rho_all[i] for i in sm_i)
        j_cs = max(sm_i, key=lambda i: rho_all[i])
        check("G32-r316-anchors",
              n344 == N344_REF
              and all(abs(rho_kz.get(kz, -1.0) - R316_RHO[kz])
                      <= R316_RHO_TOL for kz in R316_RHO)
              and abs(C_small - R316_CSMALL) <= R316_CSMALL_TOL
              and srt[j_cs]["kz"] == R316_CSMALL_KZ,
              "r316 anchors reproduced: class ladder n = %d (rec "
              "%d); rho kz53/kz67/kz55/kz83 = %.4f/%.4f/%.4f/"
              "%.4f (rec tol %.3f); C_small %.4f @ kz%d"
              % (n344, N344_REF, rho_kz.get(53, -1.0),
                 rho_kz.get(67, -1.0), rho_kz.get(55, -1.0),
                 rho_kz.get(83, -1.0), R316_RHO_TOL, C_small,
                 srt[j_cs]["kz"]))
        m2_col = [rc["ev"]["mqs"]["m2"] for rc in srt]
        C_M2 = max(m2_col[i] for i in ca_i)
        viol_m2 = tuple(sorted(srt[i]["kz"] for i in te_i
                               if m2_col[i] > C_M2))
        check("G33-r324pre-m2-anchor",
              abs(C_M2 - R324P_CM2) <= R324P_CM2_TOL
              and viol_m2 == tuple(sorted(R324P_M2VIOL)),
              "the r324-pre m2 record reproduced: mid-ladder "
              "freeze C_M2 %.4f (rec %.4f tol %.3f); the seven "
              "test violators %s == the banked set EXACT"
              % (C_M2, R324P_CM2, R324P_CM2_TOL, str(viol_m2)))
    dict2_w = 0.0
    dict3_w = 0.0
    dictq_w = 0.0
    for rc in live:
        ev = rc["ev"]
        dic = ev["dic"]
        mloc = ev["m"]
        dict2_w = max(dict2_w, abs(dic["d2"] - ev["mqs"]["m2"])
                      / max(ev["mqs"]["m2"], 1e-300))
        rid = ev["rho2"] * (math.log(float(mloc)) ** 2)
        dict3_w = max(dict3_w, abs(dic["d3"] - rid)
                      / max(rid, 1e-300))
        dictq_w = max(dictq_w,
                      abs(dic["ymx"] / float(mloc)
                          - ev["mqs"]["qm"])
                      / max(ev["mqs"]["qm"], 1e-300))
    check("G34-dictionary-chain-identity",
          dict2_w <= DICT_BAR and dict3_w <= DICT_BAR
          and dictq_w <= DICT_BAR,
          "THE MOMENT DICTIONARY anchored bit-near on %d live "
          "worlds: E[X_inf^2] == m M_2 (worst rel %.1e), "
          "E[X_inf^3] == m^2 M_3 == rho_2 (log m)^2 (worst "
          "%.1e), max y / m == q_max (worst %.1e; bars %.0e)"
          % (len(live), dict2_w, dict3_w, dictq_w, DICT_BAR))
    if smoke:
        check("G35-r339-record-anchors", True, "SMOKE: skipped")
        check("G36-r324-chain-anchor", True, "SMOKE: skipped")
        check("G37-r341-record-anchors", True, "SMOKE: skipped")
        check("G38-r341-freeze-anchors", True, "SMOKE: skipped")
        check("G39-r321-fa-anchors", True, "SMOKE: skipped")
    else:
        srt_x = sorted(x3recs, key=lambda rc: (rc["N"], rc["kz"]))
        srt_x = [rc for rc in srt_x
                 if rc["ev"]["mx_mult"] <= MULT_CAP
                 and not rc["ev"]["degenerate"]]
        n_x3 = len(srt_x)
        srt_full = srt + srt_x
        n_full = len(srt_full)
        m_full = [rc["ev"]["m"] for rc in srt_full]
        wf_col = [rc["ev"]["dst"]["wf"] for rc in srt_full]
        wg_col = [rc["ev"]["dst"]["wg"] for rc in srt_full]
        hshF_col = [rc["ev"]["dst"]["hsh"] for rc in srt_full]
        gmw_col = [max(rc["ev"]["dst"]["gmx_lv"])
                   if rc["ev"]["dst"]["gmx_lv"] else 1.0
                   for rc in srt_full]
        d2_col = [rc["ev"]["dic"]["d2"] for rc in srt_full]
        d3_col = [rc["ev"]["dic"]["d3"] for rc in srt_full]
        ymx_col = [rc["ev"]["dic"]["ymx"] for rc in srt_full]
        wf_med = float(np.median(wf_col))
        wg_med = float(np.median(wg_col))
        hshF_med = float(np.median(hshF_col))
        gmx_max = max(gmw_col)
        e_wf = L2D.halves_slope([m_full[i] for i in te_i],
                                [max(wf_col[i], 1e-300)
                                 for i in te_i])
        e_d3 = L2D.halves_slope([m_full[i] for i in te_i],
                                [max(d3_col[i], 1e-300)
                                 for i in te_i])
        check("G35-r339-record-anchors",
              abs(wf_med - R339_WF_MED) <= R339_WF_MED_TOL
              and abs(wg_med - R339_WG_MED) <= R339_WG_MED_TOL
              and abs(hshF_med - R339_HSH_MED) <= R339_HSH_MED_TOL
              and abs(gmx_max - R339_GMX_MAX) <= R339_GMX_MAX_TOL
              and abs(e_wf - R339_EWF) <= R339_EWF_TOL
              and abs(e_d3 - R339_EM3) <= R339_EM3_TOL,
              "the r339 RECORD reproduced through the imported "
              "FDD builders on the same 65+%d rows: W_F med %.2f "
              "(rec %.2f), W_G med %.2f (rec %.2f), hsh med %.3f "
              "(rec %.3f), Gamma_max max %.3f (rec %.3f), e(W_F) "
              "%+.3f (rec %+.3f), e(m^2 M_3) %+.3f (rec %+.3f)"
              % (n_x3, wf_med, R339_WF_MED, wg_med, R339_WG_MED,
                 hshF_med, R339_HSH_MED, gmx_max, R339_GMX_MAX,
                 e_wf, R339_EWF, e_d3, R339_EM3))
        e_g324 = L2D.halves_slope(
            [m_full[i] for i in te_i],
            [max(ymx_col[i], 1e-300)
             / math.log(float(m_full[i])) for i in te_i])
        e_m2324 = L2D.halves_slope([m_full[i] for i in te_i],
                                   [max(d2_col[i], 1e-300)
                                    for i in te_i])
        e_tot324 = e_g324 + e_m2324
        check("G36-r324-chain-anchor",
              abs(e_g324 - R324_EG) <= R324_EG_TOL
              and abs(e_m2324 - R324_EM2) <= R324_EM2_TOL
              and abs(e_tot324 - R324_ETOT) <= R324_ETOT_TOL,
              "the r324 MEASURED chain reproduced: e(G/log m) "
              "%+.3f (rec %+.3f), e(m M_2) %+.3f (rec %+.3f), "
              "e_tot %+.3f (rec %+.3f) -- the honest comparison "
              "threshold stays m_0* = 10^%.1f (record, adopted)"
              % (e_g324, R324_EG, e_m2324, R324_EM2, e_tot324,
                 R324_ETOT, R324_M0_L10))
        # r341 record anchors through the imported FBR builders
        e3_col = [rc["ev"]["pbg"][R_STAR_IDX]["e3"]
                  for rc in srt_full]
        e3h_star = [rc["ev"]["pbg"][R_STAR_IDX]["e3h"]
                    for rc in srt_full]
        wb_star = [rc["ev"]["pbg"][R_STAR_IDX]["wb"]
                   for rc in srt_full]
        hsh_star = [rc["ev"]["pbg"][R_STAR_IDX]["hsh"]
                    for rc in srt_full]
        hres_star = [rc["ev"]["pbg"][R_STAR_IDX]["hvy_res"]
                     for rc in srt_full]
        hsh_alt = [rc["ev"]["pbg"][R_ALT_IDX]["hsh"]
                   for rc in srt_full]
        e3h_alt = [rc["ev"]["pbg"][R_ALT_IDX]["e3h"]
                   for rc in srt_full]
        wb_alt = [rc["ev"]["pbg"][R_ALT_IDX]["wb"]
                  for rc in srt_full]
        hshare_star = [e3h_star[i] / max(e3_col[i], 1e-300)
                       for i in range(n_full)]
        hshare_alt = [e3h_alt[i] / max(e3_col[i], 1e-300)
                      for i in range(n_full)]
        kz_rank = {rc["kz"]: i for i, rc in enumerate(srt_full)}
        wb55 = wb_star[kz_rank[55]]
        e3h_med_lad = float(np.median(e3h_star[:n344]))
        e3_med = float(np.median(e3_col))
        e3h_derived = e3_med * float(np.median(hshare_star))
        check("G37-r341-record-anchors",
              abs(float(np.median(wb_star)) - R341_WB_MED)
              <= R341_WB_MED_TOL
              and abs(wb55 - R341_WB_KZ55) <= R341_WB_KZ55_TOL
              and abs(e3_med - R341_E3_MED) <= R341_E3_MED_TOL
              and abs(e3h_derived - R341_E3H_MED)
              <= R341_E3H_MED_TOL
              and abs(float(np.median(hshare_star))
                      - R341_HSHARE_MED) <= R341_HSHARE_MED_TOL
              and abs(float(np.median(hsh_star)) - R339_HSH_MED)
              <= R339_HSH_MED_TOL
              and abs(float(np.median([v for v in hres_star
                                       if v < INF_SENT]))
                      - R341_HRES_MED) <= R341_HRES_MED_TOL
              and abs(min(hres_star) - R341_HRES_MIN)
              <= R341_HRES_MIN_TOL
              and abs(float(np.median(hsh_alt))
                      - R341_ALT_HSH_MED) <= R341_ALT_HSH_MED_TOL
              and abs(float(np.median(hshare_alt))
                      - R341_ALT_HSHARE_MED)
              <= R341_ALT_HSHARE_MED_TOL
              and abs(float(np.median(wb_alt)) - R341_ALT_WB_MED)
              <= R341_ALT_WB_MED_TOL,
              "the r341 RECORD reproduced through the imported "
              "FBR builders, BOTH banked columns: R* = 3/2 (W_B "
              "med %.3f rec %.3f, kz55 %.3f rec %.3f, E3 med "
              "%.2f rec %.2f, E3h derived med(E3) x med(share) "
              "%.2f rec %.2f -- the r341 narrative 'E3h med' is "
              "this derived display, not a raw column median "
              "(ladder med %.2f / full med %.2f), reading fixed "
              "as amendment a1, E3h share med %.3f rec %.3f, "
              "hsh med %.3f, hand-off reserve med %.1f min %.1f "
              "rec %.1f/%.1f) and R_ALT = 7/4 (hsh med %.3f rec "
              "%.3f, share med %.3f rec %.3f, W_B med %.3f rec "
              "%.3f)"
              % (float(np.median(wb_star)), R341_WB_MED, wb55,
                 R341_WB_KZ55, e3_med, R341_E3_MED, e3h_derived,
                 R341_E3H_MED, e3h_med_lad,
                 float(np.median(e3h_star)),
                 float(np.median(hshare_star)),
                 R341_HSHARE_MED, float(np.median(hsh_star)),
                 float(np.median([v for v in hres_star
                                  if v < INF_SENT])),
                 min(hres_star), R341_HRES_MED, R341_HRES_MIN,
                 float(np.median(hsh_alt)), R341_ALT_HSH_MED,
                 float(np.median(hshare_alt)),
                 R341_ALT_HSHARE_MED, float(np.median(wb_alt)),
                 R341_ALT_WB_MED))
        te_x = list(te_i) + list(range(n344, n_full))
        named_rank = {}
        for kz in NAMED_KZ + MIDBAND_KZ:
            named_rank[kz] = kz_rank[kz]
        lg_full = [math.log(float(v)) for v in m_full]
        ch2_col = [e3h_star[i] / lg_full[i] ** 2
                   for i in range(n_full)]
        CH2 = max(ch2_col[i] for i in ca_i)
        vH2 = [i for i in te_x if ch2_col[i] > CH2]
        r51 = ch2_col[kz_rank[51]]
        cb2_col = [wb_star[i] / lg_full[i] ** 2
                   for i in range(n_full)]
        CB2 = max(cb2_col[i] for i in ca_i)
        vB2 = [i for i in te_x if cb2_col[i] > CB2]
        r55 = cb2_col[kz_rank[55]] / CB2
        e_wb = L2D.halves_slope([m_full[i] for i in te_i],
                                [max(wb_star[i], 1e-300)
                                 for i in te_i])
        e_e3h = L2D.halves_slope([m_full[i] for i in te_i],
                                 [max(e3h_star[i], 1e-300)
                                  for i in te_i])
        env_h3 = max(e3h_star[i] / lg_full[i] ** 3
                     for i in range(n_full))
        e3g_star = [rc["ev"]["pbg"][R_STAR_IDX]["e3g"]
                    for rc in srt_full]
        env_g3 = max(e3g_star[i] / lg_full[i] ** 3
                     for i in range(n_full))

        def solve_m0(log_rhs):
            t = math.log(73.0)
            while t < 1e7:
                if CRIT_EXP * t >= log_rhs(t):
                    return t / math.log(10.0)
                t *= 1.02
            return None

        m0_env = solve_m0(lambda t: math.log(
            max((env_h3 + env_g3) * t ** 3, 1e-300)))
        m0_306 = solve_m0(lambda t: math.log(
            max(R306_C2 * t ** 2, 1e-300)))
        check("G38-r341-freeze-anchors",
              abs(CH2 - R341_CH2) <= R341_CH2_TOL
              and len(vH2) == R341_CH2_VIOL
              and abs(r51 - R341_KZ51_RATIO) <= R341_KZ51_RATIO_TOL
              and abs(CB2 - R341_CB2) <= R341_CB2_TOL
              and len(vB2) == R341_CB2_VIOL
              and abs(r55 - R341_KZ55_RATIO) <= R341_KZ55_RATIO_TOL
              and abs(e_wb - R341_E_WB) <= R341_E_WB_TOL
              and abs(e_e3h - R341_E_E3H) <= R341_E_E3H_TOL
              and abs(env_h3 - R341_ENV_H3) <= R341_ENV_H3_TOL
              and abs(env_g3 - R341_ENV_G3) <= R341_ENV_G3_TOL
              and m0_env is not None
              and abs(m0_env - R341_M0_ENV_L10) <= R341_M0_ENV_TOL
              and m0_306 is not None
              and abs(m0_306 - R306_M0_L10) <= R306_M0_TOL,
              "the r341 freezes + exponents + envelope "
              "composition reproduced through the same code "
              "path: C_H(2) %.4f (rec %.4f) viol %d (rec %d) "
              "kz51 excess %.2f (rec %.2f; the r341 record "
              "number is the COLUMN VALUE E3h/(log m)^2, "
              "reading fixed as amendment a1) ratio-over-"
              "constant %.1fx; C_B(2) %.4f (rec %.4f) viol %d "
              "(rec %d) kz55 %.2fx (rec %.2f); e(W_B) %+.3f "
              "(rec %+.3f), e(E3h) %+.3f (rec %+.3f); a=3 "
              "envelopes heavy %.4f (rec %.4f) + good %.4f "
              "(rec %.4f) -> m_0* 10^%.1f (rec 10^%.1f); r306 "
              "census solve 10^%.1f (rec 10^%.1f)"
              % (CH2, R341_CH2, len(vH2), R341_CH2_VIOL, r51,
                 R341_KZ51_RATIO, r51 / CH2, CB2, R341_CB2,
                 len(vB2), R341_CB2_VIOL, r55, R341_KZ55_RATIO,
                 e_wb, R341_E_WB, e_e3h, R341_E_E3H, env_h3,
                 R341_ENV_H3, env_g3, R341_ENV_G3, m0_env,
                 R341_M0_ENV_L10, m0_306, R306_M0_L10))
        # r321 F_A coordinate (third arm), ladder + insertion
        q_lad = [srt[i]["ev"]["mqs"]["qm"] for i in range(n344)]
        fa_lad = EFP.local_ratio(q_lad)
        N_lad = [srt[i]["N"] for i in range(n344)]
        fa_full = list(fa_lad)
        for rc in srt_x:
            fa_full.append(CCP.world_coord(rc["ev"]["mqs"]["qm"],
                                           rc["N"], N_lad, q_lad))
        fa_kz = {srt_full[i]["kz"]: fa_full[i]
                 for i in range(n_full)}
        check("G39-r321-fa-anchors",
              all(abs(fa_kz[kz] - R321_FA_TOP[kz]) <= R321_FA_TOL
                  for kz in R321_FA_TOP),
              "the r321/r317 F_A coordinate reproduced through "
              "EFP.local_ratio on the module-own q_max column: "
              "top-3 kz53 %.2f / kz83 %.2f / kz67 %.2f (rec "
              "%.2f/%.2f/%.2f tol %.2f); EXT3 rows via the r321 "
              "INSERTION RULE (CCP.world_coord, the r329 "
              "convention); F_A range full %.2f..%.2f; the "
              "third-arm constant GSQ = %.4f is the BANKED r321 "
              "record, never recalibrated here"
              % (fa_kz[53], fa_kz[83], fa_kz[67],
                 R321_FA_TOP[53], R321_FA_TOP[83],
                 R321_FA_TOP[67], R321_FA_TOL, min(fa_full),
                 max(fa_full), GSQ_R321))

    # ---------------- S4: seal + purity + toys + live wards
    section("S4  LEG 0b -- SEAL + PURITY + TOYS + LIVE GRID "
            "WARDS")
    pure_lits = []
    for fn in ("rhat_rule", "partition_predict",
               "good_level_anatomy", "two_scale_tree_verdict",
               "fr_balance_toy", "fr_handoff_toy"):
        pure_lits += literal_audit(fn)
    e3_hits = scope_audit("mutant_cheavy_from_target",
                          PHI3_FORBIDDEN)
    e1_hits = scope_audit("mutant_rhat_posthoc", BOUND_FORBIDDEN)
    e2_hits = scope_audit("mutant_partition_posthoc",
                          BOUND_FORBIDDEN)
    check("G40-purity-audits",
          (not sc_own) and (not pure_lits)
          and len(e3_hits) >= 1 and len(e1_hits) >= 1
          and len(e2_hits) >= 1,
          "SOURCE PURITY: the balance builders clean vs the "
          "forbidden sets (%d id hits) and vs the sealed "
          "r314..r341 record-literal set (%d literal hits); e3 "
          "cheavy-from-target FLAGGED (%s); e1 rhat-posthoc "
          "FLAGGED (%s); e2 partition-posthoc FLAGGED (%s)"
          % (len(sc_own), len(pure_lits),
             e3_hits[0] if e3_hits else "MISS",
             e1_hits[0] if e1_hits else "MISS",
             e2_hits[0] if e2_hits else "MISS"))
    fb_dev = fr_balance_toy()
    fh_dev, fh_brk = fr_handoff_toy()
    st_stop = FBR.path_bellman_state(
        FDD.fold_mass_tree_exact([7.0, 1.0, 5.0, 3.0]),
        R_GRID[R_ALT_IDX])
    ok_stop_alt = (st_stop["ok"]
                   and st_stop["e3h"] <= TOY_BAR
                   and abs(st_stop["wb"] - float(Fr(31, 16)))
                   <= TOY_BAR)
    mut4 = mutant_handoff_no_msh(
        FDD.fold_mass_tree_exact([7.0, 1.0, 5.0, 3.0]),
        R_GRID[R_STAR_IDX])
    toy_rhat, toy_elig = rhat_rule((0.3, 0.2, 0.1, 0.15, 0.25,
                                    0.4),
                                   (-0.2, -0.1, 0.05, 0.2, 0.3,
                                    0.5),
                                   (True,) * 6)
    toy_rhat2, toy_elig2 = rhat_rule((0.3,), (0.1,), (False,))
    toy_part = (partition_predict(0.9), partition_predict(0.1))
    mut1 = mutant_rhat_posthoc((0, 1, 2, 3, 4, 5),
                               (0.1, 0.1, 0.1, 0.1, 0.9, 0.1),
                               0.5)
    mut2 = mutant_partition_posthoc(("H", "G"), (0.9, 0.9), 0.5)
    tr_br = (two_scale_tree_verdict(True, True, True, True, True),
             two_scale_tree_verdict(False, True, True, True,
                                    True),
             two_scale_tree_verdict(False, False, True, True,
                                    True),
             two_scale_tree_verdict(False, False, False, True,
                                    True),
             two_scale_tree_verdict(False, False, False, False,
                                    True),
             two_scale_tree_verdict(False, False, False, False,
                                    False))
    ok_tr = tr_br == ("TARGET_LEAK", "BALANCE_STATE_NOT_EXACT",
                      "TWO_SCALE_GO", "TWO_SCALE_PARTIAL",
                      "THRESHOLD_NOT_CANONICAL",
                      "RESIDUAL_FAMILY_IRREDUCIBLE")
    check("G41-toy-exactness",
          fb_dev == 0 and fh_dev == 0 and fh_brk == Fr(49, 32)
          and ok_stop_alt
          and abs(mut4 - float(Fr(49, 16))) <= TOY_BAR
          and toy_rhat == 2 and toy_elig
          and toy_rhat2 == 0 and (not toy_elig2)
          and toy_part == ("H", "G")
          and mut1 == 4 and mut2 == ("G", "H")
          and ok_tr,
          "the Fractions balance toys EXACT: stop toy (7,1,5,3) "
          "across the WHOLE grid worst dev %s (hsh 1/2 -> 0 and "
          "W_B 1 -> 31/16 at the sealed 12/7 | 7/4 boundary, "
          "hand-off 49/32 slack 3/16, strict-boundary pin at R "
          "= 7/4); even toy stable at every R; e4 no-msh mutant "
          "49/16, break 49/32 EXACT (float builder %.4f); "
          "rhat_rule toy argmin-max = index 2 + empty-grid "
          "fallback (0, DENIED); partition rule ('H', 'G'); e1 "
          "mutant picks 4 != protocol head 0; e2 mutant flips "
          "to ('G', 'H') != sealed rule; verdict tree all six "
          "branches EXACT %s"
          % (str(fb_dev), mut4, str(tr_br)))
    # live grid wards: every FBR exact ward at every grid R
    tilt_w = wqd_w = part_w3 = bkd_w = env_w = pgi_w = hvy_w = 0.0
    e3d_w = 0.0
    mart_w = unit_w = rec_w = jen_w = 0.0
    part_w = panc_w = l1rec_w = chain_w = xw_cube = 0.0
    nz_tot = 0
    mult_all_ok = True
    for rc in live:
        ev = rc["ev"]
        trs = ev["trs"]
        nc = trs["nrm"] * ev["cube"]
        xw_cube = max(xw_cube, abs(nc - ev["rho2"])
                      / max(ev["rho2"], 1e-300))
        for a, b in ((nc, trs["phiL1"]),
                     (trs["phiL1"], trs["phiL2"]),
                     (trs["phiL2"], trs["phiH2"]),
                     (nc, trs["phiH1"])):
            chain_w = max(chain_w,
                          max(0.0, a - b) / max(b, 1e-300))
        st = ev["dst"]
        if st["ok"]:
            mart_w = max(mart_w, st["mart_dev"])
            unit_w = max(unit_w, st["unit_dev"])
            rec_w = max(rec_w, st["rec_dev"])
            jen_w = max(jen_w,
                        max((d for d in st["drift"]),
                            default=0.0))
            nz_tot += st["nzero"]
        for pb in ev["pbg"]:
            if pb["ok"]:
                tilt_w = max(tilt_w, pb["tilt_dev"])
                wqd_w = max(wqd_w, pb["wq_dev"])
                part_w3 = max(part_w3, pb["part_dev"])
                bkd_w = max(bkd_w, pb["bk_dev"])
                env_w = max(env_w, pb["env_dev"])
                pgi_w = max(pgi_w, pb["pgi_dev"])
                hvy_w = max(hvy_w, pb["hvy_dev"])
                e3d_w = max(e3d_w, abs(pb["e3"] - ev["dic"]["d3"])
                            / max(ev["dic"]["d3"], 1e-300))
        led = ev["led327"]
        mloc = ev["m"]
        x_abs = np.abs(ev["sct"]["x"])
        Lx = float(np.sum(x_abs))
        A1led = np.bincount(led["gblk"], weights=led["gabs"],
                            minlength=mloc)
        part_w = max(part_w,
                     float(np.max(np.abs(A1led - ev["A1"])))
                     / max(float(np.max(ev["A1"])), 1e-300))
        xled = np.bincount(led["gblk"], weights=led["G1"],
                           minlength=mloc)
        l1rec_w = max(l1rec_w,
                      abs(float(np.sum(np.abs(xled))) - Lx)
                      / max(Lx, 1e-300))
        if led["ng"]:
            panc_w = max(panc_w,
                         float(np.max(led["gabs"]
                                      - led["mult"] * led["gmax"]))
                         / max(float(np.max(led["gabs"])), 1e-300))
        mult_all_ok = mult_all_ok \
            and QMO.mult_ward(ev["gen"]["mult"])[1]
    # the Fractions bit-equality at the two sealed grid pins
    fr_ok = True
    fr_nodes = 0
    for rc in mrecs:
        if rc["ev"]["degenerate"]:
            continue
        leaves = [Fr(float(abs(v)))
                  for v in rc["ev"]["sct"]["x"]]
        for j in FR_PIN_IDX:
            stf = FBR.fr_path_state(leaves, FR_GRID[j])
            okx = (stf["tilt"] == stf["e3"]
                   and stf["e3h"] + stf["e3g"] == stf["e3"]
                   and stf["e3g"] <= stf["wb"]
                   and stf["pgi_dev"] == 0
                   and stf["blist"][-1] == stf["e3g"]
                   and stf["hslack"] >= 0)
            fr_ok = fr_ok and okx
        okm, wm, nm = FDD.fr_mart_check(FDD.fr_pair_tree(leaves))
        fr_ok = fr_ok and okm and (wm == 0)
        fr_nodes += nm
    check("G42-live-grid-wards",
          chain_w <= CHAIN_BAR and xw_cube <= CHAIN_BAR
          and mart_w <= TREE_BAR and unit_w <= TREE_BAR
          and rec_w <= TREE_BAR and jen_w <= JEN_BAR
          and tilt_w <= TILT_BAR and wqd_w <= WQ_BAR
          and part_w3 <= PART_BAR and bkd_w <= BK_BAR
          and env_w <= ENV_BAR and pgi_w <= PGI_BAR
          and hvy_w <= HVY_BAR and e3d_w <= DICT_BAR
          and part_w <= SA_BAR and l1rec_w <= SA_BAR
          and panc_w <= TOY_BAR and fr_ok and nz_tot == 0
          and mult_all_ok,
          "the FULL r341 exact path layer live at EVERY sealed "
          "grid R on %d live worlds: tilted tower %.1e, weight "
          "norm %.1e, stopped partition %.1e, B-chain %.1e, "
          "envelope viol %.1e, Phi-Gamma %.1e, heavy hand-off "
          "viol %.1e, E3 == d3 %.1e (bars %.0e); martingale "
          "wards %.1e/%.1e/%.1e, Jensen %.1e; r316 chain %.1e; "
          "FRACTIONS BIT-EQUALITY at the two sealed pins R = "
          "3/2 and 12/7 on w9+w13: %s (%d martingale nodes); "
          "r327 grounding %.1e/%.1e/%.1e; zero-mass leaves %d; "
          "fold multiplicity <= %d"
          % (len(live), tilt_w, wqd_w, part_w3, bkd_w, env_w,
             pgi_w, hvy_w, e3d_w, TILT_BAR, mart_w, unit_w,
             rec_w, jen_w, chain_w,
             "EXACT" if fr_ok else "BROKEN", fr_nodes, part_w,
             l1rec_w, panc_w, nz_tot, MULT_CAP))

    # ---------------- S5: Leg A -- the mass-balance curve
    section("S5  LEG A -- THE MASS-BALANCE CURVE + THE SEALED "
            "CHOICE RULE")
    if smoke:
        check("G50-balance-curve", True, "SMOKE: skipped")
        check("G51-rhat-adjudication", True, "SMOKE: skipped")
    else:
        cols_by_r = []
        for j, rr in enumerate(R_GRID):
            e3h_j = [rc["ev"]["pbg"][j]["e3h"] for rc in srt_full]
            e3g_j = [rc["ev"]["pbg"][j]["e3g"] for rc in srt_full]
            wb_j = [rc["ev"]["pbg"][j]["wb"] for rc in srt_full]
            hsh_j = [rc["ev"]["pbg"][j]["hsh"] for rc in srt_full]
            msh_j = [rc["ev"]["pbg"][j]["msh"] for rc in srt_full]
            hb_j = [srt_full[i]["ev"]["dic"]["ymx"] ** 2
                    * msh_j[i] for i in range(n_full)]
            shr_j = [e3h_j[i] / max(e3_col[i], 1e-300)
                     for i in range(n_full)]
            emptyg = sum(1 for i in range(n_full)
                         if e3g_j[i] <= 0.0)
            emptyh = sum(1 for i in range(n_full)
                         if e3h_j[i] <= 0.0)
            nh_te = sum(1 for i in te_i if e3h_j[i] > 0.0)
            ng_te = sum(1 for i in te_i if e3g_j[i] > 0.0)
            elig = (nh_te >= ELIG_FRAC * len(te_i)
                    and ng_te >= ELIG_FRAC * len(te_i))
            e_h_j = L2D.halves_slope([m_full[i] for i in te_i],
                                     [max(e3h_j[i], 1e-300)
                                      for i in te_i])
            e_g_j = L2D.halves_slope([m_full[i] for i in te_i],
                                     [max(wb_j[i], 1e-300)
                                      for i in te_i])
            h = len(te_i) // 2
            e_h_a = L2D.halves_slope(
                [m_full[i] for i in te_i[:h]],
                [max(e3h_j[i], 1e-300) for i in te_i[:h]])
            e_h_b = L2D.halves_slope(
                [m_full[i] for i in te_i[h:]],
                [max(e3h_j[i], 1e-300) for i in te_i[h:]])
            e_g_a = L2D.halves_slope(
                [m_full[i] for i in te_i[:h]],
                [max(wb_j[i], 1e-300) for i in te_i[:h]])
            e_g_b = L2D.halves_slope(
                [m_full[i] for i in te_i[h:]],
                [max(wb_j[i], 1e-300) for i in te_i[h:]])
            cols_by_r.append(dict(
                e3h=e3h_j, e3g=e3g_j, wb=wb_j, hsh=hsh_j,
                msh=msh_j, hb=hb_j, shr=shr_j, emptyg=emptyg,
                emptyh=emptyh, elig=elig, e_h=e_h_j, e_g=e_g_j,
                e_h_halves=(e_h_a, e_h_b),
                e_g_halves=(e_g_a, e_g_b)))
            info("R = %-6s hsh med %.3f  msh med %.3f  E3h med "
                 "%8.3f  share med %.3f  W_B med %7.3f  "
                 "empty-good %2d  empty-heavy %2d  e_H %+.3f "
                 "(halves %+.3f/%+.3f)  e_G %+.3f (halves "
                 "%+.3f/%+.3f)  %s"
                 % (("%.4f" % rr), float(np.median(hsh_j)),
                    float(np.median(msh_j)),
                    float(np.median(e3h_j)),
                    float(np.median(shr_j)),
                    float(np.median(wb_j)), emptyg, emptyh,
                    e_h_j, e_h_a, e_h_b, e_g_j, e_g_a, e_g_b,
                    "ELIGIBLE" if elig else "INELIGIBLE"))
        check("G50-balance-curve", True,
              "THE MASS-BALANCE CURVE over the sealed grid %s: "
              "hsh med %s; E3h share med %s; W_B med %s; "
              "empty-good counts %s; the two banked endpoints "
              "reproduce the r339/r341 columns (G37), the four "
              "interior points are NEW"
              % (str(tuple(round(r, 4) for r in R_GRID)),
                 str([round(float(np.median(c["hsh"])), 3)
                      for c in cols_by_r]),
                 str([round(float(np.median(c["shr"])), 3)
                      for c in cols_by_r]),
                 str([round(float(np.median(c["wb"])), 3)
                      for c in cols_by_r]),
                 str([c["emptyg"] for c in cols_by_r])))
        e_h_t = tuple(c["e_h"] for c in cols_by_r)
        e_g_t = tuple(c["e_g"] for c in cols_by_r)
        elig_t = tuple(c["elig"] for c in cols_by_r)
        j_hat, elig_any = rhat_rule(e_h_t, e_g_t, elig_t)
        interior = 0 < j_hat < len(R_GRID) - 1
        # halves stability of the choice
        e_h_a_t = tuple(c["e_h_halves"][0] for c in cols_by_r)
        e_h_b_t = tuple(c["e_h_halves"][1] for c in cols_by_r)
        e_g_a_t = tuple(c["e_g_halves"][0] for c in cols_by_r)
        e_g_b_t = tuple(c["e_g_halves"][1] for c in cols_by_r)
        j_a, el_a = rhat_rule(e_h_a_t, e_g_a_t, elig_t)
        j_b, el_b = rhat_rule(e_h_b_t, e_g_b_t, elig_t)
        halves_stable = elig_any and el_a and el_b \
            and (j_a == j_hat) and (j_b == j_hat)
        canonical = elig_any and interior and halves_stable
        eq_sign = [1 if e_h_t[j] >= e_g_t[j] else -1
                   for j in range(len(R_GRID))]
        eq_cross = [j for j in range(len(R_GRID) - 1)
                    if eq_sign[j] != eq_sign[j + 1]]
        check("G51-rhat-adjudication", True,
              "THE SEALED CHOICE RULE: R_HAT = %s (grid index "
              "%d) by argmin max(e_H, e_G) over the eligible "
              "points (%s); INTERIOR %s; HALVES-STABLE %s "
              "(halves pick %s/%s); CANONICAL %s; equilibration "
              "census: sign(e_H - e_G) = %s, crossings at grid "
              "gaps %s"
              % (("%.4f" % R_GRID[j_hat]), j_hat,
                 str([("%.4f" % R_GRID[j]) for j in
                      range(len(R_GRID)) if elig_t[j]]),
                 str(interior), str(halves_stable),
                 ("%.4f" % R_GRID[j_a]) if el_a else "NONE",
                 ("%.4f" % R_GRID[j_b]) if el_b else "NONE",
                 str(canonical), str(eq_sign), str(eq_cross)))

    # ---------------- S6: Leg B -- two-scale freezes + coverage
    section("S6  LEG B -- TWO-SCALE FREEZES + MIN-COVERAGE + "
            "PARTITION")
    if smoke:
        check("G60-two-scale-freezes", True, "SMOKE: skipped")
        check("G61-min-coverage", True, "SMOKE: skipped")
        check("G62-partition-census", True, "SMOKE: skipped")
    else:
        third_ok = [ev_i["rho2"] <= GSQ_R321 * fa_full[i] ** 2
                    for i, ev_i in
                    enumerate(rc["ev"] for rc in srt_full)]
        cover_by_r = []
        for j in range(len(R_GRID)):
            cj = cols_by_r[j]
            per_a = {}
            for a in GA_FAM:
                CH = max(cj["hb"][i] / lg_full[i] ** a
                         for i in ca_i)
                CG = max(cj["wb"][i] / lg_full[i] ** a
                         for i in ca_i)
                covH = [e3_col[i] <= CH * lg_full[i] ** a
                        for i in range(n_full)]
                covG = [e3_col[i] <= CG * lg_full[i] ** a
                        for i in range(n_full)]
                cov = [covH[i] or covG[i] or third_ok[i]
                       for i in range(n_full)]
                viol = [i for i in te_x if not cov[i]]
                named = sum(1 for kz in NAMED_KZ
                            if cov[named_rank[kz]])
                mb = sum(1 for kz in MIDBAND_KZ
                         if cov[named_rank[kz]])
                third_used = sum(1 for i in te_x
                                 if cov[i] and not covH[i]
                                 and not covG[i])
                per_a[a] = dict(CH=CH, CG=CG, covH=covH,
                                covG=covG, cov=cov, viol=viol,
                                named=named, mb=mb,
                                third_used=third_used)
            aa = None
            for a in GA_FAM:
                if (not per_a[a]["viol"]
                        and per_a[a]["named"] == len(NAMED_KZ)
                        and per_a[a]["CH"] < CERT_GUARD
                        and per_a[a]["CG"] < CERT_GUARD):
                    aa = a
                    break
            cover_by_r.append(dict(per_a=per_a, aa=aa))
            info("R = %-6s " % ("%.4f" % R_GRID[j])
                 + "; ".join(
                     "a=%d C_H %.4f C_G %.4f viol %d/%d named "
                     "%d/4 mb %d/6 third-only %d"
                     % (a, per_a[a]["CH"], per_a[a]["CG"],
                        len(per_a[a]["viol"]), len(te_x),
                        per_a[a]["named"], per_a[a]["mb"],
                        per_a[a]["third_used"])
                     for a in GA_FAM)
                 + "; minimal a = %s" % str(aa))
        check("G60-two-scale-freezes", True,
              "THE TWO-SCALE FREEZES (r337 directive; C_H on the "
              "hand-off column (m q_max)^2 msh, C_G on the "
              "eps-chain column W_B, both mid-ladder max-cal; "
              "third arm = banked r321 %.4f F_A^2): freeze table "
              "printed per (R, a) above; the coverage claim on a "
              "covered set is E3 <= [C_H + C_G] (log m)^a + "
              "%.4f F_Amax^2 (log m)^2 (SUM form, disclosed)"
              % (GSQ_R321, GSQ_R321))
        ch_hat = cover_by_r[j_hat]
        aa_hat = ch_hat["aa"]
        cover0_rhat = aa_hat is not None
        cover0_any_j = [j for j in range(len(R_GRID))
                        if cover_by_r[j]["aa"] is not None]
        # residual census at the best (R, a)
        best_j, best_a, best_v = 0, GA_FAM[0], 10 ** 9
        for j in range(len(R_GRID)):
            for a in GA_FAM:
                nv = len(cover_by_r[j]["per_a"][a]["viol"])
                nn = cover_by_r[j]["per_a"][a]["named"]
                if (nv, -nn) < (best_v,
                                -cover_by_r[best_j]["per_a"][
                                    best_a]["named"]):
                    best_j, best_a, best_v = j, a, nv
        resid = cover_by_r[best_j]["per_a"][best_a]["viol"]
        resid_kz = [srt_full[i]["kz"] for i in resid]
        check("G61-min-coverage", True,
              "THE MIN-COVERAGE CENSUS (each rung covered by >= "
              "1 arm on its own scale, 51 test rows + named): at "
              "R_HAT = %.4f minimal certifying a = %s (%s); "
              "0-violation grid points %s; BEST cell (R = %.4f, "
              "a = %d) with %d residual violators %s"
              % (R_GRID[j_hat], str(aa_hat),
                 "CERTIFIES" if cover0_rhat else "does NOT "
                 "certify", str([("%.4f" % R_GRID[j])
                                 for j in cover0_any_j]),
                 R_GRID[best_j], best_a, len(resid),
                 str(resid_kz)))
        # partition census at R_HAT (display a = minimal or 2)
        a_disp = aa_hat if aa_hat is not None else 2
        pa = ch_hat["per_a"][a_disp]
        hsh_hat = cols_by_r[j_hat]["hsh"]
        pred = [partition_predict(hsh_hat[i])
                for i in range(n_full)]
        agree = 0
        nonpred_only = 0
        ncov = 0
        for i in te_x:
            if not pa["cov"][i]:
                continue
            ncov += 1
            arm = "H" if pa["covH"][i] else (
                "G" if pa["covG"][i] else "T")
            if arm == pred[i]:
                agree += 1
            elif arm != "T" and not (
                    pa["covH"][i] and pa["covG"][i]):
                nonpred_only += 1
        # the r341 component-AND comparison at R_HAT
        and_tab = []
        for a in GA_FAM:
            CHc = max(cols_by_r[j_hat]["e3h"][i]
                      / lg_full[i] ** a for i in ca_i)
            CGc = max(cols_by_r[j_hat]["wb"][i]
                      / lg_full[i] ** a for i in ca_i)
            vAND = [i for i in te_x
                    if cols_by_r[j_hat]["e3h"][i]
                    > CHc * lg_full[i] ** a
                    or cols_by_r[j_hat]["wb"][i]
                    > CGc * lg_full[i] ** a]
            and_tab.append((a, CHc, CGc, len(vAND)))
        check("G62-partition-census", True,
              "THE PARTITION (structure test, sealed rule hsh >= "
              "%.1f -> H): at (R_HAT, a = %d) rule accuracy "
              "%d/%d covered test rows; covered ONLY by the "
              "non-predicted arm %d (the posthoc surface); "
              "third-arm-only coverage %d; the r341 "
              "component-AND comparison at R_HAT: %s -- the "
              "disjunctive form vs the AND form, census"
              % (HSH_SPLIT, a_disp, agree, ncov, nonpred_only,
                 pa["third_used"],
                 "; ".join("a=%d C_H' %.4f C_G' %.4f viol %d"
                           % t for t in and_tab)))

    # ---------------- S7: Leg C -- anatomy
    section("S7  LEG C -- kz55 / DEEP-ANCHOR ANATOMY + THIRD-ARM "
            "CROSS-CHECK")
    if smoke:
        check("G70-kz55-anatomy", True, "SMOKE: skipped")
        check("G71-deep-anchor-anatomy", True, "SMOKE: skipped")
    else:
        i55 = kz_rank[55]
        gt55 = srt_full[i55]["ev"]["gtree"]
        for lbl, rr in (("R* = 3/2", R_GRID[R_STAR_IDX]),
                        ("R_HAT = %.4f" % R_GRID[j_hat],
                         R_GRID[j_hat])):
            an = good_level_anatomy(gt55, rr)
            wbv = FBR.path_bellman_state(gt55, rr)["wb"]
            info("kz55 @ %s: W_B %.3f; per-level (k, #good, "
                 "maxGamma, (n(v), maxR), gamma_k): %s"
                 % (lbl, wbv,
                    "; ".join("(%d, %d, %.3f, (%d, %.2f), %.3f)"
                              % (k, ng, gm, ga[0], ga[1], gk)
                              for k, ng, gm, ga, _b, gk in an)))
        an55 = good_level_anatomy(gt55, R_GRID[R_STAR_IDX])
        eps55 = [(k, max(gk - 1.0, 0.0))
                 for k, _ng, _gm, _ga, _b, gk in an55]
        k_arg = max(eps55, key=lambda t: t[1])[0] if eps55 else -1
        wb55_hat = cols_by_r[j_hat]["wb"][i55]
        cb_hat = cover_by_r[j_hat]["per_a"][a_disp]["CG"]
        check("G70-kz55-anatomy", True,
              "kz55 SECTION (the single r341 Form-1 violator): "
              "eps-argmax level %d of K = %d (r339 kz53 anomaly "
              "was an EARLY level-2 spike -- kz55 %s); at R_HAT "
              "W_B %.3f vs good freeze C_G(a=%d) budget %.4f "
              "(log m)^%d = %.3f -> %s; third arm: F_A %.2f, "
              "rho2/(GSQ F_A^2) = %.2f -> %s"
              % (k_arg, srt_full[i55]["ev"]["pbg"][0]["kk"],
                 "MATCHES the early-level class" if k_arg <= 2
                 else "is a LATE/MID-level class (different "
                 "anatomy)", wb55_hat, a_disp, cb_hat, a_disp,
                 cb_hat * lg_full[i55] ** a_disp,
                 "COVERED" if wb55_hat <= cb_hat
                 * lg_full[i55] ** a_disp else "violating",
                 fa_full[i55],
                 srt_full[i55]["ev"]["rho2"]
                 / (GSQ_R321 * fa_full[i55] ** 2),
                 "COVERED" if third_ok[i55] else "NOT covered"))
        deep_rows = []
        for kz in EXT3_KZ_B:
            i = kz_rank[kz]
            ev_i = srt_full[i]["ev"]
            pbh = ev_i["pbg"][j_hat]
            hb_i = ev_i["dic"]["ymx"] ** 2 * pbh["msh"]
            t_ratio = ev_i["rho2"] / (GSQ_R321 * fa_full[i] ** 2)
            deep_rows.append((kz, ev_i["m"], ev_i["dic"]["ymx"],
                              pbh["msh"], hb_i, fa_full[i],
                              t_ratio, third_ok[i]))
            info("EXT3-B kz%-3d m %3d mq %6.2f msh %.3f HB "
                 "%8.2f F_A(ins) %.2f rho2/(GSQ FA^2) %.2f %s"
                 % (kz, ev_i["m"], ev_i["dic"]["ymx"],
                    pbh["msh"], hb_i, fa_full[i], t_ratio,
                    "THIRD-COVERED" if third_ok[i] else
                    "third-MISS"))
        n_covered_3rd = sum(1 for t in deep_rows if t[7])
        i51 = kz_rank[51]
        check("G71-deep-anchor-anatomy", True,
              "DEEP-ANCHOR SECTION (the r341 heavy-arm violator "
              "family): EXT3-B third-arm coverage %d/6 (r329 "
              "banked reading: the r321 sliding bound covered "
              "exactly these small-gap spikes out-of-sample with "
              "reserves 5.0-6.1 at frozen constant -- "
              "cross-checked here at the r321 INSERTION "
              "convention); kz51: F_A(ins) %.2f, third ratio "
              "%.2f (%s); the r335 mass-arm record C_D = %s is "
              "the adopted comparison scale (census, never "
              "recomputed)"
              % (n_covered_3rd, fa_full[i51],
                 srt_full[i51]["ev"]["rho2"]
                 / (GSQ_R321 * fa_full[i51] ** 2),
                 "COVERED" if third_ok[i51] else "NOT covered",
                 str(R335_CD)))

    # ---------------- S8: Leg D -- adjudication + composition
    section("S8  LEG D -- SEALED ADJUDICATION + COMPOSITION")
    if smoke:
        check("G80-adjudication", True, "SMOKE: skipped")
        check("G81-composition", True, "SMOKE: skipped")
        verdict_main = "SMOKE_NO_ADJUDICATION"
    else:
        leak = bool(sc_own) or bool(pure_lits)
        brk_struct = (tilt_w > TILT_BAR or wqd_w > WQ_BAR
                      or part_w3 > PART_BAR or bkd_w > BK_BAR
                      or env_w > ENV_BAR or pgi_w > PGI_BAR
                      or hvy_w > HVY_BAR or e3d_w > DICT_BAR
                      or mart_w > TREE_BAR or unit_w > TREE_BAR
                      or rec_w > TREE_BAR or not fr_ok
                      or part_w > SA_BAR or l1rec_w > SA_BAR
                      or panc_w > TOY_BAR or not mult_all_ok
                      or fb_dev != 0 or fh_dev != 0)
        fa_max = max(fa_full)
        if cover0_rhat:
            a_star = aa_hat
            CHs = ch_hat["per_a"][a_star]["CH"]
            CGs = ch_hat["per_a"][a_star]["CG"]
            use3 = ch_hat["per_a"][a_star]["third_used"] > 0
            A_tot = max(a_star, 2) if use3 else a_star

            def rhs_tot(t):
                v = (CHs + CGs) * t ** a_star
                if use3:
                    v += GSQ_R321 * fa_max ** 2 * t ** 2
                return v
            m0_new = solve_m0(lambda t: math.log(
                max(rhs_tot(t), 1e-300)))
            c_txt = ("[%.4f + %.4f] (log m)^%d%s"
                     % (CHs, CGs, a_star,
                        (" + %.4f x %.2f^2 (log m)^2 (third "
                         "arm)" % (GSQ_R321, fa_max))
                        if use3 else ""))
        else:
            a_star = None
            A_tot = None
            CHs = cover_by_r[best_j]["per_a"][best_a]["CH"]
            CGs = cover_by_r[best_j]["per_a"][best_a]["CG"]
            m0_new = None
            c_txt = ("NO certifying cover -- best cell (R = "
                     "%.4f, a = %d) leaves %d violators"
                     % (R_GRID[best_j], best_a, len(resid)))
        beats = (m0_new is not None) and (m0_new <= R324_M0_L10)
        go = cover0_rhat and canonical and beats
        partial = cover0_rhat and not go
        thresh = (not cover0_rhat) and bool(cover0_any_j)
        vkey = two_scale_tree_verdict(leak, brk_struct, go,
                                      partial, thresh)
        det_v = {
            "TARGET_LEAK": "purity/scope audit hit on a balance "
                           "builder",
            "BALANCE_STATE_NOT_EXACT":
                "an exact ward broke (tilt %.1e / part %.1e / "
                "env %.1e / hvy %.1e / Fractions %s / toys "
                "%s+%s)"
                % (tilt_w, part_w3, env_w, hvy_w, str(fr_ok),
                   str(fb_dev), str(fh_dev)),
            "TWO_SCALE_GO":
                "the min-coverage certifies at the CANONICAL "
                "R_HAT = %.4f (a = %s) AND the composition "
                "beats the r324 chain: m_0* 10^%.1f <= 10^%.1f"
                % (R_GRID[j_hat], str(a_star),
                   m0_new if m0_new is not None else -1.0,
                   R324_M0_L10),
            "TWO_SCALE_PARTIAL":
                "the min-coverage certifies at R_HAT = %.4f (a "
                "= %s) but %s"
                % (R_GRID[j_hat], str(a_star),
                   ("the stop rule is NOT canonical (interior "
                    "%s, halves-stable %s)"
                    % (str(interior), str(halves_stable)))
                   if not canonical else
                   ("the composed m_0* %s does not beat 10^%.1f"
                    % (("10^%.1f" % m0_new)
                       if m0_new is not None else "NONE",
                       R324_M0_L10))),
            "THRESHOLD_NOT_CANONICAL":
                "the coverage exists at grid point(s) %s but "
                "NOT at the sealed R_HAT = %.4f -- R* remains "
                "a tuning surface; said honestly"
                % (str([("%.4f" % R_GRID[j])
                        for j in cover0_any_j]),
                   R_GRID[j_hat]),
            "RESIDUAL_FAMILY_IRREDUCIBLE":
                "no sealed grid point reaches a 0-violation "
                "three-arm cover; the resisting family at the "
                "best cell (R = %.4f, a = %d): %s"
                % (R_GRID[best_j], best_a, str(resid_kz))}
        verdict_main = "%s(%s)" % (vkey, det_v[vkey])
        check("G80-adjudication", True,
              "exactly one sealed letter fired: %s"
              % verdict_main)
        info("COMPOSITION (typed, POLYLOG form -- NO premature "
             "powerization): E[X_inf^3] <= %s => M_3 <= C_tot "
             "(log m)^A_tot/m^2 => N_3 >= m/sqrt(C_tot (log "
             "m)^A_tot), N_2 >= N_3; the %.3f need enters ONLY "
             "as the final solve: m_0* = %s vs the r324 "
             "MEASURED route 10^%.1f (+%.3f) -- %s; vs the "
             "r306 census reading 10^%.1f; vs the r341 "
             "envelope typing 10^%.1f (no cert)."
             % (c_txt, CRIT_EXP,
                ("10^%.1f" % m0_new) if m0_new is not None
                else "NONE", R324_M0_L10, R324_ETOT,
                "BEATEN" if beats else "NOT beaten",
                m0_306, m0_env))
        check("G81-composition", True,
              "composition printed with explicit constants "
              "(%s, m_0* %s, A_tot %s); typed: the path/"
              "hand-off identities exact, every constant "
              "MEASURED on the finite ladder + EXT3, the "
              "ladder-to-m_0* step an extrapolation hypothesis "
              "-- NO cofinal claim; the disjunctive cover "
              "composes by the SUM of the arm budgets "
              "(disclosed pre-spec)"
              % (c_txt,
                 ("10^%.1f" % m0_new) if m0_new is not None
                 else "NONE", str(A_tot)))

    # ---------------- S9: Leg E -- worlds + must-fails + verdict
    section("S9  LEG E -- WORLD CENSUS + MUST-FAILS + VERDICT")
    ev9w = (recs[0] if smoke else mrecs[0])["ev"]
    ev13 = None if smoke else mrecs[1]["ev"]
    wtab = [("w9", ev9w)] + ([("w13(twin)", ev13)]
                             if ev13 is not None else [])
    for c in ("EPST", "SCR"):
        if not crecs[c]["ev"]["degenerate"]:
            wtab.append((c, crecs[c]["ev"]))
    for w, ev in wtab:
        info("world %-10s m %3d grid hsh %s  E3h share %s"
             % (w, ev["m"],
                str([round(p["hsh"], 3) for p in ev["pbg"]]),
                str([round(p["e3h"] / max(p["e3"], 1e-300), 3)
                     for p in ev["pbg"]])))
    if smoke:
        check("G90-world-census", len(wtab) >= 1,
              "SMOKE: world curves printed (w9 + live controls)")
    else:
        scr_pb = crecs["SCR"]["ev"]["pbg"]
        lad_hsh_hat = float(np.median(cols_by_r[j_hat]["hsh"]))
        check("G90-world-census", len(wtab) >= 2,
              "WORLD SEPARATION (census, NO claim): SCRAMBLE "
              "hsh(R_HAT) %.3f vs ladder med %.3f; SCR balance "
              "curve %s; EPSTEIN curve %s; twin w13 curve %s -- "
              "does the BALANCE CURVE separate where the r341 "
              "path budget did not?"
              % (scr_pb[j_hat]["hsh"], lad_hsh_hat,
                 str([round(p["hsh"], 3) for p in scr_pb]),
                 str([round(p["hsh"], 3)
                      for p in crecs["EPST"]["ev"]["pbg"]]),
                 str([round(p["hsh"], 3) for p in ev13["pbg"]])))
    check("G91-e1-rhat-posthoc",
          len(e1_hits) >= 1 and mut1 == 4 and mut1 != 0,
          "e1 protocol-CAUGHT twice: the after-sight R grid "
          "re-pick consumes the evaluated violation column -- "
          "AST-FLAGGED (%s) -- and on the toy returns index %d "
          "!= the protocol head 0 (the choice rule is sealed as "
          "a FORMULA in this spec BEFORE the freeze)"
          % (e1_hits[0] if e1_hits else "MISS", mut1))
    check("G92-e2-partition-posthoc",
          len(e2_hits) >= 1 and mut2 == ("G", "H")
          and mut2 != toy_part,
          "e2 protocol-CAUGHT twice: the per-rung arm flip "
          "consumes the evaluated violation column -- "
          "AST-FLAGGED (%s) -- and on the toy returns %s != the "
          "sealed hsh-rule output %s"
          % (e2_hits[0] if e2_hits else "MISS", str(mut2),
             str(toy_part)))
    ev9m = (recs[0] if smoke else mrecs[0])["ev"]
    g3m = mutant_cheavy_from_target(ev9m)
    check("G93-e3-cheavy-from-target",
          len(e3_hits) >= 1 and (not sc_own) and g3m >= 0.0,
          "e3 AST-CAUGHT: the 'heavy freeze constant' derived "
          "from the cubic TARGET record is FLAGGED (%s) while "
          "the module-own balance builders are clean (%d hits) "
          "-- the freezes consume the hand-off / eps-chain "
          "columns, never the target record (mutant value %.3e "
          "computed only to prove it runs)"
          % (e3_hits[0] if e3_hits else "MISS", len(sc_own),
             g3m))
    check("G94-e4-handoff-no-msh",
          fh_brk == Fr(49, 32)
          and abs(mut4 - float(Fr(49, 16))) <= TOY_BAR,
          "e4 CAUGHT exact: the hand-off WITHOUT msh claims "
          "(m q_max)^2 = 49/16 on the stop toy while the exact "
          "hand-off bound is (m q_max)^2 msh = 49/32 -- break "
          "49/32 EXACT (float builder reproduces %.4f); the "
          "true hand-off is warded live across the WHOLE grid "
          "in G42 at %.1e" % (mut4, hvy_w))
    check("G95-mincut-unchanged", True,
          "mincut base 4 / refined 5 UNCHANGED; what the round "
          "adds: the mass-balance curve over the sealed R grid, "
          "the sealed choice formula + canonicality "
          "adjudication, the two-scale freezes (hand-off / "
          "eps-chain) with three-arm min-coverage, the "
          "partition identifiability census, the kz55 / "
          "deep-anchor anatomy and the honest composition -- NO "
          "new certificate promoted, NO universal bound claimed "
          "beyond the measured rungs")
    if smoke:
        verd = "SMOKE_NO_ADJUDICATION"
    else:
        parts = ["R344_ANCHORS(identity %.1e, r306 C2 %.3f viol "
                 "%d/57, r316 n %d, r324-pre C_M2 %.4f, r339 WF "
                 "med %.2f / e(WF) %+.3f, r324 e_tot %+.3f, "
                 "r341 WB med %.3f / kz55 %.3f / CH2 %.4f v%d / "
                 "CB2 %.4f v%d / e(WB) %+.3f / e(E3h) %+.3f / "
                 "env %.4f+%.4f m0 10^%.1f, r321 FA top3 "
                 "%.2f/%.2f/%.2f)"
                 % (rec3_w, C2r, viol2, n344, C_M2, wf_med, e_wf,
                    e_tot324, float(np.median(wb_star)), wb55,
                    CH2, len(vH2), CB2, len(vB2), e_wb, e_e3h,
                    env_h3, env_g3, m0_env, fa_kz[53], fa_kz[83],
                    fa_kz[67])]
        parts.append("SEAL(grid tilt %.1e, part %.1e, env %.1e, "
                     "hvy %.1e, mart %.1e, Fractions %s at pins "
                     "3/2+12/7, grounding %.1e, purity clean, "
                     "toys exact)"
                     % (tilt_w, part_w3, env_w, hvy_w, mart_w,
                        "EXACT" if fr_ok else "BROKEN",
                        max(part_w, l1rec_w, panc_w)))
        parts.append("BALANCE(hsh med %s; share med %s; WB med "
                     "%s; e_H %s; e_G %s; R_HAT %.4f elig %s "
                     "interior %s halves %s CANONICAL %s; "
                     "crossings %s)"
                     % (str([round(float(np.median(c["hsh"])), 3)
                             for c in cols_by_r]),
                        str([round(float(np.median(c["shr"])), 3)
                             for c in cols_by_r]),
                        str([round(float(np.median(c["wb"])), 2)
                             for c in cols_by_r]),
                        str([round(c["e_h"], 3)
                             for c in cols_by_r]),
                        str([round(c["e_g"], 3)
                             for c in cols_by_r]),
                        R_GRID[j_hat], str(elig_any),
                        str(interior), str(halves_stable),
                        str(canonical), str(eq_cross)))
        parts.append("COVERAGE(R_HAT a* %s %s; cells0 %s; best "
                     "(%.4f, %d) resid %d %s; partition acc "
                     "%d/%d nonpred %d third-only %d; AND-form "
                     "%s)"
                     % (str(aa_hat),
                        "CERT" if cover0_rhat else "no",
                        str([("%.4f" % R_GRID[j])
                             for j in cover0_any_j]),
                        R_GRID[best_j], best_a, len(resid),
                        str(resid_kz), agree, ncov,
                        nonpred_only, pa["third_used"],
                        str([(t[0], t[3]) for t in and_tab])))
        parts.append("ANATOMY(kz55 eps-argmax k %d WB@hat %.3f "
                     "%s; EXT3-B third-cover %d/6; kz51 FA "
                     "%.2f %s)"
                     % (k_arg, wb55_hat,
                        "abs" if wb55_hat <= cb_hat
                        * lg_full[i55] ** a_disp else "viol",
                        n_covered_3rd, fa_full[i51],
                        "cov" if third_ok[i51] else "miss"))
        parts.append(verdict_main)
        parts.append("COMPOSITION(%s, m_0* %s vs r324 10^%.1f "
                     "%s, r306 census 10^%.1f, r341 env "
                     "10^%.1f)"
                     % (c_txt,
                        ("10^%.1f" % m0_new)
                        if m0_new is not None else "NONE",
                        R324_M0_L10,
                        "BEATEN" if beats else "NOT beaten",
                        m0_306, m0_env))
        parts.append("MUSTFAIL_LEDGER(e1-e4 + scopes)")
        verd = " + ".join(parts)
    npass = sum(1 for _n, ok, _d in CHECKS if ok)
    check("G96-verdict", npass == len(CHECKS),
          "%s%s -- SATZ (machine-gated): the path identities, "
          "the hand-off algebra, the Fractions toys and grid "
          "pins, the tree logic and the purity audits (exact / "
          "AST-decided); MEASURED: every census, constant, "
          "violation count and exponent (the finite class "
          "ladder + 12 EXT3 + 2 mains + 2 live controls); OPEN: "
          "any bound beyond the measured rungs, the cofinal "
          "law, the actual two-scale PROOF (a certifying cover "
          "fixes a theorem candidate with explicit constants, "
          "it proves nothing cofinal); NO RH claim"
          % (verd, " (SMOKE)" if smoke else ""))
    wall = time.time() - T0_WALL
    check("G99-runtime", wall <= 1800.0,
          "WALL %.1f s (bar 1800)" % wall)
    npass = sum(1 for _n, ok, _d in CHECKS if ok)
    print("\n" + "=" * 78)
    print("RESULT: %d/%d gates PASS%s   SPEC_SHA %s"
          % (npass, len(CHECKS), " (SMOKE)" if smoke else "",
             SPEC_SHA[:16]))
    print("NO RH CLAIM in either direction.")
    print("=" * 78)
    return 0 if npass == len(CHECKS) else 1


if __name__ == "__main__":
    sys.exit(main())
