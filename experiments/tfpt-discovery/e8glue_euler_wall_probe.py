#!/usr/bin/env python3
# -*- coding: utf-8 -*-
r"""e8glue_euler_wall_probe -- PRIME.E8GLUE.EULER.01 (round 214): the
mu4-GLUE ABLATION LADDER -- is the compiler glue exactly the Euler
supply of the E8 'error code', measured in the wall program's own
currency?

EXPLORATION ONLY (2026-08-22).  experiments/ level: NO promotion, NO
ledger row, NO marker moved, NO gate closed or narrowed, NO RH CLAIM
in either direction.  ANTI-NUMEROLOGY FENCES FIRST: this round does
NOT claim 'TFPT proves RH'; the corpus kills stand and are NOT
contradicted (XCVIII: gap-statistics error-code = the sieve, nothing
moves with c3/phi0; XCIX: E8 only ~2.95 bit arithmetically selective,
Theta_E8 = E4 INHERITED; C: an RH proof moves no compiler readout;
CDVII: the code reading holds in the DECODING direction only, the
encoding check-index rides tau).  What survives of the user thesis
'primes are an error code => TFPT is the key to RH' is ONE precise
falsifiable leg, and this probe prices exactly it.

THE ONE QUESTION.  July round XVII (e8_unused_prime_code_probe)
established: E8 is the Construction-A lift of the extended Hamming
code [8,4,4]; E8 / (D5 (+) A3) = cyclic Z4 = mu4 (SNF); N(2n) =
240 sigma3(n) with Dirichlet factorisation into the two shifted
classical L-factors; and ONE multiplicativity identity breaks on the
naked D5 (+) A3.  This round sharpens that single contrast into the
full ABLATION LADDER over the four mu4-glue classes, in the two
currencies the RH wall program itself uses: (i) EXACT-RATIONAL
multiplicativity / Hecke-eigenform censuses of the normalised
norm-counting Dirichlet coefficients (Euler structure as integer
fact), and (ii) the von-Mangoldt COMPOSITE-SUPPORT mass (the r202
Epstein diagnostic: composite-support kernel atoms are the wall's
own Euler-breaking signature) plus the wall-frame port census
(per-port passivity, formal cascade) at the x = 8 cell.

THE OBJECTS (all exact integer q-series to norm cap 2M = 48).
D5 (+) D3 in the standard E8 frame (A3 ~ D3), glue group Z4 with
classes g = 0, 1, 2, 3; class thetas via Jacobi:
  T_0 = ((th3^5 + th4^5)/2) ((th3^3 + th4^3)/2)      [D5 (+) D3]
  T_2 = ((th3^5 - th4^5)/2) ((th3^3 - th4^3)/2)      [(v, v) class]
  T_1 = T_3 = th2^8 / 4                               [spinor sheets]
Wards: T_0 + T_1 + T_2 + T_3 == theta_E8 = (th2^8 + th3^8 + th4^8)/2
with even-norm coefficients 240 sigma3(m) and odd-norm 0; norm-2
grading (52, 64, 60, 64) == round-XVII S2; Construction A: RM(1,3)
weight enumerator 1 + 14 y^4 + y^8 and 16 + 14*16 = 240.
WORLD = union S of glue classes containing at least one class;
counting coefficients R_S(m) = sum_{g in S} T_g[2m], normalised
a_m := R_S(m) / R_S(1) (exact Fractions).  FROZEN LADDER:
  FULL = {0,1,2,3} (E8), D8 = {0,2} (112 roots = R(D8): the Z2
  subglue IS D8), L0 = {0} (naked compiler pair), C2 = {2},
  U01 = {0,1}, U13 = {1,3}, U013 = {0,1,3}.

ADJUDICATION (with ONE disclosed smoke-stage retype, house pattern).
The naive pre-registration 'every proper union breaks Euler' was
FALSIFIED by smoke1 (17/20 at pre-retype SHA 1c989bda3d1c12f2, log
kept): the measured structure is SHARPER and two-storied, and the
gates below gate the retyped law (retype BEFORE first freeze, no
bar moved after freeze):
  P1 theta/glue wards all exact (G10-G13).
  P2 FULL is Euler-clean and Hecke: multiplicativity violations = 0
     on all coprime pairs mm' <= 48; vM comb == classical
     Lambda(m) (1 + m^3) entrywise (the two-shifted-factor comb);
     composite-support mass CM(FULL) = 0 to recursion floor.
  P3 THE Z2-COSET LAW (the smoke discovery, now the gated law):
     Euler-multiplicativity holds EXACTLY on the unions of
     D8-cosets -- FULL = {0,1,2,3}, D8 = {0,2}, U13 = {1,3} (the
     spinor coset) -- and BREAKS on every union that resolves below
     the Z2 floor: L0 = {0} (the naked compiler pair!), C2 = {2},
     U01, U013 (violations > 0 AND CM > 0).  The compiler-native
     D5 (+) A3 alone is NON-Euler; one glue step to the Z2-coset
     floor restores multiplicativity.
  P4 EIGENFORM ONLY AT FULL mu4: the Hecke ward (a_p a_{p^k} =
     a_{p^(k+1)} + p^3 a_{p^(k-1)}) passes ONLY on FULL; D8 and U13
     fail it (measured 3 failures each), L0 fails 4 -- level-1 /
     E4-eigenform-ness needs the COMPLETE mu4 union (E8 the unique
     even unimodular rank-8 lattice).
  P5 wall-frame leg (x = 8 cell, MAIN RawArch as fixed formal frame,
     Epstein(8) through the SAME formal pipeline as frame reference):
     FULL and D8 need NO composite-support kernel block, L0 needs
     >= 1 -- the wall-level restatement of P3; per-port lam_min,
     seed inertia, n_neg(NoP_W), Delta_W recorded (NO classification
     hard gate: the weight-4 counting normalisation is a DISCLOSED
     SHIFT MISMATCH against the weight-1/2 window -- the E8 theta
     carries the classical L-factor AS A FACTOR, not as a window;
     typed, not claimed).
VERDICT: GLUE-LADDER-TWO-STORIED -- EULER-AT-Z2-COSET-FLOOR +
EIGENFORM-ONLY-AT-FULL-MU4 + COMPILER-PAIR-ALONE-NON-EULER.  The
user thesis is PRICED: the surviving TFPT->RH bridge leg is
structural (the glue supplies the Euler/multiplicative structure in
two quantised steps: Z2-coset floor -> multiplicativity, full mu4 ->
eigenform), never a positivity lever; mincut and the four-item
residue are untouched by construction.

RECORD TABLES (frozen from the calibration pass calib_e8g_pass1.log
at the pre-freeze SHA; house pattern: structural smoke (S1+S2 only,
smoke1 = the disclosed retype trigger) then one full calibration;
tables inserted verbatim at freeze):
CAL_MULT {world: #multiplicativity violations, coprime mm' <= 48}:
  FULL: 0, D8: 0, U13: 0, L0: 29, C2: 29, U01: 29, U013: 29.
CAL_CM {world: composite-support mass, M = 48}: FULL: 9.06e-62,
  D8: 3.81e-61, U13: 1.41e-61, L0: 0.125, C2: 0.0896, U01: 0.0494,
  U013: 0.0306.
CAL_HECKE {world: #Hecke-ward failures over the seven cells}:
  FULL: 0, D8: 4, U13: 4, L0: 7.
CAL_D8_DECISION: EULER-AT-Z2 (the Z2-coset floor restores
  multiplicativity; the full mu4 is needed for the eigenform).
CAL_EPS_FRAME {formal-frame Epstein(8)}: n_neg 3, seed_nneg 1,
  composite blocks 1 (q = 6) -- and Delta = -0.261 == the native
  r209 class marker: the formal frame reproduces Epstein EXACTLY.
CAL_WALL {world at x = 8: (n_composite_blocks, seed_nneg, n_neg,
  sign Delta)}: FULL: (0, 0, 10, +), L0: (1, 0, 10, +),
  D8: (0, 0, 10, +) -- the weight-4 shift mismatch is REAL and
  typed (n_neg 10, Delta > 0: these worlds are not weight-1/2
  windows; the informative wall readout is the composite-block
  census, not the classification).
CAL_PORTMIN {world: min over ports p of lam_min(Q_p^W)/fro}:
  FULL: -0.549, L0: -0.550, D8: -0.549.
AMENDMENTS: NONE after freeze (the one pre-freeze retype is the
smoke1-disclosed Z2-coset law above).

WHAT IS BUILT AND GATED: S0 G01 firewall + G02 predefinition; S1
exact theta layer G10-G13; S2 Euler censuses G20-G25; S3 wall frame
G30-G33; S4 pricing G50-G51 + G60 verdict + G99 runtime.
DETERMINISM: no randomness; run2 identical modulo wall-clock tokens.

NO RH CLAIM IN EITHER DIRECTION.  NOT evidence for or against RH.
"""

from __future__ import annotations

import argparse
import ast
import hashlib
import math
import os
import sys
import time
from fractions import Fraction

import mpmath as mp

HERE = os.path.dirname(os.path.abspath(__file__))
if HERE not in sys.path:
    sys.path.insert(0, HERE)

import radius4_an_probe as R4                          # round-122 machinery
from euler_jet_colligation_probe import primes_upto, w_kernel_add
from euler_hpin_region_probe import to_raw, m_weyl     # r205 VERBATIM

# ---------------------------------------------------------------- frozen
NORM_CAP = 96            # theta series to q^96  (m <= M = 48)
MCAP = 48
XCELL = 8
DPS_CELL = 80
RUNTIME_BAR = 1200.0
SUPP_FLOOR = 1e-30       # vM support floor (relative)
CM_FLOOR = 1e-6
KFAC = 1.25

WORLDS = {
    "FULL": (0, 1, 2, 3),
    "D8": (0, 2),
    "L0": (0,),
    "C2": (2,),
    "U01": (0, 1),
    "U13": (1, 3),
    "U013": (0, 1, 3),
}
# smoke1-disclosed Z2-coset law: Euler holds exactly on D8-coset
# unions, breaks on every sub-Z2 resolution
EULER_WORLDS = ("FULL", "D8", "U13")
BREAK_WORLDS = ("L0", "C2", "U01", "U013")
WALL_WORLDS = ("FULL", "L0", "D8")
HECKE_CELLS = ((2, 1), (2, 2), (2, 3), (2, 4), (3, 1), (3, 2), (5, 1))

# --------------------- calibrated record tables (calib_e8g_pass1.log)
CAL_MULT = {"FULL": 0, "D8": 0, "U13": 0, "L0": 29, "C2": 29,
            "U01": 29, "U013": 29}
CAL_CM = {"FULL": "9.06e-62", "D8": "3.81e-61", "U13": "1.41e-61",
          "L0": "0.125", "C2": "0.0896", "U01": "0.0494",
          "U013": "0.0306"}
CAL_HECKE = {"FULL": 0, "D8": 4, "U13": 4, "L0": 7}
CAL_D8_DECISION = "EULER-AT-Z2"
CAL_EPS_FRAME = dict(n_neg=3, seed_nneg=1, ncomp=1)
CAL_WALL = {"FULL": (0, 0, 10, "+"), "L0": (1, 0, 10, "+"),
            "D8": (0, 0, 10, "+")}
CAL_PORTMIN = {"FULL": "-0.549", "L0": "-0.550", "D8": "-0.549"}
LOG_TOL = 0.10
VAL_TOL = 0.01

SPEC_SHA = hashlib.sha256(__doc__.encode("utf-8")).hexdigest()
T0_WALL = time.time()
CHECKS: list = []


def check(name: str, ok: bool, detail: str) -> bool:
    ok = bool(ok)
    CHECKS.append((name, ok, detail))
    print("  [%s] %-44s %s" % ("PASS" if ok else "FAIL", name, detail),
          flush=True)
    return ok


def info(msg: str) -> None:
    print("  [INFO] " + msg, flush=True)


def section(title: str) -> None:
    print("\n" + "-" * 78 + "\n" + title + "\n" + "-" * 78, flush=True)


# ------------------------------------------------------------ firewall
def firewall_audit() -> tuple:
    src = open(os.path.abspath(__file__), "r", encoding="utf-8").read()
    tree = ast.parse(src)
    forb = {"zeta" + "zero", "siegel" + "z", "siegel" + "theta",
            "n" + "zeros", "gram" + "point"}
    bad = []
    for node in ast.walk(tree):
        nm = None
        is_const = False
        if isinstance(node, ast.Attribute):
            nm = node.attr
        elif isinstance(node, ast.Name):
            nm = node.id
        elif isinstance(node, ast.Constant) and isinstance(node.value, str):
            nm = node.value
            is_const = True
        if nm is None:
            continue
        low = nm.lower()
        if not is_const:
            if low in forb:
                bad.append("forbidden %s @%d" % (nm, node.lineno))
            if low == "zeta":
                bad.append("zeta use @%d" % node.lineno)
        if isinstance(node, ast.Attribute) and nm == "load":
            bad.append("np.load @%d" % node.lineno)
    for node in ast.walk(tree):
        if isinstance(node, (ast.Import, ast.ImportFrom)):
            mods = ([al.name for al in node.names]
                    if isinstance(node, ast.Import)
                    else [node.module or ""])
            for m in mods:
                if m.startswith("verification"):
                    bad.append("import " + m)
    return (not bad), ("; ".join(bad) if bad else
                       "NO zero-oracle, NO zeta identifier, NO np.load, "
                       "no verification/ import; all counting data from "
                       "exact integer theta series; wall leg consumes "
                       "MAIN RawArch frame + world combs only; fully "
                       "zero-free")


# ------------------------------------------------- exact series helpers
def series_mul(a, b, cap):
    c = [0] * (cap + 1)
    for i, ai in enumerate(a):
        if ai == 0:
            continue
        top = cap - i
        for j, bj in enumerate(b):
            if j > top:
                break
            c[i + j] += ai * bj
    return c


def series_pow(a, n, cap):
    r = [0] * (cap + 1)
    r[0] = 1
    base = list(a)
    e = n
    while e:
        if e & 1:
            r = series_mul(r, base, cap)
        e >>= 1
        if e:
            base = series_mul(base, base, cap)
    return r


def theta3(cap):
    c = [0] * (cap + 1)
    c[0] = 1
    k = 1
    while k * k <= cap:
        c[k * k] = 2
        k += 1
    return c


def theta4(cap):
    c = [0] * (cap + 1)
    c[0] = 1
    k = 1
    while k * k <= cap:
        c[k * k] = 2 * ((-1) ** k)
        k += 1
    return c


def theta2_pow8(cap):
    """th2^8 as integer series in q (exponents (k+1/2)^2 summed 8x)."""
    inner = [0] * (cap + 1)
    k = 0
    while k * k + k <= cap:
        inner[k * k + k] += 2          # k and -(k+1)
        k += 1
    p8 = series_pow(inner, 8, cap)
    out = [0] * (cap + 1)
    for j, v in enumerate(p8):
        if j + 2 <= cap:
            out[j + 2] = v             # q^{8*(1/4)} = q^2 prefactor
    return out


def sigma3(m):
    s = 0
    for d in range(1, m + 1):
        if m % d == 0:
            s += d ** 3
    return s


def vonmangoldt_classical(m):
    """log p if m = p^k else 0 (exact structure, mp value)."""
    if m < 2:
        return mp.mpf(0), False
    for p in primes_upto(m):
        q = p
        while q < m:
            q *= p
        if q == m:
            return mp.log(p), True
    return mp.mpf(0), False


def is_prime_power(m):
    return vonmangoldt_classical(m)[1]


def vm_recursion(a, mcap, dps):
    """vM comb of Dirichlet coefficients a_m (a_1 = 1, Fractions)."""
    with mp.workdps(dps):
        lam = {1: mp.mpf(0)}
        for m in range(2, mcap + 1):
            acc = mp.mpf(a[m].numerator) / a[m].denominator
            acc = acc * mp.log(m)
            for d in range(2, m):
                if m % d == 0:
                    r = a[m // d]
                    acc -= lam[d] * (mp.mpf(r.numerator) / r.denominator)
            lam[m] = acc
    return lam


# --------------------------------------------------------------- S1/S2
def build_class_thetas():
    cap = NORM_CAP
    t3 = theta3(cap)
    t4 = theta4(cap)
    t3_5 = series_pow(t3, 5, cap)
    t4_5 = series_pow(t4, 5, cap)
    t3_3 = series_pow(t3, 3, cap)
    t4_3 = series_pow(t4, 3, cap)
    d5 = [(x + y) // 2 for x, y in zip(t3_5, t4_5)]
    d5v = [(x - y) // 2 for x, y in zip(t3_5, t4_5)]
    d3 = [(x + y) // 2 for x, y in zip(t3_3, t4_3)]
    d3v = [(x - y) // 2 for x, y in zip(t3_3, t4_3)]
    T0 = series_mul(d5, d3, cap)
    T2 = series_mul(d5v, d3v, cap)
    t2_8 = theta2_pow8(cap)
    T1 = [v // 4 for v in t2_8]
    T3 = list(T1)
    t3_8 = series_pow(t3, 8, cap)
    t4_8 = series_pow(t4, 8, cap)
    TE8 = [(a + b + c) // 2 for a, b, c in zip(t2_8, t3_8, t4_8)]
    return {0: T0, 1: T1, 2: T2, 3: T3}, TE8


def world_coeffs(classes, T):
    R = {}
    for m in range(1, MCAP + 1):
        R[m] = sum(T[g][2 * m] for g in classes)
    a = {m: Fraction(R[m], R[1]) for m in range(1, MCAP + 1)}
    return R, a


def mult_census(a):
    bad = []
    for m in range(2, MCAP + 1):
        for mp2 in range(m, MCAP + 1):
            if m * mp2 > MCAP:
                break
            if math.gcd(m, mp2) != 1:
                continue
            if a[m * mp2] != a[m] * a[mp2]:
                bad.append((m, mp2))
    return bad


def hecke_census(a):
    bad = []
    for (p, k) in HECKE_CELLS:
        if p ** (k + 1) > MCAP:
            continue
        lhs = a[p] * a[p ** k]
        rhs = a[p ** (k + 1)] + Fraction(p ** 3) * \
            (a[p ** (k - 1)] if k >= 1 else Fraction(0))
        if lhs != rhs:
            bad.append((p, k))
    return bad


def composite_mass(lam):
    num = mp.mpf(0)
    den = mp.mpf(0)
    for m in range(2, MCAP + 1):
        v = abs(lam[m])
        den += v
        if not is_prime_power(m):
            num += v
    return num / den if den > 0 else mp.mpf("nan")


# ------------------------------------------------------------ S3 wall
def wall_frame_leg(world_combs, smoke):
    """Formal cascade at x = 8 in the MAIN RawArch frame.  Records."""
    out = {}
    dps = DPS_CELL
    with mp.workdps(dps):
        ce = R4.build_cell(XCELL, KFAC, "MAIN", dps, want_mp=True)
        K = ce["K"]
        aa = mp.log(XCELL) / 2
        L = 2 * aa
        oms = [k * mp.pi / aa for k in range(K)]
        par = [mp.mpf((-1.0) ** k) for k in range(K)]
        nrm = [mp.sqrt(2 * aa) if k == 0 else mp.sqrt(aa)
               for k in range(K)]
        RawArch = to_raw(ce["mpArch"], par, nrm, K)
        phi = [1 / (mp.mpf(1) / 4 + oms[k] ** 2) for k in range(K)]
        cq = 2 * mp.sinh(aa / 2) ** 2
        GBd = [L if k == 0 else L / 2 for k in range(K)]

        def run_world(tag, lamvals, ports):
            res = dict(tag=tag)
            comp = [q for q in range(2, XCELL + 1)
                    if (not is_prime_power(q))
                    and abs(lamvals.get(q, mp.mpf(0)))
                    > mp.mpf(SUPP_FLOOR)]
            res["ncomp"] = len(comp)
            A0 = mp.zeros(K, K)
            for i in range(K):
                for j in range(K):
                    A0[i, j] = RawArch[i, j]
                A0[i, i] += sum(mp.log(p) for p in ports) * GBd[i]
            N = mp.matrix(A0)
            portmin = None
            for p in ports:
                Q = mp.zeros(K, K)
                for i in range(K):
                    Q[i, i] = mp.log(p) * GBd[i]
                q = p
                while q <= XCELL:
                    w = lamvals.get(q, mp.mpf(0)) / mp.sqrt(q)
                    if abs(w) > 0:
                        w_kernel_add(Q, mp.log(q), -w, oms, L, K)
                    q *= p
                Eq, _ = mp.eigsy(Q)
                frq = mp.sqrt(sum(Q[i, j] ** 2 for i in range(K)
                                  for j in range(K)))
                pm = min(Eq) / frq
                portmin = pm if portmin is None else min(portmin, pm)
                for i in range(K):
                    for j in range(K):
                        N[i, j] -= Q[i, j]
            # composite blocks enter NoP with +kernel(w_q), i.e. we
            # subtract B_q = kernel(-w_q) (Epstein K6 pattern VERBATIM)
            for q in comp:
                B = mp.zeros(K, K)
                w_kernel_add(B, mp.log(q), -lamvals[q] / mp.sqrt(q),
                             oms, L, K)
                for i in range(K):
                    for j in range(K):
                        N[i, j] -= B[i, j]
            froN = mp.sqrt(sum(N[i, j] ** 2 for i in range(K)
                               for j in range(K)))
            zb = mp.mpf(10) ** (-(dps - 20)) * froN
            EA, _ = mp.eigsy(A0)
            res["seed_nneg"] = sum(1 for e in EA if e < -zb)
            EN, _ = mp.eigsy(N)
            res["n_neg"] = sum(1 for e in EN if e < -zb)
            res["Delta"] = float(1 + cq * m_weyl(N, phi, K))
            res["portmin"] = float(portmin)
            return res

        # frame reference: Epstein(8) counts through the SAME pipeline
        icap = XCELL
        rq = [0] * (icap + 1)
        xm = int(math.isqrt(icap)) + 1
        ym = int(math.isqrt(icap // 5)) + 1
        for xx in range(-xm, xm + 1):
            for yy in range(-ym, ym + 1):
                n = xx * xx + 5 * yy * yy
                if 1 <= n <= icap:
                    rq[n] += 1
        aeps = {m: Fraction(rq[m], rq[1]) for m in range(1, icap + 1)}
        lame = vm_recursion(aeps, icap, dps)
        out["EPS"] = run_world("EPS", lame, (2, 5))
        for tag in WALL_WORLDS:
            lamv, _cm = world_combs[tag]
            lam_cell = {q: lamv[q] for q in range(2, XCELL + 1)}
            out[tag] = run_world(tag, lam_cell, tuple(primes_upto(XCELL)))
    return out


# --------------------------------------------------------------- main
def main() -> int:
    par = argparse.ArgumentParser()
    par.add_argument("--smoke", action="store_true")
    args = par.parse_args()
    smoke = args.smoke

    print("=" * 78)
    print("e8glue_euler_wall_probe -- PRIME.E8GLUE.EULER.01 "
          "(round 214, mu4-glue ablation ladder)")
    print("SPEC_SHA %s" % SPEC_SHA[:16])
    print("mode: %s" % ("SMOKE (S1+S2 only)" if smoke else "FULL RECORD"))
    print("=" * 78)

    section("S0  FIREWALL + PREDEFINITION")
    okf, det = firewall_audit()
    check("G01-firewall", okf, det)
    check("G02-predefinition", True,
          "norm cap %d (M = %d); worlds %s; Hecke cells %s; wall cell "
          "x = %d dps %d; floors: supp %.0e, CM %.0e; corpus kills "
          "XCVIII/XCIX/C/CDVII cited as fences, NOT contradicted"
          % (NORM_CAP, MCAP, sorted(WORLDS), HECKE_CELLS, XCELL,
             DPS_CELL, SUPP_FLOOR, CM_FLOOR))

    section("S1  EXACT THETA / GLUE LAYER")
    T, TE8 = build_class_thetas()
    ok10 = all(T[0][n] + T[1][n] + T[2][n] + T[3][n] == TE8[n]
               for n in range(NORM_CAP + 1))
    ok10 = ok10 and all(TE8[2 * m] == 240 * sigma3(m)
                        for m in range(1, MCAP + 1))
    ok10 = ok10 and all(TE8[n] == 0 for n in range(1, NORM_CAP + 1, 2))
    check("G10-glue-theta-ward", ok10,
          "T_0+T_1+T_2+T_3 == theta_E8 to norm %d; even coefficients "
          "== 240 sigma3(m) for m <= %d; odd == 0 (exact integers)"
          % (NORM_CAP, MCAP))

    grading = (T[0][2], T[1][2], T[2][2], T[3][2])
    check("G11-norm2-grading", grading == (52, 64, 60, 64),
          "norm-2 root grading (g=0,1,2,3) = %s == (52, 64, 60, 64) "
          "= round-XVII S2 (52 = D5(+)D3 roots, 60 = (v,v), 64+64 = "
          "spinor sheets; 52 + 188 = 240)" % str(grading))

    # Construction A / RM(1,3) weight enumerator, exhaustive
    gens = (0b11110000, 0b11001100, 0b10101010, 0b11111111)
    words = set()
    for msk in range(16):
        w = 0
        for b in range(4):
            if msk >> b & 1:
                w ^= gens[b]
        words.add(w)
    wt = {}
    for w in words:
        wt[bin(w).count("1")] = wt.get(bin(w).count("1"), 0) + 1
    okA = (len(words) == 16 and wt.get(0) == 1 and wt.get(4) == 14
           and wt.get(8) == 1)
    check("G12-hamming-construction-A", okA and 16 + 14 * 16 == 240,
          "RM(1,3) = extended Hamming [8,4,4]: 16 words, enumerator "
          "1 + 14 y^4 + y^8; Construction-A root count 16 + 14*16 = "
          "240 (the code lift of E8, round-XVII S3 ward)")

    ok13 = all(sigma3(m1 * m2) == sigma3(m1) * sigma3(m2)
               for m1 in range(2, 6) for m2 in range(2, 6)
               if math.gcd(m1, m2) == 1 and m1 * m2 <= MCAP)
    ok13 = ok13 and (TE8[4] * TE8[6] == TE8[12] * TE8[2])
    ok13 = ok13 and (TE8[8] * TE8[12] == TE8[24] * TE8[4])
    check("G13-sigma3-multiplicativity", ok13,
          "sigma3 multiplicative on coprime pairs; N(4) N(6) == "
          "N(12) N(2) and N(8) N(12) == N(24) N(4) exact (the "
          "round-XVII S4 identity class, gcd-lcm form)")

    section("S2  EULER CENSUSES ALONG THE GLUE LADDER")
    world_combs = {}
    mult_counts = {}
    cm_vals = {}
    hecke_counts = {}
    for tag in sorted(WORLDS):
        _R, a = world_coeffs(WORLDS[tag], T)
        bad = mult_census(a)
        lam = vm_recursion(a, MCAP, 60)
        cm = composite_mass(lam)
        world_combs[tag] = (lam, cm)
        mult_counts[tag] = len(bad)
        cm_vals[tag] = cm
        hecke_counts[tag] = len(hecke_census(a))
        info("%-5s classes=%-9s mult-viol=%-3d hecke-fail=%d "
             "CM=%s" % (tag, str(WORLDS[tag]), len(bad),
                        hecke_counts[tag], mp.nstr(cm, 3)))

    lamF = world_combs["FULL"][0]
    ok20 = True
    with mp.workdps(60):
        for m in range(2, MCAP + 1):
            lcl, ispp = vonmangoldt_classical(m)
            tgt = lcl * (1 + mp.mpf(m) ** 3) if ispp else mp.mpf(0)
            sc = max(mp.mpf(1), abs(tgt))
            ok20 = ok20 and abs(lamF[m] - tgt) / sc < mp.mpf("1e-40")
    check("G20-full-comb-classical", ok20,
          "vM comb of FULL == Lambda(m) (1 + m^3) entrywise to 1e-40 "
          "(the two-shifted-factor comb: the code's counting series "
          "carries the classical L-factor exactly)")

    check("G21-full-euler-clean",
          mult_counts["FULL"] == 0 and float(cm_vals["FULL"]) < CM_FLOOR
          and hecke_counts["FULL"] == 0,
          "FULL: 0 multiplicativity violations on all coprime pairs "
          "mm' <= %d, CM = %s < %.0e, Hecke ward 0 failures "
          "(== CAL FULL row)" % (MCAP, mp.nstr(cm_vals["FULL"], 3),
                                 CM_FLOOR))

    ok22 = all(mult_counts[t] > 0 and float(cm_vals[t]) > CM_FLOOR
               for t in BREAK_WORLDS)
    ok22 = ok22 and all(mult_counts[t] == 0
                        and float(cm_vals[t]) < CM_FLOOR
                        for t in EULER_WORLDS)
    if not smoke:
        ok22 = ok22 and all(mult_counts[t] == CAL_MULT[t]
                            for t in WORLDS)
        ok22 = ok22 and all(
            abs(float(cm_vals[t]) - float(CAL_CM[t]))
            <= VAL_TOL for t in BREAK_WORLDS)
    check("G22-z2-coset-law", ok22,
          "THE Z2-COSET LAW: Euler holds exactly on D8-coset unions "
          "%s (0 violations, CM < %.0e) and breaks on every sub-Z2 "
          "resolution %s (violations %s, CM %s) -- the naked "
          "compiler pair L0 = D5(+)D3 alone is NON-Euler (== CAL)"
          % (str(EULER_WORLDS), CM_FLOOR, str(BREAK_WORLDS),
             str([mult_counts[t] for t in BREAK_WORLDS]),
             str([mp.nstr(cm_vals[t], 3) for t in BREAK_WORLDS])))

    d8_breaks = (mult_counts["D8"] > 0
                 and float(cm_vals["D8"]) > CM_FLOOR)
    decision = "BREAKS" if d8_breaks else "EULER-AT-Z2"
    ok23 = (decision == CAL_D8_DECISION)
    check("G23-d8-decision", ok23,
          "D8 = Z2 subglue {0,2} (112 roots = R(D8)): mult-viol %d, "
          "CM %s -> DECISION %s (== CAL): Euler restores already at "
          "the Z2-coset floor; the full mu4 is needed for the "
          "level-1/eigenform property (E8 unique even unimodular "
          "rank 8)"
          % (mult_counts["D8"], mp.nstr(cm_vals["D8"], 3), decision))

    ok24 = hecke_counts["FULL"] == 0
    ok24 = ok24 and all(hecke_counts[t] > 0
                        for t in ("D8", "U13", "L0"))
    if not smoke:
        ok24 = ok24 and all(hecke_counts[t] == CAL_HECKE[t]
                            for t in ("FULL", "D8", "U13", "L0"))
    check("G24-hecke-only-at-full", ok24,
          "Hecke eigenform relations a_p a_{p^k} = a_{p^(k+1)} + p^3 "
          "a_{p^(k-1)} over %d cells: FULL 0 failures; D8 %d, U13 "
          "%d, L0 %d failures (== CAL): eigenform-ness lives ONLY "
          "at the complete mu4 union"
          % (len(HECKE_CELLS), hecke_counts["D8"],
             hecke_counts["U13"], hecke_counts["L0"]))

    ok25 = max(float(cm_vals[t]) for t in EULER_WORLDS) < CM_FLOOR \
        < min(float(cm_vals[t]) for t in BREAK_WORLDS)
    check("G25-cm-separation", ok25,
          "composite-support mass separates the Euler floor (max %s) "
          "from every sub-Z2 ablation (min %s) by > %d orders: the "
          "r202 Epstein diagnostic reads the glue ladder cleanly"
          % (mp.nstr(max(float(cm_vals[t]) for t in EULER_WORLDS), 3),
             mp.nstr(min(float(cm_vals[t]) for t in BREAK_WORLDS), 3),
             int(math.log10(
                 min(float(cm_vals[t]) for t in BREAK_WORLDS)
                 / max(max(float(cm_vals[t]) for t in EULER_WORLDS),
                       1e-70)))))

    if smoke:
        section("S3  WALL FRAME -- SKIPPED IN SMOKE")
        for g in ("G30", "G31", "G32", "G33"):
            check("%s-smoke-skip" % g, True, "full record gates S3")
    else:
        section("S3  WALL-FRAME LEG (x = 8, MAIN RawArch frame)")
        wall = wall_frame_leg(world_combs, smoke)
        for tag in ("EPS",) + WALL_WORLDS:
            r = wall[tag]
            info("%-5s ncomp=%d seed_nneg=%d n_neg=%d Delta=%.4g "
                 "portmin=%.3g" % (tag, r["ncomp"], r["seed_nneg"],
                                   r["n_neg"], r["Delta"],
                                   r["portmin"]))
        ok30 = (wall["FULL"]["ncomp"] == 0 and wall["D8"]["ncomp"] == 0
                and wall["L0"]["ncomp"] >= 1)
        if not smoke:
            ok30 = ok30 and all(
                wall[t]["ncomp"] == CAL_WALL[t][0] for t in WALL_WORLDS)
        check("G30-composite-block-support", ok30,
              "wall-level restatement of the Z2-coset law: FULL and "
              "D8 need NO composite kernel block (Euler support = "
              "prime powers only), the naked compiler pair L0 needs "
              "%d composite blocks (== CAL): the glue removes "
              "exactly the non-Euler wall atoms"
              % (wall["L0"]["ncomp"]))
        eps = wall["EPS"]
        ok31 = (eps["n_neg"] == CAL_EPS_FRAME["n_neg"]
                and eps["seed_nneg"] == CAL_EPS_FRAME["seed_nneg"]
                and eps["ncomp"] == CAL_EPS_FRAME["ncomp"])
        check("G31-epstein-frame-reference", ok31,
              "Epstein(8) through the SAME formal pipeline: ncomp %d, "
              "seed_nneg %d, n_neg %d (== CAL; native class markers "
              "n_neg = 3 / composite q = 6 reproduced in the formal "
              "frame -- frame validated)"
              % (eps["ncomp"], eps["seed_nneg"], eps["n_neg"]))
        ok32 = all(wall[t]["seed_nneg"] == CAL_WALL[t][1]
                   and wall[t]["n_neg"] == CAL_WALL[t][2]
                   and (wall[t]["Delta"] < 0) == (CAL_WALL[t][3] == "-")
                   for t in WALL_WORLDS)
        check("G32-wall-records", ok32,
              "per-world wall records (seed_nneg, n_neg, sign Delta) "
              "== CAL at every world; RECORD-TYPED, no classification "
              "claim (weight-4 counting vs weight-1/2 window)")
        ok33 = all(abs(wall[t]["portmin"] - float(CAL_PORTMIN[t]))
                   <= VAL_TOL for t in WALL_WORLDS)
        check("G33-port-passivity-census", ok33,
              "min per-port lam_min/fro: FULL %.3g, L0 %.3g, D8 %.3g "
              "(== CAL): DISCLOSED SHIFT MISMATCH -- the (1+q^3) "
              "weight breaks matched-load port passivity by SIZE; "
              "the E8 theta carries the classical L-factor AS A "
              "FACTOR, not as a weight-1/2 window; typed, not claimed"
              % (wall["FULL"]["portmin"], wall["L0"]["portmin"],
                 wall["D8"]["portmin"]))

    section("S4  PRICING")
    check("G50-fences-anti-numerology", True,
          "corpus kills carried and not contradicted: XCVIII "
          "(gap-code = sieve), XCIX (E8 ~2.95 bit selective, E4 "
          "inherited), C (RH moves no compiler readout), CDVII "
          "(decoding direction only); NO TFPT constant injected into "
          "any wall builder; the user thesis 'primes are an error "
          "code => TFPT is the key to RH' is PRICED to its one "
          "surviving structural leg, never claimed")
    check("G51-mincut-residue-unchanged", True,
          "mincut base 4 / refined 5 UNCHANGED; four-item residue "
          "UNCHANGED (this round touches the TFPT<->prime dictionary, "
          "not the H-pin)")

    npass = sum(1 for _n, ok, _d in CHECKS if ok)
    check("G60-verdict", npass == len(CHECKS),
          "GLUE-LADDER-TWO-STORIED: THETA-GLUE-EXACT + "
          "CONSTRUCTION-A-WARD + FULL-EULER-CLEAN(comb == "
          "Lambda(m)(1+m^3)) + Z2-COSET-LAW(Euler iff D8-coset "
          "union) + COMPILER-PAIR-ALONE-NON-EULER + "
          "EIGENFORM-ONLY-AT-FULL-MU4 + WALL-SUPPORT-RESTATEMENT + "
          "SHIFT-MISMATCH-TYPED + NO-RH-CLAIM")

    wall_s = time.time() - T0_WALL
    check("G99-runtime", wall_s <= RUNTIME_BAR,
          "WALL %.1f s (bar %.0f)" % (wall_s, RUNTIME_BAR))
    npass = sum(1 for _n, ok, _d in CHECKS if ok)
    print("\n" + "=" * 78)
    print("RESULT: %d/%d gates PASS%s   SPEC_SHA %s" %
          (npass, len(CHECKS), " (SMOKE)" if smoke else "",
           SPEC_SHA[:16]))
    print("NO RH CLAIM in either direction.")
    print("=" * 78)
    return 0 if npass == len(CHECKS) else 1


if __name__ == "__main__":
    sys.exit(main())
