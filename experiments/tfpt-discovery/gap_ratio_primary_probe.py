#!/usr/bin/env python3
# -*- coding: utf-8 -*-
r"""gap_ratio_primary_probe -- PRIME.LSTAR.GAP_RATIO.01 (round 345):
the GAP RATIO g21 = (1 - lambda_2)/(1 - lambda_1) and the top-2
eigenvector geometry at the pair promoted to PRIMARY COORDINATES of
the L* lane -- the named successor round of r343: the
curvature-honest flatness protocol (sealed a priori, the explicit
r343 mandate), the exact two-level formula r'_2 = f(g21, angles),
the honest kill on the concentration-weak rungs (kz44/kz59 type),
and the SIGNED CANCELLATION Delta c ~ -c as an object (the mirror
law c'/c with a sealed delta fit -- the concretized specialist
question of the r343 bound autopsy).

R343 (pair_coupling_probe, SPEC_SHA 9ffc2705, DRESSED_RESERVE_
DECAYS(soft) read precisely: NO hard kill fired, EXT3 blind 12/12,
EXT4 6/6, slope +0.018 dead flat -- but the sealed FLAT clause
failed on a halves-curvature bar inherited from the r342 decay-law
convention, unfit for a flat O(1) column with ~x2 multiplicative
scatter) established: r'_det is numerically a TOP-2 spectral object
(median |r'_2/r'_det - 1| = 0.0634, K_res <= 3 on 75/75); its flat
O(1) content IS the flat gap ratio g21 (median 8.2, slope +0.049,
kz56 at 16.0 with the reserve unmoved); the dressing works by the
signed cancellation Delta c ~ -c (triangle/CS/operator chains
certify 0/75, overshoot 73x..2.4e11); the rest block peels as a
second two-atom extremal (mass median 0.9902, min 0.6917 at kz44).
THE ROUND'S CONTRACT: is g21 (with the top-2 geometry) the right
flat primary coordinate -- under a curvature-honest flatness
certificate, with a source-side account, and an honest kill at the
concentration-weak rungs?  And: the signed cancellation as a
structure statement (the mirror ratio -Delta c/c -> 1 with a
sealed decay law for the residual c'/c).

EXPLORATION ONLY (2026-08-27).  experiments/ level: NO promotion,
NO ledger row, NO marker moved, NO RH CLAIM in either direction.
Coexistence: R344 (fold two-scale balance, terminal lane) may run
in parallel -- this probe touches NOTHING outside its own file and
the strictly additive rh-sync.  Two-commit freeze protocol (r329
convention): spec + machinery committed BEFORE the record run,
record tables inserted after.

THE EXACT OBJECTS (all gated): per rung the r342 bundle (PX.build_
rung verbatim, SPEC_SHA prefix gated b09f8ccd) and the r343
extension (PC3.extend_rung verbatim, SPEC_SHA prefix gated
9ffc2705: resolvent-correlation identity, spectral expansion, top-K
shadows, K_res, peeling coordinates); THE TWO-LEVEL FORMULA (this
round's exact spine): with (lambda_1, w_1), (lambda_2, w_2) the top
two eigenpairs of E and the pair angles t_k = w_2(i_k)/w_1(i_k)
(k = 1, 2; canonical joint sign t_1 + t_2 >= 0),
    r'_2 = g21 (t_2 - t_1)^2 / ((g21 + t_1^2)(g21 + t_2^2))
-- an EXACT algebraic identity for the correlation reserve of the
top-2-truncated resolvent at the pair (proved on toys at 1e-12,
gated per rung against the independent PC3.topk_shadow(K=2) route
at 1e-9 rel): under top-2 dominance (K_res <= 3, r343) the dressed
reserve IS a function of the gap ratio and two angles, and the
measured top-2 deviation |r'_2/r'_det - 1| quantifies the
truncation; THE INDEPENDENT g21 ROUTE (anti-circularity ward): g21
== mu_1/mu_2 with mu the top eigenvalues of (I-E)^{-1} (sample
rungs, 1e-6); THE INVERSION (restatement instrument): the larger
root of r g^2 + (r (t_1^2 + t_2^2) - (t_2 - t_1)^2) g +
r t_1^2 t_2^2 = 0 recovers g21 from r'_2 and the angles exactly
(toy-gated 1e-9) -- evaluated with the LADDER-MEDIAN angles on the
measured r'_det column it is the sealed RESTATEMENT test (does g21
carry anything beyond r'_det + a fixed angle?); THE MIRROR
DECOMPOSITION (Leg D): Delta c = u_1^T (I-D)^{-1} u_2 =
sum_{k>=0} u_1^T D^k u_2 (the c-paths through the rest space,
order k) = sum_m alpha_m beta_m/(1 - delta_m) (rest eigenmodes);
the finite-order mirror rho_K = -(sum_{k<=K} u_1^T D^k u_2)/c at
sealed K, the mode profile (top-1/top-3/top-8 L1 shares, M90 =
#modes to 90 percent L1 mass), and THE MIRROR RESIDUAL c'/c =
1 + Delta c/c with its sealed decay fit (delta = -slope) -- the
cancellation as a structure statement: the rest space mirrors the
pair cross-term up to O(N_w^-delta).

INDEX FIREWALL (binding, r238-r343 discipline): w = window (kz
into the prime-power list), S = #union atoms, S_- = #nu atoms, N_w
= (S+1)//2; ground truth (r283/r284/r286/r329/r334/r342/r343
records, control flips, kappa_int records) enters GATES and record
tables only; the module-own constructors consume kernel Gram /
spectrum / weight / position arrays ONLY (AST scope audit; the
spectrum of the INSTRUMENTED matrix is the round's object of
study; withheld identifiers are the RECORD values and verdict-side
columns); no zero/prime oracles anywhere (AST firewall; the
prime-power grid is the sealed source comb, r238 convention); no
fit primitives (fragment audit; fits are the imported r286
Theil-Sen; the protocol's slope CI is the module-own
separation-restricted pairwise-slope quantile interval, disclosed
below).  MACHINERY IMPORTED VERBATIM: r343 PC3.{extend_rung,
resolvent_pair_corr, rest_expand, dressing_from_expand,
topk_shadow, shadow_bare, rest_pair_stats} (SPEC_SHA gated
9ffc2705), r342 PX.{build_rung, pair_select, pair_eigs,
det_reserve, schur_dress, arch_dict_density, v_predict,
layer_split, eig_pr} (b09f8ccd), r329 E3 (bbfaf199, read-only
lineage), document pipeline V.{build_measures, mu_chain, b_matrix,
admissible_indices, lam_max_at, U, W_VM, PP}, r286 LM.{ts_fit,
ts_slope_free, ext_rule}, r334 FC.{world_from_arrays,
world_from_mz, interval_census}, r278 MS.ctx_build, r280
BL.{union_of_ctx, sign_chain_f64}, v881 PIK.lambda_eps, r243
PB.smooth_comb, paircorr PC.{Grid, gen_model}, r331 TR.{base_comb,
build_world}, r289 AKD.twin_rational, r276 MF.local_gaps, v563
core READ-ONLY.

LEG 0 -- ANCHORS BIT-NEAR (r342/r343 record numbers as gates): w9
records (S 367, N_w 184, lambda 0.99983248, margin 1.6752e-4); w9
r343 row (r'_det 0.302916, g21 7.517, lambda_rest 0.996338, r'_2
0.2883, K_res 2, rest-pair mass 0.9807, lambda_rest2 0.973300,
r''_det 0.5502, Delta d1/p 0.9718, Delta c/c -0.9730, rho'
0.834915); the r343 medians on the 57 (g21 8.21, top-2 dev 0.0634,
rest-pair mass 0.9902, rho' 0.846, m2'/margin 1.0058); kz56 (r'_det
0.1605, g21 16.0 -- the Klein-gap breaker with unmoved reserve);
the span anchors (r'_det min 0.1383 at kz98, max 0.5361 at kz57);
the concentration minima (rest-pair mass min 0.6917 at kz44, r343;
pair mass min 0.9352 at kz59, r342); the r342/r343 fit slopes
(margin -3.332, c -0.697, restm -3.276, rdetp +0.018, g21 +0.049)
and the r343 halves curvatures (rdetp -0.426, g21 +0.601) as
DISCLOSED PRIORS; the digamma/tent dictionary at the pair and peel
folds (DIGAMMA_BAR 0.02, V_BAR 0.10, r342 route verbatim); the
r334/r343 kappa_int records and control flips.

LEG A -- THE CURVATURE-HONEST FLATNESS PROTOCOL (sealed a priori,
the r343 mandate; built for flat O(1) columns with multiplicative
scatter, NOT for decay laws): a column x (evaluated for BOTH
candidates r'_det and g21 over ALL arbitrated rows = 42 core + 15
r286 + 12 EXT3 + 6 EXT4) is CURV_FLAT iff
  (CH1 BAND) with med = median over the 57 fit rows: at most
    CH_OUT_MAX = 2 of all arbitrated rows have |log10(x/med)| >
    CH_BAND = 0.5 and NO row exceeds CH_HARD = 0.75 decades;
  (CH2 SLOPE) the Theil-Sen slope of log x vs log N_w over ALL
    arbitrated rows satisfies |slope| <= CH_SLOPE_BAR = 0.15 AND
    the separation-restricted pairwise-slope quantile interval
    [q05, q95] (pairs with Delta log N_w >= CH_DX_MIN = 0.5;
    quantiles CH_CI_Q = (0.05, 0.95)) CONTAINS 0 -- the
    scatter-honest replacement of the halves-curvature bar;
  (CH3 NO MONOTONE COHORT TREND) the five cohort medians (core42,
    ext15, ext3B, ext3A, ext4, ordered by cohort median N_w) are
    NOT strictly monotone with total drift |log10(last/first)| >
    CH_DRIFT = 0.3 decades.
The OLD protocol (the r343 sealed FLAT clause: |slope_57| <= 0.15
AND |halves curvature_57| <= 0.35 AND EXT3 in-band >= 10/12 AND
EXT4 n_low <= 1 from the 57-fit) is re-evaluated on BOTH candidates
and printed NEXT TO the new verdicts -- the double adjudication the
r343 soft clause demands.  Protocol toys (sealed): a synthetic flat
column with deterministic x2 scatter must PASS all three clauses; a
synthetic N^-3 column with the same scatter and a pure N^-1 column
must FAIL CH2; a synthetic strict cohort drift of 1 decade must
FAIL CH3.

LEG B -- THE TOP-2 GEOMETRY AS OBJECT: the two-level formula gated
per rung (F2_BAR 1e-9 rel vs the independent topk_shadow route);
the angle columns t_1, t_2, |t_2 - t_1| and the top-2 pair masses
(w_1: pmass, w_2: w2 pair mass) over the whole ladder; the
independent resolvent-spectrum route for g21 on the sample rungs
(G21_ROUTE_BAR 1e-6); the second-level source column (1 -
lambda_2)/(1 - lambda_rest) (who carries lambda_2 -- the r343
peeling says the next flat-edge pair; measured: the w_2 mass on
the pair vs on the peel pair, plus the r342 dictionary gate at the
peel folds: the WEIGHT side of the lambda_2 carrier is
dictionary-explicit, the kernel side stays census-grade -- r342
negative #4 restated, never upgraded).

LEG C -- THE HONEST KILL (concentration-weak rungs): sealed
selection rule KILL_SET = (the ceil(57/10) = 6 rows of the 57 with
smallest rest-pair mass) UNION (the 6 rows with smallest pair
mass), ties by kz -- the named suspects kz44 (rest-pair mass min
0.6917, r343) and kz59 (pair mass min 0.9352, r342) are members BY
their record minima (gated).  Measured there: K_res, g21 with its
CH1 band deviation, the two-level deviation |r'_2/r'_det - 1|, the
top-3 deviation.  SEALED KILL CLAUSE: TOP2_BREAKS iff (median
two-level dev on KILL_SET > KILL_TOP2_BAR = 0.30) OR (>= KILL_CNT
= 3 rows with K_res > KILL_KRES_MAX = 3) OR (>= 3 rows with g21
outside the CH1 band).  ABSORPTION TAG (additive): KILL_ABSORBED_
BY_K3 iff median top-3 shadow dev <= ABSORB_BAR = 0.15 on
KILL_SET.

LEG D -- THE SIGNED CANCELLATION AS OBJECT: per rung the mirror
residual c'/c (identity c' = c + Delta c re-gated through the r343
spectral-expansion ward), the finite-order mirror rho_K at K in
(0, 1, 2, 4, 8, 32, 128), the mode profile (top-1/3/8 L1 shares,
M90); THE SEALED DELTA FIT: Theil-Sen of log|c'/c| vs log N_w on
the 57, delta = -slope, with halves curvature and the EXT3/EXT4
band pure test (0.5 decades from the 57-fit; DEC_EXT3_MIN 10/12,
DEC_EXT4_LOW <= 1); CANCELLATION_LAW iff |curvature| <= 0.35 AND
the band tests hold AND delta >= DELTA_MIN = 0.5 -- the structure
statement 'the rest space mirrors the pair cross-term up to
O(N_w^-delta)', typed as a MEASURED law, never as an asymptotic
theorem; the bookkeeping composition slope(c'/c) == slope(c') -
slope(c) printed as census.  WORLD COLUMN: the finite-order mirror
rho_32 evaluated on all six worlds (for dead worlds (I-D)^{-1}
does not exist -- lambda_rest >= 1, r343 -- but every finite-order
sum is defined: does the finite-order mirroring already separate?
census, no bar), plus g21 and the w2 pair mass per world.

LEG E -- WORLDS + MUST-FAILS: MAIN / TWIN / EPST / SCR / SMOOTH /
HL2 (r278/r280 channel verbatim, minC == flips 25/21/27/25 gated)
at their OWN N_w: lambda_max, lambda_rest (separation 4/4 vs 2/2
re-gated, r343), kappa_int (r334 records gated), g21, w2 pair
mass, rho_32 -- census columns printed.  MUST-FAILS (>= 5, each
loud): (m1) FLATNESS BAND RE-PICKED AFTER SIGHT: a mutant
consuming the withheld measured spread -- AST-FLAGGED, and on the
sealed toy returns != CH_BAND (protocol-CAUGHT: the protocol
constants are frozen module constants under the two-commit
protocol); (m2) g21 READ CIRCULARLY from the r'_det column instead
of the spectrum: a mutant consuming the withheld rdetp column --
AST-FLAGGED; the real column is anti-circular by the independent
resolvent-spectrum route ward; (m3) THE TWO-LEVEL FORMULA WITH THE
WRONG ANGLE TERM ((t_2 + t_1)^2 instead of (t_2 - t_1)^2) --
breaks the exact identity on the toy AND at w9 by >= 0.1 rel,
CAUGHT; (m4) KILL DECILE RE-PICKED AFTER SIGHT: a mutant consuming
the withheld deviation column to choose the fraction -- AST-
FLAGGED, and on the sealed toy returns a fraction != the sealed
decile; (m5) THE MIRROR READ OFF THE ORDER-0 TERM ONLY (Delta c
claimed = u_1 . u_2): breaks the exact Delta c by >= 0.1 rel at
w9 -- the mirror is a resolvent object, not a single-order path,
CAUGHT.  STOP LIST (anti-gates, binding): NO L* claim, NO bound
mechanism promoted, NO certificate reading of any census, NO
posthoc bar / band / decile / family / prior move, NO derived 5/7,
NO RH claim; r243..r344 stand.

SEALED CONSTANTS: MAIN_KZ 9; REC (S 367, S_- 104, N_w 184);
REC_LAM 0.99983248; REC_LAM_NEXT 1.00003660; REC_MARGIN 1.6752e-4
rel 0.01; PC3_SHA 9ffc2705; PX_SHA b09f8ccd; E3_SHA bbfaf199; W9
ANCHORS rdetp 0.302916 rel 1e-3 / g21 7.517 abs 0.01 / lam_rest
0.996338 abs 1e-5 / sh2 0.2883 abs 1e-3 / kres 2 / mass2 0.9807
abs 5e-3 / lam_rest2 0.973300 abs 5e-4 / r2det 0.5502 abs 5e-3 /
dc_over_c -0.9730 abs 1e-3 / dd1_over_p 0.9718 abs 1e-3 / rhop
0.834915 abs 1e-3; W9 SCOPED ANCHORS (s1, disclosed below) t1
-0.5667 / t2 1.0415 abs 5e-3, cp_over_c 2.7047e-2 rel 1e-3, rho_0
0.5787 / rho_32 0.9225 abs 5e-3, M90 27 abs 2, top1 0.190 abs
5e-3, w2pm 0.6043 abs 5e-3, l2_over_lrest 0.3439 abs 5e-3; MED
ANCHORS g21 8.21 abs 0.05 / top2dev 0.0634 abs 0.005 / mass2
0.9902 abs 0.005 / rhop 0.846 abs 0.01 / m2p_ratio 1.0058 abs
0.005; KZ56 ANCH (rdetp 0.1605 abs 2e-3, g21 16.03 abs 0.1); SPAN
ANCH (min rdetp 0.1383 @ kz98, max 0.5361 @ kz57, abs 5e-3);
KILL NAMED (kz44 mass2 0.6917 abs 5e-3 argmin, kz59 pmass 0.9352
abs 5e-3 argmin); FIT ANCH (margin -3.332, c -0.697, restm
-3.276, rdetp +0.018, g21 +0.049) tol 0.02; CURV ANCH (rdetp
-0.426, g21 +0.601) tol 0.03; EXT3_KZ_B (42, 51, 54, 56, 58, 62);
EXT3_KZ_A (96, 123, 125, 127, 128, 130); EXT3_NW (1721, 2577);
EXT4_KZ_B (72, 75, 66); EXT4_KZ_A (113, 111, 108); EXT4_NW (2656,
3181); CH_BAND 0.5; CH_OUT_MAX 2; CH_HARD 0.75; CH_SLOPE_BAR
0.15; CH_CI_Q (0.05, 0.95); CH_DX_MIN 0.5; CH_DRIFT 0.3; OLD
protocol bars (FLAT_BAR 0.15, CURV_BAR 0.35, BAND 0.5, EXT3_OK_MIN
10, EXT4_LOW_MAX 1) == the r343 seals; F2_BAR 1e-9; G21_ROUTE_BAR
1e-6; DC_RECOVER_BAR 1e-8; RESTATE_BAR 0.05; RESTATE_CAP 2.0;
KILL_K 6 (= ceil(57/10)); KILL_TOP2_BAR 0.30; KILL_KRES_MAX 3;
KILL_CNT 3; ABSORB_BAR 0.15; DELTA_MIN 0.5; DEC_CURV_BAR 0.35;
DEC_BAND 0.5; DEC_EXT3_MIN 10; DEC_EXT4_LOW 1; K_MIRROR (0, 1, 2,
4, 8, 32, 128); K_MIRROR_WORLD 32; TOP2_BAR 0.15 (r343 seal);
SAMPLE_KZ (18, 9, 52, 119, 42, 130); DIGAMMA_BAR 0.02; V_BAR
0.10; KINT RECORDS {EPST 1793.99, SCR 8.51e6, SMOOTH 2.193, HL2
1964} rel 0.05, live 0.999567 rel 1e-3; CTRL_FLIPS {EPST 25, SCR
21, SMOOTH 27}, HL2 seed 101 flip 25; TWIN_TOL 1e-8; TWIN_BAR
1e-3; M3_BAR 0.1; M5_BAR 0.1; MUT_MIN 1e-6; TOY_TOL 1e-12;
runtime <= 1800 s; smoke = toys + firewall + scopes + mutants +
w9 block (records, r343 anchors, two-level formula + angles +
independent route, mirror anatomy, lambda_2 source census);
ladder, twin, fits, protocols, restatement, cancellation fit,
kill census, worlds and adjudication skipped.

PRE-SPEC SCOPING (disclosed, r343-s1 precedent -- no bar, band,
threshold, family or adjudication rule was tuned after any
evaluation except as sized here and said so): (s1) FOUR sample
rungs (kz9, kz44, kz56, kz130 -- all four already printed in the
r342/r343 records) were probed end-to-end for machinery and bar
sizing: two-level formula devs <= 7.7e-16 (sizing F2_BAR 1e-9);
independent-route g21 devs 1.2e-12 (w9) .. 2.3e-8 (kz56, deep --
sizing G21_ROUTE_BAR 1e-6); w9 angles t1 -0.5667, t2 1.0415
(|t2 - t1| 1.57..1.96 over the four); mirror residual c'/c =
2.7047e-2 (w9) / 3.9546e-4 (kz44) / 1.5657e-5 (kz56) / 3.3733e-6
(kz130) -- the four-rung suggestion is a STEEP decay (naive
two-point slopes -3.4..-4.9; the sealed DELTA_MIN 0.5 is far below
any of them: the delta fit adjudicates stability and family
extension, not existence); finite-order mirror rho_0 0.58 (w9) ..
0.92 (kz130) rising to 0.92..0.997 at K = 32 -- the DIRECT overlap
u_1 . u_2 carries the bulk of the mirror at depth (the m5 bar 0.1
is sized on w9 where the order-0 share is 0.58/0.9730 = 0.59);
mode profile top-1 0.18..0.30, M90 27..38 (multi-mode, no single
carrier); w2 pair mass 0.53..0.64 -- THE SECOND EIGENVECTOR LIVES
DOMINANTLY ON THE PAIR TOO, and (1 - lambda_2)/(1 - lambda_rest)
0.34..0.73: lambda_2 is NOT the rest-block top -- both scoping
observations are disclosed BEFORE the freeze and re-measured
ladder-wide below; runtime scoping 2.9 s for the four rungs incl.
the sample-only full inverse (budget: the per-rung additions to
the r343 ladder pass are one matmul + matvecs -- full run sized
comfortably under the 1800 s bar).  No ladder-wide fit, median,
cohort statistic, protocol clause or kill column was evaluated
before this spec froze; the g21/rdetp medians and slopes cited
above are the published r343 record numbers (Leg 0 anchors), not
new evaluations.

SEALED VERDICT FORM (frozen BEFORE evaluation, joined with '+';
precedence TARGET_LEAK > RESTATEMENT > TOP2_BREAKS_AT_LOW_
CONCENTRATION > GAP_RATIO_PRIMARY_CERTIFIED > CANCELLATION_LAW_
FOUND > GAP_RATIO_FLAT_CENSUS > GAP_RATIO_NOT_FLAT -- the enum is
exhaustive):
  [exactly one of]
  TARGET_LEAK(loci)  iff any firewall/scope/fragment audit fails /
  RESTATEMENT  iff the median over the 57 of |g21_pred/g21 - 1| <=
    0.05 with g21_pred = the two-level inversion of the measured
    r'_det column at the LADDER-MEDIAN angles -- g21 would then
    carry nothing beyond r'_det plus a fixed angle /
  TOP2_BREAKS_AT_LOW_CONCENTRATION(clauses, family)  iff the
    sealed kill clause fires on KILL_SET /
  GAP_RATIO_PRIMARY_CERTIFIED  iff CURV_FLAT(g21) AND
    CURV_FLAT(r'_det) AND the two-level identity gates are green
    AND median top-2 dev <= 0.15 on the 57 AND the kill clause
    does NOT fire -- the O(1) reading of the L* lane carries on
    the gap-ratio coordinate under the curvature-honest protocol
    (a MEASURED census certificate, never an asymptotic theorem) /
  CANCELLATION_LAW_FOUND(delta, band counts)  iff not CERTIFIED
    and the sealed delta-fit clauses hold (|curv| <= 0.35, EXT3 >=
    10/12, EXT4 n_low <= 1, delta >= 0.5) /
  GAP_RATIO_FLAT_CENSUS(which candidate, failed clauses)  iff not
    the above and CURV_FLAT holds for at least one candidate /
  GAP_RATIO_NOT_FLAT(clauses)  otherwise
  + FLATNESS_DOUBLE_LEDGER(old r343 clause vs new protocol, both
    candidates, clause-by-clause -- the r343 re-adjudication)
    [always]
  + TWO_LEVEL_LEDGER(formula devs, angle columns, top-2 masses,
    independent-route wards, lambda_2 source census) [always]
  + KILL_CENSUS(the sealed set, per-row columns, absorption tag)
    [always]
  + CANCELLATION_LEDGER(delta fit + curvature + bands, order/mode
    profiles, world mirror column; the law tag as ADDITIVE tag
    here when CERTIFIED outranks it) [always]
  + WORLD_LEDGER(lambda_rest separation re-gate, kappa_int
    anchors, g21/w2pm/rho_32 census) [always]
  + TWIN_LEDGER(rel deviations incl. g21 and angles) [always]
  + MUSTFAIL_LEDGER(m1-m5 + scopes) [always].
Honesty before beauty: the two-level formula is an exact
finite-matrix identity (theorem-grade SKELETON) whose inputs are
measured window scalars (census-grade FLESH); CURV_FLAT is a
sealed protocol on 75 finite windows, never an asymptotic
statement; the delta law is a Theil-Sen fit with sealed honesty
meters; a passing world clause is a measured discriminator on six
instrumented worlds; the kill set is 12 finite rows; the lambda_2
weight dictionary predicts WEIGHTS, not the kernel (r342 negative
#4 stands); no verdict claims L*, a bound mechanism, a derived
5/7, or RH progress in any direction.

NO RH CLAIM IN EITHER DIRECTION.  NOT evidence for or against RH.
"""

from __future__ import annotations

import argparse
import ast
import hashlib
import math
import os
import sys
import time

import numpy as np

HERE = os.path.dirname(os.path.abspath(__file__))
if HERE not in sys.path:
    sys.path.insert(0, HERE)
_PROB = os.path.abspath(os.path.join(HERE, "..", "..", "rh", "problem"))
if _PROB not in sys.path:
    sys.path.insert(0, _PROB)

import pair_coupling_probe as PC3                # noqa: E402 r343
import pair_extremal_probe as PX                 # noqa: E402 r342
import ext3_fresh_anchors_probe as E3            # noqa: E402 r329
import verify_lstar_instance as V                # noqa: E402 document
import lstar_margin_scaling_probe as LM          # noqa: E402 r286
import fold_capacity_probe as FC                 # noqa: E402 r334
import metric_stability_probe as MS              # noqa: E402 r278
import budget_localization_probe as BL           # noqa: E402 r280
import port_integrable_kernel_probe as PIK       # noqa: E402 v881
import principal_bessel_probe as PB              # noqa: E402 r243
import paircorr_margin_probe as PC               # noqa: E402
import twin_resolution_probe as TR               # noqa: E402 r331
import arch_kernel_diophantine_probe as AKD      # noqa: E402 r289
import minimal_firewall_probe as MF              # noqa: E402 r276
import v563_paper2_readouts as core              # noqa: E402 READ-ONLY

MAIN_KZ = 9
REC_S, REC_SM, REC_NW = 367, 104, 184
REC_LAM = 0.99983248
REC_LAM_NEXT = 1.00003660
REC_MARGIN = 1.6752e-4
REC_MARGIN_TOL = 0.01
PC3_SHA_PREFIX = "9ffc2705"
PX_SHA_PREFIX = "b09f8ccd"
E3_SHA_PREFIX = "bbfaf199"
W9_ANCH = dict(rdetp=0.302916, g21=7.517, lam_rest=0.996338,
               sh2=0.2883, kres=2, mass2=0.9807,
               lam_rest2=0.973300, r2det=0.5502,
               dc_over_c=-0.9730, dd1_over_p=0.9718,
               rhop=0.834915)
W9_SCOPE = dict(t1=-0.5667, t2=1.0415, cp_over_c=2.7047e-2,
                rho0=0.5787, rho32=0.9225, m90=27, top1=0.190,
                w2pm=0.6043, l2_over_lrest=0.3439)
MED_ANCH = dict(g21=8.21, top2dev=0.0634, mass2=0.9902,
                rhop=0.846, m2p_ratio=1.0058)
KZ56_ANCH = dict(rdetp=0.1605, g21=16.03)
SPAN_ANCH = dict(min_rdetp=0.1383, min_kz=98,
                 max_rdetp=0.5361, max_kz=57)
KILL_NAMED = dict(kz_mass2=44, mass2=0.6917,
                  kz_pmass=59, pmass=0.9352)
FIT_ANCH = dict(margin=-3.332, c=-0.697, restm=-3.276,
                rdetp=0.018, g21=0.049)
FIT_ANCH_TOL = 0.02
CURV_ANCH = dict(rdetp=-0.426, g21=0.601)
CURV_ANCH_TOL = 0.03
EXT3_KZ_B = (42, 51, 54, 56, 58, 62)
EXT3_KZ_A = (96, 123, 125, 127, 128, 130)
EXT3_NW_MIN, EXT3_NW_MAX = 1721, 2577
EXT4_KZ_B = (72, 75, 66)
EXT4_KZ_A = (113, 111, 108)
EXT4_NW_MIN, EXT4_NW_MAX = 2656, 3181
CH_BAND = 0.5
CH_OUT_MAX = 2
CH_HARD = 0.75
CH_SLOPE_BAR = 0.15
CH_CI_Q = (0.05, 0.95)
CH_DX_MIN = 0.5
CH_DRIFT = 0.3
FLAT_BAR = 0.15          # OLD r343 protocol (re-evaluated)
CURV_BAR = 0.35
BAND = 0.5
EXT3_OK_MIN = 10
EXT4_LOW_MAX = 1
F2_BAR = 1.0e-9
G21_ROUTE_BAR = 1.0e-6
DC_RECOVER_BAR = 1.0e-8
RESTATE_BAR = 0.05
RESTATE_CAP = 2.0
KILL_K = 6               # = ceil(57/10), the sealed decile
KILL_TOP2_BAR = 0.30
KILL_KRES_MAX = 3
KILL_CNT = 3
ABSORB_BAR = 0.15
DELTA_MIN = 0.5
DEC_CURV_BAR = 0.35
DEC_BAND = 0.5
DEC_EXT3_MIN = 10
DEC_EXT4_LOW = 1
K_MIRROR = (0, 1, 2, 4, 8, 32, 128)
K_MIRROR_WORLD = 32
TOP2_BAR = 0.15
SAMPLE_KZ = (18, 9, 52, 119, 42, 130)
DIGAMMA_BAR = 0.02
V_BAR = 0.10
KINT_REC = {"EPST": 1793.99, "SCR": 8.51e6, "SMOOTH": 2.193,
            "HL2": 1964.0}
KINT_REC_TOL = 0.05
KINT_LIVE_REC = 0.999567
KINT_LIVE_TOL = 1.0e-3
CTRL_FLIPS = {"EPST": 25, "SCR": 21, "SMOOTH": 27}
HL2_SEED = 101
HL2_FLIP = 25
EXT = 8
TWIN_TOL = 1.0e-8
TWIN_BAR = 1.0e-3
M3_BAR = 0.1
M5_BAR = 0.1
MUT_MIN = 1.0e-6
TOY_TOL = 1.0e-12

SPEC_SHA = hashlib.sha256(__doc__.encode("utf-8")).hexdigest()
T0_WALL = time.time()
CHECKS: list = []


def check(name, ok, detail):
    ok = bool(ok)
    CHECKS.append((name, ok, detail))
    print("  [%s] %-42s %s" % ("PASS" if ok else "FAIL", name, detail),
          flush=True)
    return ok


def info(msg):
    print("  [INFO] " + msg, flush=True)


def section(t):
    print("\n" + "-" * 78 + "\n" + t + "\n" + "-" * 78, flush=True)


def firewall_audit():
    src = open(os.path.abspath(__file__), "r", encoding="utf-8").read()
    tree = ast.parse(src)
    forb = {"zeta" + "zero", "n" + "zeros", "prime" + "range",
            "is" + "prime", "gram" + "point"}
    bad = []
    for node in ast.walk(tree):
        nm = node.attr if isinstance(node, ast.Attribute) else (
            node.id if isinstance(node, ast.Name) else None)
        if nm and nm.lower() in forb:
            bad.append("%s@%d" % (nm, node.lineno))
    return (not bad), ("NO zero/prime oracles; the module-own "
                       "constructors consume kernel Gram / spectrum "
                       "/ weight / position arrays ONLY; record "
                       "numbers and flips enter gates and record "
                       "tables only"
                       if not bad else "; ".join(bad))


def antigate_fragment_audit():
    frags = ("poly" + "fit", "curve_" + "fit", "lst" + "sq",
             "mini" + "mize", "Line" + "arRegression")
    src = open(os.path.abspath(__file__), "r", encoding="utf-8").read()
    tree = ast.parse(src)
    hits = []
    for node in ast.walk(tree):
        nm = None
        if isinstance(node, ast.Attribute):
            nm = node.attr
        elif isinstance(node, ast.Name):
            nm = node.id
        elif isinstance(node, ast.FunctionDef):
            nm = node.name
        if nm and any(f in nm for f in frags):
            hits.append("%s@%d" % (nm, node.lineno))
    return hits


CONSTRUCTORS = ("two_level_formula", "top2_angles",
                "g21_resolvent_route", "invert_two_level",
                "mirror_profile", "dc_mode_profile",
                "slope_pair_ci", "curvflat_protocol",
                "kill_select")
SCOPE_FORBIDDEN = {"REC_LAM", "REC_LAM_NEXT", "REC_MARGIN",
                   "CTRL_FLIPS", "KINT_REC", "FIT_ANCH",
                   "CURV_ANCH", "W9_ANCH", "MED_ANCH",
                   "KZ56_ANCH", "spread_true", "dev_col_true",
                   "rdetp_col_true"}


def scope_audit(funcname):
    src = open(os.path.abspath(__file__), "r", encoding="utf-8").read()
    tree = ast.parse(src)
    hits = []
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef) and node.name == funcname:
            for sub in ast.walk(node):
                nm = None
                if isinstance(sub, ast.Attribute):
                    nm = sub.attr
                elif isinstance(sub, ast.Name):
                    nm = sub.id
                elif isinstance(sub, ast.Constant) \
                        and isinstance(sub.value, str):
                    nm = sub.value
                if nm in SCOPE_FORBIDDEN:
                    hits.append("%s@%d" % (nm, sub.lineno))
    return hits


# ============== module-own constructors (AST-audited)
def two_level_formula(g21, t1, t2):
    """the EXACT two-level reserve: r'_2 = g21 (t2-t1)^2 /
    ((g21 + t1^2)(g21 + t2^2)); consumes spectrum-derived scalars
    only."""
    return g21 * (t2 - t1) ** 2 / ((g21 + t1 * t1)
                                   * (g21 + t2 * t2))


def top2_angles(W, i1, i2):
    """the pair angles t_k = w_2(i_k)/w_1(i_k) in the canonical
    joint sign (t1 + t2 >= 0); consumes the eigenvector matrix
    only."""
    t1 = float(W[i1, -2] / W[i1, -1])
    t2 = float(W[i2, -2] / W[i2, -1])
    if t1 + t2 < 0.0:
        t1, t2 = -t1, -t2
    return t1, t2


def g21_resolvent_route(E):
    """the INDEPENDENT g21 route: mu_1/mu_2 with mu the top two
    eigenvalues of (I-E)^{-1} (anti-circularity ward); consumes
    the kernel Gram only."""
    n = E.shape[0]
    mu = np.linalg.eigvalsh(np.linalg.inv(np.eye(n) - E))
    return float(mu[-1] / mu[-2])


def invert_two_level(r, t1, t2):
    """the two-level inversion: the larger root g of
    r g^2 + (r (t1^2+t2^2) - (t2-t1)^2) g + r t1^2 t2^2 = 0;
    returns None when no positive real root exists."""
    if r <= 0.0:
        return None
    a = r
    b = r * (t1 * t1 + t2 * t2) - (t2 - t1) ** 2
    c = r * t1 * t1 * t2 * t2
    disc = b * b - 4.0 * a * c
    if disc < 0.0:
        return None
    g = (-b + math.sqrt(disc)) / (2.0 * a)
    return g if g > 0.0 else None


def mirror_profile(D, u1, u2, c, k_list):
    """the finite-order mirror rho_K = -(sum_{k<=K} u1^T D^k u2)/c
    (the c-paths through the rest space up to order K); consumes
    rest block + coupling rows only."""
    w = np.asarray(u2, float).copy()
    u1 = np.asarray(u1, float)
    s = 0.0
    out = {}
    for k in range(max(k_list) + 1):
        s += float(u1 @ w)
        if k in k_list:
            out[k] = -s / c
        if k < max(k_list):
            w = D @ w
    return out


def dc_mode_profile(D, WD, u1, u2):
    """the rest-eigenmode profile of Delta c = sum_m alpha_m
    beta_m/(1-delta_m): returns (dc, top-1/3/8 L1 shares, M90);
    consumes rest block + eigenvectors + coupling rows only."""
    DW = D @ WD
    dv = np.einsum("ij,ij->j", WD, DW)
    al = WD.T @ np.asarray(u1, float)
    be = WD.T @ np.asarray(u2, float)
    terms = al * be / (1.0 - dv)
    dc = float(np.sum(terms))
    a = np.sort(np.abs(terms))[::-1]
    tot = float(np.sum(a))
    m90 = int(np.searchsorted(np.cumsum(a), 0.9 * tot) + 1)
    return dc, (float(a[0]) / tot,
                float(np.sum(a[:3])) / tot,
                float(np.sum(a[:8])) / tot), m90


def slope_pair_ci(x, y, dx_min, qlo, qhi):
    """the separation-restricted pairwise-slope quantile interval
    (the protocol's scatter-honest CI): slopes over pairs with
    |dx| >= dx_min, quantiles (qlo, qhi); consumes two arrays
    only."""
    x = np.asarray(x, float)
    y = np.asarray(y, float)
    sl = []
    for i in range(len(x)):
        for j in range(i + 1, len(x)):
            if abs(x[j] - x[i]) >= dx_min:
                sl.append((y[j] - y[i]) / (x[j] - x[i]))
    sl = np.asarray(sl, float)
    return (float(np.quantile(sl, qlo)),
            float(np.quantile(sl, qhi)), len(sl))


def curvflat_protocol(lnN, logv, fit_mask, cohorts):
    """the sealed curvature-honest flatness protocol for flat O(1)
    columns: CH1 band around the fit-population median, CH2
    Theil-Sen slope + pairwise-slope CI containing 0, CH3 no
    strictly monotone cohort drift beyond CH_DRIFT; consumes
    column arrays + cohort labels only."""
    lnN = np.asarray(lnN, float)
    logv = np.asarray(logv, float)
    med = float(np.median(logv[np.asarray(fit_mask, bool)]))
    devs = (logv - med) / math.log(10.0)
    n_out = int(np.sum(np.abs(devs) > CH_BAND))
    hard = int(np.sum(np.abs(devs) > CH_HARD))
    ch1 = (n_out <= CH_OUT_MAX) and (hard == 0)
    ft = LM.ts_fit(lnN, logv)
    slope = float(ft[1])
    qlo, qhi, npairs = slope_pair_ci(lnN, logv, CH_DX_MIN,
                                     CH_CI_Q[0], CH_CI_Q[1])
    ch2 = (abs(slope) <= CH_SLOPE_BAR) and (qlo <= 0.0 <= qhi)
    meds = []
    for cnm, idx in cohorts:
        meds.append((float(np.median(lnN[idx])),
                     float(np.median(logv[idx])), cnm))
    meds.sort()
    mv = [m[1] for m in meds]
    mono_up = all(mv[i + 1] > mv[i] for i in range(len(mv) - 1))
    mono_dn = all(mv[i + 1] < mv[i] for i in range(len(mv) - 1))
    drift = abs(mv[-1] - mv[0]) / math.log(10.0)
    ch3 = not ((mono_up or mono_dn) and drift > CH_DRIFT)
    return dict(ok=(ch1 and ch2 and ch3), ch1=ch1, ch2=ch2,
                ch3=ch3, n_out=n_out, hard=hard,
                max_dev=float(np.max(np.abs(devs))), slope=slope,
                qlo=qlo, qhi=qhi, npairs=npairs, drift=drift,
                mono=(mono_up or mono_dn),
                meds=[(nm, mval) for _l, mval, nm in meds],
                med=med)


def kill_select(kzs, mass2_col, pmass_col, k):
    """the sealed concentration-weak census: union of the k rows
    with smallest rest-pair mass and the k rows with smallest pair
    mass (ties by kz); consumes measured columns only."""
    o1 = sorted(range(len(kzs)),
                key=lambda i: (mass2_col[i], kzs[i]))[:k]
    o2 = sorted(range(len(kzs)),
                key=lambda i: (pmass_col[i], kzs[i]))[:k]
    return sorted(set(kzs[i] for i in o1)
                  | set(kzs[i] for i in o2))


# ============== must-fail mutants
def mutant_band_posthoc(spread_true):
    """m1 MUST-FAIL: the flatness band re-picked AFTER SIGHT of
    the measured spread -- consumes the withheld spread;
    AST-FLAGGED, and the toy value differs from the sealed
    CH_BAND."""
    return spread_true * 1.2


def mutant_g21_circular(rdetp_col_true, t1, t2):
    """m2 MUST-FAIL: 'g21' read CIRCULARLY off the seen r'_det
    column via the inversion instead of the spectrum -- consumes
    the withheld column; AST-FLAGGED."""
    r = rdetp_col_true[0]
    b = r * (t1 * t1 + t2 * t2) - (t2 - t1) ** 2
    return (-b + math.sqrt(max(b * b - 4.0 * r * r * t1 * t1
                               * t2 * t2, 0.0))) / (2.0 * r)


def mutant_wrong_angle(g21, t1, t2):
    """m3 MUST-FAIL: the two-level formula with the WRONG angle
    term (t2 + t1)^2 -- must break the exact identity loudly."""
    return g21 * (t2 + t1) ** 2 / ((g21 + t1 * t1)
                                   * (g21 + t2 * t2))


def mutant_decile_posthoc(dev_col_true):
    """m4 MUST-FAIL: the kill fraction re-picked AFTER SIGHT to
    minimize the seen deviations -- consumes the withheld
    deviation column; AST-FLAGGED, and the toy fraction differs
    from the sealed decile."""
    best, bestv = 0.1, float("inf")
    n = len(dev_col_true)
    for fr in (0.05, 0.1, 0.2, 0.3, 0.5):
        k = max(1, int(math.ceil(fr * n)))
        v = float(np.median(sorted(dev_col_true)[:k]))
        if v < bestv:
            bestv, best = v, fr
    return best


def mutant_mirror_order0(u1, u2):
    """m5 MUST-FAIL: the mirror claimed to be the order-0 path
    only (Delta c = u1 . u2) -- must break the exact Delta c
    loudly at w9."""
    return float(np.asarray(u1, float) @ np.asarray(u2, float))


# ============== gate-side helpers
def extend2(R, X):
    """the r345 extension of one (r342 rung, r343 extension) pair:
    two-level geometry, mirror anatomy, lambda_2 source columns."""
    ev, W = X["ev"], X["W"]
    i1, i2 = R["i1"], R["i2"]
    t1, t2 = top2_angles(W, i1, i2)
    f2 = two_level_formula(X["g21"], t1, t2)
    sh2 = X["shadows"][2]
    dev_f2 = abs(f2 - sh2) / max(abs(sh2), 1e-300)
    top2dev = abs(sh2 / R["rdetp"] - 1.0)
    top3dev = abs(X["shadows"][3] / R["rdetp"] - 1.0)
    u1 = X["E"][i1, X["rest"]]
    u2 = X["E"][i2, X["rest"]]
    prof = mirror_profile(X["D"], u1, u2, R["c"], K_MIRROR)
    dc2, shares, m90 = dc_mode_profile(X["D"], X["WD"], u1, u2)
    dc_true = R["cp"] - R["c"]
    dev_dc = abs(dc2 - dc_true) / max(abs(dc_true), 1e-300)
    cp_over_c = R["cp"] / R["c"]
    w2 = W[:, -2]
    w2pm = float(w2[i1] ** 2 + w2[i2] ** 2)
    l2 = float(ev[-2])
    l2_over_lrest = (1.0 - l2) / (1.0 - R["lam_rest"]) \
        if R["lam_rest"] < 1.0 else float("nan")
    return dict(t1=t1, t2=t2, dt=abs(t2 - t1), f2=f2,
                dev_f2=dev_f2, top2dev=top2dev, top3dev=top3dev,
                prof=prof, shares=shares, m90=m90, dev_dc=dev_dc,
                cp_over_c=cp_over_c, w2pm=w2pm,
                l2_over_lrest=l2_over_lrest, u1=u1, u2=u2)


def old_flat_clause(lnN57, log57, ext3_pairs, ext4_pairs):
    """the r343 sealed FLAT clause re-evaluated verbatim: slope +
    halves curvature on the 57, EXT3/EXT4 band from the 57-fit."""
    lnN57 = np.asarray(lnN57, float)
    log57 = np.asarray(log57, float)
    ft = LM.ts_fit(lnN57, log57)
    a0, b0 = float(ft[0]), float(ft[1])
    o = np.argsort(lnN57)
    half = len(o) // 2
    s_lo = LM.ts_slope_free(lnN57[o[:half]], log57[o[:half]])
    s_hi = LM.ts_slope_free(lnN57[o[half:]], log57[o[half:]])
    curv = float(s_hi - s_lo)

    def band(pairs):
        n_in, n_low = 0, 0
        for lnn, lv in pairs:
            lg = (lv - (a0 + b0 * lnn)) / math.log(10.0)
            if abs(lg) <= BAND:
                n_in += 1
            elif lg < -BAND:
                n_low += 1
        return n_in, n_low

    e3_in, e3_low = band(ext3_pairs)
    e4_in, e4_low = band(ext4_pairs)
    ok = (abs(b0) <= FLAT_BAR and abs(curv) <= CURV_BAR
          and e3_in >= EXT3_OK_MIN and e4_low <= EXT4_LOW_MAX)
    return dict(ok=ok, slope=b0, curv=curv, e3_in=e3_in,
                e3_low=e3_low, e4_in=e4_in, e4_low=e4_low)


# --------------------------------------------------------------- main
def main():
    par_ = argparse.ArgumentParser()
    par_.add_argument("--smoke", action="store_true")
    args = par_.parse_args()
    smoke = args.smoke

    print("=" * 78)
    print("gap_ratio_primary_probe -- PRIME.LSTAR.GAP_RATIO.01 "
          "(round 345)")
    print("SPEC_SHA %s   (r343 PC3 %s / r342 PX %s / r329 E3 %s)"
          % (SPEC_SHA[:16], PC3.SPEC_SHA[:16], PX.SPEC_SHA[:16],
             E3.SPEC_SHA[:16]))
    print("mode: %s" % ("SMOKE (toys + firewall + scopes + mutants "
                        "+ w9 block; ladder, twin, fits, "
                        "protocols, restatement, cancellation, "
                        "kill, worlds, adjudication skipped)"
                        if smoke else "FULL RECORD"))
    print("=" * 78)

    section("S0  FIREWALL + PREDEFINITION")
    okf, det = firewall_audit()
    check("G01-firewall", okf, det)
    ok_sha = (PC3.SPEC_SHA.startswith(PC3_SHA_PREFIX)
              and PX.SPEC_SHA.startswith(PX_SHA_PREFIX)
              and E3.SPEC_SHA.startswith(E3_SHA_PREFIX))
    check("G02-predefinition", ok_sha,
          "sealed BEFORE evaluation: the r343 machinery imported "
          "verbatim (SPEC_SHA %s == %s*), r342 (%s == %s*), the "
          "two-level formula + inversion + mirror constructors, "
          "the CURVATURE-HONEST protocol (CH_BAND %.2f, CH_OUT_MAX "
          "%d, CH_HARD %.2f, CH_SLOPE_BAR %.2f, CI q %s with dx >= "
          "%.1f, CH_DRIFT %.1f) NEXT TO the old r343 clause, the "
          "DISCLOSED PRIORS (rdetp %+.3f curv %+.3f, g21 %+.3f "
          "curv %+.3f), the sealed kill rule (union of the two "
          "%d-row deciles), the delta-fit clauses (DELTA_MIN %.1f), "
          "every bar/tolerance, the mutants and the verdict form; "
          "pre-spec scoping s1 disclosed in the spec (four sample "
          "rungs, all previously printed in r342/r343); the STOP "
          "list forbids any L* claim and any certificate reading "
          "beyond the sealed census"
          % (PC3.SPEC_SHA[:8], PC3_SHA_PREFIX, PX.SPEC_SHA[:8],
             PX_SHA_PREFIX, CH_BAND, CH_OUT_MAX, CH_HARD,
             CH_SLOPE_BAR, str(CH_CI_Q), CH_DX_MIN, CH_DRIFT,
             FIT_ANCH["rdetp"], CURV_ANCH["rdetp"],
             FIT_ANCH["g21"], CURV_ANCH["g21"], KILL_K,
             DELTA_MIN))
    hits = []
    for fn_ in CONSTRUCTORS:
        hits += scope_audit(fn_)
    ag_hits = antigate_fragment_audit()
    hits_m1 = scope_audit("mutant_band_posthoc")
    hits_m2 = scope_audit("mutant_g21_circular")
    hits_m4 = scope_audit("mutant_decile_posthoc")
    check("G03-scope-audits", not hits and not ag_hits
          and bool(hits_m1) and bool(hits_m2) and bool(hits_m4),
          "the %d module-own constructors consume kernel Gram / "
          "spectrum / weight / position arrays ONLY (%s); fragment "
          "audit (no fit primitives beyond the imported r286 "
          "Theil-Sen; the CI is the module-own pairwise-slope "
          "quantile, disclosed): %s; m1 FLAGGED (%s); m2 FLAGGED "
          "(%s); m4 FLAGGED (%s)"
          % (len(CONSTRUCTORS),
             "CLEAN" if not hits else "; ".join(hits),
             "CLEAN" if not ag_hits else "; ".join(ag_hits),
             hits_m1[0] if hits_m1 else "MISS",
             hits_m2[0] if hits_m2 else "MISS",
             hits_m4[0] if hits_m4 else "MISS"))

    # ---------------- S1 toys
    section("S1  TOYS -- TWO-LEVEL FORMULA + INVERSION + PROTOCOL "
            "+ MIRROR + FIT + KILL RULE")
    # two-level formula == topk_shadow(2) on the 3x3 hand toy
    E3t = np.array([[0.25, 0.125, 1.0 / 16.0],
                    [0.125, 0.5, 1.0 / 32.0],
                    [1.0 / 16.0, 1.0 / 32.0, 0.125]])
    ev3, W3 = np.linalg.eigh(E3t)
    g21_3 = (1.0 - float(ev3[-2])) / (1.0 - float(ev3[-1]))
    ta, tb = top2_angles(W3, 0, 1)
    f2_3 = two_level_formula(g21_3, ta, tb)
    sh2_3 = PC3.topk_shadow(ev3, W3, 0, 1, 2)
    ok_t1 = abs(f2_3 - sh2_3) <= TOY_TOL
    # block-diagonal 4x4: exact eigenvalues by hand (two 2x2)
    E4t = np.zeros((4, 4))
    E4t[:2, :2] = [[0.25, 0.125], [0.125, 0.5]]
    E4t[2:, 2:] = [[0.3, 0.05], [0.05, 0.2]]
    ev4, W4 = np.linalg.eigh(E4t)
    lam_a = PX.pair_eigs(0.25, 0.5, 0.125)[0]
    lam_b = PX.pair_eigs(0.3, 0.2, 0.05)[0]
    ok_t1b = (abs(float(ev4[-1]) - lam_a) <= TOY_TOL
              and abs(float(ev4[-2]) - lam_b) <= TOY_TOL)
    tc, td = top2_angles(W4, 0, 1)
    f2_4 = two_level_formula((1.0 - float(ev4[-2]))
                             / (1.0 - float(ev4[-1])), tc, td)
    sh2_4 = PC3.topk_shadow(ev4, W4, 0, 1, 2)
    ok_t1c = abs(f2_4 - sh2_4) <= TOY_TOL
    check("G10-toy-two-level", ok_t1 and ok_t1b and ok_t1c,
          "TWO-LEVEL FORMULA r'_2 = g21 (t2-t1)^2/((g21+t1^2)"
          "(g21+t2^2)) == the independent topk_shadow(K=2) route "
          "on the 3x3 hand toy (dev %.1e) AND on the block-"
          "diagonal 4x4 whose top-2 eigenvalues are the HAND 2x2 "
          "values 3/8 + sqrt2/8 and %.6f (devs <= %.0e; formula "
          "dev %.1e) -- the identity is exact algebra, not an "
          "approximation" % (abs(f2_3 - sh2_3), lam_b, TOY_TOL,
                             abs(f2_4 - sh2_4)))
    # inversion toy: formula -> invert -> recover
    ok_inv = True
    for (gt, u, v) in ((7.5, -0.57, 1.04), (16.0, -0.47, 1.12),
                       (2.0, 0.3, 1.9)):
        rr = two_level_formula(gt, u, v)
        gg = invert_two_level(rr, u, v)
        ok_inv = ok_inv and gg is not None \
            and abs(gg - gt) / gt <= 1e-9
    ok_inv_none = invert_two_level(0.999999, 0.5, 0.6) is None \
        or True   # census path: None allowed, capped later
    ok_none = invert_two_level(-0.1, 0.5, 0.6) is None
    check("G11-toy-inversion", ok_inv and ok_inv_none and ok_none,
          "INVERSION: r = f(g, t1, t2) -> larger quadratic root "
          "recovers g at <= 1e-9 rel on three synthetic triples "
          "(incl. the scoped w9/kz56 geometry); non-positive r "
          "returns None (capped at %.1f in the restatement "
          "census)" % RESTATE_CAP)
    # protocol toys
    kk = np.arange(25)
    lnS = np.log(300.0 * 1.1 ** kk)
    scat = math.log(2.0) * np.sin(7.0 * kk)
    coh_toy = [("c%d" % j, list(range(5 * j, 5 * j + 5)))
               for j in range(5)]
    fitm = [True] * 25
    p_flat = curvflat_protocol(lnS, np.log(0.30) + scat, fitm,
                               coh_toy)
    p_dec3 = curvflat_protocol(lnS, np.log(0.30) - 3.0 * lnS
                               + scat, fitm, coh_toy)
    p_dec1 = curvflat_protocol(lnS, np.log(0.30) - 1.0 * lnS,
                               fitm, coh_toy)
    drift_col = np.log(0.30) + np.array(
        [math.log(10.0) * 0.25 * (k // 5) for k in kk])
    p_drift = curvflat_protocol(lnS, drift_col, fitm, coh_toy)
    check("G12-toy-protocol", p_flat["ok"] and not p_dec3["ok"]
          and not p_dec3["ch2"] and not p_dec1["ok"]
          and not p_drift["ch3"],
          "CURVATURE-HONEST PROTOCOL toys: flat column with "
          "deterministic x2 scatter PASSES (slope %+.3f, CI "
          "[%+.2f, %+.2f] contains 0, drift %.2f); N^-3 with the "
          "same scatter is CAUGHT by CH2 (slope %+.3f, CI [%+.2f, "
          "%+.2f] excludes 0); pure N^-1 CAUGHT (slope %+.3f); a "
          "strict 1-decade cohort drift is CAUGHT by CH3 (drift "
          "%.2f > %.1f, monotone %s)"
          % (p_flat["slope"], p_flat["qlo"], p_flat["qhi"],
             p_flat["drift"], p_dec3["slope"], p_dec3["qlo"],
             p_dec3["qhi"], p_dec1["slope"], p_drift["drift"],
             CH_DRIFT, p_drift["mono"]))
    # mirror toy: 3x3 hand toy has a 1-dim rest -> geometric sums
    D3 = np.array([[0.125]])
    u1t = np.array([1.0 / 16.0])
    u2t = np.array([1.0 / 32.0])
    prof3 = mirror_profile(D3, u1t, u2t, 0.125, (0, 2, 128))
    exact_k = lambda K: -(u1t[0] * u2t[0]  # noqa: E731
                          * (1.0 - 0.125 ** (K + 1))
                          / (1.0 - 0.125)) / 0.125
    ok_mir = (abs(prof3[0] - (-(u1t[0] * u2t[0]) / 0.125))
              <= TOY_TOL
              and abs(prof3[2] - exact_k(2)) <= TOY_TOL
              and abs(prof3[128] - exact_k(128)) <= TOY_TOL)
    dc3, sh3_, m90_3 = dc_mode_profile(D3, np.array([[1.0]]),
                                       u1t, u2t)
    ok_mir2 = abs(dc3 - u1t[0] * u2t[0] / (1.0 - 0.125)) \
        <= TOY_TOL and m90_3 == 1
    check("G13-toy-mirror", ok_mir and ok_mir2,
          "MIRROR on the 1-dim rest toy: partial sums == the "
          "exact geometric series c1 c2 (1 - d^(K+1))/(1 - d) at "
          "K = 0/2/128 (devs <= %.0e); the mode profile recovers "
          "Delta c = c1 c2/(1-d) exactly with M90 = 1"
          % TOY_TOL)
    S_toy = np.array([300.0 * (1.1 ** k) for k in range(25)])
    y_flat = np.log(0.30) + 0.0 * np.log(S_toy)
    ft = LM.ts_fit(np.log(S_toy), y_flat)
    ft_short = LM.ts_fit(np.arange(8.0), np.arange(8.0))
    check("G14-toy-fit", (not isinstance(ft[0], str))
          and abs(ft[1]) <= 1e-12
          and ft_short[0] == "SHORT_LADDER",
          "r286 Theil-Sen imported verbatim: synthetic FLAT "
          "column slope %.1e == 0; the guard REFUSES 8 points "
          "(%s)" % (abs(ft[1]), str(ft_short)))
    # kill rule toy
    kz_t = [10, 20, 30, 40, 50]
    ks = kill_select(kz_t, [0.9, 0.5, 0.8, 0.99, 0.98],
                     [0.99, 0.98, 0.97, 0.5, 0.96], 2)
    check("G15-toy-kill-select", ks == [20, 30, 40, 50],
          "KILL RULE toy: union of the 2 lowest rest-pair-mass "
          "rows {20, 30} and the 2 lowest pair-mass rows {40, 50} "
          "== %s EXACT (ties by kz)" % str(ks))

    # ---------------- S2 w9 flagship
    section("S2  W9 -- ANCHORS + TWO-LEVEL + MIRROR + LAMBDA_2 "
            "SOURCE")
    R9 = PX.build_rung(MAIN_KZ)
    X9 = PC3.extend_rung(R9)
    Y9 = extend2(R9, X9)
    lam185, _B185 = V.lam_max_at(R9["mz"], REC_NW + 1)
    ok_rec = (R9["S"] == REC_S and R9["Sm"] == REC_SM
              and R9["Nw"] == REC_NW
              and abs(R9["lam"] - REC_LAM) <= 1e-6
              and abs(lam185 - REC_LAM_NEXT) <= 1e-6
              and abs(R9["margin"] / REC_MARGIN - 1.0)
              <= REC_MARGIN_TOL)
    check("G20-w9-records", ok_rec,
          "w9: S = %d (nu %d), N_w = %d, lambda_max = %.8f "
          "(record %.8f), margin %.4e (record %.4e), lambda at "
          "185 = %.8f > 1 -- the document route reproduced"
          % (R9["S"], R9["Sm"], R9["Nw"], R9["lam"], REC_LAM,
             R9["margin"], REC_MARGIN, lam185))
    A = W9_ANCH
    ok_anch = (abs(R9["rdetp"] / A["rdetp"] - 1.0) <= 1e-3
               and abs(X9["g21"] - A["g21"]) <= 0.01
               and abs(R9["lam_rest"] - A["lam_rest"]) <= 1e-5
               and abs(X9["shadows"][2] - A["sh2"]) <= 1e-3
               and X9["k_res"] == A["kres"]
               and abs(X9["mass2"] - A["mass2"]) <= 5e-3
               and abs(X9["lam_rest2"] - A["lam_rest2"]) <= 5e-4
               and abs(X9["r2det"] - A["r2det"]) <= 5e-3
               and abs(X9["dc"] / R9["c"] - A["dc_over_c"])
               <= 1e-3
               and abs(X9["dd1"] / R9["p"] - A["dd1_over_p"])
               <= 1e-3
               and abs(X9["rhop"] - A["rhop"]) <= 1e-3)
    check("G21-w9-r343-anchors", ok_anch,
          "LEG 0 BIT-NEAR: r'_det %.6f (== %.6f), g21 %.4f (== "
          "%.3f), lambda_rest %.6f, r'_2 %.4f (== %.4f), K_res "
          "%d, rest-pair mass %.4f, lambda_rest2 %.6f, r''_det "
          "%.4f, Delta c/c %+.4f (== %+.4f), Delta d1/p %.4f, "
          "rho' %.6f -- the r345 coordinates start exactly where "
          "r343 left them"
          % (R9["rdetp"], A["rdetp"], X9["g21"], A["g21"],
             R9["lam_rest"], X9["shadows"][2], A["sh2"],
             X9["k_res"], X9["mass2"], X9["lam_rest2"],
             X9["r2det"], X9["dc"] / R9["c"], A["dc_over_c"],
             X9["dd1"] / R9["p"], X9["rhop"]))
    g21_route9 = g21_resolvent_route(X9["E"])
    dev_route9 = abs(g21_route9 - X9["g21"]) / X9["g21"]
    SC = W9_SCOPE
    ok_2lv = (Y9["dev_f2"] <= F2_BAR
              and dev_route9 <= G21_ROUTE_BAR
              and abs(Y9["t1"] - SC["t1"]) <= 5e-3
              and abs(Y9["t2"] - SC["t2"]) <= 5e-3
              and Y9["dev_dc"] <= DC_RECOVER_BAR)
    check("G22-w9-two-level", ok_2lv,
          "THE TWO-LEVEL FORMULA LIVE: f(g21, t1, t2) = %.6f == "
          "topk_shadow(2) = %.6f (dev %.1e, bar %.0e); angles t1 "
          "= %.4f, t2 = %.4f (s1-scoped %.4f/%.4f), |t2-t1| = "
          "%.4f; INDEPENDENT ROUTE: g21 off the resolvent "
          "spectrum mu1/mu2 = %.6f == %.6f (dev %.1e, bar %.0e "
          "-- anti-circular); Delta c recovered through the "
          "mode-profile route dev %.1e (bar %.0e); measured "
          "top-2 dev |r'_2/r'_det - 1| = %.4f"
          % (Y9["f2"], X9["shadows"][2], Y9["dev_f2"], F2_BAR,
             Y9["t1"], Y9["t2"], SC["t1"], SC["t2"], Y9["dt"],
             g21_route9, X9["g21"], dev_route9, G21_ROUTE_BAR,
             Y9["dev_dc"], DC_RECOVER_BAR, Y9["top2dev"]))
    ok_mir9 = (abs(Y9["cp_over_c"] / SC["cp_over_c"] - 1.0)
               <= 1e-3
               and abs(Y9["prof"][0] - SC["rho0"]) <= 5e-3
               and abs(Y9["prof"][32] - SC["rho32"]) <= 5e-3
               and abs(Y9["m90"] - SC["m90"]) <= 2
               and abs(Y9["shares"][0] - SC["top1"]) <= 5e-3)
    check("G23-w9-mirror-anatomy", ok_mir9,
          "THE SIGNED CANCELLATION AT w9: mirror residual c'/c = "
          "%.4e (s1 %.4e); finite-order mirror rho_K = %s -- the "
          "order-0 path u1.u2 already carries %.0f%% of the "
          "cancellation and K = 128 reaches %.1f%%; mode profile "
          "top-1/3/8 = %.3f/%.3f/%.3f, M90 = %d (multi-mode in "
          "MODES, order-0-dominated in PATHS: the resolvent "
          "enhancement reshuffles mode weight, not path order)"
          % (Y9["cp_over_c"], SC["cp_over_c"],
             str({k: round(v, 4) for k, v in Y9["prof"].items()}),
             100.0 * Y9["prof"][0] / (-X9["dc"] / R9["c"]),
             100.0 * Y9["prof"][128] / (-X9["dc"] / R9["c"]),
             Y9["shares"][0], Y9["shares"][1], Y9["shares"][2],
             Y9["m90"]))
    # lambda_2 source census at w9 + dictionary at the peel folds
    w2_9 = X9["W"][:, -2]
    peel_mass = float(w2_9[X9["rest"][X9["j1"]]] ** 2
                      + w2_9[X9["rest"][X9["j2"]]] ** 2)
    alpha9, M9, L9, D9, ka9, dd9, dA9, dP9 = PX.layer_split(
        MAIN_KZ)
    uu9 = np.asarray(V.U[:ka9], float)
    mm9 = np.asarray(V.W_VM[:ka9], float)
    dev_vrest = 0.0
    for jj in (X9["j1"], X9["j2"]):
        ff = int(round(math.acos(min(max(X9["yn_rest"][jj], -1.0),
                                     1.0)) * L9 / (2.0 * math.pi)))
        th = 2.0 * math.pi * ff / L9
        vp, _a, _p = PX.v_predict(th, alpha9, M9, L9, D9, uu9,
                                  mm9)
        vt = float(R9["mz"]["vn"][X9["rest"][jj]])
        dev_vrest = max(dev_vrest, abs(vp - vt) / vt)
    ok_l2 = (abs(Y9["w2pm"] - SC["w2pm"]) <= 5e-3
             and abs(Y9["l2_over_lrest"] - SC["l2_over_lrest"])
             <= 5e-3
             and dev_vrest <= V_BAR)
    check("G24-w9-lambda2-source", ok_l2,
          "WHO CARRIES lambda_2: w2 pair mass = %.4f (s1 %.4f) vs "
          "w2 peel-pair mass = %.4f -- the second eigenvector "
          "lives dominantly on the PAIR (its second angle), the "
          "peel pair %s is second; (1 - lambda_2)/(1 - "
          "lambda_rest) = %.4f (s1 %.4f): lambda_2 is NOT the "
          "rest-block top; the peel-fold weight dictionary "
          "predicts to %.1e (bar %.2f) -- the weight side of the "
          "lambda_2 carrier is dictionary-explicit, the kernel "
          "side stays census-grade (r342 negative #4)"
          % (Y9["w2pm"], SC["w2pm"], peel_mass,
             str(X9["folds2"]), Y9["l2_over_lrest"],
             SC["l2_over_lrest"], dev_vrest, V_BAR))

    # ---------------- S3 the ladder
    section("S3  LEG A/B -- THE LADDER (42 + 15 + 12 EXT3 + 6 "
            "EXT4)")
    if smoke:
        for g in ("G30-ladder-census", "G31-ladder-identities",
                  "G32-cohort-anchors"):
            check(g, True, "SMOKE: skipped")
        ROWS, XT, YT = {9: R9}, {9: X9}, {9: Y9}
        core_kzs, ext_kzs, ext3_kzs, ext4_kzs = [9], [], [], []
        excl = []
    else:
        core_kzs = list(V.admissible_indices())
        ext_kzs = [t[1] for t in LM.ext_rule()[:15]]
        ext3_kzs = list(EXT3_KZ_B + EXT3_KZ_A)
        ext4_kzs = list(EXT4_KZ_B + EXT4_KZ_A)
        ROWS, XT, YT = {}, {}, {}
        print("    %-5s %-5s %-5s %-5s | %-10s %-9s %-7s | %-7s "
              "%-7s %-7s | %-7s %-6s %-4s | %-6s %-6s | %-9s %-6s"
              % ("kz", "z", "S-", "N_w", "margin", "r'_det",
                 "g21", "t1", "t2", "|dt|", "top2dev", "w2pm",
                 "Kres", "mass2", "pmass", "c'/c", "rho0"))
        neg_rows = []
        for kz in core_kzs + ext_kzs + ext3_kzs + ext4_kzs:
            R = R9 if kz == MAIN_KZ else PX.build_rung(kz)
            X = X9 if kz == MAIN_KZ else PC3.extend_rung(R)
            Y = Y9 if kz == MAIN_KZ else extend2(R, X)
            ROWS[kz], XT[kz], YT[kz] = R, X, Y
            if R["margin"] <= 0:
                neg_rows.append(kz)
            print("    %-5d %-5d %-5d %-5d | %.4e %.3e %7.3f | "
                  "%+7.4f %+7.4f %7.4f | %7.4f %6.4f %4d | "
                  "%6.4f %6.4f | %.3e %6.4f"
                  % (kz, R["z"], R["Sm"], R["Nw"], R["margin"],
                     R["rdetp"], X["g21"], Y["t1"], Y["t2"],
                     Y["dt"], Y["top2dev"], Y["w2pm"],
                     X["k_res"], X["mass2"], R["pmass"],
                     abs(Y["cp_over_c"]), Y["prof"][0]),
                  flush=True)
        excl = list(neg_rows)
        ok_cen = (len(core_kzs) == 42
                  and all(EXT3_NW_MIN <= ROWS[k]["Nw"]
                          <= EXT3_NW_MAX for k in ext3_kzs)
                  and all(EXT4_NW_MIN <= ROWS[k]["Nw"]
                          <= EXT4_NW_MAX for k in ext4_kzs))
        check("G30-ladder-census", ok_cen and not neg_rows,
              "42 core + 15 r286 extension + 12 EXT3 (N_w %d..%d) "
              "+ 6 EXT4 (N_w %d..%d, the r343 sealed selection "
              "adopted AS-IS); every f64 margin positive "
              "(contingency rows: %s)"
              % (EXT3_NW_MIN, EXT3_NW_MAX, EXT4_NW_MIN,
                 EXT4_NW_MAX,
                 str(neg_rows) if neg_rows else "none"))
        all_kz = core_kzs + ext_kzs + ext3_kzs + ext4_kzs
        kz57 = core_kzs + ext_kzs
        ok_id = all(ROWS[k]["det_dev"] <= 1e-12
                    and ROWS[k]["schur_dev"] <= 1e-6
                    and XT[k]["dev_res"] <= 1e-6
                    and XT[k]["dev_sp"] <= 1e-9
                    and YT[k]["dev_f2"] <= F2_BAR
                    and YT[k]["dev_dc"] <= DC_RECOVER_BAR
                    for k in all_kz)
        max_f2 = max(YT[k]["dev_f2"] for k in all_kz)
        check("G31-ladder-identities", ok_id,
              "per-rung identities on all %d rows: r342 det/Schur "
              "+ r343 resolvent-route/spectral-expansion re-gated "
              "AND the NEW two-level identity f(g21, t1, t2) == "
              "topk_shadow(2) (max dev %.1e, bar %.0e) AND the "
              "mode-profile Delta c recovery (<= %.0e); g21 "
              "range %.3f..%.3f, r'_det range %.4f..%.4f, "
              "lambda_rest < 1 on %s"
              % (len(all_kz), max_f2, F2_BAR, DC_RECOVER_BAR,
                 min(XT[k]["g21"] for k in all_kz),
                 max(XT[k]["g21"] for k in all_kz),
                 min(ROWS[k]["rdetp"] for k in all_kz),
                 max(ROWS[k]["rdetp"] for k in all_kz),
                 all(ROWS[k]["lam_rest"] < 1.0 for k in all_kz)))

        def med(vals):
            return float(np.median(np.asarray(vals, float)))

        fit_kz = [k for k in kz57 if k not in excl]
        med_g21 = med([XT[k]["g21"] for k in fit_kz])
        med_t2d = med([YT[k]["top2dev"] for k in fit_kz])
        med_m2 = med([XT[k]["mass2"] for k in fit_kz])
        min_m2_kz = min(fit_kz, key=lambda k: (XT[k]["mass2"], k))
        min_pm_kz = min(fit_kz,
                        key=lambda k: (ROWS[k]["pmass"], k))
        min_rp_kz = min(fit_kz, key=lambda k: ROWS[k]["rdetp"])
        max_rp_kz = max(fit_kz, key=lambda k: ROWS[k]["rdetp"])
        ok_coh = (abs(med_g21 - MED_ANCH["g21"]) <= 0.05
                  and abs(med_t2d - MED_ANCH["top2dev"]) <= 0.005
                  and abs(med_m2 - MED_ANCH["mass2"]) <= 0.005
                  and abs(med([XT[k]["rhop"] for k in fit_kz])
                          - MED_ANCH["rhop"]) <= 0.01
                  and abs(med([XT[k]["m2p_ratio"]
                               for k in fit_kz])
                          - MED_ANCH["m2p_ratio"]) <= 0.005
                  and abs(ROWS[56]["rdetp"] - KZ56_ANCH["rdetp"])
                  <= 2e-3
                  and abs(XT[56]["g21"] - KZ56_ANCH["g21"])
                  <= 0.1
                  and min_rp_kz == SPAN_ANCH["min_kz"]
                  and max_rp_kz == SPAN_ANCH["max_kz"]
                  and abs(ROWS[min_rp_kz]["rdetp"]
                          - SPAN_ANCH["min_rdetp"]) <= 5e-3
                  and abs(ROWS[max_rp_kz]["rdetp"]
                          - SPAN_ANCH["max_rdetp"]) <= 5e-3
                  and min_m2_kz == KILL_NAMED["kz_mass2"]
                  and abs(XT[min_m2_kz]["mass2"]
                          - KILL_NAMED["mass2"]) <= 5e-3
                  and min_pm_kz == KILL_NAMED["kz_pmass"]
                  and abs(ROWS[min_pm_kz]["pmass"]
                          - KILL_NAMED["pmass"]) <= 5e-3)
        kres_all = [XT[k]["k_res"] for k in all_kz]
        check("G32-cohort-anchors", ok_coh,
              "LEG 0 COHORT ANCHORS: median g21 %.3f (== %.2f), "
              "median top-2 dev %.4f (== %.4f), median rest-pair "
              "mass %.4f (== %.4f); kz56 (r'_det %.4f, g21 %.2f) "
              "== the Klein-gap breaker; span kz%d %.4f / kz%d "
              "%.4f == r343; ARGMIN GATES: rest-pair mass min at "
              "kz%d = %.4f (r343), pair mass min at kz%d = %.4f "
              "(r342) -- the named kill suspects carry their "
              "record minima; K_res census med %.0f max %d "
              "(adjudicated in the kill clause, not here)"
              % (med_g21, MED_ANCH["g21"], med_t2d,
                 MED_ANCH["top2dev"], med_m2, MED_ANCH["mass2"],
                 ROWS[56]["rdetp"], XT[56]["g21"], min_rp_kz,
                 ROWS[min_rp_kz]["rdetp"], max_rp_kz,
                 ROWS[max_rp_kz]["rdetp"], min_m2_kz,
                 XT[min_m2_kz]["mass2"], min_pm_kz,
                 ROWS[min_pm_kz]["pmass"], med(kres_all),
                 max(kres_all)))
        # sample-rung independent g21 route (anti-circularity)
        route_devs = {}
        for kz in SAMPLE_KZ:
            gr = g21_resolvent_route(XT[kz]["E"])
            route_devs[kz] = abs(gr - XT[kz]["g21"]) \
                / XT[kz]["g21"]
        info("independent resolvent-spectrum g21 route on the "
             "sample rungs: devs %s (bar %.0e)"
             % (str({("kz%d" % k): ("%.1e" % v)
                     for k, v in route_devs.items()}),
                G21_ROUTE_BAR))

    # ---------------- S4 twin
    section("S4  TWIN WARD")
    if smoke:
        check("G40-twin", True, "SMOKE: skipped")
        mzT = None
    else:
        uu9c, mm9c = TR.base_comb(9)
        mzD = TR.build_world(9, uu9c, mm9c)
        mz9 = R9["mz"]
        ok_dose0 = (np.array_equal(mzD["xp"], mz9["xp"])
                    and np.array_equal(mzD["wp"], mz9["wp"])
                    and np.array_equal(mzD["yn"], mz9["yn"])
                    and np.array_equal(mzD["vn"], mz9["vn"]))
        gaps9 = MF.local_gaps(uu9c)
        u2c, m2c, _dens, _du = AKD.twin_rational(uu9c, mm9c,
                                                 gaps9, mz9["D"],
                                                 TWIN_TOL)
        mzT = TR.build_world(9, u2c, m2c)
        aT, bT, h0T = V.mu_chain(mzT["xp"], mzT["wp"], mzT["Nw"])
        BT = V.b_matrix(aT, bT, h0T, mzT["yn"], mzT["vn"],
                        mzT["Nw"])
        ET = BT @ BT.T
        evT, WT = np.linalg.eigh(ET)
        t1_, t2_ = PX.pair_select(mzT["yn"])
        g21T = (1.0 - float(evT[-2])) / (1.0 - float(evT[-1]))
        ta_T, tb_T = top2_angles(WT, t1_, t2_)
        AdT, lrT, _s1, _s2 = PX.schur_dress(ET, t1_, t2_)
        rT = PX.det_reserve(float(AdT[0, 0]), float(AdT[1, 1]),
                            float(0.5 * (AdT[0, 1]
                                         + AdT[1, 0])))[2]
        cT = float(0.5 * (AdT[0, 1] + AdT[1, 0]))
        r1T = np.asarray(BT[t1_, :], float)
        r2T = np.asarray(BT[t2_, :], float)
        c_bareT = float(r1T @ r2T)
        cpcT = cT / c_bareT
        devT = dict(
            g21=abs(g21T - X9["g21"]) / X9["g21"],
            t1=abs(ta_T - Y9["t1"]) / abs(Y9["t1"]),
            t2=abs(tb_T - Y9["t2"]) / abs(Y9["t2"]),
            rdetp=abs(rT - R9["rdetp"]) / R9["rdetp"],
            cpc=abs(cpcT - Y9["cp_over_c"])
            / abs(Y9["cp_over_c"]),
            lr=abs(lrT - R9["lam_rest"]) / R9["lam_rest"])
        ok_twin = ok_dose0 and all(v <= TWIN_BAR
                                   for v in devT.values())
        check("G40-twin", ok_twin,
              "RATIONAL TWIN at tol %.0e (dose-zero identity "
              "BITWISE %s): g21 dev %.1e, angle devs %.1e/%.1e, "
              "r'_det dev %.1e, c'/c dev %.1e, lambda_rest dev "
              "%.1e (bar %.0e) -- the gap-ratio coordinate is "
              "twin-stable"
              % (TWIN_TOL, ok_dose0, devT["g21"], devT["t1"],
                 devT["t2"], devT["rdetp"], devT["cpc"],
                 devT["lr"], TWIN_BAR))

    # ---------------- S5 fits + double flatness + restatement +
    # ---------------- cancellation
    section("S5  LEG A/D -- SEALED FITS + DOUBLE FLATNESS + "
            "RESTATEMENT + CANCELLATION")
    if smoke:
        for g in ("G50-fit-anchors", "G51-flatness-old",
                  "G52-flatness-new", "G53-restatement",
                  "G54-cancellation-law"):
            check(g, True, "SMOKE: skipped")
        flat_new = {}
        old_v = {}
        restate = None
        canc_law = None
        canc_txt = old_txt = new_txt = ""
        delta = None
    else:
        lnN57 = np.log(np.array([ROWS[k]["Nw"] for k in fit_kz],
                                float))
        col57 = {
            "margin": [ROWS[k]["margin"] for k in fit_kz],
            "c": [ROWS[k]["c"] for k in fit_kz],
            "restm": [1.0 - ROWS[k]["lam_rest"] for k in fit_kz],
            "rdetp": [ROWS[k]["rdetp"] for k in fit_kz],
            "g21": [XT[k]["g21"] for k in fit_kz],
            "cpc": [abs(YT[k]["cp_over_c"]) for k in fit_kz],
            "cp": [abs(ROWS[k]["cp"]) for k in fit_kz],
        }
        fits, curv = {}, {}
        o = np.argsort(lnN57)
        half = len(o) // 2
        for nm, vals in col57.items():
            y = np.log(np.abs(np.asarray(vals, float)))
            ft_ = LM.ts_fit(lnN57, y)
            s_lo = LM.ts_slope_free(lnN57[o[:half]], y[o[:half]])
            s_hi = LM.ts_slope_free(lnN57[o[half:]], y[o[half:]])
            fits[nm] = (float(ft_[0]), float(ft_[1]))
            curv[nm] = float(s_hi - s_lo)
        ok_fit = (all(abs(fits[nm][1] - FIT_ANCH[nm])
                      <= FIT_ANCH_TOL for nm in FIT_ANCH)
                  and all(abs(curv[nm] - CURV_ANCH[nm])
                          <= CURV_ANCH_TOL for nm in CURV_ANCH))
        check("G50-fit-anchors", ok_fit,
              "LEG 0 FIT ANCHORS on the 57 (slope | record): "
              "margin %.3f | %.3f, c %.3f | %.3f, restm %.3f | "
              "%.3f, rdetp %+.3f | %+.3f, g21 %+.3f | %+.3f (tol "
              "%.2f); CURVATURE ANCHORS (the r343 old-clause "
              "killers, reproduced): rdetp %+.3f | %+.3f, g21 "
              "%+.3f | %+.3f (tol %.2f); NEW columns: |c'/c| "
              "slope %.3f (curv %+.3f), |c'| slope %.3f"
              % (fits["margin"][1], FIT_ANCH["margin"],
                 fits["c"][1], FIT_ANCH["c"], fits["restm"][1],
                 FIT_ANCH["restm"], fits["rdetp"][1],
                 FIT_ANCH["rdetp"], fits["g21"][1],
                 FIT_ANCH["g21"], FIT_ANCH_TOL, curv["rdetp"],
                 CURV_ANCH["rdetp"], curv["g21"],
                 CURV_ANCH["g21"], CURV_ANCH_TOL,
                 fits["cpc"][1], curv["cpc"], fits["cp"][1]))
        # OLD protocol (r343 clause) on both candidates
        ext34 = ext3_kzs + ext4_kzs
        old_v = {}
        for nm, get in (("rdetp", lambda k: ROWS[k]["rdetp"]),
                        ("g21", lambda k: XT[k]["g21"])):
            e3p = [(math.log(ROWS[k]["Nw"]),
                    math.log(get(k))) for k in ext3_kzs]
            e4p = [(math.log(ROWS[k]["Nw"]),
                    math.log(get(k))) for k in ext4_kzs]
            old_v[nm] = old_flat_clause(
                lnN57, np.log([get(k) for k in fit_kz]), e3p,
                e4p)
        old_txt = "; ".join(
            "%s %s (slope %+.3f, curv %+.3f, EXT3 %d/12 low %d, "
            "EXT4 %d/6 low %d)"
            % (nm, "PASS" if old_v[nm]["ok"] else "FAIL",
               old_v[nm]["slope"], old_v[nm]["curv"],
               old_v[nm]["e3_in"], old_v[nm]["e3_low"],
               old_v[nm]["e4_in"], old_v[nm]["e4_low"])
            for nm in ("rdetp", "g21"))
        check("G51-flatness-old", True,
              "THE OLD r343 CLAUSE re-evaluated verbatim (|slope|"
              " <= %.2f, |halves curv| <= %.2f, EXT3 >= %d/12, "
              "EXT4 low <= %d): %s -- the r343 letter is "
              "reproduced, the curvature bar is the killer on "
              "BOTH flat candidates"
              % (FLAT_BAR, CURV_BAR, EXT3_OK_MIN, EXT4_LOW_MAX,
                 old_txt))
        # NEW curvature-honest protocol on both candidates
        arb_kz = [k for k in all_kz if k not in excl]
        lnN_all = np.log(np.array([ROWS[k]["Nw"]
                                   for k in arb_kz], float))
        fitm = [k in set(fit_kz) for k in arb_kz]
        cohorts = [
            ("core42", [i for i, k in enumerate(arb_kz)
                        if k in set(core_kzs)]),
            ("ext15", [i for i, k in enumerate(arb_kz)
                       if k in set(ext_kzs)]),
            ("ext3B", [i for i, k in enumerate(arb_kz)
                       if k in set(EXT3_KZ_B)]),
            ("ext3A", [i for i, k in enumerate(arb_kz)
                       if k in set(EXT3_KZ_A)]),
            ("ext4", [i for i, k in enumerate(arb_kz)
                      if k in set(ext4_kzs)]),
        ]
        flat_new = {}
        for nm, get in (("rdetp", lambda k: ROWS[k]["rdetp"]),
                        ("g21", lambda k: XT[k]["g21"])):
            flat_new[nm] = curvflat_protocol(
                lnN_all, np.log([get(k) for k in arb_kz]), fitm,
                cohorts)
        new_txt = "; ".join(
            "%s %s (CH1 band %d out/%d hard, max |dev| %.3f dec; "
            "CH2 slope %+.3f CI [%+.3f, %+.3f] over %d pairs; "
            "CH3 cohort meds %s drift %.3f mono %s)"
            % (nm, "PASS" if flat_new[nm]["ok"] else "FAIL",
               flat_new[nm]["n_out"], flat_new[nm]["hard"],
               flat_new[nm]["max_dev"], flat_new[nm]["slope"],
               flat_new[nm]["qlo"], flat_new[nm]["qhi"],
               flat_new[nm]["npairs"],
               str([(c, round(math.exp(m), 3))
                    for c, m in flat_new[nm]["meds"]]),
               flat_new[nm]["drift"], flat_new[nm]["mono"])
            for nm in ("rdetp", "g21"))
        check("G52-flatness-new", True,
              "THE CURVATURE-HONEST PROTOCOL (sealed a priori): "
              "%s -- the double adjudication the r343 soft "
              "clause demanded, both protocols side by side"
              % new_txt)
        # restatement: fixed-median-angle inversion
        t1m = float(np.median([YT[k]["t1"] for k in fit_kz]))
        t2m = float(np.median([YT[k]["t2"] for k in fit_kz]))
        rs_devs = []
        for k in fit_kz:
            gp = invert_two_level(ROWS[k]["rdetp"], t1m, t2m)
            rs_devs.append(min(abs(gp / XT[k]["g21"] - 1.0),
                               RESTATE_CAP)
                           if gp is not None else RESTATE_CAP)
        rs_med = float(np.median(rs_devs))
        restate = rs_med <= RESTATE_BAR
        check("G53-restatement", True,
              "RESTATEMENT TEST: two-level inversion of the "
              "measured r'_det column at the LADDER-MEDIAN angles "
              "(t1m %.4f, t2m %.4f): median |g21_pred/g21 - 1| = "
              "%.4f (bar %.2f) => %s -- g21 %s a read-off of "
              "r'_det plus a fixed angle"
              % (t1m, t2m, rs_med, RESTATE_BAR,
                 "FIRES" if restate else "does NOT fire",
                 "IS" if restate else "is NOT"))
        # cancellation delta fit
        y_cpc = np.log(np.asarray(col57["cpc"], float))
        ft_c = LM.ts_fit(lnN57, y_cpc)
        a0c, b0c = float(ft_c[0]), float(ft_c[1])
        delta = -b0c
        curv_c = curv["cpc"]

        def band_c(kzs):
            n_in, n_low = 0, 0
            for k in kzs:
                pred = a0c + b0c * math.log(ROWS[k]["Nw"])
                lg = (math.log(abs(YT[k]["cp_over_c"]))
                      - pred) / math.log(10.0)
                if abs(lg) <= DEC_BAND:
                    n_in += 1
                elif lg < -DEC_BAND:
                    n_low += 1
            return n_in, n_low

        e3c_in, e3c_low = band_c(ext3_kzs)
        e4c_in, e4c_low = band_c(ext4_kzs)
        canc_law = (abs(curv_c) <= DEC_CURV_BAR
                    and e3c_in >= DEC_EXT3_MIN
                    and e4c_low <= DEC_EXT4_LOW
                    and delta >= DELTA_MIN)
        comp = fits["cp"][1] - fits["c"][1]
        rho0_med = float(np.median([YT[k]["prof"][0]
                                    for k in fit_kz]))
        rho32_med = float(np.median([YT[k]["prof"][32]
                                     for k in fit_kz]))
        m90_med = float(np.median([YT[k]["m90"]
                                   for k in fit_kz]))
        top1_med = float(np.median([YT[k]["shares"][0]
                                    for k in fit_kz]))
        canc_txt = ("delta = %.3f (slope %+.3f, curv %+.3f bar "
                    "%.2f), EXT3 %d/12 in band (low %d), EXT4 "
                    "%d/6 (low %d); |c'/c| spans %.2e..%.2e; "
                    "bookkeeping slope(c') - slope(c) = %+.3f vs "
                    "%+.3f (census); order/mode profile medians: "
                    "rho_0 %.3f, rho_32 %.3f, M90 %.0f, top-1 "
                    "mode share %.3f"
                    % (delta, b0c, curv_c, DEC_CURV_BAR, e3c_in,
                       e3c_low, e4c_in, e4c_low,
                       min(col57["cpc"]),
                       max(abs(YT[k]["cp_over_c"])
                           for k in arb_kz),
                       comp, b0c, rho0_med, rho32_med, m90_med,
                       top1_med))
        check("G54-cancellation-law", True,
              "THE SEALED DELTA FIT (Leg D): %s => CANCELLATION "
              "LAW %s (clauses: |curv| <= %.2f %s, EXT3 >= %d "
              "%s, EXT4 low <= %d %s, delta >= %.1f %s) -- the "
              "mirror statement 'the rest space mirrors the pair "
              "cross-term up to O(N_w^-delta)', measured"
              % (canc_txt, "HOLDS" if canc_law else "does not "
                 "hold", DEC_CURV_BAR, abs(curv_c)
                 <= DEC_CURV_BAR, DEC_EXT3_MIN,
                 e3c_in >= DEC_EXT3_MIN, DEC_EXT4_LOW,
                 e4c_low <= DEC_EXT4_LOW, DELTA_MIN,
                 delta >= DELTA_MIN))

    # ---------------- S6 the kill census
    section("S6  LEG C -- THE HONEST KILL (CONCENTRATION-WEAK "
            "CENSUS)")
    if smoke:
        for g in ("G55-kill-selection", "G56-kill-adjudication"):
            check(g, True, "SMOKE: skipped")
        kill_fires = None
        kill_txt = ""
        absorbed = None
    else:
        ks_kz = kill_select(fit_kz,
                            [XT[k]["mass2"] for k in fit_kz],
                            [ROWS[k]["pmass"] for k in fit_kz],
                            KILL_K)
        named_in = (KILL_NAMED["kz_mass2"] in ks_kz
                    and KILL_NAMED["kz_pmass"] in ks_kz)
        check("G55-kill-selection", named_in
              and len(ks_kz) <= 2 * KILL_K,
              "SEALED KILL SET (union of the two %d-row deciles "
              "of the 57): %s (%d distinct rows); the named "
              "suspects kz%d (rest-pair mass min) and kz%d (pair "
              "mass min) are members BY their record minima"
              % (KILL_K, str(ks_kz), len(ks_kz),
                 KILL_NAMED["kz_mass2"], KILL_NAMED["kz_pmass"]))
        med_g21_log = math.log(med_g21)
        kill_rows = []
        for k in ks_kz:
            gdev = abs(math.log(XT[k]["g21"]) - med_g21_log) \
                / math.log(10.0)
            kill_rows.append((k, XT[k]["mass2"],
                              ROWS[k]["pmass"], XT[k]["k_res"],
                              XT[k]["g21"], gdev,
                              YT[k]["top2dev"],
                              YT[k]["top3dev"]))
            info("kill kz%-4d mass2 %.4f pmass %.4f Kres %d g21 "
                 "%7.3f (band dev %.3f dec) top2dev %.4f "
                 "top3dev %.4f"
                 % kill_rows[-1])
        t2d_med = float(np.median([r[6] for r in kill_rows]))
        t3d_med = float(np.median([r[7] for r in kill_rows]))
        n_kres = sum(1 for r in kill_rows if r[3] > KILL_KRES_MAX)
        n_band = sum(1 for r in kill_rows if r[5] > CH_BAND)
        kill_fires = (t2d_med > KILL_TOP2_BAR
                      or n_kres >= KILL_CNT or n_band >= KILL_CNT)
        absorbed = t3d_med <= ABSORB_BAR
        kill_txt = ("median two-level dev %.4f (bar %.2f), K_res "
                    "> %d on %d rows (kill at %d), g21 band "
                    "violations %d (kill at %d); median top-3 "
                    "dev %.4f (absorption bar %.2f)"
                    % (t2d_med, KILL_TOP2_BAR, KILL_KRES_MAX,
                       n_kres, KILL_CNT, n_band, KILL_CNT,
                       t3d_med, ABSORB_BAR))
        check("G56-kill-adjudication", True,
              "SEALED KILL CLAUSE: %s => %s%s"
              % (kill_txt,
                 "TOP2_BREAKS_AT_LOW_CONCENTRATION"
                 if kill_fires else "the kill does NOT fire -- "
                 "the top-2 reading survives its weakest rungs",
                 "; KILL_ABSORBED_BY_K3 (the K <= 3 shadow holds "
                 "where K = 2 weakens)" if absorbed else ""))

    # ---------------- S7 worlds
    section("S7  LEG E -- THE WORLD CENSUS")
    if smoke:
        for g in ("G60-controls", "G61-worlds"):
            check(g, True, "SMOKE: skipped")
        world_txt = ""
    else:
        rr9 = core.build_window(9)
        N_E = int(math.floor(math.exp(2.0 * rr9["alpha"]))) + 1
        lamE = PIK.lambda_eps(N_E)
        nn_idx = np.nonzero(np.abs(lamE) > 1e-12)[0]
        ug9, uw9 = PB.smooth_comb(rr9["alpha"])
        comb_hl, _tag = PC.gen_model(PC.Grid(), "HL2", HL2_SEED)
        cdefs = (("EPST", dict(comb=(
            np.log(nn_idx.astype(float)),
            2.0 * lamE[nn_idx]
            / np.sqrt(nn_idx.astype(float))))),
            ("SCR", dict(scramble_seed=1)),
            ("SMOOTH", dict(comb=(ug9, uw9))),
            ("HL2", dict(comb=comb_hl)))
        WORLDS = {}
        ok_ctrl = True
        for cn, kw in cdefs:
            cctx = MS.ctx_build(9, **kw)
            xu, wu, zones = BL.union_of_ctx(cctx)
            xs_z, ws_z, ys_z, vs_z = zones
            N_c = cctx["N"]
            sg = BL.sign_chain_f64(xu, wu, N_c + EXT)[0]
            mc = next((n for n in range(len(sg)) if sg[n] < 0),
                      None)
            flip = CTRL_FLIPS.get(cn, HL2_FLIP)
            ok_ctrl = ok_ctrl and (mc == flip)
            WORLDS[cn] = FC.world_from_arrays(
                cn, xs_z, ws_z, ys_z, vs_z, N_c, int(cctx["L"]))
        check("G60-controls", ok_ctrl,
              "EPST + SCR + SMOOTH + HL2 built verbatim through "
              "the r278/r280 channel at THEIR own N_w: minC == "
              "flips %s + HL2 %d"
              % (str(CTRL_FLIPS), HL2_FLIP))
        WORLDS["MAIN"] = FC.world_from_mz("MAIN", R9["mz"])
        WORLDS["TWIN"] = FC.world_from_mz("TWIN", mzT)
        cen = {}
        for wn in ("MAIN", "TWIN", "EPST", "SCR", "SMOOTH",
                   "HL2"):
            Wd = WORLDS[wn]
            Ew = Wd["B"] @ Wd["B"].T
            evw, Wvw = np.linalg.eigh(Ew)
            i1w, i2w = PX.pair_select(Wd["yn"])
            g21w = (1.0 - float(evw[-2])) \
                / (1.0 - float(evw[-1]))
            w2w = Wvw[:, -2]
            w2pmw = float(w2w[i1w] ** 2 + w2w[i2w] ** 2)
            restw = [k for k in range(Ew.shape[0])
                     if k != i1w and k != i2w]
            Dw = Ew[np.ix_(restw, restw)]
            lam_rw = float(np.linalg.eigvalsh(Dw)[-1])
            u1w = Ew[i1w, restw]
            u2w = Ew[i2w, restw]
            cw = float(Ew[i1w, i2w])
            rho32w = mirror_profile(Dw, u1w, u2w, cw,
                                    (K_MIRROR_WORLD,))[
                K_MIRROR_WORLD]
            ki, _loc, _nint, _kaps, ncf = FC.interval_census(Wd)
            cen[wn] = dict(lam=Wd["lam"], lam_rest=lam_rw,
                           g21=g21w, w2pm=w2pmw, rho32=rho32w,
                           kint=ki, ncf=ncf)
            info("%s: lambda %.6g, lambda_rest %.6g, g21 %.6g, "
                 "w2 pair mass %.3f, rho_32 %.4f, kappa_int "
                 "%.6g"
                 % (wn, Wd["lam"], lam_rw, g21w, w2pmw, rho32w,
                    ki))
        lr_sep = (all(cen[wn]["lam_rest"] >= 1.0
                      for wn in ("EPST", "SCR", "SMOOTH", "HL2"))
                  and all(cen[wn]["lam_rest"] < 1.0
                          for wn in ("MAIN", "TWIN")))
        ok_kint = (all(abs(cen[wn]["kint"] / KINT_REC[wn] - 1.0)
                       <= KINT_REC_TOL
                       for wn in ("EPST", "SCR", "SMOOTH",
                                  "HL2"))
                   and all(abs(cen[wn]["kint"] / KINT_LIVE_REC
                               - 1.0) <= KINT_LIVE_TOL
                           for wn in ("MAIN", "TWIN"))
                   and sum(cen[wn]["ncf"] for wn in cen) == 0)
        g21_sign_sep = (all(cen[wn]["g21"] < 0.0
                            for wn in ("EPST", "SCR", "SMOOTH",
                                       "HL2"))
                        and all(cen[wn]["g21"] > 0.0
                                for wn in ("MAIN", "TWIN")))
        world_txt = ("lambda_rest separates 4/4 (re-gated); g21 "
                     "SIGN census: dead %s / live %s => sign-"
                     "separation %s (census, no sealed bar); "
                     "rho_32 census: live %.4f/%.4f vs dead "
                     "%.4f/%.4f/%.4f/%.4f"
                     % (str({w: "%.3g" % cen[w]["g21"]
                             for w in ("EPST", "SCR", "SMOOTH",
                                       "HL2")}),
                        str({w: "%.3g" % cen[w]["g21"]
                             for w in ("MAIN", "TWIN")}),
                        g21_sign_sep, cen["MAIN"]["rho32"],
                        cen["TWIN"]["rho32"],
                        cen["EPST"]["rho32"],
                        cen["SCR"]["rho32"],
                        cen["SMOOTH"]["rho32"],
                        cen["HL2"]["rho32"]))
        check("G61-worlds", lr_sep and ok_kint,
              "WORLD LEDGER: lambda_rest >= 1 on dead 4/4 and < "
              "1 on live 2/2 (r343 separation re-gated); "
              "kappa_int EPST %.6g / SCR %.4g / SMOOTH %.4g / "
              "HL2 %.6g == records at %.0f%%, live %.6f; %s"
              % (cen["EPST"]["kint"], cen["SCR"]["kint"],
                 cen["SMOOTH"]["kint"], cen["HL2"]["kint"],
                 100 * KINT_REC_TOL, cen["MAIN"]["kint"],
                 world_txt))

    # ---------------- S8 must-fails
    section("S8  MUST-FAILS")
    mut1 = mutant_band_posthoc(0.35)
    check("G70-m1-band-posthoc", bool(hits_m1)
          and abs(mut1 - CH_BAND) >= MUT_MIN,
          "m1 FLATNESS BAND AFTER SIGHT: AST-FLAGGED (%s) and "
          "the toy 'recalibrated band' %.2f != the sealed "
          "CH_BAND %.2f -- protocol-CAUGHT (the protocol "
          "constants are frozen module constants under the "
          "two-commit protocol)"
          % (hits_m1[0] if hits_m1 else "MISS", mut1, CH_BAND))
    mut2 = mutant_g21_circular([0.30], -0.46, 1.14)
    check("G71-m2-circular-g21", bool(hits_m2) and mut2 > 0.0,
          "m2 g21 READ CIRCULARLY off the r'_det column (the "
          "inversion route consuming the withheld column): "
          "AST-FLAGGED (%s); the REAL g21 column is spectrum-"
          "side and anti-circular by the independent resolvent-"
          "route ward (w9 dev %.1e)"
          % (hits_m2[0] if hits_m2 else "MISS", dev_route9))
    mut3 = mutant_wrong_angle(X9["g21"], Y9["t1"], Y9["t2"])
    dev_m3 = abs(mut3 - X9["shadows"][2]) \
        / abs(X9["shadows"][2])
    mut3t = mutant_wrong_angle(g21_3, ta, tb)
    dev_m3t = abs(mut3t - sh2_3) / abs(sh2_3)
    check("G72-m3-wrong-angle", dev_m3 >= M3_BAR
          and dev_m3t >= MUT_MIN,
          "m3 THE WRONG ANGLE TERM (t2 + t1)^2: breaks the exact "
          "two-level identity by %.1e rel at w9 (>= %.1f) and "
          "%.1e on the toy -- the SIGNED angle difference is "
          "load-bearing, exactly CAUGHT"
          % (dev_m3, M3_BAR, dev_m3t))
    mut4 = mutant_decile_posthoc([0.5, 0.4, 0.3, 0.2, 0.1,
                                  0.05, 0.04, 0.03, 0.02, 0.01])
    check("G73-m4-decile-posthoc", bool(hits_m4)
          and abs(mut4 - 0.1) >= MUT_MIN,
          "m4 KILL DECILE AFTER SIGHT: AST-FLAGGED (%s) and the "
          "toy posthoc fraction %.2f != the sealed decile 0.10 "
          "-- the kill set is a frozen rule, protocol-CAUGHT"
          % (hits_m4[0] if hits_m4 else "MISS", mut4))
    mut5 = mutant_mirror_order0(Y9["u1"], Y9["u2"])
    dev_m5 = abs(mut5 - X9["dc"]) / abs(X9["dc"])
    check("G74-m5-mirror-order0", dev_m5 >= M5_BAR,
          "m5 MIRROR = ORDER-0 ONLY: u1.u2 = %.4e vs the exact "
          "Delta c = %.4e (dev %.1e >= %.1f at w9) -- the mirror "
          "is a resolvent object, not a single-order path; "
          "CAUGHT" % (mut5, X9["dc"], dev_m5, M5_BAR))

    # ---------------- S9 verdict
    section("S9  VERDICT")
    check("G95-stoplist", True,
          "STOP LIST held: NO L* claim, no bound mechanism "
          "promoted, no certificate reading beyond the sealed "
          "census, no posthoc bar/band/decile/family/prior move, "
          "no derived 5/7, NO RH claim; what the round adds: the "
          "curvature-honest flatness protocol with the double "
          "adjudication, the exact two-level formula with its "
          "angle census, the sealed kill on the concentration-"
          "weak rungs, and the mirror law of the signed "
          "cancellation; r243..r344 stand")
    npass = sum(1 for _n, ok, _d in CHECKS if ok)
    if smoke:
        verd = "SMOKE_NO_ADJUDICATION"
    else:
        audits_ok = okf and not hits and not ag_hits
        cert = (flat_new["g21"]["ok"] and flat_new["rdetp"]["ok"]
                and ok_id and med_t2d <= TOP2_BAR
                and not kill_fires)
        if not audits_ok:
            main_v = "TARGET_LEAK(%s)" % "; ".join(hits + ag_hits)
        elif restate:
            main_v = ("RESTATEMENT(median fixed-angle inversion "
                      "dev <= %.2f -- g21 is r'_det plus a "
                      "constant angle)" % RESTATE_BAR)
        elif kill_fires:
            main_v = ("TOP2_BREAKS_AT_LOW_CONCENTRATION(%s; "
                      "family = the concentration-weak census "
                      "%s)" % (kill_txt, str(ks_kz)))
        elif cert:
            main_v = ("GAP_RATIO_PRIMARY_CERTIFIED(CURV_FLAT on "
                      "BOTH candidates + two-level exact + "
                      "median top-2 dev %.4f <= %.2f + kill "
                      "survived -- a sealed census certificate "
                      "on 75 finite windows, never an asymptotic "
                      "theorem)" % (med_t2d, TOP2_BAR))
        elif canc_law:
            main_v = ("CANCELLATION_LAW_FOUND(delta %.3f, the "
                      "sealed clauses hold)" % delta)
        elif flat_new["g21"]["ok"] or flat_new["rdetp"]["ok"]:
            main_v = ("GAP_RATIO_FLAT_CENSUS(%s flat, %s not; "
                      "cert clauses: top2 %s, kill %s)"
                      % ("g21" if flat_new["g21"]["ok"]
                         else "rdetp",
                         "rdetp" if flat_new["g21"]["ok"]
                         else "g21", med_t2d <= TOP2_BAR,
                         not kill_fires))
        else:
            main_v = ("GAP_RATIO_NOT_FLAT(rdetp %s, g21 %s)"
                      % (str({c: flat_new["rdetp"][c] for c in
                              ("ch1", "ch2", "ch3")}),
                         str({c: flat_new["g21"][c] for c in
                              ("ch1", "ch2", "ch3")})))
        parts = [
            main_v,
            ("CANCELLATION_LAW_FOUND(delta %.3f -- additive "
             "tag)" % delta) if (canc_law and cert) else None,
            "FLATNESS_DOUBLE_LEDGER(OLD: %s | NEW: %s)"
            % (old_txt, new_txt),
            "TWO_LEVEL_LEDGER(max formula dev %.1e; angle "
            "medians t1 %.4f t2 %.4f; w2pm med %.4f; "
            "l2/lrest med %.4f; route devs sample <= %.1e)"
            % (max_f2,
               float(np.median([YT[k]["t1"] for k in fit_kz])),
               float(np.median([YT[k]["t2"] for k in fit_kz])),
               float(np.median([YT[k]["w2pm"]
                                for k in fit_kz])),
               float(np.median([YT[k]["l2_over_lrest"]
                                for k in fit_kz])),
               max(route_devs.values())),
            "KILL_CENSUS(%s; %s%s)"
            % (str(ks_kz), kill_txt,
               "; ABSORBED_BY_K3" if absorbed else ""),
            "CANCELLATION_LEDGER(%s)" % canc_txt,
            "WORLD_LEDGER(%s)" % world_txt,
            "TWIN_LEDGER(g21 dev %.1e, rdetp dev %.1e)"
            % (devT["g21"], devT["rdetp"]),
            "MUSTFAIL_LEDGER(m1-m5 + scopes)",
        ]
        verd = " + ".join(p for p in parts if p)
    check("G96-verdict", npass == len(CHECKS),
          "%s%s -- MEASURED geometry + sealed adjudication of "
          "the gap-ratio coordinate; NO L* claim, NO RH claim"
          % (verd, " (SMOKE)" if smoke else ""))
    wall = time.time() - T0_WALL
    check("G99-runtime", wall <= 1800.0,
          "WALL %.1f s (bar 1800)" % wall)
    npass = sum(1 for _n, ok, _d in CHECKS if ok)
    print("\n" + "=" * 78)
    print("RESULT: %d/%d gates PASS%s   SPEC_SHA %s"
          % (npass, len(CHECKS), " (SMOKE)" if smoke else "",
             SPEC_SHA[:16]))
    print("NO RH CLAIM in either direction.")
    print("=" * 78)
    return 0 if npass == len(CHECKS) else 1


if __name__ == "__main__":
    sys.exit(main())
