#!/usr/bin/env python3
# -*- coding: utf-8 -*-
r"""alpha_source_anatomy_probe -- PRIME.LSTAR.ALPHA_SOURCE_ANATOMY.01
(round 350): THE ALPHA SOURCE ANATOMY -- the kernel side of the L*
margin legislation as an object.  The named successor round of r348
(RATE_EQUALITY_THEOREM): after r348 the irreducible measured rest of
the margin law is the PAIR {alpha, a_c}; a_c = 0.697 is
dictionary-NEAR (c = sqrt(v1 v2) K_N(y1, y2): the weights v are
closed-form digamma/tent, the KERNEL side K_N has no closed form --
the standing r342 negative #4).  THE ROUND'S QUESTION: can the
kernel side (K_N at the pair atoms and in the overlap products) be
brought onto a source-explicit law -- and does one of the two routes
(A: alpha directly from source slopes; B: a_c from source + the
pinning delta structural) close the exponent chain of the L* margin
source-side?

THE CHAIN (r342/r343/r345/r347/r348, all sealed records): r342 the
two-atom law (bare decay laws p -0.754 / q -0.645 / c -0.697;
digamma/tent weight dictionary at v_pred 9.0e-4; kernel-growth
census vs the r285 exponent 0.38); r343 the exact resolvent
dressing; r345 the cancellation law delta = 2.668; r347 the one-line
closure alpha = a_c + delta (C4 residual 0.033); r348 the
RATE_EQUALITY_THEOREM (two-level identity Fractions-exact, delta_x =
alpha - a_x exact under top-2 dominance + flat geometry, live E4 max
0.033) + ORDER0_LAW (y0 ~ N^-0.401 / rho_hi ~ N^-0.393 twin slow
laws whose difference is the c'/c law, resolvent-pinned to the
margin scale; CARRIES_DELTA refuted, gap 2.267) + DICT_T0_GO
(overlap weight side dictionary-explicit, median 0.0177).  THE
ROUND'S CONTRACT: (Leg A) THE KERNEL ANATOMY AT THE PAIR ATOMS:
(i) the Christoffel-Darboux decomposition -- K_N(y_k, y_l) is
SOURCE-COMPUTABLE from the three-term recursion coefficients (a_n,
b_n) of the mu-chain via the CD endpoint / confluent formulas
(gated against the Gram route on the sealed sample); (ii) the exact
slope BOOKKEEPING: from the pointwise identities c = sqrt(v1 v2)
K12, d_k = v_k K_kk, c^2 = (1 - r_det) p q the fitted-slope
bookkeeping a_c == (a_v1 + a_v2)/2 + a_K12 (BK1), s_K11 == -s_v1 +
s_d1 (BK2, the Christoffel saturation v K -> 1), a_c == (a_p +
a_q)/2 + (backward-CS correction) (BK3), each at the fit-
non-additivity bar; the WEIGHT slopes replaced by the r342
digamma/tent dictionary slopes (a_v == a_vpred at 0.02: the weight
side of the kernel bookkeeping is dictionary-grade); (iii) sealed
CANDIDATE adjudication for the kernel exponents: per target column
(a_p, a_q as the Christoffel-deficit laws; g_K12, a_kappa as the
cross-kernel laws) a FROZEN candidate list (simple rationals, the
r285 exponent 0.38 and its complements, the classical hard-edge 2
and bulk 1) at CAND_BAR 0.05 PLUS the sealed decay-law clauses
(|curv| <= 0.35, EXT3 >= 10/12, EXT4 n_low <= 1) -- a KERNEL LAW
fires only as candidate-hit AND clause-clean, else honestly census;
the relation candidates REL1 (perfect correlation s_K12 == (s_K11 +
s_K22)/2, the null expected to FAIL by the r_det -> 0 law) and REL2
(square-root decorrelation s_K12 == (s_K11 + s_K22)/4, sized by the
disclosed two-point scoping ratio 0.52).  (Leg B) ROUTE A, THE
DIRECT ALPHA BALANCE: alpha_bal = -(1/2 (s_vpred1 + s_vpred2) +
s_K12 + s_cpc) -- the margin exponent recomposed from DICTIONARY
weight slopes + the KERNEL cross slope + the PINNING cancellation
slope (anti-circular: no margin column enters), gated against
alpha_meas at 0.1; plus the r342 composition re-gate alpha == a_p +
a_q + rho_r - a_(p+q) at 0.1 and its CANDIDATE image (first-hit
candidates for a_p, a_q with the measured rho_r as the single
census member).  (Leg C) ROUTE B, THE PINNING THEOREM CANDIDATE:
the r348 reading 'the difference of two twin slow laws is
resolvent-pinned to the margin scale' made ALGEBRAIC: on the top-2
truncated resolvent block the identities (i) m2'_2 == margin
EXACTLY (both dressed-2x2 deficits are (1 - lambda_1), (1 -
lambda_2): the bridge is IDENTICALLY 1 in the truncation), (ii)
p'_2 q'_2 - c'_2^2 == margin^2 g21 / Dt^2 EXACTLY (the determinant
identity that binds c' to the margin scale), (iii) c'_2 == margin x
G (G the flat geometry factor), hence Delta c == -c + margin x G:
THE NEAR-CANCELLATION IS THE ALGEBRAIC SELF-CONSISTENCY OF THE
RESOLVENT EQUATION, not an accident -- proved in EXACT Fractions on
TWO rational rank-2 models (the r348 model 9/10, 3/10, 3/5, 4/5 and
a fresh 4/5, 2/5, 5/13, 12/13), carried LIVE by the sealed census
(bridge max <= 0.10 r347 verbatim; det-shadow |p'q' - c'^2| vs
margin^2 g21/Dt^2 median <= 0.35; the r345 CURV_FLAT protocol on
all three margin-pinning ratios r348 verbatim); corollary typed:
slope(y0 - rho_hi) = -(alpha - a_c) is FORCED by (ii) -- the twin
near-cancellation of Leg D is pinned, algebraically.  (Leg D) THE
Y0 TWIN AS CONTROL: the r348 y0/rho_hi laws re-gated bit-near
(slopes -0.401/-0.393, ORDER0 clauses verbatim), the sealed
delta_0 candidate list {0.38 = the r285 kernel exponent, 2/5} at
CAND_BAR, the twin ward |s_y0 - s_rho_hi| <= 0.05, and the WORLD
COLUMN: on the 8 instrumented worlds the order-0 miss y0 = 1 -
rho_0 printed per world -- on the dead worlds lambda_rest >= 1
means the rho_hi series DIVERGES: the twin-law PAIR cannot exist
without the contraction (typed, census).  (Leg E) must-fails.

EXPLORATION ONLY (2026-08-27).  experiments/ level: NO promotion,
NO ledger row, NO marker moved, NO L* CLAIM, NO RH CLAIM in either
direction, mincut unchanged.  Coexistence: R349 (third-arm law,
terminal lane) runs in parallel -- this probe touches NOTHING
outside its own file and the strictly additive rh-sync.  Two-commit
freeze protocol (r329 convention): spec + machinery committed
BEFORE the record run, record tables inserted after.

THE EXACT OBJECTS (all gated): per rung the r342 bundle
(PX.build_rung verbatim, SPEC_SHA prefix gated b09f8ccd), the r343
extension (PC3.extend_rung verbatim, 9ffc2705), the r347 closure
coordinates (DA.closure_cols verbatim, bd1aa7f3), the r348 extras
(DSA.rung_extras verbatim, 307814e9: order-0 split, two-level
shadows, margin-pinning ratios, y0/rho_hi); THE KERNEL COLUMNS
(this round's first spine): K11 = d1/v1, K22 = d2/v2, K12 =
c/sqrt(v1 v2), kappa = K12/sqrt(K11 K22) -- exact rearrangements of
the pair identities d_k = v_k K_N(y_k, y_k), c = sqrt(v1 v2)
K_N(y1, y2) (r342, exact by construction of E = B B^T); THE
CD-ROUTE WARD (this round's second spine): K11, K22, K12 recomputed
INDEPENDENTLY from the mu-chain recursion coefficients via the
Christoffel-Darboux endpoint formula K_N(x, y) = b_{N-1} (p_N(x)
p_{N-1}(y) - p_{N-1}(x) p_N(y))/(x - y) and its confluent form
K_N(x, x) = b_{N-1} (p_N'(x) p_{N-1}(x) - p_{N-1}'(x) p_N(x))
(three-term recursion for p_n and p_n', source-computable) --
gated on the sealed sample at CD_BAR 1e-6 (scoped 1.7e-13 ..
1.2e-10); THE DET SHADOW (the live pinning identity): dev_ds =
|(p' q' - c'^2)/(margin^2 g21 / Dt^2) - 1| with Dt = a1 b2 - a2 b1
the top-2 eigenvector pair determinant (truncation-limited, scoped
0.056..0.11 on the four sample rungs); THE SLOPE BALANCE
constructor alpha_bal (consumes fitted dictionary/kernel/dressed
slopes ONLY -- anti-circular); the sealed candidate adjudicator.

INDEX FIREWALL (binding, r238-r348 discipline): w = window (kz
into the prime-power list), S = #union atoms, S_- = #nu atoms,
N_w = (S+1)//2; ground truth (r283..r348 records, control flips,
kappa_int records) enters GATES and record tables only; the
module-own constructors consume kernel Gram / spectrum / weight /
position / chain-coefficient arrays and measured columns ONLY (AST
scope audit; withheld identifiers are the RECORD values ALPHA_REC
and DELTA_REC and the verdict-side column slope_col_true); no
zero/prime oracles anywhere (AST firewall; the prime-power grid is
the sealed source comb, r238 convention); no fit primitives
(fragment audit; fits are the imported r286 Theil-Sen; the flat
protocol is the imported r345 curvflat_protocol; the decay
instrument is the imported r347 decay_law).  MACHINERY IMPORTED
VERBATIM: r342 PX.{build_rung, pair_select, pair_eigs, det_reserve,
schur_dress, v_predict} (b09f8ccd), r343 PC3.extend_rung
(9ffc2705), r345 GR.{curvflat_protocol, mirror_profile} (1f99235a),
r347 DA.{closure_cols, decay_law, bridge_stats, mirror_world_row}
(bd1aa7f3), r348 DSA.{rung_extras, order0_split, two_level_dressed}
(307814e9), r330 DSW.{dirichlet_comb, dirichlet_abs_comb}
(66526018), document pipeline V.{build_measures, mu_chain,
b_matrix, admissible_indices, lam_max_at, window_shape, U, W_VM,
PP}, r286 LM.{ts_fit, ts_slope_free, ext_rule}, r334
FC.{world_from_arrays, world_from_mz, interval_census}, r278
MS.ctx_build, r280 BL.{union_of_ctx, sign_chain_f64}, v881
PIK.lambda_eps, r243 PB.smooth_comb, paircorr PC.{Grid, gen_model},
r331 TR.{base_comb, build_world}, r289 AKD.twin_rational, r276
MF.local_gaps, v563 core READ-ONLY.

LEG 0 -- ANCHORS BIT-NEAR (r342..r348 record numbers as gates): w9
records (S 367, N_w 184, lambda 0.99983248, margin 1.6752e-4); the
r347 w9 closure row (p' 8.4606e-4, q' 1.2903e-3, c' 8.7234e-4, m2'
1.6800e-4, bridge 1.0029, c'/c 2.7047e-2, g21 7.517, K_res 2); the
r348 w9 S1 anchors (rho0 0.5787, y0 0.42134, rho_hi 0.39430, sh
0.0537/0.0577/0.0667, pm/qm/cm 5.0506/7.7025/5.2075); the r348
cohort medians (rho0 0.839, bridge 1.0058); the sealed fit slopes
(margin -3.332, c -0.697, p -0.754, q -0.645, |c'/c| -2.668, y0
-0.401, rho_hi -0.393) and curvatures (margin -0.347, c +0.308,
cpc -0.189, y0 +0.094, rhi +0.080) as DISCLOSED PRIORS; the
r343 sealed EXT3/EXT4 selections adopted AS-IS; the r334/r345
kappa_int records and control flips; the r285/r342 within-window
kernel exponent P_BIND = 0.38 (sealed constant, candidate side).

LEG E -- MUST-FAILS (>= 5, each loud): (m1) THE CD FORMULA WITHOUT
THE b_{N-1} PREFACTOR -- breaks the kernel ward by >= 0.1 rel at w9
(the recursion normalization is load-bearing); exactly CAUGHT.
(m2) THE KERNEL LAW READ BACK FROM ALPHA: a mutant consuming the
withheld record exponents (ALPHA_REC - DELTA_REC as 'the kernel
slope') -- AST-FLAGGED.  (m3) CANDIDATES PICKED AFTER SIGHT: a
mutant consuming the withheld measured slope column and returning
the nearest simple rational -- AST-FLAGGED, and the toy value
differs from every sealed candidate list entry (protocol-CAUGHT:
the lists are frozen module constants under the two-commit
protocol).  (m4) THE BALANCE WITH THE DOUBLE-COUNTED WEIGHT SHARE:
(s_v1 + s_v2) instead of (s_v1 + s_v2)/2 -- breaks the exact toy
balance by EXACTLY |s_v1 + s_v2|/2 = 1.1 >= 0.5; exactly CAUGHT.
(m5) THE PINNING FAMILY WITH BUILT-IN TARGET: a mutant planting
the withheld record exponent into a synthetic pinning family --
AST-FLAGGED, and the sealed toy balance exponent differs from the
record readback by construction (construction-CAUGHT).  STOP LIST
(anti-gates, binding): NO L* claim, NO bound mechanism promoted,
NO certificate reading of any census, NO posthoc bar / band /
family / prior / candidate move, NO derived 5/7, NO RH claim,
mincut unchanged; r243..r349 stand.

SEALED CONSTANTS: MAIN_KZ 9; REC (S 367, S_- 104, N_w 184);
REC_LAM 0.99983248; REC_LAM_NEXT 1.00003660; REC_MARGIN 1.6752e-4
rel 0.01; PX_SHA b09f8ccd; PC3_SHA 9ffc2705; GR_SHA 1f99235a;
DA_SHA bd1aa7f3; DSA_SHA 307814e9; DSW_SHA 66526018; W9 ANCHORS
(r347/r348 verbatim) d1p 0.999154 rel 1e-5 / d2p 0.998710 rel 1e-5
/ cp 8.7234e-4 rel 1e-3 / ppr 8.4606e-4 rel 1e-3 / qpr 1.2903e-3
rel 1e-3 / m2p 1.6800e-4 rel 1e-3 / bridge 1.0029 abs 5e-3 / cpc
2.7047e-2 rel 1e-3 / g21 7.517 abs 0.01 / kres 2 / lam_rest
0.996338 abs 1e-5 / rho0 0.5787 abs 5e-3 / y0 0.42134 abs 5e-3 /
rho_hi 0.39430 abs 5e-3 / pm 5.0506 abs 0.05 / qm 7.7025 abs 0.05
/ cm 5.2075 abs 0.05; K9 ANCHORS (this round's scoping, s1) v1
4.023172e-6 rel 1e-3 / v2 4.360131e-6 rel 1e-3 / K11 2.411068e5
rel 1e-3 / K22 2.213016e5 rel 1e-3 / K12 7.700682e3 rel 1e-3 /
kap 3.333744e-2 rel 1e-3 / ds 0.0574 abs 5e-3 / vp1 4.023992e-6
rel 1e-3 / vp2 4.364056e-6 rel 1e-3; SAMPLE ANCHORS kap (kz44
1.746679e-2, kz56 1.547570e-2, kz130 8.014410e-3) rel 1e-3, ds
(0.1105, 0.1081, 0.0559) abs 5e-3; MED ANCHORS rho0 0.839 abs
0.01 / bridge 1.0058 abs 5e-3; FIT ANCH (margin -3.332, c -0.697,
p -0.754, q -0.645, cpc -2.668, y0 -0.401, rhi -0.393) tol 0.02;
CURV ANCH (margin -0.347, c +0.308, cpc -0.189, y0 +0.094, rhi
+0.080) tol 0.03; ALPHA_REC 3.332 + DELTA_REC 2.668 (WITHHELD
record constants -- gates and m2/m5 only); P_BIND 0.38; EXT3_KZ_B
(42, 51, 54, 56, 58, 62); EXT3_KZ_A (96, 123, 125, 127, 128,
130); EXT3_NW (1721, 2577); EXT4_KZ_B (72, 75, 66); EXT4_KZ_A
(113, 111, 108); EXT4_NW (2656, 3181) (r343 sealed selections
AS-IS); CD_KZ (18, 9, 52, 119, 42, 130) (= the r342 sample);
CD_BAR 1e-6; SPLIT_BAR 1e-9; CPC_ID_BAR 1e-6; ID_BAR 1e-8;
RDEF_BAR 1e-12; BOOK_BAR 0.1; DICT_SLOPE_BAR 0.02; CAND_BAR 0.05;
BAL_BAR 0.1; COMP_BAR 0.1; BRIDGE_DEV_BAR 0.10; DSHADOW_MED_BAR
0.35; DEC_CURV_BAR 0.35; DEC_EXT3_MIN 10; DEC_EXT4_LOW 1;
DELTA0_MIN 0.3; TWINLAW_BAR 0.05; CAND_P (3/4, 0.76, 0.62, 1.0);
CAND_Q (2/3, 3/4, 0.76, 0.62, 1.0); CAND_GK12 (1/2, 0.62, 0.38,
1.0, 2.0); CAND_KAP (1/2, 2/3, 3/4, 1.0); CAND_D0 (0.38, 2/5);
REL2_FACTOR 1/4 (REL1 factor 1/2); TOY BALANCE FAMILY (N0 200,
ratio 1.25, 20 points; SV1 -1.2, SV2 -1.0, SK12 +0.55, SCPC -2.7
=> planted alpha 3.25); PIN MODELS ((9/10, 3/10, 3/5, 4/5) r348
verbatim, (4/5, 2/5, 5/13, 12/13) fresh); MIRROR_LIVE_BAR 0.5
(via the r347 rule verbatim, MIRROR_K 32 in DA); KINT RECORDS
{EPST 1793.99, SCR 8.51e6, SMOOTH 2.193, HL2 1964} rel 0.05, live
0.999567 rel 1e-3; CTRL_FLIPS {EPST 25, SCR 21, SMOOTH 27}, HL2
seed 101 flip 25; EXT 8; TWIN_TOL 1e-8; TWIN_BAR 1e-3; M1_BAR
0.1; M4_MIN 0.5; MUT_MIN 1e-6; TOY_TOL 1e-12; runtime <= 1800 s;
smoke = toys + firewall + scopes + mutants + w9 block (records,
dressed anchors, kernel columns + CD route live, det shadow +
dictionary live); ladder, twin, fits, bookkeeping, candidates,
balance, pinning census, y0 laws, worlds and adjudication skipped.

PRE-SPEC SCOPING (disclosed, r343-s1..r348-s1 precedent -- no bar,
band, threshold, family, candidate list or adjudication rule was
tuned after any evaluation except as sized here and said so): (s1)
FOUR sample rungs (kz9, kz44, kz56, kz130 -- all four already
printed in the r342..r348 records) were probed end-to-end for
machinery and bar sizing: kernel columns at w9 (v1 4.023172e-6, v2
4.360131e-6, K11 2.411068e5, K22 2.213016e5, K12 7.700682e3, kappa
3.333744e-2), CD-route devs 1.7e-13 (w9) / 5.2e-13 (kz44) /
1.8e-11 (kz56) / 1.2e-10 (kz130) (sizing CD_BAR 1e-6), det-shadow
devs 0.0574 / 0.1105 / 0.1081 / 0.0559 (sizing DSHADOW_MED_BAR
0.35 with heavy-tail headroom -- the r348 weak rungs reach shadow
4.06), v_pred at the pair 4.023992e-6 / 4.364056e-6 at w9 (the
r342 dictionary, dev 2e-4 / 9e-4); (s2) the NAIVE TWO-POINT slopes
w9 -> kz130 (disclosed expectation-setting, sealed-fit previews of
NOTHING): v1 -1.1848, v2 -1.0328, K11 +1.1939, K22 +1.0425, K12
+0.5782, kappa -0.5401 -- the cross-kernel grows at roughly HALF
the diagonal geometric-mean rate (ratio 0.5782/1.1182 = 0.517),
which sized the inclusion of the 1/2-family candidates and REL2
(factor 1/4), said so; the diagonal growths track the reciprocal
weights (Christoffel saturation, the BK2 expectation); (s3) the
second rational pinning model (4/5, 2/5, 5/13, 12/13) was solved
in exact arithmetic at scoping time: p' 457/845, q' 219/845, c'
24/169, m2' == margin == 1/5 EXACT, p'q' - c'^2 == m^2 g21/Dt^2
== 3/25 EXACT -- the toy gates are exact-value gates.  Runtime
scoping 3.2 s for the four rungs; the full run is sized under the
1800 s bar (the r348 ladder pass with the same extend_rung is
63 s).  No ladder-wide fit, median, protocol clause, candidate
adjudication or law was evaluated before this spec froze; the
slopes cited above are published record numbers or the disclosed
two-point scoping values, never sealed-fit previews.

SEALED VERDICT FORM (frozen BEFORE evaluation, joined with '+';
precedence TARGET_LEAK > ALPHA_SOURCE_CLOSED > KERNEL_LAW_FOUND >
KERNEL_IRREDUCIBLE -- the enum is exhaustive):
  [exactly one of]
  TARGET_LEAK(loci)  iff any firewall/scope/fragment audit fails /
  ALPHA_SOURCE_CLOSED(blocks typed)  iff KERNEL_LAW fires for BOTH
    a_p and a_q AND PINNING_THEOREM fires AND the candidate
    composition |alpha_cand - alpha_meas| <= 0.1 AND the balance
    |alpha_bal - alpha_meas| <= 0.1 -- the margin exponent closes
    source-side at CANDIDATE-LAW grade with the single census
    member rho_r; honesty: candidate-law grade is a sealed-hit +
    clause-clean statement on 57 finite windows, NEVER an
    asymptotic derivation /
  KERNEL_LAW_FOUND(loci)  iff a sealed candidate (list or REL2)
    hits at CAND_BAR on a column whose decay clauses are clean
    (|curv| <= 0.35, EXT3 >= 10/12, EXT4 n_low <= 1) for at least
    one of {a_p, a_q, g_K12, a_kappa} -- the kernel side carries a
    candidate law, ambiguity count printed /
  KERNEL_IRREDUCIBLE(steckbrief)  otherwise -- the kernel side
    resists; the final specialist question is exact: the CD-kernel
    growth at discrete arch-rim atoms, with every measured
    exponent as the profile
  + [exactly one of] PINNING_THEOREM(clauses) / PINNING_CENSUS
    (loci)  [always -- Leg C: theorem iff both Fractions models
    exact AND bridge max <= 0.10 AND det-shadow median <= 0.35 AND
    the r345 CURV_FLAT re-gates on all three pinning ratios]
  + [exactly one of] DELTA0_CANDIDATE(hits) / DELTA0_CENSUS
    [always -- Leg D: candidate iff the y0 law re-gates (ORDER0
    clauses verbatim) AND a sealed delta_0 candidate hits AND the
    twin ward |s_y0 - s_rhi| <= 0.05 holds]
  + BOOKKEEPING_LEDGER(BK1/BK2/BK3 residuals, dict slope devs)
    [always]
  + BALANCE_LEDGER(alpha_bal, alpha_comp, alpha_cand, every block
    typed satz/woerterbuch/zensus/fit) [always]
  + KERNEL_LEDGER(all kernel slopes + curvatures + bands, CD ward,
    candidate hit table) [always]
  + WORLD_LEDGER(lambda_rest separation, kappa_int anchors, y0
    column, the twin-pair existence typing) [always]
  + TWIN_LEDGER(kernel-column deviations) [always]
  + MUSTFAIL_LEDGER(m1-m5 + scopes) [always].
Honesty before beauty: the CD decomposition and the pinning
identities are exact finite-matrix facts (theorem-grade SKELETON)
whose inputs are measured window scalars (census-grade FLESH); a
candidate hit at 0.05 on a 57-window ladder is a CANDIDATE, never
an identification -- the sealed lists contain neighbours closer
than the bar (3/4 vs 2 x 0.38; 2/3 vs 1 - 0.38) and the ambiguity
is printed, not resolved; the slope bookkeeping closes up to
Theil-Sen non-additivity, it does not derive any exponent; the
pinning theorem lives EXACTLY on the rank-2 models where top-2
dominance and flat geometry are hypotheses, and LIVE only as
sealed census; alpha, a_p, a_q, delta_0 remain measured laws; a
verdict ALPHA_SOURCE_CLOSED types the chain {dictionary weights
(woerterbuch), kernel candidate laws (sealed-hit census), pinning
(theorem-image), rho_r (the one census member)} and does NOT claim
an asymptotic source derivation; the mirror world clause is a
measured discriminator on eight instrumented worlds; no verdict
claims L*, a bound mechanism, a derived 5/7, or RH progress in
any direction.

NO RH CLAIM IN EITHER DIRECTION.  NOT evidence for or against RH.
"""

from __future__ import annotations

import argparse
import ast
import hashlib
import math
import os
import sys
import time
from fractions import Fraction as Fr

import numpy as np

HERE = os.path.dirname(os.path.abspath(__file__))
if HERE not in sys.path:
    sys.path.insert(0, HERE)
_PROB = os.path.abspath(os.path.join(HERE, "..", "..", "rh", "problem"))
if _PROB not in sys.path:
    sys.path.insert(0, _PROB)

import pair_extremal_probe as PX                 # noqa: E402 r342
import pair_coupling_probe as PC3                # noqa: E402 r343
import gap_ratio_primary_probe as GR             # noqa: E402 r345
import delta_alpha_closure_probe as DA           # noqa: E402 r347
import delta_source_anatomy_probe as DSA         # noqa: E402 r348
import dirichlet_secondworld_probe as DSW        # noqa: E402 r330
import verify_lstar_instance as V                # noqa: E402 document
import lstar_margin_scaling_probe as LM          # noqa: E402 r286
import fold_capacity_probe as FC                 # noqa: E402 r334
import metric_stability_probe as MS              # noqa: E402 r278
import budget_localization_probe as BL           # noqa: E402 r280
import port_integrable_kernel_probe as PIK       # noqa: E402 v881
import principal_bessel_probe as PB              # noqa: E402 r243
import paircorr_margin_probe as PC               # noqa: E402
import twin_resolution_probe as TR               # noqa: E402 r331
import arch_kernel_diophantine_probe as AKD      # noqa: E402 r289
import minimal_firewall_probe as MF              # noqa: E402 r276
import v563_paper2_readouts as core              # noqa: E402 READ-ONLY

MAIN_KZ = 9
REC_S, REC_SM, REC_NW = 367, 104, 184
REC_LAM = 0.99983248
REC_LAM_NEXT = 1.00003660
REC_MARGIN = 1.6752e-4
REC_MARGIN_TOL = 0.01
PX_SHA_PREFIX = "b09f8ccd"
PC3_SHA_PREFIX = "9ffc2705"
GR_SHA_PREFIX = "1f99235a"
DA_SHA_PREFIX = "bd1aa7f3"
DSA_SHA_PREFIX = "307814e9"
DSW_SHA_PREFIX = "66526018"
W9_ANCH = dict(d1p=0.999154, d2p=0.998710, cp=8.7234e-4,
               ppr=8.4606e-4, qpr=1.2903e-3, m2p=1.6800e-4,
               bridge=1.0029, cpc=2.7047e-2, g21=7.517, kres=2,
               lam_rest=0.996338, rho0=0.5787, y0=0.42134,
               rho_hi=0.39430, pm=5.0506, qm=7.7025, cm=5.2075)
K9_ANCH = dict(v1=4.023172e-6, v2=4.360131e-6, K11=2.411068e5,
               K22=2.213016e5, K12=7.700682e3, kap=3.333744e-2,
               ds=0.0574, vp1=4.023992e-6, vp2=4.364056e-6)
SAMPLE_ANCH = {44: dict(kap=1.746679e-2, ds=0.1105),
               56: dict(kap=1.547570e-2, ds=0.1081),
               130: dict(kap=8.014410e-3, ds=0.0559)}
MED_ANCH = dict(rho0=0.839, bridge=1.0058)
FIT_ANCH = dict(margin=-3.332, c=-0.697, p=-0.754, q=-0.645,
                cpc=-2.668, y0=-0.401, rhi=-0.393)
FIT_ANCH_TOL = 0.02
CURV_ANCH = dict(margin=-0.347, c=0.308, cpc=-0.189, y0=0.094,
                 rhi=0.080)
CURV_ANCH_TOL = 0.03
ALPHA_REC = 3.332            # WITHHELD record constant
DELTA_REC = 2.668            # WITHHELD record constant
P_BIND = 0.38
EXT3_KZ_B = (42, 51, 54, 56, 58, 62)
EXT3_KZ_A = (96, 123, 125, 127, 128, 130)
EXT3_NW_MIN, EXT3_NW_MAX = 1721, 2577
EXT4_KZ_B = (72, 75, 66)
EXT4_KZ_A = (113, 111, 108)
EXT4_NW_MIN, EXT4_NW_MAX = 2656, 3181
CD_KZ = (18, 9, 52, 119, 42, 130)
CD_BAR = 1.0e-6
SPLIT_BAR = 1.0e-9
CPC_ID_BAR = 1.0e-6
ID_BAR = 1.0e-8
RDEF_BAR = 1.0e-12
BOOK_BAR = 0.1
DICT_SLOPE_BAR = 0.02
CAND_BAR = 0.05
BAL_BAR = 0.1
COMP_BAR = 0.1
BRIDGE_DEV_BAR = 0.10
DSHADOW_MED_BAR = 0.35
DEC_CURV_BAR = 0.35
DEC_EXT3_MIN = 10
DEC_EXT4_LOW = 1
DELTA0_MIN = 0.3
TWINLAW_BAR = 0.05
CAND_P = (0.75, 0.76, 0.62, 1.0)
CAND_Q = (2.0 / 3.0, 0.75, 0.76, 0.62, 1.0)
CAND_GK12 = (0.5, 0.62, 0.38, 1.0, 2.0)
CAND_KAP = (0.5, 2.0 / 3.0, 0.75, 1.0)
CAND_D0 = (0.38, 0.4)
REL1_FACTOR = 0.5
REL2_FACTOR = 0.25
TOY_N0 = 200.0
TOY_RATIO = 1.25
TOY_NPTS = 20
TOY_SV1 = -1.2
TOY_SV2 = -1.0
TOY_SK12 = 0.55
TOY_SCPC = -2.7
MIRROR_LIVE_BAR = 0.5
KINT_REC = {"EPST": 1793.99, "SCR": 8.51e6, "SMOOTH": 2.193,
            "HL2": 1964.0}
KINT_REC_TOL = 0.05
KINT_LIVE_REC = 0.999567
KINT_LIVE_TOL = 1.0e-3
CTRL_FLIPS = {"EPST": 25, "SCR": 21, "SMOOTH": 27}
HL2_SEED = 101
HL2_FLIP = 25
EXT = 8
TWIN_TOL = 1.0e-8
TWIN_BAR = 1.0e-3
M1_BAR = 0.1
M4_MIN = 0.5
MUT_MIN = 1.0e-6
TOY_TOL = 1.0e-12

SPEC_SHA = hashlib.sha256(__doc__.encode("utf-8")).hexdigest()
T0_WALL = time.time()
CHECKS: list = []


def check(name, ok, detail):
    ok = bool(ok)
    CHECKS.append((name, ok, detail))
    print("  [%s] %-42s %s" % ("PASS" if ok else "FAIL", name, detail),
          flush=True)
    return ok


def info(msg):
    print("  [INFO] " + msg, flush=True)


def section(t):
    print("\n" + "-" * 78 + "\n" + t + "\n" + "-" * 78, flush=True)


def firewall_audit():
    src = open(os.path.abspath(__file__), "r", encoding="utf-8").read()
    tree = ast.parse(src)
    forb = {"zeta" + "zero", "n" + "zeros", "prime" + "range",
            "is" + "prime", "gram" + "point"}
    bad = []
    for node in ast.walk(tree):
        nm = node.attr if isinstance(node, ast.Attribute) else (
            node.id if isinstance(node, ast.Name) else None)
        if nm and nm.lower() in forb:
            bad.append("%s@%d" % (nm, node.lineno))
    return (not bad), ("NO zero/prime oracles; the module-own "
                       "constructors consume kernel Gram / spectrum "
                       "/ weight / position / chain-coefficient "
                       "arrays and measured columns ONLY; record "
                       "numbers and flips enter gates and record "
                       "tables only" if not bad else "; ".join(bad))


def antigate_fragment_audit():
    frags = ("poly" + "fit", "curve_" + "fit", "lst" + "sq",
             "mini" + "mize", "Line" + "arRegression")
    src = open(os.path.abspath(__file__), "r", encoding="utf-8").read()
    tree = ast.parse(src)
    hits = []
    for node in ast.walk(tree):
        nm = None
        if isinstance(node, ast.Attribute):
            nm = node.attr
        elif isinstance(node, ast.Name):
            nm = node.id
        elif isinstance(node, ast.FunctionDef):
            nm = node.name
        if nm and any(f in nm for f in frags):
            hits.append("%s@%d" % (nm, node.lineno))
    return hits


CONSTRUCTORS = ("kernel_cols", "cd_kernel_route", "det_shadow",
                "slope_balance", "cand_hits")
SCOPE_FORBIDDEN = {"REC_LAM", "REC_LAM_NEXT", "REC_MARGIN",
                   "CTRL_FLIPS", "KINT_REC", "FIT_ANCH",
                   "CURV_ANCH", "W9_ANCH", "K9_ANCH", "MED_ANCH",
                   "SAMPLE_ANCH", "ALPHA_REC", "DELTA_REC",
                   "slope_col_true"}


def scope_audit(funcname):
    src = open(os.path.abspath(__file__), "r", encoding="utf-8").read()
    tree = ast.parse(src)
    hits = []
    for node in ast.walk(tree):
        if isinstance(node, ast.FunctionDef) and node.name == funcname:
            for sub in ast.walk(node):
                nm = None
                if isinstance(sub, ast.Attribute):
                    nm = sub.attr
                elif isinstance(sub, ast.Name):
                    nm = sub.id
                elif isinstance(sub, ast.Constant) \
                        and isinstance(sub.value, str):
                    nm = sub.value
                if nm in SCOPE_FORBIDDEN:
                    hits.append("%s@%d" % (nm, sub.lineno))
    return hits


# ============== module-own constructors (AST-audited)
def kernel_cols(d1, d2, c, v1, v2):
    """the kernel columns from the exact pair identities d_k =
    v_k K_N(y_k, y_k), c = sqrt(v1 v2) K_N(y1, y2): K11, K22, K12,
    kappa = K12/sqrt(K11 K22); consumes pair scalars + weights
    only."""
    K11 = d1 / v1
    K22 = d2 / v2
    K12 = c / math.sqrt(v1 * v2)
    return K11, K22, K12, K12 / math.sqrt(K11 * K22)


def cd_kernel_route(aa, bb, h0, y1, y2, Nw):
    """the INDEPENDENT Christoffel-Darboux route: K_N at the pair
    atoms from the mu-chain recursion coefficients (a_n, b_n) via
    the CD endpoint formula K_N(x, y) = b_{N-1}(p_N(x) p_{N-1}(y) -
    p_{N-1}(x) p_N(y))/(x - y) and its confluent form; the p_n and
    p_n' evaluated by the three-term recursion; consumes chain
    coefficients + positions only."""
    def pvals(y):
        p = np.zeros(Nw + 1)
        dp = np.zeros(Nw + 1)
        p[0] = 1.0 / math.sqrt(h0)
        pm, dm = 0.0, 0.0
        for i in range(Nw):
            pn = ((y - aa[i]) * p[i]
                  - (bb[i - 1] * pm if i > 0 else 0.0)) / bb[i]
            dn = (p[i] + (y - aa[i]) * dp[i]
                  - (bb[i - 1] * dm if i > 0 else 0.0)) / bb[i]
            pm, dm = p[i], dp[i]
            p[i + 1], dp[i + 1] = pn, dn
        return p, dp

    p1, d1v = pvals(y1)
    p2, d2v = pvals(y2)
    bN = bb[Nw - 1]
    K12 = bN * (p1[Nw] * p2[Nw - 1] - p1[Nw - 1] * p2[Nw]) \
        / (y1 - y2)
    K11 = bN * (d1v[Nw] * p1[Nw - 1] - d1v[Nw - 1] * p1[Nw])
    K22 = bN * (d2v[Nw] * p2[Nw - 1] - d2v[Nw - 1] * p2[Nw])
    return K11, K22, K12


def det_shadow(margin, g21, a1, a2, b1, b2, ppr, qpr, cpr):
    """the LIVE pinning identity: dev of the true dressed
    determinant p'q' - c'^2 against the two-level prediction
    margin^2 g21 / Dt^2 (exact on the truncation, Leg C (ii));
    consumes spectrum-derived scalars + dressed deficits only."""
    Dt = a1 * b2 - a2 * b1
    rhs = margin * margin * g21 / (Dt * Dt)
    lhs = ppr * qpr - cpr * cpr
    return abs(lhs / rhs - 1.0)


def slope_balance(s_vp1, s_vp2, s_k12, s_cpc):
    """ROUTE A, the direct alpha balance: alpha_bal = -(1/2(s_vp1 +
    s_vp2) + s_K12 + s_cpc) -- dictionary weight slopes + kernel
    cross slope + pinning cancellation slope; consumes fitted
    slopes only (anti-circular: no margin column enters)."""
    return -(0.5 * (s_vp1 + s_vp2) + s_k12 + s_cpc)


def cand_hits(target, cands, bar):
    """the sealed candidate adjudicator: list of candidates within
    the bar of the measured target exponent; consumes a measured
    scalar + the frozen list only."""
    return [c for c in cands if abs(target - c) <= bar]


# ============== must-fail mutants
def mutant_cd_unnormalized(aa, bb, h0, y1, y2, Nw):
    """m1 MUST-FAIL: the CD endpoint formula WITHOUT the b_{N-1}
    prefactor -- must break the kernel ward loudly."""
    K11, K22, K12 = cd_kernel_route(aa, bb, h0, y1, y2, Nw)
    return K12 / bb[Nw - 1]


def mutant_klaw_readback():
    """m2 MUST-FAIL: 'the kernel slope' read back from the withheld
    record exponents -- AST-FLAGGED."""
    return ALPHA_REC - DELTA_REC


def mutant_cand_posthoc(slope_col_true):
    """m3 MUST-FAIL: the candidate re-picked AFTER SIGHT as the
    nearest twelfth of the seen slope -- consumes the withheld
    column; AST-FLAGGED."""
    s = sorted(slope_col_true)[len(slope_col_true) // 2]
    return round(abs(s) * 12.0) / 12.0


def mutant_balance_doublecount(s_v1, s_v2, s_k12, s_cpc):
    """m4 MUST-FAIL: the balance with the DOUBLE-COUNTED weight
    share (s_v1 + s_v2 instead of the half) -- must break the exact
    toy balance by |s_v1 + s_v2|/2."""
    return -((s_v1 + s_v2) + s_k12 + s_cpc)


def mutant_pin_plant():
    """m5 MUST-FAIL: a synthetic pinning family planting the
    withheld record exponent as its own -- AST-FLAGGED; the sealed
    toy balance exponent differs by construction."""
    return DELTA_REC + 0.664


# ============== gate-side helpers
def rung_kernel_extras(R, X, C, Y):
    """the r350 extension of one (r342, r343, r347, r348) bundle:
    kernel columns, det shadow, dictionary weights at the pair."""
    K11, K22, K12, kap = kernel_cols(R["d1"], R["d2"], R["c"],
                                     R["v1"], R["v2"])
    ds = det_shadow(R["margin"], X["g21"], Y["a1"], Y["a2"],
                    Y["b1"], Y["b2"], C["ppr"], C["qpr"], C["cpr"])
    alpha_, M_, L_, _Nw, D_ = V.window_shape(R["kz"])
    ka_ = int(np.searchsorted(V.U, 2.0 * alpha_ + 1e-14,
                              side="right"))
    uu_ = np.asarray(V.U[:ka_], float)
    mm_ = np.asarray(V.W_VM[:ka_], float)
    vps = []
    for ff in (R["f1"], R["f2"]):
        th = 2.0 * math.pi * ff / L_
        vp, _a, _p = PX.v_predict(th, alpha_, M_, L_, D_, uu_, mm_)
        vps.append(vp)
    return dict(K11=K11, K22=K22, K12=K12, kap=kap, ds=ds,
                vp1=vps[0], vp2=vps[1])


def cd_ward_dev(R):
    """gate-side CD ward for one rung: mu-chain recomputed
    (document route verbatim), the CD route vs the Gram-route
    kernel columns."""
    mz = R["mz"]
    aa, bb, h0 = V.mu_chain(mz["xp"], mz["wp"], mz["Nw"])
    y1 = float(mz["yn"][R["i1"]])
    y2 = float(mz["yn"][R["i2"]])
    K11c, K22c, K12c = cd_kernel_route(aa, bb, h0, y1, y2,
                                       mz["Nw"])
    K11, K22, K12, _k = kernel_cols(R["d1"], R["d2"], R["c"],
                                    R["v1"], R["v2"])
    return max(abs(K11c / K11 - 1.0), abs(K22c / K22 - 1.0),
               abs(K12c / K12 - 1.0)), (aa, bb, h0, y1, y2)


def pin_model_fractions(lam1, lam2, fa1, fa2, fb1, fb2):
    """one exact rational rank-2 pinning model: returns (p', q',
    c', margin, g21, Dt, ok) with ok = the three pinning identities
    EXACT in Fractions: (i) the dressed-2x2 deficits are (1-lam1),
    (1-lam2) (root identity: sum + product), (ii) p'q' - c'^2 ==
    m^2 g21/Dt^2, (iii) c' == m G."""
    mu1, mu2 = 1 / (1 - lam1), 1 / (1 - lam2)
    M11 = mu1 * fa1 * fa1 + mu2 * fb1 * fb1
    M22 = mu1 * fa2 * fa2 + mu2 * fb2 * fb2
    M12 = mu1 * fa1 * fa2 + mu2 * fb1 * fb2
    detM = M11 * M22 - M12 * M12
    pp, qq, cc = M22 / detM, M11 / detM, M12 / detM
    m = 1 - lam1
    g21 = (1 - lam2) / (1 - lam1)
    Dt = fa1 * fb2 - fa2 * fb1
    ok_roots = (pp + qq == (1 - lam1) + (1 - lam2)
                and pp * qq - cc * cc == (1 - lam1) * (1 - lam2))
    ok_det = (pp * qq - cc * cc == m * m * g21 / (Dt * Dt))
    G = (g21 * fa1 * fa2 + fb1 * fb2) / (Dt * Dt)
    ok_geom = (cc == m * G)
    return pp, qq, cc, m, g21, Dt, (ok_roots and ok_det
                                    and ok_geom)


def slim350(R, X, C, Y, K):
    """memory hygiene: drop the large matrices after the per-rung
    extras are computed (kz9 keeps its full bundle)."""
    keep_R = {k: R[k] for k in ("kz", "z", "Nw", "Sm", "margin",
                                "c", "p", "q", "d1", "d2", "v1",
                                "v2", "cp", "rdet", "lam_rest",
                                "det_dev", "schur_dev")}
    keep_X = {k: X[k] for k in ("g21", "k_res", "dc", "dev_res",
                                "dev_sp")}
    keep_Y = {k: Y[k] for k in ("rho0", "rho_hi", "y0",
                                "dev_split", "dev_cpc", "sgn",
                                "pm", "qm", "cm")}
    return keep_R, keep_X, keep_Y, K


# --------------------------------------------------------------- main
def main():
    par_ = argparse.ArgumentParser()
    par_.add_argument("--smoke", action="store_true")
    args = par_.parse_args()
    smoke = args.smoke

    print("=" * 78)
    print("alpha_source_anatomy_probe -- PRIME.LSTAR."
          "ALPHA_SOURCE_ANATOMY.01 (round 350)")
    print("SPEC_SHA %s   (r342 PX %s / r343 PC3 %s / r345 GR %s / "
          "r347 DA %s / r348 DSA %s / r330 DSW %s)"
          % (SPEC_SHA[:16], PX.SPEC_SHA[:16], PC3.SPEC_SHA[:16],
             GR.SPEC_SHA[:16], DA.SPEC_SHA[:16], DSA.SPEC_SHA[:16],
             DSW.SPEC_SHA[:16]))
    print("mode: %s" % ("SMOKE (toys + firewall + scopes + mutants "
                        "+ w9 block; ladder, twin, fits, "
                        "bookkeeping, candidates, balance, pinning "
                        "census, y0 laws, worlds, adjudication "
                        "skipped)" if smoke else "FULL RECORD"))
    print("=" * 78)

    section("S0  FIREWALL + PREDEFINITION")
    okf, det = firewall_audit()
    check("G01-firewall", okf, det)
    ok_sha = (PX.SPEC_SHA.startswith(PX_SHA_PREFIX)
              and PC3.SPEC_SHA.startswith(PC3_SHA_PREFIX)
              and GR.SPEC_SHA.startswith(GR_SHA_PREFIX)
              and DA.SPEC_SHA.startswith(DA_SHA_PREFIX)
              and DSA.SPEC_SHA.startswith(DSA_SHA_PREFIX)
              and DSW.SPEC_SHA.startswith(DSW_SHA_PREFIX))
    check("G02-predefinition", ok_sha,
          "sealed BEFORE evaluation: r342/r343/r345/r347/r348/r330 "
          "machinery imported verbatim (SPEC_SHA %s == %s*, %s == "
          "%s*, %s == %s*, %s == %s*, %s == %s*, %s == %s*), the "
          "kernel columns with their CD-route ward (bar %.0e), the "
          "det shadow, the slope bookkeeping (bar %.1f), the "
          "dictionary slope clause (bar %.2f), the FROZEN candidate "
          "lists (bar %.2f: P %s, Q %s, GK12 %s, KAP %s, D0 %s; "
          "REL factors %.2f/%.2f), the balance and composition "
          "closures (bars %.1f/%.1f), the pinning clauses (bridge "
          "%.2f, det-shadow med %.2f, two Fractions models), the "
          "y0 re-gates, every bar/tolerance, the mutants and the "
          "verdict form; pre-spec scoping s1-s3 disclosed in the "
          "spec (four printed sample rungs + two-point slopes + "
          "the second rational model); the STOP list forbids any "
          "L* claim and any certificate reading beyond the sealed "
          "census"
          % (PX.SPEC_SHA[:8], PX_SHA_PREFIX, PC3.SPEC_SHA[:8],
             PC3_SHA_PREFIX, GR.SPEC_SHA[:8], GR_SHA_PREFIX,
             DA.SPEC_SHA[:8], DA_SHA_PREFIX, DSA.SPEC_SHA[:8],
             DSA_SHA_PREFIX, DSW.SPEC_SHA[:8], DSW_SHA_PREFIX,
             CD_BAR, BOOK_BAR, DICT_SLOPE_BAR, CAND_BAR,
             str(CAND_P), str(CAND_Q), str(CAND_GK12),
             str(CAND_KAP), str(CAND_D0), REL1_FACTOR, REL2_FACTOR,
             BAL_BAR, COMP_BAR, BRIDGE_DEV_BAR, DSHADOW_MED_BAR))
    hits = []
    for fn_ in CONSTRUCTORS:
        hits += scope_audit(fn_)
    ag_hits = antigate_fragment_audit()
    hits_m2 = scope_audit("mutant_klaw_readback")
    hits_m3 = scope_audit("mutant_cand_posthoc")
    hits_m5 = scope_audit("mutant_pin_plant")
    check("G03-scope-audits", not hits and not ag_hits
          and bool(hits_m2) and bool(hits_m3) and bool(hits_m5),
          "the %d module-own constructors consume kernel Gram / "
          "spectrum / weight / position / chain-coefficient arrays "
          "and measured columns ONLY (%s); fragment audit (no fit "
          "primitives beyond the imported r286 Theil-Sen + r345 "
          "protocol + r347 decay instrument): %s; m2 FLAGGED (%s); "
          "m3 FLAGGED (%s); m5 FLAGGED (%s)"
          % (len(CONSTRUCTORS),
             "CLEAN" if not hits else "; ".join(hits),
             "CLEAN" if not ag_hits else "; ".join(ag_hits),
             hits_m2[0] if hits_m2 else "MISS",
             hits_m3[0] if hits_m3 else "MISS",
             hits_m5[0] if hits_m5 else "MISS"))

    # ---------------- S1 toys
    section("S1  TOYS -- CD IDENTITY + PINNING FRACTIONS + BALANCE "
            "FAMILY + INSTRUMENTS")
    # (T1) CD identity on a synthetic 5-atom measure
    x_t = np.array([-0.8, -0.3, 0.2, 0.5, 0.9])
    w_t = np.array([0.2, 0.3, 0.1, 0.25, 0.15])
    dep_t = 4
    aa_t, bb_t, h0_t = V.mu_chain(x_t, w_t, dep_t)
    y1_t, y2_t = 0.7, -0.6
    B_t1 = V.b_matrix(aa_t, bb_t, h0_t, np.array([y1_t, y2_t]),
                      np.array([1.0, 1.0]), dep_t)
    K12_dir = float(B_t1[0] @ B_t1[1])
    K11_dir = float(B_t1[0] @ B_t1[0])
    K22_dir = float(B_t1[1] @ B_t1[1])
    K11_cd, K22_cd, K12_cd = cd_kernel_route(aa_t, bb_t, h0_t,
                                             y1_t, y2_t, dep_t)
    dev_t1 = max(abs(K11_cd / K11_dir - 1.0),
                 abs(K22_cd / K22_dir - 1.0),
                 abs(K12_cd / K12_dir - 1.0))
    check("G10-toy-cd-identity", dev_t1 <= TOY_TOL * 10.0,
          "THE CD IDENTITY on a synthetic 5-atom measure (depth "
          "%d): the endpoint formula b_{N-1}(p_N(x) p_{N-1}(y) - "
          "p_{N-1}(x) p_N(y))/(x-y) and its confluent form == the "
          "direct sum sum_i p_i(x) p_i(y), max rel dev %.1e -- the "
          "kernel is source-computable from the recursion "
          "coefficients, exactly" % (dep_t, dev_t1))
    # (T2) pinning Fractions models
    ppA, qqA, ccA, mA, gA, DtA, okA = pin_model_fractions(
        Fr(9, 10), Fr(3, 10), Fr(3, 5), Fr(4, 5), Fr(-4, 5),
        Fr(3, 5))
    ppB, qqB, ccB, mB, gB, DtB, okB = pin_model_fractions(
        Fr(4, 5), Fr(2, 5), Fr(5, 13), Fr(12, 13), Fr(-12, 13),
        Fr(5, 13))
    ok_vals = (ppA == Fr(121, 250) and qqA == Fr(79, 250)
               and ccA == Fr(72, 250) and ppB == Fr(457, 845)
               and qqB == Fr(219, 845) and ccB == Fr(24, 169)
               and ppA * qqA - ccA * ccA == Fr(7, 100)
               and ppB * qqB - ccB * ccB == Fr(3, 25))
    pp2f, qq2f, cc2f = DSA.two_level_dressed(
        float(mA), float(gA), 3.0 / 5.0, 4.0 / 5.0, -4.0 / 5.0,
        3.0 / 5.0)
    dev_f64 = max(abs(pp2f - float(ppA)), abs(qq2f - float(qqA)),
                  abs(cc2f - float(ccA)))
    check("G11-toy-pinning-fractions", okA and okB and ok_vals
          and dev_f64 <= TOY_TOL,
          "THE PINNING IDENTITIES EXACT IN FRACTIONS on TWO rank-2 "
          "models: (i) the dressed-2x2 deficits ARE (1-lam1), "
          "(1-lam2) (root sum + product exact) -- m2'_2 == margin "
          "IDENTICALLY, the bridge is 1 in the truncation; (ii) "
          "p'q' - c'^2 == m^2 g21/Dt^2 (model A 7/100, model B "
          "3/25); (iii) c' == m G exactly -- Delta c == -c + "
          "margin x G: THE NEAR-CANCELLATION IS ALGEBRAIC "
          "SELF-CONSISTENCY; the r348 constructor matches at %.1e "
          "-- corollary: slope(y0 - rho_hi) = -(alpha - a_c) is "
          "FORCED by (ii)" % dev_f64)
    # (T3) balance family: exact planted powers
    Nt = TOY_N0 * TOY_RATIO ** np.arange(TOY_NPTS)
    lnNt = np.log(Nt)
    v1_t = 3e-6 * (Nt / TOY_N0) ** TOY_SV1
    v2_t = 4e-6 * (Nt / TOY_N0) ** TOY_SV2
    K12_t = 8e3 * (Nt / TOY_N0) ** TOY_SK12
    cpc_t = 3e-2 * (Nt / TOY_N0) ** TOY_SCPC
    sl_t = {}
    for nm, col in (("v1", v1_t), ("v2", v2_t), ("K12", K12_t),
                    ("cpc", cpc_t)):
        sl_t[nm] = float(LM.ts_fit(lnNt, np.log(col))[1])
    a_bal_t = slope_balance(sl_t["v1"], sl_t["v2"], sl_t["K12"],
                            sl_t["cpc"])
    a_plant = -(0.5 * (TOY_SV1 + TOY_SV2) + TOY_SK12 + TOY_SCPC)
    check("G12-toy-balance-family", abs(a_bal_t - a_plant)
          <= 1e-9,
          "THE BALANCE TOY (planted s_v1 %.1f, s_v2 %.1f, s_K12 "
          "%+.2f, s_cpc %.1f): alpha_bal = %.9f == the planted "
          "alpha %.2f at 1e-9 -- the balance constructor recovers "
          "the margin exponent from dictionary + kernel + pinning "
          "slopes exactly on exact powers"
          % (TOY_SV1, TOY_SV2, TOY_SK12, TOY_SCPC, a_bal_t,
             a_plant))
    # (T4) instruments re-gate
    kk = np.arange(25)
    lnS = np.log(300.0 * 1.1 ** kk)
    scat = math.log(2.0) * np.sin(7.0 * kk)
    e3p_t = [(float(lnS[-1] + 0.3), float(np.log(0.3)
                                          - 3.0 * (lnS[-1] + 0.3)
                                          + 0.1))] * 12
    e4p_t = [(float(lnS[-1] + 0.5), float(np.log(0.3)
                                          - 3.0 * (lnS[-1] + 0.5)
                                          - 0.2))] * 6
    dl3 = DA.decay_law(lnS, np.log(0.3) - 3.0 * lnS + scat,
                       e3p_t, e4p_t)
    dl_flat = DA.decay_law(lnS, np.log(0.3) + scat, [], [])
    coh_toy = [("c%d" % j, list(range(5 * j, 5 * j + 5)))
               for j in range(5)]
    fitm_t = [True] * 25
    p_flat = GR.curvflat_protocol(lnS, np.log(0.30) + scat, fitm_t,
                                  coh_toy)
    p_dec3 = GR.curvflat_protocol(lnS, np.log(0.30) - 3.0 * lnS
                                  + scat, fitm_t, coh_toy)
    ft = LM.ts_fit(lnS, np.log(0.30) + 0.0 * lnS)
    ft_short = LM.ts_fit(np.arange(8.0), np.arange(8.0))
    check("G13-toy-instruments", abs(dl3["slope"] + 3.0) <= 0.2
          and dl3["e3_in"] == 12 and dl3["e4_low"] == 0
          and (-dl_flat["slope"]) < DELTA0_MIN
          and p_flat["ok"] and not p_dec3["ok"]
          and (not isinstance(ft[0], str)) and abs(ft[1]) <= 1e-12
          and ft_short[0] == "SHORT_LADDER",
          "INSTRUMENT RE-GATES: the r347 decay_law fits synthetic "
          "N^-3 with x2 scatter to %.3f (EXT3 12/12, EXT4 low 0) "
          "and refuses the flat column (delta %.3f < %.1f); the "
          "r345 CURV_FLAT protocol passes the flat column (slope "
          "%+.3f) and catches N^-3 (%+.3f); r286 Theil-Sen flat "
          "slope %.1e == 0, 8-point guard %s -- all three imported "
          "verbatim with module-sealed constants"
          % (dl3["slope"], -dl_flat["slope"], DELTA0_MIN,
             p_flat["slope"], p_dec3["slope"], abs(ft[1]),
             str(ft_short)))
    # (T5) candidate adjudicator toy
    h_t = cand_hits(0.752, CAND_P, CAND_BAR)
    h_none = cand_hits(0.45, CAND_P, CAND_BAR)
    check("G14-toy-candidates", h_t == [0.75, 0.76]
          and h_none == [],
          "CANDIDATE ADJUDICATOR: target 0.752 hits %s of the "
          "sealed P list (both 3/4 and 2 x %.2f -- the ambiguity "
          "the honesty section names); target 0.45 hits nothing "
          "-- the bar %.2f separates" % (str(h_t), P_BIND,
                                         CAND_BAR))

    # ---------------- S2 w9 flagship
    section("S2  W9 -- RECORDS + DRESSED ANCHORS + KERNEL COLUMNS "
            "+ CD ROUTE + DET SHADOW")
    R9 = PX.build_rung(MAIN_KZ)
    X9 = PC3.extend_rung(R9)
    C9 = DA.closure_cols(R9)
    Y9 = DSA.rung_extras(R9, X9, C9)
    K9 = rung_kernel_extras(R9, X9, C9, Y9)
    lam185, _B185 = V.lam_max_at(R9["mz"], REC_NW + 1)
    ok_rec = (R9["S"] == REC_S and R9["Sm"] == REC_SM
              and R9["Nw"] == REC_NW
              and abs(R9["lam"] - REC_LAM) <= 1e-6
              and abs(lam185 - REC_LAM_NEXT) <= 1e-6
              and abs(R9["margin"] / REC_MARGIN - 1.0)
              <= REC_MARGIN_TOL)
    check("G20-w9-records", ok_rec,
          "w9: S = %d (nu %d), N_w = %d, lambda_max = %.8f "
          "(record %.8f), margin %.4e (record %.4e), lambda at "
          "185 = %.8f > 1 -- the document route reproduced"
          % (R9["S"], R9["Sm"], R9["Nw"], R9["lam"], REC_LAM,
             R9["margin"], REC_MARGIN, lam185))
    A = W9_ANCH
    ok_anch = (abs(R9["d1p"] / A["d1p"] - 1.0) <= 1e-5
               and abs(R9["d2p"] / A["d2p"] - 1.0) <= 1e-5
               and abs(R9["cp"] / A["cp"] - 1.0) <= 1e-3
               and abs(C9["ppr"] / A["ppr"] - 1.0) <= 1e-3
               and abs(C9["qpr"] / A["qpr"] - 1.0) <= 1e-3
               and abs(C9["m2p"] / A["m2p"] - 1.0) <= 1e-3
               and abs(C9["bridge"] - A["bridge"]) <= 5e-3
               and abs(C9["cpc"] / A["cpc"] - 1.0) <= 1e-3
               and abs(X9["g21"] - A["g21"]) <= 0.01
               and X9["k_res"] == A["kres"]
               and abs(R9["lam_rest"] - A["lam_rest"]) <= 1e-5
               and abs(Y9["rho0"] - A["rho0"]) <= 5e-3
               and abs(Y9["y0"] - A["y0"]) <= 5e-3
               and abs(Y9["rho_hi"] - A["rho_hi"]) <= 5e-3
               and abs(Y9["pm"] - A["pm"]) <= 0.05
               and abs(Y9["qm"] - A["qm"]) <= 0.05
               and abs(Y9["cm"] - A["cm"]) <= 0.05)
    check("G21-w9-dressed-anchors", ok_anch,
          "LEG 0 BIT-NEAR: dressed (d1', d2', c') = (%.6f, %.6f, "
          "%.6e) == r343/r347; p' %.4e, q' %.4e, m2' %.4e, bridge "
          "%.4f, c'/c %.4e, g21 %.4f, K_res %d, lambda_rest %.6f; "
          "r348 order-0 row rho_0 %.4f / y0 %.4f / rho_hi %.4f; "
          "pinning ratios p'/m %.4f, q'/m %.4f, |c'|/m %.4f -- "
          "the r350 coordinates start exactly where r342..r348 "
          "left them"
          % (R9["d1p"], R9["d2p"], R9["cp"], C9["ppr"], C9["qpr"],
             C9["m2p"], C9["bridge"], C9["cpc"], X9["g21"],
             X9["k_res"], R9["lam_rest"], Y9["rho0"], Y9["y0"],
             Y9["rho_hi"], Y9["pm"], Y9["qm"], Y9["cm"]))
    KA = K9_ANCH
    ok_k9 = (abs(R9["v1"] / KA["v1"] - 1.0) <= 1e-3
             and abs(R9["v2"] / KA["v2"] - 1.0) <= 1e-3
             and abs(K9["K11"] / KA["K11"] - 1.0) <= 1e-3
             and abs(K9["K22"] / KA["K22"] - 1.0) <= 1e-3
             and abs(K9["K12"] / KA["K12"] - 1.0) <= 1e-3
             and abs(K9["kap"] / KA["kap"] - 1.0) <= 1e-3
             and abs(K9["vp1"] / KA["vp1"] - 1.0) <= 1e-3
             and abs(K9["vp2"] / KA["vp2"] - 1.0) <= 1e-3)
    dev_cd9, cd_args9 = cd_ward_dev(R9)
    check("G22-w9-kernel-columns", ok_k9 and dev_cd9 <= CD_BAR,
          "THE KERNEL COLUMNS LIVE: v1 %.6e / v2 %.6e (dictionary "
          "v_pred %.6e / %.6e -- the r342 closed form), K11 %.6e, "
          "K22 %.6e, K12 %.6e, kappa %.6e (all == the s1 scoping "
          "bit-near); CD-ROUTE WARD: the recursion-coefficient "
          "route reproduces the Gram route at %.1e (bar %.0e) -- "
          "K_N at the pair atoms IS source-computable from the "
          "mu-chain, the open question is its LAW, not its value"
          % (R9["v1"], R9["v2"], K9["vp1"], K9["vp2"], K9["K11"],
             K9["K22"], K9["K12"], K9["kap"], dev_cd9, CD_BAR))
    ok_ds9 = (abs(K9["ds"] - KA["ds"]) <= 5e-3
              and Y9["dev_split"] <= SPLIT_BAR
              and Y9["dev_cpc"] <= CPC_ID_BAR
              and C9["dev_id"] <= ID_BAR
              and C9["dev_rdef"] <= RDEF_BAR)
    check("G23-w9-det-shadow", ok_ds9,
          "THE DET SHADOW LIVE at w9: |(p'q' - c'^2)/(margin^2 "
          "g21/Dt^2) - 1| = %.4f (s1 %.4f) -- the exact two-level "
          "pinning identity carries to the true dressed scalars "
          "at the truncation error; order-0 split %.1e (bar %.0e), "
          "bookkeeping c'/c == y0 - rho_hi %.1e (bar %.0e), "
          "one-line identity %.1e / %.1e re-gated"
          % (K9["ds"], KA["ds"], Y9["dev_split"], SPLIT_BAR,
             Y9["dev_cpc"], CPC_ID_BAR, C9["dev_id"],
             C9["dev_rdef"]))

    # ---------------- S3 the ladder
    section("S3  LEG A -- THE LADDER (42 + 15 + 12 EXT3 + 6 EXT4)")
    if smoke:
        for g in ("G30-ladder-census", "G31-ladder-identities",
                  "G32-cohort-anchors"):
            check(g, True, "SMOKE: skipped")
        RT, XT, YT, CT, KT = ({9: R9}, {9: X9}, {9: Y9}, {9: C9},
                              {9: K9})
        core_kzs, ext_kzs, ext3_kzs, ext4_kzs = [9], [], [], []
        excl = []
        cd_devs = {9: dev_cd9}
    else:
        core_kzs = list(V.admissible_indices())
        ext_kzs = [t[1] for t in LM.ext_rule()[:15]]
        ext3_kzs = list(EXT3_KZ_B + EXT3_KZ_A)
        ext4_kzs = list(EXT4_KZ_B + EXT4_KZ_A)
        RT, XT, YT, CT, KT = {}, {}, {}, {}, {}
        cd_devs = {}
        print("    %-5s %-5s %-5s %-5s | %-10s %-10s %-10s | "
              "%-10s %-10s %-10s | %-7s %-7s | %-9s %-9s %-9s | "
              "%-7s"
              % ("kz", "z", "S-", "N_w", "margin", "v1", "v2",
                 "K11", "K12", "kappa", "ds", "m2'/mg", "y0",
                 "rho_hi", "c'/c", "Kres"))
        neg_rows = []
        for kz in core_kzs + ext_kzs + ext3_kzs + ext4_kzs:
            if kz == MAIN_KZ:
                R, X, C, Y, K = R9, X9, C9, Y9, K9
            else:
                R = PX.build_rung(kz)
                X = PC3.extend_rung(R)
                C = DA.closure_cols(R)
                Y = DSA.rung_extras(R, X, C)
                K = rung_kernel_extras(R, X, C, Y)
            if kz in CD_KZ:
                cd_devs[kz] = (dev_cd9 if kz == MAIN_KZ
                               else cd_ward_dev(R)[0])
            if R["margin"] <= 0:
                neg_rows.append(kz)
            print("    %-5d %-5d %-5d %-5d | %.4e %.4e %.4e | "
                  "%.4e %.4e %.4e | %7.4f %7.4f | %.3e %.3e "
                  "%.3e | %4d"
                  % (kz, R["z"], R["Sm"], R["Nw"], R["margin"],
                     R["v1"], R["v2"], K["K11"], K["K12"],
                     K["kap"], K["ds"], C["bridge"], Y["y0"],
                     Y["rho_hi"], C["cpc"], X["k_res"]),
                  flush=True)
            if kz == MAIN_KZ:
                RT[kz], XT[kz], YT[kz], CT[kz], KT[kz] = \
                    R, X, Y, C, K
            else:
                rs, xs, ys, ks = slim350(R, X, C, Y, K)
                RT[kz], XT[kz], YT[kz], CT[kz], KT[kz] = \
                    rs, xs, ys, C, ks
        excl = list(neg_rows)
        ok_cen = (len(core_kzs) == 42
                  and all(EXT3_NW_MIN <= RT[k]["Nw"]
                          <= EXT3_NW_MAX for k in ext3_kzs)
                  and all(EXT4_NW_MIN <= RT[k]["Nw"]
                          <= EXT4_NW_MAX for k in ext4_kzs))
        check("G30-ladder-census", ok_cen and not neg_rows,
              "42 core + 15 r286 extension + 12 EXT3 (N_w %d..%d) "
              "+ 6 EXT4 (N_w %d..%d, the r343 sealed selections "
              "adopted AS-IS); every f64 margin positive "
              "(contingency rows: %s)"
              % (EXT3_NW_MIN, EXT3_NW_MAX, EXT4_NW_MIN,
                 EXT4_NW_MAX,
                 str(neg_rows) if neg_rows else "none"))
        all_kz = core_kzs + ext_kzs + ext3_kzs + ext4_kzs
        kz57 = core_kzs + ext_kzs
        max_split = max(YT[k]["dev_split"] for k in all_kz)
        max_cpc = max(YT[k]["dev_cpc"] for k in all_kz)
        max_id = max(CT[k]["dev_id"] for k in all_kz)
        max_rd = max(CT[k]["dev_rdef"] for k in all_kz)
        max_cd = max(cd_devs.values())
        ok_id = (max_split <= SPLIT_BAR and max_cpc <= CPC_ID_BAR
                 and max_id <= ID_BAR and max_rd <= RDEF_BAR
                 and max_cd <= CD_BAR
                 and all(RT[k]["det_dev"] <= 1e-12
                         and RT[k]["schur_dev"] <= 1e-6
                         and XT[k]["dev_res"] <= 1e-6
                         and XT[k]["dev_sp"] <= 1e-9
                         for k in all_kz))
        check("G31-ladder-identities", ok_id,
              "identities on all %d rows: order-0 split (max %.1e, "
              "bar %.0e), c'/c == y0 - rho_hi (max %.1e, bar "
              "%.0e), one-line identity (%.1e / %.1e), r342 "
              "det/Schur + r343 resolvent/spectral wards re-gated; "
              "CD-ROUTE WARD on the sealed sample %s: max %.1e "
              "(bar %.0e) -- the kernel values are recursion-"
              "computable on every probed depth"
              % (len(all_kz), max_split, SPLIT_BAR, max_cpc,
                 CPC_ID_BAR, max_id, max_rd, str(CD_KZ), max_cd,
                 CD_BAR))

        def med(vals):
            return float(np.median(np.asarray(vals, float)))

        fit_kz = [k for k in kz57 if k not in excl]
        med_rho0 = med([YT[k]["rho0"] for k in fit_kz])
        med_br = med([CT[k]["bridge"] for k in fit_kz])
        ok_coh = (abs(med_rho0 - MED_ANCH["rho0"]) <= 0.01
                  and abs(med_br - MED_ANCH["bridge"]) <= 5e-3
                  and all(abs(KT[k]["kap"] / SAMPLE_ANCH[k]["kap"]
                              - 1.0) <= 1e-3
                          and abs(KT[k]["ds"]
                                  - SAMPLE_ANCH[k]["ds"]) <= 5e-3
                          for k in SAMPLE_ANCH))
        check("G32-cohort-anchors", ok_coh,
              "LEG 0 COHORT ANCHORS: median rho_0 %.4f (r348 "
              "%.3f), median bridge %.4f (r343 %.4f); scoped "
              "sample kappa/ds (kz44/56/130) all bit-near -- "
              "Leg 0 closed"
              % (med_rho0, MED_ANCH["rho0"], med_br,
                 MED_ANCH["bridge"]))

    # ---------------- S4 twin
    section("S4  TWIN WARD")
    if smoke:
        check("G40-twin", True, "SMOKE: skipped")
        mzT = None
    else:
        uu9c, mm9c = TR.base_comb(9)
        mzD = TR.build_world(9, uu9c, mm9c)
        mz9 = R9["mz"]
        ok_dose0 = (np.array_equal(mzD["xp"], mz9["xp"])
                    and np.array_equal(mzD["wp"], mz9["wp"])
                    and np.array_equal(mzD["yn"], mz9["yn"])
                    and np.array_equal(mzD["vn"], mz9["vn"]))
        gaps9 = MF.local_gaps(uu9c)
        u2c, m2c, _dens, _du = AKD.twin_rational(uu9c, mm9c,
                                                 gaps9, mz9["D"],
                                                 TWIN_TOL)
        mzT = TR.build_world(9, u2c, m2c)
        aT, bT, h0T = V.mu_chain(mzT["xp"], mzT["wp"], mzT["Nw"])
        BT = V.b_matrix(aT, bT, h0T, mzT["yn"], mzT["vn"],
                        mzT["Nw"])
        t1_, t2_ = PX.pair_select(mzT["yn"])
        d1T, d2T, cT = PX.pair_block(BT, t1_, t2_)
        v1T = float(mzT["vn"][t1_])
        v2T = float(mzT["vn"][t2_])
        K11T, K22T, K12T, kapT = kernel_cols(d1T, d2T, cT, v1T,
                                             v2T)
        devT = dict(
            K11=abs(K11T / K9["K11"] - 1.0),
            K22=abs(K22T / K9["K22"] - 1.0),
            K12=abs(K12T / K9["K12"] - 1.0),
            kap=abs(kapT / K9["kap"] - 1.0))
        ok_twin = ok_dose0 and all(v <= TWIN_BAR
                                   for v in devT.values())
        check("G40-twin", ok_twin,
              "RATIONAL TWIN at tol %.0e (dose-zero identity "
              "BITWISE %s): kernel-column devs K11 %.1e, K22 "
              "%.1e, K12 %.1e, kappa %.1e (bar %.0e) -- the "
              "kernel coordinates are twin-stable"
              % (TWIN_TOL, ok_dose0, devT["K11"], devT["K22"],
                 devT["K12"], devT["kap"], TWIN_BAR))

    # ---------------- S5 fits + bookkeeping + candidates + balance
    section("S5  LEG A/B -- SEALED FITS + BOOKKEEPING + CANDIDATES "
            "+ THE ALPHA BALANCE")
    if smoke:
        for g in ("G50-fit-anchors", "G51-bookkeeping",
                  "G52-dict-slopes", "G53-candidates",
                  "G54-balance", "G55-pinning-census",
                  "G56-y0-laws"):
            check(g, True, "SMOKE: skipped")
        laws = {}
        klaw = {}
        pin_theorem = None
        d0_cand = None
        bal_txt = book_txt = cand_txt = pin_txt = d0_txt = ""
        alpha_meas = None
    else:
        lnN57 = np.log(np.array([RT[k]["Nw"] for k in fit_kz],
                                float))
        getters = {
            "margin": lambda k: RT[k]["margin"],
            "c": lambda k: abs(RT[k]["c"]),
            "p": lambda k: RT[k]["p"],
            "q": lambda k: RT[k]["q"],
            "cpc": lambda k: CT[k]["cpc"],
            "y0": lambda k: abs(YT[k]["y0"]),
            "rhi": lambda k: abs(YT[k]["rho_hi"]),
            "v1c": lambda k: RT[k]["v1"],
            "v2c": lambda k: RT[k]["v2"],
            "vp1": lambda k: KT[k]["vp1"],
            "vp2": lambda k: KT[k]["vp2"],
            "K11": lambda k: KT[k]["K11"],
            "K22": lambda k: KT[k]["K22"],
            "K12": lambda k: abs(KT[k]["K12"]),
            "kap": lambda k: abs(KT[k]["kap"]),
            "d1c": lambda k: RT[k]["d1"],
            "d2c": lambda k: RT[k]["d2"],
            "cc": lambda k: 1.0 - RT[k]["rdet"],
            "rdetc": lambda k: RT[k]["rdet"],
            "ppq": lambda k: RT[k]["p"] + RT[k]["q"],
            "pm": lambda k: YT[k]["pm"],
            "qm": lambda k: YT[k]["qm"],
            "cm": lambda k: YT[k]["cm"],
        }
        laws = {}
        for nm, get in getters.items():
            e3p = [(math.log(RT[k]["Nw"]), math.log(get(k)))
                   for k in ext3_kzs]
            e4p = [(math.log(RT[k]["Nw"]), math.log(get(k)))
                   for k in ext4_kzs]
            laws[nm] = DA.decay_law(lnN57,
                                    np.log([get(k)
                                            for k in fit_kz]),
                                    e3p, e4p)
        ok_fit = (all(abs(laws[nm]["slope"] - FIT_ANCH[nm])
                      <= FIT_ANCH_TOL for nm in FIT_ANCH)
                  and all(abs(laws[nm]["curv"] - CURV_ANCH[nm])
                          <= CURV_ANCH_TOL for nm in CURV_ANCH))
        check("G50-fit-anchors", ok_fit,
              "LEG 0 FIT ANCHORS on the 57 (slope | record): "
              "margin %.3f | %.3f, c %.3f | %.3f, p %.3f | %.3f, "
              "q %.3f | %.3f, cpc %.3f | %.3f, y0 %.3f | %.3f, "
              "rhi %.3f | %.3f (tol %.2f); CURV ANCHORS margin "
              "%+.3f | %+.3f, c %+.3f | %+.3f, cpc %+.3f | %+.3f, "
              "y0 %+.3f | %+.3f, rhi %+.3f | %+.3f (tol %.2f); "
              "THE NEW KERNEL SLOPES (this round's numbers): v1 "
              "%.3f, v2 %.3f, K11 %+.3f, K22 %+.3f, K12 %+.3f, "
              "kappa %.3f, d1 %+.4f, d2 %+.4f"
              % (laws["margin"]["slope"], FIT_ANCH["margin"],
                 laws["c"]["slope"], FIT_ANCH["c"],
                 laws["p"]["slope"], FIT_ANCH["p"],
                 laws["q"]["slope"], FIT_ANCH["q"],
                 laws["cpc"]["slope"], FIT_ANCH["cpc"],
                 laws["y0"]["slope"], FIT_ANCH["y0"],
                 laws["rhi"]["slope"], FIT_ANCH["rhi"],
                 FIT_ANCH_TOL, laws["margin"]["curv"],
                 CURV_ANCH["margin"], laws["c"]["curv"],
                 CURV_ANCH["c"], laws["cpc"]["curv"],
                 CURV_ANCH["cpc"], laws["y0"]["curv"],
                 CURV_ANCH["y0"], laws["rhi"]["curv"],
                 CURV_ANCH["rhi"], CURV_ANCH_TOL,
                 laws["v1c"]["slope"], laws["v2c"]["slope"],
                 laws["K11"]["slope"], laws["K22"]["slope"],
                 laws["K12"]["slope"], laws["kap"]["slope"],
                 laws["d1c"]["slope"], laws["d2c"]["slope"]))
        # bookkeeping
        bk1 = abs(laws["c"]["slope"]
                  - (0.5 * (laws["v1c"]["slope"]
                            + laws["v2c"]["slope"])
                     + laws["K12"]["slope"]))
        bk2a = abs(laws["K11"]["slope"] + laws["v1c"]["slope"]
                   - laws["d1c"]["slope"])
        bk2b = abs(laws["K22"]["slope"] + laws["v2c"]["slope"]
                   - laws["d2c"]["slope"])
        bk3 = abs(laws["c"]["slope"]
                  - (0.5 * (laws["p"]["slope"]
                            + laws["q"]["slope"])
                     + 0.5 * laws["cc"]["slope"]))
        ok_book = max(bk1, bk2a, bk2b, bk3) <= BOOK_BAR
        book_txt = ("BK1 |s_c - (1/2(s_v1 + s_v2) + s_K12)| = "
                    "%.4f; BK2 Christoffel saturation |s_K11 + "
                    "s_v1 - s_d1| = %.4f / |s_K22 + s_v2 - s_d2| "
                    "= %.4f; BK3 backward-CS |s_c - (1/2(s_p + "
                    "s_q) + 1/2 s_cc)| = %.4f (bar %.1f; s_cc "
                    "%.4f = the 1 - r_det column: c^2 tracks pq "
                    "with the vanishing reserve)"
                    % (bk1, bk2a, bk2b, bk3, BOOK_BAR,
                       laws["cc"]["slope"]))
        check("G51-bookkeeping", ok_book,
              "THE SLOPE BOOKKEEPING (exact pointwise identities, "
              "fitted at the non-additivity bar): %s -- a_c "
              "DECOMPOSES exactly into weights + kernel, and the "
              "diagonal kernel growths ARE the reciprocal weight "
              "laws (v K -> 1): the ONLY non-dictionary block of "
              "a_c is s_K12" % book_txt)
        # dictionary slopes
        dv1 = abs(laws["v1c"]["slope"] - laws["vp1"]["slope"])
        dv2 = abs(laws["v2c"]["slope"] - laws["vp2"]["slope"])
        ok_dict = max(dv1, dv2) <= DICT_SLOPE_BAR
        check("G52-dict-slopes", ok_dict,
              "THE WEIGHT SLOPES ARE DICTIONARY-GRADE: |s_v1 - "
              "s_vpred1| = %.4f, |s_v2 - s_vpred2| = %.4f (bar "
              "%.2f; dictionary slopes %.3f / %.3f) -- the r342 "
              "digamma/tent closed form CARRIES the across-ladder "
              "weight laws, not only the values"
              % (dv1, dv2, DICT_SLOPE_BAR, laws["vp1"]["slope"],
                 laws["vp2"]["slope"]))
        # candidates
        targets = {
            "a_p": (-laws["p"]["slope"], CAND_P, "p"),
            "a_q": (-laws["q"]["slope"], CAND_Q, "q"),
            "g_K12": (laws["K12"]["slope"], CAND_GK12, "K12"),
            "a_kap": (-laws["kap"]["slope"], CAND_KAP, "kap"),
        }
        klaw = {}
        cand_rows = []
        for nm, (tv, cl, col) in targets.items():
            hh = cand_hits(tv, cl, CAND_BAR)
            lw = laws[col]
            clean = (abs(lw["curv"]) <= DEC_CURV_BAR
                     and lw["e3_in"] >= DEC_EXT3_MIN
                     and lw["e4_low"] <= DEC_EXT4_LOW)
            klaw[nm] = dict(tv=tv, hits=hh, clean=clean,
                            fires=bool(hh) and clean, col=col)
            cand_rows.append("%s %.4f hits %s clauses %s (curv "
                             "%+.3f, EXT3 %d/12 low %d, EXT4 %d/6 "
                             "low %d) => %s"
                             % (nm, tv, str(hh),
                                "CLEAN" if clean else "DIRTY",
                                lw["curv"], lw["e3_in"],
                                lw["e3_low"], lw["e4_in"],
                                lw["e4_low"],
                                "LAW" if klaw[nm]["fires"]
                                else "census"))
        rel1_dev = abs(laws["K12"]["slope"] - REL1_FACTOR
                       * (laws["K11"]["slope"]
                          + laws["K22"]["slope"]))
        rel2_dev = abs(laws["K12"]["slope"] - REL2_FACTOR
                       * (laws["K11"]["slope"]
                          + laws["K22"]["slope"]))
        rel1_hit = rel1_dev <= CAND_BAR
        rel2_hit = rel2_dev <= CAND_BAR
        lwK = laws["K12"]
        rel2_clean = (rel2_hit
                      and abs(lwK["curv"]) <= DEC_CURV_BAR
                      and lwK["e3_in"] >= DEC_EXT3_MIN
                      and lwK["e4_low"] <= DEC_EXT4_LOW)
        cand_txt = ("; ".join(cand_rows)
                    + "; REL1 (perfect correlation, factor %.2f) "
                    "dev %.4f => %s; REL2 (sqrt decorrelation, "
                    "factor %.2f) dev %.4f => %s%s"
                    % (REL1_FACTOR, rel1_dev,
                       "HIT" if rel1_hit else "fails (expected: "
                       "the r_det -> 0 law forbids it)",
                       REL2_FACTOR, rel2_dev,
                       "HIT" if rel2_hit else "fails",
                       " + clauses CLEAN" if rel2_clean else ""))
        check("G53-candidates", True,
              "THE SEALED CANDIDATE ADJUDICATION (bar %.2f): %s"
              % (CAND_BAR, cand_txt))
        # balance + composition
        alpha_meas = -laws["margin"]["slope"]
        alpha_bal = slope_balance(laws["vp1"]["slope"],
                                  laws["vp2"]["slope"],
                                  laws["K12"]["slope"],
                                  laws["cpc"]["slope"])
        dev_bal = abs(alpha_bal - alpha_meas)
        rho_r = -laws["rdetc"]["slope"]
        alpha_comp = (-laws["p"]["slope"]) + (-laws["q"]["slope"]) \
            + rho_r - (-laws["ppq"]["slope"])
        dev_comp = abs(alpha_comp - alpha_meas)
        if klaw["a_p"]["fires"] and klaw["a_q"]["fires"]:
            cand_p0 = klaw["a_p"]["hits"][0]
            cand_q0 = klaw["a_q"]["hits"][0]
            alpha_cand = cand_p0 + cand_q0 + rho_r \
                - (-laws["ppq"]["slope"])
            dev_cand = abs(alpha_cand - alpha_meas)
        else:
            cand_p0 = cand_q0 = alpha_cand = dev_cand = None
        bal_txt = ("ROUTE A alpha_bal = -(1/2(%.3f + %.3f)[DICT] "
                   "+ %+.3f[KERNEL census] + %.3f[PINNING image]) "
                   "= %.3f vs alpha_meas %.3f (dev %.3f, bar "
                   "%.1f); r342 composition a_p + a_q + rho_r - "
                   "a_(p+q) = %.3f + %.3f + %.3f - %.3f = %.3f "
                   "(dev %.3f, bar %.1f)%s"
                   % (laws["vp1"]["slope"], laws["vp2"]["slope"],
                      laws["K12"]["slope"], laws["cpc"]["slope"],
                      alpha_bal, alpha_meas, dev_bal, BAL_BAR,
                      -laws["p"]["slope"], -laws["q"]["slope"],
                      rho_r, -laws["ppq"]["slope"], alpha_comp,
                      dev_comp, COMP_BAR,
                      "" if alpha_cand is None else
                      "; CANDIDATE image %.4g + %.4g + %.3f - "
                      "%.3f = %.3f (dev %.3f)"
                      % (cand_p0, cand_q0, rho_r,
                         -laws["ppq"]["slope"], alpha_cand,
                         dev_cand)))
        check("G54-balance", dev_bal <= BAL_BAR
              and dev_comp <= COMP_BAR,
              "LEG B THE ALPHA BALANCE: %s -- every block typed: "
              "dictionary (woerterbuch), s_K12 (kernel census/"
              "candidate), s_cpc (pinning theorem-image), rho_r "
              "(the one census member of the composition)"
              % bal_txt)
        # pinning census (Leg C live)
        arb_kz = [k for k in all_kz if k not in excl]
        lnN_all = np.log(np.array([RT[k]["Nw"] for k in arb_kz],
                                  float))
        br_max, br_slope, _brv = DA.bridge_stats(
            [CT[k]["m2p"] for k in arb_kz],
            [RT[k]["margin"] for k in arb_kz], lnN_all)
        ds_med = float(np.median([KT[k]["ds"] for k in fit_kz]))
        ds_max = max(KT[k]["ds"] for k in arb_kz)
        fitm = [k in set(fit_kz) for k in arb_kz]
        cohorts = [
            ("core42", [i for i, k in enumerate(arb_kz)
                        if k in set(core_kzs)]),
            ("ext15", [i for i, k in enumerate(arb_kz)
                       if k in set(ext_kzs)]),
            ("ext3B", [i for i, k in enumerate(arb_kz)
                       if k in set(EXT3_KZ_B)]),
            ("ext3A", [i for i, k in enumerate(arb_kz)
                       if k in set(EXT3_KZ_A)]),
            ("ext4", [i for i, k in enumerate(arb_kz)
                      if k in set(ext4_kzs)]),
        ]
        flat_r = {}
        for nm in ("pm", "qm", "cm"):
            flat_r[nm] = GR.curvflat_protocol(
                lnN_all, np.log([getters[nm](k) for k in arb_kz]),
                fitm, cohorts)
        flats_ok = all(flat_r[nm]["ok"] for nm in flat_r)
        pin_theorem = (br_max <= BRIDGE_DEV_BAR
                       and ds_med <= DSHADOW_MED_BAR and flats_ok)
        pin_txt = ("Fractions models 2/2 EXACT (S1); bridge max "
                   "|m2'/margin - 1| = %.4f (bar %.2f, slope "
                   "%+.4f); det-shadow median %.4f (bar %.2f, max "
                   "%.2f over the arbitrated rows -- truncation-"
                   "limited, the r348 weak-rung caveat stands); "
                   "CURV_FLAT re-gates %s (p'/m %+.3f CI [%+.2f, "
                   "%+.2f] %d out; q'/m %+.3f %d out; |c'|/m "
                   "%+.3f %d out)"
                   % (br_max, BRIDGE_DEV_BAR, br_slope, ds_med,
                      DSHADOW_MED_BAR, ds_max,
                      "3/3" if flats_ok else "BROKEN",
                      flat_r["pm"]["slope"], flat_r["pm"]["qlo"],
                      flat_r["pm"]["qhi"], flat_r["pm"]["n_out"],
                      flat_r["qm"]["slope"], flat_r["qm"]["n_out"],
                      flat_r["cm"]["slope"],
                      flat_r["cm"]["n_out"]))
        check("G55-pinning-census", True,
              "LEG C THE PINNING %s: %s -- %s"
              % ("THEOREM" if pin_theorem else "CENSUS", pin_txt,
                 "the near-cancellation Delta c ~ -c is the "
                 "algebraic self-consistency of the resolvent "
                 "equation (exact on the truncation, certified "
                 "census live)" if pin_theorem else
                 "the pinning stays census, loci printed"))
        # y0 laws (Leg D)
        delta0 = -laws["y0"]["slope"]
        order0_ok = (abs(laws["y0"]["curv"]) <= DEC_CURV_BAR
                     and laws["y0"]["e3_in"] >= DEC_EXT3_MIN
                     and laws["y0"]["e4_low"] <= DEC_EXT4_LOW
                     and delta0 >= DELTA0_MIN)
        twin_dev = abs(laws["y0"]["slope"] - laws["rhi"]["slope"])
        d0_hits = cand_hits(delta0, CAND_D0, CAND_BAR)
        d0_cand = order0_ok and bool(d0_hits) \
            and twin_dev <= TWINLAW_BAR
        d0_txt = ("delta_0 = %.3f (curv %+.3f, EXT3 %d/12 low %d, "
                  "EXT4 %d/6 low %d => ORDER0 %s); candidate hits "
                  "%s of %s (bar %.2f -- 0.38 and 2/5 are CLOSER "
                  "THAN THE BAR to each other: a hit is not an "
                  "identification, said so); twin ward |s_y0 - "
                  "s_rhi| = %.4f (bar %.2f); PINNED COROLLARY: "
                  "slope(y0 - rho_hi) == slope(c'/c) = %.3f == "
                  "-(alpha - a_c) by Leg C (ii), residual vs "
                  "-(%.3f - %.3f) = %.3f"
                  % (delta0, laws["y0"]["curv"],
                     laws["y0"]["e3_in"], laws["y0"]["e3_low"],
                     laws["y0"]["e4_in"], laws["y0"]["e4_low"],
                     "RE-GATES" if order0_ok else "FAILS",
                     str(d0_hits), str(CAND_D0), CAND_BAR,
                     twin_dev, TWINLAW_BAR, laws["cpc"]["slope"],
                     alpha_meas, -laws["c"]["slope"],
                     abs(laws["cpc"]["slope"]
                         + (alpha_meas - (-laws["c"]["slope"])))))
        check("G56-y0-laws", order0_ok and twin_dev <= TWINLAW_BAR,
              "LEG D THE Y0 TWIN CONTROL: %s" % d0_txt)

    # ---------------- S6 worlds
    section("S6  LEG D -- THE WORLD CENSUS (8 WORLDS, Y0 COLUMN)")
    if smoke:
        for g in ("G70-controls", "G71-worlds"):
            check(g, True, "SMOKE: skipped")
        mirror_tag = ""
        world_txt = ""
    else:
        rr9 = core.build_window(9)
        N_E = int(math.floor(math.exp(2.0 * rr9["alpha"]))) + 1
        lamE = PIK.lambda_eps(N_E)
        nn_idx = np.nonzero(np.abs(lamE) > 1e-12)[0]
        ug9, uw9 = PB.smooth_comb(rr9["alpha"])
        comb_hl, _tag = PC.gen_model(PC.Grid(), "HL2", HL2_SEED)
        uD, wD, _nnD, _chD = DSW.dirichlet_comb(9)
        uA, wA, _nnA = DSW.dirichlet_abs_comb(9)
        cdefs = (("EPST", dict(comb=(
            np.log(nn_idx.astype(float)),
            2.0 * lamE[nn_idx]
            / np.sqrt(nn_idx.astype(float))))),
            ("SCR", dict(scramble_seed=1)),
            ("SMOOTH", dict(comb=(ug9, uw9))),
            ("HL2", dict(comb=comb_hl)),
            ("DIR", dict(comb=(uD, wD))),
            ("ABS", dict(comb=(uA, wA))))
        WORLDS = {}
        ok_ctrl = True
        for cn, kw in cdefs:
            cctx = MS.ctx_build(9, **kw)
            xu, wu, zones = BL.union_of_ctx(cctx)
            xs_z, ws_z, ys_z, vs_z = zones
            N_c = cctx["N"]
            if cn in CTRL_FLIPS or cn == "HL2":
                sg = BL.sign_chain_f64(xu, wu, N_c + EXT)[0]
                mc = next((n for n in range(len(sg))
                           if sg[n] < 0), None)
                flip = CTRL_FLIPS.get(cn, HL2_FLIP)
                ok_ctrl = ok_ctrl and (mc == flip)
            WORLDS[cn] = FC.world_from_arrays(
                cn, xs_z, ws_z, ys_z, vs_z, N_c, int(cctx["L"]))
        check("G70-controls", ok_ctrl,
              "EPST + SCR + SMOOTH + HL2 built verbatim through "
              "the r278/r280 channel at THEIR own N_w: minC == "
              "flips %s + HL2 %d; DIR/ABS built through the SAME "
              "channel from the r330 combs (verbatim import)"
              % (str(CTRL_FLIPS), HL2_FLIP))
        WORLDS["MAIN"] = FC.world_from_mz("MAIN", R9["mz"])
        WORLDS["TWIN"] = FC.world_from_mz("TWIN", mzT)
        cen = {}
        order = ("MAIN", "TWIN", "EPST", "SCR", "SMOOTH", "HL2",
                 "DIR", "ABS")
        for wn in order:
            Wd = WORLDS[wn]
            Ew = Wd["B"] @ Wd["B"].T
            lam_rw, rho32w, cw = DA.mirror_world_row(Ew, Wd["yn"])
            i1w, i2w = PX.pair_select(Wd["yn"])
            restw = [k for k in range(Ew.shape[0])
                     if k != i1w and k != i2w]
            rho0w = -float(Ew[i1w, restw] @ Ew[i2w, restw]) / cw
            exists = lam_rw < 1.0
            rtxt = ("%.4f" % rho32w if np.isfinite(rho32w)
                    and abs(rho32w) < 1e4
                    else ("NONFINITE" if not np.isfinite(rho32w)
                          else "%.3g" % rho32w))
            ki = None
            ncf = 0
            if wn in ("MAIN", "TWIN", "EPST", "SCR", "SMOOTH",
                      "HL2"):
                ki, _loc, _nint, _kaps, ncf = \
                    FC.interval_census(Wd)
            cen[wn] = dict(lam=Wd["lam"], lam_rest=lam_rw,
                           rho32=rho32w, rho0=rho0w,
                           y0=1.0 - rho0w, exists=exists,
                           kint=ki, ncf=ncf)
            info("%s: S_- %d, lambda %.6g, lambda_rest %.6g "
                 "(twin pair %s), rho_32 %s, rho_0 %.4g, y0 "
                 "%.4g%s"
                 % (wn, len(Wd["yn"]), Wd["lam"], lam_rw,
                    "EXISTS" if exists else
                    "CANNOT EXIST (rho_hi series diverges)",
                    rtxt, rho0w, 1.0 - rho0w,
                    (", kappa_int %.6g" % ki)
                    if ki is not None else ""))
        lr_sep = (all(cen[wn]["lam_rest"] >= 1.0
                      for wn in ("EPST", "SCR", "SMOOTH", "HL2"))
                  and all(cen[wn]["lam_rest"] < 1.0
                          for wn in ("MAIN", "TWIN")))
        ok_kint = (all(abs(cen[wn]["kint"] / KINT_REC[wn] - 1.0)
                       <= KINT_REC_TOL
                       for wn in ("EPST", "SCR", "SMOOTH",
                                  "HL2"))
                   and all(abs(cen[wn]["kint"] / KINT_LIVE_REC
                               - 1.0) <= KINT_LIVE_TOL
                           for wn in ("MAIN", "TWIN"))
                   and sum(cen[wn]["ncf"] for wn in cen
                           if cen[wn]["ncf"]) == 0)
        live_ok = all(cen[wn]["exists"]
                      and abs(cen[wn]["rho32"] - 1.0)
                      <= MIRROR_LIVE_BAR
                      for wn in ("MAIN", "TWIN"))
        dead_ok = all(not cen[wn]["exists"]
                      for wn in ("EPST", "SCR", "SMOOTH", "HL2"))
        if live_ok and dead_ok:
            mirror_tag = ("MIRROR_WORLD_CLAUSE_SEALED(live 2/2, "
                          "dead 4/4 diverge; DIR/ABS typed "
                          "DEAD-side -- the r347/r348 typing "
                          "reproduced)")
        else:
            mirror_tag = ("MIRROR_WORLD_INCOMPLETE(live_ok %s, "
                          "dead_ok %s)" % (live_ok, dead_ok))
        world_txt = ("lambda_rest separates 4/4 (re-gated); "
                     "kappa_int == records at %.0f%%; Y0 COLUMN "
                     "(census, no bar): live %.4g/%.4g vs dead "
                     "%s, DIR %.4g / ABS %.4g; THE TWIN-PAIR "
                     "TYPING: on every dead world lambda_rest >= "
                     "1 -- the rho_hi series diverges, the twin "
                     "law pair (y0, rho_hi) CANNOT EXIST without "
                     "the contraction: the r348 twin slow laws "
                     "are a live-world structure, typed"
                     % (100 * KINT_REC_TOL, cen["MAIN"]["y0"],
                        cen["TWIN"]["y0"],
                        str({w: "%.3g" % cen[w]["y0"]
                             for w in ("EPST", "SCR", "SMOOTH",
                                       "HL2")}),
                        cen["DIR"]["y0"], cen["ABS"]["y0"]))
        check("G71-worlds", lr_sep and ok_kint,
              "WORLD LEDGER: lambda_rest >= 1 on dead 4/4 and < 1 "
              "on live 2/2; kappa_int EPST %.6g / SCR %.4g / "
              "SMOOTH %.4g / HL2 %.6g == records at %.0f%%, live "
              "%.6f; THE SEALED MIRROR RULE => %s; %s"
              % (cen["EPST"]["kint"], cen["SCR"]["kint"],
                 cen["SMOOTH"]["kint"], cen["HL2"]["kint"],
                 100 * KINT_REC_TOL, cen["MAIN"]["kint"],
                 mirror_tag, world_txt))

    # ---------------- S8 must-fails
    section("S8  MUST-FAILS")
    aa9, bb9, h09, y19, y29 = cd_args9
    mut1 = mutant_cd_unnormalized(aa9, bb9, h09, y19, y29,
                                  R9["Nw"])
    dev_m1 = abs(mut1 / K9["K12"] - 1.0)
    mut1t = mutant_cd_unnormalized(aa_t, bb_t, h0_t, y1_t, y2_t,
                                   dep_t)
    dev_m1t = abs(mut1t / K12_dir - 1.0)
    check("G80-m1-cd-unnormalized", dev_m1 >= M1_BAR
          and dev_m1t >= MUT_MIN,
          "m1 THE CD FORMULA WITHOUT b_{N-1}: breaks the kernel "
          "ward by %.1e rel at w9 (>= %.1f) and %.1e on the toy "
          "(>= %.0e) -- the recursion normalization is "
          "load-bearing, exactly CAUGHT" % (dev_m1, M1_BAR,
                                            dev_m1t, MUT_MIN))
    mut2 = mutant_klaw_readback()
    check("G81-m2-klaw-readback", bool(hits_m2)
          and abs(mut2 - 0.5) >= MUT_MIN,
          "m2 THE KERNEL SLOPE READ BACK from the withheld record "
          "exponents: AST-FLAGGED (%s; toy value %.3f) -- the "
          "REAL kernel slopes are sealed fits of measured "
          "columns, scope-audited CLEAN"
          % (hits_m2[0] if hits_m2 else "MISS", mut2))
    mut3 = mutant_cand_posthoc([0.5812, 0.58, 0.579])
    all_cands = set(CAND_P) | set(CAND_Q) | set(CAND_GK12) \
        | set(CAND_KAP) | set(CAND_D0)
    check("G82-m3-cand-posthoc", bool(hits_m3)
          and all(abs(mut3 - c) >= MUT_MIN for c in all_cands),
          "m3 CANDIDATES PICKED AFTER SIGHT: AST-FLAGGED (%s) and "
          "the toy posthoc candidate %.4f is in NO sealed list -- "
          "protocol-CAUGHT (the lists are frozen module constants "
          "under the two-commit protocol)"
          % (hits_m3[0] if hits_m3 else "MISS", mut3))
    mut4 = mutant_balance_doublecount(TOY_SV1, TOY_SV2, TOY_SK12,
                                      TOY_SCPC)
    dev_m4 = abs(mut4 - a_plant)
    check("G83-m4-double-count", dev_m4 >= M4_MIN
          and abs(dev_m4 - 0.5 * abs(TOY_SV1 + TOY_SV2))
          <= TOY_TOL,
          "m4 THE DOUBLE-COUNTED WEIGHT SHARE: breaks the exact "
          "toy balance by %.3f == |s_v1 + s_v2|/2 = %.3f EXACTLY "
          "(>= %.1f) -- the half is load-bearing (c carries "
          "sqrt(v1 v2), not v1 v2), exactly CAUGHT"
          % (dev_m4, 0.5 * abs(TOY_SV1 + TOY_SV2), M4_MIN))
    mut5 = mutant_pin_plant()
    check("G84-m5-pin-plant", bool(hits_m5)
          and abs(mut5 - a_plant) >= 0.01,
          "m5 SYNTHETIC PINNING FAMILY WITH BUILT-IN TARGET: "
          "AST-FLAGGED (%s) and the planted readback %.3f differs "
          "from the sealed toy balance exponent %.2f by >= 0.01 "
          "-- the toy families do NOT plant the record value, "
          "construction-CAUGHT"
          % (hits_m5[0] if hits_m5 else "MISS", mut5, a_plant))

    # ---------------- S9 verdict
    section("S9  VERDICT")
    check("G95-stoplist", True,
          "STOP LIST held: NO L* claim, no bound mechanism "
          "promoted, no certificate reading beyond the sealed "
          "census, no posthoc bar/band/family/prior/candidate "
          "move, no derived 5/7, NO RH claim, mincut unchanged; "
          "what the round adds: the kernel columns with the "
          "CD-route ward, the slope bookkeeping, the dictionary "
          "slope clause, the sealed candidate adjudication, the "
          "alpha balance, the pinning theorem toys + census and "
          "the y0 control; r243..r349 stand")
    npass = sum(1 for _n, ok, _d in CHECKS if ok)
    if smoke:
        verd = "SMOKE_NO_ADJUDICATION"
    else:
        audits_ok = okf and not hits and not ag_hits
        klaw_loci = [nm for nm in klaw if klaw[nm]["fires"]]
        if rel2_hit and rel2_clean:
            klaw_loci.append("K12:REL2")
        if not audits_ok:
            main_v = "TARGET_LEAK(%s)" % "; ".join(hits + ag_hits)
        elif (klaw.get("a_p", {}).get("fires")
              and klaw.get("a_q", {}).get("fires")
              and pin_theorem and dev_cand is not None
              and dev_cand <= COMP_BAR and dev_bal <= BAL_BAR):
            main_v = ("ALPHA_SOURCE_CLOSED(candidate-law grade: "
                      "alpha == %.4g[cand a_p] + %.4g[cand a_q] "
                      "+ %.3f[rho_r, THE single census member] - "
                      "%.3f[a_(p+q)] = %.3f vs %.3f (dev %.3f); "
                      "blocks typed: weights WOERTERBUCH (dict "
                      "slopes at %.3f/%.3f), deficits CANDIDATE-"
                      "LAW (hits %s / %s, ambiguity printed), "
                      "pinning THEOREM (Fractions exact + "
                      "certified census), rho_r CENSUS; honesty: "
                      "a sealed-hit on 57 windows is NOT an "
                      "asymptotic derivation)"
                      % (cand_p0, cand_q0, rho_r,
                         -laws["ppq"]["slope"], alpha_cand,
                         alpha_meas, dev_cand, dv1, dv2,
                         str(klaw["a_p"]["hits"]),
                         str(klaw["a_q"]["hits"])))
        elif klaw_loci:
            main_v = ("KERNEL_LAW_FOUND(%s -- candidate hits with "
                      "clean decay clauses; every hit list "
                      "printed with its ambiguity; the kernel "
                      "side carries candidate laws, NOT an "
                      "identification)" % ", ".join(
                          "%s %.4f -> %s"
                          % (nm, klaw[nm]["tv"],
                             str(klaw[nm]["hits"]))
                          if nm in klaw else nm
                          for nm in klaw_loci))
        else:
            main_v = ("KERNEL_IRREDUCIBLE(the kernel side resists "
                      "every sealed candidate; THE FINAL "
                      "SPECIALIST PROFILE: s_v1 %.3f / s_v2 %.3f "
                      "(dictionary), g_K11 %+.3f / g_K22 %+.3f "
                      "(== reciprocal weights, BK2), g_K12 %+.3f, "
                      "a_kappa %.3f, a_p %.3f, a_q %.3f, delta_0 "
                      "%.3f -- the CD-kernel growth at discrete "
                      "arch-rim atoms under log 2)"
                      % (laws["v1c"]["slope"], laws["v2c"]["slope"],
                         laws["K11"]["slope"], laws["K22"]["slope"],
                         laws["K12"]["slope"],
                         -laws["kap"]["slope"],
                         -laws["p"]["slope"], -laws["q"]["slope"],
                         delta0))
        parts = [
            main_v,
            (("PINNING_THEOREM(%s)" if pin_theorem
              else "PINNING_CENSUS(%s)") % pin_txt),
            (("DELTA0_CANDIDATE(%s)" if d0_cand
              else "DELTA0_CENSUS(%s)") % d0_txt),
            "BOOKKEEPING_LEDGER(%s; dict slope devs %.4f/%.4f)"
            % (book_txt, dv1, dv2),
            "BALANCE_LEDGER(%s)" % bal_txt,
            "KERNEL_LEDGER(v1 %.3f, v2 %.3f, K11 %+.3f, K22 "
            "%+.3f, K12 %+.3f, kappa %.3f (curv %+.3f, EXT3 "
            "%d/12, EXT4 %d/6); CD ward max %.1e on %s; %s)"
            % (laws["v1c"]["slope"], laws["v2c"]["slope"],
               laws["K11"]["slope"], laws["K22"]["slope"],
               laws["K12"]["slope"], laws["kap"]["slope"],
               laws["kap"]["curv"], laws["kap"]["e3_in"],
               laws["kap"]["e4_in"], max_cd, str(CD_KZ),
               cand_txt),
            "WORLD_LEDGER(%s)" % world_txt,
            "TWIN_LEDGER(kernel devs K11 %.1e, K22 %.1e, K12 "
            "%.1e, kappa %.1e)"
            % (devT["K11"], devT["K22"], devT["K12"],
               devT["kap"]),
            "MUSTFAIL_LEDGER(m1-m5 + scopes)",
        ]
        verd = " + ".join(p for p in parts if p)
    check("G96-verdict", npass == len(CHECKS),
          "%s%s -- MEASURED anatomy + sealed adjudication of the "
          "alpha source; NO L* claim, NO RH claim"
          % (verd, " (SMOKE)" if smoke else ""))
    wall = time.time() - T0_WALL
    check("G99-runtime", wall <= 1800.0,
          "WALL %.1f s (bar 1800)" % wall)
    npass = sum(1 for _n, ok, _d in CHECKS if ok)
    print("\n" + "=" * 78)
    print("RESULT: %d/%d gates PASS%s   SPEC_SHA %s"
          % (npass, len(CHECKS), " (SMOKE)" if smoke else "",
             SPEC_SHA[:16]))
    print("NO RH CLAIM in either direction.")
    print("=" * 78)
    return 0 if npass == len(CHECKS) else 1


if __name__ == "__main__":
    sys.exit(main())
