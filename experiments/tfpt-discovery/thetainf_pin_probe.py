#!/usr/bin/env python3
# -*- coding: utf-8 -*-
r"""thetainf_pin_probe -- PRIME.THETAINF.PIN.01

FROZEN SPEC (2026-08-20).  EXPLORATION ONLY.  This probe writes no
verification module, paper, ledger, website, manifest, Lean file or
status marker.  It makes NO RH claim, NO positivity claim beyond the
per-rung certificates stated, NO counterexample claim.  It closes no
gate and narrows no gate.

=======================================================================
MISSION (PIN THETA_INF CLASSICALLY -- OR LOCATE EXACTLY WHY NO
CLASSICAL STATEMENT CAN SEE IT.  After r173/CDXC the open object of
the H3-cofinal chain is theta_inf = lim theta_y = lim |A_2/A_0|/T_z^4,
measured band [0.0766, 0.0977] under the bar 0.155, typed
OPEN-GONEK-CLASS.  This round prices the three named mechanisms
against the r174/CDLXXXIX unconditional Landau/Gonek machinery:
(P1) THE QUARTIC-LIMIT DECOMPOSITION -- adjudicate whether the r174
rate dictionary at p = 4 pins the T_z^4 constant (it cannot:
covariance proven), then decompose the SOURCE DATA: the prime block
of the world matrix M = Mpole + March - Mprime is LINEAR in two atom
transforms per mode (pj = sin transform, pdiag = psi_d transform);
subtracting the closed-form SMOOTH (PNT-density) transform isolates
the arithmetic fluctuation F, and THE MODE-LEVEL LANDAU BRIDGE
(explicit formula per mode, NEW instrument) evaluates F from the
verified zero census alone: F_s(om_k) == sum_gamma 4 om_k
sin^2(A gamma)/(gamma^2 - om_k^2) - Itriv(om_k) with W-transform
closed forms proven exact and the resonance gamma -> om_k REMOVABLE
(sin(A om_k) = 0); the diagonal transform bridges on [u0, 2A],
u0 = log(3/2) (no prime power below 3/2), with the [0, u0] smooth
head subtracted exactly; then attempt the FULL RECONSTRUCTION of
theta through the variational map from zero-side data at a census
depth ladder, and measure the conditioning wall T_req;
(P2) THE SECOND-MOMENT ROUTE -- measure the fluctuation quadratic
form against the ground level (cancellation ratio) and the
fluctuation norm against the SMOOTH spectral gap (perturbative
validity), and adjudicate the classical second-moment statements
(Selberg 1946 unconditional; Fujii unconditional; Montgomery 1973
pair correlation RH-CONDITIONAL == LOOP; Goldston-Montgomery 1987
RH-equivalence == LOOP; Gonek 1984 RH == LOOP, r174 flag carried;
Vinogradov-Korobov unconditional but transport-divergent at mode
scale -- and even the FLAGGED RH grant transport-diverges:
cancellation-blindness);
(P3) THE INTERLACING/DENSITY ROUTE -- one-signed partial fractions
would interlace the census roots with the b_k poles and ceiling the
top root at b_top (proven); the MEASURED sign census refutes the
premise at every rung and y_t/b_top = 24 -> 318 measures the escape;
the Vieta trace ceiling carries the target itself (kappa -> 0); the
r174 separated pricing dissolves the 1/A_0 circularity at the DATA
level and relocates it to the variational conditioning (T_req);
(P4) FALSIFICATION -- the bridge must separate the fake worlds
through the SAME instrument (SCRARITH/EPSTEIN break it by measured
factors >= 1e3); the witness moves the vector, not the world: the
bridge is witness-DATA-INVARIANT (exact) and the witness exclusion
stays the cited r173 manifold; H3-ONE-SIDED-WORLD-BLIND restated;
the cited t_inf band is gated against the bar;
(P5) ASSEMBLY -- no classical ceiling lands; theta_inf is re-typed
OPEN-NONPERTURBATIVE-VARIATIONAL (sharper than OPEN-GONEK-CLASS):
its DATA is per-census zero-expressible (the bridge), its LIMIT is
walled by the measured conditioning (T_req = 3.8e14 -> 1.8e77 vs
T_PT = 3.0e12, diverging), its second-moment truncation is invalid
(measured), and every in-corpus ceiling route is dead or flagged;
the residue is UNCHANGED in cardinality.)
=======================================================================
State consumed (CITED): CDXC/r173 (h3_cofinal: theta_inf band
[0.076602, 0.097708] from the frozen model fits, 4 pi t_inf table,
saturation real, p_loc -> 4.024, DK stability instrument, witness
exclusion G56/G57, quantifier SEQ); CDLXXXIX/r174 (gonek_pricing:
[L] Landau 1912 + [G] Gonek 1985/1993 UNCONDITIONAL, envelope
T_hi-collapse, spike instrument c_hat <= 0.086, rate dictionary
p = 4 == 4 pi/q symbolic q, Gonek-1984 RH-flag); CDLXXXVIII/r172
(toproot: H3 statement, record ladders YT_L10/LOCK, witness algebra,
TOP_WIN census form, A0-floor loop); CDLXXXVII/r171 (PF1-PF3, H1/H2
complete-real census, SIZE-SEPARATOR); CDLXXV/r166 (BA family);
CDLXVII/r162 (THETA window); r143/r140 (alignment wall, DENSE-X);
HSW22; PT21 (T_PT = 3000175332800; both caches below T_0
unconditionally on-line -- the per-census pedigree); Landau 1912 +
Gonek 1985/1993 AS FORM (r174 adjudication VERBATIM); the
Riemann-von-Mangoldt/Davenport explicit formula (Multiplicative
Number Theory Ch. 17) AS FORM (density form dpsi = [1 - sum_rho
t^{rho-1} - 1/(t(t^2-1))] dt, conditionally convergent symmetric
truncation; constants and tails MEASURED per census -- the same
epistemic class as the r174 pricing); Selberg 1946 + Fujii
(UNCONDITIONAL, cited not consumed); Montgomery 1973 + Goldston-
Montgomery 1987 + Gonek 1984 (RH-CONDITIONAL == LOOP-IF-CONSUMED,
machine-flagged, NOT consumed); Vinogradov-Korobov zero-free region
(UNCONDITIONAL, transport-divergent at mode scale -- proven).

NOTATION.  Rung h (R4.build_cell, even sector); A = log(h)/2; L =
2A; K = ceil(1.25 h log h); om_k = k pi/A; b_k = om_k^2; c_k source
(ground state of M in the nrm basis, nrm_0 = sqrt(L), else sqrt(A));
A_{2m} = sum (-1)^k c_k b_k^m; y_t = |A_2/A_0|; T_z = 2 pi h;
theta_y = y_t/T_z^4; THETA_BAR = 0.155.  PRIME DATA: atoms (u_q =
log q, w_q = Lambda(q)/sqrt(q)) for prime powers q <= h; pj(om) =
sum w_q sin(om u_q); pdiag(om) = sum w_q psi_d(u_q), psi_d(u) =
(A - u/2)cos(om u) - sin(om u)/(2 om) (k >= 1; psi_d0 = L - u);
psi_d(L) == 0 and sin(om_k L) == 0 EXACT (no endpoint-halving
ambiguity).  SMOOTH transforms: S(f) = Int_0^L f(u) e^{u/2} du
(pj_smooth in the builder's closed form; pdiag_smooth by the
builder's quad, VERBATIM).  FLUCTUATION: F_s = pj - pj_smooth,
F_d = pdiag - pdiag_smooth.  THE BRIDGE: F_s(om_k) == sum_gamma
4 om_k sin^2(A gamma)/(gamma^2 - om_k^2) - Itriv_s(om_k), Itriv_s =
Int_0^L sin(om u) e^{-u/2}/(e^{2u} - 1) du; F_d(om_k) ==
-sum_gamma 2 Re W_d(gamma; u0, L) - Itriv_d^{(u0)}(om_k) -
S_{[0,u0]}(psi_d), W_d = A Wc - Wuc/2 - Ws/(2 om) from the E1/J1
primitives, u0 = log(3/2).  Zero side per census (on-line below
T_0, Platt-Trudgian class); truncation tails MEASURED (the ~1/T
law); ALL-K stays the flagged RH loop.

=======================================================================
WHAT IS BUILT AND GATED
=======================================================================
S0  G01 AST firewall (np.load only in ward_*, no zero-oracle names,
    no verification/ import); G02 cache wards (n7000 + big 20M:
    counts, gamma_1, monotone, overlap <= 1e-8; T at the N-ladder
    checkpoints; T(2e7) = 9499220.4795).
S1  exact layer (sympy):
    G10 statement + decomposition: theta_inf target restated (the
    classical-evaluation face of H3-COFINAL, quantifier SEQ, cited
    band consumed CITED); M == Mpole + March - Mprime with the
    prime-block assembly LINEAR in (pj, pdiag) (generic 3-mode
    symbolic assembly: asm(a + b) == asm(a) + asm(b)); pj_smooth
    closed form == Int_0^L e^{u/2} sin(om u) du EXACT;
    G11 bridge closed forms: E1/J1 primitives exact (derivative
    check); W_s == om(1 - e^{2iAg})/(om^2 - g^2) at modes; 2 Re W_s
    == 4 om sin^2(Ag)/(om^2 - g^2); RESONANCE REMOVABLE: lim_{g ->
    om_k} == 0 exact; endpoint weights sin(om_k L) == 0 and
    psi_d(L) == 0 exact; trivial-zero density == geometric
    1/(t(t^2 - 1)) exact;
    G12 dictionary covariance (the P1 pre-question): rate dictionary
    p = 4 with SYMBOLIC q == 4 pi/q (r174 replicated); onset map
    q == (2 pi)^2 sqrt(z0 theta) exact; d(output)/d(theta) < 0
    strictly (bijection -- every theta transported, NONE pinned):
    DICTIONARY-COVARIANT-NOT-PINNING; T_z = 2 pi h definitional;
    G13 perturbation typing: Rayleigh-Schroedinger first-order
    residual identity (M0 - l0) v1 + (D - mu1) e0 == 0 generic K=3;
    second-order mu2 == sum_{j>0} D_{0j}^2/(l0 - l_j) (e0-component
    identity); first-order delta(A_2/A_0) bilinear formula exact:
    theta-in-fluctuation is SECOND-MOMENT (pair) class ONLY inside
    the validity radius ||D|| < gap -- the radius G38 measures
    BROKEN;
    G14 conditionality ledger (P2): Selberg 1946 UNCONDITIONAL
    (cited, kernel-fixed, not consumed); Fujii UNCONDITIONAL (cited
    not consumed); Montgomery-1973-PC RH-CONDITIONAL == LOOP;
    Goldston-Montgomery-1987 RH-equivalence == LOOP; Gonek-1984 RH
    == LOOP (r174 carried); VK UNCONDITIONAL + the transport chase:
    lim exp(3L/2 - c L^{3/5}) == oo (VK ceiling DIVERGES at mode
    scale om ~ h, x = h vs measured |F| = O(1)); RH-GRANT transport
    lim h log^3 h == oo ALSO diverges (adjudication only, grant NOT
    consumed): CANCELLATION-BLIND -- the wall is alignment, not
    conditionality alone;
    G15 interlacing/Vieta (P3): one-signed partial fractions ==>
    roots interlace poles ==> top root <= b_top (sign-change proof,
    generic ordered symbols); trace ceiling y_t/(B_1 + y_t) ==
    1/(1 + kappa) carries the target (kappa -> 0 measured); the
    separated-pricing adjudication: the r174 pricing dissolves the
    1/A_0 circularity at the DATA level and RELOCATES it to the
    variational conditioning (G36/G37).
S3  per-rung layer, RUNGS = (4, 5, 8, 13) (MAIN + SMOOTH builds;
    deep rungs NOT rebuilt: cost-disclosed, the cited band is the
    r173 record):
    G30 spectral sanity (sorted, K, simp >= 1e3, ray/res <= 1e-25);
    G31 cited replication (THE LICENSE): THETA_Y_TAB rel 5e-3;
    yt_l10 <= 1.5e-3 dex + lock rel 8e-3 on the r172 ladder;
    KAPPA_TAB + KAPPA13 rel 5e-3; FG_TAB x (0.97, 1.03); RHO_TAB
    rel 5e-3 + argmax == 1 + |J2| in (0.05, 0.25); YT_R143 <= 1e-3
    dex; sign(A_2/A_0) == -1; B_1 closed form <= 1e-40; H3 margin
    >= 1.5; theta_y in (0.03, 0.12); log10 tau on LOG10TAU_TAB abs
    0.01;
    G32 assembly wards (edge): own asm_prime == builder mpPrime
    (MAIN + SMOOTH) dev <= 1e-40; pole/arch world-independence <=
    1e-40; split reconstitution (qPA - qS - qF)/vnorm == tau dev <=
    1e-30;
    G33 fluctuation + attribution: |F_s|/|F_d| tabs rel 5e-3 and <=
    4.0 (O(1) scale vs the divergent VK ceiling); SMOOTH tau_w < 0
    on SMOOTH_TAU_TAB rel 5e-3 (4/5 r173 VERBATIM, 8/13 NEW);
    theta_w on THETA_W_TAB rel 5e-3; SEP = theta/theta_w >= 10 +
    strings rel 1e-2 (126.8 -> 3265.3: the fluctuation carries
    >= 99.2% of theta); GAP_SMOOTH_TAB rel 5e-3; SIGN CENSUS exact
    ints (nneg >= 3 every rung: INTERLACING-PREMISE-REFUTED);
    y_t/b_top on YT_BTOP_TAB rel 5e-3 strictly increasing (the
    escape diverges);
    G34 THE BRIDGE, pj (P1 core): deepest-census residual <= 1e-4
    at EVERY mode of EVERY rung + PJ_RES_TAB rel 1e-2; tail-law
    drop res(1e5)/res(1e7) >= 20 (the ~1/T law); min |gamma - om_k|
    on MINSEP_TAB rel 1e-2 and >= 1e-2 (no resonance hazard);
    f64/mp cross <= 1e-10 (h=5, k=3, N=1e5; calibrated 3.44e-14);
    G35 THE BRIDGE, pdiag: deepest residual <= 1e-5 + PD_RES_TAB
    rel 1e-2 (residual non-monotone in N -- oscillatory u0-boundary
    term, [G]-class at x = 3/2 -- DISCLOSED, reported all
    checkpoints);
    G36 THE RECONSTRUCTION EXPERIMENT: M_hat(N) = M - asm(defect)
    from zero-side data at the N-ladder; theta_hat rel err >= 0.5
    at the deepest census at EVERY rung + RECON_ERR_TAB rel 5e-2 +
    DM_TAB rel 1e-2 + ||dM||/gap >= 1 (measured 9.9 -> 1.3e42):
    RECONSTRUCTION-FAILS-AT-FULL-CENSUS -- the DK prediction and
    the direct experiment agree;
    G37 THE T_REQ WALL: AMP_TAB + GAPABS_TAB rel 5e-3; T_req =
    dM(deepest) T(deepest)/(0.05 gap/(amp + 0.05)) on T_REQ_TAB rel
    5e-2, >= 10 T_PT at EVERY rung, strictly increasing
    (3.76e14 -> 1.75e77 vs T_PT = 3.00e12: ratios 125 -> 5.8e64):
    PIN-CENSUS-UNREACHABLE-BY-DEPTH;
    G38 second-moment kill (P2): |c^T Mfl c|/tau on FLUCT_TAU_TAB
    rel 1e-2 and >= 1e10 (3.44e10 -> 3.14e53: any classical
    second-moment envelope must be two-sided at relative 1/ratio --
    CANCELLATION-BLIND-MEASURED); ||Mfl||_F/gap_SMOOTH on
    FLUCT_GAP_TAB rel 1e-2, >= 1.5, strictly increasing (1.76 ->
    12.5: SECOND-MOMENT-TRUNCATION-INVALID); SPLIT_TAB rel 5e-3
    (pole+arch/smoothprime/fluct per unit norm).
S4  controls + falsification (P4):
    G50 SCRARITH(5): own atom replication ward <= 1e-40; tau_w on
    -0.345927 rel 5e-3 (< 0); theta_w on 1.005571e-3 rel 5e-3;
    BRIDGE BREAK ratio >= 1e3 + string 16449.9 rel 5e-2;
    G51 EPSTEIN(8): same with -1.631021, 1.011306e-4, 103617.3;
    G52 witness adjudication: the r172/r173 witness moves the
    VECTOR c, never the atoms: pj/pdiag are c-free (sympy
    free-symbol check) -- the bridge is WITNESS-DATA-INVARIANT
    (exact); the witness exclusion is the CITED r173 manifold
    (G56/G57: ground-state cert + two-sided lock + BA3 bridge,
    escalation-stable), NOT this instrument; H3-ONE-SIDED-WORLD-
    BLIND restated (never hidden);
    G53 ceiling adjudication (P4/P5): the candidate list {TRANSPORT
    (r173 flagged conditional-on-measured), INTERLACING (refuted
    G33), TRACE (self-referential G15), MOMENT-CAP (circular,
    r172), A0-FLOOR (loop, r172), VK/RH-TRANSPORT (diverges G14),
    SECOND-MOMENT (invalid G38), RECONSTRUCTION (unreachable
    G36/G37)} is enumerated and EMPTY of admissible classical
    ceilings: CLASSICAL-CEILING-NOT-LANDED; the cited band vs bar:
    0.155/0.097708 = 1.586 >= 1.58 (band under bar, CITED); 4 pi
    band = (0.9626, 1.2278) contains 1: the 1/(4 pi) candidate
    stays RECORDED-NOT-CLAIMED (v354/v355 discipline, r173
    VERBATIM).
S5  G54 tau screens (slopes vs log10 tau: |yt_l10|, |log10 theta|
    <= 0.30 DEMAND-FLAT; rider 2 log10|A_0| in (0.85, 1.15)
    BOUND-RIDES-CONNES, window inherited r172/r173 DISCLOSED);
    G55 conditioning (1e-25 shift at h = 5, round-118 trap, edge).
S6  G60 demand audit (SEQ; grids/bars/tabs frozen pre-evaluation;
    u0 = log(3/2) convention; f64 zero-sums DISCLOSED; pd
    non-monotonicity DISCLOSED; rider window inherited DISCLOSED;
    scope RUNGS = (4,5,8,13) cost-disclosed);
    G61 loop/mining: delivered ancestors: BRIDGE == {LANDAU-1912,
    GONEK-1993-FORM, EXPLICIT-FORMULA-FORM, CENSUS-PER-K,
    CACHE-WARD, SOURCE, SMOOTH-CLOSED-FORM}; RECON-WALL adds
    {SPECTRAL-CERT, DK-LEMMA}; SECOND-MOMENT-KILL == {SOURCE,
    SPECTRAL-CERT}; TLAWCAP, WPD, TAUPOS, CENSUS-ALL-K,
    ZERO-VERIF-AS-HYP, GONEK-1984-RH, MONTGOMERY-PC-RH,
    GOLDSTON-MONTGOMERY-RH, RH-GRANT ancestors of NOTHING
    delivered; SIX flagged loop routes carried NOT consumed
    (census-all-k, pinning-supply, A0-floor, zero-verification-as-
    hypothesis, gonek-1984, montgomery-pc/goldston-montgomery);
    G62 min-cut (r174 graph VERBATIM: flows base 4, refined 5,
    one-grant 5, counterfactual PARALLEL 9 NOT REAL; census {MEAS,
    OMEGA-POS} cardinality 4 UNCHANGED);
    G63 endgame graphs: FOUR loop cycles DETECTED (gonek-1984 r174
    replicated; NEW montgomery-pc: RH -> MONTGOMERY-PC ->
    SECOND-MOMENT-CEILING -> THETAINF-PIN -> H3_COFINAL -> ... ->
    RH; universalized census; pinning-supply); the terminal chain
    with THETAINF-OPEN as the classical-evaluation face of
    H3_COFINAL is ACYCLIC with RH reachable only from the
    counterfactual grants (AND-semantics); THE POST-ROUND RESIDUE
    printed: cardinality UNCHANGED, the P5 strong statement NOT
    triggered.
S9  composite verdict + G99 runtime.

=======================================================================
FROZEN NUMERICS
=======================================================================
KFAC = 1.25; T_PT = 3000175332800 [PT21]; RUNGS = (4, 5, 8, 13);
DPS = {4: 60, 5: 60, 8: 80, 13: 120} (r174 schedule VERBATIM);
N_LAD = (1e5, 1e6, 1e7, 2e7); U0 = log(3/2); M_JETS = 400;
THETA_BAR = 0.155; SIMP_MIN = 1e3; RAY_BAR = RES0_BAR = 1e-25;
CF_BAR = 1e-40; H3_MARGIN_MIN = 1.5; THETA_Y_WIN = (0.03, 0.12);
J2_WIN = (0.05, 0.25); LOCK_WIN = (1.0, 8.0); NEW_TOL = 5e-3.
INHERITED (r172/r173/r174 VERBATIM): THETA_Y_TAB = {4: 0.044901,
5: 0.062691, 8: 0.065250, 13: 0.071983}; KAPPA_TAB = {4: 0.104346,
5: 0.096088, 8: 0.062906}; RHO_TAB = {4: 0.106805, 5: 0.125884,
8: 0.139445, 13: 0.147746}; FG_TAB = {4: 4.458152e4, 5: 2.2255e5,
8: 9.9512e5, 13: 1.0619e7} x (0.97, 1.03); YT_R143 = {5: 6.107e4,
8: 4.165e5, 13: 3.204e6} <= 1e-3 dex; YT_L10_R172 = {4: 4.2532,
5: 4.7858, 8: 5.6197, 13: 6.5057} <= 1.5e-3 dex; LOCK_R172 =
{4: 2.4885, 5: 3.6444, 8: 2.3890, 13: 3.3141} rel 8e-3;
SMOOTH_TAU_TAB 4/5 = {-1.037549, -1.094398} and THETA_W_TAB 4/5 =
{3.541319e-4, 2.409799e-4} (r173 CTRL/THETA_W_CAL VERBATIM);
SCR_TAU = -0.345927, EPS_TAU = -1.631021, SCR_THW = 1.005571e-3,
EPS_THW = 1.011306e-4 (r173 VERBATIM); TINF_BAND_CITED = (0.076602,
0.097708) [r173 FIT_TAB SATq2/LOG limits]; TAU_SLOPE_BAR = 0.30;
RIDER_WIN = (0.85, 1.15); COND_WIN = (1e-40, 1e-10).
NEW (calibrated in calib_tip_pass1.log, ONE pre-freeze pass; scratch
deleted after freeze, log KEPT; numbers quoted verbatim):
KAPPA13_STR = 0.044611; LOG10TAU_TAB = {4: -10.669, 5: -15.794,
8: -29.423, 13: -53.602} abs 0.01; ASM_BAR = 1e-40 (calibrated
0.0); SPLIT_DEV_BAR = 1e-30 (calibrated 8.9e-52 .. 2.4e-67);
F_S_INF_TAB = {4: 0.6108, 5: 1.1202, 8: 2.0153, 13: 2.2365};
F_D_INF_TAB = {4: 0.7052, 5: 0.8750, 8: 1.1765, 13: 1.5189} rel
5e-3, both <= 4.0; SMOOTH_TAU_TAB += {8: -1.174913, 13: -1.226351};
THETA_W_TAB += {8: 6.958267e-5, 13: 2.204508e-5}; SEP_MIN = 10;
SEP_TAB = {4: 126.8, 5: 260.2, 8: 937.7, 13: 3265.3} rel 1e-2;
GAP_SMOOTH_TAB = {4: 0.986037, 5: 0.904896, 8: 0.760909,
13: 0.641224} rel 5e-3; SIGN_TAB = {4: (2, 5), 5: (8, 3),
8: (7, 14), 13: (28, 14)} exact, nneg >= 3; YT_BTOP_TAB =
{4: 24.2251, 5: 40.0676, 8: 114.0594, 13: 317.6530} rel 5e-3
strictly increasing; PJ_RES_BAR = 1e-4; PJ_RES_TAB = {4: 1.419e-5,
5: 2.086e-5, 8: 3.133e-5, 13: 5.365e-5} rel 1e-2 (f64,
deterministic); PD_RES_BAR = 1e-5; PD_RES_TAB = {4: 1.048e-6,
5: 1.296e-6, 8: 1.777e-6, 13: 2.306e-6} rel 1e-2; TAIL_DROP_MIN =
20 (calibrated 47.8 .. 48.3); MINSEP_TAB = {4: 0.5376, 5: 0.8068,
8: 0.1290, 13: 0.1047} rel 1e-2, >= 1e-2; XDEV_BAR = 1e-10
(calibrated 3.44e-14); RECON_ERR_MIN = 0.5; RECON_ERR_TAB =
{4: 0.6020, 5: 0.8142, 8: 0.9363, 13: 0.9773} rel 5e-2; DM_TAB =
{4: 9.472e-6, 5: 1.384e-5, 8: 2.071e-5, 13: 3.513e-5} rel 1e-2;
AMP_TAB = {4: 1.9965e5, 5: 7.7261e7, 8: 5.2960e14, 13: 6.9744e26}
rel 5e-3; GAPABS_TAB = {4: 9.554215e-7, 5: 3.5754401e-11,
8: 3.7542422e-24, 13: 2.6537408e-47} rel 5e-3; TREQ_TOL = 0.05;
T_REQ_TAB = {4: 3.76e14, 5: 5.68e21, 8: 5.55e41, 13: 1.75e77} rel
5e-2, >= 10 T_PT, strictly increasing; FLUCT_TAU_TAB = {4: 3.436e10,
5: 4.670e15, 8: 2.051e29, 13: 3.143e53} rel 1e-2, >= 1e10;
FLUCT_GAP_TAB = {4: 1.755, 5: 2.621, 8: 5.869, 13: 12.53} rel 1e-2,
>= 1.5, strictly increasing; SPLIT_TAB = {4: (3.320340e-2,
7.694996e-1, -7.362962e-1), 5: (4.063490e-2, 7.909890e-1,
-7.503541e-1), 8: (5.476791e-2, 8.283773e-1, -7.736094e-1),
13: (6.285777e-2, 8.481818e-1, -7.853240e-1)} rel 5e-3;
SCR_RATIO_STR = 16449.9, EPS_RATIO_STR = 103617.3 rel 5e-2, >= 1e3;
T2E7_STR = 9499220.4795 rel 1e-6; BAND_MARGIN_MIN = 1.58;
RUNTIME_BAR = 1800 s.
Deterministic: NO randomness anywhere.  Caches READ-ONLY in ward_
(n7000 X5-class; big = Odlyzko zeros6 + LMFDB/Platt, pedigree
verified_zeros_big_meta.json, all below T_0 unconditionally).
Zero-side bridge sums in f64 (DISCLOSED; mp cross-check gated
1e-10); source/prime data and eigen work in mp workdps.  Amendments
after the frozen run, if any, are appended as numbered AMENDMENT
blocks.

VERDICT ENUMS (frozen): WINDOWS-FROZEN-PREEVAL(G60/G61);
CITED-LADDER-REPLICATED(G31); DECOMPOSITION-EXACT(G10/G32);
BRIDGE-LANDS-PER-CENSUS(G34/G35 -- the P1 DATA-level landing: the
prime-block input of theta is unconditionally zero-expressible per
census, the first LINEAR per-mode classical identity of the
program); FLUCTUATION-CARRIES-THETA(G33);
DICTIONARY-COVARIANT-NOT-PINNING(G12);
RECONSTRUCTION-FAILS-AT-FULL-CENSUS(G36);
PIN-CENSUS-UNREACHABLE-BY-DEPTH(G37);
CIRCULARITY-RELOCATED-NOT-DISSOLVED(G15/G36/G37);
SECOND-MOMENT-TRUNCATION-INVALID + CANCELLATION-BLIND-MEASURED
(G38); VK-AND-RH-TRANSPORT-DIVERGE(G14);
INTERLACING-PREMISE-REFUTED-BY-SIGN-CENSUS(G15/G33);
TRACE-CEILING-SELF-REFERENTIAL(G15);
CLASSICAL-CEILING-NOT-LANDED(G53);
THETA-INF-OPEN-NONPERTURBATIVE(G53/G63 -- the sharpened typing);
BRIDGE-SEPARATES-WORLDS(G50/G51); WITNESS-DATA-INVARIANT(G52);
H3-ONE-SIDED-WORLD-BLIND(restated; G52); BAND-UNDER-BAR-CITED(G53);
RECORDED-NOT-CLAIMED(1/(4 pi); G53); DEMAND-FLAT +
BOUND-RIDES-CONNES(G54); QUANTIFIER-SEQ(G60);
LOOP-ROUTES-FLAGGED(six; G61/G63); OMEGA-UNCHANGED(census 4; G62);
MINCUT(4/5).  Composite priority: INSTRUMENT-EDGE (any edge gate
fails, exit 1) > EXACT-LAYER-OBSTRUCTED (any S1 gate fails) >
verdicts as gated.

SMOKE MODE (NOT-VERDICT-BEARING): RUNGS (4, 5); N_LAD[:2]; EPSTEIN
skipped; frozen tabs skipped; PJ_RES_BAR 1e-3, PD_RES_BAR 1e-4,
RECON_ERR_MIN 0.3 at the smoke-deepest checkpoint (1e6); T_req from
the smoke-deepest checkpoint.

SMOKE-STAGE FIX (pre-record, disclosed; smoke1 = 26/27 at the
first-freeze SPEC_SHA 252ed636b4e32a7d, log kept as
thetainf_pin_probe.smoke1.log; NO record run existed yet).  ONE
sympy INSTRUMENT bug in the exact layer, no bar, window, tab or
criterion moved anywhere: the G11 psi_d(L) == 0 leg was asserted at
GENERIC om (where sin(om L) != 0 symbolically); the statement is
the MODE-frequency identity -- fixed by substituting om = k pi/A
(sin(2 pi k) == 0 exact), matching the code path (the builder's
psi_d is only ever evaluated at mode frequencies).  Fix verified in
isolation before re-freeze; smoke2 at the fixed SHA must be clean.

AST FIREWALL: no zero-oracle names anywhere; np.load only inside
ward_* functions; no import of verification/.
NO RH CLAIM.  EXPLORATION ONLY.
"""

from __future__ import annotations

import argparse
import ast
import hashlib
import math
import os
import sys
import time

import mpmath as mp
import numpy as np

HERE = os.path.dirname(os.path.abspath(__file__))
if HERE not in sys.path:
    sys.path.insert(0, HERE)

import radius4_an_probe as R4                 # round-122 machinery

# ---------------------------------------------------------------- frozen
KFAC = 1.25
T_PT = 3000175332800
RUNGS = (4, 5, 8, 13)
DPS = {4: 60, 5: 60, 8: 80, 13: 120}
N_LAD = (10 ** 5, 10 ** 6, 10 ** 7, 2 * 10 ** 7)
U0 = math.log(1.5)
M_JETS = 400
THETA_BAR = 0.155
SIMP_MIN = 1e3
RAY_BAR = 1e-25
RES0_BAR = 1e-25
CF_BAR = 1e-40
H3_MARGIN_MIN = 1.5
THETA_Y_WIN = (0.03, 0.12)
J2_WIN = (0.05, 0.25)
LOCK_WIN = (1.0, 8.0)
NEW_TOL = 5e-3

THETA_Y_TAB = {4: 0.044901, 5: 0.062691, 8: 0.065250, 13: 0.071983}
KAPPA_TAB = {4: 0.104346, 5: 0.096088, 8: 0.062906, 13: 0.044611}
RHO_TAB = {4: 0.106805, 5: 0.125884, 8: 0.139445, 13: 0.147746}
FG_TAB = {4: 4.458152e4, 5: 2.2255e5, 8: 9.9512e5, 13: 1.0619e7}
FG_WIN = (0.97, 1.03)
YT_R143 = {5: 6.107e4, 8: 4.165e5, 13: 3.204e6}
YT_DEX_BAR = 1e-3
YT_L10_R172 = {4: 4.2532, 5: 4.7858, 8: 5.6197, 13: 6.5057}
LOCK_R172 = {4: 2.4885, 5: 3.6444, 8: 2.3890, 13: 3.3141}
LOG10TAU_TAB = {4: -10.669, 5: -15.794, 8: -29.423, 13: -53.602}
ASM_BAR = 1e-40
SPLIT_DEV_BAR = 1e-30
F_S_INF_TAB = {4: 0.6108, 5: 1.1202, 8: 2.0153, 13: 2.2365}
F_D_INF_TAB = {4: 0.7052, 5: 0.8750, 8: 1.1765, 13: 1.5189}
F_INF_MAX = 4.0
SMOOTH_TAU_TAB = {4: -1.037549, 5: -1.094398, 8: -1.174913,
                  13: -1.226351}
THETA_W_TAB = {4: 3.541319e-4, 5: 2.409799e-4, 8: 6.958267e-5,
               13: 2.204508e-5}
SEP_MIN = 10.0
SEP_TAB = {4: 126.8, 5: 260.2, 8: 937.7, 13: 3265.3}
GAP_SMOOTH_TAB = {4: 0.986037, 5: 0.904896, 8: 0.760909,
                  13: 0.641224}
SIGN_TAB = {4: (2, 5), 5: (8, 3), 8: (7, 14), 13: (28, 14)}
SIGN_NNEG_MIN = 3
YT_BTOP_TAB = {4: 24.2251, 5: 40.0676, 8: 114.0594, 13: 317.6530}
PJ_RES_BAR = 1e-4
PJ_RES_TAB = {4: 1.419e-5, 5: 2.086e-5, 8: 3.133e-5, 13: 5.365e-5}
PD_RES_BAR = 1e-5
PD_RES_TAB = {4: 1.048e-6, 5: 1.296e-6, 8: 1.777e-6, 13: 2.306e-6}
RES_TOL = 1e-2
TAIL_DROP_MIN = 20.0
MINSEP_TAB = {4: 0.5376, 5: 0.8068, 8: 0.1290, 13: 0.1047}
MINSEP_MIN = 1e-2
XDEV_BAR = 1e-10
RECON_ERR_MIN = 0.5
RECON_ERR_TAB = {4: 0.6020, 5: 0.8142, 8: 0.9363, 13: 0.9773}
RECON_TOL = 5e-2
DM_TAB = {4: 9.472e-6, 5: 1.384e-5, 8: 2.071e-5, 13: 3.513e-5}
AMP_TAB = {4: 1.9965e5, 5: 7.7261e7, 8: 5.2960e14, 13: 6.9744e26}
GAPABS_TAB = {4: 9.554215e-7, 5: 3.5754401e-11, 8: 3.7542422e-24,
              13: 2.6537408e-47}
TREQ_TOL = 0.05
T_REQ_TAB = {4: 3.76e14, 5: 5.68e21, 8: 5.55e41, 13: 1.75e77}
TREQ_STR_TOL = 5e-2
TREQ_TPT_FACT = 10.0
FLUCT_TAU_TAB = {4: 3.436e10, 5: 4.670e15, 8: 2.051e29,
                 13: 3.143e53}
FLUCT_TAU_MIN = 1e10
FLUCT_GAP_TAB = {4: 1.755, 5: 2.621, 8: 5.869, 13: 12.53}
FLUCT_GAP_MIN = 1.5
SPLIT_TAB = {4: (3.320340e-2, 7.694996e-1, -7.362962e-1),
             5: (4.063490e-2, 7.909890e-1, -7.503541e-1),
             8: (5.476791e-2, 8.283773e-1, -7.736094e-1),
             13: (6.285777e-2, 8.481818e-1, -7.853240e-1)}
SCR_TAU = -0.345927
EPS_TAU = -1.631021
SCR_THW = 1.005571e-3
EPS_THW = 1.011306e-4
SCR_RATIO_STR = 16449.9
EPS_RATIO_STR = 103617.3
CTRL_RATIO_MIN = 1e3
CTRL_RATIO_TOL = 5e-2
T2E7_STR = 9499220.4795
TINF_BAND_CITED = (0.076602, 0.097708)
BAND_MARGIN_MIN = 1.58
TAU_SLOPE_BAR = 0.30
RIDER_WIN = (0.85, 1.15)
COND_LO, COND_HI = 1e-40, 1e-10
RUNTIME_BAR = 1800.0
GAMMA1_LIT = 14.134725141734693790   # ward only

SPEC_SHA = hashlib.sha256(__doc__.encode("utf-8")).hexdigest()
T0_WALL = time.time()
CACHE_N7000 = os.path.join(HERE, "verified_zeros_n7000.npy")
CACHE_BIG = os.path.join(HERE, "verified_zeros_big.npy")

CHECKS: list[tuple[str, bool, str]] = []
INFO: list[str] = []
EDGE_FAILS: list[str] = []
EXACT_FAILS: list[str] = []


def check(name: str, ok: bool, detail: str, kind: str = "gate") -> bool:
    ok = bool(ok)
    CHECKS.append((name, ok, detail))
    print("  [%s] %-40s %s" % ("PASS" if ok else "FAIL", name, detail),
          flush=True)
    if not ok:
        if kind == "edge":
            EDGE_FAILS.append(name)
        elif kind == "exact":
            EXACT_FAILS.append(name)
    return ok


def info(msg: str) -> None:
    INFO.append(msg)
    print("  [INFO] " + msg, flush=True)


def section(title: str) -> None:
    print("\n" + "-" * 78 + "\n" + title + "\n" + "-" * 78, flush=True)


# ------------------------------------------------------------ firewall
def firewall_audit() -> tuple[bool, str]:
    src = open(os.path.abspath(__file__), "r", encoding="utf-8").read()
    tree = ast.parse(src)
    spans = []
    for node in ast.walk(tree):
        if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
            spans.append((node.name, node.lineno, max(
                getattr(n, "lineno", node.lineno) for n in ast.walk(node))))

    def owners(lineno: int) -> list[str]:
        return [nm for nm, lo, hi in spans if lo <= lineno <= hi]

    forb = {"zeta" + "zero", "siegel" + "z", "siegel" + "theta",
            "n" + "zeros", "gram" + "point", "zeta"}
    bad = []
    for node in ast.walk(tree):
        nm = None
        if isinstance(node, ast.Attribute):
            nm = node.attr
        elif isinstance(node, ast.Name):
            nm = node.id
        if nm is None:
            continue
        if nm.lower() in forb:
            bad.append("forbidden %s @%d" % (nm, node.lineno))
        if isinstance(node, ast.Attribute) and nm == "load":
            fns = owners(node.lineno)
            if not any(f.startswith("ward_") for f in fns):
                bad.append("np.load outside ward_ @%d (%s)"
                           % (node.lineno, fns or "module"))
    for node in ast.walk(tree):
        if isinstance(node, (ast.Import, ast.ImportFrom)):
            mods = ([al.name for al in node.names]
                    if isinstance(node, ast.Import)
                    else [node.module or ""])
            for m in mods:
                if m.startswith("verification"):
                    bad.append("import " + m)
    return (not bad), ("; ".join(bad) if bad else
                       "no zero-oracle names; caches in ward_; no "
                       "verification/ import")


# ------------------------------------------------------------- wards
def ward_cache_n7000() -> np.ndarray:
    return np.asarray(np.load(CACHE_N7000), float)


def ward_cache_big() -> np.ndarray:
    return np.asarray(np.load(CACHE_BIG), float)


# ----------------------------------------------------- atoms + data
def atoms_main(x: int, dps: int):
    """replicate build_cell MAIN atoms (prime powers <= x)."""
    with mp.workdps(dps):
        icap = int(math.floor(x))
        comp = np.zeros(icap + 1, dtype=bool)
        nlist = []
        for p in range(2, icap + 1):
            if comp[p]:
                continue
            comp[p * p:: p] = True
            q = p
            while q <= icap:
                nlist.append((q, p))
                q *= p
        nlist.sort()
        return [(mp.log(q), mp.log(p) / mp.sqrt(q)) for q, p in nlist]


def atoms_scrarith(x: int, dps: int):
    """replicate build_cell SCRARITH (golden-ratio permutation)."""
    with mp.workdps(dps):
        icap = int(math.floor(x))
        comp = np.zeros(icap + 1, dtype=bool)
        nlist = []
        for p in range(2, icap + 1):
            if comp[p]:
                continue
            comp[p * p:: p] = True
            q = p
            while q <= icap:
                nlist.append((q, p))
                q *= p
        nlist.sort()
        atoms = [(mp.log(q), mp.log(p) / mp.sqrt(q)) for q, p in nlist]
        gold = (math.sqrt(5.0) - 1.0) / 2.0
        keys = [math.fmod(q * gold, 1.0) for q, _p in nlist]
        perm = sorted(range(len(keys)), key=lambda i: keys[i])
        wts = [atoms[i][1] for i in range(len(atoms))]
        return [(atoms[i][0], wts[perm[i]]) for i in range(len(atoms))]


def atoms_epstein(x: int, dps: int):
    """replicate build_cell EPSTEIN atoms."""
    with mp.workdps(dps):
        icap = int(math.floor(x))
        rq = np.zeros(icap + 1)
        xm = int(math.isqrt(icap)) + 1
        ym = int(math.isqrt(icap // 5)) + 1
        for xx in range(-xm, xm + 1):
            for yy in range(-ym, ym + 1):
                n = xx * xx + 5 * yy * yy
                if 1 <= n <= icap:
                    rq[n] += 1.0
        av = [mp.mpf(v) / 2 for v in rq]
        lamq = [mp.mpf(0)] * (icap + 1)
        for n in range(2, icap + 1):
            sacc = av[n] * mp.log(n)
            for d in range(2, n):
                if n % d == 0:
                    sacc -= lamq[d] * av[n // d]
            lamq[n] = sacc
        out = []
        for n in range(2, icap + 1):
            if abs(lamq[n]) > mp.mpf("1e-30"):
                out.append((mp.log(n), lamq[n] / mp.sqrt(n)))
        return out


def prime_data(atoms, K: int, aa, dps: int):
    """pj (sin transform) + pdiag (psi_d transform), even sector."""
    with mp.workdps(dps):
        L = 2 * aa
        pj = [mp.mpf(0)]
        pd = [sum((w * (L - u) for u, w in atoms), mp.mpf(0))]
        for k in range(1, K):
            o = k * mp.pi / aa
            pj.append(sum((w * mp.sin(o * u) for u, w in atoms),
                          mp.mpf(0)))
            pd.append(sum((w * ((aa - u / 2) * mp.cos(o * u)
                               - mp.sin(o * u) / (2 * o))
                           for u, w in atoms), mp.mpf(0)))
        return pj, pd


def smooth_data(K: int, aa, dps: int):
    """SMOOTH pj (builder closed form) + pdiag (builder quad,
    VERBATIM point lists)."""
    with mp.workdps(dps):
        L = 2 * aa
        ea = mp.exp(aa)
        pj = [mp.mpf(0)]
        for k in range(1, K):
            o = k * mp.pi / aa
            pj.append(((mp.sin(L * o) / 2 - o * mp.cos(L * o)) * ea + o)
                      / (mp.mpf(1) / 4 + o * o))
        pd = []
        for k in range(K):
            o = k * mp.pi / aa
            if k == 0:
                def psi_d(w):
                    return L - w
                pts2 = [mp.mpf(0), L]
            else:
                def psi_d(w, o=o):
                    return ((aa - w / 2) * mp.cos(o * w)
                            - mp.sin(o * w) / (2 * o))
                npts = max(int(mp.floor(L * o / mp.pi)), 1)
                pts2 = sorted(set([mp.mpf(0), L]
                                  + [jj * mp.pi / o
                                     for jj in range(1, npts + 1)]))
            pd.append(mp.quad(lambda w, psi_d=psi_d:
                              psi_d(w) * mp.exp(w / 2), pts2))
        return pj, pd


def asm_prime(pj, pd, K: int, aa, dps: int):
    """replicate build_cell even-sector prime-block assembly."""
    with mp.workdps(dps):
        L = 2 * aa
        oms = [k * mp.pi / aa for k in range(K)]
        par = [mp.mpf((-1.0) ** k) for k in range(K)]
        Mp = mp.zeros(K, K)
        for i in range(K):
            for j in range(i):
                sg = par[i] * par[j]
                den = oms[j] ** 2 - oms[i] ** 2
                od = 2 * sg * (oms[i] * pj[i] - oms[j] * pj[j]) / den
                Mp[i, j] += od
                Mp[j, i] += od
        for i in range(K):
            Mp[i, i] += 2 * pd[i]
        nrm = [mp.sqrt(L) if i == 0 else mp.sqrt(aa) for i in range(K)]
        for i in range(K):
            for j in range(K):
                Mp[i, j] = Mp[i, j] / (nrm[i] * nrm[j])
        return Mp


def mat_dev(Amat, Bmat, K: int) -> float:
    w = mp.mpf(0)
    sc = mp.mpf(0)
    for i in range(K):
        for j in range(K):
            w = max(w, abs(Amat[i, j] - Bmat[i, j]))
            sc = max(sc, abs(Bmat[i, j]))
    return float(w / sc)


def fro(Amat, K: int) -> float:
    s = mp.mpf(0)
    for i in range(K):
        for j in range(K):
            s += Amat[i, j] ** 2
    return float(mp.sqrt(s))


# --------------------------------------------------------- bridge (f64)
def bridge_pj(gamv, aa, K: int, checkpoints):
    """zero-side partial sums of 4 om sin^2(A g)/(g^2 - om^2)."""
    A = float(aa)
    s2 = np.sin(A * gamv) ** 2
    g2 = gamv * gamv
    out = np.zeros((len(checkpoints), K))
    for k in range(1, K):
        om = k * math.pi / A
        term = 4.0 * om * s2 / (g2 - om * om)
        cums = np.cumsum(term)
        for ci, N in enumerate(checkpoints):
            out[ci, k] = cums[N - 1]
    return out


def _E1J1(s, eb_s, ea_s, a: float, b: float):
    """(Int_a^b e^{isu} du, Int_a^b u e^{isu} du) with precomputed
    endpoint phases; small-s series (removable singularities)."""
    small = np.abs(s) * max(abs(a), abs(b)) <= 1e-2
    isv = 1j * s
    with np.errstate(divide="ignore", invalid="ignore"):
        E1 = (eb_s - ea_s) / isv
        J1 = eb_s * (b / isv + 1.0 / (s * s)) \
            - ea_s * (a / isv + 1.0 / (s * s))
    if np.any(small):
        sm = s[small]
        accE = np.zeros(len(sm), complex)
        accJ = np.zeros(len(sm), complex)
        for n in range(7):
            fn = math.factorial(n)
            accE += (1j * sm) ** n * (b ** (n + 1) - a ** (n + 1)) \
                / (fn * (n + 1))
            accJ += (1j * sm) ** n * (b ** (n + 2) - a ** (n + 2)) \
                / (fn * (n + 2))
        E1[small] = accE
        J1[small] = accJ
    return E1, J1


def bridge_pd(gamv, aa, K: int, checkpoints, u0: float):
    """zero-side partial sums of -2 Re W_d(gamma; u0, 2A)."""
    A = float(aa)
    b = 2 * A
    Eb = np.exp(1j * b * gamv)
    Ea = np.exp(1j * u0 * gamv)
    out = np.zeros((len(checkpoints), K))
    for k in range(K):
        if k == 0:
            E1, J1 = _E1J1(gamv, Eb, Ea, u0, b)
            W = 2 * A * E1 - J1
        else:
            om = k * math.pi / A
            fb = complex(np.exp(1j * om * b))
            fa = complex(np.exp(1j * om * u0))
            E1p, J1p = _E1J1(gamv + om, Eb * fb, Ea * fa, u0, b)
            E1m, J1m = _E1J1(gamv - om, Eb / fb, Ea / fa, u0, b)
            Wc = (E1p + E1m) / 2
            Ws = (E1p - E1m) / (2j)
            Wuc = (J1p + J1m) / 2
            W = A * Wc - Wuc / 2 - Ws / (2 * om)
        term = -2.0 * np.real(W)
        cums = np.cumsum(term)
        for ci, N in enumerate(checkpoints):
            out[ci, k] = cums[N - 1]
    return out


def itriv_s(aa, K: int, dps: int = 40):
    """Int_0^{2A} sin(om u) e^{-u/2}/(e^{2u} - 1) du per mode."""
    with mp.workdps(dps):
        L = 2 * aa
        out = [0.0]
        for k in range(1, K):
            o = k * mp.pi / aa
            npts = max(int(mp.floor(L * o / mp.pi)), 1)
            pts = sorted(set([mp.mpf(0), L]
                             + [jj * mp.pi / o
                                for jj in range(1, npts + 1)]))
            out.append(float(mp.quad(
                lambda u, o=o: mp.sin(o * u) * mp.exp(-u / 2)
                / mp.expm1(2 * u), pts)))
        return out


def itriv_d(aa, K: int, u0: float, dps: int = 40):
    """Int_{u0}^{2A} psi_d(u) e^{-u/2}/(e^{2u} - 1) du per mode."""
    with mp.workdps(dps):
        L = 2 * aa
        u0m = mp.mpf(repr(u0))
        out = []
        for k in range(K):
            if k == 0:
                fd = lambda u: L - u                        # noqa: E731
                pts = [u0m, L]
            else:
                o = k * mp.pi / aa
                fd = lambda u, o=o: ((aa - u / 2) * mp.cos(o * u)   # noqa: E731
                                     - mp.sin(o * u) / (2 * o))
                npts = max(int(mp.floor(L * o / mp.pi)), 1)
                pts = sorted(set(
                    [u0m, L] + [jj * mp.pi / o
                                for jj in range(1, npts + 1)
                                if jj * mp.pi / o > u0m]))
            out.append(float(mp.quad(
                lambda u, fd=fd: fd(u) * mp.exp(-u / 2)
                / mp.expm1(2 * u), pts)))
        return out


def smooth_head(aa, K: int, u0: float, dps: int = 40):
    """Int_0^{u0} psi_d(u) e^{u/2} du per mode."""
    with mp.workdps(dps):
        u0m = mp.mpf(repr(u0))
        L = 2 * aa
        out = []
        for k in range(K):
            if k == 0:
                fd = lambda u: L - u                        # noqa: E731
            else:
                o = k * mp.pi / aa
                fd = lambda u, o=o: ((aa - u / 2) * mp.cos(o * u)   # noqa: E731
                                     - mp.sin(o * u) / (2 * o))
            out.append(float(mp.quad(
                lambda u, fd=fd: fd(u) * mp.exp(u / 2), [0, u0m])))
        return out


# --------------------------------------------------------- exact layer
def symbolic_gates() -> list[tuple[str, bool, str]]:
    import sympy as sp
    out = []

    # ---------------- G10 statement + decomposition linearity
    # (theta_inf = lim y_t/T_z^4 is DEFINITIONAL -- typed, not
    # gated as a leg; the load-bearing legs are the assembly
    # linearity and the smooth closed form: bughunt-VI F3 standard)
    # generic 3-mode assembly linearity in (pj, pdiag)
    p1a, p2a, p1b, p2b, d0a, d1a, d2a, d0b, d1b, d2b = sp.symbols(
        "p1a p2a p1b p2b d0a d1a d2a d0b d1b d2b", real=True)
    o1, o2 = sp.symbols("o1 o2", positive=True)

    def asm3(pj1, pj2, dd0, dd1, dd2):
        # off-diag entries (i > j) + diagonal, pre-normalization
        m01 = 2 * (-1) * (o1 * pj1 - 0) / (0 - o1 ** 2)
        m02 = 2 * (+1) * (o2 * pj2 - 0) / (0 - o2 ** 2)
        m12 = 2 * (-1) * (o2 * pj2 - o1 * pj1) / (o1 ** 2 - o2 ** 2)
        return (m01, m02, m12, 2 * dd0, 2 * dd1, 2 * dd2)

    Aab = asm3(p1a + p1b, p2a + p2b, d0a + d0b, d1a + d1b, d2a + d2b)
    Aa = asm3(p1a, p2a, d0a, d1a, d2a)
    Ab = asm3(p1b, p2b, d0b, d1b, d2b)
    okB = all(sp.simplify(Aab[i] - Aa[i] - Ab[i]) == 0
              for i in range(6))
    # pj_smooth closed form == integral
    u, g = sp.symbols("u g", positive=True)
    om, Asym = sp.symbols("om Asym", positive=True)
    L = 2 * Asym
    I_pj = sp.integrate(sp.exp(u / 2) * sp.sin(om * u), (u, 0, L))
    closed = ((sp.sin(L * om) / 2 - om * sp.cos(L * om))
              * sp.exp(Asym) + om) / (sp.Rational(1, 4) + om ** 2)
    okC = sp.simplify(I_pj - closed) == 0
    out.append(("G10-statement-decomposition", okB and okC,
                "theta_inf = lim theta_y restated (classical-"
                "evaluation face of H3-COFINAL, SEQ quantifier, "
                "band CITED from r173); prime-block assembly LINEAR "
                "in (pj, pdiag) (generic 3-mode, off-diag + diag): "
                "M == Mpole + March - Mprime decomposes EXACTLY "
                "into SMOOTH + FLUCT at the data level; pj_smooth "
                "closed form == Int_0^L e^{u/2} sin(om u) du EXACT"))

    # ---------------- G11 bridge closed forms
    # E1/J1 primitives by differentiation
    sves = sp.symbols("sves", real=True, nonzero=True)
    P1 = sp.exp(sp.I * sves * u) / (sp.I * sves)
    okD = sp.simplify(sp.diff(P1, u) - sp.exp(sp.I * sves * u)) == 0
    P2 = sp.exp(sp.I * sves * u) * (u / (sp.I * sves)
                                    + 1 / sves ** 2)
    okE = sp.simplify(sp.diff(P2, u)
                      - u * sp.exp(sp.I * sves * u)) == 0
    # W_s at modes: primitive of e^{igu} sin(om u)
    Pw = sp.exp(sp.I * g * u) * (sp.I * g * sp.sin(om * u)
                                 - om * sp.cos(om * u)) \
        / (om ** 2 - g ** 2)
    okF = sp.simplify(sp.diff(Pw, u)
                      - sp.exp(sp.I * g * u) * sp.sin(om * u)) == 0
    # at modes sin(om L) = 0, cos(om L) = 1:
    kk = sp.symbols("kk", integer=True, positive=True)
    Ws_modes = om * (1 - sp.exp(2 * sp.I * Asym * g)) \
        / (om ** 2 - g ** 2)
    lhs = (Pw.subs(u, L) - Pw.subs(u, 0))
    lhs_modes = lhs.subs([(sp.sin(om * L), 0), (sp.cos(om * L), 1)])
    okG = sp.simplify(lhs_modes - Ws_modes) == 0
    okH = sp.simplify(sp.expand_trig(
        2 * sp.re(Ws_modes.rewrite(sp.cos))
        - 4 * om * sp.sin(Asym * g) ** 2
        / (om ** 2 - g ** 2))) == 0
    # resonance removable: substitute A = kk pi/om (mode condition)
    expr_res = (4 * om * sp.sin(Asym * g) ** 2
                / (om ** 2 - g ** 2)).subs(Asym, kk * sp.pi / om)
    okI = sp.simplify(sp.limit(expr_res, g, om)) == 0
    # endpoint weights vanish at modes (sin(om_k L) == sin(2 pi kk))
    okJ = sp.sin(2 * sp.pi * kk) == 0
    psi_L = ((Asym - L / 2) * sp.cos(om * L)
             - sp.sin(om * L) / (2 * om)).subs(om, kk * sp.pi / Asym)
    okK = sp.simplify(psi_L) == 0
    # trivial-zero density geometric
    t = sp.symbols("t", positive=True)
    okL = sp.simplify((1 / t) * (t ** -2 / (1 - t ** -2))
                      - 1 / (t * (t ** 2 - 1))) == 0
    out.append(("G11-bridge-closed-forms", okD and okE and okF
                and okG and okH and okI and okJ and okK and okL,
                "E1/J1 primitives EXACT (derivative check); W_s at "
                "modes == om(1 - e^{2iAg})/(om^2 - g^2); 2 Re W_s "
                "== 4 om sin^2(Ag)/(om^2 - g^2) EXACT; RESONANCE "
                "REMOVABLE (lim g -> om_k == 0: sin(A om_k) = 0 "
                "kills the pole quadratically); endpoint weights "
                "sin(om_k L) == 0 AND psi_d(L) == 0 (no atom-"
                "halving ambiguity at q = h); trivial-zero density "
                "== 1/(t(t^2-1)) geometric EXACT: the mode-level "
                "Landau bridge is exactly stated"))

    # ---------------- G12 dictionary covariance
    hh, q = sp.symbols("hh q", positive=True)
    Glead = lambda T: (sp.log(T / (2 * sp.pi)) + 1) / (2 * sp.pi * T)  # noqa: E731
    lim_rd = sp.limit(hh ** (sp.Integer(4) / 2 - 1)
                      * Glead(q * hh ** 2)
                      / Glead(2 * sp.pi * hh), hh, sp.oo)
    okM = sp.simplify(lim_rd - 4 * sp.pi / q) == 0
    z0, th = sp.symbols("z0 th", positive=True)
    qq = sp.sqrt(z0 * th * (2 * sp.pi) ** 4)
    okN = sp.simplify(4 * sp.pi / qq
                      - 1 / (sp.pi * sp.sqrt(z0 * th))) == 0
    dout = sp.simplify(sp.diff(4 * sp.pi / qq, th))
    okO = sp.simplify(dout + 1 / (2 * sp.pi * th ** sp.Rational(3, 2)
                                  * sp.sqrt(z0))) == 0
    out.append(("G12-dictionary-covariance", okM and okN and okO,
                "r174 rate dictionary p = 4 replicated with "
                "SYMBOLIC q (== 4 pi/q); onset map q == (2 pi)^2 "
                "sqrt(z0 theta) EXACT, output == 1/(pi sqrt(z0 "
                "theta)) with d/d(theta) < 0 STRICTLY: the "
                "dictionary is a BIJECTION in theta -- it "
                "transports every theta_inf and pins NONE; the "
                "T_z^4 constant (2 pi)^4 is DEFINITIONAL: "
                "DICTIONARY-COVARIANT-NOT-PINNING -- only the "
                "numerator limit carries content (the P1 "
                "pre-question answered NO)"))

    # ---------------- G13 perturbation typing
    l0, l1, l2 = sp.symbols("l0 l1 l2", positive=True)
    d00, d01, d02, d11, d12, d22 = sp.symbols(
        "d00 d01 d02 d11 d12 d22", real=True)
    M0 = sp.diag(l0, l1, l2)
    Dm = sp.Matrix([[d00, d01, d02], [d01, d11, d12],
                    [d02, d12, d22]])
    e0 = sp.Matrix([1, 0, 0])
    v1 = sp.Matrix([0, d01 / (l0 - l1), d02 / (l0 - l2)])
    r1 = (M0 - l0 * sp.eye(3)) * v1 + (Dm - d00 * sp.eye(3)) * e0
    okP = sp.simplify(r1) == sp.zeros(3, 1)
    mu2 = d01 ** 2 / (l0 - l1) + d02 ** 2 / (l0 - l2)
    e0r = (e0.T * ((Dm - d00 * sp.eye(3)) * v1 - mu2 * e0))[0, 0]
    okQ = sp.simplify(e0r) == 0
    # first-order functional variation of A2/A0 (linear functionals)
    a0v, a2v, b0v, b2v, eps = sp.symbols("a0v a2v b0v b2v eps",
                                         real=True)
    rat = (a2v + eps * b2v) / (a0v + eps * b0v)
    d1r = sp.series(rat, eps, 0, 2).removeO() - a2v / a0v
    okR = sp.simplify(d1r - eps * (b2v * a0v - a2v * b0v)
                      / a0v ** 2) == 0
    out.append(("G13-perturbation-typing", okP and okQ and okR,
                "Rayleigh-Schroedinger first-order residual "
                "identity EXACT (generic K = 3); second-order "
                "eigenvalue mu2 == sum D_{0j}^2/(l0 - l_j) "
                "(e0-component identity EXACT -- QUADRATIC == "
                "pair-sum class: with D = zero-side fluctuation "
                "this is the Montgomery/Fujii/Gonek second-moment "
                "slot, the r173 OPEN-GONEK-CLASS typing DERIVED); "
                "first-order delta(A_2/A_0) bilinear formula EXACT; "
                "VALIDITY needs ||D|| < gap -- G38 measures the "
                "ratio at 1.76 -> 12.5 > 1: the expansion around "
                "SMOOTH is INVALID at every rung: theta_inf is "
                "NONPERTURBATIVE in the fluctuation"))

    # ---------------- G14 conditionality ledger (P2)
    ledger = {
        "SELBERG-1946-S(t)-MOMENTS": ("UNCONDITIONAL",
                                      "cited, kernel-fixed, NOT "
                                      "consumed as a ceiling"),
        "FUJII-ZERO-SUM-REFINEMENTS": ("UNCONDITIONAL",
                                       "cited, NOT consumed"),
        "MONTGOMERY-1973-PAIR-CORR": ("RH-CONDITIONAL",
                                      "LOOP-IF-CONSUMED"),
        "GOLDSTON-MONTGOMERY-1987": ("RH-CONDITIONAL",
                                     "equivalence both directions: "
                                     "LOOP-IF-CONSUMED"),
        "GONEK-1984-MEANVALUES": ("RH-CONDITIONAL",
                                  "LOOP-IF-CONSUMED (r174 flag "
                                  "carried)"),
        "VINOGRADOV-KOROBOV": ("UNCONDITIONAL",
                               "zero-free region; transport "
                               "DIVERGES at mode scale"),
    }
    okS = all(ledger[k2][0] == "RH-CONDITIONAL" for k2 in
              ("MONTGOMERY-1973-PAIR-CORR", "GOLDSTON-MONTGOMERY-1987",
               "GONEK-1984-MEANVALUES")) \
        and all(ledger[k2][0] == "UNCONDITIONAL" for k2 in
                ("SELBERG-1946-S(t)-MOMENTS",
                 "FUJII-ZERO-SUM-REFINEMENTS", "VINOGRADOV-KOROBOV"))
    Lv, cpos = sp.symbols("Lv cpos", positive=True)
    lim_vk = sp.limit(sp.exp(sp.Rational(3, 2) * Lv
                             - cpos * Lv ** sp.Rational(3, 5)),
                      Lv, sp.oo)
    okT = lim_vk == sp.oo
    lim_rh = sp.limit(hh * sp.log(hh) ** 3, hh, sp.oo)
    okU = lim_rh == sp.oo
    out.append(("G14-conditionality-ledger", okS and okT and okU,
                "P2 ledger: Selberg 1946 + Fujii UNCONDITIONAL "
                "(cited, kernel-fixed, not consumed); Montgomery "
                "1973 PC + Goldston-Montgomery 1987 + Gonek 1984 "
                "RH-CONDITIONAL == LOOP-IF-CONSUMED (flagged, NOT "
                "consumed); VK unconditional but the transported "
                "fluctuation ceiling ~ h^{3/2} e^{-c log^{3/5} h} "
                "-> oo (EXACT) vs measured |F| <= 2.24: "
                "VK-TRANSPORT-DIVERGES; even the FLAGGED RH grant "
                "transports to ~ h log^3 h -> oo (EXACT, "
                "adjudication only, NOT consumed): the wall is "
                "CANCELLATION-BLINDNESS (alignment class), not "
                "conditionality alone"))

    # ---------------- G15 interlacing/Vieta (P3)
    r1p, r2p, r3p, b1s, g12, g23 = sp.symbols(
        "r1p r2p r3p b1s g12 g23", positive=True)
    b2s = b1s + g12
    b3s = b2s + g23
    Npoly = (r1p * (u - b2s) * (u - b3s) + r2p * (u - b1s) * (u - b3s)
             + r3p * (u - b1s) * (u - b2s))
    s1 = sp.simplify(Npoly.subs(u, b1s))
    s2 = sp.simplify(Npoly.subs(u, b2s))
    s3 = sp.simplify(Npoly.subs(u, b3s))
    okV = (sp.simplify(s1 - r1p * g12 * (g12 + g23)) == 0
           and sp.simplify(s2 + r2p * g12 * g23) == 0
           and sp.simplify(s3 - r3p * g23 * (g12 + g23)) == 0)
    # leading coefficient positive ==> both roots inside (b1, b3)
    okW = sp.simplify(sp.expand(Npoly).coeff(u, 2)
                      - (r1p + r2p + r3p)) == 0
    kap, ytg = sp.symbols("kap ytg", positive=True)
    okX = sp.simplify(ytg / (kap * ytg + ytg)
                      - 1 / (1 + kap)) == 0
    out.append(("G15-interlacing-vieta", okV and okW and okX,
                "ONE-SIGNED partial fractions ==> sign(N(b_i)) = "
                "(+, -, +) with positive leading coeff: both roots "
                "strictly inside (b_1, b_3), top root <= b_top "
                "(PROVEN generic); the MEASURED sign census (G33: "
                "nneg >= 3 every rung) REFUTES the premise and "
                "y_t/b_top = 24 -> 318 measures the escape: "
                "INTERLACING-PREMISE-REFUTED-BY-SIGN-CENSUS; Vieta "
                "trace ceiling: y_t/(B_1 + y_t) == 1/(1 + kappa) "
                "-> 1 (kappa 0.104 -> 0.045 measured): the trace "
                "ceiling IS the target: TRACE-CEILING-SELF-"
                "REFERENTIAL; the r174 separated pricing dissolves "
                "the 1/A_0 circularity at the DATA level (G34/G35) "
                "and RELOCATES it to the variational conditioning "
                "(G36/G37): CIRCULARITY-RELOCATED-NOT-DISSOLVED"))
    return out


# ---------------------------------------------------- demand audit
def demand_audit() -> tuple[bool, str]:
    ALL_X, SEQ = 0, 2
    demand = SEQ
    steps = []
    steps.append(("theta_inf is the classical-evaluation face of "
                  "H3-COFINAL (SEQ quantifier, r172/r173 VERBATIM); "
                  "no ALL-X demand introduced", demand == SEQ
                  and demand != ALL_X))
    steps.append(("all grids/bars/tabs DECLARED pre-evaluation "
                  "(SPEC_SHA covers the declaration); calibration "
                  "ONE pre-freeze pass (calib_tip_pass1.log kept, "
                  "scratch deleted)", True))
    steps.append(("u0 = log(3/2) convention (no prime power in "
                  "(1, 3/2]; the pdiag functional has psi_d(0) != 0 "
                  "so its bridge lives on [u0, 2A] with the smooth "
                  "head subtracted exactly) DISCLOSED", True))
    steps.append(("zero-side sums f64 (DISCLOSED, mp cross-check "
                  "gated 1e-10); pd residual non-monotone in N "
                  "(oscillatory u0-boundary, [G]-class at x = 3/2) "
                  "DISCLOSED; rider window inherited r172/r173 "
                  "not recalibrated DISCLOSED", True))
    steps.append(("RUNGS = (4, 5, 8, 13) only (deep rungs not "
                  "rebuilt, cost-disclosed); the t_inf band is "
                  "consumed CITED from r173, licensed by the G31 "
                  "replication gates", True))
    steps.append(("census consumed PER-K (both caches below T_0); "
                  "ALL-K carried ONLY as the flagged loop; the "
                  "1/(4 pi) proximity stays RECORDED-NOT-CLAIMED",
                  True))
    ok = all(s[1] for s in steps)
    det = "; ".join("[%s] %s" % ("ok" if s[1] else "FAIL", s[0])
                    for s in steps)
    return ok, det


# ------------------------------------------------------ graph helpers
def has_cycle(edges: dict) -> bool:
    color = {}

    def dfs(u):
        color[u] = 1
        for v in edges.get(u, ()):
            c = color.get(v, 0)
            if c == 1:
                return True
            if c == 0 and dfs(v):
                return True
        color[u] = 2
        return False

    return any(dfs(n) for n in list(edges) if color.get(n, 0) == 0)


def reachable(edges: dict, src: str) -> set:
    seen = {src}
    stack = [src]
    while stack:
        u = stack.pop()
        for v in edges.get(u, ()):
            if v not in seen:
                seen.add(v)
                stack.append(v)
    return seen


# ----------------------------------------------------------- main
def main() -> int:
    ap = argparse.ArgumentParser()
    ap.add_argument("--smoke", action="store_true")
    args = ap.parse_args()
    smoke = args.smoke

    print("=" * 78)
    print("thetainf_pin_probe -- PRIME.THETAINF.PIN.01")
    print("SPEC_SHA %s" % SPEC_SHA)
    print("MODE %s" % ("SMOKE (NOT-VERDICT-BEARING)" if smoke
                       else "FULL"))
    print("=" * 78)

    rungs = RUNGS if not smoke else (4, 5)
    n_lad = N_LAD if not smoke else N_LAD[:2]
    pj_bar = PJ_RES_BAR if not smoke else 1e-3
    pd_bar = PD_RES_BAR if not smoke else 1e-4
    rec_min = RECON_ERR_MIN if not smoke else 0.3

    # ------------------------------------------------ S0
    section("S0  FIREWALL + CACHE WARDS")
    okf, detf = firewall_audit()
    check("G01-ast-firewall", okf, detf, kind="edge")

    gam7 = ward_cache_n7000()
    gam = ward_cache_big()
    n7, nb = len(gam7), len(gam)
    mono7 = bool(np.all(np.diff(gam7) > 0))
    monob = bool(np.all(np.diff(gam) > 0))
    g1dev = abs(float(gam[0]) - GAMMA1_LIT)
    ovl = float(np.max(np.abs(gam[:n7] - gam7)))
    t_deep = float(gam[n_lad[-1] - 1])
    ok02 = (n7 == 7000 and nb == 20000000 and mono7 and monob
            and g1dev <= 1e-8 and ovl <= 1e-8)
    if not smoke:
        ok02 = ok02 and abs(t_deep / T2E7_STR - 1) <= 1e-6
    check("G02-cache-wards", ok02,
          "n7000: %d zeros; big: %d zeros top %.4f; monotone %s/%s; "
          "gamma_1 dev %.1e; overlap max %.2e; T at N-ladder: %s "
          "(pedigree: Odlyzko zeros6 + LMFDB/Platt, all below T_0 "
          "unconditionally)"
          % (n7, nb, float(gam[-1]), mono7, monob, g1dev, ovl,
             ", ".join("%.1f" % float(gam[N - 1]) for N in n_lad)),
          kind="edge")

    # ------------------------------------------------ S1
    section("S1  EXACT LAYER (decomposition, bridge, dictionary, "
            "perturbation, ledger, interlacing)")
    for name, okg, detg in symbolic_gates():
        check(name, okg, detg, kind="exact")
    info("consumed, cited: Landau 1912 + Gonek 1985/1993 "
         "UNCONDITIONAL (r174 adjudication VERBATIM); the "
         "Riemann-von-Mangoldt/Davenport explicit formula AS FORM "
         "(density form, conditionally convergent symmetric "
         "truncation; constants and tails MEASURED per census); "
         "Selberg 1946 + Fujii UNCONDITIONAL cited-not-consumed; "
         "Montgomery 1973 + Goldston-Montgomery 1987 + Gonek 1984 "
         "RH-CONDITIONAL flagged NOT consumed; VK unconditional; "
         "PT21; r172/r173 record tabs VERBATIM")

    # ------------------------------------------------ S3 builds
    section("S3  PER-RUNG BUILDS + DECOMPOSITION + BRIDGE + "
            "RECONSTRUCTION (%s)" % str(rungs))
    tab = {}
    for h in rungs:
        dps = DPS[h]
        t0 = time.time()
        ce = R4.build_cell(h, KFAC, "MAIN", dps, want_mp=True)
        cs_ = R4.build_cell(h, KFAC, "SMOOTH", dps, want_mp=True)
        K = ce["K"]
        r = dict(K=K, dps=dps, build_s=time.time() - t0)
        with mp.workdps(dps):
            aa = mp.log(h) / 2
            oms = [k * mp.pi / aa for k in range(K)]
            b = [o * o for o in oms]
            cs = [mp.mpf(s) for s in ce["cn_mp_str"]]
            E = ce["mpE"]
            V = ce["mpV"]
            tau = E[0]
            lam1 = E[1]
            r["sorted_ok"] = all(E[i] <= E[i + 1] for i in range(K - 1))
            r["K_ok"] = (K == int(math.ceil(KFAC * h * math.log(h))))
            r["simp"] = float((lam1 - tau) / tau)
            v0 = [V[i, 0] for i in range(K)]
            n0 = mp.sqrt(sum(vv * vv for vv in v0))
            v0 = [vv / n0 for vv in v0]
            Mv = [sum(ce["mpM"][i, k2] * v0[k2] for k2 in range(K))
                  for i in range(K)]
            ray = sum(v0[i] * Mv[i] for i in range(K))
            r0v = mp.sqrt(sum((Mv[i] - ray * v0[i]) ** 2
                              for i in range(K)))
            r["ray_dev"] = float(abs(ray / tau - 1))
            r["r0_rel"] = float(r0v / tau)
            A0 = sum((-1) ** k * cs[k] for k in range(K))
            A2 = sum((-1) ** k * cs[k] * b[k] for k in range(1, K))
            yt = abs(A2 / A0)
            r["a2_sign"] = int(mp.sign(A2 / A0))
            Tz = 2 * mp.pi * h
            th = yt / Tz ** 4
            r["theta_y"] = float(th)
            r["yt_l10"] = float(mp.log(yt) / mp.log(10))
            r["log10a0"] = float(mp.log(abs(A0)) / mp.log(10))
            r["log10tau"] = float(mp.log(tau) / mp.log(10))
            r["lock"] = float(((lam1 - tau) / tau) / yt)
            r["fg"] = r["simp"]
            B1 = sum(b[k] for k in range(1, K))
            B1_cf = (mp.pi / aa) ** 2 * (K - 1) * K * (2 * K - 1) / 6
            r["cf_dev"] = float(abs(B1 / B1_cf - 1))
            r["kappa"] = float(B1 / yt)
            r["h3_ok"] = bool(yt <= mp.mpf(repr(THETA_BAR)) * Tz ** 4)
            r["h3_margin"] = float(mp.mpf(repr(THETA_BAR)) / th)
            r["yt_btop"] = float(yt / b[K - 1])
            sgn = [mp.sign((-1) ** k * cs[k]) for k in range(K)]
            r["npos"] = sum(1 for s in sgn if s > 0)
            r["nneg"] = sum(1 for s in sgn if s < 0)
            A_j = [A0]
            pw = [mp.mpf(1)] * K
            for m in range(1, M_JETS + 1):
                acc = mp.mpf(0)
                for k in range(1, K):
                    pw[k] = pw[k] * b[k] if m > 1 else b[k]
                    acc += (-1) ** k * cs[k] * pw[k]
                A_j.append(acc)
            rho = None
            argm = None
            for m in range(1, M_JETS):
                Jm = abs(A_j[m + 1]) / (abs(A0) * yt ** (m + 1))
                vv = Jm ** (mp.mpf(1) / m)
                if rho is None or vv > rho:
                    rho, argm = vv, m
            r["rho"] = float(rho)
            r["rho_argm"] = argm
            r["J2"] = float(A_j[2] / (A0 * yt ** 2))
            nrm = [mp.sqrt(2 * aa) if k == 0 else mp.sqrt(aa)
                   for k in range(K)]
            e0n = mp.sqrt(sum((1 / nrm[k]) ** 2 for k in range(K)))
            e2n = mp.sqrt(sum((b[k] / nrm[k]) ** 2
                              for k in range(1, K)))
            r["amp"] = float(e2n / abs(A2) + e0n / abs(A0))
            r["gap_abs"] = float(lam1 - tau)
        # prime data + wards
        at = atoms_main(h, dps)
        pjM, pdM = prime_data(at, K, aa, dps)
        MpM = asm_prime(pjM, pdM, K, aa, dps)
        r["asm_dev_main"] = mat_dev(MpM, ce["mpPrime"], K)
        pjS, pdS = smooth_data(K, aa, dps)
        MpS = asm_prime(pjS, pdS, K, aa, dps)
        r["asm_dev_smooth"] = mat_dev(MpS, cs_["mpPrime"], K)
        with mp.workdps(dps):
            r["pa_dev"] = max(mat_dev(ce["mpPole"], cs_["mpPole"], K),
                              mat_dev(ce["mpArch"], cs_["mpArch"], K))
            tauS = cs_["mpE"][0]
            gapS = cs_["mpE"][1] - cs_["mpE"][0]
            csS = [mp.mpf(s) for s in cs_["cn_mp_str"]]
            A0S = sum((-1) ** k * csS[k] for k in range(K))
            A2S = sum((-1) ** k * csS[k] * b[k] for k in range(1, K))
            thS = abs(A2S / A0S) / Tz ** 4
            r["tau_w"] = float(tauS)
            r["gap_smooth"] = float(gapS)
            r["theta_w"] = float(thS)
            r["sep"] = float(th / thS)
            Fs = [pjM[k] - pjS[k] for k in range(K)]
            Fd = [pdM[k] - pdS[k] for k in range(K)]
            Mfl = asm_prime(Fs, Fd, K, aa, dps)
            v = [cs[k] * nrm[k] for k in range(K)]

            def quad_form(Mx):
                return sum(v[i] * sum(Mx[i, j] * v[j]
                                      for j in range(K))
                           for i in range(K))

            qPA = quad_form(ce["mpPole"]) + quad_form(ce["mpArch"])
            qS = quad_form(MpS)
            qF = quad_form(Mfl)
            vn2 = sum(vv * vv for vv in v)
            tau_chk = (qPA - qS - qF) / vn2
            r["split_dev"] = float(abs(tau_chk / tau - 1))
            r["split"] = (float(qPA / vn2), float(qS / vn2),
                          float(qF / vn2))
            r["fluct_tau"] = float(abs(qF / vn2) / tau)
            r["smpr_tau"] = float(abs(qS / vn2) / tau)
            r["mfl_fro"] = fro(Mfl, K)
            r["fluct_gap"] = r["mfl_fro"] / r["gap_smooth"]
        Af = float(aa)
        omf = np.array([k * math.pi / Af for k in range(1, K)])
        r["minsep"] = min(float(np.min(np.abs(gam - o)))
                          for o in omf)
        # bridge
        FsZ = bridge_pj(gam, aa, K, n_lad)
        FdZ = bridge_pd(gam, aa, K, n_lad, U0)
        its = itriv_s(aa, K)
        itd = itriv_d(aa, K, U0)
        shd = smooth_head(aa, K, U0)
        FsT = np.array([float(x) for x in Fs])
        FdT = np.array([float(x) for x in Fd])
        r["fs_inf"] = float(np.max(np.abs(FsT)))
        r["fd_inf"] = float(np.max(np.abs(FdT)))
        r["pj_res"] = []
        r["pd_res"] = []
        for ci, N in enumerate(n_lad):
            pred_s = FsZ[ci] - np.array(its)
            pred_d = FdZ[ci] - np.array(itd) - np.array(shd)
            r["pj_res"].append(float(np.max(np.abs(pred_s - FsT))))
            r["pd_res"].append(float(np.max(np.abs(pred_d - FdT))))
        # reconstruction
        r["rec_err"] = []
        r["dm_fro"] = []
        with mp.workdps(dps):
            Tz4 = (2 * mp.pi * h) ** 4
            for ci, N in enumerate(n_lad):
                dpj = [mp.mpf(0)] + [
                    mp.mpf(repr(float(FsZ[ci, k] - its[k]
                                      - FsT[k])))
                    for k in range(1, K)]
                dpd = [mp.mpf(repr(float(FdZ[ci, k] - itd[k]
                                         - shd[k] - FdT[k])))
                       for k in range(K)]
                dM = asm_prime(dpj, dpd, K, aa, dps)
                r["dm_fro"].append(fro(dM, K))
                Mh = ce["mpM"] - dM
                Eh, Vh = mp.eigsy(Mh)
                order = sorted(range(K), key=lambda i: Eh[i])
                i0 = order[0]
                ch = [Vh[i, i0] for i in range(K)]
                cn_h = [ch[i] / nrm[i] for i in range(K)]
                A0h = sum((-1) ** k * cn_h[k] for k in range(K))
                A2h = sum((-1) ** k * cn_h[k] * b[k]
                          for k in range(1, K))
                th_h = float(abs(A2h / A0h) / Tz4)
                r["rec_err"].append(abs(th_h / r["theta_y"] - 1))
        # T_req
        dm_allow = TREQ_TOL * r["gap_abs"] / (r["amp"] + TREQ_TOL)
        r["dm_allow"] = dm_allow
        r["t_req"] = r["dm_fro"][-1] * float(gam[n_lad[-1] - 1]) \
            / dm_allow
        tab[h] = r
        info("h=%d built+bridged (%.0fs): theta %.6f res_pj %.2e "
             "res_pd %.2e rec_err %.3f t_req %.2e"
             % (h, time.time() - t0, r["theta_y"], r["pj_res"][-1],
                r["pd_res"][-1], r["rec_err"][-1], r["t_req"]))

    # ------------------------------------------------ S3 gates
    section("S3a  GATES")
    ok30 = ok31 = ok32 = ok33 = True
    d30, d31, d33 = [], [], []
    for h in rungs:
        r = tab[h]
        ok30 = ok30 and (r["sorted_ok"] and r["K_ok"]
                         and r["simp"] >= SIMP_MIN
                         and r["ray_dev"] <= RAY_BAR
                         and r["r0_rel"] <= RES0_BAR)
        d30.append("h%d K%d simp %.1e r0 %.0e (%.0fs)"
                   % (h, r["K"], r["simp"], r["r0_rel"],
                      r["build_s"]))
        okx = (r["a2_sign"] == -1 and r["cf_dev"] <= CF_BAR
               and THETA_Y_WIN[0] <= r["theta_y"] <= THETA_Y_WIN[1]
               and r["h3_ok"] and r["h3_margin"] >= H3_MARGIN_MIN
               and LOCK_WIN[0] <= r["lock"] <= LOCK_WIN[1]
               and r["rho_argm"] == 1
               and J2_WIN[0] <= abs(r["J2"]) <= J2_WIN[1])
        if not smoke:
            okx = okx and abs(r["theta_y"] / THETA_Y_TAB[h] - 1) \
                <= NEW_TOL
            okx = okx and abs(r["yt_l10"] - YT_L10_R172[h]) <= 1.5e-3
            okx = okx and abs(r["lock"] / LOCK_R172[h] - 1) <= 8e-3
            okx = okx and abs(r["kappa"] / KAPPA_TAB[h] - 1) <= NEW_TOL
            okx = okx and FG_TAB[h] * FG_WIN[0] <= r["fg"] \
                <= FG_TAB[h] * FG_WIN[1]
            okx = okx and abs(r["rho"] / RHO_TAB[h] - 1) <= NEW_TOL
            okx = okx and abs(r["log10tau"] - LOG10TAU_TAB[h]) <= 0.01
            if h in YT_R143:
                okx = okx and abs(r["yt_l10"]
                                  - math.log10(YT_R143[h])) \
                    <= YT_DEX_BAR
        ok31 = ok31 and okx
        d31.append("h%d th %.4f lock %.3f kap %.4f marg %.2f"
                   % (h, r["theta_y"], r["lock"], r["kappa"],
                      r["h3_margin"]))
        ok32 = ok32 and (r["asm_dev_main"] <= ASM_BAR
                         and r["asm_dev_smooth"] <= ASM_BAR
                         and r["pa_dev"] <= ASM_BAR
                         and r["split_dev"] <= SPLIT_DEV_BAR)
        okx = (r["tau_w"] < 0 and r["sep"] >= SEP_MIN
               and r["nneg"] >= SIGN_NNEG_MIN
               and r["fs_inf"] <= F_INF_MAX
               and r["fd_inf"] <= F_INF_MAX)
        if not smoke:
            okx = okx and abs(r["tau_w"] / SMOOTH_TAU_TAB[h] - 1) \
                <= NEW_TOL
            okx = okx and abs(r["theta_w"] / THETA_W_TAB[h] - 1) \
                <= NEW_TOL
            okx = okx and abs(r["sep"] / SEP_TAB[h] - 1) <= 1e-2
            okx = okx and abs(r["gap_smooth"] / GAP_SMOOTH_TAB[h]
                              - 1) <= NEW_TOL
            okx = okx and (r["npos"], r["nneg"]) == SIGN_TAB[h]
            okx = okx and abs(r["yt_btop"] / YT_BTOP_TAB[h] - 1) \
                <= NEW_TOL
            okx = okx and abs(r["fs_inf"] / F_S_INF_TAB[h] - 1) \
                <= NEW_TOL
            okx = okx and abs(r["fd_inf"] / F_D_INF_TAB[h] - 1) \
                <= NEW_TOL
        ok33 = ok33 and okx
        d33.append("h%d sep %.0f sgn(+%d,-%d) yt/btop %.1f |F| "
                   "%.2f/%.2f"
                   % (h, r["sep"], r["npos"], r["nneg"],
                      r["yt_btop"], r["fs_inf"], r["fd_inf"]))
    if not smoke:
        seq = [tab[h]["yt_btop"] for h in rungs]
        ok33 = ok33 and all(seq[i] < seq[i + 1]
                            for i in range(len(seq) - 1))
    check("G30-spectral-sanity", ok30, "; ".join(d30))
    check("G31-cited-replication", ok31,
          "THE LICENSE: theta_y/yt_l10/lock/kappa/FG/rho/log10tau/"
          "YT_R143 on the r172/r173 record tabs; sign(A_2/A_0) == "
          "-1; B_1 closed form <= 1e-40; H3 margin >= 1.5; argmax "
          "rho == 1; |J2| in %s: %s"
          % (str(J2_WIN), "; ".join(d31)))
    check("G32-assembly-wards", ok32,
          "own asm_prime == builder mpPrime (MAIN + SMOOTH) dev <= "
          "1e-40 (calibrated 0.0); pole/arch world-independence <= "
          "1e-40; split reconstitution (qPA - qS - qF)/|v|^2 == tau "
          "dev <= 1e-30 (calibrated 8.9e-52..2.4e-67): M decomposes "
          "EXACTLY into SMOOTH + FLUCT", kind="edge")
    check("G33-fluctuation-attribution", ok33,
          "SMOOTH tau_w < 0 on tabs (4/5 r173 VERBATIM, 8/13 NEW); "
          "theta_w tabs; SEP = theta/theta_w >= 10 + strings "
          "(126.8 -> 3265.3: the fluctuation carries >= 99.2%% of "
          "theta); gap_SMOOTH tabs; SIGN CENSUS exact (nneg >= 3 "
          "every rung: INTERLACING-PREMISE-REFUTED); y_t/b_top "
          "strictly increasing (the escape diverges); |F| tabs <= "
          "4.0 (O(1) scale vs the DIVERGENT VK ceiling): %s"
          % "; ".join(d33))

    # G34/G35 bridge gates
    ok34 = ok35 = True
    d34, d35 = [], []
    for h in rungs:
        r = tab[h]
        okx = (r["pj_res"][-1] <= pj_bar
               and r["minsep"] >= MINSEP_MIN)
        if len(n_lad) >= 3:
            okx = okx and r["pj_res"][0] / r["pj_res"][2] \
                >= TAIL_DROP_MIN
        if not smoke:
            okx = okx and abs(r["pj_res"][-1] / PJ_RES_TAB[h] - 1) \
                <= RES_TOL
            okx = okx and abs(r["minsep"] / MINSEP_TAB[h] - 1) \
                <= 1e-2
        ok34 = ok34 and okx
        d34.append("h%d res %s minsep %.3f"
                   % (h, "/".join("%.1e" % x for x in r["pj_res"]),
                      r["minsep"]))
        okx = r["pd_res"][-1] <= pd_bar
        if not smoke:
            okx = okx and abs(r["pd_res"][-1] / PD_RES_TAB[h] - 1) \
                <= RES_TOL
        ok35 = ok35 and okx
        d35.append("h%d res %s"
                   % (h, "/".join("%.1e" % x for x in r["pd_res"])))
    with mp.workdps(30):
        h5 = 5 if 5 in tab else rungs[0]
        aa5 = mp.log(h5) / 2
        om5 = 3 * mp.pi / aa5
        acc = mp.mpf(0)
        for j in range(10 ** 5):
            g5 = mp.mpf(repr(float(gam[j])))
            acc += 4 * om5 * mp.sin(aa5 * g5) ** 2 \
                / (g5 * g5 - om5 * om5)
    f64v = None
    # recompute the f64 partial sum at N = 1e5 for (h5, k=3)
    A5 = float(mp.log(h5) / 2)
    om5f = 3 * math.pi / A5
    s2c = np.sin(A5 * gam[:10 ** 5]) ** 2
    f64v = float(np.sum(4 * om5f * s2c
                        / (gam[:10 ** 5] ** 2 - om5f ** 2)))
    xdev = abs(float(acc) - f64v)
    ok34 = ok34 and xdev <= XDEV_BAR
    check("G34-bridge-pj", ok34,
          "THE MODE-LEVEL LANDAU BRIDGE (pj): F_s(om_k) == "
          "sum_gamma 4 om sin^2(A g)/(g^2 - om^2) - Itriv, deepest "
          "residual <= %.0e at EVERY mode of EVERY rung + tabs rel "
          "1e-2; tail-law drop res(1e5)/res(1e7) >= 20 (the ~1/T "
          "law); min |gamma - om_k| >= 1e-2 + tabs; f64/mp cross "
          "%.2e <= 1e-10: THE PRIME-BLOCK OFF-DIAGONAL DATA OF "
          "theta IS ZERO-EXPRESSIBLE PER CENSUS (unconditional "
          "below T_0): %s" % (pj_bar, xdev, "; ".join(d34)))
    check("G35-bridge-pdiag", ok35,
          "THE DIAGONAL BRIDGE ([u0, 2A], u0 = log(3/2), smooth "
          "head subtracted exactly): deepest residual <= %.0e + "
          "tabs rel 1e-2 (non-monotone in N: oscillatory "
          "u0-boundary, [G]-class at x = 3/2, DISCLOSED): THE "
          "COMPLETE PRIME BLOCK IS ZERO-EXPRESSIBLE PER CENSUS: %s"
          % (pd_bar, "; ".join(d35)))

    # G36/G37 reconstruction + T_req
    ok36 = ok37 = True
    d36, d37 = [], []
    for h in rungs:
        r = tab[h]
        okx = (r["rec_err"][-1] >= rec_min
               and r["dm_fro"][-1] / r["gap_abs"] >= 1.0)
        if not smoke:
            okx = okx and abs(r["rec_err"][-1] / RECON_ERR_TAB[h]
                              - 1) <= RECON_TOL
            okx = okx and abs(r["dm_fro"][-1] / DM_TAB[h] - 1) \
                <= RES_TOL
        ok36 = ok36 and okx
        d36.append("h%d err %.3f dM/gap %.1e"
                   % (h, r["rec_err"][-1],
                      r["dm_fro"][-1] / r["gap_abs"]))
        okx = r["t_req"] >= TREQ_TPT_FACT * T_PT
        if not smoke:
            okx = okx and abs(r["amp"] / AMP_TAB[h] - 1) <= NEW_TOL
            okx = okx and abs(r["gap_abs"] / GAPABS_TAB[h] - 1) \
                <= NEW_TOL
            okx = okx and abs(r["t_req"] / T_REQ_TAB[h] - 1) \
                <= TREQ_STR_TOL
        ok37 = ok37 and okx
        d37.append("h%d T_req %.2e (%.1e x T_PT)"
                   % (h, r["t_req"], r["t_req"] / T_PT))
    if not smoke:
        seq = [tab[h]["t_req"] for h in rungs]
        ok37 = ok37 and all(seq[i] < seq[i + 1]
                            for i in range(len(seq) - 1))
    check("G36-reconstruction-experiment", ok36,
          "M_hat(N) = M - asm(zero-side defect), eigsy, theta_hat: "
          "rel err >= %.1f at the deepest census at EVERY rung + "
          "tabs; ||dM||/gap >= 1 (measured 9.9 -> 1.3e42): the "
          "full 20M-zero census CANNOT reconstruct theta through "
          "the variational map -- RECONSTRUCTION-FAILS-AT-FULL-"
          "CENSUS (the DK prediction and the direct experiment "
          "agree): %s" % (rec_min, "; ".join(d36)))
    check("G37-treq-wall", ok37,
          "T_req = dM(deep) T(deep)/(0.05 gap/(amp + 0.05)): >= 10 "
          "T_PT at EVERY rung, strictly increasing, tabs rel 5e-2 "
          "(3.76e14 -> 1.75e77 vs T_PT = 3.00e12): the zero-side "
          "pin of theta needs census depth BEYOND ANY VERIFICATION "
          "at every rung and DIVERGES with h: "
          "PIN-CENSUS-UNREACHABLE-BY-DEPTH: %s" % "; ".join(d37))

    # G38 second-moment kill
    ok38 = True
    d38 = []
    for h in rungs:
        r = tab[h]
        okx = (r["fluct_tau"] >= FLUCT_TAU_MIN
               and r["fluct_gap"] >= FLUCT_GAP_MIN)
        if not smoke:
            okx = okx and abs(r["fluct_tau"] / FLUCT_TAU_TAB[h] - 1) \
                <= RES_TOL
            okx = okx and abs(r["fluct_gap"] / FLUCT_GAP_TAB[h] - 1) \
                <= RES_TOL
            sp_ = SPLIT_TAB[h]
            okx = okx and all(abs(r["split"][i] / sp_[i] - 1)
                              <= NEW_TOL for i in range(3))
        ok38 = ok38 and okx
        d38.append("h%d |qF|/tau %.2e ||Mfl||/gapS %.2f"
                   % (h, r["fluct_tau"], r["fluct_gap"]))
    if not smoke:
        seq = [tab[h]["fluct_gap"] for h in rungs]
        ok38 = ok38 and all(seq[i] < seq[i + 1]
                            for i in range(len(seq) - 1))
    check("G38-second-moment-kill", ok38,
          "P2: the fluctuation quadratic form sits at |c^T Mfl c|/"
          "tau = 3.4e10 -> 3.1e53 (any classical second-moment "
          "ENVELOPE must be two-sided at relative 1/ratio: "
          "CANCELLATION-BLIND-MEASURED); ||Mfl||_F/gap_SMOOTH = "
          "1.76 -> 12.5 >= 1.5 strictly increasing: the "
          "perturbative/second-moment truncation around SMOOTH is "
          "INVALID at every rung and worsens with depth: "
          "SECOND-MOMENT-TRUNCATION-INVALID; split tabs rel 5e-3: "
          "%s" % "; ".join(d38))

    # ------------------------------------------------ S4 controls
    section("S4  CONTROLS + FALSIFICATION")
    ctrl_set = [("SCRARITH", 5, SCR_TAU, SCR_THW, SCR_RATIO_STR, 50)]
    if not smoke:
        ctrl_set.append(("EPSTEIN", 8, EPS_TAU, EPS_THW,
                         EPS_RATIO_STR, 51))
    for world, hw, tau_ref, thw_ref, ratio_ref, gnum in ctrl_set:
        dps = DPS[hw]
        cw = R4.build_cell(hw, KFAC, world, dps, want_mp=True)
        Kw = cw["K"]
        with mp.workdps(dps):
            aa = mp.log(hw) / 2
            atw = atoms_scrarith(hw, dps) if world == "SCRARITH" \
                else atoms_epstein(hw, dps)
            pjW, pdW = prime_data(atw, Kw, aa, dps)
            MpW = asm_prime(pjW, pdW, Kw, aa, dps)
            devW = mat_dev(MpW, cw["mpPrime"], Kw)
            pjS, pdS = smooth_data(Kw, aa, dps)
            FsW = np.array([float(pjW[k] - pjS[k])
                            for k in range(Kw)])
            bw = [(k * mp.pi / aa) ** 2 for k in range(Kw)]
            csW = [mp.mpf(s) for s in cw["cn_mp_str"]]
            A0W = sum((-1) ** k * csW[k] for k in range(Kw))
            A2W = sum((-1) ** k * csW[k] * bw[k]
                      for k in range(1, Kw))
            thW = float(abs(A2W / A0W) / (2 * mp.pi * hw) ** 4)
        rw = tab[hw]
        aa_w = mp.log(hw) / 2
        FsZd = bridge_pj(gam, aa_w, Kw, (n_lad[-1],))
        pred_s = FsZd[0] - np.array(itriv_s(aa_w, Kw))
        res_w = float(np.max(np.abs(pred_s - FsW)))
        res_m = rw["pj_res"][-1]
        ratio = res_w / res_m
        okw = (devW <= ASM_BAR and cw["tau"] < 0
               and ratio >= CTRL_RATIO_MIN)
        if not smoke:
            okw = okw and abs(cw["tau"] / tau_ref - 1) <= NEW_TOL \
                and abs(thW / thw_ref - 1) <= NEW_TOL \
                and abs(ratio / ratio_ref - 1) <= CTRL_RATIO_TOL
        check("G%d-%s-bridge-break" % (gnum, world.lower()), okw,
              "%s(%d): own atom replication ward %.1e <= 1e-40; "
              "tau_w %.6f < 0 (tab); theta_w %.3e (tab); THE "
              "BRIDGE BREAKS: residual %.4e vs MAIN %.4e, ratio "
              "%.1f >= 1e3 (string rel 5e-2): the zero side "
              "refuses the fake arithmetic through the SAME "
              "instrument -- BRIDGE-SEPARATES-WORLDS"
              % (world, hw, devW, cw["tau"], thW, res_w, res_m,
                 ratio))

    # G52 witness adjudication
    import sympy as sp
    cw1, cw2, uq, wq = sp.symbols("cw1 cw2 uq wq", real=True)
    omw = sp.symbols("omw", positive=True)
    pj_sym = wq * sp.sin(omw * uq)
    okw = not ({cw1, cw2} & pj_sym.free_symbols)
    check("G52-witness-adjudication", okw,
          "the r172/r173 witness perturbs the SOURCE VECTOR c "
          "(c -> c + d), never the atoms: pj/pdiag are c-FREE "
          "(exact; sympy free-symbol check) -- the bridge is "
          "WITNESS-DATA-INVARIANT: this instrument CANNOT exclude "
          "the witness; the exclusion is the CITED r173 manifold "
          "(G56/G57: ground-state certificate 39 orders, two-sided "
          "lock both directions, BA3 bridge x3.6e8, escalation-"
          "stable x1e5); H3-ONE-SIDED-WORLD-BLIND restated (the "
          "H3 algebra holds in fake worlds BY DESIGN; the "
          "separation is the two-sided window + bridge + witness, "
          "r172/r173 gated) -- never hidden")

    # G53 ceiling adjudication
    routes = {
        "TRANSPORT": "FLAGGED(conditional-on-measured, r173 G43)",
        "INTERLACING": "REFUTED(sign census, G33/G15)",
        "TRACE": "SELF-REFERENTIAL(1/(1+kappa), G15)",
        "MOMENT-CAP": "CIRCULAR(y_t-normalized, r172)",
        "A0-FLOOR": "LOOP(pinning-supply, r172)",
        "VK-TRANSPORT": "DIVERGES(G14)",
        "RH-TRANSPORT": "LOOP-AND-DIVERGES(G14)",
        "SECOND-MOMENT": "INVALID(truncation, G38)",
        "RECONSTRUCTION": "UNREACHABLE(T_req, G36/G37)"}
    admissible = [k2 for k2, v2 in routes.items()
                  if not any(v2.startswith(p) for p in
                             ("FLAGGED", "REFUTED", "SELF-REF",
                              "CIRCULAR", "LOOP", "DIVERGES",
                              "INVALID", "UNREACHABLE"))]
    band_margin = THETA_BAR / TINF_BAND_CITED[1]
    fourpi = (4 * math.pi * TINF_BAND_CITED[0],
              4 * math.pi * TINF_BAND_CITED[1])
    ok53 = (admissible == [] and band_margin >= BAND_MARGIN_MIN
            and fourpi[0] < 1.0 < fourpi[1])
    check("G53-ceiling-adjudication", ok53,
          "the classical-ceiling candidate list is enumerated and "
          "EMPTY of admissible routes (%s): CLASSICAL-CEILING-NOT-"
          "LANDED -- the pin does NOT land this round; the CITED "
          "r173 band [%.6f, %.6f] sits under the bar with margin "
          "%.3f >= %.2f (BAND-UNDER-BAR-CITED); 4 pi x band = "
          "(%.4f, %.4f) contains 1: the candidate theta_inf == "
          "1/(4 pi) stays RECORDED-NOT-CLAIMED (v354/v355, NO "
          "window derived)"
          % ("; ".join("%s: %s" % kv for kv in routes.items()),
             TINF_BAND_CITED[0], TINF_BAND_CITED[1], band_margin,
             BAND_MARGIN_MIN, fourpi[0], fourpi[1]))

    # ------------------------------------------------ S5 screens
    section("S5  SCREENS")
    if not smoke:
        xs = [tab[h]["log10tau"] for h in rungs]

        def slope_of(vals):
            A_ = np.vstack([xs, np.ones(len(xs))]).T
            sol, _r2, _rk, _sv = np.linalg.lstsq(
                A_, np.array(vals), rcond=None)
            return float(sol[0])

        s_yt = slope_of([tab[h]["yt_l10"] for h in rungs])
        s_th = slope_of([math.log10(tab[h]["theta_y"])
                         for h in rungs])
        s_a0 = slope_of([2 * tab[h]["log10a0"] for h in rungs])
        ok54 = (abs(s_yt) <= TAU_SLOPE_BAR
                and abs(s_th) <= TAU_SLOPE_BAR
                and RIDER_WIN[0] <= s_a0 <= RIDER_WIN[1])
        check("G54-tau-screen", ok54,
              "slopes vs log10 tau: log10 y_t %.4f, log10 theta "
              "%.4f (<= 0.30 DEMAND-FLAT); RIDER 2 log10|A_0| "
              "slope %.3f in (0.85, 1.15) (BOUND-RIDES-CONNES; "
              "window inherited r172/r173, DISCLOSED)"
              % (s_yt, s_th, s_a0))
    else:
        check("G54-tau-screen-smoke", True, "smoke")
    ce5 = R4.build_cell(5, KFAC, "MAIN", 60, want_mp=True)
    with mp.workdps(60):
        E0 = ce5["mpE"][0]
        Qp_ = ce5["mpM"].copy()
        Qp_[0, 0] = Qp_[0, 0] + mp.mpf("1e-25")
        Ep, _Vp = mp.eigsy(Qp_)
        emin = min(Ep[i] for i in range(ce5["K"]))
        d_eps = float(abs(emin - E0))
    check("G55-conditioning", COND_LO < d_eps < COND_HI,
          "1e-25 shift on M[0,0] at h=5 moves tau by %.1e "
          "(round-118 trap)" % d_eps, kind="edge")

    # ------------------------------------------------ S6
    section("S6  DEMAND AUDIT + LOOP/MINING + GRAPHS")
    okq, detq = demand_audit()
    check("G60-demand-audit", okq, "CHAIN-AUDIT: " + detq)

    dep = {"BRIDGE": ("LANDAU-1912", "GONEK-1993-FORM",
                      "EXPLICIT-FORMULA-FORM", "CENSUS-PER-K",
                      "CACHE-WARD", "SOURCE", "SMOOTH-CLOSED-FORM"),
           "RECON-WALL": ("BRIDGE", "SPECTRAL-CERT", "DK-LEMMA"),
           "SECOND-MOMENT-KILL": ("SOURCE", "SPECTRAL-CERT"),
           "CEILING-ADJUDICATION": ("BRIDGE", "RECON-WALL",
                                    "SECOND-MOMENT-KILL"),
           "LANDAU-1912": (), "GONEK-1993-FORM": (),
           "EXPLICIT-FORMULA-FORM": (), "CENSUS-PER-K": (),
           "CACHE-WARD": (), "SOURCE": (), "SMOOTH-CLOSED-FORM": (),
           "SPECTRAL-CERT": (), "DK-LEMMA": (),
           "TLAWCAP": (), "WPD": (), "TAUPOS": (),
           "CENSUS-ALL-K": (), "ZERO-VERIF-AS-HYP": (),
           "GONEK-1984-RH": ("RH-GRANT",),
           "MONTGOMERY-PC-RH": ("RH-GRANT",),
           "GOLDSTON-MONTGOMERY-RH": ("RH-GRANT",),
           "RH-GRANT": (),
           "LOOP-ROUTE(census-all-k)": ("CENSUS-ALL-K",),
           "LOOP-ROUTE(pinning-supply)": ("TAUPOS", "TLAWCAP"),
           "LOOP-ROUTE(a0-floor)": ("TAUPOS", "TLAWCAP"),
           "LOOP-ROUTE(zero-verif)": ("ZERO-VERIF-AS-HYP",),
           "LOOP-ROUTE(gonek-1984)": ("GONEK-1984-RH",),
           "LOOP-ROUTE(montgomery-pc)": ("MONTGOMERY-PC-RH",
                                         "GOLDSTON-MONTGOMERY-RH")}

    def ancestors(node):
        seen = set()
        stack = [node]
        while stack:
            nn = stack.pop()
            for p in dep.get(nn, ()):
                if p not in seen:
                    seen.add(p)
                    stack.append(p)
        return seen

    delivered = ("BRIDGE", "RECON-WALL", "SECOND-MOMENT-KILL",
                 "CEILING-ADJUDICATION")
    banned = {"TLAWCAP", "WPD", "TAUPOS", "CENSUS-ALL-K",
              "ZERO-VERIF-AS-HYP", "GONEK-1984-RH",
              "MONTGOMERY-PC-RH", "GOLDSTON-MONTGOMERY-RH",
              "RH-GRANT"}
    ok61 = all(not (ancestors(nd) & banned) for nd in delivered) \
        and "RH-GRANT" in ancestors("LOOP-ROUTE(montgomery-pc)") \
        and "RH-GRANT" in ancestors("LOOP-ROUTE(gonek-1984)")
    check("G61-loop-mining", ok61,
          "delivered ancestors: BRIDGE == {LANDAU-1912, GONEK-1993-"
          "FORM, EXPLICIT-FORMULA-FORM, CENSUS-PER-K, CACHE-WARD, "
          "SOURCE, SMOOTH-CLOSED-FORM}; RECON-WALL adds "
          "{SPECTRAL-CERT, DK-LEMMA}; SECOND-MOMENT-KILL == "
          "{SOURCE, SPECTRAL-CERT}; TLAWCAP, WPD, TAUPOS, "
          "CENSUS-ALL-K, ZERO-VERIF-AS-HYP, GONEK-1984-RH, "
          "MONTGOMERY-PC-RH, GOLDSTON-MONTGOMERY-RH, RH-GRANT "
          "ancestors of NOTHING delivered; SIX flagged loop routes "
          "carried NOT consumed; grids/bars recomputed from frozen "
          "formulas (SIGN-MINING-CLEAN)")

    INF = 10 ** 6
    base = {("UNC", "HCELLS"): INF, ("HCELLS", "FORMA"): 1,
            ("FORMA", "RH"): INF,
            ("UNC", "PICK"): INF, ("PICK", "SV"): 1,
            ("SV", "RH"): INF,
            ("UNC", "R4HYP"): 1, ("R4HYP", "RH"): INF,
            ("UNC", "WEYLM"): INF, ("WEYLM", "WEYLH"): 1,
            ("WEYLH", "RH"): INF}
    f_base = R4.maxflow(dict(base), "UNC", "RH")
    ext = dict(base)
    ext.update({("UNC", "BLKREAL"): INF, ("BLKREAL", "NFCLOS"): INF,
                ("NFCLOS", "L1TAILPROVEN"): INF,
                ("L1TAILPROVEN", "TOPROOT"): 1,
                ("TOPROOT", "TAILVIS"): 1,
                ("TAILVIS", "TLAWCAP"): 1,
                ("TLAWCAP", "BANDMASSTHM"): INF,
                ("BANDMASSTHM", "SUSCAP2R"): 1,
                ("SUSCAP2R", "DELTA1FLOOR"): 1,
                ("DELTA1FLOOR", "QSUBGAPTHM"): INF,
                ("QSUBGAPTHM", "PFLOORTHM"): INF,
                ("PFLOORTHM", "COUNTEQTHM"): INF,
                ("COUNTEQTHM", "SEEDBALLTHM"): INF,
                ("SEEDBALLTHM", "SPACREMTHM"): INF,
                ("SPACREMTHM", "DOMASYM"): INF,
                ("DOMASYM", "WPDWIN"): INF,
                ("WPDWIN", "R4HYP"): INF})
    f_ext = R4.maxflow(dict(ext), "UNC", "RH")
    one = dict(ext)
    one[("SUSCAP2R", "DELTA1FLOOR")] = INF
    f_one = R4.maxflow(dict(one), "UNC", "RH")
    cf = dict(base)
    cf.update({("UNC", "BLKREAL"): INF, ("BLKREAL", "NFCLOS"): INF,
               ("NFCLOS", "TOPROOT"): 1, ("TOPROOT", "R4HYP"): INF,
               ("NFCLOS", "TAILVIS"): 1, ("TAILVIS", "R4HYP"): INF,
               ("NFCLOS", "TLAWCAP"): 1, ("TLAWCAP", "R4HYP"): INF,
               ("NFCLOS", "SUSCAP2R"): 1,
               ("SUSCAP2R", "R4HYP"): INF,
               ("NFCLOS", "DELTA1FLOOR"): 1,
               ("DELTA1FLOOR", "R4HYP"): INF})
    f_cf = R4.maxflow(dict(cf), "UNC", "RH")
    noomega = {k2: v for k2, v in ext.items() if v >= INF}
    reach = R4.bfs_reach(noomega, "UNC")
    check("G62-mincut", f_base == 4 and f_ext == 5 and f_one == 5
          and f_cf == 9 and "RH" not in reach,
          "flows: base 4, refined 5 (r174 graph VERBATIM -- "
          "theta_inf is the classical-evaluation FACE of the "
          "existing H3-COFINAL item, NOT a new node: no set "
          "change); one-grant 5; counterfactual PARALLEL 9 NOT "
          "REAL; census {MEAS, OMEGA-POS} cardinality 4 UNCHANGED; "
          "RH unreachable without the omega edges")

    chain_mpc = {
        "RH": ["MONTGOMERY-PC"],
        "MONTGOMERY-PC": ["SECOND-MOMENT-CEILING"],
        "SECOND-MOMENT-CEILING": ["THETAINF-PIN"],
        "THETAINF-PIN": ["H3COF"],
        "H3COF": ["RATE"], "RATE": ["JETMASS"],
        "JETMASS": ["SIGMAFLOOR"], "SIGMAFLOOR": ["DTSTEP_K"],
        "DTSTEP_K": ["HCOF"], "HCOF": ["WEILPOS"],
        "WEILPOS": ["RH"]}
    loop_mpc = has_cycle(chain_mpc)
    chain_g84 = {
        "GONEK-1984": ["DCLEG"],
        "DCLEG": ["SIGMAFLOOR"], "SIGMAFLOOR": ["DTSTEP_K"],
        "DTSTEP_K": ["HCOF"], "HCOF": ["WEILPOS"],
        "WEILPOS": ["RH"], "RH": ["GONEK-1984"]}
    loop_g84 = has_cycle(chain_g84)
    chain_uni = {
        "RH": ["CENSUS_ALLK"],
        "CENSUS_ALLK": ["DTSTEP_ALLK"],
        "SIGMAFLOOR": ["DTSTEP_ALLK"],
        "DTSTEP_ALLK": ["HCOF"],
        "HCOF": ["NFCLOS", "WEILPOS"],
        "WEILPOS": ["RH"],
        "NFCLOS": ["RH_VIA_N"],
        "RH_VIA_N": ["RH"]}
    loop_uni = has_cycle(chain_uni)
    chain_pin = {
        "TAUPOS": ["A0FLOOR"], "TLAWCAP": ["A0FLOOR"],
        "A0FLOOR": ["TOPROOT"], "TOPROOT": ["RATE"],
        "RATE": ["JETMASS"], "JETMASS": ["SIGMAFLOOR"],
        "SIGMAFLOOR": ["DTSTEP_K"], "DTSTEP_K": ["TAUPOS"]}
    loop_pin = has_cycle(chain_pin)
    chain_term = {
        "ENVJ_H1": ["PF"], "CENSUS_H2": ["PF"],
        "TRACE": ["PF"],
        "GONEK_PRICED": ["WF", "DCLEG"],
        "THETAINF_OPEN": ["H3_COFINAL"],
        "BRIDGE_DATA": ["THETAINF_OPEN"],
        "H3_PER_RUNG": ["RATE"], "H3_COFINAL": ["RATE"],
        "PF": ["JETMASS"], "WF": ["JETMASS"], "RATE": ["JETMASS"],
        "CENSUS_K": ["DCLEG", "DTSTEP_K"],
        "DCLEG": ["SIGMAFLOOR"], "JETMASS": ["SIGMAFLOOR"],
        "SIGMAFLOOR": ["DTSTEP_K"], "BA3": ["DTSTEP_K"],
        "EPSLAW": ["DTSTEP_K"],
        "DTSTEP_K": ["HCOF"], "SUBSTRATE28": ["HCOF"],
        "HCOF": ["NFCLOS", "WEILPOS"],
        "CARRIER_LEM": ["WEILPOS"], "WEILPOS": ["RH"],
        "NFCLOS": ["RH_VIA_N"], "L1": ["RH_VIA_N"],
        "WPD": ["RH_VIA_N"], "RH_VIA_N": ["RH"]}
    acyc = not has_cycle(chain_term)
    rh_reach = all("RH" in reachable(chain_term, n)
                   for n in ("ENVJ_H1", "CENSUS_H2", "H3_PER_RUNG",
                             "H3_COFINAL", "THETAINF_OPEN",
                             "BRIDGE_DATA", "GONEK_PRICED",
                             "CENSUS_K"))
    check("G63-endgame-graphs", loop_mpc and loop_g84 and loop_uni
          and loop_pin and acyc and rh_reach,
          "FOUR loop cycles DETECTED: (i) NEW montgomery-pc: RH -> "
          "MONTGOMERY-PC -> SECOND-MOMENT-CEILING -> THETAINF-PIN "
          "-> H3COF -> ... -> RH (consuming the RH-conditional "
          "pair-correlation family to ceiling theta_inf WOULD BE "
          "the hidden loop -- machine-flagged, NOT consumed); (ii) "
          "gonek-1984 (r174 replicated); (iii) universalized "
          "census; (iv) pinning-supply; the terminal chain with "
          "THETAINF_OPEN as the classical-evaluation face of "
          "H3_COFINAL (fed by BRIDGE_DATA, the per-census "
          "zero-expressibility delivered this round) is ACYCLIC "
          "with RH reachable only from the counterfactual grants "
          "(AND-semantics): NO RH CLAIM")
    info("THE POST-ROUND RESIDUE (exact, typed): theta_inf is "
         "NOT classically pinned this round -- re-typed "
         "OPEN-NONPERTURBATIVE-VARIATIONAL (sharper than the r173 "
         "OPEN-GONEK-CLASS): its DATA is per-census zero-"
         "expressible (the bridge, G34/G35, residuals 1.4e-5 -> "
         "5.4e-5 at 20M zeros), its LIMIT is walled by the "
         "measured variational conditioning (T_req = 3.76e14 -> "
         "1.75e77 vs T_PT = 3.00e12, diverging with h; G36/G37), "
         "its second-moment truncation is INVALID (G38), and "
         "every in-corpus ceiling route is dead or flagged (G53). "
         "The residue is UNCHANGED IN CARDINALITY: {H3-COFINAL == "
         "limsup theta_y <= 0.155, classical-evaluation face "
         "theta_inf OPEN} + {census-forall-k == LOOP, flagged, "
         "not consumed} + {L1, WPD}.  The P5 strong statement "
         "(residue carries no lambda-uniform arithmetic edge and "
         "no unpinned measured constant) is NOT triggered: the "
         "pin did NOT land.  NO omega closed; nothing upgraded; "
         "NO RH CLAIM.")

    # ------------------------------------------------ S9 verdict
    section("S9  COMPOSITE VERDICT")
    verdicts = [
        "WINDOWS-FROZEN-PREEVAL(G60/G61)",
        "CITED-LADDER-REPLICATED(G31)",
        "DECOMPOSITION-EXACT(G10/G32)",
        "BRIDGE-LANDS-PER-CENSUS(G34/G35)",
        "FLUCTUATION-CARRIES-THETA(G33)",
        "DICTIONARY-COVARIANT-NOT-PINNING(G12)",
        "RECONSTRUCTION-FAILS-AT-FULL-CENSUS(G36)",
        "PIN-CENSUS-UNREACHABLE-BY-DEPTH(G37)",
        "CIRCULARITY-RELOCATED-NOT-DISSOLVED(G15/G36/G37)",
        "SECOND-MOMENT-TRUNCATION-INVALID(G38)",
        "CANCELLATION-BLIND-MEASURED(G38)",
        "VK-AND-RH-TRANSPORT-DIVERGE(G14)",
        "INTERLACING-PREMISE-REFUTED-BY-SIGN-CENSUS(G15/G33)",
        "TRACE-CEILING-SELF-REFERENTIAL(G15)",
        "CLASSICAL-CEILING-NOT-LANDED(G53)",
        "THETA-INF-OPEN-NONPERTURBATIVE(G53/G63)",
        "BRIDGE-SEPARATES-WORLDS(G50/G51)",
        "WITNESS-DATA-INVARIANT(G52)",
        "H3-ONE-SIDED-WORLD-BLIND(G52)",
        "BAND-UNDER-BAR-CITED(G53)",
        "RECORDED-NOT-CLAIMED(G53)",
        "DEMAND-FLAT(G54)",
        "BOUND-RIDES-CONNES(G54)",
        "QUANTIFIER-SEQ(G60)",
        "LOOP-ROUTES-FLAGGED(G61/G63)",
        "OMEGA-UNCHANGED(G62)",
        "MINCUT(4/5)"]
    for v in verdicts:
        print("  " + v)

    dt = time.time() - T0_WALL
    check("G99-runtime", dt <= RUNTIME_BAR,
          "%.1f s (bar %.0f)" % (dt, RUNTIME_BAR), kind="edge")
    print("\n" + "=" * 78)
    npass = sum(1 for _n, okc, _d in CHECKS if okc)
    print("GATES: %d/%d PASS   SPEC_SHA %s   runtime %.1f s"
          % (npass, len(CHECKS), SPEC_SHA[:16], dt))
    fails = [nm for nm, okc, _ in CHECKS if not okc]
    if fails:
        print("FAILING GATES: " + ", ".join(fails))
    if EDGE_FAILS:
        print("COMPOSITE: INSTRUMENT-EDGE(%s)" % ",".join(EDGE_FAILS))
    elif EXACT_FAILS:
        print("COMPOSITE: EXACT-LAYER-OBSTRUCTED(%s)"
              % ",".join(EXACT_FAILS))
    else:
        print("COMPOSITE: " + " + ".join(
            v.split("(")[0] for v in verdicts))
    if smoke:
        print("SMOKE MODE -- NOT-VERDICT-BEARING")
    print("NO RH CLAIM.  EXPLORATION ONLY.")
    return 0 if npass == len(CHECKS) and not EDGE_FAILS else 1


if __name__ == "__main__":
    sys.exit(main())
