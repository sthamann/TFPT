% rh/paper/rh_program.tex -- the focused RH-program paper of the rh/ workspace.
% Standalone preamble (house style, but WITHOUT the \veri machinery of the
% main docset; certification boxes carry ledger ID + module + SPEC SHA
% explicitly).  Build: pdflatex x2 (or latexmk -pdf).
\documentclass[11pt]{article}

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage[a4paper,margin=2.6cm]{geometry}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{booktabs}
\usepackage{array}
\usepackage{longtable}
\usepackage{enumitem}
\usepackage{xcolor}
\usepackage[colorlinks=true,linkcolor=blue!50!black,citecolor=blue!50!black,urlcolor=blue!50!black]{hyperref}

\emergencystretch=2.5em
\definecolor{certgray}{gray}{0.94}
\definecolor{openred}{rgb}{0.72,0.13,0.13}
\definecolor{certgreen}{rgb}{0.05,0.42,0.16}

% -- status markers (display system of the docset: [E]/[C]/[O]/[X]) --
\newcommand{\mE}{\textcolor{certgreen}{\textbf{[E]}}}
\newcommand{\mO}{\textcolor{openred}{\textbf{[O]}}}
\newcommand{\mC}{\textbf{[C]}}
\newcommand{\mX}{\textbf{[X]}}

% -- certification box: ledger ID + module + SPEC SHA(s) --
\newcommand{\certbox}[3]{%
  \par\medskip\noindent\colorbox{certgray}{\parbox{\dimexpr\linewidth-2\fboxsep}{%
    \footnotesize\raggedright\textbf{Certification.} Ledger: \texttt{#1}.\quad
    Module: \texttt{#2}.\\ #3}}\par\medskip}

\newcommand{\statusbox}[1]{%
  \par\medskip\noindent\fcolorbox{openred}{white}{\parbox{\dimexpr\linewidth-2\fboxsep-2\fboxrule}{%
    \small #1}}\par\medskip}

\newtheorem{theorem}{Theorem}[section]
\newtheorem{proposition}[theorem]{Proposition}
\theoremstyle{definition}
\newtheorem{openproblem}[theorem]{Open problem}
\theoremstyle{remark}
\newtheorem{remark}[theorem]{Remark}

\newcommand{\taua}{\tau^{\mathrm{aug}}}
\newcommand{\mut}{\tilde\mu}

\title{The TFPT Prime-Front RH Program:\\
Certified Finite Reductions and the Remaining Arithmetic Edge}
\author{TFPT verification programme --- \texttt{rh/} workspace}
\date{2026-09-02 \quad (research documentation, no RH claim)}

\begin{document}
\maketitle

\begin{abstract}
\noindent We document the current state of the TFPT prime-front program on
the Riemann Hypothesis: a chain of \emph{certified finite reductions} that
compresses the positivity of the finite Galerkin compression of the
localized Weil operator (``the wall'') into exact integrable-systems
dictionaries --- IIKS tau functions, a half-filling moment geometry, a
bordered Riemann--Hilbert dictionary, and a coupled two-term tau recursion
whose budget telescope reduces the entire fiber positivity to one terminal
inequality $q_N<1$.  The certified part is machine-checked at the
exact-identity level (\mE\ in the repository ledger); what remains open are
two precisely named edges (\mO): the terminal cross-ratio inequality
cofinally, and a globally oriented prefix resummation --- since r273 both
are corollaries of one master target, the augmented prefix positivity of
Section~\ref{sec:master}.  We also catalogue
the measured no-gos (the honest negatives), position the program relative
to the external unconditional result arXiv:2608.13637, and report the Lean~4
formalization status of the provable core.
\emph{This document is research documentation.  Nothing in it is evidence
for or against the Riemann Hypothesis in either direction.  NO RH CLAIM.}
\end{abstract}

\statusbox{\textbf{Status (binding).}  Research documentation, \textbf{no
RH claim in either direction}.  Status markers follow the repository ledger
\texttt{verification/status\_ledger.csv} (the ledger wins on any
disagreement): \mE{} = certified finite theorems (machine-checked exact
identities with mutation controls), \mO{} = honestly open.  The reduction
mincut of the program is \textbf{base 4 / refined 5} and is unchanged by
everything reported here (dictionaries and adjudications move no edge).
All numerical statements below are \emph{finite, window-level} statements
about the concrete verification objects; none extends to $\zeta$ itself.}

\tableofcontents

%======================================================================
\section{The window object and the claim chain}\label{sec:window}

\subsection{The window object}
The program studies one concrete finite object per \emph{window}~$w$: the
Galerkin compression of the localized Weil operator onto a window of the
von-Mangoldt double comb (prime side $\mu$, smooth/archimedean side $\nu$),
together with three control worlds (\textsc{scramble}: randomized weights;
\textsc{epstein}: an Epstein zeta comb, off-line zeros; \textsc{smooth}: a
smooth density).  ``The wall'' of window $w$ is the statement that this
compression stays positive (quasi-definite) through the whole window ---
a finite, machine-decidable statement per window.  The measured ladder
spans $42$ rungs with terminal degrees $N=142$ to $878$.

\subsection{The equivalence chain (all \mE, typed as equivalences)}
The certified dictionaries identify the wall, exactly and finitely, in
five languages.  Each identification is an \emph{equivalence}: it moves no
edge of the mincut, it changes the coordinates.
\[
\begin{array}{rcl}
\text{wall of } w
&\Longleftrightarrow&
\text{quasi-definiteness of the signed defect measure } \mut=\mu-\nu
\quad\text{(v955)}\\[2pt]
&\Longleftrightarrow&
\text{all Christoffel ratios } r_n=h_n(\mut)/h_n(\mu)>0
\quad\text{(v955, Toda/Hirota)}\\[2pt]
&\Longleftrightarrow&
\text{survival of the full free moment window at half-filling}
\quad\text{(v956)}\\[2pt]
&\Longleftrightarrow&
\text{bordered PSD: } \begin{bmatrix} G_N & u\\ u^{\mathsf T} & B\end{bmatrix}\succeq 0
\ \text{i.e. wall} + \text{budget}
\quad\text{(v958)}\\[2pt]
&\Longleftrightarrow&
\text{budget inequality } \textstyle\sum_n \rho_n < B \ \text{with}\
\rho_n = F_n^2/h_n
\quad\text{(v959)}\\[2pt]
&\Longleftrightarrow&
\text{ONE terminal inequality } q_N = \rho_{N-1}/(5/7) < 1
\quad\text{(v959, r258)}.
\end{array}
\]

\subsection{Mincut}
The reduction mincut stays \textbf{base 4 / refined 5}: the two named open
edges of Section~\ref{sec:edges} (fiber and base/orientation), plus the
frozen upstream contracts, none of which is moved by a dictionary.  The
wall itself is contracted as \texttt{PRIME.CASE.PAIRCORR.CONTRACT.01}~\mO.
A parallel \emph{conditional} Gabor-wavepacket route
(Section~\ref{sec:gabor}) does not move this mincut: its live Lean
criterion is positivity of \texttt{gaborZeroSide} ($\mathrm{hpos}^*$);
the historical wrapper \texttt{gabor\_inputs\_to\_mathlib\_rh} is
VACUOUS (r600); the r541 numeric precheck is not a completed
separation.

%======================================================================
\section{Certified finite theorems}\label{sec:certified}

All theorems in this section are \mE: machine-checked exact identities
(sympy-symbolic and/or exact rationals, with f64+mpmath wards and firing
must-fail mutations), promoted into the load-bearing suite.  We state the
mathematical content; the certification boxes carry the provenance.

\subsection{The IIKS tau dictionary (v955)}

\begin{theorem}[Tau identification, finite IIKS set]\label{thm:v955}
On the measured heavy rungs the wall determinant is the exact IIKS Fredholm
tau function of the discrete $2\times2$ problem:
(i) Sylvester as a full characteristic identity
$\det(I-sQ_{\mathrm{state}})=\det(I-sE_{\mathrm{node}})$ on the whole
$s$-grid;
(ii) the $s$-family factorizes exactly through the $s$-dressed port,
$\det(I-sE)=\det(I-sR)\,\det\!\bigl(I-s\,D_P(s)\bigr)$ with
$D_P(s)=P+sX(I-sR)^{-1}X^{\mathsf T}$;
(iii) the diagonal is the confluent limit of the same generator formula
(constructed kernel, no completion divisor);
(iv) the dressed resolvent is IIKS-integrable again, with source-explicit
generators via the Schur transport $\hat F=F_p+X(I-R)^{-1}F_b$;
(v) the Fredholm/JMU variational identity
$d\log\det(I-sK(t))=-s\,\mathrm{Tr}[(I-sK)^{-1}\,dK]$ holds in two
independent times;
(vi) the relative tau transfer
$\log\tau_{h+1}-\log\tau_h=\log\det[I-(I-K_\uparrow)^{-1}\Delta K]$ is
exact across all dimension changes.
The $h$-step is exactly rank one and carries a predictive degree-1 Lax
dynamics; the closing direction is $h$, not $s$.
\end{theorem}

\begin{theorem}[Toda/Hirota dictionary and sign adjudication]\label{thm:v955b}
With $\tau_{w,n}=D_n(\mu-\nu)/D_n(\mu)$ (Hankel-determinant quotient) and
$r_n=h_n(\mu-\nu)/h_n(\mu)$, the Hirota coefficient
$H_n=r_n/r_{n-1}$ comes from a scaled signed Stieltjes recursion consuming
only node positions and weights; the flag Hirota identity holds in Schur
innovation form, and
\[
\text{all } r_n>0 \iff \text{all } h_n(\mu-\nu)>0 \iff
\text{quasi-definiteness of } \mut \iff \text{the wall},
\]
with $h_n(\mut)=\|\pi_n\|_\mu^2-\|\pi_n\|_\nu^2$ an explicit difference of
two positive norms.  On \textsc{main} all $184$ ratios $r_n$ are measured
positive (min $+0.3666$); the controls flip at $n=25/21/27$.
\end{theorem}

\certbox{PRIME.PORT.TAU.FINITE.IIKS.01 [E];\ PRIME.PORT.TAU.NOPOLE.COFINAL.01 [O]}%
{verification/v955\_tau\_iiks\_toda\_dictionary.py}%
{Probes (embedded byte-exact, re-run at promotion): \texttt{tau\_symbolic\_probe.py}
(18/18, SPEC \texttt{fb04cd7ce9fb306b}), \texttt{lax\_conditioned\_probe.py}
(15/15, SPEC \texttt{93fd9759db7e7b3c}), \texttt{hirota\_sign\_probe.py}
(17/17, SPEC \texttt{d78e236bf633de7b}); rounds r224--r226.}

\subsection{The half-filling geometry (v956)}

\begin{theorem}[Half-filling law and free moment window]\label{thm:v956}
Let $S=\#\mathrm{supp}(\mut)$ be the union support count of the signed
defect measure.  Then on all five measured windows the window cap obeys the
exact law
\[
N_w=\Bigl\lceil S/2\Bigr\rceil=\frac{S+1}{2}\Big|_{\mathbb N},
\]
i.e.\ the wall lives exactly at \emph{half-filling} of the union support:
$H_n$ consumes the moments $m_0,\dots,m_{2n-2}$, an $S$-atom signed measure
has exactly $S$ free moment parameters, and the largest Hankel window
inside them is $n=(S+1)/2$.  \textsc{main} survives the \emph{entire} free
moment window plus $O(1)$ forced-tail degrees ($0/2/2/3/1$ on the five
windows), while the controls die at $0.11$--$0.15\,N_w$: free-window
survival is not generic for signed measures --- it is the arithmetic.
Beyond the cap the wall dies immediately (first $r$-flip at $N_w+O(1)$,
no growth with $N$): \emph{the wall is maximal}.
\end{theorem}

\begin{theorem}[Four-path dictionary, dual reversal, $L$-gauge]\label{thm:v956b}
Moment prefix $=$ Jacobi chain $=$ Euclidean chain $=$ value chain: one
object in four exact source-pure coordinate systems
($m_k=h_0(J^k)_{00}$ exactly in rationals; parameter budget $2N-1$).  The
dual signed measure carries the mirrored Jacobi chain
$\alpha^{\#}_m=\alpha_{S-1-m}$, $\beta^{\#}_m=\beta_{S-m}$ (exact), and the
original and dual FIK problems are connected by an explicit $h$-free
$L$-gauge whose one blind spot is exactly the wall's orientation.  The
control-world collapse is a genuine false pivot of the Euclidean remainder
algorithm, localized without Hankel or tau data (first wrong pivot on
\textsc{epstein} exactly at the known flip $n=25$).
\end{theorem}

\certbox{PRIME.FREEMOMENT.JFRACTION.01 [E];\ PRIME.JACOBI.DUAL.REVERSAL.01 [E];\
PRIME.FREEMOMENT.POSITIVEPREFIX.01 [O]}%
{verification/v956\_signedmoment\_halffilling\_duality.py}%
{Probes: \texttt{holedual\_probe.py} (14/14, SPEC \texttt{a823d4be3f0b1c06}),
\texttt{pontryagin\_maxpos\_probe.py} (17/17, SPEC \texttt{b062906cb458da2a}),
\texttt{jfraction\_probe.py} (17/17, SPEC \texttt{124cda6f00caeeca}),
\texttt{rhp\_midpoint\_probe.py} (13/13, SPEC \texttt{90d2c5a8926d820a});
rounds r228--r231.}

\subsection{The bordered-RHP readout dictionary (v958)}

\begin{theorem}[Bordered dictionary]\label{thm:v958}
The bordered Hankel end-object (bordered PSD $=$ wall $+$ budget) has an
exact Riemann--Hilbert dictionary:
(i) the Uvarov border column $C^{\sigma}_n(z)=\int \hat\pi_n\,
d\sigma/(z-x)$ carries the border readout $F_n$ as its $z^{-1}$
coefficient;
(ii) the budget is an RHP functional of the terminal data alone,
$S_{n-1}=\iint K_n\,d\sigma\,d\sigma$ with the CD kernel
$K_n(x,y)=\bigl(Y_n^{-1}(y)Y_n(x)\bigr)_{21}/(y-x)$;
(iii) the border is a Schlesinger rank-1 insertion: the $3\times3$ RHP is
unipotent/triangular over a canonical $2\times2$ core with a
one-dimensional budget cocycle (no irreducible $3\times3$, no growing
rank; $\det Y_3=-h_{n-2}F_{n-1}$);
(iv) the centering congruence with $c=F_0/h_0$ eliminates the geometric
rank-1 head algebraically (bordered PSD iff centered PSD with
$B^{\circ}=B-\rho_0$), and the extensive tail compresses to one terminal
CD readout;
(v) three exact error-evaluation formulas hold, including
$\delta h_n=(R_1)_{12}$ with $R=YM^{-1}$ and $R_1$ computable as the
contour integral $\frac1{2\pi i}\oint(R(z)-I)\,dz$;
(vi) the PSD base theorem (module-own, exact rationals): for any positive
measure the Gram/Hankel matrix is PSD and every border readout
$T=t^{\mathsf T}G_Nt\ge0$; a signed measure breaks the minor chain --- any
bordered-positivity route using only positive-basis Gram structure proves
the wrong statement.
\end{theorem}

\certbox{PRIME.PORT.RHP.BORDERED.READOUT.01 [E];\
PRIME.PORT.RHP.FULLSOURCE.BASEFIBER.01 [O]}%
{verification/v958\_bordered\_tau\_readout\_dictionary.py}%
{Probes: \texttt{bordered\_hankel\_probe.py} (23/23, SPEC \texttt{c21e15b5852126b9}),
\texttt{bordered\_finite\_rank\_probe.py} (29/29, SPEC \texttt{886c65618145b838}),
\texttt{border\_centering\_probe.py} (24/24, SPEC \texttt{767d16cff674f312}),
\texttt{targetreadout\_error\_probe.py} (20/20, SPEC \texttt{b76487877383f645}),
\texttt{base\_gauge\_constant\_probe.py} (22/22, SPEC \texttt{26276cb2758152e9}),
\texttt{schlesinger\_pairing\_probe.py} (26/26, SPEC \texttt{e5f0498c98112da4});
rounds r244--r253.}

\subsection{The coupled-tau terminal dictionary (v959)}\label{sec:v959}

\begin{theorem}[Coupled recursion and Riccati drain; v959 S0.1]\label{thm:drain}
With $a_n=c_n^2h_n$ and $b_n=-(c_nF_n)^2$ the pair
\[
\tau_{n+1}=a_n\tau_n,\qquad
\taua_{n+1}=a_n\taua_n+b_n\tau_n
\]
implies, wherever the divisions are defined, the Riccati drain
\[
D_{n+1}=D_n-\frac{F_n^2}{h_n},\qquad D=\frac{\taua}{\tau}.
\]
The coefficient $b_n$ is a manifest negative square (source-sign-pure in
every world); $a_n$ reads the $h$-chain --- \emph{the base alone carries
the sign, the border only nonnegative magnitude}.
\end{theorem}

\begin{theorem}[Bilinear tau form; v959 S0.2]\label{thm:bilinear}
Pure ring algebra gives the Wronskian-type bilinear identity
\[
\taua_n\,\tau_{n+1}-\taua_{n+1}\,\tau_n=(c_nF_n)^2\,\tau_n^2 ,
\]
whose right side is a manifest square: the fiber increment is a positive
tau cross-ratio, and the chain telescopes exactly to
$D_N=B-\sum_{n<N}\rho_n$.
\end{theorem}

\begin{theorem}[Terminal reduction and consequence gate; v959 S0.3--S0.4]\label{thm:terminal}
With the r243 budget definition $B=S_{N-2}+\tfrac57$ (by construction),
\[
D_N=\tfrac57-\rho_{N-1},\qquad\text{and}\qquad
D_N>0 \iff q_N=\frac{\rho_{N-1}}{5/7}<1 :
\]
the \emph{entire} fiber positivity is one terminal inequality.  The corner
ray $B(t)=S_{N-2}+t\cdot\tfrac57$ makes the terminal pivot affine in $t$
with machine root $t^{*}=q_N$.  The provenance of $5/7$ is sober: it is
the inverted r241 floor $7/5$ of the \emph{measured}
$\min h_{N-1}/F_{N-1}^2=1.4278$ (margin $0.028$), typed
\textsc{floor\_imported} --- its source-pure derivation is the named
terminal task.
\end{theorem}

\begin{theorem}[Positive-prefix firewall; v959 S0.5]\label{thm:firewall}
For a signed rational measure the prefix moment kernel $H_p$ is positive
definite iff the whole $h$-prefix $h_0,\dots,h_{p-1}$ is positive (exact
Sylvester chain, $\det H_p=\prod_{i<p}h_i$).  Firewall table: \textsc{main}
$p_{\max}=184=N$ with zero negative modes (the only positive-prefix
world); \textsc{scramble}/\textsc{epstein}/\textsc{smooth} continue
indefinitely at $p_{\max}=21/25/27$.
\end{theorem}

\begin{remark}[The $q$-census and the covariant ratio; r258]
Across the full ladder ($42$ rungs, $N=142..878$): $q_N<1$ everywhere,
$\min/\mathrm{med}/\max=0.0015/0.4188/0.9805$, minimal margin $0.0139$;
trend \textsc{flat} (gap slope $+0.41$, Spearman $+0.36$) --- \emph{the
wall margin does not shrink with $N$}.  The sharpest single finding:
$\mathrm{Spearman}(2\log F_{N-1},\log h_{N-1})=+1.000$ --- border-readout
square and terminal norm are rank-perfectly covariant; the driver is
neither $F$ nor $h$ but the quotient itself.  Any terminal proof must bind
the \emph{ratio}.
\end{remark}

\certbox{PRIME.PORT.RHP.COUPLEDTAU.TERMINAL.01 [E];\
PRIME.PORT.COUPLEDTAU.TERMINAL\_CROSSRATIO.01 [O];\
PRIME.PORT.FULLSOURCE.PREFIX\_RESUMMATION.01 [O]}%
{verification/v959\_coupledtau\_terminal\_dictionary.py}%
{Probes: \texttt{baseborder\_factorial\_probe.py} (21/21, SPEC \texttt{c7aa9f46889414c4}),
\texttt{coupledtau\_probe.py} (26/26, SPEC \texttt{73d8247f6de36a2b}),
\texttt{budget\_anatomy\_probe.py} (27/27, SPEC \texttt{28026ae6b0750098}),
\texttt{parametrix\_pass\_probe.py} (19/19, SPEC \texttt{e79da4f43d63213a});
rounds r256--r259.}

\subsection{Kernel-Loewner positivity at $L=0.3$ (v1017)}

\begin{theorem}[Kernel-Loewner floor at $L=0.3$]\label{thm:kernel-loewner-03}
Let $h\in L^2(\mathbb R)$ be supported in $[-0.3,0.3]$ (so $2L=0.6<\log 2$ and
the classical prime term is empty).  Write $g=h*h(-{\cdot})$ and
\[
  Q_W(h)=\int_0^{2L} k(x)\bigl(g(0)-g(x)\bigr)\,dx - c_L\,g(0)+\Pi(h),
\]
with $k(x)=e^{x/2}/\sinh x$ and
$\Pi(h)=2\bigl(\langle h,\cosh(x/2)\rangle^2-\langle h,\sinh(x/2)\rangle^2\bigr)$.
Then
\[
  Q_W(h)\;\ge\; 2.1{\times}10^{-3}\,\|h\|_2^2.
\]
The enclosed floor is $2.122{\times}10^{-3}$ as a \emph{float64}
machine check (not interval arithmetic, not an~\mE\ exact/interval
certificate): a quintic $C^2$ cutoff books the
near-diagonal as $\kappa_w$, a $401$-dimensional Legendre section of a
degree-$48$ Chebyshev surrogate is enclosed by Bernstein HS bounds, and the
exact tail is charged three times.  Independently, the zero-extension identity
$2(g(0)-g(x))=\|h-\tau_x h\|_{L^2(\mathbb R)}^2$ holds exactly over $\mathbb Q$
on two step functions (boundary-strip mass strictly positive), and doubling
$c_L$ produces a negative certificate budget.
\end{theorem}

This is a \emph{single-length} operator inequality.  It is not
$\lambda_*(L)\ge 0$ for general $L$.  Round r496 is a method no-go for the
same compact-tail completion at $L=0.8$ (Section~\ref{sec:nogos}).
No RH claim.

\certbox{PRIME.RDAGGER.KERNEL\_LOEWNER.01 [Numerical/certified]}%
{verification/v1017\_kernel\_loewner\_positivity.py}%
{FLOAT64 certificate (Gauss--Legendre $+$ Chebyshev/Bernstein $+$ HS tail),
not interval arithmetic, not~\mE.  Probes (stay experiments-side; module re-derives):
\texttt{kernel\_loewner\_probe.py} (r494, $17/17$,
VERDICT \texttt{PROVED(c=2.1e-3)}, SPEC \texttt{2e6d71138dff4540}),
\texttt{kernel\_redteam\_probe.py} (r495, $11/11$ smoke /
\texttt{CONFIRMED(c=2.1e-3)}, SPEC \texttt{431251a9bcfd40a1}).
Boundary: \texttt{kernel\_loewner08\_probe.py} (r496,
\texttt{NO\_GO} compact-tail@$L=0.8$).}

%======================================================================
\section{The two-branch theorem and the surface closure (v960)}\label{sec:twobranch}

Rounds r260--r263 organize the one remaining fiber inequality; rounds
r268--r271 close its $42$-rung surface.  Since wave~4 the whole set is
promoted as \texttt{v960} (certification box at the end of this section).

\subsection{The exact decomposition (r263)}
Write the terminal object in scaled coordinates: $Z_w=r_{N-1}$ (the driven
terminal readout; identity $Z_w\equiv r_{N-1}$ gated at $1.8\times10^{-14}$),
$M_w=\sqrt{5/7}$, and let $U_w$ be the sealed target-blind triangle
certificate (the C1b terminal triangle
$|r_{N-1}|\le|t_{N-2}|+|a'||r_{N-2}|+|b'||r_{N-3}|$, consuming prefix data
only; $t_k=T_k/\sqrt{h_{k+1}}$ is the border-comb drive).  Then $q_N<1$
splits into two branches on the scalar $g_w=M_w-U_w$:
\begin{itemize}[leftmargin=2em]
\item \textbf{Generic (cheap) branch $g_w\ge0$:} the triangle inequality
alone closes the target, \emph{without any cancellation}:
$|Z_w|\le U_w\le M_w\Rightarrow q_N\le1$ (strict with strict slack).
Measured: exactly $35/42$ rungs, including \emph{both} mains
(w9 $g=+0.442$, w13 $g=+0.212$).
\item \textbf{Exception branch $g_w<0$:} exactly the seven rungs
$\{kz15,20,22,36,38,39,52\}$, all with $Z_w<0$, all \emph{drive-dominated}
($|t_{N-2}|$ the largest triangle term), minimal modulus slack
$M-|Z|=0.027$ (kz15).  The missing decade is genuine cancellation between
the border-comb drive and the chain terms, at one degree per window.
\end{itemize}

\subsection{The binding retype (r263)}
\begin{center}\small
\textsc{one\_degree\_per\_window\_localization} $+$
\textsc{triangle\_generic\_branch}\\
$+$ \textsc{pairing\_cancellation\_exception\_branch}
\end{center}
\noindent A finite sum term per
window is \emph{not} a finitely solvable problem --- the unbounded window
quantifier makes the family of growing finite comb sums the same
arithmetic wall.  The eventual-triangle hope is refuted: exceptions occur
in all terciles and the deepest rung kz52 ($N=878$) is itself an
exception.  The measured cancellation degree separates sharply:
$\kappa=|r_{N-1}|/\text{triangle}=0.427$ on misses vs $0.998$ on covers.

\subsection{The S6 requirement and the RHP dictionary (r264)}
The campaign charter (frozen, SHA \texttt{7b9e751d}; 7 exceptions, 5
kills, 2 blocked paths) states the remaining goal verbatim:
\[
\textbf{S6:}\qquad Z_w=Z^{\mathrm{RHP}}_w+E_w,\qquad
\bigl|Z^{\mathrm{RHP}}_w\bigr|+\bigl|E_w\bigr|<\sqrt{5/7}
\quad\text{on the branch } g_w<0,
\]
or directly the cross-ratio positivity.  Round r264 delivered the exact
dictionary $Z^{\mathrm{RHP}}=$ (border-column readout)$/$(Y1 root):
$F_{N-1}$ from the rank-1 Uvarov pairing, $h_{N-1}$ from the $Y_1$ entry,
the base constant cancelling exactly (worst $9.2\times10^{-9}$ on all
$42{+}2$ worlds, sign $44/44$) --- the target package is entirely in RHP
language.  The data signature of the exception branch sits in the drive
column: \textsc{s1\_termdrive} $|t_{N-2}|$ separates at $0.959$, while the
bare comb/source geometry is blind ($0.14/0.18$).

\subsection{The surface closure (r268--r271): $42/42$ certified}
\label{sec:surface}
The exception branch is now closed \emph{as a surface}:
\begin{itemize}[leftmargin=2em]
\item \textbf{Edge truncation (r268).}  The outer-hull triangle bound
\texttt{b3F0.20} delivered the \emph{first source-pure certification of an
exception rung ever} (kz22, margin $+0.04$), with the remaining six gaps
measured \emph{sub-paircorr} ($0.18$--$0.44$ dec, all demands $<1.0$ dec,
no wall flag).  Structurally closed en route: the $L$-gauge path collapses
at the nodes exactly ($\hat\pi^{\#}_{S-1-k}(x_j)=w_jL'(x_j)\hat\pi_k(x_j)/
h_k$) --- the dual degree of the drive is $N$, not small.
\item \textbf{Block alternation (r269).}  Adjacent sign-run pairs as exact
pair sums, then the triangle on the pairs: gains $+0.26$ to $+0.41$ dec on
\emph{all} $42$ rungs and certifies six of the seven exceptions
source-purely (kz20/22/36/38/39/52; full ladder $37/42$); \emph{no}
detector fires (paircorr demand max $+0.07$ at bar $1.0$ --- adjacent-run
pairing is local phase structure).
\item \textbf{The kz15 interval certificate (r270).}  The whole bordered
chain in outward-rounded interval arithmetic (mpmath, dps $640$, $202$
steps, ${\sim}548$ digits of dependency consumption, end width
$1.5\times10^{-92}$, all $810$ source atoms exactly converted):
$\sup|Z_{kz15}|<\inf\sqrt{5/7}$ \emph{strictly}, margin $+0.02680$; the
dps-halving ward destroys the certificate as demanded.  Honestly typed an
exact \emph{finite} certificate (\texttt{EnclOK} class) --- no mechanism,
no target-blind class evidence.
\item \textbf{The universal pair theorem (r271).}  The fixed form
\texttt{c2PAIR} as a theorem: hypotheses H1--H4 are structural/source-side
(exact pair decomposition, alternation, fixed pairing, boundary term) and
are \emph{proved in Lean} (\texttt{rh/lean/RH/PairBound.lean}, no
\texttt{sorry}); H5 --- the margin $|Z_{\rm local}|+\varepsilon<\sqrt{5/7}$
--- is the \emph{single} window-dependent hypothesis, the formal entry
gate of the cofinal step.
\end{itemize}

\noindent\textbf{The milestone.}  Together: the $42$-rung surface census
$q_N<1$ is \emph{certified on all $42$ rungs} as a finite fact ---
$41$ mechanism-certified target-blind ($35$ cheap via the two-branch
theorem, $6$ exceptions via the sealed phase bounds) $+$ kz15
exact-finite; independent cross-validation (r270-b1 vs r271-b2LEVEL2
identical on separate implementations: kz39 $0.002$ / kz15 $0.06$ dec).
The surface is a census $+$ certificate set, \emph{not} a cofinal
theorem: the window quantifier stays (Section~\ref{sec:edges}).

\subsection{The cofinal state (r272/r273, typed measurements)}
The r271 scaling alarm $\mathrm{sp}(N,\varepsilon)=+0.67$ is dissected
exactly (r272): pair count $\alpha=+1.01$, amplitude $\beta=-0.81$,
bound-side cancellation $\gamma=-0.01$ (exactly flat) --- while the
\emph{truth} side runs opposite ($\gamma_{\rm true}=+0.453$; the truth
margin \emph{rises} with $N$, $+0.264\to+0.414$): sealed adjudication
\textsc{bound\_coarseness\_confirmed} --- the bound loses cancellation,
not the world; H5 is contradicted by no measured truth quantity.  The
quantified flip condition: a \emph{non-adjacent/global} mechanism
(address c3) must supply $N^{-\delta'}$ with $\delta'>0.21$ of the
measured available $0.45$ (\textsc{truth\_allows}).  The r273 Euler
surgery ladder shows the required cancellation is \emph{generic} (no
surgery collapses $\gamma_{\rm true}$) while \emph{every} surgery kills
the free-prefix positivity at degrees $33$--$101$: \emph{the wall, not
the cancellation rate, is what is arithmetically special about}
\textsc{main}.

\certbox{PRIME.PORT.COUPLEDTAU.SURFACE\_CLOSURE.01 [E];\
PRIME.PORT.COUPLEDTAU.TERMINAL\_CROSSRATIO.01 [O] (cofinal front)}%
{verification/v960\_terminal\_surface\_closure.py}%
{Probes (embedded byte-exact, sealed smoke stage; full 16-hex SPEC SHAs
pinned in the module and in \texttt{rh/INVENTORY.json}):
\texttt{terminal\_crossratio} (31/31, \texttt{36eee7b4}),
\texttt{terminal\_triangle} (26/26, \texttt{db32d31f}),
\texttt{cancellation\_adjudication} (26/26, \texttt{151a9176}),
\texttt{drive\_local\_asymptotics} (28/28, \texttt{0c4df408}),
\texttt{phase\_bulk\_bound} (29/29, \texttt{f5578bb9}),
\texttt{kz15\_boss} (29/29, \texttt{8834a1c2}),
\texttt{universal\_pair\_theorem} (28/28, \texttt{66f61c8a}),
\texttt{l2\_scaling\_anatomy} (28/28, \texttt{3fb76b65}),
\texttt{euler\_mechanism} (24/24, \texttt{8cb1804e}),
\texttt{kyp\_memory} (24/24, \texttt{88ebdb00}); rounds r260--r275.}

%======================================================================
\section{The orientation dictionary and the Maslov census (v961)}
\label{sec:orientation}

\begin{theorem}[Wronskian dictionary; r274, v961 S0.1--S0.2]\label{thm:wronskian}
On every measured window the monic orthogonal chain $\hat\pi_n$ and its
second-kind chain $q_n$ satisfy the base Casoratian identity
\[
\hat\pi_{n+1}(z)\,q_n(z)-\hat\pi_n(z)\,q_{n+1}(z)=h_n
\qquad(c'=1,\ \text{identically in }z),
\]
the right solution is triple-constructed with exact coincidence (residue
route; downward recursion with the exact Dirichlet boundary $q_S=0$ ---
\emph{the node polynomial is the right boundary condition}; dual chain
$+$ $L$-gauge), the $h$-free midpoint form
$\hat\pi_{n+1}\hat\pi^{\#}_{S-1-n}-\beta_{n+1}\hat\pi_n\hat\pi^{\#}_{S-2-n}
=L(z)$ \emph{is} the node polynomial (constant in $n$: the r231
sign-blindness as an identity --- it provably carries no orientation),
and the augmented Casoratian is a polynomial (border poles cancel
exactly) with the telescope
\[
D_{n+1}=B-\frac{W^{\rm aug}_n}{W^{\rm base}_n},\qquad
W^{\rm aug}_n=h_nS_n ,
\]
exact against the independent r263 route.  The honest c-typology:
$c_n=1/W^{\rm base}_n$ is source-pure and positive on the free prefix,
but carries the \emph{entire} orientation --- the dictionary relocates
the orientation question, it does not solve it.
\end{theorem}

\begin{theorem}[Maslov census; r277]\label{thm:maslov}
Rule R2 $=$ the interlacing/reality structure of the Jacobi zeros passes
the sealed blind examiner protocol: blind $42/42$ rungs $+$ all mains
\textsc{safe} at full depth; the controls fire \emph{exactly} at
flip$+1$ ($26/22/28$); R2 is provably \textsc{safe} on every positive
prefix ($\beta>0\Rightarrow$ symmetrizable $\Rightarrow$ real $+$
Cauchy-strict), is \emph{not} $h$-equivalent ($79$ pattern mismatches vs
$78$ $h$ re-entry pivots) and never heals: a one-way break detector ---
the cofinally monotone object the proof plan needs.
\end{theorem}

\begin{remark}[The honest refutation; r277]
The \emph{raw atom-Sturm census is not the right winding quantity}: on
\textsc{main} $c_n\equiv n$ breaks at $n=56/48$ at throughout-positive
$h$ (zeros first escape the atom hull, then pair inside single atom
gaps) --- the classical separation theorem for positive measures fails
genuinely for the signed comb.  \textsc{sturm\_chain\_verified} is
honestly \emph{not} awarded; the named consequence is the
\emph{contraposition} form: an independent index obstruction must forbid
the R2 break before half-filling on \textsc{main} --- exactly there the
\texttt{MainWindow} arithmetic must enter (the oriented midpoint theorem,
Section~\ref{sec:edges}).
\end{remark}

\begin{remark}[The metric firewall; r276/r278, typed measurements]
The r273 firewall map is refined to a graded \emph{continuum} law
($D\sim\theta^b$, $b$ $0.04$--$1.09$; no jump; single operations
typically survive to depth $0.88$--$1.00$).  The property ranking puts
\emph{support exactness} first (jitter at $2\%$ of the local atom gap
already costs $3/4$ of the depth) and the Euler family structure last:
the arithmetic firewall is more precisely a \emph{metric} firewall.  The
exact position gradients (Hellmann--Feynman class, identity fine type:
$d\log h_n/du_j=\langle\dot w_j,\hat\pi_n^2\rangle/h_n$ --- the
polynomial variation drops by orthogonality) localize the lethal
positions (u-profile \textsc{predictive}, $\mathrm{sp}=-0.82$; the top-3
atoms are the small primes $2,3,5$), but the local stability law is
\textsc{perturbative\_only}: $\theta^*$ sits $25$--$170\times$ below the
smallest measured dose (the $0.02$ collapse is a nonlinear cascade
across tent kinks; $L_D$ window-specific, no uniform lemma candidate).
The \textsc{main} positivity itself stays the open center.
\end{remark}

\certbox{PRIME.PORT.RHP.MIDPOINT.ORIENTATION\_DICTIONARY.01 [E];\
PRIME.PORT.RHP.MIDPOINT.ORIENTED\_THEOREM.01 [O];\
PRIME.PORT.WALL.METRIC\_FIREWALL.01 [O]}%
{verification/v961\_midpoint\_orientation\_dictionary.py}%
{Probes (embedded byte-exact, sealed smoke stage; full 16-hex SPEC SHAs
pinned in the module and in \texttt{rh/INVENTORY.json}):
\texttt{wronskian\_dictionary} (32/32, \texttt{56e8a03e}),
\texttt{minimal\_firewall} (25/25, \texttt{ed17d79f}),
\texttt{maslov\_census} (29/29, \texttt{3858fd16}),
\texttt{metric\_stability} (31/31 full record / 30 smoke gates,
\texttt{7031200f}); rounds r274--r278.}

%======================================================================
\section{The half-filling pinning theory (v962): four theorems, four
refutations, and the north star in reinstform}\label{sec:pinning}

Wave 5 (rounds r279--r281) freezes the pinning anatomy as a small
mathematical theory --- the reviewer demand ``not just as
experiments''.  All four theorems are machine-certified at the
exact-rational level (module-own re-derivation in pure Fractions plus
the embedded sealed probes).

\begin{theorem}[Moment counting; r281, v962 T1]\label{thm:counting}
The pivot $h_n$ consumes the moments $m_0..m_{2n}$.  An $S$-atom signed
measure has exactly $S$ free moments $m_0..m_{S-1}$ (Vandermonde
bijection); every $m_k$ with $k\ge S$ is forced by the monic
node-polynomial recurrence $\sum_{i=0}^{S}c_im_{k-S+i}=0$.  Hence the
\emph{free} pivots are exactly $h_0..h_{N_w-1}$ with
$N_w=\lceil S/2\rceil$, and $h_{N_w}$ is the first forced pivot:
\textbf{half-filling is the end of the free moment space}.  (Gated as
arithmetic for all $S=2..2000$; freedom demonstration exact in
rationals: $dm=e_{S-1}$ moves $h_{N_w-1}$ alone and shifts the first
forced moment by exactly $-c_{S-1}$; the wrong-$L$ must-fail is loud.
Lean: \texttt{moment\_counting\_free\_pivots}, proved.)  The reviewer
question ``why half-filling'' is answered by counting.
\end{theorem}

\begin{theorem}[Crossing budget; r279, v962 T2]\label{thm:budget}
For a nondegenerate signed measure ($h_n\ne0$ for $n<S$), Jacobi's
minor-sign rule and the Sylvester congruence $G_S=VWV^{\mathsf T}\sim
W=\mathrm{diag}(w_j)$ give
\[
\#\{n<S:\ h_n<0\}\;=\;S_-\;=\;\#\{j:\ w_j<0\}
\]
exactly: the total Maslov crossing budget over the full algebraic
continuation is the negative-atom count, \emph{world-blindly} (w9
$104$, w13 $98$, controls $141/94/6$, kz15 $121$, kz52 $551$).  Lean:
\texttt{crossing\_budget} (stated, v962 reference).
\end{theorem}

\begin{theorem}[Two-sided parity; r279, v962 T3]\label{thm:parity}
With $e_j=\mathrm{sign}(w_j)$ and $L'(x_j)=(-1)^{S-1-j}|L'(x_j)|$ on
sorted nodes, the union polynomial
$Q_n=\hat\pi_n\hat\pi^{\#}_{S-1-n}$ satisfies
$\mathrm{sign}\,Q_n(x_j)=e_j(-1)^{S-1-j}\mathrm{sign}(h_n)$ at every
node and degree; hence the union zero occupancy of every open atom gap
is \emph{odd} in every weight-agreement gap (never empty) and
\emph{even} in every disagreement gap --- at every degree, $h$-blind.
The census bilanz $c_n+c^{\#}_{S-1-n}=(S-1)-|D|+2\,\mathrm{scD}(n)$
holds identically (exact via Fraction Sturm chains at every degree;
proved by exhaustion over all $87376$ sign-vector pairs $k\le8$).
\end{theorem}

\begin{theorem}[Main window reduction; v962 T4]\label{thm:reduction}
Given Theorem~\ref{thm:counting} (the free window ends at $N_w$) and
Theorem~\ref{thm:budget} (the budget is fixed at $S_-$), the entire
open statement of the wall is the localization $\min C\ge N_w$, and
this is exactly equivalent to
\[
\forall\,n<N_w:\ h_n>0
\]
(free-window quasi-definiteness; Lean:
\texttt{main\_window\_reduction}, proved).  No size, cancellation or
orientation question remains: one placement question.
\end{theorem}

\begin{remark}[The north star, reinstform]\label{rem:northstar}
The campaign's open center is, by Theorem~\ref{thm:reduction},
\emph{why is the signed prime moment form quasi-definite up to the
maximally free order?}  In Lean this is the fog-free central
\texttt{sorry} \texttt{free\_window\_positivity}
(\texttt{rh/lean/RH/Window.lean}); via Theorem~\ref{thm:reduction} it
is the base half of the master theorem
\texttt{augmented\_prefix\_positive}
(\texttt{master\_implies\_free\_window} is proved).  Since wave 6 the
open center has its \emph{canonical form}, lemma L*
(Section~\ref{sec:lstar}): carried on
\texttt{PRIME.LSTAR.SUBORDINATION.01}~\mO\ (the predecessor contract
\texttt{PRIME.PORT.RHP.FULLSOURCE.QUASIDEFINITENESS.01} is consumed by
\texttt{v963}).
\end{remark}

\begin{remark}[The four refutations; named negative gates]
\label{rem:refutations}
(N1) \textsc{no\_universal\_o1\_pinning} (r281, exact): the
one-negative toy measure has $\min C=S-1$ and offset $N_w-2$,
\emph{unbounded} in $S$ --- any $O(1)$ upper pinning must consume the
comb structure; the only provable ceiling today is the pigeonhole
$\min C\le S-S_-$ from Theorem~\ref{thm:budget}, and the measured
pinning constant $C\le5$ is a 42-rung \emph{measurement} (the Lean
guard \texttt{upper\_pinning\_not\_universal} carries an exact 4-atom
instance with machine-checked Hankel minors).  (N2)
\textsc{no\_extremality} (r280, measured $+$ exact witness): three
structured mp-confirmed gradient directions push the w9 first crossing
$184\to185$ \emph{past} half-filling; a 4-atom one-negative measure has
$\min C=N_w+1$ exactly --- $N_w$ was the free-window bound, never an
absolute maximum.  (N3) \textsc{no\_generic\_maslov\_obstruction}
(r279, exact): three rational counterexamples refute the candidate
obstruction ``two-sided compatibility forbids a crossing before
half-filling'' --- the two-sided machinery is world-blind classical
structure and provably carries no arithmetic.  (N4)
\textsc{no\_simple\_offset\_law} (r281, measured): six sealed
source-pure offset candidates reach $\max|\mathrm{sp}|=0.273\ll0.75$
over the 42-rung census, all world-blind.
\end{remark}

\certbox{PRIME.WALL.HALFFILLING\_PINNING\_THEORY.01 [E];\
PRIME.PORT.REPRESENTATION.CONTEST.01 [O];\
PRIME.PORT.RHP.FULLSOURCE.QUASIDEFINITENESS.01 [O]}%
{verification/v962\_halffilling\_pinning\_theory.py}%
{Probes (embedded byte-exact, sealed smoke stage; full 16-hex SPEC SHAs
pinned in the module and in \texttt{rh/INVENTORY.json}):
\texttt{oriented\_theorem} (32/32, \texttt{9107709b}),
\texttt{budget\_localization} (29/29, \texttt{7abf7a20}),
\texttt{halffilling\_pinning} (28/28, \texttt{a0081572}); rounds
r279--r281.}

%======================================================================
\section{The L* reduction (v963): the canonical form of the open
center}\label{sec:lstar}

Wave 6 (rounds r282--r285 plus the standalone problem document)
freezes the L* arc.  The r283 attack reduces the entire free-window
question --- and with Theorem~\ref{thm:reduction} the entire open
statement of the wall --- to \emph{one} two-measure subordination
scalar; the r282 contest closes the classical representation exits
with exact reasons; the r285 round makes the decomposition bookkeeping
exact.  All exact statements are machine-certified at the
exact-rational level (module-own re-derivation plus the embedded
sealed probes).

\begin{theorem}[The mu-frame reduction; r283, v963 A1--A3]
\label{thm:frame}
Split the signed defect measure $\tilde\mu=\mu-\nu$ into its positive
channel $\mu$ ($S_+$ atoms) and negative channel $\nu$ ($S_-$ atoms,
weights $v_k>0$).  With $U$ the unit lower-triangular monic $\mu$-OP
change of basis, $U H_k(\tilde\mu)U^{\mathsf T}=D_\mu-G$ where
$D_\mu=\mathrm{diag}(h^\mu_i)$ and $G_{ij}=\int\pi_i\pi_j\,d\nu$;
leading principal minors are preserved, so
\[
\mathrm{minor}_k(D_\mu-G)\;=\;D_k(\tilde\mu)
\qquad\text{exactly, at every }k\le S_+ .
\]
Consequently, with $E_n$ the $\nu$-dressed $\mu$-Christoffel--Darboux
kernel ($E_n=B_nB_n^{\mathsf T}$, $B[k,i]=\sqrt{v_k}\,\varphi_i(y_k)$),
\[
h_0,\dots,h_{m-1}>0
\;\Longleftrightarrow\;
\lambda_{\max}(E_m)<1 ,
\]
and by monotone rank-one loading $\lambda_{\max}$ crosses $1$
\emph{exactly} at $\min C+1$.  The pigeonhole ceiling $\min C\le S_+$
is re-proved in the frame (null-polynomial witness
$\int p^2\,d\tilde\mu=-\int p^2\,d\nu<0$), and capacity-as-counting is
refuted by the exact rank-one pair (same rank, same support, opposite
window fate; the deciding values are metric,
$1.25\times10^{-13}$ vs $1.25\times10^{2}$).
\end{theorem}

\begin{theorem}[The canonical form: lemma L*]\label{thm:lstar}
By Theorem~\ref{thm:frame} the free-window quasi-definiteness of
Theorem~\ref{thm:reduction} is exactly the statement
\[
\boxed{\;\int p^2\,d\nu\;<\;\int p^2\,d\mu
\quad\text{for every real polynomial }p\ne0,\ \deg p<N_w\;}
\]
--- equivalently $\lambda_{\max}(E_{N_w})<1$: the $\nu$-channel is
strictly $\mu$-subordinate on the free polynomial window.  This is a
two-measure moment problem instead of a determinant cascade; \emph{it
is not proved} --- it is the open center in canonical form, registered
as \texttt{PRIME.LSTAR.SUBORDINATION.01}~\mO\ and stated fully
standalone (no project vocabulary) in
\texttt{rh/problem/lstar\_problem.tex} for external mathematicians.
Lean: \texttt{lstar\_subordination}
(\texttt{rh/lean/RH/Window.lean}, documented \texttt{sorry}); the
direction L* $\Rightarrow$ free-window positivity is \emph{proved}
(\texttt{lstar\_implies\_free\_window}).
\end{theorem}

\begin{theorem}[Decomposition bookkeeping; r285, v963 B]
\label{thm:decomp}
With $\mathrm{maxdiag}_n=\max_k v_kK_n(y_k)$ and
$\mathrm{assist}_n=\lambda_{\max}(E_n)/\mathrm{maxdiag}_n-1$:
$\lambda_{\max}=\mathrm{maxdiag}\times(1+\mathrm{assist})$ exactly,
the Christoffel sandwich $\mathrm{maxdiag}_n\le\lambda_{\max}(E_n)\le
\mathrm{trace}(E_n)$ holds with its exact-rational consequences at
every degree, and the budget equivalence
$(1/\mathrm{maxdiag}-1)-\mathrm{assist}>0\iff\lambda_{\max}<1$ is
sign-exact.  L* thus splits into (D) the per-atom diagonal condition
$v_kK_{N_w}(y_k)<1$ and (C) the coherence condition --- exact
bookkeeping, no give-away bound.
\end{theorem}

\begin{remark}[The four-language elimination; r282, exact negative
gates]\label{rem:contest}
The contest ``what \emph{is} $h_n$?''\ is closed in all four classical
languages, each with the exact reason (verdict
\textsc{contest\_all\_dead}):
\begin{center}\small
\begin{tabular}{@{}p{0.26\linewidth}p{0.30\linewidth}p{0.32\linewidth}@{}}
\toprule
\textbf{Language} & \textbf{Verdict} & \textbf{Exact reason}\\
\midrule
K1 RHP / monodromy norm & \textsc{restatement} &
the SOS/$|m|^2$ structure exists iff the negative register is empty
($N_n\equiv0$); the surviving clause $N<P$ is $h>0$ verbatim (fake SOS
misses $h_2$ by exactly $N_2=48360721965/70120631072$)\\
\addlinespace
K2 Hodge / Kasteleyn orientation & \textsc{not constructible} &
full $2^S$ exhaustion: the coherent class is exactly
$\{\pm\mathrm{sign}\,w\}$; value preservation fails by exactly
$2\times$negative mass --- an orientation exists iff $S_-=0$\\
\addlinespace
K3 de Branges / canonical system & \textsc{restatement} &
Hamiltonian PSD $\iff\beta>0\iff h>0$: exact bookkeeping, no second
gestalt (the only independent shadow lags $+1$ everywhere)\\
\addlinespace
K4 operator / dual pair & \textsc{world-blind by theorem} &
$h_nh^{\#}_{S-1-n}=1$ exactly at every degree: the dual pair is
sign-synchronized identically, no second condition\\
\bottomrule
\end{tabular}
\end{center}
The common deep reason (the program finding): \emph{every classical
positivity language forces positivity exactly for the positive measure
class} --- MAIN is signed, and its free-window positivity is a genuine
arithmetic statement without a known structural carrier.
\end{remark}

\begin{remark}[Measured anatomy and the honest margin decay; typed,
not laws]\label{rem:lstar-measured}
(i) \emph{One atom vs collective} (r284): on w9 the pure single-atom
Christoffel bound crosses at $n_{\rm DIAG}=187$, only two degrees above
the true crossing $185=\min C+1$, while all four controls die
collectively far below their single-atom degree; the near-breaking
direction is $99.7\%$ two shallow-$u$ ARCH background atoms below the
first prime (PR $1.89$ vs control $4.97..9.72$;
\textsc{smallp\_depleted}, \textsc{one\_band}); the shielding geometry
is world-blind and the Nyquist/resolution formula fails on all four
controls by $1.5$--$3$ octaves.  (ii) \emph{Where the arithmetic sits}
(r285): (D) separates at window scale (controls' $n_{\rm DIAG}=50..91
\ll184$); the binding edge atom's kernel growth is \emph{sub-classical}
($p=0.38$ vs bulk law $1$, classical coverage $0/42$ --- any (D) proof
must be a discrete bound); MAIN's wall assist is the extreme low
outlier of both conservation-gated replicate ensembles (pct $0.00$);
and the round delivers the first two positively MAIN-separating
detectors of the program (assist-at-crossing $0.0195$ vs dead
$1.69..2.99$; random-sign $z=-3.15$ destructive vs dead
$+4.95..+12.44$ constructive).  (iii) \emph{The margin decay} (stated
neutrally): all $42$ ladder margins $1-\lambda_{\max}(E_{N_w})$ are
strictly positive, but the family minimum is $1.4175\times10^{-7}$
(anchor $z=233$, $S=1717$) while the flagship $z=16$ carries the
\emph{largest} margin $1.68\times10^{-4}$ --- a decay of $\sim$3
orders over the tested range, driven by the $\mathrm{maxdiag}\to1$
trend of the per-atom budget.  At this cut the unbounded-family
version of L* was genuinely uncertain in both directions; the r286
margin-scaling round has since landed and is consumed in wave 7
(Section~\ref{sec:coherence}): resolved \emph{harmless} --- no
counterexample on the extended ladder, the $O(1)$ census offset
survives.
\end{remark}

\certbox{PRIME.LSTAR.REDUCTION\_DICTIONARY.01 [E];\
PRIME.LSTAR.SUBORDINATION.01 [O]}%
{verification/v963\_lstar\_reduction\_dictionary.py}%
{Probes (embedded byte-exact, sealed smoke stage; full 16-hex SPEC SHAs
pinned in the module and in \texttt{rh/INVENTORY.json}):
\texttt{representation\_contest} (30/30, \texttt{0f9954b8}),
\texttt{fullsource\_quasidefiniteness} (26/26, \texttt{4cf5ea53}),
\texttt{lstar\_two\_measure} (30/30, \texttt{687112a8}),
\texttt{christoffel\_decomposition} (33/33, \texttt{9781e6d6});
rounds r282--r285; standalone document
\texttt{rh/problem/lstar\_problem.tex} $+$
\texttt{verify\_lstar\_instance.py} (12/12).}

%======================================================================
\section{The L* coherence census (v964): the margin law, the van der
Corput theorem, and the metric-only adjudication}\label{sec:coherence}

Wave 7 (rounds r286--r289) freezes the coherence arc.  The r286 round
resolves the margin-decay warning of
Remark~\ref{rem:lstar-measured}(iii); the r287 round gives the L2
lemma its first \emph{named classical theorem candidate}; the r288
round maps the carrier of the destructive coherence; and the r289
round adjudicates the diophantine question --- \emph{against} number
theory.

\begin{theorem}[The van der Corput inequality; r287, v964 S0,
module-own exact]\label{thm:vdc}
For any real sequence $P_1,\dots,P_m$ and any window $H\ge1$, with
$A(h)=\sum_jP_jP_{j+h}$,
\[
\Bigl|\sum_{j=1}^{m}P_j\Bigr|^{2}
\;\le\;
\frac{m+H-1}{H}\sum_{|h|<H}\Bigl(1-\frac{|h|}{H}\Bigr)A(h),
\]
an exact, constant-free, arithmetic-free finite theorem (proof route
gated in v964: the Fej\'er window-sum identity
$\sum_{|h|<H}(1-|h|/H)A(h)=\frac1H\sum_tW_t^2$ with $W_t$ the sliding
$H$-window sums, the $H$-fold covering identity $\sum_tW_t=H\sum_jP_j$
and Cauchy--Schwarz over the $m+H-1$ window positions; the wrong
prefactor $m/H$ breaks exactly on $P=(1,1,1,1)$, $H=2$: $14<16$).
Measured on the 42-rung ladder at the frozen window
$H=\lceil\sqrt m\rceil$: $\delta'=+0.309>0.21$ \emph{world-blind}
(admissible: L2 is the generic half), certification $6/7$ exceptions
$+38/42$ rungs; only the kz15 razor blocks $7/7$ (miss $0.10$ dec) and
kz15 is exact-finite certified per r270.  The F1
discrepancy/Erd\H{o}s--Tur\'an frame dies loudly ($\delta'=-0.21$,
$0/7$: both factors grow).  Registered as
\texttt{PRIME.PORT.L2.VDC\_LEMMA.01}~\mO: the remaining input is the
\emph{chain origin} of the $P$-variance scaling --- per the r287
autocorrelation map the cancellation is already absorbed into the
$P_j$ magnitudes (on $39/44$ worlds the block sequence
\emph{reinforces} at root scale), so the frames win through the
$\sqrt m$ count economy, not lag structure.
\end{theorem}

\begin{remark}[The margin law resolved harmless; r286, typed
census]\label{rem:margin-law}
The ladder is extended by 15 new anchors ($N_w=942..1218$, $40\%$
beyond the old family cap): \emph{all} margins are mp-sign-safe
positive (minimum $+1.806\times10^{-8}$ at $z=529$; every anchor mp
dps-$30/45$ staggered; builder cross-check $\le2.9\times10^{-15}$) ---
no counterexample; the $O(1)$ census offset survives beyond the cap
(off distribution $\{0{:}1,1{:}10,2{:}2,3{:}1,4{:}1\}$, max $+4$).
The decay law is a \emph{flattening} power law ($\alpha\approx3.05$ vs
$S$; halves $3.67/2.07$, curvature $-1.60$) driven by the cancellation
sharpening $c_w\to1$, and the \textsc{harmless} reading is
quantified: the margin falls \emph{slower} than the local eigenvalue
loading speed (slope $-3.33\ge-4.44-1.0$) --- the decay is the
kinematic signature of the fixed $O(1)$ census offset at growing
eigenvalue density, not an approach to a crossing.  The terminal ratio
$q_N$ is reconciled (r258 \textsc{flat} reproduced: two different
objects), and the frozen 42-rung fit \emph{predicts} all 15 extension
margins inside the sealed 1-decade band
(\textsc{extrap-calibrated} $15/15$).
\end{remark}

\begin{remark}[The carrier map; r288, typed measurements]
\label{rem:carrier}
The destructive coherence of the L* coordinate has a named carrier
class: the neg fraction $0.503$ of the $E$-kernel sign field is
globally coin-like but sharply band-structured --- fold distance
$1$--$2$ is $87\%$ positive (sampled in phase), distance $3$--$4$ is
$80\%$ negative (antiphase); the wall destructivity is carried by the
\emph{antiphase next-nearest ARCH--ARCH pairs}
($X_v=-0.0517=+0.222-0.273$, $z_v=-3.149$ exactly reproduced,
$C_{\rm off}=-0.1046$), with \emph{total} control reversal (all four
controls constructive at their own crossing,
$C_{\rm off}=+0.30..+1.00$).  The coherence lives in \emph{finest
alignments} far below the median phase resolution: the chain zeros
co-move with the atoms (turn rate $0.24$ per unit dose $=$ the static
prediction $0.23$) yet $z_v$ flips already at dose $0.005$.  Honest
negatives: \textsc{sampling\_blind} (the sinc/CD predictor is only
partial, $0.728/0.878<0.90$), \textsc{source\_separator\_not\_found}
(plain phase dispersion world-blind and ensemble-generic),
\textsc{different\_objects} (the r287 magnitude absorption and the
kernel destructivity are two coordinates).
\end{remark}

\begin{remark}[The metric-only adjudication; r289, the central
refutation]\label{rem:metriconly}
The diophantine question --- does the wall consume the number-theoretic
exactness of the $\log n$ positions?\ --- is adjudicated by the sealed
three-way rule (rational keeps $+$ shuffle loses $+$ ladder does not
keep at $\theta_{\rm eff}$), with the twin table as the executioner:
\begin{center}\small
\begin{tabular}{@{}lccccc@{}}
\toprule
\textbf{Twin} & \textbf{depth} & \textbf{crossing} & $z_v$ &
\textbf{band 3--4} & \textbf{verdict}\\
\midrule
MAIN & $1.000$ & $185$ & $-3.149$ & $-0.105$ & ---\\
\textbf{rational} (dens $\le56801$, $|du|\le2.1\times10^{-9}$) &
$1.000$ & $185$ & $-3.149$ & $-0.105$ & \textbf{keeps, identical}\\
shuffle r0/r1/r2 ($\theta_{\rm eff}$ $0.18/0.12/0.12$) &
$0.201/0.212/0.239$ & $38/40/45$ & all $>0$ & $>0$ &
loses, metric-explained\\
\bottomrule
\end{tabular}
\end{center}
The rational twin replaces every tent center $\log n$ by a rational
$p/q\times\Delta$ (CF convergents; weights and cells bitwise
preserved), destroying \emph{every} exact log-linear-form relation ---
and keeps the full r288 signature \emph{identical} (mp dps-60 ward
$\rho_{184}=0.99983248<1<1.00003660=\rho_{185}$ confirmed at the
$1.7\times10^{-4}$ margin): the effect of the diophantine structure is
\emph{null}.  The jitter ladder places the metric coherence threshold
between $10^{-3}$ and $3\times10^{-3}$ of the local gap; the shuffle's
$\theta_{\rm eff}=0.12$ is $\sim$40--120x beyond it, so its loss is
fully metric-explained.  The only sub-gap entry point is exactly
identified: the tent-split fractions $\{\log n/\Delta\}$ (completeness
identity $4.2\times10^{-14}$; with $\Delta=\log2/46$ exactly, the
fractions are $\{46\log_2n\}$ and the eight on-node 2-power atoms give
exactly $\binom82=28$ exact resonances --- module-own exact in v964).
The Baker need is documented, never applied: the flip dose needs
fraction structure at exponent $\sim8.9$ while Baker/Matveev-class
two-log bounds deliver $\sim7\times10^{4}$ --- $\sim$7900x too weak
\emph{and} unnecessary per the verdict.  \emph{Verdict}
\textsc{metric\_only}: the firewall reads the fraction \emph{profile}
metrically, not its arithmetic --- every profile of $10^{-3}$-gap
precision carries (the rational twin proves it); the why-MAIN search
continues in profile space, and the precise open front is: \emph{which
functional of the fraction profile forces the destructive coherence?}
(The r290 profile-functional round was in flight at the wave-7 cut;
rounds r290--r295 are consumed in wave 8,
Section~\ref{sec:curvature}.)
\end{remark}

\certbox{PRIME.LSTAR.COHERENCE\_CENSUS.01 [E];\
PRIME.PORT.L2.VDC\_LEMMA.01 [O]}%
{verification/v964\_lstar\_coherence\_census.py}%
{Probes (embedded byte-exact, sealed smoke stage; full 16-hex SPEC SHAs
pinned in the module and in \texttt{rh/INVENTORY.json}):
\texttt{lstar\_margin\_scaling} (29/29, \texttt{0a44ac4e}),
\texttt{l2\_deterministic\_cancellation} (25/25, \texttt{761d88fa}),
\texttt{destructive\_coherence} (31/31, \texttt{da46f7ee}),
\texttt{arch\_kernel\_diophantine} (42/42 record, \texttt{91cdc2b1});
rounds r286--r289; the standalone document
\texttt{rh/problem/lstar\_problem.tex} carries the updated
``what is known'' state.}

%======================================================================
\section{The curvature arc (v965): the geometry of the working set and
the functional kill catalog}\label{sec:curvature}

Wave 8 (rounds r290--r295) freezes the curvature arc --- the answer to
the wave-7 question ``\emph{which functional of the fraction profile
forces the destructive coherence?}''\ in its honest form: the
\emph{geometry} of the working set is now quantitatively measured, and
the \emph{closed-functional} search is sealed negative over five class
families.  Nothing here is a theorem about L*; the arc is a measured
census plus a catalog of sealed rejections, and the rejections are the
load-bearing content.

\begin{remark}[The basin geometry; r290, typed measurements]
\label{rem:basin}
In the exact $\theta_{\rm eq}$ LAG coordinate (analytic reference
$\mathrm{REF} = \frac12\sum_j m_jg_j/\Delta = 125.75$; inversion
identity gated at $10^{-12}$), the working set around MAIN is a
\emph{soft-shouldered, strongly anisotropic tube}: the kill fraction
over 16 conservation-gated random directions is $0.38/0.62/1.00$ at
distances $5\times10^{-4}/10^{-3}/2\times10^{-3}$ (NEAR radius
$\sim5\times10^{-4}..2\times10^{-3}$ gap-equivalent, consistent with
the r285 ensemble percentile $0.00$); every onset is a soft shoulder,
no cliff.  The \emph{world-directed} axes kill 5--50x earlier than
random (onsets $4\times10^{-5}..2.3\times10^{-4}$ against the r289
jitter threshold $10^{-3}..3\times10^{-3}$): the rim is highly
direction-structured.  The SMOOTH (arithmetic) axis is a privileged
killer (onset $1.36\times10^{-4}$) yet \emph{orthogonal} to the
first-order wall gradient ($\cos = -3\times10^{-5}$; the midpoint is
already control-like, $\min C = 29$): its lethality is collective, not
gradient-linear.  The r280 ridge is \emph{real}: $\min C = 185$ ---
one degree above MAIN --- at extension factors $1..8$
($\theta_{\rm eq}$ $1.5\times10^{-4}..1.2\times10^{-3}$, inside a tube
where half the random directions already kill), first death at factor
16.
\end{remark}

\begin{remark}[The budget mechanism of the lift; r291/r293, typed]
\label{rem:budget}
The ridge lift is a \emph{first-order budget phenomenon}, not a
distinguished carrier: a conservation-gated sub-direction lifts iff
its side-selected linearized wall budget $-\sum_{j\in A}c_j\times$dose
crosses one sharp threshold in $(1.280, 1.291]$ --- perfect separation
over all 18 matched-dose cases (PRIME/HEAD/XIPOS lift,
POWER/MID/TAIL/XINEG do not; $k_{\min} = 9$, top atoms
$2,3,5,13,11,4,29,7,89$: small-prime head-heavy, not one-atom), with
exactly one overdrive retraction (TOP6 at factor 8).  The budget
itself is an exactly additive linear functional (the partition
telescope is gated module-own in exact Fractions, with the
dropped-atom mutant loud).  There is \emph{no fixed point}: the
step-controlled iteration saturates on the one-degree plateau
$\min C = 185$ (never 186) while the axis decoheres; and the lift is
MAIN-specific (EPSTEIN is first-order flat, $\theta_{\rm up} =
5.03\times10^{10}$, and never lifts).  The r293 flip anatomy closes
the mechanism at the singularity: all 8 selected flips are
\emph{simple zeros} of $h_{184}$ ($\alpha = 1.000\pm0.003$ both
sides; the doubling law $|h(t_0+2d)|/|h(t_0+d)| = 2^{\alpha}$ is
gated module-own exactly, with the double-zero mutant giving exactly
$4$), and the TOP6 retraction \emph{is} a second simple $h_{184}$
zero at $f_{\rm ret} = 7.107$ (lift window $1.226..7.107$) --- on the
side the monotone budget prognosis is structurally blind to.  But
\textsc{mstar\_no\_law}: the per-direction thresholds
$m^*_{\rm dir} = b\times f^*$ scatter $1.1295..1.3931$, straddling
the r291 bracket (a fixed-dose artifact); no fixed safety distance to
the singularity exists in budget, $\theta_{\rm eq}$, L2 or curvature
units (spreads $3.0/3.0/3.8/10.2$ vs bar $1.25$).
\end{remark}

\begin{remark}[The curvature spectroscopy; r292, typed]
\label{rem:spectroscopy}
The wall Hesse form at MAIN over the sealed 29-direction set
(diagonals at three dyadic steps plus the full 406-pair polarization
matrix $H(u,v) = [d^2(u{+}v) - d^2(u{-}v)]/4$, whose exact skeleton
is gated module-own in Fractions with the wrong-prefactor mutant
loud):
\begin{center}\small
\begin{tabular}{@{}lll@{}}
\toprule
\textbf{Finding} & \textbf{Value} & \textbf{Reading}\\
\midrule
all 29 diagonals & negative & a genuine quadratic valley\\
top eigendirection & $92.5\,\%$ of $\sum|\lambda|$ &
\textsc{hessian\_low\_rank}(1)\\
$\lambda_{\rm top}$ (per unit L2) & $-0.418$ & the valley scale\\
top axis & DENS05 $+1.00$ / DENS04 $-0.71$ / DENS03 $+0.69$ &
a pure on-support DENS combination\\
SMOOTH alignment & $|\cos| = 0.07 < 0.7$ & \emph{not} the arithmetic
axis\\
ridge position & L2-diagonal rank $28/29$ &
\textsc{ridge\_curv\_flat} (the r291 expectation)\\
EPSTEIN contrast & curvature ratio $5.4\times10^{-15}$ &
second-order structureless too\\
$m^*$ through jets & $q_R$ median $-0.911$ (assist);
$B_2 = 27.6$, $B_3 = 150.9$ & \textsc{threshold\_not\_explained} +
\textsc{retraction\_higher\_order}\\
\bottomrule
\end{tabular}
\end{center}
The curvature valley of the wall is real, rank-1 dominated, and its
axis is a DENS combination --- neither the SMOOTH axis nor the ridge;
the $m^*$ threshold and the retraction are near-flip nonlinearities
invisible to jets up to third order.
\end{remark}

\begin{remark}[The functional kill catalog; r290--r295, the sealed
negatives]\label{rem:killcatalog}
Five class families of closed profile functionals were sealed before
evaluation and all fail the honest bar:
\begin{center}\small
\begin{tabular}{@{}llll@{}}
\toprule
\textbf{Family} & \textbf{Round} & \textbf{Best $|\mathrm{sp}|$} &
\textbf{Bar / verdict}\\
\midrule
1: local profile scalars & r290 & $0.263$ &
$0.5$; \textsc{all\_functionals\_blind}\\
2: non-local / low-rank & r291 & $0.471$ &
size baseline $0.881$; \textsc{nonlocal\_blind}\\
3: curvature two-form (mixed metric) & r292 & $0.884$ &
baseline $0.907$; \textsc{curvature\_blind} (gap $0.023$)\\
4: metric-reconciled two-form F10 & r293/r294 &
home win $+0.024$ & partial $2/5 < 4/5$;
\textsc{f10\_fragile}\\
5: hardened partial-free $|\mathrm{sp}|$ form & r295 &
wins $14/20$ & HARDENED bar fails 2 clauses;
\textsc{f10\_sp\_majority}\\
\bottomrule
\end{tabular}
\end{center}
The r294/r295 decision bars are themselves gated module-own as exact
decision logic (every clause has tipping mutants in both directions).
After five families, \emph{no closed predictive profile functional of
the working set exists over the honest bar}; the question is
re-registered as \texttt{PRIME.LSTAR.CLOSED\_FUNCTIONAL.01}~\mO\ with
the named open fronts (loss-corpus forensics, the rank-2 DENS core,
why L2).
\end{remark}

\begin{remark}[The honest F10 story --- a case study in sealing
discipline]\label{rem:f10story}
The F10 arc is the program's cleanest demonstration of why bars are
sealed \emph{before} evaluation.  (i) r292: F10 loses the mixed-metric
contest by $0.023$ --- the near miss flags the metric mix.
(ii) r293: with three metrics sealed \emph{first}, F10 beats the
baseline of its own metric ($+0.024 = 0.884 - 0.860$, and the mixed
margin $-0.023 = 0.884 - 0.907$ is re-derived from the same pins:
\textsc{mix\_is\_cause}) and carries partial information ($+0.423$) —
a promotion candidate is flagged, nothing promoted.  (iii) r294: on
five fresh corpora the $|\mathrm{sp}|$ win replicates $5/5$ but the
partial channel does not (median $0.299 < 0.3$, $2/5$):
\textsc{f10\_fragile}, promotion \emph{rejected}.  (iv) r295: on
twenty fresh corpora even the partial-free form wins only $14/20$
(median $+0.028$, min $-0.079$): \textsc{f10\_sp\_majority} --- the
r294 $5/5$ was itself top-of-distribution, exactly as the r293
partial was (\textsc{r293\_luck}: the composition-matched subsample
explains part, $+0.067$ gain, and misses the level clause by
$0.004$).  A documented regularity ($19/25$ combined census), not a
theorem --- and the wave freezes exactly this: the regularity
\emph{with} its rejections.  F10 is not promoted.
\end{remark}

\certbox{PRIME.LSTAR.CURVATURE\_ARC.01 [E];\
PRIME.LSTAR.CLOSED\_FUNCTIONAL.01 [O]}%
{verification/v965\_lstar\_curvature\_arc.py}%
{Probes (embedded byte-exact, sealed smoke stage; full 16-hex SPEC SHAs
pinned in the module and in \texttt{rh/INVENTORY.json}):
\texttt{profile\_functional} (32/32, \texttt{f953dd71}),
\texttt{ridge\_anatomy} (30/30, \texttt{bb512c17}),
\texttt{curvature\_form} (36/36, \texttt{050821ff}),
\texttt{metric\_reconciliation} (43/43, \texttt{33c44cc6}),
\texttt{f10\_stability} (42/42, \texttt{88c6fd1e}),
\texttt{f10\_sp\_hardening} (44/44, \texttt{4d7d8095});
rounds r290--r295; F10 stays UNPROMOTED per the sealed r294/r295
bars; no round in flight at this cut.}

%======================================================================
\section{The L2 reduction chain (v966): the DENS fork closed, the
generic-half target reduced to one inequality}\label{sec:l2chain}

Wave 9 (rounds r296--r300) freezes two things.  First, the reviewer's
hard identity fork on the r292 curvature axis is \emph{closed
honestly}: r296 adjudicated \textsc{dens\_world\_blind} under sealed
rules --- the coupling number $\cos(e_{\rm top}, \nabla\lambda_{\max})
= +0.394$ sits below the sealed $0.40$ bar (noise max $0.331$; the
miss is $0.006$, reported as such), the $\lambda_3$ control couples
\emph{harder} ($-0.574$: what coupling exists is a near-crossing
eigenvalue-\emph{band} property, not top-specific), every arithmetic
candidate of the hash-sealed library stays $\le 0.38$, and the only
above-chance structure (the 4-dim moment-gradient subspace, $0.825$)
holds on both dead controls ($0.571/0.603$) --- construction-adjacent
low-moment smoothness, so the sealed world-downgrade fires.  The
DENS-identity lane is closed and, per the pre-adjudicated fork, the
resources moved to the L2 front.  Second --- the load-bearing content
of the wave --- the four-round chain r297$\to$r300 \emph{reduces} the
$\delta' > 0.21$ target of the generic half (the vdC L2 lemma,
Section~\ref{sec:coherence}) to \textbf{one remaining inequality}:
\[
\underbrace{\textsc{neff\_target}}_{\substack{\text{open at the
wave-9 cut}\\ \text{(wave 10: retyped, v967)}}}
\;\Longrightarrow\;
\underbrace{\mathrm{sl}_D \le \sigma^*}_{\text{exact algebra (r300)}}
\;\Longrightarrow\;
\underbrace{\sigma \le \sigma^*}_{\text{ratio structural (r300)}}
\;\Longrightarrow\;
\underbrace{\text{r297 target}}_{\text{margin } 0.198}
\;\Longrightarrow\;
\underbrace{\text{vdC (v964-S0)}}_{\text{proved}}
\;\Longrightarrow\;
\delta' > 0.21 .
\]
Every link is exact or structurally settled except the first;
exceptions stay finitely certified (6 via the r287 F2 certificates,
kz15 exact-finite via r270).  \textbf{No RH claim}; nothing here is a
cofinal theorem --- every slope is measured on the 42-rung ladder.

\begin{theorem}[The target equivalence; r297, exact composition]
\label{thm:sigmastar}
Let $\mathrm{sl}_{c2} = \mathrm{slope}(\varepsilon_{c2}/M)$ and
$\mathrm{sl}_{\rm pref} = \mathrm{slope}((m{+}H{-}1)/H)$ be the ladder
slopes of the r287 vdC bound and $\sigma =
\mathrm{slope}(S_F/M_W^2)$ the input-scaling slope.  By the exact
additivity $\mathrm{slope}(\varepsilon_{F2}/M) =
\tfrac12[\mathrm{sl}_{\rm pref} + \sigma]$, the statement
$\delta'(F2) > 0.21$ is \emph{equivalent} to the target inequality
\[
\sigma \;\le\; \sigma^* \;:=\; 2\,(\mathrm{sl}_{c2} - 0.21)
- \mathrm{sl}_{\rm pref} \;=\; -0.516,
\]
and $\sigma^*$ composes back to $\delta' = 0.21$ \emph{exactly}
(gated module-own in Fractions for arbitrary rational slope pairs;
the pad-dropped mutant composes to exactly $0$).  Measured: $\sigma =
-0.714$ --- the truth clears the target with $0.198$ sigma margin
($0.099$ in $\delta'$): \emph{what is missing is not room, it is
provenance}.
\end{theorem}

\begin{theorem}[The sign-preserving transfer; r298, exact identity]
\label{thm:transfer}
Let $\beta$ (border) and $\omega$ (window) be the two sealed measures,
$\Delta := \beta \ominus \omega$ their exact signed difference, and
$B(u,v) = \tfrac1H\sum_t W_t(u)W_t(v)$ the frozen positional Fej\'er
block form.  Then $S_F = B(\beta,\beta)$ and
\[
S_F \;=\; B(\omega,\omega) \;+\; B(\Delta,\, \omega + \beta)
\]
\emph{identically} (bilinear difference-of-squares; gated module-own
in exact Fractions on the r298 hand toy $3 = 1 + 2$ and a second
rational witness at $H = 2, 3$; on the real chain the identity holds
to $8.8\times10^{-16}$ of scale on 47 worlds).  Nothing passes through
magnitudes: the $|\Delta|$ mutant breaks the identity loudly (exactly
$4$ on the toy, $\sim$1.7 of scale on the real chain).  Measured
adjudication \textsc{transfer\_dominant}: the window main term is
\emph{empty} (med $-3.94$ dec, slope $-1.386$), the cross term
negligible (med $-1.4\times10^{-4}$), so $S_F \approx B(P\Delta,
P\Delta)$ --- \emph{the vdC input is the Fej\'er energy of the
difference measure itself}, and $\sigma$ is its decay exponent.
r299 adds the structural find: the two unions share their full
support on 42/42 rungs ($\Delta_{\rm fresh} \equiv 0$) --- $\Delta$
is a pure c-value difference measure on one shared node set, with no
pointwise c-convergence (cconv $0.86$, slope $+0.045$): the decay is
aggregate cancellation.
\end{theorem}

\begin{theorem}[The participation identity; r300, exact algebra]
\label{thm:participation}
For the one positive vector $|P\Delta|$ define $D = \sum_j
P\Delta_j^2$, $L_1 = \sum_j |P\Delta_j|$, $m_x = \max_j |P\Delta_j|$,
$n_{\rm eff} = L_1^2/D$ and $\mathrm{fill} = D/(m_x L_1)$.  Then
$D \cdot n_{\rm eff} = L_1^2$ and $D = m_x L_1 \cdot \mathrm{fill}$
identically, hence at the ladder-ratio level
\[
\mathrm{sl}_D \;=\; 2\,\mathrm{sl}_{L1} - \mathrm{sl}_{\rm neff}
\;=\; \mathrm{sl}_{mx} + \mathrm{sl}_{L1} + \mathrm{sl}_{\rm fill}
\]
exactly (gated module-own in Fractions with ratio multiplicativity;
the signed-sum mutant breaks by exactly $8/3$ on $(1,-1,1)$).  On the
record: $\mathrm{sl}_D = 2(+0.196) - (+0.963) = -0.571 = -0.542 +
0.196 - 0.225$ (devs $6.7/4.4\times10^{-16}$) --- \emph{the entire
diagonal decay is participation growth}, and the r299
\textsc{diag\_target} is an exact reparametrization of
\textsc{neff\_target}: $\mathrm{slope}(n_{\rm eff}) \ge
2\,\mathrm{sl}_{L1} - \sigma^* = +0.908$, measured $+0.963$, margin
$0.055$ exact in \emph{both} coordinates (machine-checked).
\end{theorem}

\begin{theorem}[The kernel envelope; r300, exact inequality]
\label{thm:envelope}
The Fej\'er kernel satisfies $F_H(\theta) \le \min\!\big(H,\,
1/(H\sin^2(\theta/2))\big)$ for all $\theta$ (pure algebra:
$\sin^2(H\theta/2) \le 1$), with exact equality witnesses on both
branches (as a rational function of $s = \sin^2(\theta/2)$: $H = 2$
at $s = 0$ and $s = \tfrac12$; $H = 3$ at $s = 0$ and $s = \tfrac14$;
the halved-envelope mutant breaks at the witness).  Consequence
(measured): $B/D \le R_{\rm env}$ with $R_{\rm env}$ med $1.61$ and
\emph{falling} ($-0.122 \le +0.05$) on 47/47 --- adjudicated
\textsc{ratio\_bounded\_structural}: the ratio half of the r299 rest
pair is settled at the structural level (honesty clause: $R_{\rm
env}$ still consumes the measured spectrum shape; the lobe-width
heuristic is refuted, $\mathrm{sp}(B/D, q_{50}) = -0.47$).
\end{theorem}

\begin{remark}[The magnitude no-go catalog; r297/r299/r300, sealed
negatives]\label{rem:magnitude-nogo}
Every magnitude route to the input scaling was closed with a named
break locus --- the signed structure carrying $\sigma = -0.71$ is
what $|\cdot|$-chains cannot see:
\begin{center}\small
\begin{tabular}{@{}llll@{}}
\toprule
\textbf{Route} & \textbf{Round} & \textbf{Composed vs $\sigma^* =
-0.516$} & \textbf{Break locus}\\
\midrule
B1 pair-identity ($\mathrm{gapmax}\times\varepsilon_{c2}$) & r297 &
$\delta'$ $-0.112$ & max pair gap falls $-0.535$, need $\le -1.178$\\
B2 node-density ($2\,\mathrm{maxM}\times\mathrm{sumM}$) & r297 &
$\delta'$ $-0.097$ & mass imbalance grows $+0.244$\\
B3 Fej\'er/Parseval (per-run CS) & r297 & $\delta'$ $-0.097$ &
measure transfer $+$ signed-to-abs gap ($+0.217$)\\
ET/Abel ($D_{\rm pos}\times V$, frequency level) & r299 & $+1.948 =
1.504 + 0.444$ exact & position/kernel factor \emph{grows}\\
chain-norm CS on $|dw|$ & r300 & $-0.384$ (miss $0.133$) & the
$|dw|$ identity census \emph{breaks} (cross $0.932$)\\
$\max \times L_1$ factorization & r300 & $-0.346$ (miss $0.170$
exact) & the fill decay $-0.225$ is invisible\\
\bottomrule
\end{tabular}
\end{center}
The r300 anatomy localizes \emph{why}: the max is fully
atom-controlled ($m_x/\mathrm{maxatom}$ med $1.07$), so the
irreducible loss of every $\max\times$mass bound is the profile's
internal evenness loss (fill, $-0.225$); and the proven window sum
rule does \emph{not} attach to the difference measure (the $|dw|$
census breaks) --- the chain-norm shortcut is closed honestly.
\end{remark}

\begin{remark}[Two world-separating classes; r299/r300, disclosed]
\label{rem:worldclasses}
The lane now carries two disclosed \textsc{world\_sensitive} classes,
the first world-separating signals of the L2 side: the \emph{O-sign
class} (r299: MAIN's $P\Delta$ pair field \emph{reinforces}, $O < 0$
on 13/42 only --- \textsc{o\_pos} --- while both broken-arithmetic
controls cancel, \textsc{o\_neg}) and the \emph{fill class} (r300:
MAIN is \textsc{fill\_low}, both dead controls \textsc{fill\_high}).
Both are typed disclosed findings on control rungs, not gates, and
neither is used as a proof premise.
\end{remark}

\begin{openproblem}[The NEFF target; the one remaining inequality]
\label{op:neff}
Prove $\mathrm{slope}(n_{\rm eff}) \ge 2\,\mathrm{sl}_{L1} - \sigma^*
= +0.908$ for the participation number $n_{\rm eff} = L_1^2/D$ of the
aggregated difference profile $P\Delta$ (measured $+0.963$, margin
$0.055$, thin) --- source-pure and window-independent.  By
Theorem~\ref{thm:participation} this is exactly the diagonal decay
$\mathrm{sl}_D \le \sigma^*$, and through the chain above it closes
$\delta' > 0.21$ on the generic half.  The named bridge to provable
terrain is \emph{correlational, not causal}: the carrier count grows
where the boundary-position discrepancy falls ($D_{\rm rank}(\Delta)$
slope $-0.117$, $\mathrm{sp}(D_{\rm rank}, n_{\rm eff}) = -0.81$) ---
a proof must turn this correlation into a mechanism.  Ledger
\texttt{PRIME.L2.NEFF\_TARGET.01}~\mO; the executing round r301 was
in flight at the wave-9 cut and \emph{not} consumed there.  In Lean
no new statement is added: \textsc{neff\_target} is a measured
ladder-slope aggregate (a halves log-slope estimator over 42 rungs
with a threshold derived from measured slopes), not an exact finite
identity or a matrix theorem, and a universally quantified ladder
form would be refutable exactly like the pre-r273 universal edge
forms (the machine-checked guard pattern of
\texttt{RH/Counterexamples.lean}); the hole stays
\texttt{lstar\_subordination}.  \emph{Wave-10 resolution
(Section~\ref{sec:cascade-closure}): rounds r301--r304 executed and
consumed in \texttt{v967} --- the r303 regress audit retyped this
inequality: NEFF/UNIF/ATOM are one algebraic slack $S = \sigma^* -
\mathrm{sl}_D = +0.0547$ (Theorem~\ref{thm:margininv}), and the
contract stands retyped to the documented stop state
(Remark~\ref{rem:lanestop}).}
\end{openproblem}

\certbox{PRIME.L2.REDUCTION\_CHAIN.01 [E];\
PRIME.L2.NEFF\_TARGET.01 [O]}%
{verification/v966\_l2\_reduction\_chain.py}%
{Probes (embedded byte-exact, sealed smoke stage; full 16-hex SPEC SHAs
pinned in the module and in \texttt{rh/INVENTORY.json}):
\texttt{dens\_identity} (39/39, \texttt{ffb413c8}),
\texttt{vdc\_chain\_provenance} (27/27, \texttt{e42a76eb}),
\texttt{window\_border\_transfer} (28/28, \texttt{05e831be}),
\texttt{fejer\_decay} (32/32, \texttt{f432e944}),
\texttt{diag\_target} (31/31, \texttt{55218b5d});
rounds r296--r300; the DENS lane closed honestly
(\textsc{dens\_world\_blind}); the chain reduced to
\textsc{neff\_target}; r301 in flight at that cut (consumed in wave
10, \texttt{v967}, Section~\ref{sec:cascade-closure}).}

%======================================================================
\section{The cascade closure (v967): the regress audit, the retyping
and the documented lane stop}\label{sec:cascade-closure}

Wave 10 (rounds r301--r304) is the honest end of the reduction
cascade --- \emph{the rejections and retypings are the content}.
Rounds r301/r302 executed the wave-9 rest targets to their end: r301
proved the count/uniformity identity $n_{\rm eff} = n_{\rm act}/(1 +
CV^2)$ exactly with the \emph{perfect} count link $n_{\rm act} = m$
on 42/42 (the effective-carrier count \emph{is} the constructive
level-2 block count; the whole R\'enyi order family grows at one
exponent --- echt anti-concentration; the margin is
jackknife-stable) and relocated the rest onto \textsc{unif\_target};
r302 found the normalized block profile \textsc{profile\_stationary}
(KS $0.043 \ll 0.125$ over a $3\times$ depth range) with an exact
$1/N$ transient onto $m_2^\infty = 1.973$ (held-out dev $0.002$; the
r299 pointwise-cconv negative was the wrong convergence notion),
proved the coherence identity below, and fired
\textsc{unif\_derived}(B1{+}A2) --- the first \textsc{derived}
verdict of the lane, every hypothesis typed \textsc{measured} ---
relocating the rest onto \textsc{atom\_target}.  Then the reviewer
set the regress audit, and r303/r304 closed the lane.  \textbf{No RH
claim}; nothing here is a cofinal theorem.

\begin{theorem}[The coherence identity; r302, exact algebra]
\label{thm:coherence}
On the shared bulk support with atom c-differences $dc$, block sums
$P_j$, atom energy $Q = \sum dc^2$, atom mass $L_1^a = \sum|dc|$,
block energy $D = \sum P_j^2$ and block mass $L_1 = \sum|P_j|$, let
$\chi = D/Q$ (the in-block coherence factor), $\mathrm{surv} =
L_1/L_1^a$ and $n_{\rm eff}^{\rm atom} = (L_1^a)^2/Q$.  Then
\[
1 + CV^2 \;=\; \frac{n_{\rm act}\,\chi}{\mathrm{surv}^2\,
n_{\rm eff}^{\rm atom}}
\qquad\text{and}\qquad
\chi \;=\; 1 + \frac{2}{Q}\sum_{k\ge1} T_k
\]
identically, where $T_k$ is the within-block lag-$k$ cross sum
(gated module-own in exact Fractions on two rational toys, $\chi =
5/7$ and $5/19$; on the real chain the wards hold at
$6.0\times10^{-16}$ and $4.7\times10^{-16}$ on 47 worlds; the lag-0
double-count, swapped-$D/Q$ and factor-1 mutants break by exactly
$5/7$, $48/35$ and $1/7$).  Measured: $\chi = 0.63$
\emph{destructive} and falling --- the lag-1 anti-correlation
$\rho_1 = -0.22$ of the $dc$ profile is its atom-level mechanism.
\end{theorem}

\begin{theorem}[The margin invariance; r303, exact algebra ---
the regress audit]\label{thm:margininv}
Let $\mathrm{NEED} = 2\,\mathrm{sl}_{L1} - \sigma^*$,
$\mathrm{UNIF\_NEED} = \mathrm{sl}_{\rm nact} - \mathrm{NEED}$ and
$\mathrm{ATOM\_NEED} = \mathrm{sl}_{\rm nact} + \mathrm{sl}_\chi -
2\,\mathrm{sl}_{\rm surv} - \mathrm{UNIF\_NEED}$.  Under the three
gated product identities (r300: $\mathrm{sl}_D = 2\,\mathrm{sl}_{L1}
- \mathrm{sl}_{\rm neff}$; r301: $\mathrm{sl}_{\rm neff} =
\mathrm{sl}_{\rm nact} - \mathrm{sl}_{CV^2}$; r302:
Theorem~\ref{thm:coherence} at the slope level) the four target
margins
\[
m_D = \sigma^* - \mathrm{sl}_D,\quad
m_{\rm NEFF} = \mathrm{sl}_{\rm neff} - \mathrm{NEED},\quad
m_{\rm UNIF} = \mathrm{UNIF\_NEED} - \mathrm{sl}_{CV^2},\quad
m_{\rm ATOM} = \mathrm{sl}_{\rm natom} - \mathrm{ATOM\_NEED}
\]
are pairwise \emph{equal} --- the cascade slack is \textbf{one
algebraic number} (gated module-own in exact Fractions: all four
margins $= 3/50$ on the rational slope set; the halved-need mutant
breaks the $D \leftrightarrow$ NEFF invariance by exactly
$\mathrm{sl}_{L1} = 1/5$).  Measured: $m_D = m_{\rm NEFF} = m_{\rm
UNIF} = m_{\rm ATOM} = +0.0547$ (invariance devs $\le
9\times10^{-16}$), the core being $S = \sigma^* - \mathrm{sl}_D$,
i.e.\ the r299 \textsc{diag} margin.  The $\sigma$ level is
\emph{not} the core ($\sigma^* - \sigma = +0.1976 = S + $ ratio
surplus $+0.1429$ exact), and the $1/2$-conversion conjecture is
\emph{refuted} ($0.0988 \ne 0.0547$: the factor 2 lives only in
$\mathrm{NEED}$ and cancels in every margin difference).
\end{theorem}

\begin{remark}[The retyping --- a methodological lesson]
\label{rem:retyping}
Theorem~\ref{thm:margininv} \emph{retypes} the cascade
r297$\to$r302: six rounds of exact reduction were \emph{coordinate
finding} --- an exact dictionary locating one measured slack in
progressively sharper coordinates --- not six proof steps.  The
sealing discipline is what caught the reparametrization trap: each
round had disclosed its own clause as ``an exact reparametrization''
(r300-B3, r301-B1, r302-B1), and the sealed regress audit turned the
disclosures into an adjudicated verdict (\textsc{regress\_confirmed})
instead of letting the lane count the same slack four times.  The
structure gains of the levels (count link, stationarity, coherence
identity, $\chi$ destructive, lag-1 trace) \emph{stand}; but the hard
reviewer rule binds from r303 on: \emph{a round counts as progress
only if it adds new information, not if it re-expresses the same
slack in a new coordinate}.  The [E] status of the wave-9 exact
identities is unchanged --- the reading is refined.
\end{remark}

\begin{remark}[The first causal coordinate and the two-scale
mechanism; r303/r304, typed measurements]\label{rem:twoscale}
The r303 mechanism test (1008 sealed marginal-preserving builds:
bitwise multiset permutations with $\rho_1$ steered to matched /
zero / flipped targets) delivers the lane's first \emph{causal}
coordinate: the ladder is monotone in $\rho_1$ ($\chi$ $0.630 \to
0.764 \to 1.029 \to 1.342$; end margin $+0.055 \to +0.057 \to +0.032
\to -0.044$) --- \emph{flipping the mixing sign kills the target
inequality}.  But $\rho_1$ alone is insufficient
(\textsc{mixing\_insufficient}: the matched family misses the
destructive $\chi$ level by $0.134$), and r304 names the split
exactly: matching $\rho_{1..8}$ closes the $\chi$ \emph{level}
($0.652$ vs $0.630$, $|d| = 0.022 \le 0.05$) at the price of the
\emph{slopes} ($0.028/0.027 > 0.02$) --- the exact complement of the
r303 family.  \textbf{The mechanism is two-scale}: the level lives
in the short lags ($k \le 3$, lag-2-dominated, exactly attributed
via Theorem~\ref{thm:coherence}), the slopes in the rung-wise
$\rho_1$ trend; no lag-matching family reproduces both.  The
graduated reviewer ladder is monotone in $\chi$ but \emph{not} in
the margin (honest negative: absolute-level targets do not kill the
inequality --- the r303 kill came from per-rung flipped targets).
Bycatch at theorem grade: $n_{\rm eff}^{\rm atom} = (L_1^a)^2/Q$ is
a pure \emph{marginal} functional (invariance $10^{-15}$ on all
$2100 + 1008$ builds) --- stationarity and mixing are complementary,
not redundant.
\end{remark}

\begin{openproblem}[The period-4 comb --- the named open object]
\label{op:comb}
The centered lag profile $\rho_{1..16}$ of the $dc$ profile on the
shared bulk support is a \emph{stable, world-specific period-4 comb}
(med $-0.222/-0.140/+0.089/+0.130$, then strong lags at every $k =
4m$ ($+0.13..+0.16$) and $k = 4m+2$ ($-0.14..-0.15$) up to 16;
halves-stable 3/3; \textsc{epst}/\textsc{scr} differ), \emph{not} a
decaying tail: the sealed short-range rule finds no $k_0 \le 8$.
The reviewer condition splits exactly: the net covariance $NC(16) =
1 + 2\sum_{k\le16}\rho_k = 0.712 < 1$ \emph{holds} (net-negative)
while summability-with-small-tail \emph{fails} ($\mathrm{SUM}(16) =
1.563$); the full lag sum is the zero-sum tautology ($1 +
2\sum_{\rm all}\rho_k \equiv 0$ for mean-removed finite profiles,
gated live), so only the truncated $NC$ carries content.  The sign
pattern of $S_{1..4}$ is $-/-/+/+$ with Fractions-exact certificates
on both smallest rungs (kz18, kz23).  \emph{What is the comb?}  Its
derivation from the three-term recursion, its world-specificity and
its invisibility to $\chi$ (the within-block shares die by $k \sim
4$) are measured facts without a mechanism --- the comb is
registered as the named open object of the stopped lane, not as a
target.
\end{openproblem}

\begin{remark}[The documented lane stop; r304 consequence (iii)]
\label{rem:lanestop}
The sealed reviewer stop case fired (\textsc{law\_longrange}): the
\textbf{global-profile mixing route of the L2 lane is documented
closed}.  The clean equivalence state at the stop: \emph{L2 on the
generic half $\Leftrightarrow$ anti-concentration of an explicit
block field with long-range period-4 structure} (core slack $S =
\sigma^* - \mathrm{sl}_D = +0.0547$ measured); return later with new
tools --- per the hard rule, no further coordinate change counts as
progress.  What stands honestly for a future attempt: the exact
identity dictionary (count, coherence, $\chi$-lag), the two-scale
mechanism split, $NC < 1$, the $-/-/+/+$ sign pattern, and the
marginal-functional invariance of $n_{\rm eff}^{\rm atom}$.  This is
a documented stop, not a defeat theorem: the period-4 comb is a
measured finite-window object (42 rungs); no cofinal law is claimed
for or against.  In Lean no new statement is added: the stop carries
no new exact target form (the comb and the two-scale split are
measured ladder aggregates, not exact finite identities, and a
universally quantified ladder form would be refutable --- the
machine-checked guard pattern of
\texttt{RH/Counterexamples.lean}); the hole stays
\texttt{lstar\_subordination}.
\end{remark}

\certbox{PRIME.L2.CASCADE\_CLOSURE.01 [E];\
PRIME.L2.NEFF\_TARGET.01 [O, retyped to the documented stop
state];\ PRIME.L2.REDUCTION\_CHAIN.01 [E, reading refined]}%
{verification/v967\_l2\_cascade\_closure.py}%
{Probes (embedded byte-exact, sealed smoke stage; full 16-hex SPEC
SHAs pinned in the module and in \texttt{rh/INVENTORY.json}):
\texttt{neff\_target} (32/32, \texttt{6f8cc404}),
\texttt{unif\_target} (30/30, \texttt{36df9424}),
\texttt{atom\_target} (26/26, \texttt{375e9f2b}),
\texttt{shortrange\_law} (37/37, \texttt{2cc5d23f});
rounds r301--r304; the cascade retyped as an exact dictionary
(\textsc{regress\_confirmed}, core $S = +0.0547$); the
global-profile mixing route documented closed
(\textsc{law\_longrange}); no round in flight at this cut.}

%======================================================================
\section{The architecture adjudication (v968)}
\label{sec:architecture-adjudication}

Wave 11 (rounds r305--r316 incl.\ r310b) is the \emph{architecture
day}: after the wave-10 lane stop the reviewer set a three-lane
contest (A: block-Green / constructive PSD-G rule; B: paired-cone /
rank-one stream; C: fixed head) in parallel with the sharpest new
fiber attack (the pointwise R\'enyi-3 law) and a Lean reconstruction
of the formal layer.  The day is frozen \emph{in the reviewer's
binding four-level structure} --- each level is a different epistemic
type, and the module \texttt{v968\_architecture\_adjudication.py}
gates the composition so that the assignment is machine-checked, not
editorial.  \textbf{No RH claim}; nothing here is a cofinal theorem.

\subsection{Level 1: real formal theorems (Lean, proved)}

The formal layer was rebuilt in three Lean rounds (r305, r310,
r310b; \texttt{rh/lean/RH/Closure.lean} and
\texttt{RH/Source.lean}); the artifacts are already merged and
green --- \emph{no Lean change ships with this promotion}.  The
sorry census fell $9 \to 5$ with clean bookkeeping: the two
\emph{true} formal window-local holes now stand alone
(\texttt{lstar\_subordination}, \texttt{terminal\_positive\_main}),
next to the \texttt{crossing\_budget} mathlib gap (not
mathematical; v962-certified), the definitional source bridge
(opacity-forced) and the pair-coordinate refinement
\texttt{pair\_margin\_main}.

\begin{center}\small
\begin{tabular}{@{}lp{0.55\linewidth}l@{}}
\toprule
\textbf{Theorem (\texttt{RH/Closure.lean}, r305)} & \textbf{Content}
& \textbf{Status}\\
\midrule
\texttt{lstar\_terminal\_implies\_master} & L* $+$ terminal $q_N<1$
$\Rightarrow$ full master positivity (Schur bordering $+$ principal
submatrices; the Riccati backward drain in matrix coordinates) &
proved\\
\texttt{augmented\_prefix\_positive} & the former master
\texttt{sorry} --- now a \emph{corollary}, no third arithmetic
axiom & proved (corollary)\\
\texttt{free\_window\_positivity} & the base half in fog-free
coordinates & corollary\\
\texttt{terminal\_positive\_main} & $0 < B \wedge q < 1$ (the
border/fiber half; r258 \textsc{terminal\_q\_law} form) & the
TRUE hole (\texttt{sorry})\\
\midrule
\textbf{Inertia layer (r305)} & \texttt{psd\_base},
\texttt{positive\_prefix\_firewall} (incl.\ a from-scratch Sylvester
criterion \texttt{posDef\_of\_leading\_det\_pos} --- mathlib v4.29.1
has none), \texttt{sylvester\_pullback},
\texttt{half\_filling\_boundary} & proved (4 of 5;
\texttt{crossing\_budget} stays a mathlib gap)\\
\bottomrule
\end{tabular}
\end{center}

\begin{center}\small
\begin{tabular}{@{}lp{0.55\linewidth}l@{}}
\toprule
\textbf{Theorem (\texttt{RH/Source.lean}, r310/r310b)} &
\textbf{Content} & \textbf{Status}\\
\midrule
\texttt{primePow\_index\_injective}, \texttt{nodes\_injective} &
unique factorization $\Rightarrow$ strict node monotonicity on the
unfolded prime-power chain & proved\\
\texttt{buildPrimeWindow\_source\_exact} & source exactness by
\texttt{rfl} \emph{through derivation} (nodes $= \log p^k$, weights
$= \Lambda(p^k)$ via mathlib \texttt{vonMangoldt}) & proved\\
\texttt{foldedWindow\_mass} (+ comb/arch channels) & mass
conservation of \emph{any} folding map via
\texttt{Finset.sum\_fiberwise} --- aggregation is a quotient, not a
projection & proved\\
\texttt{buildPrimeWindow\_support\_canonical} & the full four-stage
support chain for every folding map & proved\\
\texttt{predefined\_family}, \texttt{cofinal\_prime\_windows} &
constructive atom census; cofinality via Euclid & proved\\
\texttt{finite\_forms\_converge\_to\_weil} & the \emph{strong}
stabilization form: the finite comb form \emph{equals} the Weil
tsum prime side from anchor $\ge \lceil e^b \rceil$ (exact
stabilization, not mere pointwise convergence) & proved\\
\texttt{mainWindow\_iff\_builtFromPrimeSource} & the definitional
source bridge (opacity-forced) & documented \texttt{sorry}\\
\bottomrule
\end{tabular}
\end{center}

Also on this level: the r309 \emph{rank-one update identities}
(Section below, frozen per reviewer case 3 as
\texttt{PRIME.SOURCE.RANKONE.UPDATE.IDENTITY.01}~\mE) and the r306
Hill monotonicity $+$ R\'enyi-3${}\Rightarrow N_2$ bridge (two
Lagrange witnesses, Fractions dev 0; re-proved module-own in the
v968 exact section S0, Lean-ready statement printed by the probe).

\subsection{Level 2: certified finite statements}

\begin{center}\small
\begin{tabular}{@{}p{0.44\linewidth}p{0.38\linewidth}l@{}}
\toprule
\textbf{Statement} & \textbf{Certificate grade} & \textbf{Round}\\
\midrule
R\'enyi-3: $\sum_j q_j^3 \le C(\log m)^2/m^2$ with $C = 1.069$,
$A = 2$ on all 57 rungs, growing reserve (trend $-0.322$) &
record-pinned, 57/57; the Hill/Lagrange chain exact (Fractions dev
0) & r306\\
Fixed-head census: the \emph{full} flat arch edge restores
macroscopic reserve (min $2.19\times10^{-3}$, median amplification
$3.1\times10^4$) on 77/77 windows & record-pinned census & r307\\
Block-Green identity $Q = \sum\Delta^{\mathsf T}G\Delta +
\tfrac57 t^2$ on the finite objects & symbolically exact (toy $t^2$
pin $4/7$); f64 $4.0\times10^{-14}$ on w9, 57/57 rungs & r308\\
Strictness of the source cone: $C_w \subsetneq
\mathrm{span}\cap S_+$ & \emph{fully exact} rational in-span Farkas
certificates (MINI\#0, SM1\#0); $0/16$ in-span PD samples decompose
& r311\\
Paired-cone soundness: the sealed mass-ratio cone, $0$ violations
on $20714$ steps (certifying 56\,\%) & record-pinned; sound, not
complete & r309\\
Fold multiplicity $\equiv 2$ (exact class bound) $+$ 93\,\%
far-triple cancellation ($TC_{\rm far} = 0.069$, falling) & exact /
record-pinned on 57/57 & r313\\
The signed cubic identity $\sum_j|x_j|^3 = \Delta F + C_{\rm
collision} + C_{\rm boundary}$ with \emph{vanishing} boundary
($F_1 \equiv 0$, cubic algebra) & decided in \emph{exact Fractions
on the real w9 data} (every deviation the rational 0); f64
$4.5\times10^{-17}$ on all 57 rungs & r314\\
The 21 small-$m$ certificates ($C_{\rm small} = 1.0694$ $=$ the
r306 shallow maximum) & individually certified rungs & r316\\
\bottomrule
\end{tabular}
\end{center}

\subsection{Level 3: negative architecture decisions}

The negative decisions are the day's load-bearing content ---
verbatim, with the break loci named:

\begin{center}\small
\begin{tabular}{@{}p{0.30\linewidth}p{0.50\linewidth}l@{}}
\toprule
\textbf{Decision} & \textbf{Content (break locus)} & \textbf{Round}\\
\midrule
\textsc{fixed\_head\_dead} & no fixed $r \le 8$ exists (best
reserve $6.5\times10^{-6}$, four decades under the sealed
$10^{-2}$ bar; reserve decays with $S$ in the r286 law class) ---
the one-atom anatomy is proof-relevant as a \emph{coordinate},
refuted as a \emph{fixed dimension} & r307\\
\textsc{paired\_cone\_no\_induction} $+$
\textsc{paired\_cone\_world\_blind} & the B1--B4 adjudication: B2
only partial (56\,\%), B3 (real induction $K_j \to K_{j+1}$) not
reached, B4 (world semantics) missed --- \textbf{B does not
overtake A}; the only separating scalar is the friendly mirror
(the r243 wall reading itself) & r309\\
r308 discriminator demarcated & the r308 world separation is
\emph{entirely} the budget sign: the $\Delta$-sparsity graph is
chordal on every size (incl.\ w9 $S = 367$), the trivial common
dual $Y_t = e_te_t^{\mathsf T}$ closes the r308 stalls two-sidedly
& r311\\
\textsc{coefficient\_sign\_wall} & \textbf{Lane A closes as a cone
language} with the mechanism \emph{named}: block-psd membership
without rank-one-SDD membership, obstructed at the budget/border
sign (Farkas mass on the border/budget row: $t$-share $0.583$,
$0.725$ at deg28; the entire obstruction in 2.4\,\% SDD-forbidden
negative cross-mixings) & r312\\
\textsc{floquet\_expanding} & the four-step monodromy on the split
cubic mode space \emph{expands} (med sr $1.0600$, $0/57$
contracting, exponent $+1.917$) --- after r304 $+$ r313 no
transfer-operator contraction form remains open at chain level &
r313\\
\textsc{triple\_type\_majorants\_wrong} & T2/T3 hold $0/57$ but T1
falls at kz55/kz53 and T4 at kz55 (trends falling) --- composition
refuted in form; type constants not world-portable & r313\\
\textsc{phi3\_all\_blind} & all three presealed source-pure
candidates fail; the \emph{entire} break sits on kz55$+$kz67 (the
same near-critical family as r306-$A{\le}1$ and r313-T1/T4); local
cause named: FCIX $0.955/0.915$ vs med $0.629$ --- exactly where
the flux cancellation dies & r315\\
\textsc{two\_regime\_dead} & the anatomy is the find: the
obstruction family cuts \emph{across} the FCIX stratum (kz53 kills
the L side at bulk-normal FCIX $0.654$); the r306 first-5 protocol
was \emph{load-bearing}, not a shallow artifact --- any tight
pointwise majorant dies at kz53 under a mid-ladder freeze;
kz55/kz67 are near-one-block worlds (TOP1 cube share $0.558/0.785$
vs $0.18$): concentration, not counting tightness, separates &
r316\\
\bottomrule
\end{tabular}
\end{center}

\subsection{Level 4: open mechanisms}

\begin{center}\small
\begin{tabular}{@{}p{0.40\linewidth}p{0.48\linewidth}@{}}
\toprule
\textbf{Open mechanism} & \textbf{Type / where it lives}\\
\midrule
The constructive PSD-G rule & closed as a \emph{lane} (r312), open
as a \emph{question}; \texttt{PRIME.LSTAR.CLOSED\_FUNCTIONAL.01}~\mO\
carries the wave-11 lane closure note\\
The signed cubic flux theorem & the identity is exact (r314), no
bound follows yet; the majorant chains transfer any future
certification by algebra onto $\sum q^3$\\
The quantitative source functional & the class
\textsc{RecursiveDifferenceProfile} needs a \emph{quantitative}
membership functional (r313's sharpest honest negative: no boolean
property separates like the r306 total bound)\\
The tensor mechanism & \texttt{PRIME.SOURCE.TENSOR.MECHANISM.01}~\mO:
does a source-pure cofinal control arise from the banked rank-one
update identities?  Registered \emph{after} r313/r316 with the
documented negatives\\
The R\'enyi-3 provenance & \texttt{PRIME.L2.RENYI3.PROVENANCE.01}~\mO:
the GO stands, the provenance is open (trilogy: identity exact,
functional blind, two-regime dead); the R317 fork sits with the
reviewer --- a near-critical family coordinate (TOP1/QMAX
source-pure in advance; kz53 needs a second coordinate) \emph{or} a
certified exception family (the r287-F2 pattern)\\
\texttt{crossing\_budget} & a mathlib gap (Jacobi inertia / minor
dictionary), not a mathematical one --- v962-certified\\
The definitional source bridge & \texttt{mainWindow\_iff\_
builtFromPrimeSource}, opacity-forced\\
L* and the terminal & the two TRUE formal window-local holes
(\texttt{lstar\_subordination}, \texttt{terminal\_positive\_main})\\
\bottomrule
\end{tabular}
\end{center}

\begin{remark}[The claim split --- binding]\label{rem:claimsplit}
Per the reviewer's explicit instruction the r309 outcome is
\emph{not} frozen under one ``transfer'' title.
\texttt{PRIME.SOURCE.RANKONE.UPDATE.IDENTITY.01}~\mE\ carries
\emph{only} the exact update identities (the determinant update
dictionary, the Sherman--Morrison chain along the ordered stream,
the exact $\pm(\eta \pm e_t)$ border split, the local Schur reserve
$r = 1 - L_{bb} + L_{ab}^2/(1 + L_{aa})$, the signed source
updates); \texttt{PRIME.SOURCE.TENSOR.MECHANISM.01}~\mO\ carries
the open question whether a source-pure cofinal control arises from
them --- registered \emph{after} r313/r316 so the documented
negatives (no chain-level contraction, no two-regime majorant) are
part of the contract, not a surprise.  The v968 four-level
composition gate checks that the split is \emph{not} hard-wired:
a passing B2--B4 would hand the lane to architecture B, a firing
r312 GO would keep Lane A open, a surviving two-regime bound would
resolve the provenance --- each tipping mutant changes the composed
adjudication.
\end{remark}

\begin{remark}[Universality classes --- base and fiber]
\label{rem:universality}
The day sharpens a structural reading that frames every lane: the
\emph{base} is fine-metric --- the r289 \textsc{metric\_only}
adjudication and the twin invariance recur on every wave-11 surface
(the rational twin keeps the full signature; the r307 twin metric,
the r308 diophantine-trivialized twin and the r315/r316 twin bands
are the strongest of the lane).  The \emph{fiber} is recursive ---
on every fiber law of the day \textsc{epstein} holds and
\textsc{scramble} breaks (the r306 bound, the r314 shares, the
r315 candidates, the r316 majorants): the laws live in the
recursion structure, not in block combinatorics.  Base and fiber
share the \emph{algebra} (the identity dictionary applies to both)
but not necessarily the \emph{mechanism} --- the reviewer's
maturity assessment raised the program $4 \to 5$ (formal
architecture $9.5$) on exactly this reading; it is typed here as a
\textbf{reviewer assessment}, not a machine-checked fact.  The
wave-12 adjudication supersedes this single scalar with a per-layer
table (Remark~\ref{rem:reviewertable}).
\end{remark}

\certbox{PRIME.LSTAR.ARCHITECTURE\_ADJUDICATION.01 [E];\
PRIME.SOURCE.RANKONE.UPDATE.IDENTITY.01 [E];\
PRIME.SOURCE.TENSOR.MECHANISM.01 [O];\
PRIME.L2.RENYI3.PROVENANCE.01 [O]}%
{verification/v968\_architecture\_adjudication.py}%
{Probes (embedded byte-exact, sealed smoke stage; full 16-hex SPEC
SHAs pinned in the module and in \texttt{rh/INVENTORY.json}):
\texttt{renyi3} (27/27, \texttt{3bb365e1}),
\texttt{fixed\_head} (28/28, \texttt{ec2bb008}),
\texttt{block\_green} (30/30, \texttt{d5147850}),
\texttt{paired\_cone} (33/33, \texttt{f8d99877}),
\texttt{blockgreen\_nontriviality} (34/34, \texttt{fac7a8df}),
\texttt{blockgreen\_membership} (32/32, \texttt{6c32f749}),
\texttt{renyi3\_proof\_fork} (32/32, \texttt{6505dd10}),
\texttt{signed\_cubic\_flux} (29/29, \texttt{841b3196}),
\texttt{phi3\_functional} (29/29, \texttt{92d35a3a}),
\texttt{two\_regime\_bound} (35/35, \texttt{5c28b12b});
rounds r305--r316 incl.\ r310b; the Lean rounds r305/r310/r310b are
consumed as reports (artifacts in \texttt{rh/lean/}, re-verified by
\texttt{lake build} and the RH suite); no round in flight at this
cut --- R317 waits on the reviewer fork.}

%======================================================================
\section{Red team and the two forks (v969)}\label{sec:forks-redteam}

Wave 12 (rounds r317--r322) is the \emph{red-team morning}: after the
reviewer's asymmetry decision (the terminal has a proof path, L*
needs a new idea; resource split 40/40/20) an independent
extraction-chain audit ran against the formal layer while the two
reviewer forks executed in parallel lanes.  The module
\texttt{v969\_forks\_and\_redteam.py} freezes the six rounds; the
audit findings U1--U3 are re-established \emph{module-own in exact
arithmetic} (rational witnesses, integer certificates --- no Lean
call), so the suite carries the inconsistencies independently of the
Lean toolchain.  \textbf{No RH claim}; nothing here is a cofinal
theorem.

\subsection{The extraction-chain red team (r319) and the repair
(r320)}\label{sec:redteam}

Round r319 reconstructed the chain from the two lemmata
(L* $+$ terminal) to Weil/RH independently and found \emph{three
kernel-checked type inconsistencies}, three levels above the boxes
--- not in the proofs, but in the statement \emph{types} of the
interface layer (the r273 disease, one level up):

\begin{itemize}[leftmargin=2em]
\item \textbf{U1 (bridge $\times$ terminal).}  The r310b source
bridge \texttt{mainWindow\_iff\_builtFromPrimeSource} as typed bound
only nodes/comb/arch --- never $u$, never $B$.  A window with
$B = -1$ therefore \emph{represents the empty spec}, while
\texttt{terminal\_positive\_main} demands $0 < B$: the two types
refute each other for \emph{every} interpretation of the opaque
predicate.
\item \textbf{U2 (bridge $\times$ L*).}  At mesh level 0 the
tolerance is $\log(\mathrm{anchor})$, so the \emph{total node
collision} (all nodes $= 1$) represents the $\{2,3,4\}$ spec ---
and $p = X - 1$ vanishes on the collided window, forcing the strict
subordination to demand $0 < 0$.  In v969~S0 the collision
admissibility reduces \emph{exactly} to three rational facts about
$e$ ($2 \le e$, $3 \le 2e$, $e \le 4$) on the certified series
bracket $685/252 \le e \le 685/252 + 1/35280$.
\item \textbf{U3 (pair margin $\times$ existence).}  The pre-r320
\texttt{pair\_margin\_main} quantified $(Z_{\rm loc},
\mathit{runs})$ freely, bound only by the split; the adversary
$Z_{\rm loc} = |F| + 1$, $\mathit{runs} = []$ satisfies the split
and demands $(|F| + 1)^2 < 5/7$ --- false for every $F$ since
$(|F| + 1)^2 \ge 1 > 5/7$ exactly.  The old type forces its source
predicate \emph{empty}.
\end{itemize}

\noindent In addition the audit named the \textbf{mesh-vs-anchor
cofinality seam}: \texttt{cofinal\_prime\_windows} proves the
\emph{anchor} direction at fixed mesh, which is exactly the
$H_{\rm cof}$ direction proved inadmissible in the carrier lane
(mesh refinement needed, false floors measured); the v749 tower
identification is the named open Lean target.

\begin{remark}[The honest chain reading --- binding]
\label{rem:honestchain}
The audit's verdict on the composed claim is the most important
sentence of the wave and is adopted verbatim as the binding
formulation for every downstream surface: \textbf{``two lemmata
$\Rightarrow$ RH'' does not survive the literal reading.}  Literally,
the two lemmata (L* $+$ terminal) give \emph{only window-local
master positivity for the opaque, currently witness-less}
\texttt{MainWindow}.  The road to RH additionally needs:
\emph{(i)} the corrected source bridge with $u$/$B$/fold/arch
fidelity (repaired in r320, one definitional \texttt{sorry}
remains), \emph{(ii)} the transport onto the real folded von-Mangoldt
source, \emph{(iii)} the mesh-cofinal v749 tower identification (to
be formalized --- the seam above), and \emph{(iv)} the cited
classics (form convergence \emph{proved},
\texttt{finite\_forms\_converge\_to\_weil}; density; Dini
continuity; the Weil criterion).  Every earlier ``two lemmata''
phrasing is superseded by this reading.
\end{remark}

\begin{remark}[The three levels of the proof graph --- binding
reviewer adjudication, wave 12]\label{rem:threelevels}
The proof graph has three cleanly separated levels, and from wave 12
on the two arithmetic holes are named \emph{window-local} on every
surface:
\begin{itemize}[leftmargin=2em]
\item \textbf{Level A (local window mathematics, PROVED):}
$L^*(w) + T(w) \Rightarrow \mathrm{MasterPositive}(w)$ --- cleanly
typed after r305/r320.  In the reviewer's words: \emph{``For every
canonical finite window, L* and terminal positivity imply augmented
prefix positivity.  This is a window-local theorem.''}
\item \textbf{Level B (the universal window family --- THE two
arithmetic window-local holes):} $\forall w \in W_{\rm canonical}:
L^*(w) \wedge T(w)$ --- the contracts
\texttt{lstar\_subordination} and \texttt{terminal\_positive\_main}.
\item \textbf{Level C (extraction to Weil/RH, OPEN):} the
source-exact realization of the \emph{actual} source $+$ an
admissible transport theorem $+$ a convergence/stabilization theorem
in the correct directed order $+$ density/continuity for the Weil
criterion.  \emph{``Any implication to Weil positivity or RH
additionally requires a source-exact realization, an admissible
transport theorem, and a convergence or extraction theorem in the
correct directed order.''}
\end{itemize}
Two honest qualifications belong to this picture.  First, \textbf{the
false cofinality direction is documented, not solved}: the U3-adjacent
seam of Section~\ref{sec:redteam} (\texttt{cofinal\_prime\_windows}
proves the anchor direction, not the $H_{\rm cof}$ refinement
direction) is a named Level-C gap, and no wave-12 artifact closes it.
Second, \textbf{the sorry census does not currently measure the
global distance in full}: the missing Level-C extraction architecture
appears in \emph{no} \texttt{sorry}, because the correct theorem
statements do not yet exist --- the census of five counts the
window-local layer only.  \emph{Wave-13 update
(Section~\ref{sec:elementwise-extraction}):} the second caveat is
\textbf{partially discharged} --- the r326 module
\texttt{RH/Elementwise.lean} raises the census $5 \to 8$ with three
\emph{named typed Level-C statements} (arch/pole transcription
classical, source completion definitional), so the proof graph now
shows Level~C as named statements; the distance is \emph{named},
not closed.
\end{remark}

\begin{remark}[SourceExact: firewall now, construction theorem
later]\label{rem:sourceexact-target}
The reviewer's classification of the r320 \texttt{SourceExact}
predicate is adopted: it is a \emph{good firewall} (it seals the
arch/border disease against the U-class adversaries), but in a final
proof it must \emph{not} stand as a free assumption.  The named open
Lean target is the target architecture
\texttt{CanonicalPrimeWindow} plus \emph{exactly one} construction
theorem \texttt{sourceExact\_buildPrimeWindow} --- load-bearing for
the global (Level-C) connection, not formalization polish.
\end{remark}

Round r320 repaired the interface completely (all artifacts merged
and green in \texttt{rh/lean/}; no Lean change ships with the v969
promotion): \texttt{RepresentsWindow}/\texttt{RepresentsSpec}
retyped with $u$/$B$ fidelity, the \emph{separation discipline}
($2\delta < $ min node gap and $\delta < $ min comb weight; honest:
mesh level 0 represents \emph{nothing}) and \texttt{budget\_pos};
the additional finding beyond the audit --- the free spec fields
arch/border carry the same disease --- sealed by the new opaque
\texttt{SourceExact} (r273 convention spec-side); the bridge lifted
to $\exists s,\ \texttt{SourceExact}\,s \wedge
\texttt{RepresentsWindow}\dots$ (the one definitional \texttt{sorry};
the earlier \texttt{Iff.rfl} promise corrected);
\texttt{pair\_margin\_main} retyped onto the \emph{canonical
extraction} (\texttt{signRuns}/\texttt{terminalDrive}/%
\texttt{bulkRuns}/\texttt{edgeLocal} as definitions,
\texttt{signRuns\_sum} and \texttt{canonical\_split} \emph{proved}
--- no free $(Z_{\rm loc}, \mathit{runs})$ anymore); \textbf{three
permanent sorry-free guards}
(\texttt{old\_bridge\_terminal\_inconsistent},
\texttt{old\_bridge\_lstar\_inconsistent},
\texttt{old\_pair\_margin\_forces\_empty} in
\texttt{RH/Counterexamples.lean}, $\forall P$ form, axiom census
kernel-checked without \texttt{sorryAx}); and the \textbf{sorry-free
witness} \texttt{witness\_represents} (anchor 2, atoms $\{2,3,4\}$,
mesh level 4 --- the first level with satisfiable separation
discipline, via the integer facts $2^7 < 3^5 < 2^8$; v969~S0
re-gates the reduction to $1/8 < 2/5$ exactly, and the level-0
failure).  $\exists\,\texttt{MainWindow}$ stays \emph{deliberately}
unprovable --- two opaque predicates block exactly the adversaries.
Sorry census $5 \to 5$ with two typed \emph{retypes}; the two true
\emph{window-local} holes (\texttt{lstar\_subordination},
\texttt{terminal\_positive\_main} --- Level~B of
Remark~\ref{rem:threelevels}) and \texttt{crossing\_budget}
are byte-identical.

\subsection{The fiber fork: from the exception-family abort to the
sliding bound (r317/r321)}\label{sec:slidingbound}

Reviewer fork (b) executed first (r317,
\texttt{exception\_families\_probe.py}, 38/38): the statement form
``generic theorem $+$ at most two source-pure exception families''
under a hard gate sealed in advance.  The gate fired
\textbf{\textsc{whac\_a\_mole}(kz53, kz83)} by contract --- the
sealed blind gap rule \emph{does} recover the spike family
$B = \{kz55, kz67\}$ (the FCIX pair, gap 1.78, finite certificates
$C_B = 1.0536$), but class A stays empty at bar distance $0.017$
and kz53/kz83 stay uncovered on the complement.  The census behind
the abort is the round's find: the $F_A$ top-3 are \emph{exactly}
kz53 ($2.47$), kz83 ($2.39$), kz67 ($2.38$) --- one source-pure
rank-local QMAX coordinate ranks the complete mid/deep
near-critical family on top, but as a \emph{continuum}: the
exception-family form is the wrong statement shape.

Fork (a) in the r317-sharpened form (r321,
\texttt{continuous\_coordinate\_probe.py}, 39/39) then fired
\textbf{\textsc{sliding\_bound\_go}($G_{SQ}$)}:

\begin{theorem}[Sliding cubic bound --- \emph{theorem candidate},
certified on the measured rungs only]\label{thm:slidingbound}
Let $F_A(w)$ be the rank-local QMAX spike ratio (window $W = 5$,
the r317 import) and $q_j$ the normalized atomic $|P\Delta|$
profile of the window $w$ with $m$ blocks.  On \emph{every measured
rung} of the 65-rung cofinal ladder with $m \ge 73$:
\[
  \sum_j q_j^3 \;\le\; 1.3056 \; F_A(w)^2 \, \frac{(\log m)^2}{m^2},
\]
with the 21 small-$m$ rungs certified individually
($C_{\rm small} = 1.0694$).  The constant is \emph{algebra-derived
and source-pure}: $b = (\max_{\rm cal} B)^2 = 1.1426^2$ with
$B = \mathrm{medloc} \times m/\log m$ --- no target value enters the
calibration --- backed by the exact two-sided concentration bracket
$q_{\max}\,\Phi_{H1} \le \rho_2 \le \Phi_{H1} = (F_A B)^2$
(warded live on 69 worlds; re-proved from scratch in v969~S0).
\emph{Status: a certified finite statement on the measured rungs
($0/39$ test violations; all four named r316/r317 violators
kz53/kz83/kz67/kz55 inside at reserves $7.0..9.6$), registered as
the open contract} \texttt{PRIME.L2.RENYI3.SLIDING\_BOUND.01}~\mO.
\emph{It proves no universal bound and no cofinal law.}
\end{theorem}

\noindent Honesty, disclosed with the GO: the implied uniform
constant $C_{\rm impl} = g(2.47) = 7.97$ is $7.5\times$ looser than
the r306 first-5 constant $1.069$ --- the round buys \emph{form}
(one gliding bound, no regimes, no exception families, all old
violators inside), not sharpness.  The pure-algebra transfer route
does \emph{not} close ($B$ is not bounded by its calibration max:
test max $1.4088 = 1.23\times$, trend $+0.122$ rising); and the
sliding bound is \emph{not} world-separating by itself
(\textsc{scramble} is a concentration spike too, covered at
$5.21$ --- the rejection is carried by the r317 class side
condition).  The provenance question of the fiber is now
\emph{source-pure, local and split in two}: (a) is $F_A$ bounded
(measured max $2.47$; the near-critical family is its top), and
(b) what bounds the $q_{\max}$-share $\rho_2/(F_A^2 B^2)$ (the
median-of-max shape route; the r302 $M_2$ stationarity $1.973$
pins the second moment of the same shape).

\subsection{The base fork: index language, sign regularity, and the
basin lesson (r318/r322)}\label{sec:basinlesson}

Round r318 (\texttt{indefinite\_fork\_probe.py}, 25/25) ran the new
L* idea class --- Pontryagin/Krein \emph{index} vs \emph{sign
regularity} --- as one representation test.  Verdict
\textbf{\textsc{p2\_main\_specific}}: the $2.4\,\%$ negative cross
mixtures of the converged r308 Dykstra family are \emph{not}
structureless --- on MAIN-class worlds the block residual lives
lawfully on the \emph{antiphase pair} $(D3, D4)$ with fixed sign
$-1$ ($12/12$ rungs, twin exact), while the dead controls break by
\emph{pattern} on the arch-mean$\times$border pair.  The P1 index
route closes \emph{as language}: the bookkeeping is exact (window
index defect $0/0/0$ on the mains/twin vs $55/37/4/31$ on the
controls --- the control flips \emph{are} negative directions inside
the window), but ``$n_+(\mathrm{window}) = N_w$'' is a total
restatement of L* ($10/10$ instances, both truth values realized),
the global inequality is vacuous and the negative-subspace
invariants are world-blind: a language gain, no lever.  Classical
sign regularity is dead on MAIN
(\textsc{premise\_fails\_on\_main}: orientation-sensitive minor
censuses are coin-flip $\sim 0.50$).

The dig round r322 (\texttt{antiphase\_sign\_law\_probe.py}, 25/25)
then adjudicated the fingerprint itself:
\textbf{\textsc{algorithm\_artifact}}.  The sign law \emph{and} the
r312 $97.6/2.4$ cone anatomy are properties of the
\emph{least-norm-proximal Dykstra basin}, not of the psd solution
set: the sealed random-start variants (staged to $20\,000$ steps)
carry modal pair $(0, 2)$ at shares $0.310/0.264$, while the
canonical variants (least-norm start, long, zero start, swapped
projection order) coincide within rel $2\times10^{-7}$ and carry
the law exactly.  The r318 pattern dichotomy does \emph{not}
survive fair convergence (the budget-ablated \emph{converged} EPST
control carries the MAIN law \emph{stronger} than MAIN, $0.742$ vs
$0.692$); and the law is \emph{not} identity-forced --- the exact
forced-component analysis (Fractions, machine-exact on the TOY4
calibrator $4/7$) shows the $(D3, D4)$ sector is roughly half free
with a \emph{positive} forced value: the negative sign law lives
entirely in the free/selection directions.

\begin{remark}[The basin lesson]\label{rem:basinlesson}
The r322 adjudication is a methodological lesson of the same class
as the r303 reparametrization trap: a lawful, reproducible,
position-structured fingerprint with total sign consistency
($1.000$; $92.3\,\%$ of all blocks) can still be an
\emph{algorithm artifact} --- a property of the solver's selection
geometry, not of the mathematical object.  The sealed
canonical-vs-random variant battery is the honest test, and it is
now module-gated in v969 (the basin adjudication logic with tipping
mutants).  What survives as the dig yield: the named residual
question --- \emph{why does the least-norm-proximal basin organize
its indefiniteness on the antiphase pair?} (best reporting-grade
hook: the block-local antiphase mass pairing, Spearman $+0.81$,
below the sealed $0.9$ bar) --- and the r312 wall fact that no
canonical source-pure selection rule is known.
\end{remark}

\begin{remark}[Typed reviewer assessment --- wave 12; supersedes the
wave-11 $4 \to 5$ scalar]\label{rem:reviewertable}
Where the wave-11 assessment placed one scalar
(Remark~\ref{rem:universality}), the wave-12 adjudication types the
program per layer.  It is a \textbf{reviewer assessment}, not a
machine-checked fact:
\begin{center}\small
\begin{tabular}{@{}lc@{}}
\toprule
\textbf{Layer} & \textbf{Rating}\\
\midrule
Finite dictionaries & $9.5/10$\\
Formal window-local architecture & $9/10$\\
Terminal mechanism & $6/10$\\
$L^*$ mechanism & $2/10$\\
Source/extraction architecture & $2$--$3/10$\\
Complete RH proof path & $\sim 3/10$\\
Audit quality & $9/10$\\
\bottomrule
\end{tabular}
\end{center}
with the binding sentence: \emph{``Stronger as a window-local
research program; as a complete RH proof chain currently less far
than previously assumed --- the removal of a shortcut that did not
exist.''}
\end{remark}

\begin{remark}[Lane status after wave 12]\label{rem:lanestatus}
$L^*$ is internally \textbf{paused} (allocation $0\,\%$, reviewer
decision): the selection geometry is not pursued further
(\textsc{algorithm\_artifact}, r322); reopening only under the six
documented conditions of the r322 round notes; as an \emph{external}
object, the two-weights sampling formulation $\lVert J_N \rVert < 1$
remains a memo candidate.  Terminal is sharpened to the
QMAX$\times M_2$ shape route (round R324 in flight --- deliberately
\emph{not} consumed by v969).  The extraction-repair fork R325
(towards \texttt{CanonicalPrimeWindow} $+$
\texttt{sourceExact\_buildPrimeWindow},
Remark~\ref{rem:sourceexact-target}) is in flight --- deliberately
\emph{not} consumed by v969.  \emph{Wave-13 update:} both in-flight
rounds are executed and consumed by v970
(Section~\ref{sec:elementwise-extraction}); the lane status after
wave 13 is Remark~\ref{rem:lanestatus-v970}.
\end{remark}

\certbox{PRIME.REDTEAM.EXTRACTION\_AUDIT.01 [E];\
PRIME.L2.RENYI3.SLIDING\_BOUND.01 [O];\
PRIME.L2.RENYI3.PROVENANCE.01 [O update];\
PRIME.LSTAR.SUBORDINATION.01 [O update]}%
{verification/v969\_forks\_and\_redteam.py}%
{Probes (embedded byte-exact, sealed smoke stage; full 16-hex SPEC
SHAs pinned in the module and in \texttt{rh/INVENTORY.json}):
\texttt{exception\_families} (38/38, \texttt{04fbe5c0}),
\texttt{indefinite\_fork} (25/25, \texttt{f2d98683}),
\texttt{continuous\_coordinate} (39/39, \texttt{e68883ad}),
\texttt{antiphase\_sign\_law} (25/25, \texttt{761b51d4});
rounds r317--r322; the Lean rounds r319 (audit) and r320 (repair)
are consumed as reports (artifacts in \texttt{rh/lean/}, re-verified
by \texttt{lake build} and the RH suite; the U1--U3 witnesses
re-established in exact arithmetic in the module's S0); the false
cofinality direction is documented, not solved; rounds R324
(terminal QMAX$\times M_2$) and R325 (extraction repair) are in
flight and deliberately not consumed at this cut.}

%======================================================================
\section{Elementwise extraction and the measured composition
(v970)}\label{sec:elementwise-extraction}

Wave 13 (rounds r323--r327) executes the two lanes the wave-12
reallocation opened (55 terminal / 35 extraction / 10 formalization
/ 0 internal L*): the \emph{extraction repair} answers the R319
cofinality seam with an elementwise quantifier architecture and
implements it in Lean (r325/r326), and the \emph{terminal lane}
composes its chain to \texttt{terminal\_positive\_main} as an
honestly typed MEASURED statement (r324 after a binding mid-round
reviewer course correction, with the pre-work banked as r324-pre;
r327 the follow-up anatomy).  Round r323 --- the planned L* fork ---
was \emph{cleanly aborted} by the same reviewer adjudication at the
end of its design phase, before any write access: no probe, no
measurement, no \texttt{rh/} file, nothing counts as a round
(post-abort suite $73/73$ with $103/103$ pins).  The module
\texttt{v970\_extraction\_and\_composition.py} freezes the wave;
the onset formula, the interpolation, the $F_A$ identity, the group
ledger and the $m_0^*$ arithmetic are re-established
\emph{module-own in exact arithmetic} (pure Fractions; no Lean
call), including the chain-honest bar of the independent chain
audit r328B (the S3$'$ item below).  \textbf{No RH claim}; nothing
here is a cofinal theorem.

\subsection{The extraction repair: elementwise stabilization (r325)
and the Level-C architecture (r326)}\label{sec:elementwise-repair}

Round r325 (\texttt{extraction\_order\_probe.py}, 18/18, SPEC
\texttt{31277f91} final / freeze \texttt{57e50d36}) measured the
machine-checkable halves of the three reviewer variants for
repairing the RH-connection seam of Section~\ref{sec:redteam} (the
\texttt{rh/} cofinality theorems run in the \emph{anchor} direction;
$H_{\rm cof}$ demands the \emph{mesh-refinement} direction, measured
inadmissible with false floors).  Verdict:
\textbf{\textsc{elementwise\_stabilization\_go}} primary, with
co-letters \textsc{canonical\_mesh\_rebuild\_go} $+$
\textsc{signed\_defect\_correction\_go}(\textsc{sign}) $+$
\textsc{anchor\_only\_insufficient}.  The repair statement is a
\emph{quantifier statement}, not a mesh theory: on the native dense
class (dyadic step-function autocorrelations --- the v749 ``Weil
form of step functions'' class, elements sealed by seed)
\emph{all three channels} (comb/arch/pole) of the canonical tower
windows stabilize \emph{exactly} at the predicted finite anchor
onset $\alpha^* = (n_g + 1) D_0 / 2$ ($0.0$/$3.3\times10^{-18}$
above onset, $2.7\times10^{-2}$..$5.3\times10^{-2}$ below --- a real
onset) \emph{and} the value is constant under dyadic mesh refinement
(comb $3.3\times10^{-18}$, arch $1.5\times10^{-15}$, pole
$2.0\times10^{-17}$): the extraction consumes \emph{no} mesh-cofinal
ladder and \emph{no} transport --- $H_{\rm cof}$ \textbf{is not
needed and is replaced as the target route}.  Variant B is the
\emph{construction substrate} of A, not an independent route (v749
T1.4b reproduced: every deployed frame-A window \emph{is} a directly
rebuilt tower member at rel dev $0.0$; the rebuild ladder is PD per
stage with honestly falling margins).  Variant C is the
\emph{quantified density step}, not an independent extraction: on
the sealed non-native $C^2$ class the per-channel quadrature defect
is \emph{one-signed} (comb negative, arch/pole positive), falls at
the $D^2$ class rate per channel (ratios $3.6$..$9.0$) and sits
inside the rigorous closed-form interpolation envelope
$|E_{\rm ch}| \le (D^2/8)\,\lVert F''\rVert\, K_{\rm ch}$ ---
controlled on its own, \emph{no wall margin anywhere}.  The
anchor-only floor is real ($E_{\rm tot}$ constant nonzero
$+1.77\times10^{-4}$/$+1.59\times10^{-4}$ --- the R319 false-floor
mechanism reproduced on this probe's own elements).  In v970~S0 the
onset formula is re-established as an \emph{exact} rational toy
stabilization (the tent-window read equals the exact atom sum
$35/16$ for every $\alpha \ge \alpha^* = 5/8$, differs below, and is
constant under refinement $D_0 \to D_0/4$; the nearest-neighbour
mutant is caught).

Round r326 implemented the adjudicated architecture in
\texttt{rh/lean/RH/Elementwise.lean} (merged and green; consumed by
v970 as a report, re-verified by \texttt{lake build} and the RH
suite).  The three \textbf{proved kernel pieces} are the load-bearing
content:

\begin{center}\small
\begin{tabular}{@{}lp{0.60\linewidth}@{}}
\toprule
\textbf{Theorem (proved, kernel-checked)} & \textbf{Content}\\
\midrule
\texttt{sourceExact\_buildPrimeWindow} & the wave-12 reviewer target
(Remark~\ref{rem:sourceexact-target}) \emph{implemented}: every
canonical family member satisfies the transcribable
\texttt{SourceExactSpec} (nodes/comb derived by interface design,
atom-set completeness via \texttt{predefined\_family}, budget
positivity) --- \texttt{SourceExact} is \emph{eliminated as a free
assumption from the extraction route}; built windows provably carry
their source\\
\texttt{comb\_elementwise\_stabilization} /
\texttt{elementwise\_finite\_stabilization} & the comb channel of
the R325 statement, proved with the onset
$a_0(f) = \max(1, \lceil \exp(\mathrm{steps}\cdot D_0)\rceil)$
\emph{predefined from the element's support alone} (corpus gauge
$2\Lambda(n)/\sqrt n$; no mesh quantifier; \texttt{GridElement}
built for real with proved compact support; honesty tie:
\texttt{combRead\_eq\_window\_channel} --- the read \emph{is} the
built window's comb channel); the full form composes from the three
channels\\
\texttt{weil\_nonneg\_of\_windowlocal} & \textbf{the extraction
without the ladder}: window-local positivity of the canonical
family implies Weil-form nonnegativity on every grid element by
\emph{one finite instantiation} per element (a Euclid prime anchor
above the onset $+$ the element's own native mesh) --- no refinement
tower, no PSD transport, no $H_{\rm cof}$; plus
\texttt{weil\_nonneg\_of\_bordered} (the composition through the
\emph{named} Prop \texttt{BorderedCompressionBridge} --- no truth
commitment)\\
\bottomrule
\end{tabular}
\end{center}

\noindent The sorry census moves $5 \to 8$: the three \emph{new}
\texttt{sorry}s are \emph{typed Level-C statements that were not
formalizable before this round} ---
\texttt{arch\_elementwise\_stabilization} (classical, S2: the arch
kernel transcription \texttt{arch\_A} is classical integral
analysis, R325-measured exact at $1.5\times10^{-15}$),
\texttt{pole\_elementwise\_stabilization} (classical, S2: the v716
pole closed form, $2.0\times10^{-17}$), and
\texttt{specFamily\_sourceExact\_completion} (classical $+$
definitional, opacity-forced; the extraction route does \emph{not}
consume it --- that is the point of the architecture).  The five
legacy \texttt{sorry}s are byte-identical; the two true
\emph{window-local} holes (Level~B) are unchanged.

\begin{remark}[The honest formulation of the new connection ---
binding, wave 13]\label{rem:honestchain-v970}
The wave-12 honest chain reading (Remark~\ref{rem:honestchain})
is \emph{updated} by the r325/r326 architecture to the following
binding formulation: \textbf{two window-local lemmata (over the
canonical family) $+$ the elementwise architecture $+$ cited
classics (density, v912 continuity, the Weil criterion)
$\Rightarrow$ RH; open links: the arch/pole transcription
(classical), the compression bridge, the source completion
(definitional).}  The window-local positivity premise itself
(Level~B) is proved nowhere; the three open links are the named
typed \texttt{sorry}s and the named Prop above.
\end{remark}

\subsection{The measured terminal composition (r324, r324-pre
banked) and the coincidence anatomy (r327)}
\label{sec:measured-composition}

Round r324 ran under a binding mid-round \emph{reviewer course
correction}, disclosed in full: the round's original contract (the
$F_A$-boundedness hunt) was executed and sealed \emph{first}
(\texttt{fa\_provenance\_probe.py}, 36/36, SPEC \texttt{9a6696f8}
--- verdict \textsc{fa\_bounded\_distributional} $+$
\textsc{qmax\_share\_open} $+$ \textsc{not\_composed}: the $F_A$
distribution matches the iid stationary max-statistic null, KS
$0.0982 \le 0.125$, $p_{\max} = 0.090$, envelope $C_F = 3.357$; the
honest tension disclosed --- the per-rank $p$ values at the named
spikes are $0.005/0.000/0.000$), then \emph{superseded}: the
$F_A$-boundedness statement was demoted as potentially
unnecessarily strong, the cleaner decomposition is
$Q_{\max} \times M_2$.  The pre-work is \emph{banked} as r324-pre,
stays sealed and citable, and its record is consumed as disclosed
anchors.  The binding contract
(\texttt{qmax\_m2\_origin\_probe.py}, 36/36, SPEC \texttt{dc36cacb})
fired \textbf{\textsc{pileup\_grows\_subcritical}}
($e_{\rm tot} = +0.172 < \mathrm{CRIT} = 0.224$, stability halves
$+0.141/-0.160$ both subcritical, the second \emph{falling} ---
decided; by the reviewer contract a sufficient, first-class
outcome):
\begin{itemize}[leftmargin=2em]
\item \textbf{S0 exact} (re-proved module-own in v970): $M_3 \le
q_{\max} M_2$ and $M_2 \le q_{\max}$ (Fractions; toy slack exactly
$1/36$, \emph{double} equality at the flat profile; live 69 worlds
slack $0.0$), and the identity $F_A \cdot B \cdot \log m =
m\,q_{\max}$ ($2.7\times10^{-16}$ live): \textbf{$F_A$ is
de-black-boxed} --- it is the rank-local normalization of
$m\,q_{\max}$, not an independent coordinate; the r321 bracket
upper side equals $\Phi_{H1}$ and the interpolation improves it by
exactly $M_2/q_{\max} \le 1$.
\item \textbf{S1:} the pointwise $M_2$ export is \emph{refuted}
($C_2 = 2.2557$ mid-ladder freeze fails $7/39$ --- exactly the
banked spike set, $+35$..$42\,\%$) while the r302
\emph{distributional} export holds GO (stationarity KS
$0.023/0.016$, tighter than the r302 core $0.043$).
\item \textbf{S2, the mechanism half and half:} the $O(\log m)$
\emph{scale count} certifies pointwise ($C_{\rm NSC} = 2.0258$,
$0/39$) but the per-scale mass $O(1/m)$ is refuted ($C_{\rm PIL} =
9.3583$, $11/39$) --- the hardness answer: the near-critical
windows are \emph{one near-maximal heavy source scale} at the
argmax block (kz53: $s_0$ carries $13.78$ of $G = 13.02$ at
$\mathrm{nsc} = 5$, the ladder median), not multiscale convergence.
\item \textbf{S3, the composition (typed MEASURED):}
$\sum_j q_j^3 \le 8.941\,(\log m)\, m^{+0.172}/m^2 \Rightarrow
N_3 \ge m^{0.914}/\sqrt{8.941 \log m}$, $N_2 \ge N_3$ (the r306
exact chain) $\Rightarrow$ the $N_2$ density need for all $m \ge
m_0^*$ $\Rightarrow$ the r297 target $\Rightarrow$ the v964-S0 vdC
theorem $\Rightarrow \delta' > 0.21$ generic $\Rightarrow$ the
\texttt{terminal\_positive\_main} edge.  All measured $m \le 274$
are closed by the finite certificates; \textbf{the gap from $274$
to $m_0^*$ is the disclosed extrapolation hypothesis} --- $m_0^*$
is astronomical, no cofinal claim.  (The $m_0^*$ log-space solver
is rebuilt verbatim in v970~S0, and the disclosed pre-freeze
linear-space bug --- a search capped at $10^{18}$ that
\emph{misses} the root --- is rebuilt and caught.)
\item \textbf{S3$'$, the chain-honest bar (independent chain
audit, r328B):} the record bar $\mathrm{CRIT} = 0.224$ was derived
from the \emph{atom-level} need $0.888$ (\textsc{r302\_atom\_need}
for $n_{\rm eff,atom}$); the composition functional $N_2 =
n_{\rm eff}$ is \emph{block-level} --- its need is $0.908$ in $N$
units, i.e.\ $908/1002 = 0.9062$ in $m$ units via
$\mathrm{sl}_{\rm nact} = +1.002$.  The chain-honest bar is
therefore $\mathrm{CRIT} = 2\,(1 - 0.9062) = 0.188$:
\textbf{the verdict survives} ($+0.172 < 0.188$, exact in
v970~S0-T5d), but the sealed bar was too loose --- any future
$e_{\rm tot}$ in the window $(0.188, 0.224)$ would fire falsely,
so the going-forward bar is $0.188$.  The record threshold
$N_2 \ge m^{0.888}$ does \emph{not} by itself reach the
block-level need ($0.888 \times 1.002 = 0.890 < 0.908$); the
inference carries via the full polylog form $1 - e_{\rm tot}/2 =
0.914 \ge 0.9062$ (margin $+0.008$, exact).  Both onsets are
astronomical --- $m_0^* = 10^{59.6}$ at the record (atom-level)
need, $m_0^* = 10^{238}$ chain-honest, from the same solver ---
so the qualitative statement (measured rungs plus a typed
extrapolation hypothesis) is unchanged.  The sealed r324 record
is \emph{not} edited: the correction lives as a chain annotation
and an exact v970 gate, and the record pressure line attributing
the whole $e_{\rm tot}$ (incl.\ $e_{M_2} = +0.014$) to the
$q_{\max}$ factor is correct as a product bound but misleading
per-factor.
\end{itemize}

Round r327 (\texttt{group\_mass\_cap\_probe.py}, 34/34, SPEC
\texttt{11e4fd40} final / freeze \texttt{71f8b7b4}) descended one
level, from the heavy scale to the fold \emph{group}.  Verdict
\textbf{\textsc{cap\_partial}} with anatomy letter \textbf{SOURCE}
(median named ratio $1.057 \ge 0.5$), and the structural finding of
the round: \emph{on all 65 rungs the heaviest group of the argmax
block is exactly one $\beta/\omega$ fold pair} (one bulk $+$ one
window atom at one position; window-atom histogram $[0, 65, 0]$)
with median alignment $1.000$ --- the two ancestors
\emph{reinforce}.  At kz53 the single pair carries $88.8\,\%$ of the
argmax block's atom mass at gap $0.076$ (the second group $13\times$
lighter): \textbf{the spike is literally one bulk/window
coincidence} (kz67 is the exception shape: 6 groups, bshare
$0.416$); position thirds $[21, 29, 15]$ mid-heavy (the r322
mid/tail echo).  The $\Lambda$-pair cap is \emph{refuted} as a
mid-ladder polylog ($C_{LV} = 1.1838$ fails $19/39$, named $0/4$ ---
the exact two-ancestor algebra $g_{\rm abs} \le 2\,v_{\max}$ holds
at slack $0.0$, the \emph{constant} does not close); the direct
group cap fails at $A = 1$ and $A = 2$; \textbf{the group count
certifies} pointwise ($C_{NG} = 2.6351$, $0/39$, named $4/4$) ---
the \emph{third} certified $O(\log m)$ count of the lane after the
r324 scale count.  The route exponent $e_{\rm route} = +0.106 <
0.224$ but the stability halves straddle ($+0.411/-1.183$):
\textsc{heavy\_group\_unbounded} does \emph{not} fire --- the
heavy-group coordinate \emph{decays} with depth (a shallow/mid
phenomenon, not growth).  Q3: no recomposition --- the r324
MEASURED chain stays the honest end state of the route.

\begin{remark}[The coincidence geometry --- the named
direction]\label{rem:coincidence}
The typed direction after r327: the spike cap, if it exists, is a
statement about \emph{where} the single heavy $\beta/\omega$
coincidence can \emph{sit} --- the source geometry of the
bulk/window pairing at one position --- not about group count
(certified) or multiplicity (proven).  Alternatively the honest end
state of the route is the r324 MEASURED composition with its typed
extrapolation gap.
\end{remark}

\begin{remark}[Lane status after wave 13]\label{rem:lanestatus-v970}
$L^*$ stays internally \textbf{paused} (the r323 fork was cleanly
aborted before any write access by the same adjudication).  The
\emph{terminal} lane ends at the MEASURED composition
(Section~\ref{sec:measured-composition}) with the
coincidence-geometry question (Remark~\ref{rem:coincidence}) as the
named direction.  The \emph{extraction} lane has its architecture
standing (Section~\ref{sec:elementwise-repair}): three proved kernel
pieces, three typed Level-C rests (arch/pole transcription,
compression bridge, source completion).  Lean: 8 \texttt{sorry}s,
all typed --- the two true window-local holes unchanged.
\end{remark}

\certbox{PRIME.EXTRACTION.ELEMENTWISE.01 [E];\
PRIME.L2.RENYI3.MEASURED\_COMPOSITION.01 [O];\
PRIME.L2.RENYI3.SLIDING\_BOUND.01 [O update];\
PRIME.L2.RENYI3.PROVENANCE.01 [O update]}%
{verification/v970\_extraction\_and\_composition.py}%
{Probes (embedded byte-exact, sealed smoke stage; full 16-hex SPEC
SHAs pinned in the module and in \texttt{rh/INVENTORY.json}):
\texttt{fa\_provenance} (36/36, \texttt{9a6696f8}, r324-pre ---
banked), \texttt{qmax\_m2\_origin} (36/36, \texttt{dc36cacb}),
\texttt{extraction\_order} (18/18, \texttt{31277f91}),
\texttt{group\_mass\_cap} (34/34, \texttt{11e4fd40});
rounds r323--r327; the Lean round r326
(\texttt{rh/lean/RH/Elementwise.lean}, census $5 \to 8$) is
consumed as a report (artifacts in \texttt{rh/lean/}, re-verified
by \texttt{lake build} and the RH suite); the r323 abort is
consumed as a note (clean, before any write access); the
window-local positivity premise is proved nowhere; no round in
flight at this cut.}

%======================================================================
\section{The wave-14 consolidation (v978--v982): terminal martingale,
cover and growth, the closed margin chain, the Borodin duality and the
second arithmetic}\label{sec:wave14}

Wave 14 --- reviewer-authorized after the double substance condition
(r355 K2 NU-free $+$ r357 second arithmetic) --- freezes rounds
r334--r357 as \emph{five} modules.  Unlike the embedding waves 8--13,
each module re-derives its exact layer \emph{from scratch} (sympy
symbolic $+$ pure Fractions, no probe imports) and re-runs the sealed
round verdicts as exact decision logic on the frozen record aggregates
with tipping mutants; the eighteen discovery probes stay sealed in
\texttt{experiments/tfpt-discovery/}, pinned in
\texttt{rh/INVENTORY.json} and re-verified by this suite.

\begin{theorem}[Density martingale and moment dictionary; v978]
\label{thm:density-martingale}
On any finite genealogy tree with node mass $A(v)$ and leaf count
$n(v)$, the normalized density $X(v) = d(v)/d(\mathrm{root})$ with
$d(v) = A(v)/n(v)$ is a nonnegative martingale under the count path
measure $P(c \mid v) = n(c)/n(v)$, using \emph{only} mass conservation
$\sum_c A_c = A_v$.  With $m$ leaves of masses $y_i$, total $Y$, $M_k
= \sum_i (y_i/Y)^k$ and $q_{\max} = \max_i y_i/Y$:
\[
E[X_\infty^2] = m M_2, \qquad E[X_\infty^3] = m^2 M_3, \qquad
\max X_\infty = m\,q_{\max},
\]
so the terminal target $\sum q^3 \le C (\log m)^A$ \emph{is} a cubic
martingale-moment statement.  The tilted tower $E[X_\infty^3] =
E_Q[\prod_k \Gamma(V_k)]$ holds with tilted steps $\tilde p_c = p_c
R_c^3/\Gamma(v)$ (the untilted reading is false --- exact-Fraction
counterexample); the hand-off $E_{3h} \le (m q_{\max})^2\,\mathrm{msh}$
is exact for every heavy leaf subset; every two-child fold obeys the
pair ceiling $\Gamma \le 4$ (from $4R - R^3 = R(2-R)(2+R) \ge 0$ on
$0 \le R \le 2$), the source of the K1 constant $R_{\rm ALG} =
4^{1/3}$.
\end{theorem}

\begin{theorem}[Pinning; v980]\label{thm:pinning}
On the rank-2 spectral model the pair block of $(I-E)^{-1}$ inverts to
$p'_2 = m(g_{21}a_2^2 + b_2^2)/\Delta t^2$, $q'_2 = m(g_{21}a_1^2 +
b_1^2)/\Delta t^2$, $c'_2 = m(g_{21}a_1a_2 + b_1b_2)/\Delta t^2$ with
$p'q' - c'^2 = m^2 g_{21}/\Delta t^2$ (Lagrange).  Under orthonormal
top-2 geometry ($\Delta t^2 = 1$) the dressed characteristic
polynomial factors as $(t - m)(t - m g_{21})$: the smaller dressed
eigenvalue deficit \emph{equals the margin identically}, and $r'_2 =
1 - c'^2/(p'q') = g_{21}(t_2-t_1)^2/((g_{21}+t_1^2)(g_{21}+t_2^2))$
(the r345 two-level formula).  Together with the one-line identity
$m_2'(p'+q'-m_2') = p'q'-c'^2$ (Vieta), the rate-equality theorem
(flat geometry $\Rightarrow$ dressed ratio $=$ margin ratio exactly)
and the $\rho_K$ identities, the L* margin-law chain is complete and
typed: weights dictionary, deficits candidate law, pinning theorem,
$\rho_r = \varphi$-difference computable pointwise.
\end{theorem}

\begin{theorem}[Borodin particle--hole duality; v981]\label{thm:borodin}
On a discrete support of $S = 2N-1$ atoms with positive weight $u$
(half filling: $\lceil S/2\rceil = N$, an integer lemma --- the r228
law \emph{is} Borodin's complementary rank condition), the rank-$N$
projection kernel and the rank-$(N{-}1)$ kernel of the reciprocal dual
weight $u^\vee_j = 1/(u_j P'(x_j)^2)$ satisfy
\[
\Pi_N^u + G\,\Pi_{N-1}^{u^\vee} G^{-1} = I, \qquad
G = \mathrm{diag}\bigl(1/(u_j P'(x_j))\bigr)
\]
(rational conjugator, no square roots).  Restricted to any pair $Y$,
$E = G_Y (R^{-1} - I) G_Y^{-1}$ with $R = (\Pi_{N-1}^{u^\vee})_{Y,Y}$,
hence
\[
L^* \;\Longleftrightarrow\; \lambda_{\max}(E) < 1
\;\Longleftrightarrow\; R > \tfrac12 I, \qquad
\text{margin} = 2 - 1/\lambda_{\min}(R),
\]
and $p/q = 2 - (R^{-1})_{kk}$ exactly.  On the cosine lattice the dual
weight is exactly $u^\vee \propto c_j (1 - x_j)/|f|$: the original
weight carries $+\log|f|$, the dual $-\log|f|$ --- \emph{the r354
anti-correlation of the two $\varphi$ blocks is duality algebra by
design}.
\end{theorem}

\begin{theorem}[The derived matched Dirichlet frame; v982]
\label{thm:matched-frame}
From $\Lambda(s,\chi) = (q/\pi)^{(s+a)/2}\Gamma((s+a)/2)L(s,\chi)$ the
archimedean density on the critical line is
\[
F_A^\chi(\xi) = -\log(\pi/q) + \mathrm{Re}\,
\psi\!\Bigl(\tfrac{1+2a}{4} + \tfrac{i\xi}{2}\Bigr)
\]
for $a \in \{0, 1\}$ --- parity and conductor enter only through the
functional equation; at $a = 0$, $q = 1$ this \emph{is} the zeta
dictionary (bitwise); the conductor shift is exactly $\log q$; the
Lag-space parity kernel is $e^{-w/2}$ (even) $\to$ $e^{-3w/2}$ (odd).
\end{theorem}

\begin{remark}[The binding Frame-A restriction; v979]
\label{rem:frameA}
The first certifying three-arm cover ($0/51$, $m_0^* = 10^{22.6}$),
the P02 predictor ($51/51$) and the growth ceiling $C_{\rm FAB} =
14.93$ (fresh tranche $0/6$; polylog $m_0^* = 10^{18.9}$) are
census-certified at frozen constants --- and \emph{all of it is
Frame-A census} (binding, r353): the second construction family
breaks the FAB ceiling ($+21\,\%$) and kills the reserve floor at
$m \sim 760$; the sliding coverage is \emph{finite}.  The one
cross-family survivor is K2 ($F_A B\,g_{\rm rel} \le 11.87$),
certified NU-free over three window aspects (r355; the gap geometry
is the $\nu$-free coordinate, its source chain exactly closed with
vacuous census caps) and $0/126$ on the second arithmetic (r357) ---
two families, three aspects, three arithmetics.  The data-free K1
formula $R_{\rm ALG} = 4^{1/3} \to 8/5$ (interior, exact cube
comparisons) and the mesh identity $h - \nu u \in (0, 3/2]$ (exact)
are the wave's constructional-algebra additions.
\end{remark}

\begin{remark}[Honest verdicts and the lane status after wave 14]
\label{rem:lanestatus-wave14}
The Borodin duality's own sealed verdict is
\textsc{duality-reparam-only}: no dual block passes the carrier
clauses, the equivalence is a \emph{reparametrization} of the open
condition --- the L* lane goes \textbf{final} to the specialist memo
(\texttt{rh/problem/lstar\_problem.tex}), with the one-object
question halved: \emph{why is the difference of the
by-design-coupled blocks $\sim 500\times$ small} --- now a question
about local dual two-point data (\textsc{reserve-localized}: the
local dual $2\times2$ block predicts the log reserve without
readback), and with the Lubinsky-type AC hard-edge class measurably
excluded ($a_{\rho K} = 1.4222$ vs the constant prediction).  The
matched frame moves the living-world evidence from $n = 1$ to
$n = 3$ arithmetics on 126 windows and retypes the r330 splits as
frame artifacts, while the zeta-graded signatures (exact half-filling
pinning, saturated reserve, $\varphi$ suppression $439\times$ vs
$3\times$, counting constants) type the L* margin legislation as a
statement about \emph{how close zeta's world sits to the wall}; the
matched scramble breaks --- the wall needs the arithmetic.  The
terminal lane ends unchanged at the MEASURED composition
(Section~\ref{sec:measured-composition}).  Lean: \emph{no change
ships with this wave} --- the duality is a reparametrization of the
existing hole \texttt{lstar\_subordination}, not a new target form;
the two true window-local holes are unchanged.  No RH claim, no GRH
claim, no L* claim.
\end{remark}

\certbox{PRIME.TERMINAL.DENSITY\_MARTINGALE.01 [E];\
PRIME.L2.COVER\_GROWTH\_K2.01 [E];\
PRIME.LSTAR.MARGIN\_CHAIN.01 [E];\
PRIME.LSTAR.DUAL\_HOLE.01 [E];\
PRIME.WORLD.DIRICHLET\_MATCHED\_FRAME.01 [E];\
PRIME.LSTAR.SUBORDINATION.01 [O update];\
PRIME.L2.RENYI3.SLIDING\_BOUND.01 [O update]}%
{verification/v978\_terminal\_density\_martingale.py;\
verification/v979\_cover\_growth\_k2.py;\
verification/v980\_lstar\_margin\_chain.py;\
verification/v981\_lstar\_borodin\_duality.py;\
verification/v982\_dirichlet\_matched\_frame.py}%
{Rounds r334--r357 (notes DCLXXXI--DCCIX).  The wave-14 modules embed
no probes: each re-derives its exact layer from scratch and gates the
frozen record aggregates; the sealed probes (fold\_capacity r334
\texttt{d5698a1d}, edge\_packing r335 \texttt{950c9b9e},
lstar\_parity r336 \texttt{9e4bf4f1}, fold\_martingale r337
\texttt{617a83a6}, fold\_density r339 \texttt{822d6bff},
cauchybinet\_hall r340 \texttt{86587b48}, fold\_bellman r341
\texttt{7a9cf6b3}, pair\_extremal r342 \texttt{b09f8ccd},
pair\_coupling r343 \texttt{9ffc2705}, fold\_two\_scale r344
\texttt{06375c3a}, gap\_ratio r345 \texttt{1f99235a},
fold\_cover r346 \texttt{95559d5d}, delta\_alpha r347
\texttt{bd1aa7f3}, delta\_source r348 \texttt{307814e9},
thirdarm r349 \texttt{9b593e63}, alpha\_source r350
\texttt{c3998c87}, qmax\_growth r351 \texttt{67102e4c},
rhor\_source r352 \texttt{dc6bbd2c}, second\_family r353
\texttt{bd89e331}, phi\_wander r354 \texttt{f9db84da},
k2\_source r355 \texttt{1f14bd93}, borodin\_dual\_hole r356
(freeze \texttt{5d277d57}), dirichlet\_matched\_frame r357
\texttt{4bf1a94b}) stay experiments-side, pinned in
\texttt{rh/INVENTORY.json} and re-verified by this suite; the r338
door-2 revision and the r334--r336/r340/r343/r345/r349/r353 honest
negatives are consolidated as prose; no round is in flight at this
cut.}

%======================================================================
\section{The measured walls and honest negatives}\label{sec:nogos}

The program's negative results are load-bearing: they are typed, with
break loci, and every future proof attempt must route around them.

\begin{center}\small
\begin{tabular}{@{}p{0.30\linewidth}p{0.44\linewidth}l@{}}
\toprule
\textbf{Attack class} & \textbf{Measured failure (break locus)} & \textbf{Round}\\
\midrule
Compression / spectral compactness & inkompressible on the exact
coordinate: no compact eigenspace ($K80=20$), participation dimension
$\approx0.195N$; arithmetic worlds are spectrally \emph{spread}, destroyed
worlds compact & r253/r254\\
\addlinespace[2pt]
Global gauge constant & no constant survives the quotient test; the
missing layer drifts $-1.1..-2.1$ decades end-to-end and is
orientation-relevant & r252\\
\addlinespace[2pt]
Band locality (node bands) & transitions are dense (0.55--0.63 of
degrees), drift anti-correlated with them & r255\\
\addlinespace[2pt]
Mass majorants (sup / Cauchy--Schwarz / triangle on masses) & $0/42$
coverage, median $\times490$ excess, break locus $0.97N$ (terminal $h$
collapse with $+8.2$-decade rebound) & r258\\
\addlinespace[2pt]
Level crossing & parity branches cross 5--16 degrees too early;
\textsc{main} w9 crosses 45 times without a single true flip & r259\\
\addlinespace[2pt]
One-swap statistics & the critical swap exists ($0.2$ dec) but \textsc{main}
carries the same structure at the same degrees; gap distributions fully
overlap & r259\\
\addlinespace[2pt]
Subtraction-free positivity (LGV) & no-go at depth $k=1$, single Gram
entry, every world including \textsc{main} & r261\\
\addlinespace[2pt]
Global block involution & 4 unpaired negative configurations at $n=2$
already (65\,152 pairings machine-checked) & r261\\
\addlinespace[2pt]
Transfer cone & best cone certificate is \emph{identical} to terminal
budget positivity: wall-equivalent, no new mechanism & r261\\
\addlinespace[2pt]
Smooth (Szeg\H{o}) dressing & the $D$-layer is essentially singular
($p=0.997$, $\exp(c/(z-s))$ type, up to 380 decades): not
$\sigma^{\circ}$-pairable & r251/r253\\
\addlinespace[2pt]
Bordered spectral bound & $f^{\mathsf T}(I-Q)^{-1}f<1$ is measured
\emph{false}: $g(1)=6.06..15.41$ on $42/42$; both augmented border
variants cross the wall & r257/r265\\
\addlinespace[2pt]
Naive $s$-coordinate & closed form $D_n(s)=B-s^2S_{n-1}$: the monotonicity
is a trivial corollary and the endpoint \emph{is} the budget inequality
(restatement) & r265\\
\addlinespace[2pt]
IIKS $s$-family & the cross-ratio ODE
$X'(s)=-\|(I-sQ)^{-1}f\|^2$ is exact and fully definite ($0/17849$
indefinite terms) --- but its endpoint criterion is exactly the blocked
wall bound: \textsc{definiteness\_wall\_equivalent} & r265\\
\addlinespace[2pt]
Fixed head (finite-dimensional core) & no fixed $r\le8$ exists; the
best candidate's reserve $6.5\times10^{-6}$ sits four decades under
the sealed bar and decays in the r286 law class; $\lambda_{\min}(S_H(8))$
tracks the wall margin to 4 digits --- the hoped-for simplification is
\emph{empty}, $S_H$ carries the whole problem & r307\\
\addlinespace[2pt]
Paired-cone induction (architecture B) & B1--B4 adjudication: the
sealed mass-ratio cone is sound but certifies only 56\,\%; no real
induction $K_j\to K_{j+1}$; world-blind at deg 8 --- the whole
\textsc{epst}/\textsc{scr} indefiniteness sits in the negative budget
step ($=$ the r243 wall reading) & r309\\
\addlinespace[2pt]
Block-Green feasibility as a new discriminator & the r308 world
separation is \emph{entirely} the budget sign: the $\Delta$-sparsity
graph is chordal on every size, the trivial dual $Y_t=e_te_t^{\mathsf T}$
closes the stalls two-sidedly --- a chordal restatement of the known
wall reading & r311\\
\addlinespace[2pt]
Constructive PSD-G rule (Lane A, cone language) &
\textsc{coefficient\_sign\_wall}: the identity forces negative
coefficients exactly on the wall coordinates (Farkas mass on the
border/budget row, $t$-share $0.583/0.725$; exact rational
certificates on SM1/MINI16); mechanism named: block-psd membership
without rank-one-SDD membership & r312\\
\addlinespace[2pt]
Transfer-operator contraction (chain level) &
\textsc{floquet\_expanding}: the four-step monodromy on the split
cubic mode space expands (med sr $1.0600$, $0/57$ contracting,
exponent $+1.917$ instead of $-2$) & r313\\
\addlinespace[2pt]
Triple-type majorant composition & T1/T4 fall on the near-critical
family (kz55/kz53) at falling trends; type constants not
world-portable (\textsc{epstein} violates T2/T3 while holding the
total) & r313\\
\addlinespace[2pt]
$\Phi_3$ source functionals (divergence-form structure with first-5
constants) & \textsc{phi3\_all\_blind}: all three presealed
candidates fail; the entire break on kz55$+$kz67 (FCIX
$0.955/0.915$ vs $0.629$ --- where the flux cancellation dies) &
r315\\
\addlinespace[2pt]
Two-regime pointwise majorants over the FCIX stratum &
\textsc{two\_regime\_dead}: the obstruction family cuts across the
stratum (kz53 at bulk-normal FCIX); any tight pointwise majorant
dies at kz53 under a mid-ladder freeze --- the r306 first-5
protocol was load-bearing & r316\\
\addlinespace[2pt]
Exception-family classification (threshold form) &
\textsc{whac\_a\_mole}: the hard two-family gate aborted by
contract --- the near-critical family is a \emph{continuum} on the
$F_A$ coordinate (top-3 kz53/kz83/kz67, values $2.47/2.39/2.38$,
then $1.93, 1.90, \dots$); sealed thresholds cannot cover
kz53/kz83 without swallowing harmless rungs & r317\\
\addlinespace[2pt]
Pontryagin/Krein index language & exact bookkeeping, no lever:
``$n_+(\mathrm{window}) = N_w$'' is a total L* restatement
($10/10$), the global index inequality is vacuous ($\mu$-majority),
the negative-subspace invariants world-blind & r318\\
\addlinespace[2pt]
Sign regularity / variation diminishing &
\textsc{premise\_fails\_on\_main}: no census object is
MAIN-specifically sign-regular; orientation-sensitive minors are
coin-flip $\sim0.50$ even on MAIN --- the VD chain to L* cannot
start & r318\\
\addlinespace[2pt]
Antiphase sign law as an indefinite theorem &
\textsc{algorithm\_artifact}: the $(D3,D4)$ law and the $97.6/2.4$
anatomy are least-norm-proximal Dykstra-basin properties (random
starts carry $(0,2)$); not identity-forced (the exact forced
component is \emph{positive}); no theorem without a canonical
selection rule & r322\\
\addlinespace[2pt]
Compact-tail kernel/Loewner scaling beyond the prime-free zone &
at $L=0.8$ the best booked $101$-section is already negative
($-1.61\cdot10^{-4}$), its full $3\times$ archimedean HS charge is
$27.37$, and the bounded prime translations are noncompact
(measured off-block norm $0.9395$); the r494 compact completion
cannot treat the prime section as the operator.
\emph{The $L=0.3$ certificate itself stands}
(Theorem~\ref{thm:kernel-loewner-03}, v1017) & r496\\
\bottomrule
\end{tabular}
\end{center}

\begin{center}\small
\begin{tabular}{@{}p{0.30\linewidth}p{0.44\linewidth}l@{}}
\toprule
\textbf{Attack class (wave 4)} & \textbf{Measured failure (break locus)} & \textbf{Round}\\
\midrule
Border-dressed resolvent identities & exhaustive census (four
augmented-first candidates $+$ 72 Pl\"ucker/Dodgson combinations): no
identity in the class; every definite direction is a drain restatement,
the square never sits on the target side & r266\\
\addlinespace[2pt]
Eventual triangle & $\mathrm{sp}(g,N)=+0.31$ under the bar, exceptions in
all terciles, the deepest rung kz52 ($N=878$) itself an exception ---
no magnitude-separation law & r263\\
\addlinespace[2pt]
Exact-identity routes for the exception scalar & K1 bordered-Schur $=$
target restatement $+$ support geometry (terminal ratio stays positive on
flipped controls); K2 squares on the drain side; K3 paircorr miniature
($d2$ fires $42/42$) & r263\\
\addlinespace[2pt]
Euler mechanism for the cancellation decay & no surgery collapses
$\gamma_{\rm true}$ (all 12 stage medians $\ge+0.20$; free permutation
$+1.40$): the $N^{-0.45}$ decay is generic root-scale, not an arithmetic
fingerprint & r273\\
\addlinespace[2pt]
KYP / state-space memory & two-sided structural no-go: every uniform
quadratic memory exact-rationally infeasible (o1 fiber forcing $p_{11}=0
\Rightarrow p_{12}=0$; o2 descent window $\sum b^2\le0$), and the unique
admissible Riccati memory is target-inverse ($\mathrm{sp}=+1.00$, falsely
certifies \textsc{scramble}) & r275\\
\addlinespace[2pt]
Raw atom-Sturm winding & \textsc{main} breaks $c_n\equiv n$ at $56/48$ at
positive $h$: the positive-measure separation theorem fails genuinely for
the signed comb --- not the Maslov object (R2 is) & r277\\
\addlinespace[2pt]
Uniform metric stability constant & the local law exists but
$\theta^*$ sits $25$--$170\times$ below the smallest measured dose;
$L_D$ window-specific ($17.8$--$309$): perturbative only, no uniform
lemma candidate & r278\\
\bottomrule
\end{tabular}
\end{center}

\noindent The consistent reading (r259/r261): in both cores the object is
a \emph{coherent totality}, not a dominant component --- the true sign is
carried by coherent cancellation over the configuration ensemble
($\sim5.7$ decades deep at $n=6$ already), which no dominance model at any
$k$-swap resolution sees.  This is the consistent explanation of why two
years of single-component attacks measurably failed.  The sharpened
residual question after r265: \emph{no admissible $s$-coordinate carries
both the right endpoint and a definite dynamics} --- a genuine $s$-tool
must couple the border drive into the resolvent structure without reducing
to the wall bound (the r266 question, Section~\ref{sec:edges}).

\noindent Round r607 (probe \texttt{event\_lindblad\_twokey\_probe.py},
SHA \texttt{3c1db81b}, SPEC \texttt{3abe6b9b}, $15/15$) is a method
no-go: \textsc{killed(recoordinatization)}.  Lindblad CP
$\Leftrightarrow\lambda_{\min}(Q_W)$ to $10^{-15}$ in
\textsc{true}/\textsc{scramble}/\textsc{epstein}/\textsc{wperm};
the diagonal compensator $\Sigma\lambda$ diverges $\sim 2e^{X/2}$;
Euler--Pick Gram needs $10$ scalars; labels are decorative.
Experiments only.  \textbf{No RH claim.}

\noindent (2026-09-02) Prolate hyperbolicity route.  Connes--Consani--Moscovici,
\texttt{arXiv:2310.18423}, the proposition after Lemma~3.3, gives
$F_\mu(\mathcal{E}(\psi))(s)=R(s)\cdot\Xi(s)$, so $\hat k_\lambda$
contains $\Xi$ as a factor.  Real-rootedness of $\hat k_\lambda$ is
therefore RH together with real-rootedness of $R_\lambda$, and the
ratio $F_4/F_0$ cancels $\Xi$.  Typed
\textsc{killed(factorization)}; no probe needed.
\textbf{No RH claim.}

\noindent (2026-09-02) Herglotz closure reduction.  The statement
``finite Herglotz functions $m_j\to-\Phi'/\Phi$,
$\Phi(z)=\xi(\tfrac12+\sqrt{z})$, on a set with an accumulation point
in $\mathcal{U}=\{\mathrm{Re}\sqrt{z}>1/2\}\cap\mathbb{H}$, bounded at
one point $\Rightarrow$ RH'' is correct classical analysis (normal
family of Herglotz functions, identity theorem, pole contradiction),
but its hypothesis implies RH.  Recorded as a reduction.
Round r611 (probe \texttt{connes\_observable\_aubin\_nitsche\_probe.py},
SHA \texttt{93607746}, SPEC \texttt{647335a6}, $12/12$) returns
\textsc{obs\_not\_converging}: hybrid $Q_\lambda$ has
$\lambda_{\min}\approx-10^{-5}$ for $L\ge0.5$ (r606 discretisation
crash removed, $t_{\mathrm{cut}}$-stable);
$\eta_{\mathrm{obs}}(1.2)$ on TRUE is $0.667/6.3\cdot10^{-3}/3.7\cdot10^{-4}$
at $L=0.3/0.4/0.5$ then \emph{rises}
$1.6\cdot10^{-2}/5.6\cdot10^{-2}/9.0\cdot10^{-2}/0.150$ at $L=0.6$--$1.0$
($p=-8.1$).  OFFLINE $\approx$ TRUE ($0.109$ at $L=1.0$), so the
observable is blind to an injected off-line zero at $\gamma=20$;
WPERM $0.69$, SCRAMBLE $0.20$; identity~(8) residual $4.6\cdot10^{-11}$.
The Herglotz/Aubin--Nitsche closure route (Solution~1) is STOP as a
lane; \texttt{PROLATE.GABOR.ALIGN} is moot.  r606b is CLOSED
(\textsc{inconclusive}(evals-not-converged, disc-disagree$>5$pct);
superseded by r611 and the \textsc{killed}(factorization) typing of
the prolate route; diagnostic only).  Experiments only.
\textbf{No RH claim.}

\noindent (2026-09-02) Dyadic heat-semigroup criterion.  For the Lean
pure-Gabor family the cross term $2e^{(\delta^2-\omega^2)/2a}$ carries
residual $-\delta^2/(2a^2)\ne0$, so $U=\sqrt{a}\,Z$ is not a heat
solution.  The dyadic-in-$a$ heat-semigroup reduction is unavailable
and is replaced by the countable rational-packet criterion (r612).
Parent analysis 2026-09-02; confirmed numerically at r608 (rel $3\cdot10^{-9}$
on traveling lobes).  \textbf{No RH claim.}

\noindent Round r613 (probe \texttt{prime\_e8\_ordered\_herglotz\_probe.py},
SHA \texttt{c89fc1ce}, SPEC \texttt{01b4a8be}) returns
\textsc{no\_identification}: the Weyl function is Herglotz
(min Im-eig $+3.5\cdot10^{-3}$), $(JH)^2=0$ exact; at $\varepsilon=0$
all jumps commute so order is decorative (max $10^{-14}$, same lesson
as r607); at $\varepsilon=0.05$ order/labels are non-decorative
($16.6/12.0$) but residuals at Euler points TRUE $0.66$--$2.5$ agree
with SCRAMBLE/WPERM $0.66/0.64$ and $2.36/2.36$; the twirl chain
$E_{\mathrm{full}}<E_7<E_3$ breaks ($4.5>3.1$).  The ordered-E8
Herglotz canonical system (Paket~C,
\texttt{PRIME.E8.ORDERED.HERGLOTZ.01}) is dead as a lane.
Experiments only.  \textbf{No RH claim.}

\noindent Round r615 (probe \texttt{semilocal\_firststep\_probe.py},
SHA \texttt{3a0741de}, SPEC \texttt{04f9c9ec}, $26/26$) confirms the
r496 certificate obstruction on every window that contains a prime:
the shift cross-block norm is $0.50$ for $N=40/80/160$ (no decay), and
the Schur requirement is $R=0.25/\lambda^*=1.4\cdot10^{3}$ at
$L=0.40$ up to $4.45\cdot10^{6}$ at $L=0.549$.  First-prime-step
windows are therefore not certifiable by the compact-tail/Schur route.
Experiments only.  \textbf{No RH claim.}

\noindent Typed no-go \texttt{E8.COXETER.REGULARIZED\_SPLIT.NO\_GO.01}
(round r617, probe \texttt{e8\_coxeter\_euler\_completion\_probe.py},
SHA \texttt{6ac6bab2}, SPEC \texttt{5aa3935b}, $45/45$,
\textsc{completion\_exact}): $\det_2$ removes exactly the trace term;
the RH content is the analytic continuation of
$P(s)=\sum\mu(k)/k\log\zeta(ks)$; constructions that control only the
Hilbert--Schmidt part cannot isolate $1/\zeta(s)$ (this is the
conceptual content of r607/r613).  Fence: ``The trace-free completion
is zero-free and pole-free in $\mathrm{Re}\,s>1/2$. The splitting into
the scalar zeta channel and the Coxeter channel is open and
RH-equivalent. No RH claim.''  Promotion to \texttt{verification/}
(v1018/v1019) is in progress.  Experiments only.

\noindent Round r618 (probe \texttt{jensen\_compiler\_rigidity\_probe.py},
SHA \texttt{29ccf1c7}, SPEC \texttt{bc8d8624}, $43/43$) returns
\textsc{compilers\_structurally\_rigid}.  Two no-gos:
(A)~Seifert-compiler rigidity --- the Coxeter monodromy spectrum is a
congruence invariant ($\Phi_{30}$ for $12$ random $MSM^*$);
Cayley-transformed Jensen polynomials $\mathcal{C}_{n,8}$ of $\Xi$
vary with $n$ and miss the $30$th-root grid by $\ge 0.0168$; blocks
$d=2..7$ give $\Phi_2^2$, $\Phi_2\Phi_3$, $\Phi_5$, $\Phi_2\Phi_8$,
$\Phi_3\Phi_{12}$, $\Phi_2\Phi_{18}$;
(B)~rank-one MSS mixed characteristic polynomial equals
$\det(xI-\sum A_e)$ exactly; required coefficient ratios
$c_{n+1}/c_n$ ($0.0231\to 0.0169$, $n=0..6$) are nonlinear in zero
power sums.  Both compilers are generic real-rootedness certificates;
existence for all $(n,d)$ is equivalent to RH; the E8 data are
RH-neutral.  Experiments only.  \textbf{No RH claim.}

\noindent Round r621 (probe \texttt{p2\_digamma\_duplication\_probe.py},
SHA \texttt{dff2e1b3}, SPEC \texttt{78f47c87}, $37/37$) returns
\textsc{duplication\_constant\_only}.  The duplication identity
$\psi(z)+\psi(z+\tfrac12)=2\psi(2z)-2\log 2$ holds to
$2\cdot 10^{-25}$ and the operator identity
$\mathrm{ARCH}_{1/4}=\mathrm{ARCH}_{\mathbb C}-\mathrm{ARCH}_{3/4}
-2\log 2\cdot G$ to $2\cdot 10^{-15}$, but the prime-2 operator
$P_2=-w_2 T_{\log 2}$ versus $\mathrm{span}\{\mathrm{ARCH}_{3/4},G\}$
has residual $0.997$--$0.998$ on the whole odd space (HS cosine
$\le 0.005$, fitted $\beta\approx 0.06$ not $-2\log 2$).
Translation is not a constant.  The $\mathbb Q(i)$ form
$\mathrm{POLE}+\mathrm{ARCH}_{\mathbb C}$ stays positive without
its norm-$2$ prime ($\lambda_{\min}+0.59$ at $0.549$);
$\Gamma_{\mathbb C}$ without the constant fails at $0.38$ like
$\zeta$ --- the conductor constant carries, not the $3/4$
channel.  No-go: the prime-$2$ repair is not a duplication
factor of the archimedean operator.  Experiments only.
\textbf{No RH claim.}

\noindent Round r622 (probe \texttt{support\_darboux\_probe.py},
SHA \texttt{845bb937}, SPEC \texttt{27ffd017}, PAYLOAD
\texttt{e1031d17}, $64/64$) returns
\textsc{transport\_feasible\_norule}.  On the scaled family
$\widetilde Q(L)=U_L^*Q_L U_L$ the transport identity
$\partial_L\widetilde Q+X^*\widetilde Q+\widetilde Q X+c\widetilde Q=Y^*Y$
is feasible at $X=0$ for the scalar
$c=c_0(L)=-\lambda_{\min}(\widetilde Q^{-1/2}(\partial_L\widetilde Q)\widetilde Q^{-1/2})$
(finite: $26$ at $0.25$, spike $368$ at prime entry, $1614$ at
$0.549$), but there is no two-term closed-form law for $c(L)$
(rms $347$; fails at the kink and at $0.549$); the forbidden
$X_0=-\tfrac12\widetilde Q^{-1}\partial_L\widetilde Q$ is not in
the source algebra (projection residual $0.88\to 1.0$); the
algebra produces rate $0$ versus required $31.8/46.0$.  The
prime is not the PSD obstruction --- it spikes $c_0$.  No-go:
Darboux transport in the source algebra yields no rule and no
rate.  Experiments only.  \textbf{No RH claim.}

\noindent Round r619 (probe \texttt{support\_relay\_census\_probe.py},
SHA \texttt{fcfa7771}, SPEC \texttt{7299737d}, RESULT
\texttt{e0379485}, $12/12$) returns \textsc{relay\_universal},
but the $\mathrm{RELAY\_UNIVERSAL}$ criteria ($\Delta_j\ge 0$,
$g<0$, newest-weight repair) are consequences of $Q_j\succeq 0$
(RH-consistency), not a mechanism.  Relay-as-mechanism is
therefore a no-go: the informative content is the nearly
constant leads, the control breaks at entry+lead, the
super-exponential $\lambda^*$ decay (r619 log-slopes
$-77/-79$ at $L=0.9$--$1.4$ were measured at fixed $N=80$
beyond the SVD floor and are $N$-artifacts, corrected by
r630b to $-50.6$ on $0.3$--$0.6$ and $-116$ on $0.6$--$1.0$;
the $\mathrm{RELAY\_UNIVERSAL}$ verdict, leads
$\Delta\approx 0.015$, and $L_{\mathrm{det}}$ table are
unaffected), and $L_{\mathrm{det}}$.
Windows with $L\gtrsim 0.5$ are hypersensitive off-line
detectors, so relay steps beyond $L\approx 0.5$ are RH-in-a-box
statements, not elementary finite problems.  Experiments only.
\textbf{No RH claim.}

\noindent Round r623 (probe \texttt{semilocal\_p2\_dilation\_probe.py},
SHA \texttt{d1bec728}, SPEC \texttt{5890676d}, $128/128$) returns
\textsc{p2\_dilation\_structurally\_blocked}.  The $2$-adic channel
of the Weil form is the multiplier
$m_2(t)=\log 2\cdot(1-P_r(t\log 2))$ with $r=2^{-1/2}$ and $P_r$
the Poisson kernel (boundary value of the de~Branges--Rovnyak
kernel of the Blaschke factor $b(z)=(z-r)/(1-rz)$); one Euler
factor generates the whole rung ladder $2^k$ (active rungs:
$[2]$ for $L\le 0.65$, $[2,4]+3$ for $L\in\{0.72,0.80\}$);
identity residual $4.9\cdot10^{-12}$.  Obstructions to the
proposed PSD block $\bigl[\begin{smallmatrix}A_S&B^*\\B&C\end{smallmatrix}\bigr]$
with Schur complement $Q_L$: the ancestor $\mathrm{POLE}+\mathrm{ARCH}$
has $\lambda_{\min}=-0.078$ ($L=0.40$), $-0.229$, $-0.367$,
$-0.430$ ($0.549$), $-0.536$, $-0.636$, $-0.773$, $-0.926$
($0.80$) and therefore cannot be a PSD principal block; the
$2$-channel operator $M_2$ is indefinite ($n_+/n_-$ $9/9$ at
$0.40$ \ldots $14/10$ at $0.80$) with numerical $\mathrm{rank}/N$
$0.75$--$1.0$ (not rank one) and therefore cannot be
$-B^*C^{-1}B$; the generic dilation
$\bigl[\begin{smallmatrix}Q+M_{2-}&\sqrt{M_{2-}}\\\sqrt{M_{2-}}&I\end{smallmatrix}\bigr]$
has $\lambda_{\min}$ tracking $\lambda^*(L)$
($1.82\cdot10^{-4}$ vs $1.81\cdot10^{-4}$ at $0.40$) ---
tautological.  No manifest local SOS in the named factors:
$\lVert N\rVert/\lambda_{\min}(P)=2.75$--$5.27$ (negative part
never dominated).  Literature: Connes--Consani 2021 (Selecta)
Thm~1 positivity only for support $[2^{-1/2},2^{1/2}]$
(archimedean); CCM arXiv:2310.18423 \S4.6 Sonin space without a
positivity theorem; arXiv:2403.01247 Jacobi matrices for
$S=\{\infty,p\}$ ($q=1/p$); $\zeta$-cycles numerical --- no
published unconditional first prime step.  The semilocal
dilation lane is not opened.  Experiments only.
\textbf{No RH claim.}

\noindent Round r625 (probe \texttt{iiks\_vanishing\_metric\_probe.py},
SHA \texttt{973a1ce2}, SPEC \texttt{7a4257e3}, NUM
\texttt{2d066e2d}, $19/19$) returns
\textsc{iiks\_no\_metric\_in\_class}, recorded in prose as
\textsc{not\_testable\_in\_corpus\_form}.  For the corpus's
discrete IIKS/CD object the jump $J_s=I-2\pi i s\,fg^{\mathsf T}$
on the real CD nodes fails Zhou's requirement $J^*=J^{-1}$
(rel $2.56\cdot10^{-2}$ on WPERM kz9), and $\det J=1$ at $s^*$
while $\tau(s^*)=\det(I-s^*E)=0$ ($\log\lvert\tau\rvert\approx-38$):
this $J$ is not the RHP jump equivalent to $\tau(s)\ne0$.  The
$H=I$ ``dissipativity'' $J+J^*\succeq0$ (holds on TRUE with min
eigenvalue $1.93$--$1.99$ \emph{and} on WPERM despite $\tau$-zeros)
is the wrong sufficient condition and is dropped.  Best
source-explicit contractive metric
$H=\mathrm{diag}(\mathrm{arch}^{1.50},\mathrm{arch}^{1.50})$:
TRUE worst margin $-8.17\cdot10^{-10}$ (first pass
$-3.36\cdot10^{-7}$; bar $-10^{-12}$), WPERM
$-7.9\cdot10^{-9}\ldots-4.4\cdot10^{-4}$, EPSTEIN
$-2.7\cdot10^{-2}\ldots-6\cdot10^{11}$, SCRAMBLE
$-3\cdot10^{6}\ldots-9\cdot10^{57}$; controls fail pointwise
before their $\tau$-zero ($5/5$).  The vanishing-lemma route is
not testable on the corpus object.  Experiments only.
\textbf{No RH claim.}

\noindent Round r626 (probe \texttt{xi\_finitefree\_collision\_probe.py},
SHA \texttt{c4750063}, SPEC \texttt{c7cef974}, NUM
\texttt{f533d168}, $51/51$) returns
\textsc{collision\_detector\_calibrated\_no\_barrier}.  The
identity $d/dt\log\lvert\mathrm{Disc}(P_t)\rvert=4\cdot\Phi_N(P_t)$
along the de~Bruijn--Newman heat flow (rel error
$8.9\cdot10^{-11}$) makes the ``collision barrier''
$\int\Phi\,dt<\infty$ equivalent to $\mathrm{Disc}\ne0$ along the
path (no independent content).  Taylor sections of $\Xi$ are not
real-rooted at $t=0$ ($N=6/8/12$ have $0$ real roots; the
$\gamma_1$ pair becomes real only at $N=16$--$20$); $t_0(N)$ is
always a spurious far-pair collision; inner collisions occur at
$t_c>0$ for every $N$.  Detector calibrated on the hyperbolic
control $S_N=\prod(1-x^2/\gamma_k^2)$:
$\varepsilon_{\mathrm{crit}}=-3.65\cdot10^{-4}$ ($a_1$, $N=8$),
$t_c=0.597$, $\log_{10}\lvert\Phi\rvert$ up to $7.90$.  Tur\'an
on $c_n=n!a_n$ holds (min ratio $1.02$); Newton/Laguerre on the
sections fails ($0.51$); no simple coefficient inequality
implies inner $\mathrm{Disc}>0$.  Experiments only.
\textbf{No RH claim.}

\noindent Round r627 (probe \texttt{af\_twomoment\_optimizer\_probe.py},
SHA \texttt{da1a9118}, SPEC \texttt{9b95f564}, $17/17$) returns
\textsc{no\_gain}.  Contract \texttt{PRIME.INERTIA.TWOMOMENT.OPT.01}.
The A--F two-moment functional
$R(\psi)=[\int\psi^2+\iint\lvert u-v\rvert\psi(u)\psi(v)]/(\int\psi)^2$
with $p=2-R$ on $\mathrm{supp}\,\psi\subset[-\tfrac12,\tfrac12]$
has Euler--Lagrange $(I+K)\psi=\lambda\cdot 1$, hence
$\psi''+2\psi=0$ and unique critical point
$\psi^*=\cos(\sqrt{2}\,t)$ (Montgomery--Taylor),
$p^*=0.6725007037$ ($N=8$--$64$ identical, $\omega$-scan optimum
$\sqrt{2}$); positivity slack $\min 0.760>3/4$.  The proved ceiling
$0.6818287$ bounds the richer two-moment $+$ on/off-block
certificate, not the window optimum; the gap $9.3\cdot10^{-3}$ is
not closable by window optimization.  Experiments only.
\textbf{No RH claim.}

\noindent Round r628 (probe \texttt{window\_box\_verifier\_probe.py},
SHA \texttt{9c99ed6a}, SPEC \texttt{36c0e66a}, NUM
\texttt{84aaf685}, $12/12$) returns
\textsc{compression\_certified\_box\_numerical}.  Contract
\texttt{PRIME.WINDOW.BOX.VERIFIER.01}.  Interval-certified
($40$ digits, interval Cholesky/Weyl) positivity of the finite
window compression:
$\lambda_{\min}\ge 1.05\cdot10^{-7}$ ($L=0.55$, $N=24$, primes
$\{2,3\}$), $\ge 6.1\cdot10^{-11}$ ($0.65$, $N=40$),
$\ge 9.3\cdot10^{-17}$ ($0.80$, $N=40$, $\{2,3,4\}$) ---
cancellation depth up to $6\cdot10^{14}$.  A rigorous zero-free
box is \emph{not} closed: the unconditional on-line majorant via
Trudgian $\lvert S(T)\rvert\le 0.112\log T+0.278\log\log T+2.51$
(JNT 134, 2014) is $\sim 10^{7}$ too loose; the numerical box is
nonempty ($L=0.80$: $\beta_{\min}=0.55$ for $\gamma\le 25$ and
$50/60$; $0.70$ at $30$; $0.80$--$0.90$ at $35$--$45$);
$\log\Gamma_{\mathrm{num}}=-1.24+6.86\,L$ ($\sigma=0.1$).
Missing lemma: a local zero-density bound or enumerated zeros
(then Booker/Turing-equivalent).  Typed as the explicit-formula
verification method (Booker, Exp.\ Math.\ 15 (2006)) in window form.
Experiments only.
\textbf{No RH claim.}

\noindent Round r629 (probe \texttt{certificate\_class\_atlas\_probe.py},
SHA \texttt{4dae1fbe}, SPEC \texttt{0bf7c64f}, $15/15$) returns
\textsc{atlas\_no\_new\_unconditional\_gain}.  Contract
\texttt{PRIME.CERTIFICATE.ATLAS.01}.  LP over hypothetical zero
configurations: A1 two-moment reproduces $1-p_0=0.3181713$
($p_0=0.6818287$); A2 $+N(T)/S(T)$: unchanged; A3 $+$ prime-free
window positivity (CC 2021): redundancy $0$ (min slack
$2.2\cdot10^{-4}$); A6 $+$ Huxley zero density: unchanged
(off-line mass forced to $\sigma\le 1/12$, already at
$\sigma\to 0$).  Conditional rows: GUE $k=3\to 5/6$,
$k=4\to 31/36$; pair correlation $\alpha_{\max}=1.5/2/\infty\to
0.7046/0.7159/0.7500$.  Reading: only correlation input beyond
support $1$ buys proportion.  Experiments only.
\textbf{No RH claim.}

\noindent Round r632 (probe \texttt{jet\_deflated\_ldet\_probe.py},
SHA \texttt{63ecb68f}, SPEC \texttt{31a29760}, RESULT
\texttt{6299cedc}, $14/14$) returns \textsc{jet\_gain\_small}.
Contract \texttt{PRIME.WINDOW.JETDEFLATION.01} (reviewer-proposed).
Baseline reproduces r619 $L_{\mathrm{det}}$ ($0.55/0.50/0.80$).
Deflation $\hat f^{(r)}(20)=0$: $m=0$ no gain ($G=0$), $m=1$
$G=0.40$ ($L_{\mathrm{det}}$ $0.55\to 0.95$ at $(0.6,20)$);
control at $\gamma=50$ moves $0.10$--$0.20$; deflating the first
eight true zeros: $G=0.65$ and sector Ritz floor at $L=0.6$ jumps
$1.76\cdot10^{-9}\to 2.02$.  Coverage kill: deflating all zeros up
to $\Gamma=50$ already needs $\dim K/N=0.50$ (above Nyquist $31.8$),
$\Gamma=100\to 1.45$ (sector collapses), $\Gamma=200\to 3.95$ ---
sectorization is not a cheap RH-free statement
(recoordinatization).  Deflation at $20$ does not touch the
odd-sector failure (peak frequency $4.6/3.9$).  Experiments only.
\textbf{No RH claim.}

\noindent Round r630 (probe \texttt{margin\_law\_symreg\_probe.py},
SHA \texttt{5b3d1125}, SPEC \texttt{87febb97}, RESULT
\texttt{a0765d7f}, $19/19$, $166\,\mathrm{s}$, dual-run
identical) returns \textsc{margin\_law\_unresolved}.  Contract
\texttt{PRIME.MARGIN.LAW.SYMREG.01}.  First-run facts:
$L_{\mathrm{cross}}=0.3716$, double-precision SVD floor
$L=0.78$.  Addendum r630b (tall QR, $40$ digits,
$N\in\{40,60,80,120,160\}$, $L\in[0.30,1.40]$) decides
\textsc{n\_limited\_artifact}: $\lambda^*(L)$ converges in $N$
for $L\le 1$ ($35/36$ points, relative change $120\to 160$
median $4.8\cdot10^{-2}$) but not for $L>1$ ($0/20$);
converged log-slopes $-50.6$ (band $0.3$--$0.6$, $N$-stable
to $3$ digits) and $-116$ (band $0.6$--$1.0$, stable for
$N\ge 80$); $\lambda^*_{160}(0.6)=1.65\cdot10^{-9}$,
$\lambda^*_{160}(1.0)=9.93\cdot10^{-30}$; no converged
holdout, descriptive quadratic $C=95.5$ (neither
$2\gamma_1=28.27$ nor $\gamma_1^2/\pi=63.6$) --- both the
Widom-$L^2$ and the $-2\gamma_1 L^2$ readings unsupported.
N2: the converged minimizer nulls $4707$--$4831$ of $5000$
zeros (frequency-sparse).  N3 synthetic controls
($\alpha=$ fitted decay constant): true $169$,
constant-density $101$, all ordinates scaled to first zero
at $10$: $105$, at $20$: $271$, only first zero moved to
$20$: $172$ --- density-driven (Landau--Widom plunge from the
unconditional $N(T)$ profile of the low band), not
band-edge; RH-neutral.  N4 dBN gap functional $B_k$ on
$4999$ gaps: $\max 41.8$, $p_{99}=11.5$, median $2.13$,
fraction $B_k<2=0.470$ ($0.515$ after compressing one gap
by $0.3$) --- no universal gap barrier.  Experiments only.
\textbf{No RH claim.}

\noindent Round r633 (probe \texttt{frontier\_followups\_probe.py},
SHA \texttt{9f5287e7}, SPEC \texttt{ee7c7ba7}, RESULT
\texttt{814abdf5}, $16/16$, $18\,\mathrm{s}$, dual-run
identical) returns four follow-up verdicts on sealed objects.
Contract \texttt{PRIME.FRONTIER.FOLLOWUPS.01}.
F1 \textsc{a\_compact\_other}($p\approx 1.02$):
$a_{\max}(\sigma,\gamma^*)=c\,\sigma^{1.024}(\log\gamma^*)^{-1.65}$
($r^2=0.945$, $n=24$), linear in $\sigma$, not $\sigma^2$
(mechanism: phase threshold
$\cos(\sigma(\gamma-\omega)/2a)<0\Leftrightarrow
a<\sigma\lvert\gamma^*-\omega\rvert/\pi$, not the amplitude
gain); $a_0=0.720$ at $(\sigma,\gamma^*)=(\tfrac12,20)$,
extrapolated $0.714$ at $\gamma_1$; true zeros
$\min_\omega Z(a,\omega)>0$ for
$a\in\{1,5,25,100,250\}$; adversarial $\sigma=\tfrac12$
negative only at $a=1$ ($Z=-3.86\cdot10^{-4}$ at $\gamma_1$).
Parent typing: the compactification removes only the
$a\to\infty$ end (RH-blind wide-frequency packets); the
RH-relevant end $a\to 0$ stays unbounded; a Lean theorem
would need an external explicit $N(T)$/gap bridge ---
cosmetic, not pursued.
F2 \textsc{forward\_increases\_margin}: Hellmann--Feynman
equals Loewner to $10^{-15}$;
$c(L)=\partial_t\log\lambda^*=+0.315/+0.582/+0.764$ at
$L=0.40/0.55/0.80$ ($\lambda^*$
$1.89\cdot10^{-4}/5.70\cdot10^{-8}/2.02\cdot10^{-17}$),
$t^*=-3.18/-1.72/-1.31$ ($O(1)$, not vacuous); with
injection $(\beta,\gamma)=(0.6,20)$ at $L=0.55$:
$\partial_t\lambda^*<0$.  Typing: the sign discriminates
on/off-line but the monotonicity runs in the proof-useless
direction --- detector, no lever.
F3 \textsc{random\_euler\_refuted}:
$\lambda_{\min}(\mathrm{POLE}+\mathrm{ARCH})=-0.0783/-0.430/-0.926$
at $L=0.40/0.549/0.80$ (reproduces r623 to $10^{-3}$);
MC $200$ Haar phase draws at $L=0.55$: mean
$\lambda_{\min}=-0.496$, std $0.317$, $99.5\%$ negative, vs
true $\lambda^*=5.70\cdot10^{-8}$; coherence $C(L)=1$ at
$h_0$ (all $g_{h_0}(\log q)$ same sign); Baker: required
$\delta=O(1)$ vs toy linear-forms bound $4.6\cdot10^{-8}$ ---
$7.3$ decades.  Typing: window positivity beyond
$L_{\mathrm{cross}}$ is arithmetic phase coherence of the
prime terms; magnitude-only and Diophantine-approximation
arguments are dead as a class (measured no-go; promotion
candidate with r615/r619).
F4 \textsc{lead\_law\_invariant}: growth exponent
$\alpha=8.18$ (target $2p+1=7$); argmin of the fitted
margin $+$ overlap laws predicts $0/18$ leads within
$20\%$ --- $\Delta\approx 0.015$ is not a corollary of
those laws; open descriptive invariant.
Experiments only.  \textbf{No RH claim.}

%======================================================================
\section{External position: the rank-trace ceiling (r267)}\label{sec:external}

Round r267 (probe \texttt{ranktrace\_adjudication\_probe.py}, 23/23, SPEC
\texttt{b0437394}) adjudicated \textbf{arXiv:2608.13637} (Alp\"oge--Furman,
Aug.~2026; argument found by an AI system, formalized sorry-free in
Lean~4): a rank--trace inequality on a finite Weil-form compression plus
Sylvester inertia gives \emph{unconditionally} that at least $2/3$ of the
nontrivial zeros are simple and on the critical line, together with a
\emph{proved ceiling} $p_0\le0.6818$ for every certificate built from only
two trace moments against bandwidth-one test functions.

\begin{itemize}[leftmargin=2em]
\item \textbf{Same family, dual side.}  Their compression atoms are
\emph{zeros} (near-tight frame: aspect $1$, atom-norm spread $0$,
$R=4/3$); ours is the \emph{prime comb} (aspect $\approx2$ =
half-filling, atom-norm spread $7$--$8$ decades, $R\approx73$--$126$).
No basis change connects the regimes: \textsc{no\_import}, the frame
mismatch is quantified.
\item \textbf{The ceiling is our wall --- in location.}  Their binding
constraint (off-diagonal prime-sum cancellation beyond diagonal control;
pair correlation with support $>1$) is exactly the locus of our measured
\textsc{paircorr} kill rule: the first \emph{externally proved} form of
our measured wall --- but only for their two-moment certificate class.
\item \textbf{Orthogonal in scope.}  Our full comb coordinate and the
bordered terminal scalar lie outside their certificate class; their
ceiling does not close our lane, and their theorem does not bound our
objects (\textsc{ceiling\_orthogonal}, \textsc{two\_moment\_blind}).
\item \textbf{Source note.}  Their \texttt{EnclOK} numerics are interval
arithmetic outside the Lean kernel --- their unconditional statement is
Lean-checked modulo that enclosure layer.
\end{itemize}

The strategic consequence: the wall the program measured from the inside
for two years now has an external, independently proved counterpart in the
two-moment class.  Passing it \emph{provably requires} either pair
correlation beyond support one or a certificate class outside two trace
moments --- which is precisely where the program's bordered terminal
scalar already lives.

\noindent Round r616 (probe \texttt{inertia\_highermoment\_probe.py},
SHA \texttt{26ee62ae}, SPEC \texttt{fef4a72f}, $16/16$) returns
\textsc{c\_capped} as a follow-up to r267.  The $k=2$ reconstruction
reproduces Alp\"oge--Furman $2/3$ ($\psi_0$) and the ceiling
$0.6818287$.  $\mathrm{Tr}(W^3)$ at bandwidth one is not
unconditional ($\sum\lvert\xi_j\rvert\le 3>2=$ Rudnick--Sarnak 1996
support for $\zeta$; Hejhal 1994 hexagon under RH; Montgomery 1973 and
Goldston--Montgomery 1987 under RH; the Montgomery--Vaughan diagonal
$X^k\le T^{2-\varepsilon}$ fails for $k\ge 3$), hence
$p_3^{\mathrm{uncond}}=p_2=0.6818$.  GUE ideal input $n_+$:
$0.75\to 5/6\to 31/36$; simple-on-line $0.6818\to 0.6818\to 0.7222$.
Finite moments never force negative index $0$.  Experiments only.
\textbf{No RH claim.}

%======================================================================
\section{The remaining edges and the campaign}\label{sec:edges}

\noindent The kernel-Loewner compact-tail lane is not among these edges:
Theorem~\ref{thm:kernel-loewner-03} machine-checks $\lambda_*(0.3)\ge 2.1{\times}10^{-3}$ in float64
(not interval, not~\mE),
and Section~\ref{sec:nogos} records the method no-go at $L=0.8$.

\begin{openproblem}[Edge A: terminal cross-ratio, cofinal]
Prove $q_N<1$ on the cofinal ladder --- equivalently derive the true floor
of $h_{N-1}/F_{N-1}^2$ source-purely (the measured floor is $1.4278>7/5$,
margin $0.028$, not saturated).  By Theorem~\ref{thm:bilinear} the last
fiber edge is a \emph{positive tau cross-ratio}.  \textbf{Wave-4 state:}
the $42$-rung \emph{surface} of this edge is certified
(Section~\ref{sec:surface}: $41$ mechanism target-blind $+$ kz15
exact-finite) --- a census $+$ certificate result, not a cofinal theorem.
The cofinal front is the single hypothesis H5 of the r271 universal pair
theorem (equivalently lemma L2, pair-sum decay): a non-adjacent/global
mechanism at address c3 supplying $N^{-\delta'}$ with $\delta'>0.21$ of
the measured available $0.45$; per r273 the mechanism must capture
\emph{generic} cancellation, and per r275 the state-space vehicle is
excluded.  Registered as \texttt{PRIME.PORT.COUPLEDTAU.}\allowbreak
\texttt{TERMINAL\_CROSSRATIO.01}~\mO.
\end{openproblem}

\begin{openproblem}[Edge B: prefix resummation / oriented midpoint theorem]
Find the closed form of the coherent ensemble resummation: the determinant
as a signed partition sum, resummed source-purely --- no saddle dominance,
no $k$-swap truncation (both measured dead, Section~\ref{sec:nogos}).
The r261 finite-combinatorial half closed honestly negative
(\textsc{campaign\_input\_frozen}).  \textbf{Wave-4 state:} the
orientation of the wall follows a \emph{predictable} law --- the Maslov
census GO of Theorem~\ref{thm:maslov} --- and the named path is the
\emph{oriented midpoint theorem}: the contraposition with an independent
index obstruction that forbids the R2 interlacing break before
half-filling on \textsc{main} (the raw atom-Sturm count is refuted; per
r276/r278 the \texttt{MainWindow} arithmetic must enter as \emph{metric}
exactness, not Euler bookkeeping).  Registered as
\texttt{PRIME.PORT.FULLSOURCE.}\allowbreak
\texttt{PREFIX\_RESUMMATION.01}~\mO\ with the successor contract
\texttt{PRIME.PORT.RHP.MIDPOINT.}\allowbreak
\texttt{ORIENTED\_THEOREM.01}~\mO.  \textbf{Wave-5 state:} rounds
279--281 are executed and consumed
(Section~\ref{sec:pinning}): the theorem share is promoted (the parity
and budget theorems), the candidate generic two-sided obstruction is
refuted exactly, and the center is precisified --- the entire open
content is the budget localization, i.e.\ free-window
quasi-definiteness (Remark~\ref{rem:northstar}); the successor lanes
\texttt{PRIME.PORT.}\allowbreak\texttt{REPRESENTATION.}\allowbreak
\texttt{CONTEST.01} and \texttt{PRIME.PORT.RHP.}\allowbreak
\texttt{FULLSOURCE.}\allowbreak\texttt{QUASIDEFINITENESS.01}
are executed and consumed in wave 6 (\texttt{v963},
Section~\ref{sec:lstar}); the open center continues in canonical form
as \texttt{PRIME.LSTAR.SUBORDINATION.01}~\mO.
\end{openproblem}

\begin{openproblem}[Odd-sector edge of the first prime step]
For odd $h$ supported on $[-L,L]$,
\[
  \langle h,(\mathrm{POLE}+\mathrm{ARCH})h\rangle
  + w_2\bigl(\lVert f_+\rVert^2-\lVert f_-\rVert^2\bigr)
  \;\ge\; 0,
\]
where $f_\pm$ are the even/odd parts of $h$ relative to the
reflection $(Rf)(x)=f(d-x)$ at $d=\log 2$ (overlap
$I=[d-L,L]$).  Round r620 retypes the parent's earlier
$\lvert g\rvert$ lemma: the prime term $-w_2 g$ stabilizes the
symmetric edge sector and destabilizes the antisymmetric one,
so a $\lvert g\rvert$ bound was mis-stated (the prime hurts on
$P_-$).  The negative direction of $\mathrm{POLE}+\mathrm{ARCH}$
migrates from the inner region to the $R$-symmetric edge
sector (mass inner$/+$/$-$: $0.85/0.15/0.005$ at $L=0.38$ to
$0.06/0.94/0.001$ at $L=0.549$); the antisymmetric edge
sector is safe without the prime
($\lambda_{\min}(A|_{P_-})-w_2=+5.7\to +0.29$).
The repair is a quantitative dominance with margin
$\lambda^*(L)=\lambda_{\min}(A^{\mathrm{odd}}+w_2(P_+-P_-))$,
not an exact sector identity.
Registered as \texttt{PRIME.SEMILOCAL.ODDSECTOR.EDGE.01}~\mO
(r615; retyped r620 to the oriented form).
Window forms with $L\lesssim 16$ cannot detect off-line zeros
(r605 scaling $a=\sigma^2/64$ needs $u$-support $\sim 8/\sigma$),
so the first prime step is far weaker than RH.
\textbf{No RH claim.}
\end{openproblem}

\paragraph{Relay architecture (r619--r622).}
The support-relay organizes the window by a canonical interval
per prime power $q$: entry $L_j=\tfrac12\log q$, with continuity
$Q_j(L_j)=Q_{j-1}(L_j)$ (the newest event is added at the
endpoint of the previous interval).  The conjunction
$\forall j\,R_j$ of the finite relay steps is equivalent to
Weil positivity of the window form.  After r619--r622 the
honest status is: the $\mathrm{RELAY\_UNIVERSAL}$ criteria
($\Delta_j\ge 0$, $g<0$, newest-weight repair) are
\emph{consequences} of $Q_j\succeq 0$ (RH-consistency), not a
mechanism; there is no source-algebra Darboux rule for the
rate (r622); the prime-2 repair is not a duplication factor of
the archimedean operator (r621); and relay steps beyond
$L\approx 0.5$ are RH-in-a-box statements (hypersensitive
off-line detectors, r619 $L_{\mathrm{det}}$), not elementary
finite problems.  The r619 log-slopes $-77/-79$ at
$L=0.9$--$1.4$ (fixed $N=80$ beyond the SVD floor) are
$N$-artifacts, corrected by r630b to $-50.6$ ($0.3$--$0.6$)
and $-116$ ($0.6$--$1.0$); the $\mathrm{RELAY\_UNIVERSAL}$
verdict, leads $\Delta\approx 0.015$, and $L_{\mathrm{det}}$
table are unaffected.  \textbf{No RH claim.}

\paragraph{Prime-place dilation and IIKS vanishing (r623, r625, r626).}
The proposed principal-block / Schur-complement dilation of the
first $2$-adic Weil channel is structurally blocked (r623
\textsc{p2\_dilation\_structurally\_blocked}):
$\mathrm{POLE}+\mathrm{ARCH}$ is not a PSD principal block,
$M_2$ is indefinite and not rank one, the generic dilation
tracks $\lambda^*(L)$ tautologically, and the literature stops
at the archimedean window $\sqrt{2}$
(Connes--Consani 2021, Thm~1).  The IIKS vanishing-lemma metric
is not an open lane: the corpus discrete jump is the wrong RHP
object (r625, enum \textsc{iiks\_no\_metric\_in\_class}, prose
\textsc{not\_testable\_in\_corpus\_form}), and no
source-explicit metric in the named class is contractive.  The
finite-free collision barrier is tautological (r626).  Together
with the first cut (r619--r622) every tested positivity route
reduces to Weil positivity in a new coordinate or to an
RH-strength hypothesis.  The standing assets remain the
unconditional rational Gabor criterion (r612C), the three
RH-neutral exact theorems (Eisenstein bridge, commensurability,
Coxeter completion), the first-prime-step mechanism (r615/r620,
oriented lemma~\mO) and the relay census with $L_{\mathrm{det}}$
(r619).  Recommendation: pause the active positivity campaign
pending a genuinely new mechanism.  Subsequent sealed scouts
r627 (A--F window optimum $=$ Montgomery--Taylor, \textsc{no\_gain}),
r628 (box verifier: compression certified, rigor blocked by
$S(T)$ looseness), r629 (certificate-class atlas: no
unconditional gain), and r632 (jet deflation: coverage collapse)
do not reopen a positivity lane.
\textbf{No RH claim.}

\subsection{The master target form (r273)}\label{sec:master}
Since r273 the two edges are carried by \emph{one} augmented positivity
statement (the formal home is \texttt{rh/lean/RH/Window.lean}).  For a
window $w$ with Hankel moment matrices $H_n$, border vector $u$ and
budget $B$, define the augmented matrix
\[
  A_{w,n} \;=\; \begin{pmatrix} H_n & u_n \\ u_n^{\mathsf T} & B
  \end{pmatrix},
  \qquad n \le N_w = \lceil S/2 \rceil .
\]
\textbf{Master target} (\texttt{augmented\_prefix\_positive}): \emph{for a
MAIN window, $A_{w,n} \succ 0$ for every $n \le N_w$.}  The derivation
chain is finite matrix algebra and is \emph{proved} in Lean: the upper
principal block gives $H_n \succ 0$, hence all leading principal minors
are positive and the prefix chain $h_k = \det H_{k+1}/\det H_k > 0$
follows (former edge B: the wall derived, not assumed); the Schur
complement gives $D_n = \det A_n / \det H_n = B - u^{\mathsf T} H_n^{-1}
u > 0$, hence $q_N < 1$ at the terminal (former edge A).  The budget
enters only through $B$, so the $5/7$ floor falls out as a
\emph{consequence} of the master positivity instead of entering as an
axiom.  The open content is concentrated in the honest source predicate
\texttt{MainWindow} (the von-Mangoldt comb arithmetic); the kill lists of
Section~\ref{sec:nogos} enumerate what provably does \emph{not} suffice.
The r273 retype was forced by three machine-checked counterexamples
(\texttt{rh/lean/RH/Counterexamples.lean}): the previous universal
formulations of both edges and of the r271 margin law H5 over bare
bookkeeping structures are \emph{refutable} by trivial models
($q_N = 4$ ladders, $\tau_1 = -1$ chains, $2 < 1$ margins) --- the guards
make any future ``proof from bookkeeping'' formally contradictory.

\subsection{The campaign state (charter r264)}
Frozen charter (SHA \texttt{7b9e751d}): goal S6 verbatim on the seven
exception rungs; input = the r261 package (frozen $G_n$, $t_n$, $B$;
scaled Chebyshev basis); kill list armed (\textsc{paircorr} detector,
target-inverse, selection-by-answer, support-geometry); blocked paths:
smooth dressing (r253 essentiality) and the bordered spectral bound
(r257/r265).  The campaign's deformation lever is the \emph{border-drive
functional near terminal} (data signature \textsc{s1\_termdrive},
separation $0.959$, class \textsc{prefix\_drive}).

\subsection{The named questions in flight (wave-13 state)}
\begin{itemize}[leftmargin=2em]
\item \textbf{The oriented midpoint theorem (rounds 279--281, consumed
in wave 5).}  The step-5 obstruction question is answered in
decomposed form (Section~\ref{sec:pinning}): the generic two-sided
obstruction is refuted exactly, and the demanded independent index
obstruction is precisified to the localization of the fixed budget ---
free-window quasi-definiteness (contract
\texttt{PRIME.PORT.RHP.MIDPOINT.}\allowbreak
\texttt{ORIENTED\_THEOREM.01}~\mO, rounds consumed by
\texttt{v962}).
\item \textbf{The representation contest (r282, consumed in wave 6).}
Contract \texttt{PRIME.PORT.}\allowbreak
\texttt{REPRESENTATION.CONTEST.01}: adjudicated
\textsc{contest\_all\_dead} and consumed by \texttt{v963}
(Remark~\ref{rem:contest}) --- the four classical languages are closed
with exact reasons; a non-classical ``fifth language'' would be
genuinely new mathematics and is not round-ready without a candidate.
\item \textbf{The full-source quasi-definiteness (r283, consumed in
wave 6).}  Contract \texttt{PRIME.PORT.RHP.}\allowbreak
\texttt{FULLSOURCE.QUASIDEFINITENESS.01}: adjudicated
\textsc{mechanism\_partial}(A2) and consumed by \texttt{v963}
(Theorem~\ref{thm:frame}) --- L* is established as the canonical
reduction.
\item \textbf{The L* subordination (the canonical open center).}
Contract \texttt{PRIME.LSTAR.}\allowbreak
\texttt{SUBORDINATION.01}~\mO: $\int p^2\,d\nu_z<\int p^2\,d\mu_z$ for
every real $p\ne0$ with $\deg p<N_w$ on the admissible anchor family
(Theorem~\ref{thm:lstar}); state after wave 7: $57/57$ measured (the
42 plus the 15 extension anchors, $N_w$ up to $1218$), the margin
decay resolved \emph{harmless} (Remark~\ref{rem:margin-law}), the
coherence carrier class named (Remark~\ref{rem:carrier}), the
diophantine route excluded (Remark~\ref{rem:metriconly}); state after
wave 8 (rounds r290--r295 consumed, Section~\ref{sec:curvature}): the
working-set geometry is \emph{measured} (tube, ridge, rank-1 DENS
curvature valley, simple flip zeros) and the profile-functional
question is \emph{functionally negative-sealed} over five class
families (Remark~\ref{rem:killcatalog}) --- the successor question is
the closed-functional contract below; standalone problem statement in
\texttt{rh/problem/lstar\_problem.tex} (updated ``what is known'');
state after wave 11
(Section~\ref{sec:architecture-adjudication}): the surrounding
architecture is fully adjudicated (Lane A closed as cone language,
B not overtaking, C parked) and the \emph{formal} frame is
restructured --- the master theorem is now \emph{proved from} L*
$+$ terminal (\texttt{lstar\_terminal\_implies\_master}, r305), the
window source is a real Lean construction (r310/r310b), and the
contract stands as one of the two TRUE formal window-local holes
(sorries $9 \to 5$); state after wave 12
(Section~\ref{sec:forks-redteam}): the indefinite fork is
adjudicated --- the P1 index language is banked (a total
restatement, no lever), the P2 antiphase fingerprint is closed as
\textsc{algorithm\_artifact} by the r322 dig (the selection-geometry
question is the named residual), and the contract's Lean statement
class is red-team hardened (the r319 U-findings repaired by the
r320 retypes $+$ guards; the honest chain reading of
Remark~\ref{rem:honestchain} is binding).
\item \textbf{The closed functional (r290--r295, registered in wave
8).}  Contract \texttt{PRIME.LSTAR.}\allowbreak
\texttt{CLOSED\_FUNCTIONAL.01}~\mO: exhibit a closed, source-pure
functional of the tent-split fraction profile that predicts the
survival depth of the working set at HARDENED grade (the sealed r295
three-clause bar) with a stable partial channel --- or prove
non-existence in a named class; the sealed kill catalog
(Remark~\ref{rem:killcatalog}) binds the candidate space.  State
after wave 9 (r296 consumed, Section~\ref{sec:l2chain}): the
DENS-identity fork is \emph{closed} (\textsc{dens\_world\_blind} ---
the rank-2 DENS core is not an arithmetic identity and not the
L*-gradient direction at the sealed bar; the lane routed to L2); the
remaining open fronts are the loss-corpus forensics (K05/K07/K19),
the substantive ``why L2'' question and the w7/w11 knife edge.
State after wave 11: \textbf{Lane A (the constructive PSD-G rule)
is closed as a cone language} --- r311 \textsc{strict\_source\_cone}
$+$ r312 \textsc{coefficient\_sign\_wall} with the mechanism named
(block-psd membership without rank-one SDD, obstructed at the
budget/border sign); the PSD-G rule stays open as a question, the
resources moved to the fiber.
\item \textbf{The tensor mechanism (r309/r313/r316, registered in
wave 11).}  Contract \texttt{PRIME.SOURCE.}\allowbreak
\texttt{TENSOR.MECHANISM.01}~\mO: does a source-pure cofinal control
arise from the banked rank-one update identities
(\texttt{PRIME.SOURCE.RANKONE.UPDATE.IDENTITY.01}~\mE)?  Registered
\emph{after} the documented negatives (Remark~\ref{rem:claimsplit}):
no transfer-operator contraction at chain level
(\textsc{floquet\_expanding}), no type-majorant composition, no
two-regime pointwise majorant under a mid-ladder freeze.
\item \textbf{The R\'enyi-3 provenance (r306/r313--r316, registered
in wave 11).}  Contract \texttt{PRIME.L2.}\allowbreak
\texttt{RENYI3.PROVENANCE.01}~\mO: the pointwise GO ($C = 1.069$,
$A = 2$, 57/57, growing reserve) stands as a certified finite
statement; \emph{where does the law come from?}  Trilogy state:
the identity is exact (r314), the functional is blind (r315), the
two-regime form is dead (r316).  State after wave 12
(Section~\ref{sec:slidingbound}): the R317 fork is executed both
ways and resolved --- fork (b) sealed \textsc{whac\_a\_mole}(kz53,
kz83) (the exception-family form is the wrong statement shape for
a spike continuum), fork (a) fired
\textsc{sliding\_bound\_go}($G_{SQ}$)
(Theorem~\ref{thm:slidingbound}, registered as
\texttt{PRIME.L2.RENYI3.SLIDING\_BOUND.01}~\mO); the provenance
question is now the $F_A$/$q_{\max}$ two-part split.  State after
wave 13 (Section~\ref{sec:measured-composition}): the split is
executed both ways --- r324-pre banked
(\textsc{fa\_bounded\_distributional} $+$
\textsc{qmax\_share\_open} $+$ \textsc{not\_composed}), r324
composed MEASURED (\textsc{pileup\_grows\_subcritical} $+0.172$,
subcritical also at the r328B chain-honest bar $0.188$; $m_0^*$
record $10^{59.6}$, chain-honest $10^{238}$), r327 the SOURCE
anatomy; the provenance
question is now the \emph{coincidence geometry}
(Remark~\ref{rem:coincidence}; registered as
\texttt{PRIME.L2.RENYI3.MEASURED\_COMPOSITION.01}~\mO).
\item \textbf{The sliding cubic bound (r317/r321, registered in
wave 12).}  Contract \texttt{PRIME.L2.}\allowbreak
\texttt{RENYI3.SLIDING\_BOUND.01}~\mO: the theorem candidate
$\sum q^3 \le 1.3056\,F_A(w)^2 (\log m)^2/m^2$ for $m \ge 73$
(Theorem~\ref{thm:slidingbound}) is certified on the measured
rungs with all four named violators inside; the demand is the
two-part provenance question --- is $F_A$ source-pure bounded
(measured max $2.47$), and what bounds the $q_{\max}$-share
$\rho_2/(F_A^2 B^2)$ (the median-of-max shape route; the r302
$M_2$ stationarity pins the second moment of the same shape)?
Honest: $C_{\rm impl} = 7.97$ is $7.5\times$ looser than r306 ---
form, not sharpness; the bound is not world-separating by itself.
State after wave 13 (Section~\ref{sec:measured-composition}): the
two-part demand is executed --- $F_A$ is \emph{de-black-boxed} by
the exact identity $F_A B \log m = m\,q_{\max}$ (the rank-local
normalization of $m\,q_{\max}$, no independent coordinate) and
distributionally bounded (r324-pre banked, envelope $C_F = 3.357$);
the $q_{\max}$-share route composed MEASURED (r324); the sliding
bound itself stays the certified theorem candidate on the measured
rungs.
\item \textbf{The vdC L2 lemma (r287, registered in wave 7).}
Contract \texttt{PRIME.PORT.L2.}\allowbreak
\texttt{VDC\_LEMMA.01}~\mO: prove the van der Corput bound of
Theorem~\ref{thm:vdc} at $H=\lceil\sqrt m\rceil$ as a \emph{chain}
statement (source-pure, window-independent) --- the remaining input is
the chain origin of the $P$-variance scaling; the inequality itself is
exact and world-blind valid (admissible: L2 is the generic half), and
the kz15 razor needs no new mechanism (exact-finite per r270).  State
after wave 9 (rounds r297--r300 consumed,
Section~\ref{sec:l2chain}): the remaining input stands \emph{reduced
to one inequality} --- the target level $\sigma^*$ is frozen
(Theorem~\ref{thm:sigmastar}), the transfer to the difference measure
is exact (Theorem~\ref{thm:transfer}), the diagonal decay is an exact
participation statement (Theorem~\ref{thm:participation}), the ratio
half is structurally settled (Theorem~\ref{thm:envelope}).  State
after wave 10 (rounds r301--r304 consumed,
Section~\ref{sec:cascade-closure}): the reduction route is
\emph{documented stopped} --- the cascade is retyped as an exact
dictionary around one measured core (Theorem~\ref{thm:margininv})
and the global-profile mixing route is closed
(Remark~\ref{rem:lanestop}); the contract stays \mO\ at the stop
state.
\item \textbf{The NEFF target (r300, registered in wave 9; retyped
in wave 10).}  Contract \texttt{PRIME.L2.}\allowbreak
\texttt{NEFF\_TARGET.01}~\mO: registered as
$\mathrm{slope}(n_{\rm eff}) \ge +0.908$ for the participation
number of the aggregated difference profile (measured $+0.963$,
margin $0.055$; exactly equivalent to $\mathrm{sl}_D \le \sigma^*$).
State after wave 10 (rounds r301--r304 consumed,
Section~\ref{sec:cascade-closure}): the demand is \emph{retyped to
the documented stop state} --- the r303 regress audit shows
NEFF/UNIF/ATOM are one algebraic slack $S = \sigma^* - \mathrm{sl}_D
= +0.0547$ (Theorem~\ref{thm:margininv}); the honest state is L2
generic $\Leftrightarrow$ anti-concentration of the explicit $dc$
block field; the global-profile mixing route is closed (period-4
comb, \textsc{law\_longrange}, Open problem~\ref{op:comb}); the
mechanism is two-scale (Remark~\ref{rem:twoscale}); return later
with new tools --- per the hard reviewer rule a round counts only
with new information, no further coordinate change counts.
\item \textbf{The global metric firewall (r276/r278 follow-up).}
Contract \texttt{PRIME.PORT.WALL.}\allowbreak
\texttt{METRIC\_FIREWALL.01}~\mO: the global form of the wall's metric
stability --- a wall lemma quantitatively stable under family/assignment
permutations at small dose while consuming the metric data exactly; the
certified state is graded-continuous with exact gradients and a
bottom-loaded u-profile, but perturbative-only ($\theta^*$ tiny, $L_D$
window-specific).
\item \textbf{The border-dressed resolvent question (r266, answered
negative).}  The exhaustive census found \emph{no identity in the class}
(Section~\ref{sec:nogos}); the campaign needs a genuine local RHP
asymptotic at the terminal degree --- with the important positive
by-product that no candidate reduces to the proven-false wall bound
(\textsc{wall\_detector\_armed}: target and wall stay measurably
different statements).
\item \textbf{The frame renormalization question (r267 follow-up).}
The external ceiling lives in a near-tight zero-atom frame
($R=4/3$); our comb frame is spread over $7$--$8$ decades
($R\approx73$--$126$).  Named question: does a source-pure renormalization
of the comb frame exist that trades atom-norm spread against certificate
class --- i.e.\ can any part of the bordered terminal scalar be moved into
a frame where an external-style rank--trace argument applies without
crossing the two-moment ceiling?  (Adjudicated \textsc{no\_import} for the
verbatim import; the renormalized variant is open.)
\end{itemize}

\subsection{The Gabor reduction chain (r541--r634L)}\label{sec:gabor}

The window-L* / compact Weil extraction of the preceding subsections
remains the program's live mincut (base~4 / refined~5; Level~C of
Remark~\ref{rem:threelevels}).  In parallel, rounds r541--r555 assembled a
\emph{conditional} Gabor-wavepacket reduction, formalized in
\texttt{RH/GaborSeparation.lean}.  The historical endpoint
\texttt{gabor\_inputs\_to\_mathlib\_rh} is a sorry-free logical wrapper:
the two named analytic inputs \texttt{GaborExplicitFormula} and
\texttt{GaborSeparationInequality} imply Mathlib's
\texttt{RiemannHypothesis}.  As of the r599/r600 vacuity audit that
wrapper is \textbf{VACUOUS}: it consumed positivity of
\texttt{gaborSpectralFormula}, which is not the Weil functional.
The live object is \texttt{gaborZeroSide}.  The implication is
conditional; it is not a proof of RH.  Round r612 deleted the
asserting theorem \texttt{gabor\_separationForAllZeros}; the Prop
\texttt{GaborSeparationForAllZeros} is retained unasserted
(\textsc{overspecified}/believed-false).  Separation stays
open (\mO).  \textbf{No unconditional claim.}

\paragraph{Proved bricks (sorry-free).}
Three analytic pieces are closed as Lean theorems and are not the
remaining mincut:
(i)~the Gaussian integral representation of the pure Gabor test
(\texttt{RH/GaborIntegral.lean}, r543);
(ii)~the unit-increment bound \texttt{GaborIncrementBound}, discharged by
\texttt{gaborIncrementBound\_holds} with explicit constant
$C=2\cdot\texttt{zetaZerosInDiskCardBoundInner}$
(\texttt{RH/ZeroIncrement.lean}, r546/r547);
(iii)~the Jacobi-theta majorant of unit-bin Gaussian maxima, together with
bin partial summation, giving a $\gamma$-uniform on-line Gaussian mass
bound $\le C\cdot\Theta_{\mathrm{lobe}}$
(\texttt{RH/GaborThetaBound.lean}, theorems
\texttt{gauss\_online\_mass\_uniform} and companions, r552).
None of these three statements implies the uniform Gabor inequality.

\paragraph{Explicit formula for the Gabor class.}
The arithmetic landing site of \texttt{GaborExplicitFormula} is the
classical prime comb $2\Lambda(n)/\sqrt{n}$ (r550), not the compact
honest weight $\Lambda(n)(1+n^{-1})$ refuted for $\hat h_W$ at r548.
The identity is numerically confirmed on a sealed 36-cell battery
(r548, verdict \textsc{ef\_confirmed}, 36/36, residuals
$\sim10^{-15}$).  Round r581 discharged the Lean remainder:
\texttt{gabor\_vertical\_arithmetic\_remainder} is a theorem (no
\texttt{sorryAx}).

\paragraph{Quantifier distinction (binding).}
Round r541 (\textsc{separation\_go\_gabor}) is a \emph{certified numeric
precheck} against one synthetic functional-equation quadruple: $42/42$
cells through $\gamma=10^4$.  It is \emph{not} a discharge of
\texttt{gabor\_separationInequality}, and it is not a configuration-first
statement.  The configuration-first game --- $\forall$ increment-conforming
off-line configurations, $\exists$ a Gabor witness --- is a strictly
stronger quantifier.  Round r553 (\textsc{witness\_loses}) shows that
both the one-packet and the two-packet Gabor classes lose that game
under pure counting control (clusters of equally extreme quadruples).
Round r551 shows that extremal selection alone does not close the gap:
the equal-extremal tie factor is $e^{\pi^2/128}\approx 1.080$ against a
counting budget $141\times$ the available margin.  A spectral-gap
hypothesis $\sigma'/\sigma\le 0.9194$ would compress that counting
budget; it is not assumed here.  The live attack is mixed-sign /
adaptive-nulling witnesses (r554).  Reading r541 as a completed
separation \texttt{sorry} is a category error.

All Gabor probes remain sealed in \texttt{rh/INVENTORY.json}; the RH
suite is green.  Nothing in this subsection is evidence for or against
RH.  \textbf{No RH claim.}

\paragraph{Vacuity audit (r599/r600, 2026-09-01/02).}
\texttt{gaborSpectralFormula} is \emph{not} the Weil functional: it
subtracts the pole term $\hat h(1)$.  For $F_0=\texttt{pureGaborTest}\,1\,0$
the zero-side sum is $\approx 3\cdot 10^{-43}$ while
$\mathrm{Re}\,\hat h(F_0,1)=\pi e^{1/8}\approx 3.5599$, so
\texttt{gaborSpectralFormula}\,$F_0\approx -3.56$ unconditionally.
The old hypothesis $\mathrm{hpos}:=\forall F\text{ admissible},\ 0\le
\texttt{gaborSpectralFormula}\,F$ is therefore \textbf{false} (not merely
unproven), and every Mathlib endpoint that consumed it was vacuous.
The live object is \texttt{gaborZeroSide} (spectral sum without pole
subtraction); $\mathrm{hpos}^*:=\forall F\text{ admissible},\ 0\le
\texttt{gaborZeroSide}\,F$ is the RH-core.  Through r611 this was
equivalently the asserting \texttt{sorry}
\texttt{gabor\_separationForAllZeros}; r612 deleted that theorem and
retained the Prop unasserted.
\texttt{GaborSpectralToArithmetic} is a proved theorem
(\texttt{GaborSpectralToArithmetic\_holds}): the pole-sign lemma
$\hat h(1)\ge 0$, \emph{not} the RH-core.
Round r605A records that
\texttt{gabor\_zeroSide\_pure\_criterion\_iff\_rh} takes a separator
premise \texttt{hsep} (\texttt{GaborZeroSideForAllZeros}, prescribed
packet \texttt{scalingGaborTest}$(a=\sigma^2/64,\omega=\gamma-\pi a/\sigma)$)
and is \textsc{forall\_zero\_overspecified}.
The live unconditional statement is
\texttt{gabor\_zeroSide\_pure\_criterion\_iff\_rh\_unconditional}
(Section~\ref{sec:exposed-orbit}).
For the full quartic family the iff still carries \texttt{hline}.
Old endpoints kept as a VACUOUS (r600) audit trail:
\texttt{GaborSeparatesOffCriticalZeros},
\texttt{gabor\_separation\_of\_inputs},
\texttt{gabor\_criterion\_to\_mathlib\_rh},
\texttt{gabor\_inputs\_to\_mathlib\_rh},
\texttt{gabor\_separation\_of\_arithmetic\_separators},
\texttt{gabor\_arithmetic\_inputs\_to\_mathlib\_rh},
\texttt{gabor\_window\_adaptive\_inputs\_to\_mathlib\_rh}.
Live replacements:
\texttt{GaborZeroSideSeparatesOffCriticalZeros},
\texttt{gabor\_zeroSide\_inputs\_to\_mathlib\_rh},
\texttt{gabor\_arithmetic\_zeroSide\_inputs\_to\_mathlib\_rh},
\texttt{gabor\_window\_adaptive\_zeroSide\_inputs\_to\_mathlib\_rh}.
Named open (no claim): a rigorous tsum bound $Z(F_0)<\pi e^{1/8}$ for an
unconditional spectral refutation; on-line positivity for general even
quartics (beyond $\langle 1,0,0\rangle$) so the iff holds without
\texttt{hline}.  Tactic-sorry census stayed $8$ through r605; $8\to 7$ at r612; no \texttt{sorryAx}.
\textbf{No RH claim.}

\paragraph{Numerical evidence (experiments only, no RH claim).}
Round r601 (probe \texttt{gabor\_zeroside\_dominance}, SHA
\texttt{b7089c71}) returns \textsc{dominance\_fails}: $132$ cells, explicit-formula
sanity $\le 1.4\cdot 10^{-15}$ with $2000$ zeros, no negative cell.
Positivity is certified only for $\omega\ge\omega_0(a)$
($\omega_0\approx 500$ at $a=0.5$, down to $10$ at $a\ge 3$); no $a\le 5$
is $\omega$-uniform.  The cancellation exponent
$e^{(1/8+d^2/2)/a}$ near zeros is confirmed.  Conclusion: the Gabor family
with fixed $a$ is a Bombieri / Connes--Consani-type sub-family, not a
proof of $\mathrm{hpos}^*$.
Round r603 (probe \texttt{prime\_inequality\_evosearch}, SHA
\texttt{06d2ac9a}, $13/13$, byte-identical) is a two-key genetic search
over $\Lambda(n)$, $n\le 10^6$: mechanically green, discriminatively
empty.  The $1378$ grid-survivors are comparisons of convergent constants
on $\mathrm{Re}\,s=1$
($\lvert\mathrm{Sc}(1,2)\rvert\le\lvert\mathrm{Sc}(1,14.13)\rvert
\leftrightarrow 0.42\le 1.62$ via $-\mathrm{Re}\,\zeta'/\zeta(1+it)$)
or grid artefacts.  Controls C1--C4 are all density-only: local jitter
$p^{0.6}$ moves $\psi$ by only $\sim 10^2$, and a $\beta=0.75$ fake zero
has amplitude $2100<6900$ (visible only from $X\approx 10^{10}$).
Lesson recorded: at accessible $X$, RH-level and $0.75$-level are
indistinguishable by proven-constant inequalities.
Round r604 (probe \texttt{tfpt\_spectrum\_zero\_crosscorr\_probe.py}, SHA
\texttt{748ae509}, $7/7$) returns \textsc{artifact}: TFPT-native spectra
(Moran-kernel complex dimensions, comb tones $\omega_1,\omega_2,\omega_3$,
E8-clock integers) versus the first $4000$ zeta ordinates.  Positive
controls $\log 2,3,5,7$ fire at $z\ge 5$; every candidate with $z\ge 5$
is a $\log p^k$ alias (the only raw hit $\omega_1+2\omega_2=\log 89$ dies
under unfolding).  Nearest-neighbour and clock-moduli tests are null.
Experiments only.  \textbf{No RH claim.}
Round r606 (probe \texttt{connes\_prolate\_residual\_gap\_probe.py},
SHA \texttt{aeef52a6}, SPEC \texttt{115c0409}; r606 first-run
$6$--$9$ mode, $t_{\mathrm{cut}}=150$) is
\textsc{inconclusive}: the CCM Poisson candidate $k_\lambda$ has
Davis--Kahan ratio $\eta=0.13/0.093/0.029$ at $L=0.3/0.4/0.5$, with
breakdown for $L\ge 0.6$ attributed to the uniform position grid
(small blocks fail at $L=0.8$; cf.\ r478); $\mu$ jumped to $5.9$
and $g<0$ at $L\ge 0.6$ (discretisation artefact).  Addendum r606b
(two runs byte-identical, $\sim 0.4\,\mathrm{s}$) returns
\textsc{inconclusive}(evals-not-converged, disc-disagree$>5$pct):
no $L$ meets successive-$N<1\%$ and Slepian/Fourier agreement
$<5\%$; the wall is discretisation (Slepian $\lambda_2$ pollution
beyond $N=12$--$24$, Fourier odd-mode $\sim 2\%$,
$t_{\mathrm{cut}}=1000$ blocked by spatial Nyquist), not a
GO/STOP on $\eta$.  The r606 $\mu\approx 5.9$ jump is gone
($k$ tracks $\lambda_1$); $\lambda_1^{e}$ slightly negative
($-3\cdot10^{-6}$--$-8\cdot10^{-6}$) at $L\ge 0.5$ but
unconverged --- not a Chuk contradiction; $\widehat\theta[0,30]$
NREAL $2,3,3,4,4,4$ vs Riemann count $3$ (plateaus at $4$).
Lane CLOSED --- superseded by r611
(\textsc{obs\_not\_converging}) and the
\textsc{killed}(factorization) typing of the prolate route;
diagnostic only.  Experiments only.  \textbf{No RH claim.}
Round r608 (probe \texttt{gabor\_tropical\_heat\_probe.py}, SHA
\texttt{574c1cc2}, $9/9$) returns \textsc{tropical\_confirmed}:
$2a\cdot\log\lvert Z\rvert\to F(\omega)$ with error
$\le 6.77\cdot a\cdot\log(1/a)$.  On-line,
$F=-\mathrm{dist}(\omega,\{\gamma\})^2\le 0$; a synthetic $\beta=0.9$
orbit gives $F=+\sigma^2=0.16$.  The hat obeys
$\partial_a\hat h=\tfrac12\partial_\omega^2\hat h-\hat h/(2a)$ on the
two Gaussian lobes only --- the cross lobe
$2e^{(\delta^2-\omega^2)/2a}$ carries residual $-\delta^2/(2a^2)$, so
$\sqrt{a}\,Z$ is \emph{not} a heat solution and a dyadic-in-$a$
reduction via the heat semigroup is unavailable for this family.
Source-side cancellation budget $B(a)=2.05/4.18/13.6$ at
$a=0.2/0.1/0.05$, i.e.\ $B\sim e^{1/(8a)}$: deriving $F\le 0$ from the
prime side needs cancellation to relative precision
$e^{-(1/8+\lvert F\rvert/2)/a}$ with primes up to $n=e^{1/(2a)}$ --- the
quantitative form of the NOGO-T1 wall.  Consequence: the HEAT.SOURCE
lane is a NOGO T1 (RH-strength cancellation) and is STOP.
Experiments only.  \textbf{No RH claim.}
Round r609 (probe \texttt{e8\_directed\_readout\_probe.py}, SHA
\texttt{9ab0a638}, SPEC \texttt{3edfb824}, $49/49$) returns
\textsc{e8\_readout\_sealed}($7/8$): Seifert $S+S^T=A$,
$\mathrm{charpoly}(-S^{-1}S^T)=\Phi_{30}$, $\mathrm{rank}(S-S^T)=8$;
$H_8$ weight enumerator $1+14y^4+y^8$; $240=16+14\cdot16$; Steiner
$S(3,4,8)$; $\mathrm{srg}(120,56,28,24)$ eigenvalues $56/8/-4$ with
multiplicities $1/35/84$; $N(n)=240\sigma_3(n)$ for $n\le10$;
$\tau(n)$ for $n\le8$ ($A_P(1)=1658880$); $E_4^3-E_6^2=1728\Delta$;
$(\mathbb{Z}/30)^\times\cong C_2\times C_4\not\cong C_2^3$, character
table $=H_2\otimes F_4$; $E_6\oplus A_2$ index $3$, Smith
$(1^6,3,3)$, roots $72+6+162$.  C7 was \textsc{not\_tested} at probe
time and was later supplied in \texttt{note\_e8\_gaussian\_code.tex}
(explicit $8\times8=H_2\otimes F_4$, permutation
$[0,1,4,3,5,2,7,6]$); the Gauss-code transform remains undefined
(open remark \texttt{rem:c7-audit}).  TFPT-side exact structure; no
prime bridge is shown; promotion to \texttt{verification/} is a
pending user decision.  Experiments only.  \textbf{No RH claim.}
Round r611 is recorded in Section~\ref{sec:nogos}
(\textsc{obs\_not\_converging}; Herglotz/Aubin--Nitsche STOP).
Round r613 is recorded in Section~\ref{sec:nogos}
(\textsc{no\_identification}; ordered-E8 canonical system dead).
These are sealed experiments (r606/r606b CLOSED as a diagnostic
on the same file; superseded by r611), not verification modules.

\noindent Round r615 (probe \texttt{semilocal\_firststep\_probe.py},
SHA \texttt{3a0741de}, SPEC \texttt{04f9c9ec}, $26/26$) returns
\textsc{firststep\_leakage\_controlled}.  The Weil window form with
exactly one prime ($L\in(0.3466,0.5493)$,
$w_2=\sqrt{2}\log 2=0.98026$) has Ritz $\lambda^*(L)$ equal to
zero-side leakage to $10^{-4}$--$10^{-5}$ at all tested $L$; the
extremal vector is Slepian-like (overlap with $\psi_0\ge 0.9989$,
$\ge 99.8\%$ spectral mass below $\gamma_1=14.1347$) with no
character change at the prime entry.
$\lambda^*(0.30)=7.57\cdot10^{-3}$ (v1017 padded floor
$2.1\cdot10^{-3}$, factor $3.6$),
$\lambda^*(0.40)=1.85\cdot10^{-4}$,
$\lambda^*(0.549)=5.80\cdot10^{-8}$ with POLE $+1.30$ / ARCH $-1.29$ /
PRIME $1.78\cdot10^{-2}$ (cancellation depth $2.25\cdot10^{7}$).
Pinning at $L=0.52$: $\delta_{\mathrm{crit}}=2.5\cdot10^{-5}$
relative on $w_2$, $\varepsilon_{\mathrm{crit}}=+4.1\cdot10^{-5}/-1.7\cdot10^{-6}$
on the atom position, WPERM $-0.135$, SCRAMBLE $-0.33/-0.18/-0.047$.
Mechanism (T9): the minimizer of the fictitious prime-free form
POLE$+$ARCH is purely odd, with
$\mathrm{POLE}=-2\langle h,\sinh(u/2)\rangle^2<0$ and $\mathrm{ARCH}<0$,
and $g(\log 2)<0$, so $-w_2 g(\log 2)$ carries the positivity
($L=0.40$: $-0.018-0.060+0.130=+0.053$).  The odd sector turns
negative at $L\approx 0.372$; prime $2$ enters at $0.3466$
(margin $\Delta L\approx 0.025$).  Named open lemma
\texttt{PRIME.SEMILOCAL.ODDSECTOR.EDGE.01}~\mO, retyped at r620
to the oriented form: for odd $h$ on $[-L,L]$,
$\langle h,(\mathrm{POLE}+\mathrm{ARCH})h\rangle
+w_2(\lVert f_+\rVert^2-\lVert f_-\rVert^2)\ge 0$;
the negative direction of $\mathrm{POLE}+\mathrm{ARCH}$ migrates
from the inner region to the $R$-symmetric edge sector; the
antisymmetric edge sector is safe without the prime.  The
repair is a quantitative dominance with margin $\lambda^*(L)$,
not an exact sector identity.
Fence: window forms with $L\lesssim 16$ cannot detect off-line zeros
(r605 scaling $a=\sigma^2/64$ needs $u$-support $\sim 8/\sigma$), so
the first prime step is far weaker than RH.  Experiments only.
\textbf{No RH claim.}

\noindent Round r617 (probe \texttt{e8\_coxeter\_euler\_completion\_probe.py},
SHA \texttt{6ac6bab2}, SPEC \texttt{5aa3935b}, $45/45$) returns
\textsc{completion\_exact}.
$\mathrm{Tr}\,C=-1$; the $\Phi_{30}$ M\"obius identity holds;
$(1-x)\Phi_{30}=1-x^2-x^3+x^6+x^7-x^9$;
$D_{E8}(s)=\zeta(6s)\zeta(10s)\zeta(15s)/[\zeta(2s)\zeta(3s)\zeta(5s)\zeta(30s)]$
has residuals at $X=10^6$ of $1.28\cdot10^{-4}$ ($s=0.75$) and
$3.5\cdot10^{-14}$ ($1.5$); $\det_2$ scalar control
$e^{P(s)}/\zeta(s)$ residual $1.75\cdot10^{-14}$.  Class statement:
scalar $-1$ gives $A_1\to 1/\zeta(2s)$, $A_2\to 1/\zeta(3s)$,
$E_6\to\zeta(4s)\zeta(6s)/[\zeta(2s)\zeta(3s)\zeta(12s)]$; negative
control $B_2$ (trace $0$) gives $\zeta(2s)/[\zeta(s)\zeta(4s)]$,
divergent at $0.75$.  The identity is Beurling-generic.  Typed no-go
\texttt{E8.COXETER.REGULARIZED\_SPLIT.NO\_GO.01} is recorded in
Section~\ref{sec:nogos}.  Fence: ``The trace-free completion is
zero-free and pole-free in $\mathrm{Re}\,s>1/2$. The splitting into
the scalar zeta channel and the Coxeter channel is open and
RH-equivalent. No RH claim.''  Promotion to \texttt{verification/}
(v1018/v1019) is in progress.  Experiments only.

\noindent Round r618 (probe \texttt{jensen\_compiler\_rigidity\_probe.py},
SHA \texttt{29ccf1c7}, SPEC \texttt{bc8d8624}, $43/43$) returns
\textsc{compilers\_structurally\_rigid}.  Data:
$a_0=\xi(1/2)=0.497120778$, Tur\'an inequalities for $n\le 12$,
$J_{n,d}$ hyperbolic for $d\le 8$, $n\le 4$ (consistent with
GORZ 2019; nothing new).  The two structural no-gos are recorded in
Section~\ref{sec:nogos}.  Fence: both compilers are generic
real-rootedness certificates; existence for all $(n,d)$ is equivalent
to RH; the E8 data are RH-neutral.  Experiments only.
\textbf{No RH claim.}

\noindent Round r619 (probe \texttt{support\_relay\_census\_probe.py},
SHA \texttt{fcfa7771}, SPEC \texttt{7299737d}, RESULT
\texttt{e0379485}, $12/12$) returns \textsc{relay\_universal}.
Prime powers $q\le 32$ ($18$ events), entry
$L_j=\tfrac12\log q$.  Every ancestor form $Q_{j-1}$ (without
the newest event) fails a finite lead after entry:
\begin{center}\small
\begin{tabular}{@{}rccccccccc@{}}
\toprule
$q$ & 2 & 3 & 4 & 5 & 7 & 8 & 9 & 11 & 13\\
$\Delta_j$ & 0.0279 & 0.0123 & 0.0146 & 0.0111 & 0.0105 & 0.0140 & 0.0125 & 0.0116 & 0.0131\\
\midrule
$q$ & 16 & 17 & 19 & 23 & 25 & 27 & 29 & 31 & 32\\
$\Delta_j$ & 0.0166 & 0.0126 & 0.0144 & 0.0141 & 0.0170 & 0.0181 & 0.0151 & 0.0147 & 0.0221\\
\bottomrule
\end{tabular}
\end{center}
$\Delta_j=0.011$--$0.028$ (nearly constant).  The failing mode
is always odd, reflection-symmetric edge sector w.r.t.\ the
newest event ($g<0$), edge mass $0.07$--$0.65$; the newest
weight restores positivity (all $18$).  $\lambda^*$ decay
slopes per interval $-50/-77/-79$ at fixed $N=80$
(global $-68.6$; Slepian $-28.3$; r615 $-44.8$) --- the
$-77/-79$ values at $L=0.9$--$1.4$ are $N$-artifacts
(corrected by r630b: trustworthy slopes $-50.6$ on
$0.3$--$0.6$ and $-116$ on $0.6$--$1.0$;
$\mathrm{RELAY\_UNIVERSAL}$, leads $\Delta\approx 0.015$,
and $L_{\mathrm{det}}$ unaffected) --- super-exponential
(Landau--Widom plunge); $\lambda^*$ underflows the SVD range
beyond $L\approx 1.4$.  Controls: SCRAMBLE first failure $0.376$,
WPERM $0.402$, EPSTEIN ($2^k$ only) $0.564\approx L_3+0.015$
--- each control breaks at entry+lead.
\begin{center}\small
\begin{tabular}{@{}lc@{}}
\toprule
$(\beta,\gamma)$ & $L_{\mathrm{det}}$\\
\midrule
$(0.6,20)$ & $0.55$\\
$(0.75,20)$ & $0.55$\\
$(0.6,50)$ & $0.80$\\
$(0.9,20)$ & $0.50$\\
\bottomrule
\end{tabular}
\end{center}
\textbf{Parent typing.}  The \textsc{relay\_universal} criteria
($\Delta_j\ge 0$, $g<0$, repair) are consequences of
$Q_j\succeq 0$ (RH-consistency), not a mechanism.  The
informative results are the constant leads (``greedy
consumption'': each prime power becomes load-bearing within
$\Delta L\approx 0.015$), the control breaks at entry+lead,
the super-exponential decay, and $L_{\mathrm{det}}$ --- which
shows that windows with $L\gtrsim 0.5$ are hypersensitive
off-line detectors (a nulled zero displaced by $\sigma$
contributes $-\sigma^2\lvert\partial\hat f\rvert^2$, beating
the tiny $\lambda^*$), hence relay steps beyond $L\approx 0.5$
are RH-in-a-box statements, not elementary finite problems.
Fence: window positivity for the scanned $L$ is RH-consistent.
Experiments only.  \textbf{No RH claim.}

\noindent Round r620 (probe \texttt{p2\_reflection\_factor\_probe.py},
SHA \texttt{194fe9e5}, SPEC \texttt{7c8e73b7}, $73/73$) returns
\textsc{mixed\_sector}.  Exact identity for odd $h$,
$d=\log 2$, overlap $I=[d-L,L]$, reflection
$(Rf)(x)=f(d-x)$:
$g_h(d)=-(\lVert f_+\rVert^2-\lVert f_-\rVert^2)$; the prime
term $-w_2 g=w_2(\lVert f_+\rVert^2-\lVert f_-\rVert^2)$
stabilizes the symmetric edge sector and destabilizes the
antisymmetric one.  $\dim E_-(A^{\mathrm{odd}})=1$ for
$L\ge 0.38$ ($\lambda_{\min}-2.4\cdot 10^{-2}$ at $0.38$ to
$-0.43$ at $0.549$).  Mass of the negative vector
inner$/+$/$-$: $0.85/0.15/0.005$ ($0.38$), $0.71/0.28/0.01$
($0.40$), $0.50/0.48/0.02$ ($0.43$), $0.06/0.94/0.001$
($0.549$) --- migrates from inner to $R$-symmetric edge.  The
antisymmetric sector is safe without the prime
($\lambda_{\min}(A|_{P_-})-w_2=+5.7\to +0.29$); cross term
$0.12$--$0.28$.  Oriented inequality
$\lambda_{\min}(A^{\mathrm{odd}}+w_2(P_+-P_-))=\lambda^*(L)\ge 0$
everywhere.  The contract
\texttt{PRIME.SEMILOCAL.ODDSECTOR.EDGE.01}~\mO is retyped to
this oriented statement; the repair is a quantitative
dominance with margin $\lambda^*(L)$, not an exact sector
identity.  Experiments only.  \textbf{No RH claim.}

\noindent Round r621 (probe \texttt{p2\_digamma\_duplication\_probe.py},
SHA \texttt{dff2e1b3}, SPEC \texttt{78f47c87}, $37/37$) returns
\textsc{duplication\_constant\_only} (recorded as a no-go in
Section~\ref{sec:nogos}).  Experiments only.
\textbf{No RH claim.}

\noindent Round r622 (probe \texttt{support\_darboux\_probe.py},
SHA \texttt{845bb937}, SPEC \texttt{27ffd017}, PAYLOAD
\texttt{e1031d17}, $64/64$) returns
\textsc{transport\_feasible\_norule} (recorded as a no-go in
Section~\ref{sec:nogos}).  The transport formulation relocates
the tiny margins into the scalar $c_0(L)$ (relative form of
the wall); no source-side rule.  Experiments only.
\textbf{No RH claim.}

\noindent Round r623 (probe \texttt{semilocal\_p2\_dilation\_probe.py},
SHA \texttt{d1bec728}, SPEC \texttt{5890676d}, $128/128$) returns
\textsc{p2\_dilation\_structurally\_blocked} (recorded as a no-go
in Section~\ref{sec:nogos}).  The $2$-adic identity
$m_2(t)=\log 2\cdot(1-P_r(t\log 2))$ holds to
$4.9\cdot10^{-12}$; the proposed PSD dilation is obstructed at
the principal block, at $M_2$, and as a tautology of
$\lambda^*(L)$.  The lane is not opened.  Experiments only.
\textbf{No RH claim.}

\noindent Round r625 (probe \texttt{iiks\_vanishing\_metric\_probe.py},
SHA \texttt{973a1ce2}, SPEC \texttt{7a4257e3}, NUM
\texttt{2d066e2d}, $19/19$) returns
\textsc{iiks\_no\_metric\_in\_class} (recorded as a no-go in
Section~\ref{sec:nogos}; prose
\textsc{not\_testable\_in\_corpus\_form}).  Zhou's vanishing
lemma is not testable on the corpus discrete IIKS/CD object:
the jump is not the RHP jump equivalent to $\tau(s)\ne0$, and
no metric in the source-explicit class is contractive.
Experiments only.  \textbf{No RH claim.}

\noindent Round r626 (probe \texttt{xi\_finitefree\_collision\_probe.py},
SHA \texttt{c4750063}, SPEC \texttt{c7cef974}, NUM
\texttt{f533d168}, $51/51$) returns
\textsc{collision\_detector\_calibrated\_no\_barrier}
(recorded as a no-go in Section~\ref{sec:nogos}).  The
collision barrier $\int\Phi\,dt<\infty$ is equivalent to
$\mathrm{Disc}\ne0$ along the path; Taylor sections of $\Xi$
are not real-rooted at $t=0$; the detector is calibrated on a
hyperbolic control.  Experiments only.  \textbf{No RH claim.}

\noindent Round r627 (probe \texttt{af\_twomoment\_optimizer\_probe.py},
SHA \texttt{da1a9118}, SPEC \texttt{9b95f564}, $17/17$) returns
\textsc{no\_gain} (recorded as a no-go in
Section~\ref{sec:nogos}).  Window optimum in the A--F two-moment
class is Montgomery--Taylor $p^*=0.6725007037$; the proved ceiling
$0.6818287$ is a richer certificate, not a window bound.  Gap
$9.3\cdot10^{-3}$ not closable by window optimization.
Experiments only.  \textbf{No RH claim.}

\noindent Round r628 (probe \texttt{window\_box\_verifier\_probe.py},
SHA \texttt{9c99ed6a}, SPEC \texttt{36c0e66a}, NUM
\texttt{84aaf685}, $12/12$) returns
\textsc{compression\_certified\_box\_numerical}
(recorded as a no-go in Section~\ref{sec:nogos}).  Finite-window
positivity is interval-certified; a rigorous zero-free box is
blocked by Trudgian $\lvert S(T)\rvert$ looseness
($\sim 10^{7}$).  Experiments only.  \textbf{No RH claim.}

\noindent Round r629 (probe \texttt{certificate\_class\_atlas\_probe.py},
SHA \texttt{4dae1fbe}, SPEC \texttt{0bf7c64f}, $15/15$) returns
\textsc{atlas\_no\_new\_unconditional\_gain}
(recorded as a no-go in Section~\ref{sec:nogos}).  Unconditional
rows do not improve on $p_0=0.6818287$; only pair-correlation
input beyond support $1$ buys proportion.  Experiments only.
\textbf{No RH claim.}

\noindent Round r632 (probe \texttt{jet\_deflated\_ldet\_probe.py},
SHA \texttt{63ecb68f}, SPEC \texttt{31a29760}, RESULT
\texttt{6299cedc}, $14/14$) returns \textsc{jet\_gain\_small}
(recorded as a no-go in Section~\ref{sec:nogos}).  Local
$L_{\mathrm{det}}$ gain collapses under coverage
($\dim K/N$ above Nyquist).  Experiments only.
\textbf{No RH claim.}

\noindent Round r630 (probe \texttt{margin\_law\_symreg\_probe.py},
SHA \texttt{5b3d1125}, SPEC \texttt{87febb97}, RESULT
\texttt{a0765d7f}, $19/19$) returns
\textsc{margin\_law\_unresolved} with r630b addendum
\textsc{n\_limited\_artifact} (recorded as a no-go in
Section~\ref{sec:nogos}).  Converged log-slopes $-50.6$
($0.3$--$0.6$) and $-116$ ($0.6$--$1.0$); the margin law is
density-driven from the unconditional $N(T)$ profile
(RH-neutral); the dBN gap functional $B_k$ is not a
universal barrier (fraction $B_k<2=0.470$).  Experiments
only.  \textbf{No RH claim.}

\noindent Round r633 (probe \texttt{frontier\_followups\_probe.py},
SHA \texttt{9f5287e7}, SPEC \texttt{ee7c7ba7}, RESULT
\texttt{814abdf5}, $16/16$) returns four follow-up verdicts
(recorded as a no-go in Section~\ref{sec:nogos}).
\textsc{random\_euler\_refuted} is a measured class no-go
(magnitude-only and Diophantine-approximation arguments
dead); $a$-compactification is cosmetic (the RH-relevant
end $a\to 0$ stays unbounded); forward Loewner is a
detector, not a lever; $\Delta\approx 0.015$ is not a
corollary of the fitted margin/overlap laws.  Experiments
only.  \textbf{No RH claim.}

\noindent Round r631L (Lean-only, no probe).  Classification of
the seven tracked \texttt{sorry}s: R-class Canonical.lean:264
\texttt{lstar\_canonical}, :273 \texttt{terminal\_q\_canonical};
T-class but blocked Canonical.lean:396
\texttt{pair\_terminal\_dictionary}, Source.lean:1008
\texttt{mainWindow\_iff\_builtFromPrimeSource}; A-class
Elementwise.lean:1589 \texttt{arch\_gauss\_mellin\_digamma\_identity},
ExternalBridges.lean:15641
\texttt{standard\_explicit\_formula\_identification}, :15708
\texttt{fullWeil\_separates\_offCritical\_zeros}.  No closure.
Docstring fix: \texttt{GaborContourLimitRemainder} is proved
(\texttt{gaborContourLimitRemainder\_holds}, r576).  Census stays
$7$.  \textbf{No RH claim.}

\noindent Round r634L (Lean-only, no probe).
\texttt{gabor\_explicitFormula\_pure} is unconditional for
admissible pure $F$;
\texttt{gabor\_primeSide\_rational\_criterion\_iff\_rh} is the
prime-side rational $\Pi_1$ form of RH (axioms
\texttt{propext}/\texttt{Classical.choice}/\texttt{Quot.sound}).
Remaining EF brick: \texttt{GaborHatQuarticExplicitRemainder}
(non-pure quartics only).  Census stays $7$.
\textbf{No RH claim.}

\paragraph{Two-key filter (externalization package).}
A proposed route counts as RH-relevant only if it (key~1) fails under
prime-position scrambling that preserves densities and (key~2) fails when
the functional equation / an off-line zero quadruple is injected.
Round r565 showed the Gabor dominance margin is scramble-invariant
(key~1 fails $\Rightarrow$ it is zero-geometry, not arithmetic).

\paragraph{Outlook (2026).}
arXiv:2608.13637 (Aug.~2026, Lean-verified,
\texttt{github.com/anthropics/zeta-23-lean}, Apache-2.0) obtains that at
least $2/3$ of the zeros are simple and on the line ($0.6725$ with a
Montgomery--Taylor window) via a rank--trace inequality plus Sylvester
inertia on a finite compression of Weil's Hermitian form; the prime side
is the Montgomery--Vaughan diagonal only; \S7.2 proves a
``bandwidth-one ceiling'' $\approx 0.6819$, beyond which prime-pair
information (Hardy--Littlewood strength, Fourier support $>1$) or higher
trace moments in the Rudnick--Sarnak range are required.
arXiv:2602.04022 (Connes, Feb.~2026) together with Connes--Consani--Moscovici
``Zeta spectral triples'' (EMS 2026) show that for each cutoff $c$ the
even ground state of the truncated Weil form has Mellin zeros on the
critical line (Connes--van Suijlekom); convergence to $\zeta$-zeros is
the open RH-core.
arXiv:2608.24827 (Aug.~2026) gives unconditional window positivity
$Q(f)\ge 8.9\cdot 10^{-18}\|f\|^2$ for support $1.6$, with
$-\ln\lambda^*(L)\simeq 2\pi^2 N(T^*)/\ln N(T^*)$,
$T^*=2\pi e^{2L}$; pointwise certificates need frequencies up to
$2\pi\exp(4e^L)$ (a doubly exponential barrier).
Our reading: all three meet the same wall as our r565/r603 findings ---
local (Euler-factor) information is RH-blind; progress beyond the ceiling
needs prime-pair (support $>1$) control.
\textbf{No RH claim.}

\subsection{The exposed-orbit separator (r605)}\label{sec:exposed-orbit}

Round r605A: \texttt{GaborZeroSideForAllZeros} is the prescribed-packet
pointwise form $\forall\rho$ with
\texttt{scalingGaborTest}$(a=\sigma^2/64,\ \omega=\gamma-\pi a/\sigma)$
and is \textsc{forall\_zero\_overspecified};
\texttt{gabor\_zeroSide\_pure\_criterion\_iff\_rh} takes \texttt{hsep}
as a premise.

Round r605N (probe \texttt{gabor\_exposed\_orbit\_probe.py},
SHA \texttt{c99998c4}, SPEC \texttt{1a4bf949}, $40/40$, byte-identical)
returns \textsc{exposed\_orbit\_holds}: $9/9$ configs negative at $n=0$,
normalised limit $-4m(\rho^*)$ exact, tail bound holds.

Rounds r605B+C add \texttt{RH/GaborExposedOrbit.lean} ($1734$ lines,
sorry-free).  The module supplies \texttt{gaborScore}, the
\texttt{ExposedOrbit} structure, \texttt{exists\_exposedOrbit\_of\_not\_rh},
phase lock $\cos=-1$, the three-lobe majorant
$\mathrm{Re}\,\hat h(\rho^*)\le -(\pi/4a)\,e^{M/2a}+(3\pi/4a)\,e^{(M-\Delta)/2a}$,
\texttt{gaborExposedOrbitAssembly\_holds}, and the endpoints
\texttt{exists\_negative\_pureGabor\_of\_not\_rh} (\texttt{hasm}),
\texttt{gabor\_zeroSide\_pure\_criterion\_iff\_rh\_of\_assembly}
(\texttt{hasm}), and
\[
\bigl(\forall a,\omega\ (a>0):\ 
0\le\texttt{gaborZeroSide}(\texttt{pureGaborTest}\,a\,\omega)\bigr)
\;\Longleftrightarrow\;
\texttt{RiemannHypothesis}
\]
(\texttt{gabor\_zeroSide\_pure\_criterion\_iff\_rh\_unconditional};
$\#$print axioms $=\{\texttt{propext},\ \texttt{Classical.choice},\ \texttt{Quot.sound}\}$).
Tactic-sorry census at this landing $8$ (Canonical $3$, Source $1$,
Elementwise $1$, GaborSeparation $1$, ExternalBridges $2$);
live census $7$ after r612 (GaborSeparation $0$).
Independent audit r605C-AUDIT: \textsc{statement\_sound}
(\texttt{gaborZeroSide} is $\mathrm{Re}\sum'$ over
\texttt{IsCriticalStripZetaZero} weighted by
\texttt{riemannZetaMultiplicity}; summable for every pure packet by
\texttt{gaborMultiplicityWeightedHatSummable}; $\leftarrow$ is on-line
positivity $\hat h(\tfrac12+it)=$ a real square $\ge 0$; every
\texttt{ExposedOrbit} field is discharged from $\neg$RH).

\textbf{Mandatory wording.}
RH $\Longleftrightarrow$ nonnegativity of the pure Gabor zero-side on
the two-parameter family $(a,\omega)$.  The sharpest live formal
statement (r634L) is the prime-side rational criterion
\texttt{gabor\_primeSide\_rational\_criterion\_iff\_rh}:
RH $\Longleftrightarrow$ $\forall$ rational $(a,\omega)$ with $a>0$,
$\mathrm{POLE}-\mathrm{PRIME}+\mathrm{ARCH}\ge 0$ on
$\texttt{pureGaborTest}\,a\,\omega$.
Positivity of \texttt{gaborZeroSide} itself remains open.
This is not an RH proof.  The Weil test space is reduced to a
two-parameter pure Gabor family.
\textbf{No RH claim.}

\paragraph{Low-height zero-free and spectral refutation (r610).}
Lean-only, in \texttt{RH/GaborSeparation.lean}.
\texttt{GaborLowHeightZeroFree} is an unasserted Prop at $T_0=4$
(classically true: the first zero is $14.1347$, not in Mathlib).
The pole term is exactly $\pi e^{1/8}$
(\texttt{gaborHat\_pure\_one\_zero}); the zero side is $\le 19/20$ of
it, and \texttt{gaborSpectralFormula\_refuted\_of\_lowHeight} is a
theorem.  $\#$print axioms
$=\{\texttt{propext},\ \texttt{Classical.choice},\ \texttt{Quot.sound}\}$.
Conditional/finite only.  \textbf{No RH claim.}

\paragraph{Countable rational-packet criterion (r612/r612C).}
The asserting theorem \texttt{gabor\_separationForAllZeros} is deleted.
The Prop \texttt{GaborSeparationForAllZeros} is retained unasserted with
an OVERSPECIFIED / believed-false docstring.
Module \texttt{RH/GaborCountableCriterion.lean} ($551$ lines,
imported in \texttt{RH.lean}).  Round r612C proves
\texttt{gaborZeroSideContinuous\_holds} (joint continuity of
$Z(a,\omega):=\texttt{gaborZeroSide}(\texttt{pureGaborTest}\,a\,\omega)$
on $\{a>0\}\times\mathbb{R}$ via a local uniform Gaussian majorant
$\lVert\hat h\rVert\le(\pi/a_0)\exp((1/8+\omega_1^2/2)/a_0)\exp(-t^2/(4a_1))$
and \texttt{continuousOn\_tsum}; summability via
\texttt{summable\_gauss\_over\_zeros}).  The rational-packet
criterion is now \emph{unconditional}:
\[
\texttt{gabor\_zeroSide\_rational\_criterion\_iff\_rh\_unconditional}
\;:\;
\bigl(\forall a,\omega\in\mathbb{Q},\; a>0:\ 
0\le\texttt{gaborZeroSide}(\texttt{pureGaborTest}\,a\,\omega)\bigr)
\;\Longleftrightarrow\;
\texttt{RiemannHypothesis}.
\]
$\#$print axioms
$=\{\texttt{propext},\ \texttt{Classical.choice},\ \texttt{Quot.sound}\}$.
Named-open list shrinks by one (\texttt{GaborZeroSideContinuous}
is proved).  Tracked sorry census stays $7$.  Module count
$53\to 54$ at r612.  Conditional/finite only in the sense that
positivity of \texttt{gaborZeroSide} itself remains open.
\textbf{No RH claim.}

\paragraph{Prime-side rational criterion (r634L).}
The pure-class explicit formula
\texttt{gabor\_explicitFormula\_pure} is now a theorem
(unconditional: both remainders are theorems).  Transporting the
r612C rational-packet criterion across that identity yields the
sharpest live $\Pi_1$ form
\texttt{gabor\_primeSide\_rational\_criterion\_iff\_rh}
($568$ lines in \texttt{RH/GaborCountableCriterion.lean}):
\[
\bigl(\forall a,\omega\in\mathbb{Q},\; a>0:\ 
0\le\texttt{gaborPoleSide}(\texttt{pureGaborTest}\,a\,\omega)
-\texttt{gaborPrimeComb}(\texttt{pureGaborTest}\,a\,\omega)
+\texttt{gaborArchSide}(\texttt{pureGaborTest}\,a\,\omega)\bigr)
\;\Longleftrightarrow\;
\texttt{RiemannHypothesis}.
\]
$\#$print axioms
$=\{\texttt{propext},\ \texttt{Classical.choice},\ \texttt{Quot.sound}\}$.
The remaining EF brick \texttt{GaborHatQuarticExplicitRemainder}
applies only to non-pure even quartics and is not used by the
criterion.  Positivity of \texttt{gaborZeroSide} itself remains
open.  This is not an RH proof.
\textbf{No RH claim.}

%======================================================================
\section{Lean 4 formalization status}\label{sec:lean}

Following the r267 recommendation (layer by provability), the workspace
\texttt{rh/lean} contains a Lean~4 project (toolchain v4.29.1, mathlib
pinned; builds green on the reference machine).  Status at the time of
writing: fifty-six modules under \texttt{rh/lean/RH/} (r617L:
$54\to 56$).  Tactic-sorry
census $7$ (Canonical~$3$, Source~$1$, Elementwise~$1$,
ExternalBridges~$2$; GaborSeparation~$0$); all other modules $0$.
Round r631L classifies the seven as
\textbf{R-class} (RH-core, Canonical.lean:264 \texttt{lstar\_canonical},
:273 \texttt{terminal\_q\_canonical} --- $L^*$-positivity / terminal
gate $q_N<1$; opaque \texttt{canonicalCompletion});
\textbf{T-class but blocked} (Canonical.lean:396
\texttt{pair\_terminal\_dictionary}, OP dictionary $Z=r_{N-1}$ missing;
Source.lean:1008 \texttt{mainWindow\_iff\_builtFromPrimeSource}, both
sides opaque);
\textbf{A-class} (Elementwise.lean:1589
\texttt{arch\_gauss\_mellin\_digamma\_identity};
ExternalBridges.lean:15641
\texttt{standard\_explicit\_formula\_identification}, comb
$2\Lambda/\sqrt{n}$ vs $\Lambda(1+n^{-1})$; :15708
\texttt{fullWeil\_separates\_offCritical\_zeros}, classical Weil
separator).  No closure at r631L.  The inventory below is the live census.  Historical status notes that
follow it retain their original wording and are not a second count.

\paragraph{Gabor explicit-formula brick chain (r555--r598).}
\begin{sloppypar}
The Gabor route's analytic spine is a sorry-free brick chain, recorded
module-by-module in the inventory.  In order: hat analyticity
(\texttt{GaborHatAnalytic}, r555); horizontal edges
(\texttt{GaborHorizontalEdges}, r557); right and left vertical
identification (\texttt{GaborVertical}, r564;
\texttt{GaborLeftVertical}, r570); fixed-$T$ contour residue
(\texttt{GaborContourResidue}, r557); arch contour shift and Digamma
fold (\texttt{GaborArchContour}/\texttt{GaborArchDigamma}, r577--r581);
autocorrelation closed form (\texttt{GaborAutocorrelation}, r572);
zero summability (\texttt{GaborZeroSummable}, r557);
functional-equation multiplicity (\texttt{GaborFEMultiplicity}, r582);
spectral bridge with on/off split and fundamental-domain exhaustion
(\texttt{GaborSpectralBridge}, r579--r589); dominance Log/Log2
(\texttt{GaborDominanceLog}, r575; \texttt{GaborDominanceLog2},
r583/r589); outer tail (\texttt{GaborOuterTail}, r593/r594); window
glue and anchored witness (\texttt{GaborWindowGlue}, r595;
\texttt{GaborAnchoredWitness}, r598).  Round r617L adds two
sorry-free modules outside this Gabor spine:
\texttt{CoxeterCompletion} ($\Phi_{30}$ identities and
$\mathrm{Tr}\,C=-1$) and \texttt{PrimeLogIndependence}
($\mathbb Q$-linear independence of $\{\log p\}$; Mathlib had no such
lemma).  Round r634L discharges the pure-class explicit formula
as a theorem: \texttt{gabor\_explicitFormula\_pure} states that for
every admissible pure $F$ (coeffs $\langle 1,0,0\rangle$)
\[
\texttt{gaborZeroSide}\,F
=\texttt{gaborPoleSide}\,F
-\texttt{gaborPrimeComb}\,F
+\texttt{gaborArchSide}\,F,
\]
unconditionally --- both named remainders
\texttt{gaborContourLimitRemainder\_holds} (r576) and
\texttt{gabor\_vertical\_arithmetic\_remainder} (r581, via
\texttt{gaborArchDigammaIdentification\_holds}) are theorems.
The implication wrappers
\texttt{gabor\_explicitFormula\_pure\_of\_remainders} and
\texttt{gabor\_explicitFormula\_of\_remainders} remain sorry-free.
The only remaining EF brick is
\texttt{GaborHatQuarticExplicitRemainder} (non-pure even quartics;
the prime-side criterion does not need it).
The sharpest live $\Pi_1$ statement is
\texttt{gabor\_primeSide\_rational\_criterion\_iff\_rh}:
$(\forall a,\omega\in\mathbb{Q},\; a>0:\ 
0\le\texttt{gaborPoleSide}-\texttt{gaborPrimeComb}+\texttt{gaborArchSide}
\text{ of }\texttt{pureGaborTest}\,a\,\omega)
\Longleftrightarrow\texttt{RiemannHypothesis}$,
axioms $\{\texttt{propext},\texttt{Classical.choice},\texttt{Quot.sound}\}$.
\textbf{No RH claim.}
\end{sloppypar}

\begingroup\small
\begin{longtable}{@{}
  >{\raggedright\arraybackslash}p{0.28\linewidth}
  >{\raggedright\arraybackslash}p{0.40\linewidth}
  >{\raggedright\arraybackslash}p{0.18\linewidth}
  c@{}}
\toprule
\textbf{Module} & \textbf{Role} & \textbf{Round(s)} & \texttt{sorry}\\
\midrule
\endfirsthead
\multicolumn{4}{c}{\small\itshape Complete Lean module inventory (continued)}\\
\toprule
\textbf{Module} & \textbf{Role} & \textbf{Round(s)} & \texttt{sorry}\\
\midrule
\endhead
\texttt{Audit} & axiom audit of the main theorem chain & C1 & 0\\
\texttt{Augmented} & $L^{\dagger}$ unification of $L^*$ and the terminal statement & r332 & 0\\
\texttt{Basic} & abstract coupled-tau chain data & r256--r259 & 0\\
\texttt{Canonical} & canonical final domain (three typed holes) & C1 / r326 & 3\\
\texttt{Closure} & reconstruction of master positivity from $L^*$ and terminal & r305 & 0\\
\texttt{Counterexamples} & permanent r273 reviewer refutation guards & r273 / r320 & 0\\
\texttt{CoxeterCompletion} & $\Phi_{30}$ identities and $\mathrm{Tr}\,C=-1$ & r617L & 0\\
\texttt{DualResolvent} & $L^{\dagger}\Leftrightarrow R^{\dagger}\succ\tfrac12 I$ as finite matrix identity & r362 / r430 & 0\\
\texttt{EdgeBalance} & edge-balance chain as finite real algebra & r417--r426 & 0\\
\texttt{Elementwise} & elementwise extraction without a mesh ladder & r326 & 1\\
\texttt{ExternalBridges} & typed bridges from native class to Mathlib RH & r463 & 2\\
\texttt{FaithfulFold} & production-window transcription (source to grid fold) & r459 / r463 & 0\\
\texttt{FlankEntry} & flank-entry lemma against the pivot layer & r384 & 0\\
\texttt{FrequentlySelected} & semidefinite $+$ infinitely-often extraction & r430 & 0\\
\texttt{GaborAnchoredWitness} & anchored singleton fragment and tail modulus & r598 & 0\\
\texttt{GaborArchContour} & $\chi'/\chi$ contour shift onto the critical line & r578 / r581 & 0\\
\texttt{GaborArchDigamma} & left-edge Digamma identification of $\chi'/\chi$ & r577 / r581 & 0\\
\texttt{GaborArithmeticSeparator} & existential arithmetic Gabor separator & r590 / r600 & 0\\
\texttt{GaborAutocorrelation} & closed form of the pure Gabor ACF & r572 & 0\\
\texttt{GaborContourResidue} & fixed-$T$ residue identity for $(\zeta'/\zeta)\hat h$ & r557 & 0\\
\texttt{GaborCountableCriterion} & rational-packet zero-side iff RH (unconditional, r612C); prime-side iff r634L; $568$ lines & r612/r612C/r634L & 0\\
\texttt{GaborDominance} & canonical isolation-shrink dominance & r569 & 0\\
\texttt{GaborDominanceAssembly} & packing and remainder-budget assembly & r571 & 0\\
\texttt{GaborDominanceLog} & counting-theorem (log occupancy) dominance & r575 & 0\\
\texttt{GaborDominanceLog2} & log-compatible remainder BoundLog2 & r583 / r589 & 0\\
\texttt{GaborDominanceProof} & remaining isolation-shrink dominance bricks & r569 & 0\\
\texttt{GaborExplicitFormula} & EF implication from named remainders; pure EF unconditional (r634L); $220$ lines & r558 / r581 / r634L & 0\\
\texttt{GaborExposedOrbit} & existential pure-Gabor zero-side separator & r605 & 0\\
\texttt{GaborFEMultiplicity} & FE-invariant analytic order of zeros & r582 & 0\\
\texttt{GaborHatAnalytic} & entirety and FE-symmetry of \texttt{gaborHat} & r555 & 0\\
\texttt{GaborHorizontalEdges} & horizontal contour edges vanish & r557 & 0\\
\texttt{GaborInequality} & symbolic Gabor-inequality boundary & r544 / r552 & 0\\
\texttt{GaborIntegral} & bilateral Gaussian integral for the pure test & r543 & 0\\
\texttt{GaborLeftVertical} & left-edge prime reflection & r570 & 0\\
\texttt{GaborOuterTail} & window-adaptive outer quadrupole tail & r593 / r594 & 0\\
\texttt{GaborSeparation} & Gabor-wavepacket separation architecture; $2181$ lines & r542--r634L & 0\\
\texttt{GaborSpectralBridge} & spectral on/off split and FD exhaustion & r579--r589 & 0\\
\texttt{GaborThetaBound} & Gauss-bin majorant and partial summation & r552 & 0\\
\texttt{GaborVertical} & right/left vertical $T\to\infty$ identification & r564 & 0\\
\texttt{GaborVerticalLimit} & Landau-gap $T\to\infty$ contour glue & r576 & 0\\
\texttt{GaborWindowGlue} & three-term window-plus-tail comparison & r595 & 0\\
\texttt{GaborZeroSummable} & summability of $\hat h_W$ over nontrivial zeros & r557 / r587 & 0\\
\texttt{GraphResolvent} & graph-resolvent dictionary (finite algebra) & r412 & 0\\
\texttt{Haynsworth} & Haynsworth inertia additivity & r373 & 0\\
\texttt{InnerBridges} & finite inner-bridge algebra of the $A$-cap & r464 & 0\\
\texttt{Inertia} & matrix-theorem / Sylvester layer & r305 & 0\\
\texttt{OneDefect} & one-defect absorption theorems & r406 & 0\\
\texttt{Open} & ladder bookkeeping of the former open edges & r273 & 0\\
\texttt{PairBound} & finite pair-bound theorem of the coupled-tau lane & r269 / r271 / r320 & 0\\
\texttt{PivotCoordinate} & finite-algebra lemmas in source pivot coordinates & r380 & 0\\
\texttt{PrimeLogIndependence} & $\mathbb Q$-linear independence of $\{\log p\}$ & r617L & 0\\
\texttt{Recursion} & proved coupled-tau core identities & r256--r263 & 0\\
\texttt{Selected} & exact real selected-window domain & r397 & 0\\
\texttt{Source} & explicit prime-window source interface & r310 / r320 & 1\\
\texttt{Window} & \texttt{VonMangoldtWindow} structure and master theorem & r273 / r305 & 0\\
\texttt{ZeroIncrement} & local zero-count increment bound & r546 / r547 & 0\\
\bottomrule
\end{longtable}
\endgroup

\noindent Detail on the historically named spine (live sorry counts are
those of the inventory, not the older notes below):

\begingroup\small
\begin{longtable}{@{}llp{0.36\linewidth}@{}}
\toprule
\textbf{File} & \textbf{Status} & \textbf{Content}\\
\midrule
\endfirsthead
\multicolumn{3}{c}{\small\itshape Principal modules (continued)}\\
\toprule
\textbf{File} & \textbf{Status} & \textbf{Content}\\
\midrule
\endhead
\texttt{RH/Basic.lean} & proved & abstract chain data; $\tau\ne0$ on
regular windows\\
\texttt{RH/Recursion.lean} & \textbf{proved, no \texttt{sorry}} &
Theorems~\ref{thm:drain}--\ref{thm:terminal} in abstract form: bilinear
identity (\texttt{ring}), Riccati drain (\texttt{field\_simp}), budget
telescope (induction), terminal reduction, terminal equivalence
$0<m-r\leftrightarrow r/m<1$, two-branch cheap direction
$|Z|\le U\le M\Rightarrow Z^2/M^2\le1$ (strict variant included)\\
\texttt{RH/Inertia.lean} & \textbf{proved} since r305 (1
\texttt{sorry}: the mathlib gap) & PSD base theorem, positive-prefix
firewall (incl.\ a from-scratch Sylvester criterion
\texttt{posDef\_of\_leading\_det\_pos} --- mathlib v4.29.1 has
none), Sylvester pull-back and half-filling boundary all
\textbf{proved} (r305 cleanup); only the crossing-budget statement
\texttt{crossing\_budget} (Theorem~\ref{thm:budget}, v962
reference) keeps its \texttt{sorry} --- a mathlib gap (Jacobi
inertia / minor dictionary), not a mathematical one; also proved:
Hankel symmetry, the counting half
$2n-1\le S\leftrightarrow n\le(S{+}1)/2$, and (wave 5) the moment
counting theorem \texttt{moment\_counting\_free\_pivots} $+$
\texttt{first\_forced\_pivot} (Theorem~\ref{thm:counting})\\
\texttt{RH/Closure.lean} & \textbf{reconstruction} (r305); 1
\texttt{sorry} & the reconstruction theorem
\texttt{lstar\_terminal\_implies\_master} \textbf{proved} (L* $+$
terminal $q_N<1 \Rightarrow$ full master positivity via Schur
bordering $+$ principal submatrices);
\texttt{augmented\_prefix\_positive} and
\texttt{free\_window\_positivity} now \emph{corollaries} (no third
arithmetic axiom); the remaining \texttt{sorry} is
\texttt{terminal\_positive\_main} ($0<B \wedge q<1$ --- the
border/fiber TRUE hole)\\
\texttt{RH/Source.lean} & \textbf{source interface}
(r310/r310b); \textbf{red-team retyped} (r320); 1 \texttt{sorry} &
the real PrimeWindow construction
(nodes $=\log p^k$, weights $=\Lambda(p^k)$ \emph{derived} via
mathlib \texttt{vonMangoldt}); \textbf{proved}:
\texttt{nodes\_injective}, \texttt{predefined\_family},
\texttt{cofinal\_prime\_windows} (Euclid),
\texttt{buildPrimeWindow\_source\_exact} (by \texttt{rfl} through
derivation), \texttt{foldedWindow\_mass} (mass conservation of any
folding map), \texttt{buildPrimeWindow\_support\_canonical},
\texttt{finite\_forms\_converge\_to\_weil} (the strong
stabilization form); since r320: \texttt{RepresentsWindow}/%
\texttt{RepresentsSpec} retyped with $u$/$B$ fidelity $+$ the
separation discipline $+$ \texttt{budget\_pos} (the U1/U2 repair,
Section~\ref{sec:redteam}), the opaque \texttt{SourceExact} seals
the arch/border finding, and the sorry-free witness
\texttt{witness\_represents} exists (anchor 2, atoms $\{2,3,4\}$,
mesh level 4); the remaining \texttt{sorry} is the
definitional bridge \texttt{mainWindow\_iff\_builtFromPrimeSource}
(opacity-forced, now over the retyped forms)\\
\texttt{RH/Window.lean} & \textbf{master theorem} (r273) $+$
\textbf{the fog-free hole} (wave 5) $+$ \textbf{the canonical form}
(wave 6); since r305 1 \texttt{sorry} & concrete window
structure \texttt{VonMangoldtWindow} (nodes, signed weights, window
rule, derived half-filling cap, border vector, budget); derived $H_n$,
$h_k$, $D_n$, $q$; honest opaque predicate \texttt{MainWindow}; master
theorem \texttt{augmented\_prefix\_positive}
(Section~\ref{sec:master}); wave 5: \texttt{free\_window\_positivity}
(Remark~\ref{rem:northstar} --- the same hole in base coordinates;
\texttt{master\_implies\_free\_window} and
\texttt{main\_window\_reduction} PROVED); wave 6:
\texttt{lstar\_subordination} (lemma L*,
Theorem~\ref{thm:lstar} --- the same hole in two-measure coordinates)
with \texttt{lstar\_implies\_free\_window},
\texttt{lstar\_implies\_hankel\_posDef} and the quadratic-form
dictionary \texttt{hankel\_quadform} PROVED; PROVED: block/corner
extraction, Sylvester step, Schur-margin step, terminal gate, the
Schur-complement identity $D_n = B - u^{\mathsf T}H_n^{-1}u$, and both
retyped edges as corollaries (\texttt{terminal\_crossratio\_main},
\texttt{prefix\_chain\_positive\_main}); exact rational toy instance
with tested definitions\\
\texttt{RH/Open.lean} & retyped (r273, no \texttt{sorry}) & the
\texttt{WindowLadder} bookkeeping structure and the measured kill lists;
the two former universal open statements are removed (both refutable,
see below) and now live as \texttt{MainWindow}-conditioned corollaries
of the master theorem\\
\texttt{RH/PairBound.lean} & proved $+$ 1 \texttt{sorry};
\textbf{red-team retyped} (r320) & the r269/r271
finite pair algebra (block decomposition, pair triangle, level-2
refinement, alternation identity) all proved; the H5 margin law
retyped since r320 onto the \emph{canonical extraction}
(\texttt{signRuns}/\texttt{terminalDrive}/\texttt{bulkRuns}/%
\texttt{edgeLocal} as definitions; \texttt{signRuns\_sum} and
\texttt{canonical\_split} \emph{proved} --- no free
$(Z_{\rm loc}, \mathit{runs})$ anymore, the U3 repair) as
\texttt{pair\_margin\_main} on \texttt{VonMangoldtWindow} $+$
\texttt{MainWindow} (the \texttt{sorry})\\
\texttt{RH/Counterexamples.lean} & \textbf{proved, no \texttt{sorry}}
(guards) & the three r273 reviewer refutations as permanent theorems:
\texttt{pair\_margin\_not\_universal},
\texttt{terminal\_crossratio\_not\_universal},
\texttt{prefix\_resummation\_not\_universal} --- the pre-r273 universal
forms of H5 and both edges are FALSE over bare bookkeeping structures;
wave 5: \texttt{upper\_pinning\_not\_universal} (the exact one-negative
4-atom instance, Hankel minors machine-checked, first crossing at
$N_w+1$) $+$ the proved escape \texttt{o1\_pinning\_escape}
(offset $(S-3)/2$ unbounded); r320: the three permanent red-team
guards \texttt{old\_bridge\_terminal\_inconsistent},
\texttt{old\_bridge\_lstar\_inconsistent},
\texttt{old\_pair\_margin\_forces\_empty} ($\forall P$ form; the
pre-r320 statement types conserved verbatim as the guard targets;
axiom census kernel-checked without \texttt{sorryAx})\\
\texttt{RH/Elementwise.lean} & \textbf{Level-C architecture} (r326);
3 typed \texttt{sorry}s & the R325 repair set implemented
(Section~\ref{sec:elementwise-repair}): \textbf{proved} ---
\texttt{CanonicalPrimeWindow} $+$ \texttt{SourceExactSpec} $+$ the
construction theorem \texttt{sourceExact\_buildPrimeWindow} (the
wave-12 reviewer target; \texttt{SourceExact} eliminated as a free
assumption from the extraction route), \texttt{GridElement} (the
v749 native class, compact support proved),
\texttt{comb\_elementwise\_stabilization} $+$
\texttt{elementwise\_finite\_stabilization} (onset predefined from
the support alone, corpus gauge, no mesh quantifier;
\texttt{combRead\_eq\_window\_channel} the honesty tie), and the
extraction without the ladder \texttt{weil\_nonneg\_of\_windowlocal}
$+$ \texttt{weil\_nonneg\_of\_bordered} (one finite instantiation
per element; \texttt{BorderedCompressionBridge} a named Prop, no
truth commitment); the 3 \texttt{sorry}s are the named typed
Level-C statements: \texttt{arch\_elementwise\_stabilization} $+$
\texttt{pole\_elementwise\_stabilization} (classical kernel
transcription) and \texttt{specFamily\_sourceExact\_completion}
(classical $+$ definitional, opacity-forced; \emph{not} consumed by
the extraction route)\\
\texttt{RH/Selected.lean} & \textbf{exact selected domain} (r397);
0 \texttt{sorry} &
\texttt{RealCanonicalWindow} (real fields; total
$W^{\mathbb R}(a,m)$); sequence $a_k=2^k$,
$m_k=k\cdot 2^{\lfloor\sqrt k\rfloor}-1$ with
$\Delta_k=2^{-r_k}\log 2$, $a_k\to\infty$, $\Delta_k\to 0$
\textbf{proved}; reduction
\texttt{weil\_nonneg\_of\_selected\_windows} (plain
\texttt{fullRead} along the sequence; consumes the existing
arch \texttt{sorry}); named mincut
\texttt{selected\_augDualResolvent\_gt\_half}.  NO RH CLAIM\\
\texttt{RH/GaborIntegral.lean} & \textbf{proved, no \texttt{sorry}} &
r543 Gaussian integral representation of the pure Gabor test
(Section~\ref{sec:gabor}).  NO RH CLAIM\\
\texttt{RH/ZeroIncrement.lean} & \textbf{proved, no \texttt{sorry}} &
r546/r547 unit-increment bound \texttt{GaborIncrementBound} with
constant $2\cdot\texttt{zetaZerosInDiskCardBoundInner}$.  NO RH CLAIM\\
\texttt{RH/GaborThetaBound.lean} & \textbf{proved, no \texttt{sorry}} &
r552 theta majorant $+$ bin partial summation; on-line Gauss mass
$\gamma$-uniform $\le C\cdot\Theta_{\mathrm{lobe}}$.  NO RH CLAIM\\
\texttt{RH/GaborSeparation.lean} & 0 named \texttt{sorry};
census 7 (r634L, 2026-09-02); $2181$ lines &
live: \texttt{gabor\_zeroSide\_inputs\_to\_mathlib\_rh}
($\mathrm{hpos}^*$ on \texttt{gaborZeroSide}).  Historical
\texttt{gabor\_inputs\_to\_mathlib\_rh} \textbf{VACUOUS} (r600).
\texttt{GaborZeroSideForAllZeros} \textsc{forall\_zero\_overspecified}
(r605A).  r610: \texttt{GaborLowHeightZeroFree} $T_0=4$ unasserted;
\texttt{gaborSpectralFormula\_refuted\_of\_lowHeight}.
r612: asserting \texttt{gabor\_separationForAllZeros} deleted;
Prop retained unasserted.  r541 is a
numeric precheck, not a configuration statement.  NO RH CLAIM\\
\texttt{RH/GaborExposedOrbit.lean} & \textbf{proved, no \texttt{sorry}}
(r605B+C) &
existential pure-Gabor separator;
\texttt{gabor\_zeroSide\_pure\_criterion\_iff\_rh\_unconditional}.
RH $\Longleftrightarrow$ nonnegativity of the pure Gabor zero-side on
the two-parameter family $(a,\omega)$.  Sharpest live statement
(r634L): RH $\Leftrightarrow$ $\forall$ rational $(a,\omega)$,
$\mathrm{POLE}-\mathrm{PRIME}+\mathrm{ARCH}\ge 0$.  Positivity of
\texttt{gaborZeroSide} itself remains open.  Not an RH proof.
Census at landing 8; live 7 after r612.  NO RH CLAIM\\
\texttt{RH/GaborCountableCriterion.lean} & \textbf{proved, no \texttt{sorry}}
(r612/r612C/r634L) &
rational-packet criterion
\texttt{gabor\_zeroSide\_rational\_criterion\_iff\_rh\_unconditional}
(continuity of $Z(a,\omega)$ is the theorem
\texttt{gaborZeroSideContinuous\_holds}; $568$ lines).
r634L: prime-side criterion
\texttt{gabor\_primeSide\_rational\_criterion\_iff\_rh}
(RH $\Leftrightarrow$ $\forall$ rational $(a,\omega)$:
$\mathrm{POLE}-\mathrm{PRIME}+\mathrm{ARCH}\ge 0$).
Asserting \texttt{gabor\_separationForAllZeros} retired.
Census $8\to 7$ at r612; stays $7$ at r612C/r634L.
Modules $53\to 54$.  NO RH CLAIM\\
\texttt{RH/CoxeterCompletion.lean} & \textbf{proved, no \texttt{sorry}}
(r617L) &
$\Phi_{30}$ identities, vanishing linear coefficient,
$\mathrm{Tr}\,C=-1$ from the cyclotomic characteristic polynomial.
Axioms \texttt{propext}/\texttt{Classical.choice}/\texttt{Quot.sound}.
Census stays $7$; modules $54\to 56$.  NO RH CLAIM\\
\texttt{RH/PrimeLogIndependence.lean} & \textbf{proved, no \texttt{sorry}}
(r617L) &
\texttt{linearIndependent\_log\_primes} (Mathlib had no such lemma)
and \texttt{log\_ratio\_irrational}.  Finite-order clocks cannot carry
$\{\log p\}$.  Census stays $7$.  NO RH CLAIM\\
\bottomrule
\end{longtable}
\endgroup

\noindent r397: \texttt{lstar\_canonical}/\texttt{terminal\_q\_canonical}
are degraded to the alternative (rational-certificate) route; the
named open kernel is \texttt{selected\_augDualResolvent\_gt\_half}.
Sorry census stays five.  NO RH CLAIM.

\noindent r556/r600: a parallel Gabor route
(\texttt{RH/GaborSeparation.lean}) is documented in
Section~\ref{sec:gabor}.  The historical wrapper
\texttt{gabor\_inputs\_to\_mathlib\_rh} is \textbf{VACUOUS} (r600);
the live wrapper is \texttt{gabor\_zeroSide\_inputs\_to\_mathlib\_rh}.
The former asserting Gabor sorry \texttt{gabor\_separationForAllZeros}
was deleted at r612 (Prop retained unasserted) and is
\emph{not} part of the window-local census below.  r541 is a numeric
precheck, not a configuration statement.  NO RH CLAIM.

\noindent The \texttt{sorry} declarations (\textbf{eight in total}
since the r326 Level-C round: five window-local since the
r305/r310/r310b restructuring --- census $5 \to 5$ across the r320
red-team repair, with two of the five \emph{retyped} --- plus the
three \emph{named typed Level-C statements} of
\texttt{RH/Elementwise.lean}, census $5 \to 8$) are honest
markers, not deferred work of a known proof.  The r305
reconstruction proved the master theorem \emph{from} its two true
inputs (\texttt{lstar\_terminal\_implies\_master}) and cleaned the
Inertia layer (four of five statements proved, incl.\ a
from-scratch Sylvester criterion), so the census now separates
cleanly: (i) \texttt{lstar\_subordination} --- the base/wall
\emph{window-local} TRUE hole, the program's open center in
canonical two-measure form;
(ii) \texttt{terminal\_positive\_main} --- the border/fiber
\emph{window-local} TRUE hole ($0<B \wedge q<1$, faithful to the r258
\textsc{terminal\_q\_law} $+$ r243 budget form); (iii)
\texttt{pair\_margin\_main} --- the same fiber hole in r271 pair
coordinates (the r263 dictionary not yet formalized); (iv)
\texttt{crossing\_budget} --- a mathlib gap (Jacobi inertia /
minor dictionary), exact-rationally certified in v962; (v)
\texttt{mainWindow\_iff\_builtFromPrimeSource} --- the definitional
source bridge, opacity-forced by the honest \texttt{MainWindow}
predicate (the r310 form \texttt{mainWindow\_explicit\_bridge} is
proved from it); and, since r326, the three named typed Level-C
statements (vi) \texttt{arch\_elementwise\_stabilization} and (vii)
\texttt{pole\_elementwise\_stabilization} --- classical kernel
transcription (the \texttt{arch\_A} GL-48 integrals and the v716
pole closed form; R325-measured exact) --- and (viii)
\texttt{specFamily\_sourceExact\_completion} --- classical $+$
definitional, opacity-forced, \emph{not} consumed by the extraction
route.  The two true \emph{window-local} holes (Level~B
of Remark~\ref{rem:threelevels}) \emph{are} the program's open
window-family problem, stated in truth-capable form: conditioned on
the deliberately opaque \texttt{MainWindow} predicate they can
neither be refuted by toy models (the r273 and wave-5
counterexample guards verify that the old universal forms could)
nor proved without supplying the von-Mangoldt arithmetic.  The two
wave-12 caveats are updated in wave 13: the named open Lean target
\texttt{CanonicalPrimeWindow} plus exactly one construction theorem
\texttt{sourceExact\_buildPrimeWindow}
(Remark~\ref{rem:sourceexact-target}) is \emph{implemented and
proved} (r326, Section~\ref{sec:elementwise-repair}), and the
census now \emph{names} the Level-C distance (the three typed
statements above) without closing it.  No axioms, no
\texttt{native\_decide}, no statement weakening; builds green.

%======================================================================
\section*{Reproducibility}

All objects referenced here are pinned in \texttt{rh/INVENTORY.json}
(SHA-256 per file) and exercised by the RH suite
\texttt{rh/verification/run\_rh.py} (integrity check, sealed probes
r250--r327 in smoke mode, the fifteen verification modules v955/
v956/v958/v959/v960/v961/v962/v963/v964/v965/v966/v967/v968/v969/
v970 by path, and the Lean build gate); since
wave~4 the suite's fast mode is a gate of \texttt{bash build.sh audit}.  The canonical claim
state is the repository ledger; the canonical prose is
\texttt{tfpt\_prime\_front.tex}.

\statusbox{\textbf{Closing status.}  Research documentation.  The certified
content is finite and window-level (\mE); the two edges of
Section~\ref{sec:edges} are open (\mO); the mincut is base 4 / refined 5.
The Gabor chain of Section~\ref{sec:gabor} is a parallel conditional
reduction: the live criterion is positivity of \texttt{gaborZeroSide}
($\mathrm{hpos}^*$); the historical
\texttt{gabor\_inputs\_to\_mathlib\_rh} is VACUOUS (r600); r541 is a
precheck, not a configuration statement; separation stays open.
\textbf{Nothing in this document is
evidence for or against the Riemann Hypothesis in either direction.  NO RH CLAIM.}}

\end{document}
