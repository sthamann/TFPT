% rdagger_saturation.tex -- specialist dossier on the critical
% bordered dual resolvent R^dagger.  Self-contained for a reader in
% discrete orthogonal polynomials / Riemann--Hilbert asymptotics.
% Builds with pdflatex.  Companion problem: lstar_problem.tex.
% NO RH CLAIM.  Research documentation, not a theorem.
\documentclass[11pt]{article}

\usepackage[a4paper,margin=2.6cm]{geometry}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{booktabs}
\usepackage{microtype}
\usepackage{xcolor}
\usepackage[colorlinks=true,linkcolor=blue!50!black,urlcolor=blue!50!black]{hyperref}

\newtheorem{problem}{Problem}
\newtheorem{definition}{Definition}
\newtheorem{prop}{Proposition}
\newtheorem{thm}{Theorem}
\theoremstyle{remark}
\newtheorem{remark}{Remark}

\newcommand{\R}{\mathbb{R}}
\newcommand{\N}{\mathbb{N}}
\newcommand{\E}{\mathsf{E}}
\newcommand{\Rd}{R^{\dagger}}
\newcommand{\Ld}{L^{\dagger}}
\newcommand{\Ls}{L^{*}}
\newcommand{\half}{\tfrac12}
\newcommand{\dd}{\,\mathrm{d}}
\newcommand{\Tfrak}{\mathfrak{T}}
\newcommand{\Zloc}{Z_{\mathrm{loc}}}
\newcommand{\Sk}{\mathcal{S}}
% Grade marks: exact / theorem-grade / lean-verified / census /
% hypothesis / refuted / open program.
\newcommand{\gr}[1]{\textnormal{\small[#1]}}

\title{\bfseries The saturated dual resolvent\\[2mm]
\large A specialist dossier on the critical bordered block
$\boldsymbol{R^{\dagger}}$}
\author{Stefan Hamann}
\date{August 30, 2026}

\begin{document}
\maketitle

\begin{abstract}\noindent
On a family of explicit finite discrete measures, a particle--hole
duality identifies a two-measure subordination inequality with the
spectral condition $R\succ\half I$ on a dual hole kernel.  A rank-one
border of that kernel produces a single object $\Rd$ whose positivity
$\Rd\succ\half I$ is equivalent to the original inequality
\emph{and} a terminal budget condition, and whose critical
three-by-three Schur block carries both.  A constrained equilibrium
problem on the same dual weight places the binding pair of nodes
astride a saturated hard-edge block.  All identities below are finite
linear algebra; all numerical values are a frozen census on a finite
family.  No asymptotic theorem is proved.  After the two-rank inertia
cut, the graph-resolvent dictionary, and the edge-balance chain,
the two remaining holes close at the same kind of object: an
unsigned cancellation of von~Mangoldt data
(Section~\ref{sec:cores}).  Round 427 audited the campaign
numbers (Section~\ref{sec:audit}).  The current architecture is
an \emph{open program} in the Krein--Langer class $\Sk_{\kappa}$
(Section~\ref{sec:potapov}): bulk membership $\Sk_{\le 1}$ and
edge membership $\Sk_{0}$ via a Redheffer pivot, with two
quantifier weakenings.  The older minimal-inertia and
saturation/RHP questions are retained as
Problems~\ref{prob:minimal} and~\ref{prob:rhp}.  This document is
a problem statement for specialists, not a claim, and nothing in
it is evidence for or against the Riemann hypothesis.
\end{abstract}

\medskip\noindent
\textbf{Status and grades.}\enspace
Every assertion is marked
\textsc{exact} (finite identity, including rational arithmetic),
\textsc{theorem-grade} (finite-matrix theorem on measured inputs),
\textsc{lean-verified} (sorry-free in \texttt{rh/lean/}, kernel-checked),
\textsc{census} (measured on a frozen finite family; no
extrapolation),
\textsc{hypothesis} (unproved analytic statement),
\textsc{refuted} (named internal route closed by a sealed probe),
or \textsc{open program} (named target, no existence proof).
The construction of the underlying measures $(\mu,\nu)$ is given in
full in the companion note~\cite{lstar-note}; the present dossier
does not repeat it.  Uvarov-transform language, the discrete-hard-edge
placement, and the Lubinsky-class exclusion are recorded there
(\S{}frozen-placement, \S{}frozen-dualhole) and are cited, not
duplicated.  Machine records: sealed probes of rounds
356, 359, 360, 362, 363, 366, 367 and 369--372 (repository
\texttt{experiments/tfpt-discovery/}); Lean transcription round 373
(\texttt{RH/Haynsworth.lean}, \texttt{RH/DualResolvent.lean});
graph-resolvent consolidation round 412
(\texttt{RH/GraphResolvent.lean});
edge-balance consolidation round 426
(\texttt{RH/EdgeBalance.lean});
campaign audit round 427; selected-sequence reopening round 428;
head-identity round 429.  Companion notes:
\texttt{campaign\_audit.tex}, \texttt{qn\_reopened.tex},
\texttt{zloc\_head.tex}.

\section{Setup}\label{sec:setup}

Fix a prime power $z$ (the window anchor).  A triangular (``tent'')
test function of half-width $\Delta$, determined by $z$ and a fixed
oversampling parameter, samples the von Mangoldt comb and an
archimedean kernel, producing an even trigonometric polynomial
$f$ --- the symbol of a circulant.  Folding the symbol onto $[-1,1]$
by $x_{j}=\cos\theta_{j}$ yields a signed discrete measure on the
cosine grid, split by sign into two positive measures $\mu$ and
$\nu$ with disjoint supports in $[-1,1)$ and $S$ atoms in total
\cite[construction]{lstar-note}.  Write $N=\lceil S/2\rceil$ and
$Y=\operatorname{supp}\nu$.  Across the whole family the grid is
unculled, so $S=2N-1$ \gr{exact}: half-filling is Borodin's
complementary rank condition, not an extra hypothesis.

The running instance is $z=16$: $S=367$, $N=184$, $|Y|=104$.  The
binding pair of $\nu$-atoms sits at grid (fold) indices $(2,4)$,
the two rightmost odd nodes of the cosine lattice.  The inequality
under study, denoted $\Ls$, asks
\[
\int p^{2}\dd\nu \;<\; \int p^{2}\dd\mu
\qquad\text{for every real polynomial }p\neq 0\text{ with }
\deg p<N.
\]
Equivalently, the $\nu$-sampling matrix $\E_{N}$ in the
$\mu$-orthonormal frame satisfies $\lambda_{\max}(\E_{N})<1$.  An
augmented form $\Ld$ adjoins a rank-one border (a budget-normalized
smooth test functional) and asks $\lambda_{\max}(\E_{N}+bb^{T}/B)<1$.
Both statements are open for the unbounded family.  On every tested
window the inequalities hold, with spectral margins decaying as a
power of $N$; structurally destroyed comparison inputs fail already
at a small fraction of the critical degree \cite{lstar-note}.  No
statement here claims either inequality.

\section{Definition of \texorpdfstring{$\boldsymbol{R^{\dagger}}$}{R-dagger}}
\label{sec:def}

Lift $(\mu,\nu)$ to the positive measure $\eta=\mu+\nu$ with weights
$u_{j}=|\widetilde w_{j}|$ on the union $X=\operatorname{supp}\eta$.
Let $P_{X}$ be the monic node polynomial of $X$.  Borodin's reciprocal
weight \cite{Borodin2002} is
$u^{\vee}_{j}=1\bigl/(u_{j}\,P_{X}'(x_{j})^{2}\bigr)$.  On this
cosine grid the dual weight has the closed form
\begin{equation}\label{eq:uvee}
u^{\vee}_{j}\;\propto\;
\frac{c_{j}\,(1-x_{j})}{|f(\theta_{j})|}\,,
\end{equation}
where $c_{j}$ is the folding weight ($c_{j}=\tfrac12$ at the left
endpoint, else $1$): the original weight carries $+\log|f|$, the dual
weight $-\log|f|$ \gr{exact, three evaluation routes}.  Let
$\Pi_{N-1}^{u^{\vee}}$ be the rank-$(N-1)$ Christoffel--Darboux
projection kernel of $u^{\vee}$ on $X$, dressed by
$\sqrt{u^{\vee}}$, and let $R$ be its restriction to $Y$.

Let $b$ be the border functional in the $\mu$-orthonormal frame,
$B>0$ the budget, $\beta=b/\sqrt{B}$, and $v$ the node-side image of
the border under the $\mu$-Christoffel--Darboux kernel
($v_{j}=\sqrt{\nu_{j}/B}\int K_{\mu}(y_{j},t)\,\dd\sigma(t)$).  Write
$\gamma=\beta^{T}\beta$ and $\widetilde v=Dv$, with $D$ the diagonal
sign matrix of $P_{X}'$.

\begin{definition}[Bordered dual resolvent]\label{def:rdagger}
The \emph{augmented dual kernel} is the $(|Y|+1)\times(|Y|+1)$ matrix
\begin{equation}\label{eq:rdagger}
\Rd
\;=\;
\begin{pmatrix}
R^{-1} & \widetilde v \\
\widetilde v^{T} & 1+\gamma
\end{pmatrix}^{-1}.
\end{equation}
The extra coordinate is the border; the leading principal block is a
rank-one Sherman--Morrison deformation of $R$
(Proposition~\ref{prop:A}\,(A4) below).
\end{definition}

Route~(i) --- adjoining a virtual atom and taking the
$(S+1)$-point Borodin kernel --- is available on a single-atom toy
border (half-filling shifts $S=2N-1\mapsto S^{\dagger}=2N$, dual rank
$N-1\mapsto N$) and agrees with \eqref{eq:rdagger} up to sign
conjugation \gr{exact, rational arithmetic}.  On the real windows the
border is a signed smooth-comb measure with $S$ atoms, every one of
which collides with a grid node, and is not a point evaluation
(representability spread $56.8$ on the running instance).  The
bordered-resolvent form is therefore the general object.

\section{Exact duality formulae}\label{sec:duality}

Write $\operatorname{margin}=1-\lambda_{\max}(\E_{N})$ and
$\operatorname{margin}^{\dagger}=1-\lambda_{\max}(\E_{N}+bb^{T}/B)$.

\begin{prop}[Particle--hole dictionary \cite{Borodin2002}]\label{prop:borodin}
At half-filling $S=2N-1$,
\[
\Ls
\;\iff\;
\lambda_{\max}(\E_{N})<1
\;\iff\;
R\;\succ\;\half I\,,
\]
with the spectral map
$\operatorname{margin}=2-1/\lambda_{\min}(R)$
\gr{theorem-grade; absolute deviation $\le 9.9\cdot10^{-10}$ on
$85$ windows}.  The $L$-ensemble transformation
$Q=\E_{N}(I+\E_{N})^{-1}$ is exact
\gr{rational arithmetic on a $5$-atom model; double precision
$\le 7.6\cdot10^{-12}$ on $85$ windows}, and combined with Borodin's
complementation $\Pi_{N}^{u}=D\,(I-\Pi_{N-1}^{u^{\vee}})\,D$ yields
$\E_{N}=D(R^{-1}-I)D$ on $Y$.
\end{prop}

Live census: $\lambda_{\min}(R)-\half$ ranges from $+4.2\cdot10^{-5}$
(running instance: $\lambda_{\min}(R)=0.500041882$) down to
$+5.9\cdot10^{-11}$ on the deepest tranche --- all positive, no
counterexample.  Comparison inputs with destroyed source structure
violate $R\succ\half I$ structurally
($\lambda_{\min}(R)-\half\in[-0.500,-0.083]$).  Honest limit: at the
deepest instances the double-precision resolution of the dual route
($\sim5\cdot10^{-10}$) exceeds the margins, so the hole coordinate is
exact algebra, not a sharper certification instrument.

The pair identities $c=\varepsilon_{1}\varepsilon_{2}(R^{-1})_{12}$,
$p=2-(R^{-1})_{11}$, $q=2-(R^{-1})_{22}$ identify the two-point
Uvarov block of the companion note with the $(1,2)$ principal minor
of $2I-R^{-1}$ \gr{theorem-grade}.  The absolutely continuous
hard-edge class is excluded at census grade: the rescaled pair
positions $a_{j}=2N^{2}(1-y_{j})$ are family-constant against
$\pi^{2}j^{2}/4$ on all $85$ windows (maximum deviation
$0.33/N$), while the kernel correlation $\rho_{K}$ decays with
fitted exponent $1.4222$, contrary to Lubinsky's Bessel limit at
fixed microscopic arguments \cite{Lubinsky2008},
\cite[\S{}frozen-placement]{lstar-note}.

\begin{prop}[Augmented dictionary]\label{prop:A}
Let
$Z=\bigl(\begin{smallmatrix}R^{-1}&\widetilde v\\
\widetilde v^{T}&1+\gamma\end{smallmatrix}\bigr)$,
so $\Rd=Z^{-1}$, and write $M^{\dagger}=\Rd-\half I$.  The following
are finite-matrix identities \gr{theorem-grade; rational arithmetic
on toys, graded double precision on $75$ windows of the prime-generated
family plus two character-twisted families of $42$ windows each}.
\begin{enumerate}
\item[\textup{(A1)}]
The augmented node Gram $G^{\dagger}$ is the sign-conjugate of
$(\Rd)^{-1}-I$: the particle--hole complementation extends by one
row.
\item[\textup{(A2)}]
$\operatorname{margin}^{\dagger}=2-1/\lambda_{\min}(\Rd)$
(deviation $\le 1.1\cdot10^{-11}$ shallow,
$\le 5.5\cdot10^{-10}$ at the deepest computed row).
\item[\textup{(A3)}]
$\Ld\;\iff\;\Rd\succ\half I$, two-sided.  Both truth directions
occur on real windows: six character-twisted rungs with terminal
ratio $q_{N}>1$ have $\lambda_{\min}(\Rd)-\half<0$ (e.g.\ $-9.8\cdot
10^{-5}$), exactly as the identity demands.
\item[\textup{(A4)}]
Sherman--Morrison on the $Y$-block:
$\Rd_{YY}=R+(R\widetilde v)(R\widetilde v)^{T}/
(1+\gamma-\widetilde v^{T}R\widetilde v)$,
and $\Rd_{bb}=1/(1+\gamma-\widetilde v^{T}R\widetilde v)$
(deviation $\le 1.2\cdot10^{-14}$ on every computed row, including
the deepest).  The border is a rank-one insertion at dual-resolvent
level.
\item[\textup{(A5)}]
$\Rd\succ\half I$ if and only if $R\succ\half I$ and $q^{\dagger}<1$,
where $q^{\dagger}=\gamma+v^{T}(I-\E_{N})^{-1}v$ is frame-invariant
(equals the squared dual border norm $u^{T}H^{-1}u/B$) and coincides
with the standing terminal coordinate.  The dictionary
$1-q^{\dagger}=(5/7)(1-q_{N})/B$ holds to relative
$6.7\cdot10^{-6}$.
\item[\textup{(A6)}]
The Schur complement of $M^{\dagger}$ onto the border coordinate
equals $(1-q^{\dagger})/\bigl(2(1+q^{\dagger})\bigr)$ globally, and
equals the same value inside the local $3\times3$ block of
Proposition~\ref{prop:block} (quotient $\le 1.0\cdot10^{-11}$).
\item[\textup{(A7)}]
Cauchy interlacing:
$\lambda_{k}(\Rd)\le\lambda_{k}(R)\le\lambda_{k+1}(\Rd)$.
The border can only lower the bottom of the dual spectrum
($\operatorname{margin}^{\dagger}\le\operatorname{margin}$).
On the running instance,
$0.500041459\le 0.500041882$.
\end{enumerate}
\end{prop}

A formal reconstruction, independent of the dual coordinate, is
already machine-checked in Lean: $\Ld\iff\Ls\wedge\mathrm{Terminal}$
and $\Ls\wedge\mathrm{Terminal}\Rightarrow$ positivity of every
bordered window matrix through the cap
(\texttt{RH/Augmented.lean}, \texttt{RH/Closure.lean};
axiom audit in \texttt{RH/Audit.lean}, consuming only the two named
canonical holes).  The dual-resolvent dictionary itself is
sorry-free since round 373: $\Ld\iff\Rd\succ\half I$ under the
$\mu$-ONB whitening (\texttt{RH/DualResolvent.lean})
\gr{lean-verified}.  Haynsworth additivity on the two-rank and mixed
$J$-forms is likewise sorry-free
(\texttt{RH/Haynsworth.lean}; Section~\ref{sec:minimal}).
What is \emph{not} formalized, and not proved analytically, is the
pair of census premises (P1)--(P2) of Problem~\ref{prob:minimal} for
the unbounded family --- and, a fortiori, $\Rd\succ\half I$ itself.

\section{The critical three-by-three block}\label{sec:block}

Partition $Y=\{y_{1},y_{2}\}\cup C$, with $\{y_{1},y_{2}\}$ the
binding pair (folds $2,4$) and $C$ the rest of $Y$.  Write
$M=R-\half I$ and $S_{N}$ for the $2\times2$ Schur complement of $M$
onto the pair.  Then \gr{exact}
\[
M\succ0
\;\iff\;
M_{CC}\succ0
\;\text{ and }\;
S_{N}\succ0\,,
\]
and $S_{N}$ is a ratio of Sylvester-bordered minors of $\det M$
(the rest block sits inside the border; a pair-only truncation is
false by $4.07$ nats on the running instance and exactly on the
rational toy).  The off-diagonal of the dual resolvent
$A^{-1}=(I-2R)^{-1}$ is the Casoratian of the two consecutive dual
orthonormal polynomials, resolvent-dressed (discrete IIKS identity)
\gr{theorem-grade}.  The diagonal entries $(A^{-1})_{kk}$ are
\emph{not} Casoratian data: their cross share
$(A^{-1})_{12}^{2}/\bigl((A^{-1})_{11}(A^{-1})_{22}\bigr)$ has
median $0.702$ on $85$ windows (range $0.46$--$0.84$).  No standalone
explicit prefactor turns the Casoratian into $\det S_{N}$ at the
sealed bars.

\begin{prop}[Binding]\label{prop:block}
On the $74$ resolvable windows of the family
($\lambda_{\min}(R)-\half>10^{-9}$),
\[
\operatorname{bind}
\;=\;
\frac{\lambda_{\min}(S_{N})}{\lambda_{\min}(R)-\half}
\in[1.0003,\,1.0605]\,,
\quad\text{median }1.0058
\]
\gr{census}.  After the rank-one border, the $3\times3$ Schur block
$S^{\dagger}_{3}$ of $M^{\dagger}$ on (pair, border) satisfies
\[
\operatorname{bind}^{\dagger}
\;=\;
\frac{\lambda_{\min}(S^{\dagger}_{3})}{\lambda_{\min}(\Rd)-\half}
\in[1.0003,\,1.0608]\,,
\quad\text{median }1.0056
\]
\gr{census}.  The in-block border Schur equals the global border
Schur of \textup{(A6)}.  The rank-one deformation does not move the
pair structure: on the running instance the cross share of $\Rd$ is
$0.6990$ against $0.6973$ for $R$; ladder medians $0.708$ against
$0.702$.
\end{prop}

On the running instance the three eigenvalues of $S^{\dagger}_{3}$ are
$(4.16\cdot10^{-5},\,4.90\cdot10^{-4},\,1.76\cdot10^{-2})$: the bottom
mode is the $\Ls$ pair channel, the top mode is the terminal border
channel.  The critical eigenvector's border mass is
$2.5\cdot10^{-5}$ there, and $5.8\cdot10^{-4}$ at the
near-terminal window where
$\operatorname{margin}^{\dagger}/\operatorname{margin}=0.8716$.
The giant operator behaves, at the critical point, like this
$3\times3$ block.

\section{The pair straddles the saturation edge}\label{sec:edge}

Two independent finite objects describe the dual ensemble's occupation
of the cosine grid.

\paragraph{Exact occupation.}
Let $o_{\eta}(x_{j})$ (resp.\ $o_{\mathrm{dual}}(x_{j})$) be the
diagonal of the rank-$N$ $\eta$-projection (resp.\ the rank-$(N-1)$
dual-hole projection).  Borodin complementation on the diagonal gives
\begin{equation}\label{eq:occ}
o_{\eta}(x_{j})+o_{\mathrm{dual}}(x_{j})\;=\;1
\end{equation}
at every union node, with trace ward
$\sum_{j}o_{\mathrm{dual}}(x_{j})=N-1$
\gr{exact; rational arithmetic on the toy, $\le 2.2\cdot10^{-11}$
live}.  This is the finite counterpart of an equilibrium density
versus its upper constraint, with no solver and no asymptotics.  On
the prime-generated family, fold~$1$ (the rightmost node) is the
unique occupation anomaly: $o_{\mathrm{dual}}(x_{1})$ lies
$0.25$--$0.33$ below the bulk median on all $85$ windows, while the
pair folds $2$ and $4$ are flat (deviation $\le 0.10$).

\paragraph{Obstacle problem.}
Minimize the discrete energy
\[
E(m)
=\sum_{j\neq k}m_{j}m_{k}\log\frac{1}{|x_{j}-x_{k}|}
+\sum_{j}m_{j}\bigl(-\log u^{\vee}_{j}\bigr)
\]
over $0\le m_{j}\le 1$, $\sum m_{j}=N-1$.  The box constraint
\emph{is} the Baik--Kriecherbauer--McLaughlin--Miller upper constraint
\cite{BKMM2007}; dropping it piles mass $\ge 92$ times the cap onto
single nodes.  A deterministic projected first-order solver
(FISTA plus exact box-simplex water-filling, sealed iteration
counts, KKT partition gates) produces, on four sealed instances
including the running instance, a dual-void block equal to folds
$\{1,2,3\}$ \gr{census}.  By \eqref{eq:occ} this is the primal
saturated block at the right hard edge.

\begin{prop}[Straddle, census]\label{prop:straddle}
The binding pair $(2,4)$ straddles the saturation-block edge $3\mid 4$:
fold $2$ lies inside the saturated zone, fold $4$ outside.
Auxiliary character-twisted families on the same grid have a
\emph{different} partition --- they occupy fold $1$, with no fold-$1$
occupation anomaly on $0/42+0/42$ windows --- and a sign-scrambled
comparison input is disordered at the edge while failing
$R_{CC}\succ\half I$ ($\lambda_{\min}(R_{CC})-\half=-0.4962$) with
the pair block itself still positive.  The identity chain is
world-blind algebra; the partition, and the positivity, are not.
\end{prop}

The asymptotic equilibrium-measure theorem (BKMM band and saturation
limit shape for the field $-\log u^{\vee}$) is not proved
\gr{hypothesis, open}.  The finite partition is a measurement about
this family.

\section{The observed exponent}\label{sec:alpha}

On the $57$ calibration windows a Theil--Sen fit of
$\log\lambda_{\min}(S_{N})$ has slope $-3.334$, hitting the standing
margin exponent $3.332$ at distance $0.002$ and rejecting an
alternative reading $1.65$ at distance $1.684$ \gr{census}.  The
diagonal dual-resolvent entries carry the same exponent
($\lvert(A^{-1})_{11}\rvert$ slope $+3.365$,
$\lvert(A^{-1})_{22}\rvert$ $+3.292$,
$\lvert(A^{-1})_{12}\rvert$ $+3.316$).  Weight-side Christoffel
exponents ($1.79$ and $0.38$ families) miss by a wide margin: they
do not carry the resolvent diagonals.

A dimensionless combination
$t_{\mathrm{geo}}
=(\lambda_{\min}(R)-\half)\,
\sqrt{(A^{-1})_{11}(A^{-1})_{22}}$
is margin-locked: slope $-0.009$ on the $57$, range
$[0.222,\,0.287]$ on all $74$ resolvable windows \gr{census}.  Exact
algebra identifies $t_{\mathrm{geo}}$ with a function of
$\det(S_{N}/\varepsilon)$ and the cross share; family-constancy is a
local-parametrix-class census, not a Gamma-function derivation.
The Gamma parametrix of \cite[Thm~2.7, Rem~2.8]{BKMM2007} adjacent
to a saturated band remains the natural analytic candidate
\gr{hypothesis, open}.

An adjugate identity
$\det S_{N}\cdot 4\bigl[(A^{-1})_{11}(A^{-1})_{22}-(A^{-1})_{12}^{2}\bigr]
=1$
\gr{exact} implies that the wander of $\log\det S_{N}$ \emph{is}
the wander of $-\log\bigl((A^{-1})_{11}(A^{-1})_{22}\bigr)$
(fine-structure correlation $+0.9835$), with the cross-share term
five times subleading.  The leading common wander lives in the
diagonal resolvent data and cancels algebraically in the minor.

\section{Relative error-operator form}\label{sec:error}

Two constant-matrix targets fail the census and force a relative
statement.

\paragraph{Rest block.}
Cauchy interlacing on the principal submatrix $R_{CC}$, and the
Schur-complement eigenvalue inequality, give
\begin{equation}\label{eq:rest}
\lambda_{\min}(R_{CC})-\half
\;\ge\;
c\,\bigl(\lambda_{\min}(R)-\half\bigr)
\qquad\text{with }c=1
\end{equation}
as a finite theorem, gated in both directions on the rational toy
and on $74+84$ resolvable windows \gr{theorem-grade}.  On the
prime-generated family the measured ratio lies in $[8.8,\,54.2]$
(median $20.3$).  On the character-twisted families the ratio
reaches the interlacing line: minima $1.0$ on $11$ of $84$ windows.
Any analytic rest-block clause is a theorem only at $c=1$; a
comfortable factor $8.8$ does not transfer.

\paragraph{Leading coefficient.}
The schematic
$S_{N}=N^{-\alpha}(M_{0}+O(N^{-\varepsilon}))$
with $M_{0}$ a constant positive matrix is too rigid.  The condition
number $\kappa_{S}=\lambda_{\max}(S_{N})/\lambda_{\min}(S_{N})$
ranges in $[6.3,\,30.5]$ (median $13.9$), a range ratio $4.88>1.5$:
the margin direction collapses ($\operatorname{bind}$ median
$1.0058$) but the second eigenvalue wanders by an $O(1)$ factor
\gr{census}.  The honest form is projective: one controls
$S_{N}/\lambda_{\min}(S_{N})$ in a bounded set of positive-definite
matrices, or equivalently the error operator
$E=P_{\mathrm{true}}P_{\mathrm{model}}^{-1}-I$
of a local Riemann--Hilbert parametrix in a \emph{relative} operator
norm on the critical block, not as an additive constant remainder.

No bound on $E$ is proved \gr{open}.  Equation~\eqref{eq:rest} at
$c=1$ is the only theorem-grade error control presently available,
and it is finite interlacing, not an asymptotic remainder.  After
rounds 367--373 this relative form is the \emph{stronger} target:
the primary question no longer needs a remainder that tracks
$N^{-\alpha}$ (Section~\ref{sec:minimal}).

\section{The minimal inertia form (rounds 367--373)}
\label{sec:minimal}

The schematic $S_{N}=N^{-\alpha}(M_{0}+O(N^{-\varepsilon}))$ of
Section~\ref{sec:error} is a margin asymptotic.  The two-rank
inertia cut replaces it by two coarser statements on the rest block
$A_{0}=R_{N-3}-\half I$ and the $2\times2$ Schur matrix
$K_{2}=I+U^{T}A_{0}^{-1}U$, with $U$ the last two dual
Christoffel--Darboux columns.

\paragraph{Analytic discount.}
Write $\operatorname{ind}_{-}(A)$ for the number of strictly
negative eigenvalues of a real Hermitian matrix $A$.  It is enough
to prove, uniformly on the admissible family,
\begin{align}
\operatorname{ind}_{-}(A_{0})
&\;\le\;1
\tag{P1}\\
\det K_{2}
&\;\le\;-c_{0}<0
\quad\text{on the branch }\operatorname{ind}_{-}(A_{0})=1.
\tag{P2}
\end{align}
(P1) is an integer mode count.  (P2) is the sign of an $O(1)$
$2\times2$ source.  An asymptotic argument need only beat a
\emph{fixed} signature reserve, not the shrinking
$N^{-3.33}$ spectral margin of Section~\ref{sec:alpha}.

Census on the $74$ resolvable windows
($\lambda_{\min}(R)-\half>10^{-9}$) \gr{census, round 367}:
overload $\operatorname{ind}_{-}(A_{0})\ge2$ is $0/74$; the
dichotomy is $45$ exact-one (nneg-$1$) against $29$ already
positive-definite at rank $N-3$ (P1 vacuous).  On the nneg-$1$
branch, $-\det K_{2}\in[1.157,\,1126.389]$, census floor $0.50$
holds $45/45$, Theil--Sen slope of $\log(-\det K_{2})$ against
$\log N_{w}$ is $+0.543$ (flat against the margin rate $-3.332$),
and the restatement correlation against $\log\operatorname{margin}$
on the $57$ calibration windows is $-0.0064$.  The implication
$\mathrm{(P1)}\wedge\mathrm{(P2)}\Rightarrow R\succ\half I$ holds on
all $74$ resolvable rows; it is sufficient, not necessary (the
vacuous-$29$ already have $A_{0}\succ0$).

\begin{thm}[Two-rank Haynsworth]
\label{thm:haynsworth}
Let $A_{0}$ be real Hermitian and invertible with
$\operatorname{ind}_{-}(A_{0})=1$, and let
$K_{2}=I_{2}+U^{T}A_{0}^{-1}U$.  If $\det K_{2}<0$, then
$A_{0}+UU^{T}\succ0$
\gr{lean-verified, \texttt{RH/Haynsworth.lean}, sorry-free}.
\end{thm}

Finite real algebra only; the census premises (P1)--(P2) on windows
are \emph{not} asserted.  The Lean statement is
\texttt{haynsworth\_two\_rank}.  On the hole kernel,
$A_{0}+UU^{T}=R-\half I$, so Theorem~\ref{thm:haynsworth} converts
(P1)--(P2) into $R\succ\half I$.  The R$^{\dagger}$ dictionary of
Proposition~\ref{prop:A}(A3),(A5) then lifts to $\Ld$ once the
terminal Schur $q^{\dagger}<1$ is available; both directions of
$\Ld\iff\Rd\succ\half I$ are sorry-free in
\texttt{RH/DualResolvent.lean}.

\paragraph{Mixed $3\times3$ form.}
The border is not a third unaugmented CD column.  At dual-resolvent
level the Sherman--Morrison insertion of Proposition~\ref{prop:A}(A4)
does supply a third column with an explicit signature.  Write
$s=R\widetilde v$, $\mathrm{den}=1+\gamma-\widetilde v^{T}R\widetilde v$,
and
\[
U_{\mathrm{mix}}
=
\begin{pmatrix}
u_{N-3}&u_{N-2}&s\\
0&0&-1
\end{pmatrix},\qquad
J=\operatorname{diag}\bigl(1,1,1/\mathrm{den}\bigr).
\]
Then \gr{theorem-grade, round 369; Fractions residual exactly $0$
with $\mathrm{den}=23/30$; live residual $\le1.04\cdot10^{-14}$}
\begin{equation}\label{eq:mixed}
M^{\dagger}
\;=\;
\operatorname{blkdiag}(A_{0},-\half)
\;+\;
U_{\mathrm{mix}}\,J\,U_{\mathrm{mix}}^{T}.
\end{equation}
The generalized Haynsworth identity for invertible Hermitian $J$,
\[
\operatorname{In}
\begin{pmatrix}A&U\\U^{T}&-J^{-1}\end{pmatrix}
=
\operatorname{In}(A)+\operatorname{In}(-\Phi)
=
\operatorname{In}(-J^{-1})+\operatorname{In}(A+UJU^{T}),
\]
with $\Phi=J^{-1}+U^{T}A^{-1}U$, is sorry-free
(\texttt{haynsworth\_mixed}) \gr{lean-verified}.  The two-rank
theorem is the case $J=I_{2}$.

\paragraph{Census building blocks.}
Three further finite objects are available to a specialist proof of
(P1)--(P2).  All three are typed \textsc{census} or
\textsc{theorem-grade} as marked; none is an extrapolation.
\begin{itemize}
\item
Wedge-sign stability \gr{census, round 371}: the canonical CD
two-form satisfies
$\langle\omega,\wedge^{2}A_{0}^{-1}\omega\rangle<0$ on $45/45$ of
the nneg-$1$ branch (range $[-637.7,\,-1.32]$, zero flips).  The
second compound matrix therefore has source structure.  The
capture identity that would turn this into (P2) is algebraically
identical to (P2) via the exact expansion of $\det K_{2}$; it is
not independent arithmetic.  The $\wedge^{2}$-subspace share is
\emph{not} stable (majority $\ge0.50$ only on $27/45$).
\item
$\Phi$-monotonicity \gr{theorem-grade, round 370}: the
matrix-valued Weyl function
$\Phi_{N}(z)=J^{-1}+U^{T}(A-zI)^{-1}U$ is a block resolvent
(Fractions determinant lemma exact at $z=-2$), and
$\Phi'\succeq0$ (live minimum eigenvalue $8.949\cdot10^{-9}$ on
$18/18$ admissible grid nodes).  The phase census is two-sided:
MAIN $74/74$ resolvable rows are classified positive-definite with
exact signature balance; the six terminal-dead character-twisted
rungs ($q_{N}>1$) are classified negative ($\chi_{3}$ $5/5$,
$\chi_{4}$ $1/1$).
\item
Pr\"ufer dictionary \gr{theorem-grade, round 372}: on the terminal
surface, the $\lfloor\theta/\pi\rfloor$-runs of the chain Pr\"ufer
phase coincide with the sign-runs of $v_{2}$ on $181/181$ windows,
with the XOR identity $\operatorname{sign}(c_{t})=\operatorname{sign}(w\cdot x\cdot v_{2})$
on the same $181$.  The dictionary is a theorem; the one-defect
extraction that would have closed the terminal half from a single
turning is \gr{refuted}.
\end{itemize}

\paragraph{Documented dead ends.}
Four internal routes are closed, each at a named place.
\begin{itemize}
\item
No $3\times3$ Jacobi Casoratian \gr{refuted, round 370}: dual $p$
and dual-measure Cauchy $q$ satisfy the three-term recurrence; the
border Cauchy sequence $r_{n}$ is not a homogeneous dual solution
(residual $3.615\cdot10^{-2}$ on the running instance).  There is
no $(p,q,r)$ Casoratian, so a matrix-Weyl index that would replace
Haynsworth by an oriented winding does not linearize.
\item
Phase restatement \gr{refuted, round 370}: the real-line census
index $\Xi=\operatorname{nneg}\Phi(0)$ \emph{is} Haynsworth inertia
in other coordinates.  The phase language does not replace the
inertia cut.
\item
$O(1)$ buffer of mass counting \gr{refuted, round 366}: proportional
Markov--Stieltjes counting in the pair gap predicts occupation
$Z=0$ ($nM_{I}/U=2.40\cdot10^{-5}$ on the running instance);
measured $Z=1$ lives in the $O(1)$ sandwich buffer, which cannot
separate $0$ from $1$ ($0/74$ scaled-band candidates).
\item
Edge pinning \gr{refuted, round 363}: dual OP zeros are not
exponentially node-pinned in the saturated block $\{1,2,3\}$
(Hurwitz maximum $0.462$ against bar $0.15$).  The zone is
dual-void / primal-saturated; a BKMM pinning argument at the
right hard edge does not apply.
\end{itemize}

\paragraph{Lean landing pad.}
Sorry census: five (two arithmetic holes
\texttt{lstar\_canonical}, \texttt{terminal\_q\_canonical}, now
the alternative route; the named dictionary
\texttt{pair\_terminal\_dictionary}; the opacity bridge
\texttt{mainWindow\_iff\_builtFromPrimeSource}; the remaining
Level-C arch statement
\texttt{arch\_elementwise\_stabilization}).
The entire R$^{\dagger}$ layer --- Haynsworth, the dual-resolvent
dictionary (A2)--(A5), (A7-min), the window bridge, the one-defect
absorption, and the r412 graph-resolvent identities --- consumes
no \texttt{sorryAx}.  A proof of (P1) and (P2) for all selected
windows beyond the certified head lands, via the kernel-checked
chain, at window-local master positivity.  That is a landing pad,
not a theorem of the family.  The live mincut is the named Prop
\texttt{selected\_augDualResolvent\_gt\_half}.

\section{Problem statement}\label{sec:problem}

\begin{problem}[Minimal inertia of the rest block]
\label{prob:minimal}
Let $(\mu,\nu)$ run over the admissible family of
\cite{lstar-note}, with dual kernel $R$ as in
Section~\ref{sec:def}, rest block $A_{0}=R_{N-3}-\half I$, and
$K_{2}=I+U^{T}A_{0}^{-1}U$ the two-rank Schur matrix of
Section~\ref{sec:minimal}.  Prove \textup{(P1)} and \textup{(P2)}
uniformly for the unbounded family: at most one negative inertia
direction in $A_{0}$, and $\det K_{2}\le -c_{0}<0$ on the
nneg-$1$ branch.  Which theorem in the Riemann--Hilbert analysis
of discrete orthogonal polynomials --- in particular, which
statement in the saturation regime of
Baik--Kriecherbauer--McLaughlin--Miller
\emph{\cite[Thm~2.7, Remark~2.8]{BKMM2007}} --- supplies these two
coarser facts?  The target is an integer mode count and the sign
of an $O(1)$ minor, not a rate.
\end{problem}

By Theorem~\ref{thm:haynsworth} a proof of
Problem~\ref{prob:minimal} gives $R\succ\half I$.  The R$^{\dagger}$
layer then lifts to $\Ld$ once the terminal Schur is in hand
(Proposition~\ref{prop:A}(A3),(A5), Lean-verified).  The mixed
identity \eqref{eq:mixed} is the bordered analogue: the same
Haynsworth additivity, with $J=\operatorname{diag}(1,1,1/\mathrm{den})$,
applies to $M^{\dagger}$.

\begin{problem}[Stronger saturation form]
\label{prob:rhp}
In the notation of Definition~\ref{def:rdagger}, which local
parametrix at a hard edge adjacent to a saturated band guarantees
\begin{equation}\label{eq:target}
\Rd\;\succ\;\half I
\end{equation}
uniformly, equivalently that the critical $3\times3$ Schur block
of $\Rd-\half I$ (binding pair plus border) stays
positive-definite?  This is the pre-367 target: a margin
asymptotic $S_{N}=N^{-\alpha}(M_{0}+\cdots)$ with relative error
control.  It implies Problem~\ref{prob:minimal} and is strictly
stronger.
\end{problem}

The finite input to either theorem is assembled above: reciprocal
weight \eqref{eq:uvee}; occupation duality \eqref{eq:occ}; a
census-grade saturated block $\{1,2,3\}$ with the binding pair
astride the edge $3\mid4$; the dichotomy
$\operatorname{ind}_{-}(A_{0})\le1$ on $74/74$ with (P2) on the
$45$-row nneg-$1$ branch; mixed form \eqref{eq:mixed}; observed
rate $N^{-3.332}$ (now optional); local-parametrix datum
$t_{\mathrm{geo}}$ margin-locked in $[0.222,0.287]$; error
control in relative/projective form, with rest-block theorem only
at $c=1$.  The AC/Bessel class is the wrong class
\cite{Lubinsky2008}.  A Gamma-class derivation at saturation
adjacency remains a candidate for the \emph{stronger} form and is
open.  Relative to that stronger form the cheaper target remains
(P1)--(P2).  The current externalisable architecture is the
open program of Section~\ref{sec:potapov}.

\section{The graph-resolvent form (rounds 407--412)}\label{sec:graph}

Rounds 407, 409 and 411 produced three equivalent finite-algebra
dictionaries for the rest-block count (P1).  Round 412 records them
as sorry-free matrix identities in
\texttt{rh/lean/RH/GraphResolvent.lean}.  Nothing below asserts (P1)
or $\Rd\succ\half I$ on the family; the mincut remains the named
Prop \texttt{selected\_augDualResolvent\_gt\_half}.  This is a
coordinate consolidation, not a claim.

\paragraph{Dictionary.}
Let $C\succ0$ be a real positive-definite Gram (on the campaign,
the $\mu$-only dual CD Gram on the hole set $Y$).  The
\emph{graph resolvent} is
\begin{equation}\label{eq:graphR}
R \;=\; C(I+C)^{-1}
 \;=\; (I+C^{-1})^{-1}
 \;=\; I-(I+C)^{-1}.
\end{equation}
The first form is the r407 intertwiner; the second is the
r356 dual resolvent of the L-ensemble kernel $E=C^{-1}$
(r409 graph identity).  Well-definedness, $0\prec R\prec I$, and
the spectral law $\operatorname{eig}(R)=\lambda/(1+\lambda)$ are
finite algebra \gr{lean-verified}.  The half-shift identity
\begin{equation}\label{eq:halfshift}
R-\half I
 \;=\;
 \half\,(I+C)^{-1}(C-I)
\end{equation}
is an equality of matrices.  Because $(I+C)^{-1}\succ0$, Sylvester
gives
\begin{equation}\label{eq:inertr}
\operatorname{ind}_{-}(R-\half I)
 \;=\;
 \operatorname{ind}_{-}(C-I).
\end{equation}
The factorization $I-C^{-1}=C^{-1}(C-I)$ is \emph{not} a
congruence.  The true sandwich is $C^{-1}(C-I)C^{-1}$, equivalently
the simultaneous eigenbasis, and yields the M\"obius law
\begin{equation}\label{eq:mobius}
\operatorname{ind}_{-}(C-I)
 \;=\;
 \operatorname{ind}_{-}(I-C^{-1}).
\end{equation}
If a rectangular factor satisfies $\Tfrak^{*}\Tfrak=C^{-1}$
(r409: the min-norm Birkhoff extension $\Tfrak_{0}$), the
energy split is
\begin{equation}\label{eq:esplit}
\|\Tfrak\|
 \;=\;
 1\big/\sqrt{\lambda_{\min}(C)},
\qquad
\|\Tfrak\|\le 1
 \;\iff\;
 C\succeq I,
\end{equation}
and ``at most one singular value $>1$'' is
$\operatorname{ind}_{-}(I-\Tfrak^{*}\Tfrak)\le1$, which is
\eqref{eq:inertr}--\eqref{eq:mobius} \gr{lean-verified}.

\paragraph{Four coordinates for (P1).}
The integer count (P1) is the same statement in four languages:
\begin{align}
\operatorname{ind}_{-}(R-\half I)&\;\le\;1,
\tag{inertia}\\
\lambda_{2}(C)&\;\ge\;1,
\tag{sampling}\\
\text{$\Tfrak_{0}$ has at most one singular value $>1$},
\tag{contraction}\\
C\succeq I
 \text{ except in at most one direction}.
\tag{energy split}
\end{align}
The zero-defect face $C\succ I$ together with the A5 Schur
$q^{\dagger}<1$ lifts to $\Rd\succ\half I$
\gr{lean-verified, \texttt{augDualResolvent\_gt\_half\_of\_C\_gt\_one}}.
The one-defect lift at the same depth is blocked by A7-min
(a one-defect of $R$ cannot be absorbed into $\Rd$ at the same
size).  Identifying the campaign Gram $C$ with $E^{-1}$ on a
$\mu$-ONB transcription is the named Prop
\texttt{GraphResolventIsLEnsembleInv}, the same class as
\texttt{P1EqCapInertia}; it is not a \texttt{sorry}.

\paragraph{Running-instance margins.}
On the frozen window $z=16$ ($S=367$, $N=184$, $|Y|=104$)
\gr{census, rounds 407--411}:
$C_{\min}=0.85712$,
$\lambda_{2}(C)=1.00018$,
$r_{\min}(R)=0.46153=C_{\min}/(1+C_{\min})$,
$\|\Tfrak_{0}\|=1.08014=1/\sqrt{C_{\min}}$.
Exact identity saturation $\|\Tfrak_{0}\|=1$ on already-PD
windows is \gr{refuted, round 411}:
$C_{\min}-1=+4.57\cdot10^{-8}$ at $k_{z}=42$,
$+4.9\cdot10^{-9}$ at $k_{z}=130$,
core-$42$ PD in $[+1.33\cdot10^{-7},+7.54\cdot10^{-5}]$.
These numbers certify the dictionary; they do not prove (P1)
off the certified head.

\paragraph{Closed internal languages.}
Table~\ref{tab:closed} lists twenty-eight named routes closed
between DCCXLVI and DCCXCVI: the original seventeen languages
for (P1), the two r409 graph-factor closures previously omitted
from the count, and the r413--r429 bulk and terminal kills.
Each line is a sealed probe record, not an extrapolation.
Nothing in the table is a family theorem.

\begin{table}[ht]
\centering
\caption{Twenty-eight named closed routes (bulk and terminal),
DCCXLVI--DCCXCVI.  Verdicts are probe-local.  No family
theorem is claimed.}
\label{tab:closed}
{\small
\begin{tabular}{@{}llp{5.4cm}@{}}
\toprule
Language & Round & Verdict \\
\midrule
$L^{\infty}/d_{\mathrm{arm}}$ majorant
  & r381, DCCXLVI & too coarse \\
Fold-parity pairing as SATZ
  & r383, DCCXLIX & residual $0.512$ \\
Finite-head Chebyshev / family-uniform $Z$
  & r383/r386, DCCL--DCCLI & $\chi$ mutants \\
Gershgorin/Schur assist
  & r387, DCCLII & $k^{*}\sim0.11\,N$ \\
OSC-Geronimus / Neumann $R_{2}$
  & r388, DCCLIII & $|\mathrm{quad}|/|\mathrm{lin}|\sim0.86$ \\
Checkerboard / $M$-matrix
  & r394, DCCLIX & agree $0.504$ \\
Three-gap mask
  & r395, DCCLX & $n_{\mathrm{uniq}}\in[8,18]$ \\
Isolation of the fold mask
  & r396, DCCLXI & pair count does not $\to0$ \\
Source Weyl energy decay
  & r399, DCCLXV & $E^{\mathrm{bulk}}$ grows; RH-near \\
Bulk FORM\_T / FORM\_P
  & r400, DCCLXVII & rank-$1$ / phase predictor \\
(P1) as construction class
  & r403, DCCLXVIII & \textsc{refuted} \\
Moments / Euler source Gram
  & r404, DCCLXXI & residual $209$ \\
Ones-mode / lift overflow
  & r404--r405, DCCLXXI--II & $\cos0.007$; dead $\chi$ keep $\|c\|<1$ \\
$\nu$-rank one / Loewner sandwich
  & r407, DCCLXXIII & $N_{\nu}=104$ \\
Nyquist density $\half$ / $\mathrm{diag}\,C\approx1$
  & r408, DCCLXXV & density $0.283$ \\
Literal $\Theta=C^{T}\Phi_{N}C$
  & r409, DCCLXXVI & Haynsworth template, not the Schur \\
Unfitted Krein transport
  & r409, DCCLXXVI & not a (P1) proof \\
Testpoly / Fourier / Bernstein / SEQ
  & r410, DCCLXXVII & factor $90$; off-fraction $0.968$ \\
Identity saturation $\|\Tfrak_{0}\|=1$
  & r411, DCCLXXVIII & $C_{\min}-1=+4.57\cdot10^{-8}$ \\
Hole top mode $v_{\mathrm{top}}$
  & r413, DCCLXXXII & leftover $\sigma=1.024$ \\
Top-mode as edge defect
  & r415, DCCLXXXI & $\cos0.6766/0.058$; $H$ PD $18/42$ \\
de~Branges phase dominance $\kappa\le1$
  & r416, DCCLXXXIII & $\mathrm{yy}_{A}=3$; $\mathrm{corr}=-0.17$ \\
Uniform $\varphi_{bb}<0$
  & r418, DCCLXXXV & vacuous overflow $6/14$ \\
Hoped $O(1)$ den-gap as SATZ
  & r420, DCCLXXXVII & reserve shrinks $0.105\to0.038$ \\
Razor-overlap as global death coordinate
  & r428, DCCXCIV & vacuous-chart only \\
$C_{\min}$ Nyquist face
  & r428, DCCXCIV & $\cos=-0.007$ on VAC \\
Unsigned $|\Zloc|$ triangle
  & r429, DCCXCVI & $[2.240,19.245]$; slope $+0.719$ \\
Chebyshev $\psi$ / left--right Abel
  & r429, DCCXCVI & $\mathrm{pair}_{c}=1$; no $k_{0}$ \\
\bottomrule
\end{tabular}}
\end{table}

\paragraph{Lean layer.}
\texttt{RH/GraphResolvent.lean} is sorry-free.  The axiom audit
(\texttt{RH/Audit.lean}, section (m)) prints
\texttt{[propext, Classical.choice, Quot.sound]} on the identities
\eqref{eq:graphR}--\eqref{eq:esplit}, on the four-coordinate
bridge, and on the zero-defect composition with $\Rd$.
The pilot \texttt{sorry} census stays five.  The live remainder is
still the CD identification plus the depth gap $N-3$ versus the
cap, i.e.\ the named mincut
\texttt{selected\_augDualResolvent\_gt\_half}.

\section{The edge balance chain (rounds 417--425)}\label{sec:edgebal}

Rounds 417--424 produced eight finite identities for the edge Schur
scalar $\mathrm{sch}<0$; round 425 reduced the last chain step
$\gamma<1$ to the already-named terminal ratio $q_N<1$.  Round 426
records the abstract halves in
\texttt{rh/lean/RH/EdgeBalance.lean}.  Nothing below asserts
$\mathrm{sch}<0$ cofinally, nor $\Rd\succ\half I$, on the unbounded
family.  This is a coordinate consolidation, not a claim.

\paragraph{Eight identities.}
Write $A_0=R_{N-3}-\half I$ on the hole nodes, $U_{\mathrm{CD}}$ the
last two dual Christoffel--Darboux columns, $s$ the Sherman--Morrison
border, and $\mathrm{den}>0$ the border scale.  The mixed
$3\times 3$ is
\[
\Phi
=\begin{pmatrix} K_2 & c_0\\ c_0^T & \varphi_{bb}\end{pmatrix},
\quad
K_2=I+U_{\mathrm{CD}}^TA_0^{-1}U_{\mathrm{CD}},
\quad
\varphi_{bb}=\mathrm{den}-2+s^TA_0^{-1}s,
\]
and $\mathrm{sch}$ is the Schur complement of $K_2$ in $\Phi$.
After the $2\times 2$ Sylvester map of $K_2$, write
$(\tilde a,\tilde b)^T=D_2^Tc_0$ for the \emph{unnormalized}
couplings and $\tau^2=\tilde a^2+\tilde b^2$.

\begin{enumerate}
\item \textbf{Woodbury and the unnormalized chart}
\gr{exact, lean-verified}.
\[
\mathrm{sch}
=\mathrm{den}-2+s^T(A_0+U_{\mathrm{CD}}U_{\mathrm{CD}}^T)^{-1}s
=\varphi_{bb}-c_0^TK_2^{-1}c_0.
\]
The first form is the r406 discriminant $\Delta$ at $J=I$:
$\mathrm{sch}=(\mathrm{den}-1)-\Delta$.  At $\mathrm{den}=1$ one
has $\mathrm{sch}<0$ if and only if the one-defect update is
positive definite.  The chart of $J_2=\operatorname{diag}(\varepsilon_a,\varepsilon_b)$
with $\varepsilon\in\{\pm1\}$ is the $3\times 3$ Schur
$\mathrm{sch}=\varphi_{bb}-(\varepsilon_a\tilde a^2+\varepsilon_b\tilde b^2)$,
because $J_2^{-1}=J_2$:
\begin{align*}
\text{P1 }(\varepsilon=(-1,+1))&\colon
  \mathrm{sch}=\varphi_{bb}+\tilde a^2-\tilde b^2,\\
\text{vacuous }(\varepsilon=(+1,+1))&\colon
  \mathrm{sch}=\varphi_{bb}-(\tilde a^2+\tilde b^2),\\
\text{tot }(\varepsilon=(-1,-1))&\colon
  \mathrm{sch}=\varphi_{bb}+\tilde a^2+\tilde b^2.
\end{align*}
Rational toys: $\mathrm{sch}=-2/3$ (P1) and $-7/6$ (vacuous).

\item \textbf{One balance}
\gr{exact, lean-verified}.
On both charts $\varphi_{bb}=c_J+\Sigma$ with
$c_J=\mathrm{den}-2$ and $\Sigma=s^TA_0^{-1}s$.
Flagship $z=16$: $-0.0856=-0.3989+0.3133$.

\item \textbf{Vacuous $\tau^2$-separator}
\gr{exact, lean-verified}.
On the vacuous chart,
$\mathrm{sch}<0\iff\tau^2>\varphi_{bb}$.  In particular
$\varphi_{bb}<0$ forces $\mathrm{sch}<0$ for every coupling.
On the $\varphi_{bb}>0$ class this is the exact life/death cut
(live overflow iff $\tau^2>\varphi_{bb}$; dead vacuous
$\chi_3$-$23/33$, $\chi_4$-$20$ iff $\tau^2<\varphi_{bb}$).

\item \textbf{Branch $\tau$-dynamics}
\gr{census}.
Vacuous $|\tau|$ slope $-1.24$ (vanishes); P1 slope $+0.05$
(flat).  The r417 decay was vacuous-driven.  Six core overflow
windows live now by $\tau^2>\varphi_{bb}$ (margins
$0.0015$--$0.046$) and do not cross $\varphi_{bb}=0$
(slope $-0.003$); they are finite exceptions.

\item \textbf{Stieltjes form of $\Sigma$}
\gr{exact}.
Via the r407 dictionary $A_0^{-1}=2(C+I)(C-I)^{-1}$,
\[
\Sigma
=2\|s\|^2+4\,s^T(C-I)^{-1}s
=2\sum_i\mathrm{occ}_i\,g(\lambda_i),
\]
the Stieltjes transform of the occupation measure at $z=1$.
Near-$1$ mass stays $O(0.01)$: $n_{\mathrm{eff}}$ growth is bulk
broadening, not pole-creep.  No elementary closed occupancy
density (one-pole relative error $0.73$; Cauchy two-moment
$0.95$).

\item \textbf{den formula}
\gr{exact, lean-verified}.
\[
\mathrm{den}
=1+\gamma-v_t\cdot s,
\qquad
\gamma=\|b\|^2/B_w,
\qquad
B_w=S_{N-2}+\tfrac57,
\]
and $\mathrm{den}<2\iff\gamma<1+v_t\cdot s$.  If $\gamma<1$ and
$v_t\cdot s\ge 0$ then $\mathrm{den}<2$ even at vanishing
correction.  Census: $\mathrm{den}\in[1.460,1.652]$ on all $42$
(gap to $2$ at least $0.348$); $v_t\cdot s\in[0.061,0.096]$.

\item \textbf{$b$-Parseval}
\gr{exact; identification named in Lean}.
$b_k=\langle\sigma,\hat\pi_k^\mu\rangle$ with $\sigma$ the signed
border measure, so $\|b\|^2$ is the $\mu$-side Parseval budget
(w9 residual $4\cdot10^{-14}$; $\mathbb{Q}$-toy
$b=(3/5,4/5)$, $\|b\|^2=1<16/9$).  Termwise
$w_k=b_k^2/\rho_k$ is not uniform ($\sim 43\%$ of terms $>1$).

\item \textbf{Floor and three saturation scales}
\gr{census}.
Selected $a_k=2^k$ prefers $R=-\varphi_{bb}=R_\infty+c\,k^{-\alpha}$
with $R_\infty\approx +0.030$ against decay-to-$0$
($\Delta\mathrm{AIC}\,6.9$) and against $c/\log k$ (killed,
$\Delta\mathrm{AIC}\,18.4$).  EXT-vacuous sits independently in
$[0.0285,0.0373]$.  Three scales, not one exponent: budget gap
$2-\mathrm{den}\sim O(0.5)$ flat; razor
$C_{\min}-1\sim 10^{-7}$ with
$\|\mathfrak{T}_0\|-1=-(C_{\min}-1)/2$ exactly (ratio $1.0000$);
edge reserve $R$ of order $0.03$.
\end{enumerate}

\paragraph{Branch map.}
Exact separators, not a fitted partition:
\begin{center}
\small
\begin{tabular}{@{}p{3.2cm}p{3.6cm}p{6.2cm}@{}}
\toprule
branch & chart / $K_2$ & live criterion \\
\midrule
P1 ($28/42$ core) &
  $J_2=\operatorname{diag}(-1,+1)$ &
  $\mathrm{sch}=\varphi_{bb}+\tilde a^2-\tilde b^2<0$
  (hyperbola; danger axis large $|\tilde a|$) \\
vacuous ($14/42$) &
  $J_2=I_2$ &
  $\varphi_{bb}<0$, or $\tau^2>\varphi_{bb}$ on the
  $\varphi_{bb}>0$ overflow class \\
tot / dead $\chi$ &
  $J_2=-I$ or $\mathrm{sch}>0$ &
  vacuous death: $\varphi_{bb}>0$ and $\tau^2<\varphi_{bb}$;
  P1 death: $\tau$-terms, $\varphi_{bb}<0$ retained \\
\bottomrule
\end{tabular}
\end{center}
Round 425: $\|b\|^2\le S$ is the kernel Loewner
$K_\mu\preccurlyeq K_{\mu-\nu}$ on $P_{<N}$ \gr{exact};
$\|b\|^2<B_w$ is that identity plus $q_N<1$.  On the $42$-rung
$q_N<1$ census this composes to $\gamma<1$.  Dead $\chi$ have
$q_N>1$ and need kernel slack; they keep $\gamma<1$ nonetheless.

\paragraph{Two open limit questions.}
The identities above are finite.  What remains, for the unbounded
selected sequence $a_k=2^k$, are two existence statements.  They
are the cheapest remaining targets; neither is a Riemann--Hilbert
steepest-descent, and neither is a statement about the Riemann
hypothesis.

\begin{problem}[cofinal $q_N<1$]\label{prob:qn}
On the selected sequence, prove $q_N<1$ (equivalently
$S<B_w=S_{N-2}+\tfrac57$) for all large $k$.  Together with the
Loewner identity $\|b\|^2\le S$ this yields $\gamma<1$, hence
$\mathrm{den}<2$ by the formula of item 6, even at
$v_t\cdot s=0$.  The $42$-rung census is already certified
(v960); cofinality is the named mincut
\texttt{selected\_augDualResolvent\_gt\_half}.
\end{problem}

\begin{problem}[constructed $\Sigma_\infty$]\label{prob:siginf}
Construct $\mathrm{den}_\infty<2$ and $\Sigma_\infty$ with
\[
\limsup_k\Sigma_k
\;<\;
2-\mathrm{den}_\infty
\;=\;
|c_{J,\infty}|,
\]
equivalently a strictly positive floor $R_\infty>0$ for
$R=-\varphi_{bb}$.  The occupation Stieltjes form is exact; a
closed occupancy density is refuted; near-$1$ mass is stable
$O(0.01)$.  The census prefers $R_\infty\approx 0.030$
(two independent lines: selected AIC, EXT-vacuous band).
No $k_0$ is proved.  Depth $k=10$ border-fails computationally.
\end{problem}

\paragraph{Lean layer.}
\texttt{RH/EdgeBalance.lean} is sorry-free.  The axiom audit
(\texttt{RH/Audit.lean}, section (n)) prints
\texttt{[propext, Classical.choice, Quot.sound]} on the Woodbury-sch
corollary, the chart trichotomy, the $\tau^2$-separator, the den
formula and the $\gamma$-bridge
$\|b\|^2\le S<B_w\Rightarrow\gamma<1\Rightarrow\mathrm{den}<2$.
Named Props \texttt{BorderIsMuParseval}, \texttt{BorderLoewnerLeS},
\texttt{QNLtOne} are hypotheses, not holes: they name the
window transcription of items 7--8 and Problem~\ref{prob:qn}.
The pilot \texttt{sorry} census stays five.
Rounds 428--429 retype the cheapest remaining terminal
statement on the selected sequence
(Section~\ref{sec:cores}); cofinality stays open.

\section{The two cancellation cores (rounds 413--429)}
\label{sec:cores}

The graph dictionary reduces the bulk hole to
$\lambda_{2}(C)\ge1$; the edge chain reduces the terminal hole
to $q_{N}<1$, equivalently $|\Zloc|\le\half$ on the selected
sequence once COMPOSE$^{-}$ is applied.  After r429 both
remainders are the same kind of object: an unsigned
cancellation of explicit von~Mangoldt data that has survived
every majorant, structure, phase, sampling, kernel, and
interpolation language of the campaign.  The identities below
are finite; the bounds are not.  This is a consolidation, not
a claim.

\paragraph{Bulk: window-emergent one-defect.}
On the flagship window $z=16$,
$C_{\min}=0.85712$, $\lambda_{2}(C)=1.00018$,
$\|\Tfrak_{0}\|=1.08014$.  Four candidate vectors for the
single excess direction are named and closed:
\begin{center}
\small
\begin{tabular}{@{}llp{6.4cm}@{}}
\toprule
Candidate & Round & Independent numbers \\
\midrule
ones-mode $v\equiv 1$
  & r408 & $|\langle v,1\rangle|=0.007$ \\
centred mass difference of $A_{0}$
  & r400 & cosine $0.0075$ with the genuine negative mode \\
Fourier / hole-Nyquist face $v_{F}$
  & r410 & overlap $0.386$ with $C$-ev$_{0}$;
           $\|\Tfrak_{0}v_{\mathrm{Nyq}}\|<\|\Tfrak_{0}\|$ \\
hole top OP $v_{\mathrm{top}}$
  & r413 & $\mathrm{corr}(v_{\mathrm{top}},C\text{-ev}_{0})=0.6766$;
           leftover $\sigma=1.02354$;
           $\lambda_{\min}(Q(I-\Tfrak_{0}^{*}\Tfrak_{0})Q)=-0.0476$ \\
\bottomrule
\end{tabular}
\end{center}
Quadrature dominance already fails for $p\equiv1$: the mass
asymmetry of a single full-carrier dual is
$\sum_{X}u^{\vee}/\sum_{Y}u^{\vee}=1.357130$
(r413; r427 route A $=$ route B at $3.9\cdot10^{-12}$; a
hypothetical subset-$P'$ bug would have given $1.525$ and is
not live).  The min-norm interpolant \emph{does} save the
constant direction, $\|\Tfrak_{0}v_{1}\|=0.359<1$, and does
\emph{not} save the leftover excess.  Round 415: $v_{\mathrm{top}}$
is not the bulk defect at depth either
($\cos=0.6766$ at $k_{z}=9$, $0.058$ at $k_{z}=18$).
Round 416: Hermite--Biehler interlacing holds, but phase
dominance with Potapov index $\le1$ fails
($\mathrm{yy}_{A}=3\not\le1$, $\mathrm{corr}=-0.17$ with the
$\Tfrak_{0}$-index).  The defect is window-emergent: no
source-pure vector known before the Gram is built is the
excess.  Circularity verdict of r399: $E^{\mathrm{bulk}}$
grows as $N_{w}^{0.54}$ and sits at quadratic-mean; a bound
forcing decay below that level is RH-near, hence circular as a
strategy for $\Rd\succ\half I$, and is in any case contradicted
by the census.

\paragraph{Terminal: the $\Zloc$ formula.}
On a canonical window the terminal drive splits
\gr{exact, Lean \texttt{canonical\_split}}
\begin{equation}\label{eq:zloc}
Z \;=\; t_{N-2}+\mathrm{chain},
\qquad
\Zloc \;=\; t_{\mathrm{loc}}+\mathrm{chain},
\end{equation}
with $t_{\mathrm{loc}}$ the hull-edge sum at geometric mask
$F=1/5$ (not the set of small prime powers)
\cite{zloc-head}.  Reconstruction
residual $10^{-14}$.  Live pins $k=3,4,5$ reproduce
$0.486846$, $0.157211$, $0.227187$.  Edge cardinalities on
selected occupy $85$--$1691$.  The formula exhibits death of
the $\chi$ mutants ($1.021$ and $0.892$, both $>M$) and
reproduces the old $k_{z}=16$ razor $0.756$.
Unsigned triangle, Chebyshev $\psi$, and left--right Abel are
all closed on selected: the triangle occupies
$[2.240,\,19.245]$ with two-point slope $+0.719$; cancellation
of the signed head against that triangle is a factor
$0.013$--$0.145$.  Selected is not milder for majorants than
the old $42$-rung ladder.  There is no explicit $k_{0}$.

\paragraph{Selected location change.}
On the selected sequence $a_{k}=2^{k}$ ($k=3..9$, builder pin
at $k=8$) one has
$q_{N}\in\{0.3828,0.2143,0.0228,0.00176,0.00151,\text{[pin]},0.00070\}$,
falling in $k$, with minimum margin to $1$ equal to $0.617$
against $0.0195$ on the old $42$-rung ladder
($k_{z}=16$: $q_{N}=0.9805$)
\cite{qn-reopened}.  The razor rungs of the old
ladder are a quantifier artefact: the minimal quantifier of
round 397 skips them.  COMPOSE$^{-}$ premises on selected are
comfortable ($R=S_{F}/D\le1.40\ll4$, $L_{1}\le0.63\ll3$,
$|\Zloc|\le0.487<\half$).  The sharpest remaining sublemma is
$|\Zloc(W_{k})|\le\half$ on MAIN-selected (air $0.013$ at
$k=3$, scramble-unstable at $0.524$).  Hoped $L_{1}\le3/5$
fails at $k=9$ ($0.628$); $2/3$ still holds as census.
Honest terminal balance: identity \eqref{eq:zloc} and
COMPOSE$^{-}$ are theorems; $|\Zloc|\le\half$, $R\le3/2$,
$L_{1}\le2/3$ are finite censuses; $k=8$ is a disclosed pin;
$k\to\infty$ is open.

\section{Audit (round 427)}\label{sec:audit}

Round 427 re-computed seven load-bearing campaign numbers of
R397--R426 on independent code paths (r367 block
$A_{0}+\half I$ versus dense Chebyshev--Hankel $C$;
full-carrier product dual; KKT $\|\Tfrak_{0}\|$; truncated
positive-definite Grams; AIC re-fit; Woodbury-sch).  Search
raster: truncation/depth mismatch, signed versus occupied
chain mix-up, shared-builder bugs, pin circularity, float64
on $10^{-8}$ razors.  Ausgang:
\textbf{0 BUG / 2 SUSPECT / 5 CLEAN}.
No round verdict of R397--R426 changes.  This is an audit of
recorded numbers, not a theorem of the wall
\cite{campaign-audit}.

\begin{table}[ht]
\centering
\caption{Round-427 verdict per target.  Independent
re-computation, then CLEAN / BUG / SUSPECT.}
\label{tab:audit}
{\small
\begin{tabular}{@{}clp{8.2cm}@{}}
\toprule
T & Verdict & Independent numbers \\
\midrule
1 & CLEAN
  & $R=C(I+C)^{-1}$ on $k_{z}=9,17,12$;
    $\|R_{f}-R\|_{\mathrm{w9}}=3.38\cdot10^{-12}$;
    $\mathrm{ind}_{-}=\#\{\lambda(C)<1\}$ ($1,1,0$);
    mpmath dps-$40$ relative $\le1.5\cdot10^{-14}$. \\
2 & CLEAN
  & $\sum_{X}u^{\vee}/\sum_{Y}u^{\vee}=1.357130$ from one
    full-carrier dual; subset-$P'$ would have given $1.525$. \\
3 & CLEAN
  & $\|\Tfrak_{0}\|_{\mathrm{w9}}=1.080138437324$;
    $\kappa(C)=2.086\cdot10^{3}$ so the
    $4.57\cdot10^{-8}$ razor is $56\times$ eigvalsh noise;
    r409 print $1.00000$ is display rounding. \\
4 & CLEAN
  & $K^{\mu-\nu}\succeq K^{\mu}$ on truncated PD Grams $n=16$
    (evmin $\sim-10^{-9}$).
    \emph{Definiteness hypothesis:} inversion monotonicity
    holds only when the tilde form is positive definite;
    an indefinite $\mathbb{Q}$-toy
    ($\mu=(3,1)$ at $(0,1)$, $\nu=(2)$ at $1$, depth $2$)
    has $\lambda_{\min}(G(\mu-\nu))=-1.30$ and
    $\lambda_{\min}(K_{t}-K_{\mu})=-2$.  The campaign states
    the tilde chain is definite; the implication then holds.
    The hypothesis is part of the recorded theorem, not a
    silent extra. \\
5 & SUSPECT
  & vs.\ $k$: M1 $R_{\infty}=+0.02983$ (r421 $+0.02982$);
    vs.\ $N$: winner M3 ($N=5690$ at $k=8$, $N=1433$ at $k=9$);
    $k=8$ is a disclosed builder pin (r421$\to$r425
    circularity).  Floor remains a census. \\
6 & CLEAN
  & $6/6$ dead:
    $q_{N}>1\Leftrightarrow|\Zloc|_{\mathrm{term}}\ge\sqrt{5/7}
    \Leftrightarrow\mathrm{sch}>0$;
    live $\chi_{3}$-$9$ does not fire.
    $C_{\min}-1$ is \emph{not} a death coordinate
    (three dead windows have $C_{\min}>1$ and die at the
    edge). \\
7 & SUSPECT
  & no $n_{\mathrm{ref}}$/float32 in the $17$ campaign probes;
    razor $C_{\min}$ in float64 \texttt{eigvalsh};
    precision ward: a window with $\kappa(C)\gtrsim10^{8}$
    would make a $10^{-8}$ gap undecidable
    (has not occurred; $k_{z}=42$ was not native-Hankel
    rebuilt). \\
\bottomrule
\end{tabular}}
\end{table}

The two historical artefact classes (r395 $n_{\mathrm{ref}}=60$;
r381 signed versus occupied Fej\'er) are absent from the
campaign probes.  The r428 location change (selected margin
$0.617$) stands on audited numbers.

\section{Current architecture (Potapov program)}
\label{sec:potapov}

After the symmetric closing line of Section~\ref{sec:cores}
the remaining work is not a further lemma-first grind on
$\lambda_{2}(C)$ or $\Zloc$.  Both holes are restated as two
steps of one generalised Schur process in the Krein--Langer
class $\Sk_{\kappa}$ ($\kappa=$ number of negative squares of
the kernel $K_{\Theta}$).  Status of every statement in this
section: \gr{open program}.  Nothing below is proved on the
unbounded family, and nothing is a claim about the Riemann
hypothesis.

\paragraph{Two quantifier weakenings.}
Elementwise extraction from a selected window does not need a
strict spectral gap.

\begin{problem}[Semidefinite extraction]
\label{prob:semidef}
Prove that $\Rd_{k}\succeq\half I$ (equivalently, the
augmented dual form is positive \emph{semidefinite}: the
window pairing is nonnegative) already yields the window
contribution of that $k$.
A strict free-window margin is not required.
Named Lean target:
\texttt{Rdagger\_ge\_half\_iff\_augmented\_posSemidef}.
\end{problem}

\begin{problem}[Frequently selected]
\label{prob:freq}
Prove: $\forall K\,\exists k\ge K$ with
$\Rd_{k}\succeq\half I$ implies that the Weil quadratic form
is nonnegative on every admissible test function (each test
function instantiates only finitely many windows).  In
particular, a subsequence of positive density of good windows
suffices.  Named Lean targets:
\texttt{weil\_nonneg\_of\_frequently\_selected},
\texttt{rh\_of\_frequently\_selected}.
The second name is historical: the statement is window-local
positivity of the Weil form, not a claim about zeros of
$\zeta$.
\end{problem}

Fallback route, still \gr{open program}: if a block average of
Potapov indices satisfies
\[
\frac1K\sum_{k=1}^{K}\kappa(\Theta_{k}^{\dagger})
\;<\;1,
\]
then some window in the block has index $0$.  Mean-value
theorems for Dirichlet polynomials apply to this averaged
index in a way the pointwise r399 majorant could not (that
majorant was circular).

\paragraph{Bulk and edge as Schur steps.}

\begin{problem}[Bulk in $\Sk_{\le1}$]
\label{prob:sleq1}
On every selected window, construct a rational transfer
matrix $\Theta_{k}$ with $\Theta_{k}\in\Sk_{\le1}$.  The
defect vector need not be known in advance: at index $1$ one
has $\Theta=B_{\alpha}^{-1}S$ with a wandering Blaschke
location $\alpha_{k}$.  This is the language in which
window-emergent, collective, birth-at-$k=37$, and
Nyquist-face-without-eigenvector are the same statement.
\end{problem}

\begin{problem}[Edge in $\Sk_{0}$ via Redheffer]
\label{prob:s0}
Given Problem~\ref{prob:sleq1}, prove
$\Theta_{k}^{\dagger}\in\Sk_{0}$ by a Redheffer completion
whose pivot is
\begin{equation}\label{eq:delta}
\delta_{k}
 \;=\;
 c\bigl(\tfrac14-|\Zloc|^{2}\bigr)
 \;=\;
 c'(-\mathrm{sch}).
\end{equation}
The sought identity is the Schur-parameter law
$\delta=1-|\alpha_{\mathrm{edge}}|^{2}$
(energy conservation, not a triangle inequality; the
$2.2$--$19\times$ unsigned loss of r429 is the wrong
currency).
\end{problem}

\paragraph{Source Potapov completion (target form).}
A construction
$\Theta_{k}=\Theta_{\infty}\star\prod_{p}\Theta_{p,k}$
(Redheffer product; elementary factors from $p^{r}$,
$r\log p$, $\Lambda=\log p$; $\Theta_{\infty}$ the
Gamma/pole/completion factor) is an open program if it
satisfies all four:
\begin{enumerate}
\item[(A)] Pick matrix of $\Theta$ isomorphic to
  $I-\Tfrak_{0}^{*}\Tfrak_{0}$;
\item[(B)] source-pure constructor (forbidden inputs:
  eigenvalues of $C$, SVD of $\Tfrak$, target Schur
  complement, $\Zloc$ as a constructor);
\item[(C)] $\kappa(\Theta)\le1$ read from the factorisation,
  before the target matrix is assembled;
\item[(D)] edge Redheffer with the $\delta$-identity
  \eqref{eq:delta} $\Rightarrow$ $\kappa(\Theta^{\dagger})=0$
  $\Rightarrow$ $\Rd\succeq\half I$.
\end{enumerate}
The index is counted from the source factorisation, not
after the target is built (the r416 de~Branges route counted
afterwards).  Permutation is allowed to jump the index
($1\to20$--$100$) by breaking the
$p^{r}\leftrightarrow r\log p\leftrightarrow\log p$ pairing;
a $\chi$-mutant may keep the elementary phases (bulk budget
$1$ survives, the edge factor dies).  That pattern matches
the sealed census.  Round 399 is not reopened: the statement
is a topological product, not an absolute-value majorant.

These four problems, together with
Problems~\ref{prob:qn} and~\ref{prob:siginf}, are the
externalisable remainder.  They are not theorems, not
landing pads, and not claims.

\section{What a proof would give, and what it would not}\label{sec:status}

A proof of Problem~\ref{prob:minimal} for the unbounded family
would give, in order:
$R\succ\half I$
by Theorem~\ref{thm:haynsworth} \gr{lean-verified finite algebra};
then $\Rd\succ\half I$ once $q^{\dagger}<1$, equivalently $\Ld$,
by Proposition~\ref{prop:A}(A3),(A5)
\gr{theorem-grade and lean-verified};
$\Ld\;\Rightarrow\;\Ls\wedge\mathrm{Terminal}$
and
$\Ls\wedge\mathrm{Terminal}\;\Rightarrow$
positivity of every bordered window matrix through the cap
\gr{Lean, sorry-free on the reconstruction layer, axiom audit
\texttt{RH/Audit.lean}}.  That is window-local master positivity.
A proof of the stronger form \eqref{eq:target} would give the same
chain without a separate terminal input.  A proof of the open
program of Section~\ref{sec:potapov} would give the same chain
with the two quantifier weakenings (semidefinite; frequently)
and without identifying the defect vector in advance.
None of these is an asymptotic law, a bound on the Riemann
$\xi$-function, or a statement about the Riemann hypothesis in
either direction.

What is on the table now is a finite census (at most $85$ windows
for the hole kernel, $75$ plus one deep row for the border, four
sealed obstacle instances for the partition, $74$ resolvable rows
for the two-rank dichotomy, $181$ windows for the Pr\"ufer
dictionary) together with exact finite-matrix identities and a
sorry-free R$^{\dagger}$ Lean layer.  No Riemann--Hilbert
steepest-descent analysis has been carried out.  The
equilibrium-measure limit, the Gamma parametrix, and the error
operator $E$ are open, and they are no longer the cheapest target.
The cheapest remaining targets are the four open-program
statements of Section~\ref{sec:potapov} together with
Problems~\ref{prob:qn} and~\ref{prob:siginf}.
Comparison inputs show that the identities survive source
destruction while positivity and the saturated partition do not
--- so an existence proof for (P1)--(P2) must consume the
construction, not merely the dual algebra.

Table~\ref{tab:w9} records the running instance.  Every number is a
sealed probe record.  Numbers not in those records are omitted.

\begin{table}[ht]
\centering
\caption{Running instance $z=16$ ($S=367$, $N=184$, pair folds
$(2,4)$).  Dual quantities: round 356/359.  Occupation and
obstacle: round 360.  Bordered quantities: round 362.
Two-rank inertia: round 367.}
\label{tab:w9}
\begin{tabular}{lr}
\toprule
quantity & value \\
\midrule
$\lambda_{\min}(R)$ & $0.500041882$ \\
$\lambda_{\min}(\Rd)$ & $0.500041459$ \\
$\operatorname{margin}$ / $\operatorname{margin}^{\dagger}$ &
  $1.6752\cdot10^{-4}$ / $1.6582\cdot10^{-4}$ \\
$\operatorname{bind}$ / $\operatorname{bind}^{\dagger}$ &
  $1.0029$ / $1.0028$ \\
cross share / share$^{\dagger}$ & $0.6973$ / $0.6990$ \\
$t_{\mathrm{geo}}$ & $0.2646$ \\
$o_{\mathrm{dual}}$ at folds $1,2,4$ & $0.1497$, $0.5118$, $0.5137$ \\
QP dual-void block (KKT gap) & $\{1,2,3\}$ ($+0.092$) \\
$q^{\dagger}$ / border Schur & $0.933044$ / $1.7319\cdot10^{-2}$ \\
$\operatorname{ind}_{-}(A_{0})$ / $\det K_{2}$ &
  $1$ / $-5.0389$ \\
$-\det K_{2}$ (nneg-$1$ family) & $[1.157,\,1126.389]$ \\
\bottomrule
\end{tabular}
\end{table}

\medskip\noindent
\textbf{Claim boundary.}\enspace
Research documentation.  Not a proof of $\Ls$, of $\Ld$, of
\textup{(P1)}--\textup{(P2)}, of $\Rd\succ\half I$, or of
Problems~\ref{prob:semidef}--\ref{prob:s0} for the
unbounded family.  Not evidence for or against the Riemann
hypothesis in either direction.

\begin{thebibliography}{12}

\bibitem{Borodin2002}
A.~Borodin,
\emph{Duality of orthogonal polynomials on a finite set},
J.~Statist.\ Phys.\ \textbf{109} (2002), 1109--1120.

\bibitem{BKMM2007}
J.~Baik, T.~Kriecherbauer, K.~T.-R.~McLaughlin and P.~D.~Miller,
\emph{Discrete Orthogonal Polynomials: Asymptotics and Applications},
Ann.\ of Math.\ Studies \textbf{164}, Princeton Univ.\ Press, 2007.
Theorem~2.7 and Remark~2.8.

\bibitem{Lubinsky2008}
D.~S.~Lubinsky,
\emph{A new approach to universality at the edge of the spectrum},
Int.\ Math.\ Res.\ Papers (2008).

\bibitem{lstar-note}
S.~Hamann,
\emph{A strict polynomial subordination inequality for two explicit
prime-generated discrete measures (open problem)},
TFPT research repository, \texttt{rh/problem/lstar\_problem.tex},
August 2026.

\bibitem{campaign-audit}
S.~Hamann,
\emph{Campaign audit R397--R426},
TFPT research repository, \texttt{rh/problem/campaign\_audit.tex},
August 2026.

\bibitem{qn-reopened}
S.~Hamann,
\emph{$q_{N}$ reopened on the selected sequence},
TFPT research repository, \texttt{rh/problem/qn\_reopened.tex},
August 2026.

\bibitem{zloc-head}
S.~Hamann,
\emph{The selected head bound},
TFPT research repository, \texttt{rh/problem/zloc\_head.tex},
August 2026.

\end{thebibliography}

\end{document}
