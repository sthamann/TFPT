% jp_increment.tex -- round 457, arithmetic increment from J_P.
% INCREMENT_DRIFT / RACE_EATS_K10 / LEMMA_REDUCED.
% Builds with pdflatex.
% Companion machine check: verify_jp_increment.py.
% Discovery: experiments/tfpt-discovery/jp_increment_probe.py.
% NO RH CLAIM.  NO anti-RH claim.
\documentclass[11pt]{article}

\usepackage[a4paper,margin=2.7cm]{geometry}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{microtype}
\usepackage{booktabs}
\usepackage{xcolor}
\usepackage[colorlinks=true,linkcolor=blue!50!black,urlcolor=blue!50!black]{hyperref}

\newtheorem{lemma}{Lemma}
\newtheorem{thm}{Theorem}
\newtheorem{prop}{Proposition}

\title{\bfseries The $J_P$ increment\\[2mm]
\large prime mass after the blind zone, and the honest race}
\author{Stefan Hamann}
\date{August 30, 2026}

\begin{document}
\maketitle

\begin{abstract}\noindent
Round 456 retyped the r450--r455 prefix as
\texttt{VACUOUS\_CONFIRMED}:
$n<J_P$ is prime-blind Archimedean anatomy.
This note accepts that in full and studies
the first object that sees primes:
the chain increment
$E=\mathrm{MAIN}-\mathrm{ARCH}$ from
$J_P=\lceil\log 2/\Delta-1\rceil$ to the
full cap.
The Schur increment drifts (no oscillatory
rescue).  The archimedean baseline is
$q_*$ at $J_P$, not a full-cap ARCH pack
(that pack is ill-conditioned).
The race $\Delta q_{\mathrm{arith}}/(1-q_*)$
stays below $1$ on living windows and
eats the margin at $k=10$ (kz$197$).
No RH claim.  No anti-RH claim.
\end{abstract}

\medskip\noindent
\textbf{Status.}\enspace
Ausgang
\texttt{INCREMENT\_DRIFT / RACE\_EATS\_K10 / LEMMA\_REDUCED}.
r456 \texttt{VACUOUS\_CONFIRMED} is accepted.
The r450--r455 identities remain true as
Archimedean anatomy and are not reused as
mincut progress.
No $L^{*}$ claim.  No $R^{\dagger}$ claim.
No RH claim.  No anti-RH claim.

\section{Acceptance}\label{sec:acc}

The prefix $n<J_P$ cannot tell MAIN from
ARCH ($\Delta q\sim10^{-16}$).
\texttt{Iff.rfl} named the full cone.
$M_\partial$ is archimedean window mass.
Those facts stand.  The load-bearing
Python object is $q^{\dagger}$ at
$W.\mathrm{cap}$.

\section{The increment}\label{sec:A}

$c^P$ vanishes for $i<J_P$ (r455).
The measure increment is the fold of
that delayed comb, via the three-lag
identity.
$E$ is the Jacobi image of that
perturbation --- explicit as a map,
not a closed formula for
$(\Delta\alpha_n,\Delta\beta_n)$.

\begin{lemma}[drift, not cancellation]\label{lem:drift}
On kz$17$, for every grade $n\ge J_P=19$,
$\Delta\rho_n>0$ ($77/77$, sum $+2.94$).
$\Delta q$ jumps $+0.05166$ at the $n=2$
tent and $+0.046$ at the $n=3$ tent
($n=30$).
$(a_n,b_n)$ oscillate; $(\rho,q)$ drift up.
On kz$116$, $\sum\Delta\rho=+12.5$
($1007/1274$ positive): mixed local
signs, net positive.
SCRAMBLE (seed $45701$) sends $q(30)$
above $1$ while MAIN stays below $1$.
\end{lemma}

Isolated octave bands do \emph{not}
add to $\Delta q$ (the Jacobi map is
nonlinear).  The onset is the first
prime tent; later primes interact.

\begin{lemma}[death is accumulated]\label{lem:death}
kz$197$ ($k=10$, $N=4071$):
$J_P=407$, $n_{\mathrm{flip}}=107$,
first flip $3788$ (r447, $\mathrm{dps}=50$).
The ABD death sits far past $J_P$.
It is an accumulated arithmetic
increment, not a local $J_P$ event.
\end{lemma}

\section{The honest budget}\label{sec:B}

The ARCH \emph{signed} pack at full cap
is ill-conditioned ($n_{\mathrm{flip}}=7$--$14$,
$q$ explodes on kz$\ge 116$).
The ARCH $\mu$-chain stays healthy
($\min b\sim 1/2$).
The baseline is
$q_*:=q^{\dagger}_{\mathrm{ARCH}}(J_P)$.

\begin{center}
\small
\begin{tabular}{@{}rrrrrrrc@{}}
\toprule
kz & $a$ & $q_{\mathrm{full}}$ & $q_*$ &
 $\Delta q$ & $1-q_*$ & race & note \\
\midrule
$17$  & $32$   & $0.89136$ & $0.78566$ &
 $+0.10570$ & $0.21434$ & $0.493$ & ALIVE \\
$69$  & $256$  & $0.95251$ & $0.89689$ &
 $+0.05562$ & $0.10311$ & $0.539$ & ALIVE \\
$116$ & $512$  & $0.96221$ & $0.86848$ &
 $+0.09374$ & $0.13152$ & $0.713$ & ALIVE \\
$136$ & $631$  & $0.97209$ & $0.87092$ &
 $+0.10117$ & $0.12908$ & $0.784$ & ALIVE \\
$197$ & $1024$ & $0.99958$ & $0.88786$ &
 $+0.11172$ & $0.11214$ & $0.996$ & EATS $k=10$ \\
$230$ & $1259$ & $1.14770$ & $0.87326$ &
 $+0.27445$ & $0.12674$ & $2.165$ & overshoot \\
\bottomrule
\end{tabular}
\end{center}

kz$137$, $170$, $500$: full MAIN pack
fails or returns $\mathrm{NaN}$ after
flips --- not scored as a race.

\begin{lemma}[r453 at cap]\label{lem:id}
On kz$17$,
$q^{\dagger}=S_{N-1}/B_w$ to $2\cdot10^{-12}$.
Hence
$\Delta q_{\mathrm{arith}}=q_{\mathrm{full}}-q_*$
is the accumulated leak of $\rho_j$ for
$j\ge J_P$.
\end{lemma}

\section{Reduction}\label{sec:C}

\begin{lemma}[honest smaller statement]\label{lem:C}
\[
q^{\dagger}_{\mathrm{full}}<1
\quad\Longleftrightarrow\quad
\Delta q_{\mathrm{arith}}<1-q_*.
\]
The increment is supported on $j\ge J_P$
and is net-positive in $\rho$ (no
oscillatory rescue).
The cofinal hope
$\Delta q\le(1-q_*)(1-\varepsilon)$
is \textbf{REFUTED} by $k=10$.
\end{lemma}

This is r453 evaluated at the cap on the
prime tail --- a \emph{new use}, not a
new identity.
It is \textbf{not} an alias of r429
($|Z_{\mathrm{loc}}|$ head bound /
triangle \texttt{REFUTED}):
r429 is a terminal pairing of a
selected form; $E$ is the Jacobi image
of a lag-sparse second difference of
$c^P$.

\section{Kill table}\label{sec:kills}

\begin{center}
\begin{tabular}{@{}lp{7.8cm}@{}}
\toprule
Kill & Status \\
\midrule
ARCH world, identical fold (r456)
  & used throughout \\
dps at $k=10$
  & r447 $\mathrm{dps}=50$ EXACT\_DEAD
    (flip $3788$); float64 re-gated \\
Determinism
  & two identical smokes \\
SCRAMBLE vs oscillation
  & $q_{\mathrm{SCR}}(30)>1$;
    increment is arithmetic \\
Alias vs r429
  & different object; documented \\
\bottomrule
\end{tabular}
\end{center}

\medskip\noindent
\textbf{Claim boundary.}\enspace
Finite-window increment census.
No $L^{*}$ claim.
No $R^{\dagger}$ claim.
No RH claim.
No anti-RH claim.

\end{document}
