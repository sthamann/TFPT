% extraction_joint.tex -- round 473, PRIME.RDAGGER.EXTRACTION_JOINT_REDESIGN.01.
% ARTEFACT + POLY_BRIDGE_PROVED.
% Companion: verify_extraction_joint.py.
% Discovery: experiments/tfpt-discovery/extraction_joint_probe.py.
% NO RH CLAIM.  NO anti-RH claim.
\documentclass[11pt]{article}
\usepackage[a4paper,margin=25mm]{geometry}
\usepackage{amsmath,amssymb}
\usepackage[T1]{fontenc}
\usepackage[hidelinks]{hyperref}
\newcommand{\code}[1]{\texttt{\detokenize{#1}}}

\title{R473: Extraction-joint redesign}
\author{TFPT RH programme}
\date{31 August 2026}

\begin{document}
\maketitle

\begin{center}
\fbox{\parbox{0.9\linewidth}{
\textbf{Claim boundary.} This note diagnoses one finite native read
against the classical Guinand--Weil functional and redesigns the
internal extraction joint.  It does not prove compact-window
positivity or the Riemann hypothesis.  No statement about infinitely
many $k$.
}}
\end{center}

\section{A --- artefact diagnosis}

The r470 sealed witness at $k=5$ is the least eigen-direction of the
native Hessian of \code{fullRead} on
$\mathrm{meshExp}=3$, $\mathrm{steps}=24$
(\code{elementAnchor}$=21\le a_5=32$).  Its native read is negative.
The induced test function $g_f$ is the even piecewise-linear
autocorrelation of that direction (equal, for a piecewise-constant
$h$, to the continuous autocorrelation).

The classical Guinand--Weil functional is computed at
\texttt{mpmath} $\mathrm{dps}=50$:
\begin{itemize}
\item archimedean term $A$ via the $u$-space digamma identity
$A = C_b\,g(0)+\int_0^b w(u)\,[e^{-3u/2}g(0)-g(u)]\,du$,
$w(u)=2e^{-u/2}/(1-e^{-2u})$,
$C_b=-\gamma-\log\pi-\log(1-e^{-2b})$;
\item prime term $P$ as the finite $\Lambda$-sum over
$\log n\le\mathrm{supp}\,g_f=3$;
\item pole term $\Pi$ the exact native-mesh pairing
(\code{poleRead}, definitionally \code{weilPoleSide}).
\end{itemize}
The result is
\[
 Q(g_f)=A-P+\Pi=+0.0769078530458283>0,
\]
while
\[
 \mathrm{fullRead}(a_5,m_5,f)=-0.0428854252772195<0.
\]
The comb channel matches to machine precision
($P=0.1364027472150502$).  The entire gap is the arch tent error
\[
 \mathrm{err}_{\mathrm{arch}}
 =A_{\mathrm{classical}}-A_{\mathrm{window}}
 =0.1197932783230478.
\]
The cosine transform of $g_f$ stays positive on a 120-point
$[0,30]$ grid ($\hat g_{\min}=2.78\cdot10^{-7}$): $g_f$ is a genuine
autocorrelation.  A Fejer-triangle ward gives $Q=+0.0880146104737050>0$.

\textbf{Verdict A: ARTEFACT.}  Case (i) of the contract:
$Q(g_f)\ge0$ but $\mathrm{fullRead}<0$.  The tent/window
discretisation error at $k=5$ exceeds $Q$.  This is not a
construction failure of $g_f$ and not a Weil-negativity alarm.

The exact identity (Lean, proved, no digamma \code{sorry}) is
\begin{quote}
\code{fullRead\_weilForm\_gap\_eq\_arch}:\\
once \code{elementAnchor}$\,f\le a$,
$\mathrm{fullRead}-\mathrm{weilForm}
=\mathrm{archRead}-\mathrm{weilArchSide}$.
\end{quote}
Hence
$|\mathrm{fullRead}-\mathrm{weilForm}|=\code{selectedArchError}$.
The chain needs this error term: on a PSD cap the redesigned
readout is $\mathrm{fullRead}\ge-\mathrm{err}_{\mathrm{arch}}$,
not $\mathrm{fullRead}\ge0$.

\section{B --- the provable polynomial bridge}

The class $A_{\mathrm{cap}}$ can carry is the coefficient-plus-border
space of real polynomials of degree $<\mathrm{cap}$.  Lean proves
\begin{quote}
\code{selectedACapPsdImpliesPolynomialReads}:\\
$A_{\mathrm{cap}}\succeq0$ $\Rightarrow$
$\mathrm{selectedReadQuadratic}(z)\ge0$
for every $z\in\mathbb R^{\mathrm{cap}+1}$.
\end{quote}
The channel is the moment evaluation
(\code{PrimeWindow.hankel\_quadform}:
$x^{\mathsf T}Hx=\mu(p_x^2)-\nu(p_x^2)$;
\code{PrimeWindow.A\_quadform} splits the border).  There is no
\code{steps} quantifier.  Fixed $k$ only.

The polynomial-to-\code{GridElement} gap is the named outer bridge
\begin{quote}
\code{SelectedPolynomialApproximatesGrid}:\\
at a fixed selected window, every mesh-compatible native read
lies within $\mathrm{err}_{\mathrm{arch}}$ of the polynomial-class
$A_{\mathrm{cap}}$ image.
\end{quote}
On a PSD cap this is $\mathrm{fullRead}\ge-\mathrm{err}_{\mathrm{arch}}$.
It is a named Prop, not a \code{sorry}.  At the sealed $k=5$ witness
$|\mathrm{fullRead}|=0.042885425277\le\mathrm{err}_{\mathrm{arch}}$.

\section{C --- redesigned joint}

The historical FREQ endpoint
\code{internal\_weil\_nonneg\_of\_frequently\_selected} is retained
(it still consumes the r464 full-class channel as a hypothesis).
The honest joint is now
\begin{quote}
\code{FrequentlySelectedPolynomialJoint}\\
$=$ FREQ cone $\wedge$ \code{SelectedPolynomialApproximatesGrid}.
\end{quote}
Fixed-$k$ readout (proved as a function of the named approx):
\begin{quote}
\code{weilForm\_ge\_neg\_two\_archError\_of\_joint}:\\
at one good selected window, every onset-and-mesh-compatible $f$
satisfies $\mathrm{weilForm}\,f\ge-2\,\mathrm{err}_{\mathrm{arch}}(k,f)$.
\end{quote}
No infinitely-many-$k$ conclusion.

\section{Joint map}

\begin{center}
\begin{tabular}{p{0.28\linewidth}p{0.22\linewidth}p{0.40\linewidth}}
\hline
Step & Status & Content\\ \hline
$A_{\mathrm{cap}}\succeq0$ $\Rightarrow$
poly reads $\ge0$ & proved &
\code{selectedACapPsdImpliesPolynomialReads}\\
\code{fullRead}-\code{weilForm}$=\mathrm{err}_{\mathrm{arch}}$
(onset) & proved &
\code{fullRead\_weilForm\_gap\_eq\_arch}\\
poly class $\to$ \code{GridElement}
within $\mathrm{err}_{\mathrm{arch}}$ & named &
\code{SelectedPolynomialApproximatesGrid}\\
cone $+$ approx $\Rightarrow$
$\mathrm{fullRead}\ge-\mathrm{err}$ & proved of named &
\code{selected\_reads\_ge\_neg\_archError\_of\_approx}\\
onset $\Rightarrow$
$\mathrm{weilForm}\ge-2\mathrm{err}$ & proved of named &
\code{weilForm\_ge\_neg\_two\_archError\_of\_joint}\\
historical FREQ $\Rightarrow$ \code{weilForm}$\ge0$
& retained, conditional &
still needs the obstructed full-class channel\\ \hline
\end{tabular}
\end{center}

\section{Kill table}

\begin{center}
\begin{tabular}{p{0.30\linewidth}p{0.16\linewidth}p{0.44\linewidth}}
\hline
Candidate & Status & Reason\\ \hline
Negative $Q(g_f)$ / ALARM & killed &
$Q=+0.0769078530458283$; $\hat g\ge0$.\\
$g_f$ not an autocorrelation & killed &
PWC ACF $=$ PL interpolant; $\hat g_{\min}>0$.\\
Restore the r464 full-class channel & killed &
r470 obstruction stands; residual $24>11$.\\
Cone $\Rightarrow$ native $\mathrm{fullRead}\ge0$
& killed & artefact: tent error $>Q$ at $k=5$.\\
New infinitely-many-$k$ readout & killed &
r469 anti-list; joint is fixed-$k$.\\
R473 proves RH & killed & finite diagnosis $+$ poly PSD;
census stays 8.\\ \hline
\end{tabular}
\end{center}

\section{Census}

Lean \code{sorry} census remains $8$: three in \code{Canonical.lean},
one named Gauss--Mellin--digamma identity in \code{Elementwise.lean},
three external arrows in \code{ExternalBridges.lean}, and one source
theorem in \code{Source.lean}.  No new \code{sorry}.
Smoke $13/13$, \texttt{SPEC\_SHA} \texttt{0650125bd812c1f8}.

\end{document}
