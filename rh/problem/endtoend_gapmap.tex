% endtoend_gapmap.tex -- round 462, adversarial end-to-end gap map.
% Verdict: HIDDEN_GAP_FOUND.  Companion: verify_gapmap.py.
% Discovery: experiments/tfpt-discovery/gapmap_probe.py.
% NO RH CLAIM.  NO anti-RH claim.
\documentclass[10pt]{article}

\usepackage[a4paper,margin=2.0cm]{geometry}
\usepackage{amsmath,amssymb}
\usepackage{booktabs,longtable,array}
\usepackage{xcolor}
\usepackage{microtype}
\usepackage[colorlinks=true,linkcolor=blue!50!black,urlcolor=blue!50!black]{hyperref}

\newcommand{\ok}{\textcolor{green!45!black}{PROVED}}
\newcommand{\gap}{\textcolor{red!75!black}{HIDDEN GAP}}
\newcommand{\open}{\textcolor{orange!80!black}{OPEN}}
\newcommand{\off}{\textcolor{gray!75!black}{OFF PATH}}
\newcommand{\code}[1]{\nolinkurl{#1}}
\setlength{\emergencystretch}{2em}

\title{\bfseries End-to-end gap map\\[1mm]
\large PRIME.RDAGGER.ENDTOEND\_GAPMAP.01 --- round 462}
\author{Stefan Hamann}
\date{August 30, 2026}

\begin{document}
\maketitle

\begin{center}
\fcolorbox{red!75!black}{red!5}{%
\parbox{0.91\linewidth}{\centering\Large\bfseries
HIDDEN\_GAP\_FOUND\\[1mm]\normalsize
The claimed minimal path is neither source-faithful nor an
end-to-end proof of the Riemann Hypothesis.}}
\end{center}

\begin{abstract}
This note audits every declaration and import behind
\code{frequently_selected_augDualResolvent_ge_half}
$\Rightarrow$\code{rh_of_frequently_selected}.  The finite
Loewner and filter algebra is sound and builds without
\code{native_decide}, custom axioms, or partial definitions.
The end-to-end claim nevertheless fails at three independent levels:
(1) the Lean \code{ExactFold} window is not the Python production
window measured in rounds 458--459; (2) the path assumes the unproved
named bridge \code{SelectedACapPsdImpliesPlainReads} and consumes the
\code{arch_elementwise_stabilization} \code{sorry}; and (3) its final
conclusion is only nonnegativity of a custom \code{weilForm} on
\code{GridElement}, with no spectral/zero side and no theorem connecting
it to Mathlib's zeta function or to the actual Riemann Hypothesis.
No RH claim and no anti-RH claim is made.
\end{abstract}

\section{Adversarial gap map}

\small
\begin{longtable}{@{}p{0.19\linewidth}p{0.24\linewidth}p{0.14\linewidth}p{0.35\linewidth}@{}}
\toprule
Glied & Datei:Zeile & Status & Risiko / genaue Aussage \\
\midrule
\endfirsthead
\toprule
Glied & Datei:Zeile & Status & Risiko / genaue Aussage \\
\midrule
\endhead
Selected source & \code{Selected.lean:113--178} & \gap &
\code{ExactPrimeSource} enumerates raw prime powers and
\code{ExactFold} applies \code{foldedWindow} directly to those atoms.
The production builder first assembles lag tents, takes a spectral
density, and then folds/sign-splits a fixed grid. \\

Fold geometry & \code{Selected.lean:160--178};
\code{Source.lean:384--440} & \gap &
Lean sets $\Delta=\log a/(m+1)$ and $L=2(m+1)$, so
$\Delta L=2\log a$ and the fold map is independent of $m$.
Python uses $L=2(m+1)-2$.  Lean merges raw source nodes; Python has
the spectral-grid support. \\

Moment carrier & \code{Selected.lean:240--270};
\code{FrequentlySelected.lean:97--111} & \gap &
Lean \code{combHankel} is the moment Gram of separately folded,
bare $\Lambda(p^r)$ weights.  Python's $\mu$ is the positive part
after transforming the combined archimedean plus
$2\Lambda(n)/\sqrt n$ lag vector.  These are not the same matrix. \\

Border/budget & \code{Selected.lean:119--138,181--232};
\code{Canonical.lean:105--137} & \open &
\code{ExactArch}, \code{ExactBorder}, and \code{ExactBudget} are
values of the opaque \code{canonicalCompletion}; \code{foldedWindow}
passes $u,B$ through unchanged.  There is no executable Lean value
to compare with v958/r243. \\

Selected cofinality & \code{Selected.lean:299--418} & \ok &
$a_k\to\infty$, $m_k\to\infty$, and $\Delta_k\to0$ are proved.
This does not repair the object mismatch: the window cone's cap is
computed from \code{ExactFold}'s raw folded support, while $m_k$ only
appears in the separate read/extraction index. \\

Mincut & \code{FrequentlySelected.lean:259--286} & \open &
\code{frequently_selected_augDualResolvent_ge_half} is a
\emph{definition of an unproved Prop}, not a theorem or \code{sorry}.
Rounds 458--459 provide only a finite Python census through $k=12$,
and on a different object. \\

$R^\dagger$ Loewner face & \code{DualResolvent.lean:360--387} & \ok &
\code{Rdagger_ge_half_iff_augmented_posSemidef} is sorry-free finite
matrix algebra, assuming \code{dualZ.PosDef}. \\

Real-window whitening & \code{FrequentlySelected.lean:118--251} & \ok &
\code{masterCap_posSemidef_iff_Rdagger_ge_half} is a sorry-free
congruence theorem under the explicit equations
\code{RepresentsLEnsembleReal}.  It identifies the cone with
\code{A_cap}; it does not identify \code{A_cap} with \code{fullRead}. \\

Cone to $A_{\rm cap}$ & \code{FrequentlySelected.lean:370--381} & \ok &
\code{selectedWindowConeSemidef_implies_A_cap_posSemidef} is proved
from the whitening theorem. \\

$A_{\rm cap}$ to plain read & \code{FrequentlySelected.lean:382--409} &
\gap &
\code{SelectedACapPsdImpliesPlainReads} is another unproved named
Prop.  It is an explicit hypothesis of the collapsed path.  The
claimed three-step minimal path omitted it. \\

Frequent extraction & \code{FrequentlySelected.lean:327--355,410--448} &
\ok{} modulo \code{sorry} &
Filter arithmetic is proved: eventual coverage plus frequent
positivity yields one good index.  The proof calls
\code{elementwise_finite_stabilization}. \\

Comb stabilization & \code{Elementwise.lean:360--492} & \ok &
The mesh-free prime sum stabilizes for compactly supported
\code{GridElement}s.  It concerns the custom read and does not prove
the selected matrix/source identification above. \\

Arch stabilization & \code{Elementwise.lean:553--699} & \gap &
\code{archRead} and \code{weilArchSide} are independent opaque
constants; their equality is the on-path \code{sorry}
\code{arch_elementwise_stabilization}.  It is not derivable in the
current signature without replacing those constants by actual
transcriptions. \\

Pole stabilization & \code{Elementwise.lean:522--708} & \ok &
The native-mesh pole equality is definitional after the
second-difference construction.  Dyadic-independence is only a named
Prop but is not consumed. \\

Custom Weil form & \code{Elementwise.lean:710--752} & defined &
\code{weilForm = weilArchSide - weilCombSide + weilPoleSide}.
The arch term is opaque.  The file explicitly says that the
spectral/zero side is not part of this definition. \\

Mislabeled endpoint & \code{FrequentlySelected.lean:429--448} & \gap &
\code{rh_of_frequently_selected} concludes exactly
\code{forall f : GridElement, 0 <= weilForm f}.
It has no conclusion about zeros of $\zeta$.  Its name is historical,
not a formal RH theorem. \\

Dense-class extension & absent & \gap &
No continuity/density theorem extends the autocorrelation
\code{GridElement} class to the full admissible Weil test class. \\

Explicit formula & absent & \gap &
No theorem identifies the custom \code{weilForm} with the standard
Weil explicit formula, including its zero/spectral side. \\

Weil criterion $\Leftrightarrow$ RH & absent & \gap &
No Mathlib or project theorem states the Weil criterion for
\code{Complex.zeta}, identifies its nontrivial zeros, or concludes
the genuine Riemann Hypothesis. \\
\bottomrule
\end{longtable}
\normalsize

\section{Numerical source-truth check}

The companion probe enumerates the same complete set of prime powers
$p^r\le a^2$ on both sides, then evaluates the literal Lean comb fold
and the production Python builder.  Thus the discrepancy is not a
table-cap artefact.

\begin{center}
\begin{tabular}{@{}rrrrrrr@{}}
\toprule
$k$ & $a$ & atoms & Lean $S$ & Lean cap & Python $S$ & Python cap \\
\midrule
5  & 32   & 198   & 194   & 97    & 19 & 10 \\
9  & 512  & 23150 & 23142 & 11571 & 71 & 36 \\
10 & 1024 & 82267 & 82258 & 41129 & 79 & 40 \\
\bottomrule
\end{tabular}
\end{center}

At $k=10$, the first three Lean literal \code{combHankel} entries
\((H_{00},H_{01},H_{11})\) are
\[
(1.04843042\cdot10^6,\;869736.706,\;811945.161).
\]
The production $\mu$-Gram entries are
\[
(1.91210951,\;-1.15236093,\;1.03066237).
\]
The cap ratio is $1028.2$.  The corresponding ratios at $k=5,9$ are
$9.7$ and $321.4$.  Therefore the r459
\code{LEANFAM_EXACT_ALIVE(12)} census does not evaluate the Lean
\code{selectedWindowConeSemidef} proposition.

\section{Import graph and axiom audit}

The actual import closure is:
\begin{itemize}
\item \code{FrequentlySelected} imports \code{Selected} and
\code{GraphResolvent}.
\item \code{Selected} imports \code{Canonical}, \code{DualResolvent},
and \code{Elementwise}.
\item \code{Canonical} imports \code{Source}, \code{Closure},
\code{Augmented}, \code{PairBound}, and \code{Elementwise}.
\item \code{GraphResolvent} imports \code{OneDefect} and
\code{DualResolvent}; \code{OneDefect} imports \code{PivotCoordinate},
which imports \code{Haynsworth} and \code{Window}; these lead to
\code{Inertia} and \code{Basic}.
\end{itemize}
\code{Source} imports \code{Window}; \code{Closure} and
\code{Augmented} import \code{Inertia}; \code{PairBound} imports
\code{Recursion}.  The following modules are imported but their
specialized theorems are not consumed by the final FREQ proof term:
\code{GraphResolvent}, \code{OneDefect}, \code{PivotCoordinate},
\code{Haynsworth}, \code{Closure}, \code{Augmented}, and
\code{PairBound}.
\code{EdgeBalance.lean} and \code{Open.lean} were explicitly audited
as requested but are not in this import closure and are off path.

There is no \code{native_decide}, user \code{axiom}, \code{admit}, or
\code{partial} declaration on the path.  The sorry-free matrix/filter
theorems use only \code{propext}, \code{Classical.choice}, and
\code{Quot.sound}.  The endpoint theorem additionally has
\code{sorryAx} through \code{arch_elementwise_stabilization}.
Noncomputability is ordinary classical choice; the semantic
embeddings are the opaque constants \code{canonicalCompletion},
\code{archRead}, and \code{weilArchSide}, plus the named Props listed
above.

\section{Anatomy of all five \code{sorry} declarations}

\begin{longtable}{@{}p{0.25\linewidth}p{0.16\linewidth}p{0.16\linewidth}p{0.35\linewidth}@{}}
\toprule
Declaration & Location & Path status & Closure assessment \\
\midrule
\endfirsthead
\toprule
Declaration & Location & Path status & Closure assessment \\
\midrule
\endhead
\code{lstar_canonical} & \code{Canonical.lean:262--264} & \off &
Alternative rational-certificate L* route.  Not referenced by either
FREQ endpoint theorem.  Hard arithmetic problem; current certificate
domain may be empty and must first be replaced by a faithful real
domain. \\

\code{terminal_q_canonical} & \code{Canonical.lean:271--273} & \off &
Alternative terminal route.  Not referenced by the FREQ proof.
Hard cofinal arithmetic inequality; not closed by the finite census. \\

\code{pair_terminal_dictionary} & \code{Canonical.lean:393--396} & \off &
Used only by \code{pair_closes_main}, not by the FREQ route.
Potentially closable after a faithful v958 border/orthogonal-polynomial
transcription; impossible from the current opaque completion alone. \\

\code{mainWindow_iff_builtFromPrimeSource} &
\code{Source.lean:995--1008} & \off &
Historical opaque-\code{MainWindow} bridge, absent from the FREQ proof.
Unprovable by design while both sides are opaque/free; closing it
requires a semantic redefinition and all source transcriptions, not a
local Lean lemma. \\

\code{arch_elementwise_stabilization} &
\code{Elementwise.lean:675--697} & \gap{} ON PATH &
Consumed by \code{elementwise_finite_stabilization} and therefore by
both FREQ endpoints.  Classical analysis is plausible, but the current
theorem relates two independent opaque constants.  First define the
actual tent integral and Weil arch pairing; then prove the
Gauss/Mellin/digamma identity. \\
\bottomrule
\end{longtable}

\section{Verdict and architecture gate}

The result is \gap, not \code{GAPMAP_CLEAN}.  The reviewer must not
accept the asserted three-arrow chain.  Even granting the Mincut,
three independent obligations remain before any RH theorem:
\begin{center}
\fbox{\code{SelectedACapPsdImpliesPlainReads}}\\
\(\displaystyle {}+\ \)
\fbox{\code{arch_elementwise_stabilization}}\\
\(\displaystyle {}+\ \)
\fbox{custom Weil form \(\Longleftrightarrow\) genuine RH}.
\end{center}
Before those, the selected Lean source itself must be reconciled with
the production builder.

Part C is intentionally not proposed: the round contract permits a new
full-solution architecture only if A/B are clean.  The alias check
against \code{rdagger_saturation.tex} is therefore not applicable;
proposing another de Branges/Krein/Potapov architecture would conceal
the more basic source and endpoint gaps found here.

\medskip\noindent
\textbf{Machine checks.}\enspace
\code{gapmap_probe.py --smoke}: 15/15 gates; source samples
$k=5,9,10$.
\code{verify_gapmap.py}: declaration, census, source, and
INVENTORY-stability gates; final line \code{GAPMAP VERIFIED}.

\medskip\noindent
\textbf{Claim boundary.}\enspace
Research documentation.  No $L^*$ claim, no $R^\dagger$ claim,
no RH claim, and no anti-RH claim.

\end{document}
