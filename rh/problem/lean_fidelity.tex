% lean_fidelity.tex -- r463 honest Lean fidelity repair.
% Companion: verify_lean_fidelity.py / lean_fidelity_probe.py.
% NO RH CLAIM.
\documentclass[10pt]{article}
\usepackage[a4paper,margin=2cm]{geometry}
\usepackage{amsmath,amssymb,booktabs,longtable,array,xcolor,microtype}
\usepackage[colorlinks=true,linkcolor=blue!45!black]{hyperref}
\newcommand{\code}[1]{\nolinkurl{#1}}
\newcommand{\proved}{\textcolor{green!40!black}{PROVED}}
\newcommand{\typed}{\textcolor{orange!80!black}{TYPED OPEN}}
\setlength{\emergencystretch}{2em}

\title{\bfseries Lean fidelity repair\\[1mm]
\large PRIME.RDAGGER.LEAN\_FIDELITY\_REPAIR.01 --- round 463}
\author{Stefan Hamann}
\date{August 30, 2026}

\begin{document}
\maketitle

\begin{abstract}
Round 462 found that the selected Lean window was a raw-prime-power
cosine quotient rather than the production lag/spectral-density fold.
Round 463 repairs the object, pins it numerically at $k=5,9,10$, and
turns the three absent external arrows into named Lean obligations.
The honest \texttt{sorry} census rises from five to eight.  This is an
internal formalization repair, not a claim of the Riemann hypothesis.
\end{abstract}

\section{Productive transcription}
\code{RH/FaithfulFold.lean} implements
\[
\{\Lambda(n):n\le a^2\}
\longrightarrow c^P_i
\longrightarrow c_i=c^A_i+c^P_i
\longrightarrow d_j
\longrightarrow (x_j,w_j)
\longrightarrow(\mu,\nu),
\]
where
\[
 M=m+1,\qquad L=2M-2,\qquad
d_j=c_0+2\sum_{r=1}^{M-2}c_r\cos(r\theta_j)
       +c_{M-1}\cos((M-1)\theta_j),
\]
\[
\theta_j=\frac{2\pi j}{L},\quad
x_j=\cos\theta_j,\quad
w_j=\frac{2}{L}(1-\cos\theta_j)d_j ,
\qquad 1\le j\le L/2.
\]
The endpoint $j=L/2$ is halved.  Positive weights form the
\code{combWeight} channel and negative weights, with sign reversed,
form \code{archWeight}; therefore \code{PrimeWindow.combHankel} is
the production $\mu$-Gram matrix.  Zero weights are retained in Lean;
Python's $10^{-300}$ deletion threshold is not mathematical data.

The prime lag is an exact finite sum of ordinary and reflected
triangular tents with mass
$\tfrac12\mathrm{combMass}(n)=\Lambda(n)/\sqrt n$.
\code{productionArchLag} exposes each value of
\code{arch_lags(M,Delta)}, and \code{ProductionCompletion} exposes
the v958 border and r243 budget.  Their analytic construction remains
open, but no per-atom \code{canonicalCompletion} enters the selected
window.

\section{Numerical seal}
Because Lean's \code{Real.log} and \code{Real.cos} are noncomputable,
the verifier pins exported evaluations; no \code{native_decide} or
sampled proposition is embedded.

\begin{center}
\small
\begin{tabular}{@{}rrrrrrr@{}}
\toprule
$k$ & atoms & $S$ & cap & $H_{00}^{\mu}$ & $H_{01}^{\mu}$ & $H_{11}^{\mu}$\\
\midrule
5  & 198   & 19 & 10 & 1.03660220661 & -0.725897612286 & 0.604943507078\\
9  & 23150 & 71 & 36 & 1.90736517945 & -1.15683291482  & 1.06483617754\\
10 & 82267 & 79 & 40 & 1.91210951462 & -1.15236092729  & 1.03066236713\\
\bottomrule
\end{tabular}
\end{center}
The independent explicit cosine sum agrees with the production FFT,
and all displayed values agree with the production builder within
$2\cdot10^{-12}$.  Two smoke runs are byte-identical; wall time is
below six seconds.

\section{Honest path graph}
The internal endpoint is now
\code{internal_weil_nonneg_of_frequently_selected}, whose conclusion
is only
\[
  \forall f:\mathrm{GridElement},\qquad 0\le\mathrm{weilForm}(f).
\]
The proposition \code{FrequentlySelectedInternalMincut} contains both
the spectral FREQ hypothesis and the AXIOM-CANDIDATE
\code{SelectedACapPsdImpliesPlainReads}.  Thus the read-identification
remainder is no longer easy to omit when quoting the mincut.

The three arrows beyond the internal endpoint are declarations in
\code{RH/ExternalBridges.lean}:
\begin{enumerate}
\item \code{grid_dense_extension}: native dyadic autocorrelations to
the full Weil test class;
\item \code{standard_explicit_formula_identification}: the continued
custom form equals the standard explicit formula;
\item \code{standard_weil_criterion_to_mathlib_rh}: standard
positivity implies Mathlib's actual \code{RiemannHypothesis}, defined
using \code{riemannZeta}.
\end{enumerate}
Each carries one \code{sorry}; no theorem named ``RH from frequently
selected'' remains.

\section{Sorry census}
\begin{longtable}{@{}p{4.5cm}p{2cm}p{7.3cm}@{}}
\toprule
Declaration & r463 status & Reason\\
\midrule
\endhead
\code{lstar_canonical} & unchanged & Alternative rational-certificate route.\\
\code{terminal_q_canonical} & unchanged & Alternative terminal route.\\
\code{pair_terminal_dictionary} & unchanged & Measured dictionary, off internal path.\\
\code{mainWindow_iff_builtFromPrimeSource} & unchanged & Historical opaque source bridge.\\
\code{arch_elementwise_stabilization} & unchanged & Gauss/Mellin/digamma identification; \code{archRead} itself is now concrete.\\
\code{grid_dense_extension} & new & Previously absent density and continuity theorem.\\
\code{standard_explicit_formula_identification} & new & Previously absent normalization and zero-side dictionary.\\
\code{standard_weil_criterion_to_mathlib_rh} & new & Previously absent bridge to Mathlib's zeta/RH predicate.\\
\bottomrule
\end{longtable}

\section{Kill and gate table}
\begin{center}
\small
\begin{tabular}{@{}p{5.1cm}p{2.1cm}p{7cm}@{}}
\toprule
Gate & Result & Evidence\\
\midrule
Production dimensions and sign split & \proved & Lean definitions; $L=2M-2$.\\
$k=5,9,10$ Lean-mirror versus Python & \proved & 11/11 probe gates; tolerance $2\cdot10^{-12}$.\\
No weakening of Loewner/whitening/FREQ layer & \proved & Full \code{lake build}.\\
Concrete arch read & \proved & Finite lag pairing; coefficient theorem remains open.\\
Selected A-cap to plain reads & \typed & Explicit member of compound mincut.\\
External RH bridge & \typed & Three named \code{sorry}s, actual Mathlib interface.\\
Sorry census & \proved & Five old plus three new, total eight.\\
\bottomrule
\end{tabular}
\end{center}

\medskip\noindent
\textbf{Machine checks.}\enspace
\code{lean_fidelity_probe.py --smoke}: \code{LEAN FIDELITY VERIFIED}.
\code{verify_lean_fidelity.py}: \code{LEAN FIDELITY REPAIR VERIFIED}.
\code{lake build}: green.

\medskip\noindent
\textbf{Claim boundary.}\enspace
Research documentation.  No $L^*$ claim, no $R^\dagger$ claim, and no
claim for or against the Riemann hypothesis.

\end{document}
