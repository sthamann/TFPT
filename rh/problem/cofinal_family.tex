% cofinal_family.tex -- round 458, cofinal living family.
% FAMILY_CANDIDATE / TABLE_CAP_ARTEFACT / RESIDUAL_DEEP_DEATH.
% Builds with pdflatex.
% Companion machine check: verify_cofinal_family.py.
% Discovery: experiments/tfpt-discovery/cofinal_family_probe.py.
% NO RH CLAIM.  NO anti-RH claim.
\documentclass[11pt]{article}

\usepackage[a4paper,margin=2.7cm]{geometry}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{microtype}
\usepackage{booktabs}
\usepackage{xcolor}
\usepackage[colorlinks=true,linkcolor=blue!50!black,urlcolor=blue!50!black]{hyperref}

\newtheorem{lemma}{Lemma}
\newtheorem{prop}{Proposition}

\title{\bfseries Cofinal family\\[2mm]
\large Lean $(a,m)$ lives; the $4\cdot10^5$ table\\
was killing the frame-A $2^k$ ladder}
\author{Stefan Hamann}
\date{August 30, 2026}

\begin{document}
\maketitle

\begin{abstract}\noindent
r456 retyped the frequently-mincut as the
full-cap cone
\texttt{frequently\_selected\_augDualResolvent\_ge\_half}.
The named $2^k$ frame-A ladder dies past $k=10$
on the campaign table ($\mathrm{TABLE\_CAP}=4\cdot10^5$).
A sealed 76-window scan shows a sharp wall at
$a=631/641$ --- exactly
$\sqrt{\mathrm{TABLE\_CAP}}$.
Completing the comb to $a^2$ revives
$k_z197$ ($2^{10}$) and generic $k_z230$
(exact $\mathrm{dps}=50$, $n_{\mathrm{flip}}=0$).
The Lean selected sequence
$a_k=2^k$, $m_k=k\cdot 2^{\lfloor\sqrt k\rfloor}-1$
is living at the cap through $k=16$
($k=10$ unchanged under the full $a^2$ comb).
One residual deep death remains:
$k_z137$ ($N=8300$) still flips after the comb
is completed.  No RH claim.
\end{abstract}

\medskip\noindent
\textbf{Status.}\enspace
No new SATZ.
Ausgang
\texttt{FAMILY\_CANDIDATE / TABLE\_CAP\_ARTEFACT / RESIDUAL\_DEEP\_DEATH}.
No $L^{*}$ claim.  No $R^{\dagger}$ claim.
No RH claim.  No anti-RH claim.

\section{Selected requirements}\label{sec:A}

\begin{prop}[Lean-admissible family]\label{prop:lean}
From \texttt{RH/Selected.lean}:
\begin{enumerate}
\item $W^{\mathbb{R}}(a,m)=\mathrm{ExactFold}(a,m)$
is total for every prime-power $a$ and every mesh
$m$ (ll.\,189--191, 203--205).
\item Cap is $(S+1)/2$ of the folded window
(l.\,246).  The $\mu$-ONB uses
$Q^{T}(\mathrm{combHankel})\,Q=I$ (l.\,287) ---
the full comb, not a prefix.
\item Coverage
(\texttt{selected\_covers}, ll.\,412--418):
one sequence with $a_k\to\infty$ and
$m_k\to\infty$ supplies
$a_k\ge a_0(f)$ and $m_k\ge\mathrm{meshExp}\,f$
for every $f$.
\item The named sequence (ll.\,36--41, 298--307)
is $a_k=2^k$, $r_k=\lfloor\sqrt k\rfloor$,
$m_k=k\cdot 2^{r_k}-1$,
$\Delta_k=2^{-r_k}\log 2$.
\end{enumerate}
Freedom: any prime-power anchors with both
coordinates cofinal.  The Python $k_z$ raster
couples $m$ to the local log-gap and truncates
the comb at $4\cdot10^5$; that is a builder
choice, not a Lean constraint.
r329 (EXT3) tested a different predicate
($F_A$/cubic) on fresh small-gap anchors and
found violations; it did not test $q^{\dagger}$
at the cap.  Those EXT3 windows are
ABD-living on the capped table ($a\le 601$).
\end{prop}

\section{Capped census}\label{sec:B}

\begin{prop}[sealed grid, $a$-wall]\label{prop:wall}
\texttt{SCAN\_KZ} (76 indices) was frozen
before the first pack.
55 live, 21 dead.
Every live window has $a\le 631$;
every dead window has $a\ge 641$.
Holdout (every 4th, 19 windows):
the pre-registered predictor $a\le 631$
scores $19/19$.
$N\le 1641$ scores only $14/19$
($k_z69$, $N=5690$, lives).
The wall equals the table:
$631^{2}=398161<4\cdot10^{5}<641^{2}=410881$.
\end{prop}

\section{Full comb}\label{sec:full}

\begin{prop}[table-cap artefact]\label{prop:cap}
Raising the von~Mangoldt table past $a^{2}$
and keeping the same $(a,M,L,\Delta)$:
\begin{center}
\small
\begin{tabular}{@{}rrccc@{}}
\toprule
$k_z$ & $a$ & capped & full comb & exact \\
\midrule
136 & 631 & live & live & --- \\
138 & 643 & dead $4161$ & live & --- \\
139 & 647 & dead $2796$ & live & --- \\
197 & $1024$ & dead $3788$ & live & --- \\
230 & $1259$ & dead $1818$ & live & $n_{\mathrm{flip}}=0$ dps$50$ \\
137 & $641$ & dead $7511$ & dead $7308$ & --- \\
\bottomrule
\end{tabular}
\end{center}
Lean $k=10$ ($m=79$) has the same
$q^{\dagger}=0.9319618718590412$ at
$\mathrm{ka}=34034$ (capped) and
$\mathrm{ka}=82267$ (full $a^{2}$).
The r447/r448 deaths of $k_z197$ and
$k_z230$ were exact arithmetic on a
truncated comb, not on
\texttt{ExactPrimeSource}$(a)$.
\end{prop}

\section{Candidate}\label{sec:C}

\begin{prop}[FAMILY\_CANDIDATE]\label{prop:fam}
Rule (Lean-admissible):
\[
a_k=2^k,\qquad
m_k=k\cdot 2^{\lfloor\sqrt k\rfloor}-1,
\]
comb $=$ all prime powers $\le a^{2}$
(no $4\cdot10^5$ cut).
Certified living at the cap:
$k=5,6,7,8,9,10,11,12,14,16$
(float); $k=10$ full-comb stable.
Generic $a=1259$ (not $2^k$) is exact-living
at the frame-A mesh once the comb is complete.
Residual: $k_z137$ ($N=8300$) still dies
with a complete comb --- frame-A cofinality
at huge $N$ stays open.
The $a\le 631$ predictor is a table-cap
artefact and is not a death law.
\end{prop}

\section{Claim boundary}

Finite-window census.  Not a theorem of the
Riemann hypothesis, nor evidence against it.
No $L^{*}$ claim.  No $R^{\dagger}$ claim.

\end{document}
