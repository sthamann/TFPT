# Draft email — to the Anthropic zeta research team

**Subject:** An exact positive-index-8 form of a registered Weil-positivity target, a closed finite surface, a machine-verifiable SOS proof object, and a three-level map of what remains — possible overlap with your moment/inertia method

Dear Anthropic zeta team,

I lead the prime/zeta line of the TFPT project ([fixpoint-theory.com/prime-front](https://fixpoint-theory.com/prime-front)), a machine-verified research program around a Galerkin discretization of Suzuki's localized Weil operator. I'm writing because a recent exact identity on our side lands, verbatim, in the currency of your two-thirds result — and because the finite side of our program has since closed end-to-end, leaving one precisely measured, twice-characterized open lock that seems like a natural collaboration target.

The identity first: our program's one open inequality is carried by a fixed 8×8 core M = [[n, b*],[b, B]] per window rung, where (with B ≻ 0) M ≻ 0 ⟺ n − b*B⁻¹b > 0, and we maintain a registered, frozen target n − q ≥ ½μ₁(h) (no-adjust clause, registry-hashed; 67/67 surface, first blind holdout 28/28). Machine-verified exactly (symbolic Fractions, boolean-equal on 39/39 core steps and all 67/67 registry-matched rungs): for an explicit congruence scaling A(u_h), **(tr A)²/‖A‖²_F > 7 ⟺ n − q > ½μ₁(h)** — your rank-trace/positive-index statistic. Because the core is fixed at dimension 8, integrality replaces the asymptotic-population problem: a ratio above 7 forces n₊ = 8, full positive-definiteness. Running the dictionary: the source-only *two-moment* criterion fails 0/39 — confirming your two-moment no-go inside our geometry — while at four moments the bound sits near threshold ladder-wide (UB₄ = 0.83–1.60).

The second development, from the same day (§6 of the attached summary): the finite surface has closed. On all 39 matched surface + 8 deep rungs of the deployed ladder, the entire composed wall — **B ⪰ 0.5523·I (interval-certified over the ideal source objects, 39/39; the exact-rational LDL tier certifies 0.5914 on the computed matrices) ∧ W1 (67/67) ∧ W2 (67/67 surface + 8/8 deep)** — is certified from cited inputs: verified zeros (Odlyzko's 2M table plus LMFDB/Platt to 2×10⁷ ordinates, all below the Platt–Trudgian height 3×10¹², consumed through a certified cache battery), unconditional envelopes (N(T)/Rosser, Büthe), and exact warded composition. Our route audit fixes the phrasing: positivity certified on a *finite family of Galerkin sections*, W2 along the measured critical direction. The closure's economics is itself a clean, reproducible statement — a measured **transfer law**: ZerosCertified(T) ∧ TailEnvelope(T) ⟹ M_h ≻ 0 for all deployed h ≤ H(T), with H(7000) = 254, H(2M) = 1256, H(20M) = 2806; the required supply grows like T_req ~ h^2.8 and outruns the spectral window reach ~ h². The zero supply is an external battery: the finite engine does not scale by buying more zeros. (Both the closure and the transfer law have since been promoted into the load-bearing, audit-gated verification suite — modules v909/v910, 904-script suite, all gates green — with the honest typing frozen there: W1/W2 are algorithmically independent evaluations of the same localized Weil form, a mutual crosscheck, not two proofs; the promoted gates re-verify a reduced frozen subset, and the full census and full-ladder laws are cited there from the frozen experiments runs, not re-proved by the module gates.)

To be plain about status: we have no RH proof and claim no progress toward RH. What we have, in one sentence: **a regional finite positivity theorem, an independent machine-checkable certificate object (with a tiny stdlib-only checker), the complete extraction chain, and a single named open inequality — one scalar anti-cancellation statement whose sign is decided only at resolution n ~ 2¹⁷** — explicitly *not* "almost RH": the proof object is ready for audit; the RH quantifier is not. The reduction chain (cofinal finite positivity ⇒ Weil positivity ⇒ RH — "cofinal" in the mesh-refinement order in which the form convergence holds, not merely in the window or cap parameter) is an exact reformulation at RH strength, theorem-grade modulo four named classical citations — the Suzuki/W1 identification, density plus the equi-Lipschitz/Dini continuity modulus, the faithful cap, and Weil's criterion — so "one open inequality" counts what we must supply, not the chain's total non-proven content; every certified statement is per-rung on the finite deployed ladder; the finite closure consumes published zero verifications as citations; the registered target remains a falsification instrument, not evidence; and the all-h, all-direction Weil-positivity object remains open and RH-hard. What separates the finite result from RH is the depth quantifier — *all computed h* → *all h* — together with, for the finite closure, the direction quantifier (its W2 is certified along the measured critical direction only; the pure one-quantifier form holds for the Level-1 cells of the σ-chain map, where full M ≻ 0 is certified per built cell) — and by our own measurement the depth quantifier falls neither to more zeros nor to any tested certificate architecture.

An overnight five-contract census (§7 of the attached summary, all frozen-spec machine-verified) sharpened that last sentence in both directions. On the constructive side, a new congruence-free **one-bad-mode moment certificate** — Cauchy interlacing off the certified B-floor plus a squared Chebyshev separator, tr p_r(M) < 1 ⟹ M ≻ 0 from power sums alone — is non-circular and certifies **all tested truth windows (68/68, integer-exact decisions) and nothing on any indefinite control world**, with a measured *h-flat* order price r* = 0.544·√(λ_max/c_B): the first certificate form whose price does not grow with depth. On the diagnosis side, the lock is now characterized twice, and the two characterizations agree. Read side: the true arithmetic reads at the critical direction do **not** decay (h^{+0.061}) — the h⁻² level scale sits in a *phase-sensitive cancellation between two O(1) terms*, and all four non-circular certificate shapes tested certify 0/75 while truth passes 75/75. Gram side: the directional energy bound that four independent routes had named as the missing lemma is *true* — as a one-line corollary of the wall itself, hence control-blind (it holds in all five falsifying control worlds) and forever unusable as an input — and the lock relocates to a lower bound on **c_h = 1 − λ_max(N_h, P_h) ∈ [1.4×10⁻⁸, 1.7×10⁻⁴]**, with a measured deficit of +2.81 dex median, growing. The honest open problem in one sentence: the round moves no RH marker — it closes the local transition inductions, the linear positive-measure completions, the verified-zero bootstrap, and the Christoffel energy class on their declared surfaces (the exterior algebra explains the frozen ½ and the Lorentz signature of the seat form, but not the sign), and the remaining scalar is the contraction defect c_h, whose size sits in phase-sensitive cancellation between O(1) archimedean and prime reads — so the remaining analytic object is an **Euler-factorized phase identity, not a magnitude bound**. A same-day phase round then sharpened the open object once more: the Euler phase is now *exactly constructed* — the wall read is the derivative of a completed unitary Euler phase, an exact identity that survives all five falsifying worlds, i.e. a coordinate change — its naive Krein/Pick index is measured out as a world-separator (negative index proportional to resolution, though the wall block is identified as the exact diagonal congruence image of the Krein kernel), and the one-bad-mode certificate compresses to a *single fixed 8-pole rational filter* (68/68, the certificate becoming finitely many determinant-phase values, at an n-read sensitivity above halfgap grade). Shortest form: geometry explains the half; exterior algebra explains the signature; a fixed 8-pole filter bounds the observer complexity; the Euler phase is constructed — open is *which functional of it separates the worlds*, with the grouping's parameter-free weight law as the first measured candidate.

A second same-day research lane (§8 of the attached summary) then replaced our last remaining shortcut by a derived object and left the program with a clean **three-level map** — which is the honest status I would want you to have. The lane began by killing its own best number: a σ ≤ 0.665 cap that numerically matched a measure-side invariant turned out, by provenance, to be the probe's own margin convention (0.604556 × 1.10 — a proven coincidence), and σ itself is exactly the wall's Schur quotient σ = 1 − s/n, so any cap on it imports the open half. It was replaced by a derived chain, and the map now reads: **Level 1, solved on the built surface** — every built wall-legal cell carries an exact-rational, source-only positivity certificate (n > 0, certified ordered co-block floors, and a Gauss–Radau/SOS moment bound): σ ≤ 0.727, hence M ≻ 0, on the 151-cell census, and the same chain below the definiteness bar 1 on 59/59 deep steps to h = 6344 (worst certified bound 0.7876), the first wall-positivity chain with *no member in the cancellation currency* (margins O(1), all relocation screens pass) — with the conditionality typed by a subsequent adversarial audit: per cell the certificate proves τ(r1)·S(r2) ≻ 0 (positivity of the τ-normalized step matrix; the previous rung's τ > 0 is an uncertified float64 selection gate of the build), not S(r2) ≻ 0 unconditionally, so the chain consumes wall positivity through its selection filter, while the same audit independently re-proved all 151 SOS certificates with a fresh stdlib-only verifier. **Level 2, the class relation** — separate moment envelopes provably leak (sup 2.26); the *joint* Radau relation lifts definiteness to class level (inf λ₁ = +0.008 over the entire class, against −574 admitted without it); and the whole optimization now compresses to a **machine-verifiable proof object**: 1111 exact rational SOS certificates (interval-SOS identities with closed-form Markov–Lukács Grams, PSD by structure, zero numerical error), census 151/151 at η = 0.273 — since hardened into a **theorem package** with three separately auditable certificates (the joint relation verified *symbolically*, sympy on free symbols with no numeric input; the per-cell positivity data, one exact rational witness per cell; and an honest coverage certificate stating exactly what region is covered: the class box modulo the relation, plus the 151 sampled cells with explicit rational neighborhoods) and an independent **~200-line stdlib-only checker** (Fractions and hashes, no matrix theory, no numpy/sympy) that re-verifies the full package from scratch and dies on any doctored coefficient — the checker is the audit entry point, and this package is the concrete artifact we would most like to put in your hands. (Full disclosure from our own adversarial audit of the checker: beyond that declared tooth — which does bite — six acceptance bugs were confirmed, the worst passing a silently truncated moment list with a declared bound of −5.06×10⁶ as "ALL PASS", and the package has no external anchor below the 15-ulp entry radius; a hardened v2 now ships alongside the frozen original (versioned rather than edited, since the package pins the original by hash), refusing all six planted classes with named messages and anchoring the certificate census against frozen external constants; and the audit independently re-proved all 151 + 7 certificates with a fresh AST-firewalled stdlib verifier, so the package mathematics stands.) The one named residual — the coupling currency — has since been *built*, as a certified moment/determinant dual: it excludes the plateau ceiling's thin point as a phantom of the independent-seat decomposition and narrows the certified class window to [0.9727, 1.2402], but the compression stays honestly **PARTIAL** (the positivity side is untouched and independently closed). **Level 3, the quantifier, measured to its endform and then hardened** — the one-atom identity is exact (literally: b becomes an eigenvector of B, and a rate for the angle is a rate for the form), but the collapse is measured *unfinished*: no decay rate, a misalignment that grows, driven by a cancellation of giants (median factor 10⁵); the direct envelope reading closes razor-thin (+0.0104), the budget reading does not close, and the limit point has an exact-rational positive interior distance (6/6 deformations). A same-day hardening round then typed the level's three missing bounds: the Lipschitz constant is now **certified** (L_plane = 1.3125 against the 1.2975 fit, a 400-bit interval proof object — the first proved Lipschitz object in the chain), the Rayleigh-deficit growth is **proved self-limiting** against an h-stationary cap, and the spectral gap is **reduced to one measured scalar** t = ν₁/(ν₀‖B‖_F) > 4/5 whose carrier is the arithmetic AR–OSC cancellation (the hoped archimedean seat measured false), with the razor margin typed as an irreducible measurement no certified constant can move. And it pushed the legality frontier past h = 8003 — where a clean-instrument audit round (2026-08-13) then retro-corrected the deep map end-to-end: the deep legality holes, a measured termination signal, and the deep witness cells were **all instrument artifacts** of float64 polynomial evaluation in the deployed chain past its resolution floor (the defect exhibited exactly, stage by stage — the chain is sign-reliable only to h ≈ 3948, and the guard is now fail-loud); on the clean instrument — direct Weil-kernel evaluation plus exact-rational inertia — **every built cell to h = 13026 is positive or no-witness**, the corrected ladder runs to h\* = 10513, and every affected reading is retro-typed in a correction ledger (5 retracted, 3 superseded, 2 unaffected): the construction has no measured end — where, typed by a follow-up assembly audit, the deep positives are measured-positive (reproduced end-to-end at 70-digit input precision), not assembly-certified: the certified assembly error bounds sit 10⁵–10⁶× above the ~10⁻¹⁵ margins. I mention the episode deliberately: the false signal was found, exhibited, guarded, and retro-corrected by the program's own audit discipline within one adversarial round — which is the property I would want a collaborator to be able to rely on. The same day's localization lane then finished the diagnosis: three of the five names for the deep coupling are literally the same number (κ = 1), the completed square is degenerate, the Mellin positive-energy form does not exist, the decoupled prime/geometry split fails by 5% — what remains is **one scalar anti-cancellation inequality**, v₋ᵀXv₋ ≥ −λ_min(G₀), a prime-only quadratic form against a geometry-only number with relative margin O(10⁻⁴), alignment-carried (the comb wins by direction, not size); and Selberg's symmetry formula — the strongest classical unconditional source, structurally a perfect fit — is measurably insufficient by 4–5 orders at the min end (median miss ~9×10⁵, ≈6 orders), with the theorem-grade identity A_err − B_err = P_err showing why no rewrite-then-estimate route can work: the sign is decided only at resolution n ~ 2¹⁷, practically the whole window. All measured rates are conjecture-grade; nothing at any level claims RH progress.

One further lane of the same day belongs in this status precisely because its headline is a **withdrawal** rather than a finding. An adversarial audit retracted our own θ-mean certificates for a grid-offset extension of the wall family, registered earlier the same day: the piecewise-affine Hermite–Hadamard premise they used is provably false — a 90-digit source rebuild shows the archimedean term (a Lerch-type series inside the kernel) makes the full θ-family non-affine, with the atom part enclosed around zero and the archimedean and full-source second differences strictly excluding it at all five depths — so the honest corrected mean enclosures are **[−∞, +∞]**, and what survives is five rigorous **point** certificates at θ = ½, two of them new and deep: h = 1393 with s ∈ [2.27451283174326270×10⁻⁹, 2.27451283174326353×10⁻⁹] and λ_min(B) ≥ 4.2981529638673363×10⁻¹¹, and h = 2854 with s ∈ [2.12003444567896238×10⁻¹⁰, 2.12003444567896289×10⁻¹⁰] and λ_min(B) ≥ 1.4729876002709596×10⁻¹² (congruence-preconditioned Arb interval LDL/Schur; controls refuse 4/4 and planted source shifts refuse 2/2 before any sign is released). The same round closed four further routes as measured negatives, each under a bar frozen before the reads: the all-depth version of the θ-mean misses by 11–14 orders even at deliberately optimistic classical constants, with the B-spline comb energy entering the mean with the *wrong sign*; both Ξ-resolvent/determinant-limit routes are vacuous (best true-vs-scramble separation 2.97 against a frozen bar of 5), after the same lane found and repaired a normalization bug that had gated convergence on a dimension-normalized trace whose residues cannot match −Ξ′/Ξ; a candidate "self-pairing" principle behind our frozen ½ is dead in its k-arm operationalization (k = 2 saturates 41/41, k ≥ 3 is 0/41 with depth-growing negatives, so no constant repairs it — while the exact k = 2 window provenance of the ½ stands); and the search for an independent multiplicative sign source ends in a **local obstruction** with the missing inequality named exactly — P_err,h ≤ Q_h − P_main,h − need_h on one predefined cofinal family, from a source theorem that rejects our Epstein/scramble/smooth control worlds — hence explicitly RH-hard, together with a transform-stable inertia no-go (Möbius/Dirichlet convolutions are unitriangular inverse congruences, so Sylvester preserves the residual's negative direction) and an exact PSD dual showing cross-prime bilinear terms cannot repair the Euler-local blocks. I report all five because the honest inventory of live routes is shorter after the day than before it — and because the retraction is the same audit discipline working: the withdrawn claim never entered a paper body, a ledger row, or the public mirror.

Those separate no-gos have since been composed into a single typed statement, and its exact, cheap core is now a machine-gated no-go module (`v913_signed_alignment_localization.py`, 24/24). The localization: for the deployed budget shape the missing inequality is **not** a magnitude statement — the conversion constant of any hypothesis |ψ(x) − x| ≤ f(x)√x is bounded below by the exact kernel identity ‖K_D′‖₁ = 4, uniformly in D, against an O(1) negative geometric supply, so Littlewood's unconditional Ω-theorem empties the entire magnitude class (floor against slack by a factor 214.7 on our own conservative slack, 1527.1 on the deployed one). The residual requirement is instead odd under the comb sign flip and invariant under every congruence of the matrix stage, so no reformulation, window choice or preconditioner converts it and no unsigned hull can bound it below; Selberg's symmetry formula is a formal identity and therefore neutral; and the Beurling–Nyman/Baez-Duarte target has no N-uniform Gram floor in exact arithmetic. **Any future input must therefore be signed and must carry alignment information** — which is exactly why your moment/inertia machinery is the interesting direction here rather than any sharper magnitude estimate. The scope is stated with the same care: ten argument classes are proven empty for this budget shape, five are merely unexplored (alignment statements among them — untouched, and exactly what is needed), five clauses are named open (nothing is said against arguments that legitimately consume zero positions, and the reverse implication rests on four unestablished premises), and the theorem is finite-rung (h = 184/388/839 with the ladder to 12632) with no all-h claim. It says neither that RH is unprovable nor that our wall route fails, and the counter-evidence travels with it unsmoothed: the certified reads are positive at all nine depths (deepest 2.79579794131272506×10⁻¹⁵ at h = 12632), the normalized decay flattens with depth and one re-derived step is positive, so the drift is not monotone. The row is typed as a no-go: it closes no gate, narrows no gate and is not progress — a no-go about the *type* of the missing input is not evidence about its *existence*.

A consolidation pass has since turned that whole campaign into one falsifiable object (§8.9 of the attached summary; experiments-level, no promotion, no RH claim). The RH implication chain is now an explicit 11-node DAG with **exactly one open edge** — the mesh-cofinal ladder positivity, the chain's four remaining legs staying named classical citations — six formerly separate coordinate systems machine-pinned as that one edge, and the candidate map is **complete and adjudicated: 24 candidate sign sources, 0 pass, 19 fail, no blanket-untested class left** (an externally proposed projection/alignment closure adjudicated as coordinate system #7 of the same edge, its selection rule kernel-checked as sign-mined; the five formerly untested classes each failed with exact killing numbers). What survives is exactly characterized: a signed, alignment-carrying, *unconditional* ordinate-position statement at ~1% of the mean zero spacing, whose minimal form (MIN-U2) already excludes off-line zeros below every classical zero-free width on the deployed windows — beyond the classical unconditional toolbox — and the characterization is measured to be shape-independent across five audited admissible window families (alternative shapes buy margin only at the price of the density leg; the existence/extension direction closes at the border-membership step in exact arithmetic; the finite-input bootstrap is dead at T_req ~ h^4.77). We call the consolidated endpoint the **Obstruction Completeness Theorem** — finite-rung, editorial-completeness typed as editorial, with the counter-evidence carried unsmoothed (all certified reads positive to h = 12632) — and to be precise about what it is: a machine-checked answer to "why does RH not fall to this program?", not evidence about RH in either direction, and emphatically not a statement that RH is unprovable. For a collaborator it is, we think, the most useful artifact of the campaign: the exact, measured specification of the one input that is missing.

Six same-day follow-up rounds (§8.10 of the attached summary; experiments-level, no promotion, no RH claim) then attacked the construction direction the theorem leaves untyped, and the missing object now has its most concrete form yet. The first architecture to genuinely leave the kill atlas — source-only *self-adjoint* operators, real spectra by construction, no wall positivity consumed — was adjudicated by three independent lanes and an arbiter to **REALROOT-DEPTH-UNDECIDED**: the mesh family fails a *proven* Nyquist-density obstruction, the extremal family stays exactly on the Ξ census through x = 21, but a Z1-transfer identity shows its Galerkin matrix *is* the Gram matrix of zero evaluations — source-only in input, zero-measure in content — and the surviving convergence statement (R3) alone implies Weil positivity, hence carries full RH strength; the route's measured support was separately convicted circular (the recorded counts are the smooth Riemann–von Mangoldt values to < 1). The persistence lemma behind it was then reduced — with no analysis, a finite combinatorial rewriting we call the **Parity Lemma** — to the sign pattern of one explicit matrix's ground eigenvector: the *number* of sign changes is forced by a counting law (one fixed factor e^(−11) per forced skip, constant over 23 decades), and only the *positions* remain open, supplied by the prime block alone. Against those positions a **source-only predictor** — the parity of a rounded band-limited prime phase at the Nyquist lattice — measures *exact at all 79 reachable lattice points* (x = 3/5/8/13, arithmetic load exactly 4; a scramble control fails 6/11), with the catch named precisely: exactness for all x is half-integer rounding-margin control of one explicit band-limited function of the primes (measured minimum margin 0.0317) — the same RH-hard seat for the third time, now as a rounding-margin statement. An adversarial bughunt over the whole span (nine findings, 3 MAJOR / 0 FATAL, no verdict flips) corrected three misstatements in the earlier records — the honest zero-tracking claim came out *stronger* than published — and convicted nine hardcoded pass-gates in our own sandbox tallies, which we record as a methodology limitation. Nothing here is evidence about RH in either direction; what it adds for a collaborator is that the missing input of the Obstruction Completeness Theorem now also has an explicit, cheaply computable *candidate carrier* — the rounding margins of a band-limited prime function — at which any new arithmetic idea can be aimed directly.

Two closing rounds (§8.11 of the attached summary; experiments-level, no promotion, no RH claim) then did the same for the *resolvent* direction, and the missing input now has **two precisely stated lemma forms**. An externally proposed **Stieltjes–Vitali pin route** — countably many positive resolvent scalars in the absolutely convergent Euler half-plane in place of the full trace convergence, with Herglotz/Vitali/identity-theorem steps machine-audited gate by gate — survives every gate (the second architecture of the campaign to do so), at the honestly typed price that the chain moves the entire burden into the pin convergence itself, and on the reachable family that convergence is measured to be a transcription of cached zeros. The one priced missing ingredient was then built: the **Krein inverse-spectral realization of Suzuki's screw function** — hypotheses certified, normalization derived rather than fitted, and the realized Hamiltonian visibly *seeing the primes* (Verblunsky spikes at r = log q for q = 2, 3, 4, 5, 7). It is the first source-only host whose measured convergence is *not* a zero-cache transcription (all 16 frozen σ rows converge truth-tight under a measured Weyl-disk contraction law whose disk center resums the missing tail), and its controls die at the positivity level itself: the prime-free smooth accelerant *loses screw positivity* at finite window depth — **the true primes are necessary for screw positivity**. The repriced lemma is exact — uniform Weyl-disk contraction of one explicit canonical system on σ ≥ 1/2 + ε given all-a section positivity — and so is its price: that hypothesis *is* localized Weil positivity. For a collaborator the point is symmetry: the same single missing input now stands in two independent, precisely stated currencies (a rounding-margin statement about one band-limited prime function; a disk-contraction rate for one explicit canonical system), each with a machine-checked map of everything around it.

A closing round (§8.12 of the attached summary; experiments-level, no promotion, no RH claim) then compiled that map into the artifact we think is most useful to anyone working on the *geometric* side of this problem: a **cohomological specification sheet** — seven numbered, machine-grounded properties that any cohomology/intersection theory for Spec ℤ in the sense of Deninger's program or the Connes–Consani scaling site must supply to close the one open edge. The sharp ones: the theory's pairing evaluated on exponential autocorrelations is *forced by an exact identity* to reproduce explicit Euler–Pick matrices built from ξ′/ξ(1/2+σ) — so a proposed cohomological trace formula can be *tested against this interface today, at any finite N, by machine* — and its structural positivity must fail on our control worlds exactly as measured (the prime-free world dies at window depth r = 0.264 by a support error, the weight-scrambled world at r = 0.744: the pairing must consume the exact prime support *and* the exact log p/p^(m/2) weights), must be shape-independent, cannot be a magnitude/density/existence statement, and must not be the wall scalar renamed. The function-field precedent is mapped to where it currently breaks — Deninger's trace formula is now proved on the smooth-foliation side but has no arithmetic host; Connes–Consani have Riemann–Roch on the periodic orbits of the scaling site but not on its square, which is exactly the surface substitute whose Hodge-index inequality would be the structural positivity. To be plain: the sheet is a requirements document derived from machine-checked no-gos — it proves nothing about either program and claims no progress on RH; what it adds is precision, a testable interface where there was an analogy.

Nine further freeze rounds (§8.13 of the attached summary; experiments-level, no promotion, no RH claim) then finished the instrument tier, and the status I would want you to have is this: the one missing input now stands in **three equivalent lemma forms** — uniform Weyl-disk contraction of one explicit canonical system, the sign positions of one explicit ground eigenvector, and Hausdorff cell positivity of one explicit Pascal field (an externally proposed moment-field criterion whose equivalence skeleton we audited sound; honestly NOT-WORLD-NEW — Zhang arXiv:2303.09396 has the identical criterion at the central point, the novelty being the safe-point shift, the source-only decoder and the certified conditioning) — replicated across three agreeing source-only carriers with no zero cache anywhere in construction, and guarded by **two certified falsification instruments**, each an open and empty channel where a single certified negative would be an RH-disproof candidate: the Euler–Pick ladder, now certified through N = 4 at sieve cap 10¹³ (π(10¹³) reproduced exactly; the knowledge wall starts at N = 5, beyond the verified-zeros range, so no compute helps), and the Hausdorff field at certified depth 86 with γ\*-reach 189/563 — the cheapest certified RH-consequence instrument we have. Twenty-seven pieces of the reduction chain are now kernel-checked in Lean (Euler–Pick forward PSD, the δ₁ no-go, the full Parity Lemma under one named simplicity hypothesis, and the SV skeleton with an explicit honesty lock — an instance with RH = False showing the packaging alone yields nothing; axioms exactly propext/Classical.choice/Quot.sound). One correction of record travels with this update, found by our own pricing round: the round-88 sign-position predictor's operative extension is measured **false beyond x = 13** (567 τ-screened violations to x = 890; a small-x accident of the margin budget, arithmetic-free mechanism, the budget classically out of reach by ×8.2 and above even the RH-conditional bound) — the published finite claim stands, its extension is dead, and TPL(i) itself is untouched. And the interface point for your side: we adjudicated the one external published RH reduction — Connes–Consani–Moscovici's P(n) — and its published local terms reproduce our Euler–Pick matrices **entrywise** (ξ′/ξ to rel 2.3×10⁻⁵⁰), with all six prime-bearing Galerkin sections positive as a new finite instrument: P(n) *is* localized Weil positivity in the corpus's measured currency, so any semilocal prolate candidate is finitely testable against this interface today.

Finally (§8.14 of the attached summary; experiments-level, no promotion, no RH claim), we compiled the whole machine-checked map into the one artifact the program never had — and the one I would most like to put in front of potential collaborators: a **staged, milestone-gated research roadmap** from today's frozen state to a complete proof, in which every stage is falsifiable against our own instruments (ROADMAP-COMPILED, 29/29 machine-asserted gates; every acceptance value re-computed or grep-warded in its owning frozen artifact). Stage 0 is done and frozen — the candidate atlas now complete at 25 sources 0 pass (the positive-proportion class of Selberg/Levinson/Conrey/PRZZ was the last unexamined classical type; it prices out final: a proportion theorem would need κ > 0.995–0.999 before it could feed the wall), plus the largest proven positivity region of the campaign, C_{n,k}(256) > 0 for all n and all k ≤ 10¹⁹, from Platt–Trudgian and Rosser alone. Stage 1 is the missing substrate — an arithmetic dynamical system hosting the now-proven foliated trace formula (Deninger's side), or Riemann–Roch on the square of the scaling site (Connes–Consani's side) — and the point of the roadmap is that **each milestone carries a minutes-scale acceptance test**: any candidate trace formula must reproduce our (AC)/Euler–Pick interface at 16 frozen σ values, and any candidate intersection pairing must reproduce the certified 𝒫_N ladder, so a proposal can be adjudicated by machine the day it arrives. Stage 2 is the Hodge-index-type inequality itself, with the acceptance gates being our measured consumption requirements (it must fail our control worlds exactly at r = 0.264/0.744, be signed and alignment-carrying, survive the τ-screen, and strictly extend the certified regions). Stage 3 — the descent from that inequality through localized Weil positivity to RH — is assembly our machine already holds (the extraction chain, the rate theorem, the Lean kernel tier, the equivalence skeletons). To be plain, as always: a roadmap is not progress on RH, no stage is claimed started, and the mathematics of Stages 1–2 does not yet exist; what the roadmap changes is that every step is now interface-defined and immediately falsifiable — which is exactly the property that makes it a collaboration offer rather than a manifesto.

Since the roadmap froze, four further freeze rounds (§8.15 of the attached summary; experiments-level, no promotion, no RH claim) extended it — and, more important for you to have, audited it; I owe you two corrections and two results. The corrections, from a dedicated big-picture logic audit and an adversarial bughunt over the whole rounds-86–105 span (no number wrong, no verdict flips — the drift was prose-level): where I wrote "three equivalent lemma forms" above, the machine record supports **three RH-sufficient lemma forms, of which exactly one — the Hausdorff form — is a proven RH-equivalence** (the Weyl-disk and sign-position forms are one-sided pendants: their converses are, respectively, the round-90 open lemma and the CF-realness/Connes-§6.6 hypotheses); every mutual equivalence routes through RH with **no transfer between forms** — an explicit counterexample world keeps every Hausdorff cell positive while the Euler–Pick section fires at N = 12, so certified regions are form-local and the big Form-A theorems say nothing about the other two currencies; and the two standing disproof channels, while disproof-sound, are **reach-capped** far below Platt–Trudgian (Euler–Pick sees off-line zeros only at γ ≈ 5–8, the Hausdorff field is provably blind above γ₁ = 14.13, against verified 3.0×10¹²) — "open and empty" is a theorem given the cited inputs, not live falsification power. The results: the k ≤ 10¹⁹ positivity theorem is extended 530-fold to **k ≤ 7.4447×10²¹**, now with its general law derived — **k_front(T) = λ·T²/(a − ½) with λ∞ = 0.226987**: verification height buys quadratically many columns — from Platt–Trudgian plus the corrected Hasanalizade–Shen–Wong zero-counting bound (the commissioned S(T) constants honestly replaced), together with the exact two-variable generating function (its pole-divisor positivity an RH-equivalence, not a proof) and the measured warning that Hankel positivity alone is *not* Form A (the localizers are load-bearing); and the program's founding intuition — the primes as an error-correcting code — got its first formal adjudication under that name: **true as measured structure** (the primes are the unique codeword of a code in the compiler's own Gram grammar, minimum distance the measured rigidity law, same grammar as the compiler's E8 layer but a different dual type), and running in the **decoding** direction — the reason the RH quantifier does not compress is now a measured number: the binding check index grows with slope 1.0000, and the adversarial local-testability gap is exactly zero beyond any bounded check family's support.

Two closing rounds (§8.16 of the attached summary; experiments-level, no promotion, no RH claim) then finished both open flanks, and the second one carries the most direct interface point of this whole letter. First, the five argument classes the audit had named as outside the atlas vocabulary are now all adjudicated and killed — the headline being **Zagier's published horocycle RH-equivalence**, never before adjudicated by anyone as far as we can tell: our instrument measures the horocycle error exponent at 0.7451 in the classically verified region and, on planted-zero synthetic worlds, recovers the exact affine law θ = 1 − Θ/2 to sup Re ρ (|dev| ≤ 0.0029) — the horocycle rate *is* the emptied zero-free/density currency, and the rigidity technology that would improve it is rate-free (Ratner) or non-horospherical (the 2023 effective results explicitly exclude the horospherical case, which the modular horocycle is) — so **the atlas now stands at 30 candidates, 0 pass, with every named argument class inside and outside its original enumeration formally priced**. Second, and this is the collaborator hook: we built and adjudicated the constructive resolvent-closure architecture around the new Connes–Consani–Moscovici paper (arXiv:2511.22755), and **their operator is our record family** — CCM's rank-one D_log construction is, mode for mode, the extremal family our round 89 froze as its record (kernel to 10⁻⁶⁸, spectrum match 10/10, their det_reg formula executable as a rank-one determinant identity to 10⁻⁷⁵): our corpus had independently built their operator before their paper. More useful still, **our groundspace-block variant eliminates their simplicity and evenness hypotheses at construction level** (basis-rotation-invariant to 1.5×10⁻¹⁶, stable where the simple selection swings O(1); the exterior-determinant identity to 5.2×10⁻¹⁵; what remains is one named finite-dimensional realness lemma, ζ-free and Lean-feasible). The honest price travels with the identification: the literal CCM carrier's finite pin convergence *transcribes* the zero cache (the same Z1 conviction as our round 89 — our Krein carrier remains the only non-transcribing attack surface), the cross-carrier test shows the CCM operator moments are monotone lower bounds of our Hausdorff moments (one object only in the limit, 17/17 rows), and the remaining analytic theorem is doubly named — CCM's own §7–8 prolate approximation, equivalently our round-90 Weyl-disk contraction, pointwise at the pins. If your side has contact with prolate/Slepian machinery, this is the single sharpest place where it would land.

One promotion of record since those sections froze (2026-08-15): the two certified-finite results named above have graduated from the experiments tree into the load-bearing suite — the Pascal-region theorem (`v914_pascal_region_theorem.py`: C_{n,k}(256) > 0 for every n ≥ 0 and 0 ≤ k ≤ 7,444,682,106,464,286,365,865, proven from Platt–Trudgian 2021 + Hasanalizade–Shen–Wong JNT 235 (2022) Cor. 1.2 + Rosser 1941, with the quadratic law λ∞ = 0.226987054723979955 recomputed at every run; form-local, explicitly not RH evidence) and the certified Euler–Pick floors (`v915_eulerpick_certified_floors.py`: λ_min certified positive for N = 1..4, the N = 4 interval [8.278338×10⁻¹⁵, 1.3840906×10⁻¹⁴] at cap 10¹³ pinned from the run-of-record with the split disclosed; the falsification channel disproof-sound but reach-capped at γ ~ 5–8, i.e. zero live falsification power in the classically certified range). Everything else in §8.13–§8.16 remains sandbox, and still no RH claim in any direction.

A second promotion of record (2026-08-16): the two durable certified results of the Epstein detection arc (discovery rounds 123 and 125) have since graduated as well — the certified Epstein Weil-positivity violation (`v916_epstein_weil_violation.py`: an explicit sha-pinned unit test vector, len 1038, whose Weil pairing with the source data of Q = x²+5y² is certified negative, V = −1.9697×10⁻² ± 4.3×10⁻⁶⁸ — windowed Weil positivity for ξ_Q fails at t ≤ 3.114, built purely from r_Q(n) lattice counts, while true ζ and the conductor-matched null ζ_K = ζ·L₋₂₀ complete positivity through L = 8.0; the off-line zeros themselves are classical, Potter–Titchmarsh 1935 — the claim is the certified explicit finite certificate from source data, not the existence) and the certified localized rate detection of the driver (`v917_driver_rate_certification.py`: the driver zero 0.9330+15.6682i, source-located from the sieve, fires in the band-peeled rate channel at depth m* = 14 with certified LB = 1.000137 > 1 and is localized to its own violation window; the band peel collapses the unpeeled depth price m₂ = 908 by ×65 at sieve cost N = 2×10⁵, with a quantified detection-cost law polynomial in 1/ε via the band; grade honestly CERT-GIVEN-AUDIT with declared tail/peel models, and the one pre-declared locator gate that failed is carried verbatim). Both new rows are typed NOT-RH-EVIDENCE in either direction — a certified detector is a falsifier, and ζ's omega absorbs every finite certificate.

A third promotion of record (2026-08-18): the four durable certified theorem sets of the prime-front proof arc (discovery rounds 128–151, adversarially audited by two bughunt rounds — the second with 0 MAJOR findings) have graduated as well — **Theorem R** (`v918_doublelimit_reduction_theorem.py`: the load-bearing reduction L1 + WPD ⇒ (H-conv)+(H-trace) with the explicit rate max(1,C)·(−4log(1−t/4))·d₁, so RH ⇐ [NF-closure] + [Theorem R] + {L1, WPD} on dense a; with Lemmas N/S/X, the exact jet dictionary, and the honest typing that WPD's k-tail *is* window positivity — proven is the reduction and the coordinate, not the convergence), **the secular identity + Guinand–Weil pinning** (`v919_secular_gw_pinning.py`: det(M_η − z) = −z·C(z²)/A₀ exactly — the operator nodes *are* the census-polynomial zeros, count exactly K−1; τ = 2Σ|E_N(γ)|² within certified allowances with the certified gap law, 21/21 zone zeros pinned at x = 13 to max gap 2.9×10⁻¹⁷; the tail half of L1 closed in closed form from HSW22 + Platt–Trudgian; the crossover theorem making the frozen corpus constant INBAND_F = 0.8 a theorem; independently reproduced in the second bughunt from the classical explicit formula), **the loop/equivalence theorems** (`v920_loop_equivalence_theorems.py`: BAND-MASS ⇔ TLAWCAP ⇔ EPS-LOCK and QSUBGAP ⇔ SUSCAP2R ∧ DELTA1FLOOR, both directions exact, plus the pinch identity and the source-pure interlacing DELTA1FLOOR ⇐ FULLGAP — the anti-double-counting layer: no future argument can count the looped coordinates as separate progress), and **the counting-class closures** (`v921_counting_dominance_closures.py`: unconditional counting dominance N_fin ≤ N_true for all T ≥ T*(x); TAILVIS eliminated as a standalone arithmetic omega from HSW22/RvM alone; the conditional assemblage and its Q-swamp obstruction carried, not smoothed). All four rows are typed NOT-RH-EVIDENCE — reductions, identities and counting closures that close no omega (the census stays at cardinality 4), with every conditional leg typed.

A fourth promotion of record (2026-08-19): the four durable certified theorem sets of the spectral-balance/edge arc (discovery rounds 152–161, Lean-hardened in a second Lean round — five new Lean 4 modules, 28 fully proven theorems on the three standard axioms) have graduated as well — **the HPIN/spacing theorem family** (`v922_spacing_jet_sumrule_theorems.py`: the derivative/spacing-product identity — the H-pin floor is a zero-spacing product — the jet sum rules of both orders, the A₀-cancellation and the certified lattice tail; the spacing product now proven in Lean for generic finite K over an arbitrary field), **the pinch/enclosure/razor family** (`v923_spectral_balance_razor_theorems.py`: the trace loop with its tail closed unconditionally — DELTA1FLOOR ⇔ TRACEFLOOR two-sided — the chi-cap, the enclosure ρ²δ₁ ≤ g ≤ δ₁, and the razor 1/(s + 1/δ₁) ≤ g < 1/s with *two-level equality at the lower end*: the g-floor is exactly the s-cap given the δ₁-floor, nothing in between; the value-merge with the moment family refuted at algebra level), **the moment-Laurent/root-ladder family** (`v924_moment_laurent_rootladder.py`: the escaped census-root ladder is the zero set of Φ(z) = z − 1 + Σ J_{m+1}z⁻ᵐ; the quarter-cap J₂ ≤ 1/4 + z₀|ρ| proven with measured headroom; the cascade certifies the escaped-root cap source-pure; J-boundedness arithmetic-pinned by exact witnesses), and **the edge/cleanup closures** (`v925_edge_cleanup_closures.py`: census row Z8/simplicity falls by subsumption at zero cost; the Q-swamp closes wholesale with all 357 Turing-class census certificates *recomputed at every suite run* from the committed PT21-warranted cache — Theorem A's conditionality sharpens to every integer x ≥ 3). The suite stood at 918 modules after this wave; all four rows are typed NOT-RH-EVIDENCE — identities, enclosures and finite/classical closures that close no omega (the census stays at cardinality 4), with every conditional or measured leg typed. The round-160 informative-negative (no single prime property forces the J₂-window; the single-lever search closed) deliberately stays experiments-side.

A fifth promotion of record (2026-08-19): the five durable certified theorem sets of the endgame arc (discovery rounds 162–169) have graduated as well — **the quartic/growth-law family** (`v926_fullgap_growthlaw_theorems.py`: the exact leave-one-out instrument — GL2 arrowhead compression, proven — the razor collapse of the one-row price question, and the quartic law J == Θ·T_z⁴ honestly typed MEASURED with the independent Bughunt-V free-exponent/AIC adjudication recomputed in-run: p = 3.9072 ± 0.1136, fixed 4 preferred), **the cofinal block-average substrate** (`v927_blockaverage_substrate_theorems.py`: the extraction theorem, the wall dictionary — the determinant chain is the τ sign — and the budget floor τ ≥ zsum − OFF proven-mod-cited below the verified horizon: positive-rung-per-block certified on both complete dyadic blocks), **the depth-block transfer family** (`v928_depthblock_transfer_theorems.py`: DT1 proven; the demand law DERIVED — proven-by-recipe, the dyadic factor falling onto 2^{3/2} — the census 3/2-law sympy-exact so the budget route can never become self-supporting; universalizing the census machine-detected as the RH loop; the Bertrand entry interface sieve-verified), **the sigma-floor terminal factorization** (`v929_sigmafloor_factorization.py`: σ == (1 − slop)·δ·DC exactly at all 25 rungs — the r168 σ-flatness explained as two trends; the DC leg classical per census; the jet-mass-floor named as the terminal λ-uniform residue and typed OPEN), and **the window-instrument mechanism record** (`v930_window_instrument_mechanism.py`: band positivity is exactly the carrier of the PSD premise at τ-priced thresholds; no partial comb is positive — the cone entry happens only in the last atom's tail with the tangent criterion from the prime source, re-confirmed 4/4 at true dyadic pairs; the Stieltjes-moment hypothesis for the slot ladder refuted). The suite stood at 923 modules after this wave; all five rows are typed NOT-RH-EVIDENCE — identities, recipe-derived laws and finite/classical closures that close no omega (the census stays at cardinality 4), the quartic law not claimed proven, the jet-mass-floor open. The round-163 Selberg-effectivity harvest-negative and the round-165 definitive census deliberately stay experiments-side.

A sixth promotion of record (2026-08-20): the five durable certified theorem sets of the triple-cofinal arc (discovery rounds 171–175, covered by the seventh adversarial bughunt with 0 verdict flips, its corrections of record adopted verbatim on every surface) have graduated as well — **the jet-mass product-floor theorems** (`v931_jetmass_floor_theorems.py`: the product floor F/A₀ ≥ (1 − c*/z)^((1+κ)/c*) from the census product identity + the Vieta trace at coefficient level + a concavity lemma, conditional exactly on {H1 + H2 per rung} — the exponent *is* the trace; the certified curve under the truth at ~100k true tail zeros; the round-169 measured-lock ancestor eliminated; the witness modus-tollens boundary), **the TOPROOT statement and the quartic certificate** (`v932_toproot_theta_statement.py`: TOPROOT stated SEQ-cofinal with three locked forms; the exact rate dictionary a = p/2 − 1 proven at p = 3, 4, 5; theorem TR-CAP proven and typed circular-for-TOPROOT; the admissibility ceiling recomputed, p_max = 10.2695 — six powers of margin; H3 certified 26/26, source-pure, refutable and not norm-continuous), **the H3-cofinal adjudication** (`v933_h3_cofinal_adjudication.py`: the saturation is real and exactly quartic at depth, p_loc → 4.024; pigeonhole proven yet subsidy-empty, quantifier gap D = 0.004183; the witness excluded by the certified manifold, escalation-stable, DK-norm-stable at measured residuals — scoped to the DK radius), **the unconditional Gonek pricing** (`v934_gonek_pricing_unconditional.py`: Landau 1912 and Gonek 1985/1993 identified unconditional, Gonek 1984 machine-checked not-an-ancestor with the hidden loop flagged; the envelope closed form census-depth-insensitive; ĉ ≤ 0.086 at signal/bound up to 2.7×10⁴ — the Gonek item leaves the residue), and **the mode-level Landau bridge** (`v935_thetainf_landau_bridge.py`: the program's first linear per-mode classical identity — the arithmetic fluctuation carrying ≥ 99.2% of θ evaluated from the verified zero census alone, residuals 1.4×10⁻⁵ → 5.4×10⁻⁵ at 20M zeros; the variational conditioning wall measured, T_req diverging to 1.75×10⁷⁷ vs T_PT = 3.0×10¹²; θ_∞ re-typed open-nonperturbative-variational). The suite stood at 928 modules after this wave; all five rows are typed NOT-RH-EVIDENCE — the census stays at cardinality 4, and the arc's terminal residue is stated in the bughunt-corrected form: **the composed per-block hypothesis is the *triple* {H1 AND H2 AND H3}-cofinal (one rung per dyadic block, all three at the same h — not H3 alone; the limsup form only mod the measured defect D = 0.004183)** + {census-forall-k == loop, flagged} + {L1, WPD}.

A seventh promotion of record (2026-08-20): the six durable certified theorem sets of the post-bughunt arc (discovery rounds 177–182, covered by the eighth adversarial bughunt with 0 verdict flips, its corrections of record adopted verbatim on every surface) have graduated as well — **the tau-free DK exclusion theorem** (`v936_manifold_invariance_exclusion.py`: sin θ ≤ (r₀/τ)/(FG − a) with τ cancelling in ratio form — the manifold exclusion universalized as a direction-universal, witness-free theorem for h ≤ 13 at the frozen 1e-25 bar (measured to h = 20), plus the exact required-bar schedule b*(h); the refuter price cited paired: proven 23.8/21.7/15.7/4.4 orders above the bar at h = 4/5/8/13, none at h ≥ 16, alongside the measured 30–33-order full-space ladder with alignment-sharing as the mechanism; BA3 adjudicated not universal; the induction route priced dead as a reduction — transport relabels, levels are cheaper than increments), **the L1/WPD consolidation** (`v937_l1wpd_closure_reduction.py`: WPD's λ-uniform content IS the H-pin edge, AND-fire demonstrated at the declared edges — the pair {L1, WPD} carries one open λ-uniform edge, not two; the three-part bridge-expressibility verdict; the v925 all-x sharpening propagated; the r133/r171 name collision flagged), **the exact census semigroup** (`v938_dbn_heatflow_census.py`: the de Bruijn–Newman heat flow — the one big classical import never tried in 178 rounds — lands as a closed-form finite semigroup e^(−tT) with the exact trace law (deviation 0.0); the dictionary threefold-inequivalent; the missing half of the generator is exactly the RH-conditional pair class, flagged not consumed; the λ_h criticality ladder 2.39 → 0.216 strictly decreasing with the limit open — honestly not an off-line-zero detector), **step-A exact + the CBJ adjudication** (`v939_cbj_frame_adjudication.py`: δ_h == |J_h|²_G with remainder R == 0, proven generically — the single most promotable theorem of the arc; the cluster normal form; the Montgomery–Vaughan pricing with the exact rational margin 1/21; the global review form dead at three measured walls (dof kill n = 474 vs rank 128, seam 0.994, occupancy collapse) while the per-cluster floor carries; the Bertrand-cofinal source-only selector underperforms the block average yet clears the demand), **the sub-dof mass-fraction kill** (`v940_cbj_subdof_blockfloor.py`: the sub-dof-scoped floor route honestly killed — one dex of mass leaves the well-conditioned cell per rung (+0.999, recomputed), dead by ~125 at the B3 selector rung; the unscoped floor still carries measured; Carleson priced for the first time — wrong-direction per Ortega-Cerdà–Seip 2002, with Nazarov–Turán as the classical mirror of the occupancy law; fraction-world-separating: floor value world-insensitive, mass location world-separating), and **the frozen alignment law** (`v941_alignment_law_factorization.py`: the sharpest unexplained regularity frozen as a measured triple — half-power envelope band (0.38, 0.54) descriptive, quarter-depth wall lock R² 0.9916, deep excess 5.64–7.58 dex == the r181 kill mass; the Gram source-free proven three ways; the location law projective-linear and A₀-free — the r178 obstruction does not bite; one shared nonlinear link (the wall eigenmap); the terminal adjudication in the bughunt-corrected wording SAME-CLASS-NOT-NEW-EDGE — four distinct formal statements over the same source object, no new residue edge via machine-checked exclusion, not via identity). The suite stands at 934 modules; all six rows are typed NOT-RH-EVIDENCE — the census stays at cardinality 4, θ_∞ stays open, the λ_h limit stays open, and the arc's terminal residue is stated in the canonical form: **the triple {H1 AND H2 AND H3}-cofinal (one rung per dyadic block, all three at the same h; the limsup form only mod D = 0.0042)** + {census-forall-k == loop, flagged} + {H-pin = the one λ-uniform edge of the pair: L1 = TAIL proven + H-pin open; WPD(a < γ₁²) ⟸ H-pin; the WPD non-λ legs: extension instantiated, TAILWPD world-front}. The two bughunt rounds (176 and 183) are process rounds and deliberately stay experiments-side.

An eighth promotion of record (2026-08-21): the seven durable certified theorem sets of the mechanism arc (discovery rounds 184–191, covered by the ninth adversarial bughunt with 0 verdict flips and the headline that **every claimed-exact object of the arc reproduced in fully independent code with zero failed recomputes**; its corrections of record K1–K5 adopted verbatim on every surface) have graduated as well — **the detector-specificity theorem** (`v942_pi_pattern_transfer_specificity.py`: the prime-front instruments transferred unchanged to pi-derived data land the expected honest null — all five pi worlds in the fake class, all 100 non-Mangoldt instances, zero anomalies — with transfer validity *proven* by the Mangoldt positive control firing on every detector: the mass-location separator is prime-specific, not a universal-structure detector; an instrument characterization, no pi-numerology claim), **the zero-coupled sharp localization** (`v943_mangoldt_ablation_localization.py`: the additive property ladder W0 random → W5 true Mangoldt switches both signatures on only at W5 and sharply — S1 jump 12.07 dex, S2 jump 61× — and **the Beurling answer is no**: full multiplicativity without Riemann-zero coupling produces neither signature; even ±0.01 position jitter kills both; the one 26/27 FAIL is a disclosed control-side density band, bughunt-adjudicated an honest Poisson fluctuation), **the exact residue identity** (`v944_ground_residue_observability.py`: Res_{z=τ} R_h(z) = A₀²/‖d‖² at all 14 rungs to ≤ 7.8×10⁻⁴⁵ with the Feshbach scalar form — w′(τ) eigenvector-free given τ — strict interlacing ⇔ A₀ ≠ 0 both directions, the honest headline gap-is-residue-in-disguise, and the bughunt-corrected off-diagonal-only Loewner law: the full wall is L_f + diag(Δ), *not* the canonical Loewner matrix), **the causal synthesis with its stratification** (`v945_zero_causal_stratification.py`, one joint module for rounds 187 + 190: comb worlds with *controlled* zero spectra synthesized through the mode-level bridge run backwards — the S2 signature moves causally with the zeros, monotone in the dose modulo quantified selection noise (the wiggle reads 1.07, inside every matched-dose spread), and **exact-ordinates-required**: Poisson and GUE resamples at the true density both die; a mechanism demonstration on controlled synthetic worlds — still no claim about where the true zeros lie), **the exact Krein pencil** (`v946_census_krein_pencil.py`: the census polynomial *is* the spectral determinant of a source-side pencil — but a Krein pencil at every true rung: the residue-sign ladder is mixed everywhere, so realness is *equivalent to Krein strong definitizability* rather than structural; mixedness is transform-invariant and atoms-vs-no-atoms — the wrong orientation for a sign source, honestly typed; the bracket certified without computing a single root), **the exact Loewner–Pick dictionary** (`v947_loewner_pick_dictionary.py`: the wall is a *diagonally-shifted* one-function Loewner matrix with the new exact cosine-quadrature diagonal law — both quadratures of the same atoms in different slots; the pole leg a genuine Pick function with an exact Herglotz point mass; the Pick-class reading refuted by 9.4–87.9 orders; the smooth world PSD at the nodes by exact commensurate sampling — lattice geometry, not a prime fingerprint), and **the sign-characteristic law** (`v948_krein_sign_characteristic.py`: the round-188 residue ladder *is* the Krein sign-characteristic multiset, two-route proven on an exact no-floats Sturm battery; the parity scaffold — root-count parity per pole gap from residue signs alone — is the new source-side coordinate; and the W(y) > 0 congruence upgrade path is **closed by theorem**: the sign characteristic is congruence-invariant, so the mixed ladder kills the entire class at once). The suite stands at 941 modules; all eight rows are typed NOT-RH-EVIDENCE — the census stays at cardinality 4, θ_∞ stays open, the λ_h limit stays open, and the arc's terminal residue is unchanged in the canonical form: **the triple {H1 AND H2 AND H3}-cofinal (mod D = 0.0042)** + {census-forall-k == loop, flagged} + {H-pin = the one λ-uniform edge of the pair: L1 = TAIL proven + H-pin open; WPD(a < γ₁²) ⟸ H-pin; the WPD non-λ legs: extension instantiated, TAILWPD world-front}. Round 192 (a commensurability-mechanism round with the program's first Baker-class pricing) and round 193 (the bughunt itself) deliberately stayed experiments-side at that date; round 192 has since been covered by the tenth bughunt and promoted in the ninth wave below.

A ninth promotion of record (2026-08-22): the six durable certified theorem sets of the Bughunt-X arc (discovery rounds 192 and 194–198, covered by the tenth adversarial bughunt with 0 verdict flips and the X4 headline that **every exact claim of the arc reproduced in fully own code** — the ACF/pole/arch laws to ≤ 10⁻⁶¹, the Fejér cap re-proved by an independent single-node dual route; its corrections of record KA/KB/KC/KE adopted verbatim on every surface, the KD positive pin note-only per precedent and recomputed in-module) have graduated as well — **the exact node mechanism** (`v949_commensurability_mechanism.py`: the smooth world's oscillation collapses at the nodes to Möbius data (closed forms recomputed in-run), the structural-zero census is the e | 2fk law in exact integers, and **the program's first Baker-class pricing** is recomputed from the published constants — LMN 1995 misses the measured two-log forms by 7.1×10³–3.1×10⁴ dex, Matveev 2000 by 1.4×10⁹–1.2×10¹⁰ dex: honest and honestly useless; the dose is direction-blind, the margin consumes position *exactness*, not incommensurability magnitude), **the zero-channel characterization** (`v950_zero_channel_capacity.py`: the write→read arc characterized as a channel — error-free full ternary capacity from ε = 0.01, resolution limit 0.001/0.003 with the bughunt's P = 0.0032 marginal-but-nonnull typing adopted, linearity scale 0.01, parity-erasure recovery 1.000 as a corollary of the zero-error cell; an instrument characterization, **no storage claim**), **the cancellation functional** (`v951_cancellation_functional.py`, the arc's most promotable round: **the wall is a closed truncated Weil-explicit-formula functional** of the PD test function g_x — the ACF law, the pole square and the arch law with c* = (γ + ln 2π)/2 all recomputed in-module at h = 4, 5, and **the arch kernel is the classical Weil archimedean kernel in the L → ∞ limit** (the KD handoff pin, verified 6×10⁻¹⁸; finite-L corrections typed not-small); per-atom manifest positivity provably impossible (Wiener–Khinchin); the pole alone carries positivity; priced honestly as an algebraic restatement — the relabeling barrier named, not crossed), **the Turán pricing** (`v952_turan_extremal.py`: the classical PD-cone theory priced for the first time — every cone bound negative at every rung while the truth is τ-deep positive (cone geometry cannot see positivity; the gap *is* the τ ladder, slope −1.007 at R² 1.000), yet the classical Fejér cap describes the wall's drain capacity almost exactly (0.14–0.24 dex flat); the Fejér coefficient theorem machine-proved with single-node dual certificates; the mode lattice attains the cap exactly at rational lags only), **the A_{v₀} ≥ 0 theorem** (`v953_fewatom_reduction.py`: the collapsing direction's mode polynomial is nonnegative on [0, L] at all 14 rungs — grid-measured per the bughunt's KA scope rider, and **continuum-certified by exact-rational Sturm chains** recomputed in-module at h = 4, 5 (integer primitive-PRS, no floats; the bughunt extends to h = 13, 20); the one-line mechanism: A ≥ 0 forces every atom negative — the sign law reduced; **Object-A** — "prove A_{v₀(h)} ≥ 0 for all h" — is the arc's one genuinely new tractable target, yielding the sign law only, no lever on wall positivity), and **the Perron–Frobenius adjudication** (`v954_nodeless_pf.py`: the candidate route for Object-A dead in the mode basis — off-diagonals mixed at every rung, the trivial route killed both ways by an exact exhibit; **the pole's rank-one positive kernel is the sole cone obstruction** — removing it repairs cone invariance: the piece that carries the positivity is the piece that breaks the cone; the attractive-hopping Z-structure survives in position coordinates on prime + arch; Object-A stays open, genuinely function-space). The suite stands at 947 modules; all six rows are typed NOT-RH-EVIDENCE — the census stays at cardinality 4, θ_∞ stays open, the λ_h limit stays open, and the arc's terminal residue is unchanged in the canonical form: **the triple {H1 AND H2 AND H3}-cofinal (mod D = 0.0042)** + {census-forall-k == loop, flagged} + {H-pin = the one λ-uniform edge of the pair: L1 = TAIL proven + H-pin open; WPD(a < γ₁²) ⟸ H-pin; the WPD non-λ legs: extension instantiated, TAILWPD world-front}. Rounds 193 and 199 (the bughunts themselves) are process rounds and stay experiments-side — round 199's Sturm-certificate deliverable is promoted inside `v953`.

A tenth promotion of record (2026-08-24): the seven durable certified theorem sets of the integrable-dictionary arc (discovery rounds 224–226 and 228–231; rounds 199–223 stay experiments-side as adjudication/classification rounds, and round 227, the Fermi-edge classification, stays experiments-side by design) have graduated as two modules — **the finite integrable dictionary** (`v955_tau_iiks_toda_dictionary.py`: **the tau identification executed at the finite-identity level** — the wall determinant *is* the exact IIKS Fredholm tau function (Sylvester as a full characteristic identity, the s-dressed Schur family with the fixed-kernel alias named and blocked, the confluent diagonal, the IIKS dressing identity with the dressed port generators source-explicit in closed form, the Fredholm/JMU variational identity in two independent times, and the relative tau transfer exact over −212.84/−195.50 log-unit collapses); **the Lax degree adjudicated** — the true minimal degree in the closing direction is *one* and the closing direction is h, not s, with the degree-1 connection blind-predictive (3.4×10⁻¹⁶ / 3.2×10⁻¹¹) and zero curvature world-blind; and **the signed-Toda dictionary** — τ = D_n(μ−ν)/D_n(μ) with the Hirota coefficient source-pure and its positivity *wall-equivalent*: quasi-definiteness of the signed defect measure μ−ν), and **the half-filling geometry** (`v956_signedmoment_halffilling_duality.py`: the window cap is exactly the half-filling of the union support, N_w = ⌈#supp(μ̃)/2⌉, and past the cap the wall dies immediately (first flip at N_w + 0/2/2/3/1) — **the wall is maximal**; **the free moment window law** — MAIN survives its *entire* free moment window while the controls die at 11–15% of it; **the four-path dictionary** — moment prefix = Jacobi chain = Euclidean chain = value chain, exact in rationals, with the dual measure carrying the *mirrored* Jacobi chain; and **the h-free L-gauge midpoint connection theorem** — the original and dual FIK problems are connected by a gauge built from the node polynomial alone, so the wall's orientation sits exclusively in the h-chain the gauge does not see). The suite stands at 949 modules; the claim splitting is carried honestly — PRIME.PORT.TAU.FINITE.IIKS.01 [E] and PRIME.FREEMOMENT.JFRACTION.01 [E] + PRIME.JACOBI.DUAL.REVERSAL.01 [E] are separated from the open sign contracts PRIME.PORT.TAU.NOPOLE.COFINAL.01 [O] and PRIME.FREEMOMENT.POSITIVEPREFIX.01 [O]; every wall statement is typed wall-equivalent and consumed nowhere; no RH claim in either direction.

Four concrete things we'd propose:

1. The lock as a joint target: a world-discriminating functional of the exactly constructed Euler phase (index-type functionals measured out; the measured first candidate is the grouping's parameter-free weight law) — equivalently a lower bound on c_h, against the +2.81 dex measured deficit, at ≤ 21 fixed sub-γ₁ frequencies — with the H(T) transfer law as the numerical yardstick any such bound must beat. All characterizations are frozen, machine-verified, and reproducible.
2. Your moment/inertia machinery on three concrete moment objects: our fixed 8-dim core at r = 4, where UB₄ sits at 0.83–1.60 and integrality closes everything below 1; the new one-bad-mode separator census — congruence-free, integer-exact, h-flat order price — which needs exactly the arithmetic supply that blocks everything else; and the **SOS theorem package** (machine-verifiable end-to-end — exact rational majorant coefficients, SOS Gram matrices, the symbolically verified relation chain, the honest coverage certificate, and the one-line theorem per certificate, with the tiny stdlib-only checker as the audit entry point: run it on the package JSON and you have re-proved the regional theorem from rational arithmetic alone — modulo the checker-audit caveat above, with the hardened v2 as the entry point), which is precisely a linear moment bound over a certified spectral interval and should be directly legible to your framework. The full certificate stack (composed wall census, the certified 2×10⁷-ordinate zero cache with disclosed builders, per-rung T_req/H(T) curves) comes as the benchmark.
3. The endform open problems, stated precisely (§8 of the summary), with the localization now final: **the open problem is one scalar anti-cancellation inequality** (one *inequality*; the chain's four remaining legs stay named classical citations, as above) — v₋ᵀXv₋ ≥ −λ_min(G₀), a prime-only quadratic form against a geometry-only number, relative margin O(10⁻⁴), alignment-carried (the comb wins by direction, not size; the decoupled split fails by 5%, the completed square is degenerate, the Mellin positive-energy form does not exist) — whose measured seat is the hard part: **the sign is decided only at resolution n ~ 2¹⁷, practically the whole window**, and Selberg — structurally a perfect fit — misses that seat by 4–5 orders at the min end (median miss ~9×10⁵, ≈6 orders), with the bit-identity A_err − B_err = P_err closing the entire rewrite-then-estimate class; any source of arithmetic information at that resolution is the collaboration question. Behind it, two secondary open objects stand untouched by the correction: the **last unproved deep-rate bound** — a proved lower bound for one measured scalar, t = ν₁/(ν₀‖B‖_F) > 4/5, carried by the AR–OSC cancellation, against a direct-reading margin of only +0.0104 that is itself an irreducible measurement (the Lipschitz constant is certified, the Rayleigh-deficit cap proved-conditional); and the **observer coupling** — a certified moment/determinant dual that excludes the phantom plateau and narrows the certified class window to [0.9727, 1.2402] but leaves the compression PARTIAL (the missing object is a certified non-product relation of the entry moments, or a sharper joint moment dual).
4. Comparing Lean formalization approaches — we maintain a Lean 4 tree (against Mathlib) with the extraction-chain hypotheses kernel-checked and the finite-head certificate composition in progress. The same-day hardening there is a good sample of the register we try to hold: the cofinal extraction is now kernel-checked for an *arbitrary fixed* index, and the accompanying formal result is a *negative* one — an extensional index cannot retain its provenance, so "the family was predefined, independently of the sign" stays an explicit external premise carried by a named contract, never a quantifier-order trick.

(One self-contained aside from the same round, on the physics side of the program: the compiler's seam-wiring question closed as a clean freedom theorem, now gauge-audited by exact computation — the compiler determines a single admissible wiring orbit, with the deployed representative a deployment choice rather than a compiler theorem; the reflection-positivity Gram forms transform covariantly under the rule gauge, so the only gauge-invariant content of (frame, wiring) is their relative angle, and a canonical selection would require an additionally derived physical frame.)

I attach a self-contained summary (proven / registered / open, with module identifiers and the full dictionary algebra); everything is reproducible from the public suite, and I'd be glad to share data, registries, and code in whatever form is useful.

With appreciation for your work — the formalization especially,

Stefan Hamann
TFPT project — [fixpoint-theory.com/prime-front](https://fixpoint-theory.com/prime-front)

*Attachment: paper_endform_and_inertia_bridge_en.md (research summary, 2026-08-11, updated 2026-08-15)*
